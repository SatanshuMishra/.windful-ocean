{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "no realm construction is touched. I ran `git diff --name-only e9306e6..HEAD | grep -E 'hooks/|sandbox'` myself and it matched nothing across the 15 changed paths; workflow-sandbox.mjs and everything under .claude/hooks/ are absent from the diff. I read the one new module, ci-escalation.mjs, in full at HEAD (153 lines): it contains no vm call, no DONT_CONTEXTIFY reference, no global-object construction, and its only two imports are sibling lib modules (run-engine.mjs at :1, recovery.mjs at :2). The mitosis.js additions operate on run-local values only - a CI report object, a triedSet array, a manifest and a path list. Nothing added reaches for a host-realm intrinsic or a host prototype chain."
    },
    {
      "id": "B2",
      "verdict": "threatened",
      "check": "engaged in both directions and the net is still a threat, so both halves are stated. IMPROVED: the M7 residual (b) this MSP owns is fixed - the whole-batch divergence fold now emits exactly one operator line naming the fold and its target count, which I read at divergence.mjs:63 and at its twin mitosis.js:3371, and the checkpointRef failure now logs at divergence.mjs:44 instead of folding silently. DEGRADED: the new CI classifier adds bare catches that bind no error at all - ci-escalation.mjs:134 (twin reason string at mitosis.js:2437) and ci-escalation.mjs:70 inside ciScopeViolations. A tagged SandboxViolationError raised inside scopeCovers or sensitiveScope would be swallowed there and reported to the operator as 'a declared fileScope entry could not be evaluated as a path pattern', which is a different and wrong diagnosis. The safety direction is right (both catches fail closed to a class-0 park), but the observability half of this invariant is degraded on the new path, and the agent-throw catch at divergence.mjs:57 is still bare. Not remediated in this MSP."
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: I did not open workflow-sandbox-policy.test.mjs and I did not read the guarded-intrinsic enumeration row by row. What I did establish by reading ci-escalation.mjs in full at HEAD is that the added code introduces no new intrinsic member surface. Its member calls are Array.isArray, Object.freeze, Set, Map, JSON.stringify, String, Math.imul, RegExp.test, toString/padStart/toLowerCase/includes/slice/filter/some/every/startsWith/replace - all already executed elsewhere in this engine before this change. It adds no timer, no Date read, no Reflect or Proxy use, and no dynamic property access on a host object. The receipt that no guarded member newly denies is CARRIED from the independent receipts agent (npm test 1887 tests / 1886 pass / 1 fail, the single failure being the environment-only protect-claude-config test). I ran no test."
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "I ran `git diff e9306e6..HEAD | grep -cE 'VALUE_GLOBALS|ALWAYS_DENIED|ALLOWED_GLOBALS'` myself and it returned 0: none of the three policy constants is added, removed, or even mentioned anywhere in the 1825-insertion diff, and the files that hold them are not among the 15 changed paths. The new module declares ten constants of its own, which I enumerated by reading ci-escalation.mjs:4-13 (CI_ATTEMPT_CAP, CI_PUBLISHED_TOKEN, CI_PROBE_TOKEN, CI_FIX_PREFIX, CI_SHA_PATTERN, CI_REASON_LIST_CAP, CI_TERMINAL_CONCLUSIONS, CI_ENFORCER_CHECK_TOKENS, CI_SECURITY_CHECK_TOKENS, CI_ORDINARY_CHECK_TOKENS); all ten are CI-loop policy, none is sandbox policy. No assertion whose redness depends on a sandbox policy constant moves."
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "re-derived rather than defaulted, because this MSP enlarges mitosis.js's identifier surface more than any predecessor. I enumerated the added top-level declarations myself from `git diff e9306e6..HEAD -- .claude/workflows/mitosis.js | grep -E '^\\+(const|function|async function|let|var) '`: 36 added (CI_ATTEMPT_CAP, CI_PUBLISHED_TOKEN, CI_PROBE_TOKEN, CI_FIX_PREFIX, CI_SHA_PATTERN, CI_REASON_LIST_CAP, four token lists, three CI schemas, classifyCiReport, assertionGuardBlocks, sensitivePathsTouched, ciAttemptDelta, applyCiAttemptTransition, persistCiAttemptCheckpoint, isCiRedExhausted, CI_RED_EXHAUSTED_KIND, CI_HUMAN_GATE_KIND, divergenceToken and the ci* helpers) and exactly one removed and re-added (IDENTITY_OVERLAY_FIELDS, which gains 'ciAttempts'). Every added name is a const or a function declaration, so each BINDS rather than leaving a free identifier. The inline twin's four imported dependencies resolve to names already bound in the workflow body, which I confirmed at HEAD by grep: isRepoRelativePath mitosis.js:555, normalizePath :1019, scopeCovers :1058, sensitiveScope :1138 - none of which is in the added-declarations list, so all four pre-date this change. The census rows in workflow-sandbox-census.test.mjs were not read one by one by me; the receipt that the census still closes is the CARRIED 1886-pass suite."
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "engaged, and this MSP widens the defect rather than inheriting it quietly. ci-escalation.mjs declares 19 exports (read at HEAD: ten constants at :4-13 plus ciCleanList :54, ciScopeViolations :64, ciFailureFingerprint :75, ciAttemptsSpent :94, ciHeadPublished :99, ciProbeConsumed :103, classifyCiReport :107, assertionGuardBlocks :140, sensitivePathsTouched :149). NOT ONE has a production importer: I ran `grep -rn ci-escalation .claude --include=*.mjs --include=*.js -l` myself and the only two files that name it are tests - mirror-guard.test.mjs and ci-escalation.test.mjs. Production runs the byte-identical inline twin inside mitosis.js (classifyCiReport at :2410-ish region, the class-1 predicate at :2430, assertionGuardBlocks at :2442, consumed at :5263 and :5307). dead-export-lint computes liveness by raw identifier match across mitosis.js plus siblings, so the twin's own copied text makes all 19 look live with zero real importers - exactly the self-referential lint artifact this invariant forbids. Not invented here: mitosis.js runs sandboxed with import and require denied, so a production importer is impossible by construction. Same reading recorded for A7 and M7; this entry is larger because a whole new 153-line WHOLE twin now sits under that condition. The dead-export-lint green (3 tests, 3 pass) is CARRIED from the independent receipts agent, who recorded the same caveat; I did not run it."
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this entry IS the M1 mechanism - all 12 registry ids carry a verdict here, once each, in a committed artifact rather than in the PR body. The standing threat, named rather than assumed away: scripts/invariant-coverage-check.mjs validates row shape (:106-117), id set-equality against the registry (:127-137) and the two-value verdict domain (:9), and it NEVER checks whether a check string is true, so nothing mechanical stands behind these words. That is at its sharpest here, because I am the ship agent and I was fenced out of execution: EVERY test, gate and mutation result cited in this file is CARRIED from the independent receipts agent at scratchpad/m8/receipts.md, never measured by me. What I executed myself and am accountable for: the 15-path diff enumeration, the added/removed top-level declaration lists, the four greps recorded in B1/B4/B5/B6, a full read of ci-escalation.mjs and divergence.mjs at HEAD, the MIRROR_CENSUS row count (40, computed with node over the frozen object literal), `git ls-tree -r --name-only _ledger` to confirm decisions 0067, 0087, 0151, 0209, 0210 and 0212 all exist, the `git check-ignore` measurement that corrected a carried claim (see M5), and this gate."
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "this MSP adds a brand-new classifier of check names and paths, so M2 is the most directly engaged structural id and the honest verdict is a partial. CLOSED where it counts, verified by reading rather than carried: ciUnclassifiableChecks (ci-escalation.mjs:34-40) computes the complement of the union of all three token lists, and classifyCiReport halts on it at :124-125 with a class-0 escalate, so a check name the loop cannot positively classify parks instead of falling through to a fix; ciCanonicalPaths (:42-52) returns null on any non-repo-relative spelling and each of the three path fields halts on it at :119, :121 and :123; CI_TERMINAL_CONCLUSIONS is a closed set whose complement halts at :111. No pinned count appears in the new gate. THREATENED, and this one I found myself by reading the token lists rather than by carrying a receipt: class 4 is decided by substring match against CI_SECURITY_CHECK_TOKENS (:131), while class 0's closure only requires a name to match SOME token in the union. A check named for example 'integration-fuzz' matches the ordinary token 'integration', so it is classifiable, is not security-classed, and is admitted to a fix attempt - a security-adjacent check escaping class 4 because an ordinary token appears in its name. That is the sampled-allowlist shape M2 forbids. Mitigated, not cured, by the assertion guard and scope containment still applying to any resulting fix. Not remediated in this MSP."
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "the inertness obligation was met for two assertions and only two, and every proof of it is carried. CARRIED from the independent receipts agent, who names the mutation and the restore: (1) disabling the published-head guard at mitosis.js:4784 by rewriting `if (ciHeadPublished(shipTriedSeed)) {` to `if (false && ...)` turned CI-CAP-PERSIST from 1 pass / 0 fail to 0 pass / 1 fail, reddening the load-bearing assertion itself ('the relaunch spends STRICTLY ZERO further attempts on the already-published head'), which I re-derived at mitosis-scheduler.test.mjs:5157 under the test at :5126; (2) widening the class-1 trigger to `foreignPaths.length > 99` in BOTH mirror halves (ci-escalation.mjs:128 and mitosis.js:2430, both anchors re-derived by me) reddened both layers, the integration one on the assertion 'class 1: parks WITHOUT attempting any fix, probe, diff or publish'. I ran neither mutation and neither restore. The gaps I will not paper over: escalation classes 2, 3, 4 and 5 have deny-case tests, which I re-derived at ci-escalation.test.mjs:48, :56, :64 and :75, but NO inertness mutation was run against any of them, so their non-inertness is unproven; and this is a feature MSP whose new behavior did not exist to be reproduced by a red test first, so the red-before-green half of M3 is satisfied only for the two probed assertions and is unestablished for the rest."
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "a violation with mitigations rather than a pass, and the honest reason is size plus bundling. This one reviewable range carries 1825 insertions across 15 files (my own `git diff --shortstat e9306e6..HEAD`), roughly 704 of them production (ci-escalation.mjs 153, mitosis.js 505, run-log.mjs 25, merge-policy.mjs 10, divergence.mjs 9, recovery.mjs 2) against a house target of 200-400 LOC per reviewable change. It also bundles two distinct behavior changes that share no subsystem: the M8 CI-to-green loop, and the two M7 divergence residuals decision 0212 assigned here (entry-to-endpoint binding via checkedBuiltSha/checkedMergedSha, which I read at divergence.mjs:70 and mitosis.js:3378, and the whole-batch fold log line at divergence.mjs:63). Against M4's escape clause, no characterization test preceded this range; the one characterization file in the diff, reconcile-only-advance-characterization.test.mjs, changes by exactly one line (I read the hunk: its agent double now echoes checkedBuiltSha and checkedMergedSha) - that is a fixture update forced by the new required schema fields, not an assertion weakened, but it is also not an independent pin written beforehand. Mitigation that does exist, named as a mitigation and not as compliance: no structural refactor of existing code is mixed in - IDENTITY_OVERLAY_FIELDS is the only pre-existing declaration modified, and it is a one-element addition."
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "every path:line in this file was re-derived by me against HEAD b8451e9 today with grep or Read, and not one was copied from the spec, the plan, the implementation report or the receipts report. Re-derived here: ci-escalation.mjs :1 :2 :4-13 :34-40 :42-52 :54 :64 :70 :75 :94 :99 :103 :107 :111 :119 :121 :123 :124-125 :128 :131 :134 :140 :149; mitosis.js :555 :1019 :1058 :1138 :2366 :2430 :2437 :2442 :3371 :3378 :4784 :5263 :5307; divergence.mjs :44 :57 :63 :70; mirror-guard.test.mjs :28; mitosis-scheduler.test.mjs :4696 :5126 :5157; ci-escalation.test.mjs :41 :48 :56 :64 :75 :120; invariant-coverage-check.mjs :9 :106-117 :127-137. Marked threatened rather than clean because a live copied-claim failure surfaced on this MSP and is recorded rather than buried: both the spec (section 11) and the receipts agent's residual list assert that '.mitosis/ is gitignored', and that is FALSE at HEAD. I measured it: `git check-ignore -v .mitosis/run.json` exits 1 in both the worktree and the main worktree, `grep -n mitosis .gitignore` matches nothing, and `git ls-tree -r --name-only HEAD | grep -c '^.mitosis/'` returns 0. The journal is UNTRACKED and simply never committed, not ignored - the durability conclusion that a fresh clone restores no ciAttempts survives, but the stated mechanism did not, and it had been carried unexamined through a spec and a receipt. Also disclosed: scratchpad/m8/implementation.md cross-references five receipt ids that do not match its own tables; it is untracked scratchpad, and I did not edit it."
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "this diff builds no artifact, patches no deployed file, and creates no cache copy or vendored duplicate of any gate. I ran `git diff --name-only e9306e6..HEAD | grep -E 'hooks/|sandbox|registry.json|invariant-coverage|receipts.yml'` myself and it matched NOTHING, so scripts/invariant-coverage-check.mjs, docs/invariants/registry.json and the receipts CI template are all untouched and no deployed enforcer is refreshed or regenerated here. Both copies of the CI-escalation logic are committed source - ci-escalation.mjs and the inline twin in mitosis.js - and byte identity is enforced by mirror-guard, whose census I read directly: the new row `'ci-escalation.mjs': WHOLE` sits at mirror-guard.test.mjs:28 among 40 rows (count computed by me with node over the frozen literal). The 47/47 mirror-guard pass is CARRIED from the independent receipts agent; I did not run it. The tension I name rather than assume away, unchanged from A7 and M7: mitosis.js is not a versioned BUILD of the lib modules, it is a hand-maintained copy, and this MSP hand-installs a 153-line twin. What keeps it on the right side of the invariant is that no generator exists whose reproduction could diverge, and mirror-guard fails on any one-sided edit. If a generator is ever introduced for mitosis.js, this row must be revisited."
    }
  ]
}
