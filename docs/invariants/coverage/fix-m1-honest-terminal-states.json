{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "no realm construction is touched: the diff changes computeMergePolicyStatus in merge-policy.mjs and its inline twin, onMerged and the end-of-run classifier in mitosis.js, plus two test files. workflow-sandbox.mjs is absent from git diff --name-only against origin/main, and no vm call or DONT_CONTEXTIFY reference is added, removed or reached"
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no sandbox denial path is added, removed or reached; the change raises nothing and catches nothing, so no mechanism gains an untagged host error route"
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not read row by row for this diff. What was checked is that the added member accesses are Set.prototype.has/delete on the run-local blockedByApproval set and Array iteration over the run-local awaitingApproval array, both engine-local values constructed inside the workflow body, not guarded host intrinsics. The full suite, which carries the guarded-intrinsic assertions, ran 1766 pass / 0 fail on this branch"
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS are neither read nor written by any file in this diff, and no assertion depending on them moves"
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "mitosis.js IS the workflow-body position and this diff adds a named function declaration there (reportOnlyResumePoint) plus a new destructured parameter (ciRedExhaustedCount) and a new local in onMerged, so this was checked by execution rather than assumed: a function declaration binds a name inside the workflow body and is not a free identifier reaching the host, and no new bare global reference is introduced. The receipt is that the full suite, which carries the closed census and the sandbox compile gate, ran 1766 pass / 0 fail on this branch"
    },
    {
      "id": "B6",
      "verdict": "not-threatened",
      "check": "merge-policy.mjs is one of the .mjs files dead-export-lint.test.mjs enumerates, so this was checked: the diff adds no export and removes none. computeMergePolicyStatus was already exported and keeps its real production caller at mitosis.js computeParkedStatus, which assembleReport calls on every run-completing path. reportOnlyResumePoint is an unexported inline helper in the workflow body, outside the lint's export surface. The lint ran inside the 1766 pass / 0 fail full suite"
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "candid reading per 0147: this entry is itself the M1 mechanism, so the change cannot be said to leave it unexercised. All 12 registry ids carry a verdict here, in a committed artifact and not in the PR body. The residual M1 threat is unchanged by this diff and is that the entry is orchestrator-authored prose: the gate checks the shape of every row, never the truth of any check string"
    },
    {
      "id": "M2",
      "verdict": "not-threatened",
      "check": "this diff adds no gate that classifies tokens, identifiers or paths, pins no count and samples no allowlist. computeMergePolicyStatus classifies run outcomes, not tokens, and it is total: every input falls through to the partial residual rather than halting or returning undefined, so it opens no silent-acceptance hole. scripts/invariant-coverage-check.mjs and the phase-parity gate are untouched"
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "M3 is the invariant this diff most engages. Red on the parent was proven by ORCHESTRATOR EXECUTION, not relayed: with merge-policy.mjs and mitosis.js reverted to 60f4217 and the new tests kept, the three affected files ran 218 tests, 195 pass, 23 fail, exit 1; restored to the fix they are inside a full suite of 1766 pass / 0 fail. That revert is the inertness mutation M3 requires. Stated candidly, one receipt is weaker than the brief that commissioned it: the CHANGE B acceptance test asserts the released ids named in the onMerged log line, NOT a false blocked-pending-approval park record, because the implementer reported that the report-level symptom is unreachable through today's classifier (a non-terminal unit always retains a live blocker) across 16 DAG and merge configurations. The orchestrator did NOT independently reproduce that 16-configuration probe. So the prune is landed on a real staleness of the set and on a log line that previously claimed an unblocking it did not perform, and the report-level symptom it would guard remains unproven in either direction"
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "engaged rather than incidental, and reported as threatened rather than argued away. This MSP ships three distinct behavior changes in one reviewable range (the terminal-status rewrite, the approval-set prune, the report-only resumePoint) because spec section 10 defines M1 as exactly that bundle; they are not independently shippable as smaller MSPs without one of them leaving a status vocabulary half-migrated. The diff also extracts one structural helper, reportOnlyResumePoint, which shares its range with the behavior change it serves. Mitigation, not a defence: production change is 24 lines across two files, the three changes touch disjoint functions, and no line is moved, renamed or reorganized elsewhere"
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "anchor-dense against a spec known stale, so M5 is engaged. Every path:line in the dispatch, this entry and the PR body was re-derived by execution against origin/main at 60f4217 before use, never copied from the spec: computeMergePolicyStatus at merge-policy.mjs:23 and its twin at mitosis.js:3450, computeParkedStatus at mitosis.js:3122, onMerged at mitosis.js:4875, the end-of-run classifier at mitosis.js:4906-4923, the relaunch guard at mitosis.js:3784, the twin list at mirror-guard.test.mjs:19. Per 0148 the spec's section 6 anchors resolve at 450804e and were not used. The re-derivation refuted two spec claims: section 6 calls markAwaitingMerge zero-consumer when it has four call sites (leases.mjs:204,278 and mitosis.js:2124,2198) and roughly fourteen disposition assertions in leases.test.mjs, and section 11 calls progressPossible test-free when leases.test.mjs imports it at :16 and asserts it at :284, :290, :297 and :303. Both deletions were therefore excluded from M1 and left to M3/M5, which section 10's table already assigns them to. Confirming 0151 for a second consecutive MSP, section 10's mirror-twin gotcha list names only M3, M4 and M7 and omits that merge-policy.mjs is a policed twin"
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "this diff builds no artifact, patches no deployed file and creates no cache copy or vendored duplicate of any gate. Both copies of computeMergePolicyStatus are committed source and their byte identity is enforced by mirror-guard.test.mjs, which ran 22 pass / 0 fail inside the full suite on this branch. The 0132 hand-patch window belongs to Track A and is untouched here"
    }
  ]
}
