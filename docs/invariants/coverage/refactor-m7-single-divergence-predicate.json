{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "no realm construction is touched. I ran `git diff --name-only a618338..HEAD | grep -E 'hooks/|sandbox'` and it returned nothing: workflow-sandbox.mjs and every file under .claude/hooks/ are absent from the nine changed paths. divergence.mjs was read in full at HEAD (68 lines) and contains no vm call, no DONT_CONTEXTIFY reference and no global-object construction. The mitosis.js hunks operate exclusively on run-local values - a manifest, a merged-id list, a merged-sha map, and a ctx object literal at :3923 whose four members are names already bound in the workflow body. Nothing added reaches for a host-realm intrinsic or a host prototype chain"
    },
    {
      "id": "B2",
      "verdict": "threatened",
      "check": "engaged, and this diff degrades it rather than merely carrying it. At a618338 the dispatch path had ONE catch that reported: mitosis.js:3925 `log(\\`mitosis: reconcile - divergence-probe dispatch threw (${clean(err.message)}); ...\\`)`. I confirmed by grep that this line exists at a618338 and is ABSENT at HEAD. It is replaced by two BARE catches inside the new predicate - divergence.mjs:39 / mitosis.js:3131 on the checkpointRef throw, and divergence.mjs:52 / mitosis.js:3144 on the agent throw - neither of which binds the error at all. A tagged SandboxViolationError raised inside the agent hook is therefore now discarded entirely, where previously its message reached the operator log. The sandbox's own raising half of the invariant is untouched (no sandbox file is in the diff); the observability half is degraded on exactly the fold path that resets built work. Both reviewing agents flagged this independently (review 1 LOW, review 2 MEDIUM), and the implementation plan's own followUp 2 names it as a knowingly accepted trade-off. Not remediated in this MSP"
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was NOT read row by row for this diff and workflow-sandbox-policy.test.mjs was not opened by me. What I did establish is that the added code introduces no new intrinsic member surface. The four names mitosis.js gains are all top-level declarations - DIVERGENCE_CHECK_SCHEMA :1680, divergenceCheckPrompt :3082, needKeyedParents :3098, divergedParents :3113 - enumerated by me from `git diff a618338..HEAD -- .claude/workflows/mitosis.js | grep -E '^\\+(const|function|async function|let|var) '` and re-confirmed at those lines by grep at HEAD. The member calls inside them (Array.isArray, Map, Set, RegExp.test, filter/some/every/startsWith, JSON.stringify) were all already executed by the code this replaces. The receipt that no guarded member newly denies is CARRIED from the independent receipts agent: npm test 1836 tests / 1835 pass / 1 fail, the one failure being the environment-only protect-claude-config test. I did not run the suite"
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "I ran `git diff a618338..HEAD | grep -E 'VALUE_GLOBALS|ALWAYS_DENIED|ALLOWED_GLOBALS'` and it returned nothing: none of the three policy constants is added, removed, or even referenced anywhere in the nine-file diff, and the files that hold them (workflow-sandbox.mjs and its policy and census tests) are not among the changed paths. divergence.mjs declares exactly one constant, SHA_HEX_PATTERN at :4, which is a sha-shape regex and not sandbox policy. No assertion whose redness depends on a policy constant moves"
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "re-derived rather than defaulted, because M7 changes mitosis.js's identifier surface in both directions. Removed top-level declarations: DIVERGENCE_PROBE_SCHEMA, descendantsToInvalidate, assembleDivergenceVerdicts, divergenceProbePrompt, runDivergenceProbes. Added: DIVERGENCE_CHECK_SCHEMA, divergenceCheckPrompt, needKeyedParents, divergedParents. I enumerated both lists myself from the diff rather than from the plan. Every one of the four added names is a BOUND name - a const or a function declaration - so none leaves a free identifier to resolve against the host, and a parameter or destructure likewise binds rather than frees. The only free identifier the new dispatch consumes is `agent` at mitosis.js:3140, which already resolved at that position before this change. The identifier surface therefore loses five classified names and gains no unclassified one. Candidly: the census rows in workflow-sandbox-census.test.mjs were not read one by one by me; the receipt that the census still closes is the CARRIED 1835-pass suite from the independent receipts agent"
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "engaged structurally, and M7 re-founds the defect rather than fixing it. divergence.mjs's two exports, needKeyedParents and divergedParents, are imported by NO production module: production runs the byte-identical inline twin at mitosis.js:3098-3167 from the single call site at :3923, which binds the twin's declarations, not the module's exports. dead-export-lint.test.mjs computes liveness as raw identifier matches across mitosis.js plus sibling modules, so the twin's own copied text makes both exports look live with zero real importers - exactly the self-referential lint artifact this invariant forbids. This is not a defect M7 invents: mitosis.js runs in a sandbox that denies import and require, so a production importer is impossible by construction. But M7 deletes the old export pair and installs a new one under the same condition, so the row is named rather than inherited quietly. Same shape as the B6 row shipped for A7 in refactor-divergence-facts.json. The dead-export-lint green (3 tests, 3 pass) is CARRIED from the implementing agent's receipt 10; I did not run it"
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this entry IS the M1 mechanism - all 12 registry ids carry a verdict here, once each, in a committed artifact rather than in the PR body. The standing threat, named rather than assumed away: scripts/invariant-coverage-check.mjs validates row shape (:106-117), id set-equality against the registry (:127-137) and the verdict domain, and NEVER checks whether a check string is true, so nothing mechanical stands behind these words. That is sharper here than for any predecessor entry, because I am the ship agent and I was fenced from re-running receipts: EVERY test, gate and mutation result cited in this file is CARRIED, not measured by me. Sources named - the implementing agent (21 receipts, including the inertness mutation and its verified restore), the independent post-remediation receipts agent (npm test, phase-parity, mirror-guard, git state), and two reviewing agents who were themselves fenced from re-running. What I executed myself: the git diff enumeration of the nine changed paths, the four greps recorded in rows B1/B2/B4/B5, anchor re-derivation at HEAD for every path:line in this file, `git ls-tree -r _ledger --name-only` to settle the decision-0207 dispute, `gh api repos/SatanshuMishra/.windful-ocean/branches/main/protection` which returned 404 Branch not protected, and this gate in pull_request mode"
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged in one direction and clean in the other, so both are stated. Clean: M7 adds no lib module, so no MIRROR_CENSUS row changes - divergence.mjs (:29), parking.mjs (:36) and reconcile.mjs (:39) all remain WHOLE, which I confirmed by reading mirror-guard.test.mjs directly, and the guard reports 46/46 per the independent receipts agent. phase-parity still enumerates 13 phases with dead 0, so the 'Resume' token the deleted per-parent dispatch carried is still live at mitosis.js:3142 - also CARRIED, not run by me. Threatened: implementation deviation 3 NARROWED one e2e assertion from a token-level enumeration over every dispatched prompt (the plan's 'the :(exclude)* token appears in no dispatched prompt') down to a single label check, `assert.ok(!labels.includes('divergence-check'))` at frontier-train-e2e.test.mjs:737. The narrowing was factually forced - that token legitimately appears in the manifest-publish prompt, which serializes fileScope by design - but the replacement closes by fixture shape rather than by enumeration, since pm is the only need-keyed parent in that fixture. Review 2 flagged it LOW and named the strictly stronger enumerating form already in use twelve lines earlier at :708-710. Not remediated"
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "the inertness obligation was nominally met and every one of its proofs is carried. The implementing agent reports (receipt 11) that inverting the missing-entry fold - changing `if (matches.length !== 1) { diverged.add(target.parentId); continue; }` to `{ continue; }` in BOTH mirror halves - reddened exactly 3 tests: 'divergedParents: never returns an id outside needKeyedParents' (divergence.test.mjs:114), the FAIL-CLOSED matrix (:160), and the partial-batch test; counts under mutation 1836/1832/4. Receipt 12 reports the restore verified by grep and the suite back to 1836/1835/1. I ran neither. Two further gaps I will not paper over. First, the pre-implementation red (receipt 3) was a module-load SyntaxError for a missing export, which proves nothing about behavior. Second, the load-bearing assertion added in remediation - the deepEqual over the whole dispatched target array at divergence.test.mjs:84 - was authored AFTER the code, and the independent receipts agent explicitly DECLINED to re-verify its three claimed fail-open red states, recording them as the commit author's claims rather than its measurement. The strongest pin on payload composition therefore rests on an unverified red"
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "the most engaged id in this diff, and the honest verdict is a violation with mitigations rather than a pass. The commit type is `refactor`, but the same reviewable range carries behavior changes that review 2 enumerated: planReconcile's input contract shifts (called without live.divergedParents it now invalidates nothing where it previously parked every need-keyed parent), dispatch cardinality goes from N calls labelled 'divergence-probe:<id>' to one labelled 'divergence-check', and the operator log line at a618338 mitosis.js:3925 is removed with no replacement - I confirmed that line's presence at the base and its absence at HEAD by grep myself. Against M4's escape clause, a characterization test did NOT precede the refactor: reconcile-only-advance-characterization.test.mjs was itself re-plumbed inside this diff rather than left standing as an independent pin, and the one assertion that would have caught the primary fail-open - the binding of each dispatched target to its own builtSha and mergedSha - was authored in remediation commit e662e20, after the fact, in response to review. Mitigations that do exist, named as mitigations and not as compliance: the boundary shift is pinned by a dedicated test at reconcile.test.mjs:167 rather than left implicit, and the fail-closed direction is pinned by the 27-row matrix at divergence.test.mjs:160"
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "anchor-dense and the anchors provably moved, so every path:line in this file was re-derived by me against HEAD (e662e20) today and none was copied from the plan, the reviews or the prior artifact. Re-derived: mitosis.js DIVERGENCE_CHECK_SCHEMA :1680, divergenceCheckPrompt :3082, needKeyedParents :3098, divergedParents :3113, bare catches :3131 and :3144, dispatch :3140-3142, sole call site :3923; divergence.mjs SHA_HEX_PATTERN :4, needKeyedParents :6, divergedParents :21, catches :39 and :52, batch parser :55-65, total filter :67; mirror-guard.test.mjs :29 :36 :39; divergence.test.mjs :84 :114 :160; reconcile.test.mjs :167; frontier-train-e2e.test.mjs :708-710 :717 :737. A live M5-class failure occurred on this MSP and is recorded here rather than buried: the plan-audit agent read a STALE copy of the decision ledger under .claude/plugins/data/session-continuity-inline/ and concluded from it that Decision 0207 does not exist and that the plan's citation must be rewritten to 0172, raising that as its single BLOCKING finding. It is refuted - I listed the live ledger ref myself with `git ls-tree -r _ledger --name-only`, which shows decisions/0207-m4-relands-on-main-by-cherry-pick-after-a-split-merge.md alongside 0208. The plan's 0207 citation is correct and is what the PR body carries. Candidly: the audit artifact itself was not re-baselined, so its incorrect correction survives in the recovered 05-result-5.json for the next reader"
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "this diff builds no artifact, patches no deployed file and creates no cache copy or vendored duplicate of any gate. scripts/invariant-coverage-check.mjs, docs/invariants/registry.json and the receipts CI template are all absent from the nine changed paths, which I confirmed by grep over `git diff --name-only a618338..HEAD`, so no deployed enforcer is refreshed or regenerated by this change. Both copies of the divergence logic are committed source - divergence.mjs and the inline twin at mitosis.js:3098-3167 - and their byte identity is enforced by mirror-guard, reported 46/46 by the independent receipts agent (CARRIED; I did not run it). The tension I will name rather than assume away: mitosis.js is not a versioned BUILD of the lib modules, it is a hand-maintained copy of them, and M7 edited three WHOLE twins by hand in a single commit. What keeps this on the right side of the invariant is that there is no generator whose reproduction could diverge, and mirror-guard fails on any one-sided edit - the same reading recorded for A7 in refactor-divergence-facts.json. If a generator is ever introduced for mitosis.js, this row's verdict must be revisited"
    }
  ]
}
