{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "no realm is constructed, removed or reached. The change set is exactly 8 paths (git diff --name-only 7b34a61..fec38ac) and workflow-sandbox.mjs is not among them: git diff 7b34a61..fec38ac -- .claude/lib/superpowers-parallel/workflow-sandbox.mjs .claude/lib/superpowers-parallel/mitosis-gate.mjs emits 0 lines. Measured over the diff text rather than assumed: added lines matching vm., DONT_CONTEXTIFY or createSandbox number 0, and removed lines matching the same number 0. The engine hunks this change does carry are deletions of scheduler and shepherd functions plus three added workflow-body declarations (mitosis.js:3976, :4185, :4198) operating on run-local values — a manifest msp table, a Map, a shipped array, a detail string. Nothing reaches a host-realm intrinsic or a host prototype chain. Positive receipt rather than an absence-only one: the sandbox harness was executed against this very engine, mitosis-gate.mjs phase-parity compiling .claude/workflows/mitosis.js under compileWorkflow, exit 0."
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no sandbox mechanism is touched, and the catch surface this change moves is counted rather than described. Removed: 10 catch-bearing lines, of which 2 are the fully-silent swallow catch { return { id, result: null }; } inside the deleted streaming scheduler (one in leases.mjs, one in its mitosis.js twin) and 8 are named catch (err) handlers inside the deleted runReconcileOnlyAdvance. Added: exactly 1, the catch (err) of the ship-delta memo lifted into the reconcile stage at mitosis.js:3983-3985, whose body is a log line copied from the shepherd handler it replaces. That one handler does convert a throw from persistShipCheckpoint into a log line, so a tagged SandboxViolationError raised beneath it would reach the log rather than surviving as a tag — identical in kind to the handler it replaces and to the engine's standing guarded-write pattern, so the surface is relocated, not widened. Net movement is toward B2: the two removed swallows discarded a rejected dispatch outright with no log at all. No denial path gains an untagged host-error route: grep over the diff finds SandboxViolationError added 0 times and removed 0 times, and workflow-sandbox.mjs carries 0 diff lines. Receipt: the four sandbox test files run directly report 129 tests, 129 pass, 0 fail."
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not read member by member, and no emptying mutation was run against a guarded intrinsic for this entry. What was measured is the member surface the diff actually moves, which is a net deletion — 250 removed against 37 added in mitosis.js, 84 removed against 1 added in leases.mjs, 9 removed against 0 added in reconcile.mjs (git diff --numstat) — and every added expression is Map/Set/Array.isArray/String template composition already pervasive in the engine before it. No policy list gains or loses a member: workflow-sandbox.mjs is absent from the change set. The receipts are executions performed here, not carried: the four workflow-sandbox test files report 129 pass / 0 fail on their own, the full suite reports 1838 tests / 1838 pass / 0 fail, and the harness's own production caller compiles the edited engine at exit 0."
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "checked by grep rather than assumed: VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS each appear 0 times in the added lines of this diff and 0 times in the removed lines. All three are declared only in workflow-sandbox.mjs (:18, :23, :25), which carries 0 diff lines, and are asserted only in that module's test files, none of which is among the 8 changed paths. No constant this change deletes is a sandbox policy constant: the four deleted lib members are leases.mjs mayRestack and STREAMING_DISPATCH_ENABLED and reconcile.mjs shouldReconcileOnly and hasBuildableWork, all scheduler predicates. Named candidly rather than implied: this entry ran no emptying mutation against any of the three policy constants, so their load-bearingness is proven exactly where it was proven before and nowhere newly here; the receipt that those proofs still stand is workflow-sandbox-policy.test.mjs and its three siblings at 129 pass / 0 fail, run by this author on this tree."
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "re-derived rather than defaulted, because mitosis.js IS the workflow-body position and this change rewrites 287 of its lines. Three identifiers newly enter the workflow body and every one is BOUND there by the same change: reconciledShippedSoFar (declared mitosis.js:4185), prepareHaltReport (:4198), and the body-scoped const reconcileMspById (:3976). None is free, so none is host-reachable. Eight leave it — mayRestack, release, dispatchableStreaming, runScheduleStreaming, STREAMING_DISPATCH_ENABLED, shouldReconcileOnly, hasBuildableWork, runReconcileOnlyAdvance — and a deletion cannot manufacture a free identifier, though it can orphan a reference, which is the reason this row was measured rather than waved through. The census is not a pinned list that could go stale against the edit: workflow-sandbox-census.test.mjs reads ENGINE_SOURCE_PATH (:7) and re-derives its domain from the live engine source, halting on an empty host surface (:229) or an empty engine token set (:260). It closes on the edited engine: 129 pass / 0 fail across the four sandbox files. Independent corroboration that no bare identifier was orphaned: mitosis-gate.mjs phase-parity, which compiles this engine under the sandbox and censuses its phase tokens, exits 0 reporting bare 2 and dead 0. Candidly: the census rows were not read one by one; the receipt that the census still closes is that execution."
    },
    {
      "id": "B6",
      "verdict": "not-threatened",
      "check": "the id most exposed by a deletion MSP, so the production-caller chain was traced end to end rather than assumed. The harness is workflow-sandbox.mjs; its production caller is mitosis-gate.mjs, which imports compileWorkflow at :1, calls it inside compileUnderSandbox at :580, reaches that from runMitosisGate at :690, and is entered from process.argv at :725 with DEFAULT_PHASE_PARITY_TARGET resolving to ../../workflows/mitosis.js (:14). That caller is invoked in CI at .github/workflows/test.yml:21 as node .claude/lib/superpowers-parallel/mitosis-gate.mjs phase-parity. NO line on that chain was deleted: neither mitosis-gate.mjs nor workflow-sandbox.mjs is among the 8 changed paths, and a diff restricted to both emits 0 lines. Stronger than untouched — the caller's INPUT is precisely what this MSP rewrote, so the harness's liveness is re-proven by this change rather than merely surviving it: run here as CI runs it, exit 0, reporting 13 phases with dead 0. This change also strictly REDUCES the self-referential surface this invariant names: the four deleted lib exports had no importer outside their own mitosis.js twin text, and deleting them from both copies removes four exports whose liveness the lint could only read off duplicated text; dead-export-lint reports 3 tests / 3 pass / 0 fail afterwards. Standing cost named rather than smoothed over: mitosis-gate.mjs itself has no non-test module importer — grep across .claude/ finds it referenced only by its own file, three test files and that CI line — so the harness's production reach is a CI CLI invocation, not an import. That is the pre-existing shape, unchanged by this diff."
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this entry is itself the M1 mechanism, so the change cannot be called unengaged: all 12 registry ids carry a verdict here, once each, in a committed artifact rather than in the PR body. Four residuals, named rather than assumed away. First, scripts/invariant-coverage-check.mjs validates row shape, id set-equality against the registry and the verdict domain (:106-117, :131-137) and contains no path that evaluates whether a check string is TRUE, so nothing mechanical stands behind these words. Second, the pull-request clause reads a git diff between the merge base and HEAD (:169-176) and cannot see an untracked path: measured before commit, push mode exits 0 listing 12 entries while --event pull_request --base-ref main exits 1 with exactly the error CI reported, and the gate stays unsatisfied until this file is committed. Third, and specific to this artifact: it is written by a different agent than the one that authored fec38ac, so every claim here is a re-measurement performed on this tree, and nothing is asserted about what the commit's own author ran — where their process is unknown to me the rows say so instead of inferring it. Fourth, every execution receipt cited anywhere in this artifact was run by this author on this tree, and where a check was not run the row states that it was not run."
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged on both sides and the honest verdict is threatened. Closing, verified by execution rather than by citation: mirror-guard reports 46 tests / 46 pass / 0 fail, and it stays closed under this deletion because leases.mjs and reconcile.mjs are both WHOLE containment rows (mirror-guard.test.mjs:31 and :39), so removing a member from a module and its twin preserves containment rather than needing a row edit. dead-export-lint reports 3 / 3 / 0; its domain is a readdirSync over the lib directory filtered to entry.isFile() and .mjs (:12-17) and its liveness count is computed per export (:66-71), so the four deleted exports leave the domain by derivation, not by an allowlist edit. phase-parity halts on the unclassifiable by construction (mitosis-gate.mjs refuses to guess and returns halt on an unresolvable phase position) and exits 0 with dead 0 on the edited engine. The sandbox census re-derives from live source and closes at 129 pass / 0 fail. The threat is a gate this diff HAND-EDITED rather than derived: gh-scope-lint.test.mjs:17 carries const PR_CREATE_SITES = 2, changed from 3 by this commit, and it gates three assertions (:90, :97, :154) that classify pull-request-creation SITES in the engine source by counting them against that pin. A pinned count used as the classifier is exactly what this invariant forbids as a change-detector, and deleting the shepherd-open site was discharged by moving the pin rather than by a census that would halt on a site it could not classify. Pre-existing, not invented here, but touched by this diff and therefore named. Second, smaller: the advance verdict now carries a class no code classifies into an action — advance.toRestack is still computed (reconcile.mjs:122 and :130, twin mitosis.js:3026 and :3034) and still reported to the operator (mitosis.js:3996), but at HEAD it has ZERO consumers, where the parent consumed it at mitosis.js:3349. Nothing halts on that, so a relaunch can log built branches to restack and restack none."
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "the central id for a commit typed fix, and it was MEASURED, not accepted. Method: a detached worktree at 7b34a61 created under the scratchpad (never a branch switch, never a stash; git status --porcelain re-checked identical before and after), the five post-fix test files copied onto the parent's production code, then run. Red on the parent: reconcile-only-advance-characterization.test.mjs 5 tests / 1 pass / 4 fail, with T1, T2, T3 and T5 red and T4 green; frontier-train-e2e.test.mjs 56 / 52 / 4 fail, red on bullet 5 + 2, bullet 4b, M6 portability and M6 I4; gh-scope-lint.test.mjs 18 / 15 / 3 fail. NOT red on the parent, stated plainly: leases.test.mjs 29 / 29 / 0, so the one test this commit adds there — runSchedule terminates unconditionally — is GREEN on arrival and is a regression lock, not an acceptance test; reconcile.test.mjs 24 / 24 / 0, pure deletion of tests for deleted exports. Green on the fix: npm test at fec38ac reports 1838 tests / 1838 pass / 0 fail. The inertness clause was discharged by THIS author after the fact, not shipped by the commit, against scratch copies of the engine selected through the MITOSIS_PATH indirection both files already read (characterization :6, e2e :6), with the repo engine never modified and git status re-checked clean for it afterwards: baseline on the unmutated engine is 61 pass / 0 fail; deleting the lifted ship-delta memo loop (mitosis.js:3976-3986) gives 60 pass / 1 fail, the single failure being bullet 4b; replacing reconciledShippedSoFar() with an empty array inside prepareHaltReport (:4199) gives 60 pass / 1 fail, the single failure being T5. Two mutants run, zero inert. Why threatened rather than satisfied despite that evidence: the commit is typed fix but names no reported symptom — it unifies two schedulers, so the acceptance clause attaches to a redefined contract rather than to a bug an operator reported; the inertness mutations are this receipt's work rather than the commit's; and what the commit's own author executed could not be verified from the tree, so it is not claimed."
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "engaged, and the escape clause was tested rather than granted. Its LETTER is satisfied: a characterization test pinning the surviving behavior was written BEFORE the refactor, in the parent commit 7b34a61, whose subject is test(mitosis): pin the reconcile-only advance before m3 deletes it. Its SPIRIT is not, and the measurement is what settles it. Of the parent characterization file's 22 assertion lines, only 11 survive verbatim at HEAD; the other 11 were rewritten or removed in the SAME commit as the behavior change (git diff --numstat records 105 insertions against 33 deletions on that one file). Where it mattered the pinned contract inverted: T3 pinned record.stage equal to ship and overallStatus equal to blocked at the parent, and now pins blocked and awaiting-approval. In frontier-train-e2e.test.mjs the test bullet 3: restack-on-merge was DELETED outright and the behavior it pinned was dropped without replacement — advance.toRestack survives as a computed, logged field (mitosis.js:3026, :3034, :3996) with no consumer, where the parent consumed it at :3349. M6 I4's absent-ref assertion flipped from identity local-only to published, a real behavior change that falls out of deleting the early return that used to preempt the publish block. Rewriting a characterization test's assertions inside the reviewable range of the behavior change it was written to pin is precisely the failure mode this invariant guards, so the pre-written test does not discharge it. The structural half sharing that same range is large and real: leases.mjs falls 292 lines to 209 and reconcile.mjs 143 to 134, deleting a whole unreachable streaming scheduler, and mitosis.js loses 250 lines against 37 added. Recorded as threatened rather than argued away."
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "engaged and anchor-dense, so every path:line in this artifact was re-derived live with grep and sed against the working tree at write time, and none was copied from the task brief, from a prior receipt or from a plan. Re-derived at HEAD: mitosis.js isBuildable :2270, runScheduleTick :2377, runSchedule :2418, maxSteps :2382 with its loop at :2384, reconcileMspById :3976 with the lifted memo loop running :3976-3986 and its persistShipCheckpoint call at :3981, the advance log carrying toRestack at :3996, planReconcile's toRestack at :3011, :3026, :3034 and :3037, the Resume phase declaration at :15 and its surviving reference at :3184, reconciledShippedSoFar :4185 and prepareHaltReport :4198 with its call at :4199, persistShipCheckpoint's definition at :4445; leases.mjs maxSteps :170, loop :172, runSchedule :206; reconcile.mjs :107, :122, :130, :133; mitosis-gate.mjs :1, :12, :14, :580, :690, :725; workflow-sandbox.mjs :18, :23, :25; mirror-guard.test.mjs :31 and :39; dead-export-lint.test.mjs :12-17 and :66-71; gh-scope-lint.test.mjs :17, :90, :97, :154; workflow-sandbox-census.test.mjs :7, :229, :260; invariant-coverage-check.mjs :106-117, :131-137, :169-176; the two characterization/e2e MITOSIS_PATH reads at :6; .github/workflows/test.yml:21. File lengths at write time: mitosis.js 5140 (parent 5353), leases.mjs 209 (parent 292), reconcile.mjs 134 (parent 143). The parent-side anchor mitosis.js:3349 was derived from git show 7b34a61, not from the current tree. Residuals named rather than assumed away: docs/superpowers/specs/2026-07-30-two-track-invariant-plan.md and everything under docs/superpowers/plans/ were NOT re-baselined against this deletion, so any stale anchors there survive for the next reader; and PR #30's body was not audited for stale citations, which is unremediable through this channel in any case because a PR title and body are frozen at creation."
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "registry id M6 concerns versioned gate artifacts and is unrelated to MSP M3, the scheduler unification shipped here; the two are not conflated. No gate enforcer is rebuilt, hand-patched, vendored or cached: git diff --name-only 7b34a61..fec38ac restricted to scripts/, .github/ and .claude/skills/mitosis/templates/ returns 0 paths, so scripts/invariant-coverage-check.mjs, .github/workflows/test.yml and the receipts template are byte-unchanged by this MSP, and my own change adds exactly one JSON file under docs/invariants/coverage/ and builds nothing. The mitosis engine is not a built artifact — .claude/workflows/mitosis.js is source and the deployed file is the same content through the ~/.claude symlink layout — so this id's refresh-must-reproduce-the-guard clause is discharged by the policed-twin rule, and the guard did reproduce after both twin deletions: mirror-guard.test.mjs run in isolation reports 46 tests / 46 pass / 0 fail including the leases.mjs and reconcile.mjs whole-containment rows. The two inertness mutants cited in the M3 row were produced without patching any committed or deployed artifact — each is a separate scratch file written outside the repo and selected at run time through MITOSIS_PATH — and git status --porcelain re-checked afterwards shows .claude/workflows/mitosis.js unmodified."
    }
  ]
}
