{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "the change is two added files and carries zero production change: git diff --name-only between merge-base 1986aab and HEAD is empty, and git status reports .claude/workflows/mitosis.js unmodified, so no realm is constructed, removed or reached. grep over the added test finds zero occurrences of vm., DONT_CONTEXTIFY and createSandbox, and no workflow-sandbox file appears in git status --short. Named candidly rather than left implicit: the file DOES compile the engine body with the host realm's AsyncFunction constructor (reconcile-only-advance-characterization.test.mjs:26-27), deliberately running mitosis.js in the host realm OUTSIDE the sandbox. That is the harness pattern it inherits from frontier-train-e2e.test.mjs, and it means the file proves nothing about B1 in either direction while leaving the production realm untouched"
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no denial path is added, removed or reached. grep over the added file finds zero occurrences of SandboxViolationError, and no workflow-sandbox file is in the change set. The only failures the new code can raise are node:assert assertions thrown in the test process by its four tests, never inside a compiled workflow body. The one catch-shaped construct the file relies on is the harness parallel shim at :29, which swallows a rejected thunk into null; it is inherited verbatim from the sibling e2e harness and governs the test's own dispatch rather than any sandbox mechanism, so no denial gains an untagged host-error route that a workflow could catch as its own"
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not read member by member for this entry. What was read is the whole change surface, which is one added test file and one added coverage entry, adding no member to and removing no member from any policy list, with every workflow-sandbox file absent from git status --short. No guarded intrinsic therefore gains or loses a denied member. The receipt is the full suite executed by this author on this tree: npm test reporting tests 1850, pass 1850, fail 0, which carries the existing guarded-intrinsic assertions unchanged"
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "checked by grep rather than assumed: VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS occur zero times in the added file, and no file defining or asserting them appears in git status --short. Emptying any of the three would not move an assertion in this file either, since its four tests assert over result.parked, result.awaitingApproval, result.overallStatus, dispatched stage labels and phase lines, none of which reads sandbox policy. Load-bearingness stays proven exactly where it was proven before, and the 1850 pass / 0 fail suite carries those proofs unchanged"
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "re-checked rather than defaulted. mitosis.js IS the workflow-body position and it receives no line in this change, verified by an empty git diff --name-only against merge-base 1986aab plus a clean git status for that path, so no new free identifier or bare global reaches the host and the existing classification rows stay exhaustive. The new file does name the six workflow-body parameters when it constructs its harness runner (args, agent, parallel, log, phase, workflow at :27), but those are the engine's own declared parameter list, bound as parameters of a host-realm function rather than left free to resolve; the census reads the engine source, which is byte-unchanged. Candidly: the census rows were not read one by one, and the receipt that the census still closes is the 1850 pass / 0 fail suite"
    },
    {
      "id": "B6",
      "verdict": "not-threatened",
      "check": "checked rather than assumed, because the new file DOES import a lib export and could in principle manufacture liveness for it. It cannot. dead-export-lint computes liveCallerCount as matches in mitosis.js plus sibling lib modules plus the defining module's own masked text (dead-export-lint.test.mjs:66-71), and its module set comes from libModuleNames(), a non-recursive readdirSync over the lib directory filtered to entry.isFile() and a .mjs suffix (:12-17). tests/ is a directory entry and is excluded, so no test file counts anywhere in that computation. The one export this change imports, computeLogicalRunId (declared recovery.mjs:4, imported at the new file's :4), already carries production liveness independent of this file: grep finds it twice in mitosis.js, declared at :349 and called at :4024. The change adds no export of its own, so it raises no new liveness question"
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this entry is itself the M1 mechanism, so the change cannot be called unengaged: all 12 registry ids carry a verdict here, once each, in a committed artifact rather than in the PR body. Three residuals, named rather than assumed away. First, scripts/invariant-coverage-check.mjs validates row shape, id set-equality and the verdict domain (:106-117, :131-137) and contains no path that evaluates whether a check string is TRUE, so nothing mechanical stands behind these words. Second, and specific to this change: at authoring time BOTH the new test file and this coverage entry are UNTRACKED and the branch carries no commit beyond merge-base 1986aab, while the gate's pull-request clause reads git diff between the merge base and HEAD (:169-176), which cannot see untracked paths; the gate is green here only in push mode, exit 0 listing 12 entries, and stays unsatisfied for pull-request mode until the orchestrator commits both files. Third, every execution receipt cited in this entry was run by this author on this tree, and where a check was not run the row says so"
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged, and the honest verdict is threatened rather than vacuous. This change adds no gate, so no census of tokens, identifiers or paths is weakened directly. What it does add is a file that no census classifies at all: mirror-guard resolves its MIRROR_CENSUS domain through libModuleNames(), a non-recursive readdirSync over the lib directory filtered to entry.isFile() and .mjs (mirror-guard.test.mjs:115-119, LIB at :7), so the tests/ directory falls outside it in exactly the way tests/ falls outside dead-export-lint. Measured by this author rather than inferred: 12 of the file's 15 top-level helpers share a name with frontier-train-e2e.test.mjs, and 199 of its 249 non-blank lines are byte-identical to some line in that sibling, close to 80 percent, duplication policed by neither the shared-fixture-module precedent nor the policed-twin doctrine. The drift is silent by construction: a stale fixture that omits an upstream precondition still resolves to a report rather than throwing, and the file's own entry-guard assertion catches only the fall-through-to-build shape of that degradation, not every shape"
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "the central invariant for this change, and engaged. There is no reported symptom and no production fix here, so the red-on-parent acceptance clause has nothing to attach to; the asserted symptom is the engine behavior itself, pinned before a later PR is allowed to delete or restructure it. The inertness clause IS owed and was discharged by execution, by this author, in scratch copies selected through the MITOSIS_PATH indirection, with the repo engine never modified and git status re-checked clean for it afterwards. Measured, each mutant anchored to a single verified-unique site: baseline on the unmutated engine is 4 pass / 0 fail; forcing the shouldReconcileOnly body to return false (mitosis.js:3064) gives 0 pass / 4 fail; widening the built-and-not-done selector guard (:3122-3123) gives 0 pass / 4 fail; flipping the shepherdPark stage literal from ship to plan (:3314) gives 2 pass / 2 fail, reddening T1 and T3 only, because T2 pins no stage field and T4 parks nothing; deleting only the manifest-shipped half of the shippedIds seed (:3302) gives 3 pass / 1 fail with T4 the sole failure, which is the receipt that T4 is independently load-bearing. Across 12 mutants run, zero were inert"
    },
    {
      "id": "M4",
      "verdict": "not-threatened",
      "check": "this change is the artifact M4 demands rather than a change M4 constrains. Its whole purpose is to pin the surviving reconcile-only advance behavior BEFORE a later PR restructures or deletes it, which is precisely the precondition M4 requires a refactor to satisfy in advance. The precondition M4 would otherwise test is absent here: the change carries zero production change, verified by an empty git diff --name-only between merge-base 1986aab and HEAD plus a clean git status for .claude/workflows/mitosis.js and every lib module, so no behavior change exists to share a reviewable range with anything. Suite accounting corroborates that nothing else moved: npm test reports 1850 pass on this tree and the new file contributes exactly 4 tests, leaving 1846, which is the parent figure recorded for this commit"
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "engaged and anchor-dense, so every citation in this entry was re-derived against the working tree today with grep and sed, and none was copied. Re-derived: shouldReconcileOnly at mitosis.js:3063 with its body at :3064, the selector guard at :3122-3123, runReconcileOnlyAdvance at :3293, the shippedIds seed at :3300-3303 with its manifest-shipped half at :3302, the shepherdPark stage literal at :3314, computeLogicalRunId at :349 and :4024; recovery.mjs:4; dead-export-lint.test.mjs:12-17 and :66-71; mirror-guard.test.mjs:7 and :115-119; invariant-coverage-check.mjs:9, :97 and :169-176; and the new file's own :4, :26-27 and :29. One residual named rather than assumed away: an earlier draft of this entry cited a predicted mutation matrix attributed to the authoring plan, but no such plan is a committed artifact in this tree, since grep across docs/ finds those mutant labels only inside this receipt, so that prediction could not be re-derived and has been dropped rather than transcribed; every tally in the M3 row above is an execution result measured here"
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "nothing is built, deployed, vendored or hand-patched by this change: it adds one test file executed from the repo at its own SHA, and .claude/skills/mitosis/templates/receipts.yml, scripts/invariant-coverage-check.mjs and .github/workflows/test.yml are all absent from git status --short. The mutation receipts in M3 were produced without patching any committed or deployed artifact, since each mutant is a separate scratch copy written outside the repo and selected at run time through the MITOSIS_PATH environment indirection the file already reads at :6, with git status re-checked afterwards confirming mitosis.js unmodified. One residual named rather than assumed away: that same indirection resolves the engine under test from the environment, so a stale or redirected MITOSIS_PATH in a CI environment would silently characterize a different engine than the committed one, and nothing in the file asserts that the resolved path is the repo's own"
    }
  ]
}
