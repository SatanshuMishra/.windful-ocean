{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "checked by reading the added identifiers rather than waved through, because the workflow-body position IS edited here: 68ee1bb rewrites 69 lines of .claude/workflows/mitosis.js. The positive distinguishing fact is that the identifier set resolvable in the body strictly SHRANK. Everything added — mitosis.js:3470 (const buildAheadCapOverride = input.buildAheadCap), :3745-3748 (the arg-boundary validator and the resolved buildAheadCap) and :5046 ({ window: buildAheadCap }) — resolves only to engine-local bindings (input, haltReport, BUILD_AHEAD_CAP) plus Number, which already resolved at 9ea75d6; no new bare identifier and no new intrinsic member appears anywhere in the diff. The deletions run the other way: nextWindow and clampWindow took three Math.min / Math.max / Math.ceil call sites with them. Math is not orphaned by that — it survives at mitosis.js:354, :443, :2155 and :4022 — so no classification row is left describing an identifier the engine no longer uses. workflow-sandbox.mjs and every workflow-sandbox test file are absent from git diff --name-only 9ea75d6..HEAD, which lists exactly 13 paths: five lib modules, six lib test files, the deleted tests/window.test.mjs and mitosis.js."
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no denial path is added, removed or reached: SandboxViolationError occurs zero times across the whole 9ea75d6..HEAD diff, grepped over every changed path rather than assumed from the commit subjects. The positive distinguishing fact is that this change adds a refusal of its OWN and that refusal is deliberately not a sandbox denial — mitosis.js:3745-3746 returns haltReport('input', ...) for any buildAheadCap outside 1..8, which surfaces as a structured engine report rather than as a thrown error a workflow body could catch as its own. E10 pins that it stays structured for all six rejected shapes (null, 'banana', 1.5, 0, -1, 9999): frontier-train-e2e.test.mjs:955-957 asserts overallStatus 'failed', stage 'input', and a detail naming the knob that failed. Nothing in these three commits constructs, catches or re-tags an error crossing the sandbox boundary."
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not re-read member by member for this entry. What was read is the whole change surface, which adds no member to and removes no member from any guarded intrinsic — workflow-sandbox.mjs is absent from the 13-path change set. The positive fact is directional rather than a bare denial: this change is net-SUBTRACTIVE on intrinsic member reach, deleting the Math.min / Math.max / Math.ceil call sites that lived in nextWindow and clampWindow and adding none, while leaving Math itself reached at four surviving sites (mitosis.js:354, :443, :2155, :4022) so no enumerated member's guard is left unexercised by the engine. The receipt that no guarded intrinsic changed behaviour is the full suite, which carries workflow-sandbox-traps, workflow-sandbox-policy, workflow-sandbox-census and workflow-sandbox: npm test 1841 pass / 0 fail on this tree, run by the pre-commit hook on this commit."
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "checked by grep rather than assumed: VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS occur zero times across the 9ea75d6..HEAD diff, and no file defining or asserting them appears in the 13-path change set, so the load-bearingness proofs in workflow-sandbox-census.test.mjs and workflow-sandbox-policy.test.mjs are untouched and pass inside the 1841 pass / 0 fail suite. Named because it is the nearest thing to a threat here: this change DOES delete three named constants (WINDOW_FLOOR, WINDOW_CEILING, WINDOW_INCREMENT) and introduce one (window.mjs:1, export const BUILD_AHEAD_CAP = 8), but these are engine constants, not sandbox policy constants, so B4's subject is untouched. The new constant meets B4's own standard anyway rather than being taken on faith — emptying it turns assertions red, and that was measured: under an injected cap regression dropping the bound check in isBuildable, both cap tests (frontier-train-e2e.test.mjs:886 and :1306) redden with actual 12 against expected 8 on the 12-unit chain, and both are green on the tracked tree."
    },
    {
      "id": "B5",
      "verdict": "threatened",
      "check": "engaged for real, because mitosis.js IS the workflow-body position and 68ee1bb rewrites 69 of its lines, and because this change is exactly the shape that can open a new host route: it adds a new read of external input at the arg boundary (mitosis.js:3470, const buildAheadCapOverride = input.buildAheadCap) together with a validator around it. Measured rather than reasoned: the added statements introduce no bare identifier that did not already resolve at 9ea75d6 — only input, Number.isInteger, haltReport, BUILD_AHEAD_CAP and a template literal — so the host-reachable set is a strict SUBSET of the pre-change set, and a closed census cannot halt on a subset of what it already classifies. The receipt is the census's own execution rather than a reading of its rows: workflow-sandbox-census.test.mjs runs inside npm test at 1841 pass / 0 fail on this tree. Named candidly: this author did not re-read the classification rows one by one and did not run the census file standalone; the whole-suite run is what establishes the closure, which is the mechanism the invariant specifies."
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "genuinely engaged, because this change both DELETES exports and ADDS one, and either direction could have manufactured or destroyed liveness. window.mjs is reduced to a single line (window.mjs:1): WINDOW_FLOOR, WINDOW_CEILING, WINDOW_INCREMENT, nextWindow and windowDelta are all deleted, and every consumer went with them in the SAME commit — leases.mjs's WINDOW_FLOOR import, reconcile.mjs's nextWindow import, run-log.test.mjs's windowDelta import and tests/window.test.mjs whole — so no export is orphaned and no importer is left dangling. The worked example holds and was re-derived rather than accepted: BUILD_AHEAD_CAP has a genuine non-test importer at .claude/lib/superpowers-parallel/leases.mjs:2, consumed at leases.mjs:111, and grep confirms leases.mjs is the ONLY importer of window.mjs anywhere in the lib tree. That distinction is load-bearing: liveCallerCount (dead-export-lint.test.mjs:66-71) sums mitosis.js counted RAW plus sibling modules plus the defining module's masked own text, and mitosis.js carries its own const BUILD_AHEAD_CAP = 8 twin definition, so a mitosis.js occurrence alone would have been self-referential exactly as a prior MSP's export was. Here the sibling importer is what proves liveness, not the lint arithmetic. Receipt: dead-export-lint.test.mjs runs green inside npm test 1841 pass / 0 fail."
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this artifact is itself the M1 mechanism, so the change cannot be called unengaged: all 12 registry ids carry exactly one verdict here, in a committed file under docs/invariants/coverage/ rather than in a PR body, named feat-m4-fixed-build-ahead-cap.json after the branch per the convention all 15 pre-existing entries follow. Two things are stated precisely rather than assumed. First, what the gate actually requires: scripts/invariant-coverage-check.mjs:167-177 diffs merge-base(base, HEAD)..HEAD and passes when ANY path under docs/invariants/coverage/ was added or modified in that range — NOT, as a prior coverage entry asserted, that the receipt share one commit with the production change. This file lands as its own commit on the same branch above 9ea75d6, so merge-base..HEAD contains it and the pull-request clause is satisfied. Second, the known trap was avoided deliberately rather than by luck: a bare node scripts/invariant-coverage-check.mjs resolves the event to '' (line 192), fails the PULL_REQUEST_EVENTS membership test, and silently runs in PUSH mode where pullRequestErrors is never called and the diff clause proves nothing — two documented incidents came from exactly that. The verification run for this entry is therefore GITHUB_EVENT_NAME=pull_request GITHUB_BASE_REF=main node scripts/invariant-coverage-check.mjs, whose resolveBaseCommit (line 151) tries origin/main FIRST and so pins the base at 9ea75d6, rather than the local main ref which is three commits stale at 7b34a61 and would compute a merge-base three commits too low. Candid limit: a file cannot record the exit status of the gate that validates it, because the gate reads the committed content; the executed command and its verbatim output are reported to the orchestrator alongside this commit rather than embedded here."
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged on two censuses, one new classifying gate and one pinned count, each named rather than summarized. THE MIRROR CENSUS: MIRROR_CENSUS classifies leases.mjs (mirror-guard.test.mjs:31), reconcile.mjs (:39), recovery.mjs (:40), run-log.mjs (:44) and window.mjs (:48) all as WHOLE, and this change edits ALL FIVE plus the mitosis.js twin, so a one-sided edit was the standing risk on every hunk. Receipt, cited from the supervised run rather than re-executed: mirror-guard.test.mjs 46 pass / 0 fail. The one symbol deliberately NOT twinned was checked rather than assumed — buildAheadCap has no twin in .claude/lib/superpowers-parallel/ (repo-wide grep), and its five occurrences are all in mitosis.js (:3470, :3745, :3746, :3748, :5046), which is correct because the arg boundary exists only in the engine. THE NEW GATE, a positive M2 instance: mitosis.js:3745-3746 classifies the buildAheadCap token as accepted (an integer in 1..8) or HALTS the run, with no log-and-default swallow, and E10 (frontier-train-e2e.test.mjs:916) pins all six rejected shapes to stage 'input'. THE GENUINE TENSION, named rather than passed over: deleting applyRunDelta's 'window' case moves the 'window' journal record kind from CLASSIFIED to unclassified, and applyRunDelta's terminal return-manifest ignores it silently instead of halting — the opposite of what M2 asks of a classifier. That is a deliberate backward-compatibility choice for journals written before this change, and the inertness is now pinned rather than asserted: run-log.test.mjs:157 asserts the fold carries no window key AND that a quiescent-exit record placed AFTER the legacy record still applies, so the unknown kind is skipped rather than derailing the fold. THE PINNED COUNT, named so it is not mistaken for a change-detector: the two cap tests assert builtAhead.size === 8. That 8 is BUILD_AHEAD_CAP itself, not a proxy for it, and the probe is proven capable of reddening — under an injected cap regression both fail at actual 12, expected 8."
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "engaged, and every claim below is a receipt from a supervised execution, cited rather than reproduced. E10 (frontier-train-e2e.test.mjs:916) is the acceptance test for the widening defect: it was RED on the pre-amend 777617b tree and GREEN after, and the symptom it asserts was measured directly — a probe on the pre-amend tree printed buildAheadCap=9999 -> overallStatus=awaiting-approval frontierWidth=12, meaning the supplied value was ACCEPTED and widened build-ahead to the whole 12-unit chain against a cap of 8. M2c (frontier-train-e2e.test.mjs:1151) is the acceptance test for coverage the AIMD deletion silently took with it: RED on a scratch mutant resolving an unparsed branch to the first 'built' unit — no ship:l2 dispatched, awaitingApproval = l2 carrying the foreign pull/7686, overallStatus awaiting-approval — and GREEN on the tracked tree, where ship:l2 is dispatched, awaitingApproval is empty and the run reaches all-shipped. The argument for placing it at e2e is a receipt too, not an assertion: the pure-function tests for branchToMspId stayed GREEN on that same mutant, proving the unit layer cannot express the wiring, and they sit at tests/recovery.test.mjs:49-58, re-derived by reading that range rather than copied from the brief. The inertness mutation for the surviving cap behaviour: both retitled cap tests (frontier-train-e2e.test.mjs:886 and :1306) were proven to REDDEN under an injected cap regression, failing at actual: 12, expected: 8, which closes a gap the original audit declared but never ran. Suite: 1841 pass / 0 fail, up from a 1839 baseline, the +2 being exactly E10 and M2c."
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "the invariant this branch is most at risk of violating, and it carries one DEFERRAL declared here rather than left silent. 68ee1bb does put a structural refactor and a behaviour change in one reviewable range: a 102-insertion / 207-deletion commit removing the AIMD window across both twins, alongside genuinely new behaviour (the halting buildAheadCap validator at mitosis.js:3745-3748, and buildAheadWindow's non-integer fallback moving 3 -> 8 at leases.mjs:111). The characterization that licenses it pre-existed the refactor rather than being written after it: E9 and the build-path cap test both pinned build-ahead width at exactly 8 on a 12-unit chain at 9ea75d6 under the old clamp regime, and both still pin 8 under the fixed cap, so the surviving behaviour is identical across the refactor and was asserted before it. Candid: the proof that those two are CAPABLE of reddening (actual 12 against expected 8 under a cap regression) was taken at da36e4f, after the refactor, not before it. The real M4 damage was found and repaired rather than argued away — deleting the AIMD tests took an UNRELATED surviving invariant with them, because the deleted 'H2 dedup + foreign filter' e2e test was the only coverage of classifyRunOpenPRs's foreign-branch skip, and d5ccf5d re-plumbs that invariant as M2c on a fixture that no longer mentions a window. DEFERRED, and named because it is real: the wide frontierManifest cleanup did NOT ship. da36e4f cleaned exactly the two cap tests whose titles contradicted their fixture data and stopped there, so the helper signature at frontier-train-e2e.test.mjs:122 still declares the dead window parameter, 19 call sites in that file still pass a window property (18 an integer, one an explicit undefined at :924) and five fixture-option sites still forward { window: 6 }, while the textually independent duplicate helper at tests/reconcile-only-advance-characterization.test.mjs:96 declares the same dead parameter with four call sites (:233, :279, :305, :328) still passing window: 3. Every one of those is inert, since manifest.window has no reader in any copy, but the dead argument remains and clearing it is owed to a later change."
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "engaged and anchor-dense, so every path:line in this artifact was re-derived by grep or by reading the range against the live tree before it was written, and none was carried over from the dispatch brief. Re-derived and CONFIRMED: window.mjs:1 (the whole surviving module), leases.mjs:2 (the BUILD_AHEAD_CAP import, and the only importer of window.mjs in the lib tree) and leases.mjs:111 (the fallback); mitosis.js:3470, :3745, :3746, :3748 and :5046, which grep confirms is the COMPLETE set of buildAheadCap occurrences outside tests; mitosis.js:354, :443, :2155 and :4022 (the surviving Math sites); mirror-guard.test.mjs:31, :39, :40, :44 and :48 (the five WHOLE rows this change edits); dead-export-lint.test.mjs:66-71 (liveCallerCount); frontier-train-e2e.test.mjs:122, :886, :916, :924, :955-957, :1151 and :1306; run-log.test.mjs:157; recovery.test.mjs:639; reconcile-only-advance-characterization.test.mjs:96 with call sites :233, :279, :305 and :328; and scripts/invariant-coverage-check.mjs:151, :167-177 and :192 for the gate contract quoted under M1. One anchor arrived FROM a receipt rather than from this author's own reading and was therefore re-derived instead of trusted: tests/recovery.test.mjs:49-58 was cited as the branchToMspId pure-function coverage, and reading that exact range confirms it holds the two branchToMspId tests and nothing else. Named candidly: every :line above is stated at HEAD = da36e4f, the tree this artifact is committed onto, and NOT at the pre-change 9ea75d6 — unlike the precedent entries, which were written against an uncommitted tree and so had to anchor at the parent commit. All three commits are already landed, so their line numbers are final and no post-landing shift can falsify these. Anything expressed as a count rather than a line (19 call sites, five forwards, four duplicate-helper call sites, four Math sites) was likewise counted on this tree rather than estimated."
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "nothing is built, deployed, vendored or hand-patched by these three commits. The change set is exactly 13 paths (git diff --name-only 9ea75d6..HEAD): five lib modules, six lib test files, the deleted tests/window.test.mjs and .claude/workflows/mitosis.js. No gate-enforcing artifact is among them — .github/, scripts/ and .claude/skills/mitosis/templates/receipts.yml return zero matches against that list. The positive distinguishing fact for the one file that could be mistaken for a deployed copy: .claude/workflows/mitosis.js is committed source, not a hand-patched deployment of the lib modules, and its agreement with them is enforced by a versioned check rather than by convention — MIRROR_CENSUS classifies all five touched modules WHOLE and mirror-guard.test.mjs reports 46 pass / 0 fail on this tree. Two residuals are named rather than silently tolerated. First, the untracked .bak-pre-promptsfix-043a2526 copies of mitosis.js and engine-args.mjs remain in the working tree and are exactly the hand-patched-deployed-file shape M6 forbids; they predate this branch, they are among the five pre-existing dirty paths this commit was required to leave unstaged, and they are therefore carried forward unchanged rather than fixed here. Second, the e2e suite resolves the engine under test through the MITOSIS_PATH environment indirection, so a stale or redirected value in a CI environment would characterize a different engine than the committed one, and nothing asserts that the resolved path is the repository's own."
    }
  ]
}
