{
  "rows": [
    {
      "id": "B1",
      "verdict": "threatened",
      "check": "engaged rather than waved through, because this change DOES add identifiers to the workflow-body position and the census caught it. The first implementation used new Date().toISOString(), Date.now() and Date.parse for the section 11 latency emitter, plus a bare arguments for the runSchedule arity guard. Measured, not reasoned: workflow-sandbox-census went red naming Date, NaN and arguments as unclassified, and a direct probe through compileWorkflow showed new Date() and Date.now() raise SandboxViolationError with the message 'the determinism contract bans every wall-clock and timezone read, and the engine is Date-free', while bare arguments resolves to the compiled wrapper's own 7 host-hook arguments (probe printed keys 0..6). The emitter as first written would therefore have CRASHED the run at the quiescent exit. Both were removed rather than classified: the timestamp moved to the journal-append agent (time enters this engine only through agent results), and the arity guard moved to a rest parameter. Final state: mitosis.js reaches no host intrinsic that it did not reach at 42e7d49, and workflow-sandbox-census.test.mjs plus workflow-sandbox-traps/policy/harness pass inside the 1845 pass / 0 fail (1832 before remediation round 1 added nine tests, 1841 before remediation round 2 added four more) suite run by this author on this tree"
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no denial path is added, removed or reached. grep for SandboxViolationError over the whole change set returns zero occurrences, and workflow-sandbox.mjs is absent from git status --short. The one new catch-shaped construct is the try/catch already present in the extracted appendRunJournal helper, which is the verbatim body lifted out of the pre-existing persistWindowCheckpoint (it caught and logged before this change and catches and logs after it); it wraps an agent dispatch, not a sandbox mechanism, and it swallows into a log line rather than into a workflow-catchable error. Named candidly: this change did CAUSE a SandboxViolationError during development, via the Date usage recorded under B1, and that error arrived correctly tagged, which is B2 behaving as specified rather than a threat to it. The offending code is gone from the tree"
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not read member by member for this entry. What was read is the whole change surface, which adds no member to and removes no member from any policy list; ALWAYS_DENIED, ALLOWED_GLOBALS and VALUE_GLOBALS are byte-unchanged and workflow-sandbox.mjs is absent from git status --short. Relevant and named rather than passed over: Date is a guarded intrinsic whose denial this change ran into head-on during development (see B1), and the resolution was to stop touching Date, not to soften its guard, so its enumerated denial is exactly what it was at 42e7d49. The receipt that no guarded intrinsic changed behavior is the full suite executed by this author on this tree: npm test reporting tests 1845, pass 1845, fail 0 (1832 before remediation round 1 added nine tests, 1841 before remediation round 2 added four more)"
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "checked by grep rather than assumed: VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS occur zero times in the production diff, and no file defining or asserting them appears in git status --short. The load-bearingness proofs live in workflow-sandbox-census.test.mjs and workflow-sandbox-policy.test.mjs, neither of which this change edits, and both pass inside the 1845 pass / 0 fail (1832 before remediation round 1 added nine tests, 1841 before remediation round 2 added four more) suite. One honest connection to B1: the census row set is unchanged at exactly the rows present at 42e7d49, because the three identifiers this change briefly introduced were removed from the engine rather than added to those lists; had they been added instead, this row would read threatened"
    },
    {
      "id": "B5",
      "verdict": "threatened",
      "check": "the sharpest invariant for this change and the one that caught the worst defect in it. mitosis.js IS the workflow-body position and this change edits it heavily, so the census was engaged for real rather than by assertion. Measured by execution: node --test workflow-sandbox-census.test.mjs failed with 'engine identifiers resolving in the sandbox with no classification row: Date, NaN, arguments', which is the closed census halting on the unclassifiable exactly as B5 requires. The census was NOT satisfied by adding three rows, which would have widened the host-reachable surface for instrumentation convenience; the engine was changed to stop using all three. Re-run after the fix: 75 pass / 0 fail across workflow-sandbox-census.test.mjs plus mirror-guard.test.mjs together, and the census row set is byte-identical to 42e7d49. Named candidly: this author did not re-read the classification rows one by one; the receipt that the census still closes is its own execution, which is the mechanism the invariant specifies"
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "engaged, because this change both DELETES lib exports and ADDS them, and either could have manufactured or destroyed liveness. progressPossible is deleted from leases.mjs and from its mitosis.js twin in the same change, so no export is orphaned. quiescentExitDelta and isIsoInstant are added as new exports of run-log.mjs and their production caller is mitosis.js, which calls quiescentExitDelta inside persistQuiescentExitCheckpoint and isIsoInstant inside applyRunDelta, the engine read guard and the journal read-back; dead-export-lint counts mitosis.js raw and tests/ never counts (libModuleNames is a non-recursive readdirSync filtered to entry.isFile() and .mjs), so a mitosis.js occurrence is NOT by itself proof of a call, and each of these two was checked to have a real call site rather than only a twin definition. Verified by execution: node --test dead-export-lint.test.mjs reports 3 pass / 0 fail after the change. CORRECTION, and the reason this row now reads threatened rather than not-threatened: an earlier draft of this row asserted that classifyMergeWatch, planMergeWatch, mergeWatchPrompt and MERGE_WATCH_SCHEMA 'each retain a mitosis.js occurrence and stay live'. That claim was FALSE and the lint it leaned on structurally cannot establish it. Measured: every occurrence of each of the four in mitosis.js is its own definition (classifyMergeWatch :2832, planMergeWatch :2801, mergeWatchPrompt :2815 plus its own guard string :2816, MERGE_WATCH_SCHEMA :2769, all stated at 42e7d49 like every other :line in this artifact); no sibling lib module references any of the four; and the only production importer of merge-watch.mjs takes other exports (mitosis-git.mjs takes validateRepoIdentity and parsePrRef; merge-boundary-preflight.mjs was a second such importer taking validateRepoIdentity, and was deleted outright in a later change). The poll deletion therefore left all four with ZERO production callers, and liveCallerCount (dead-export-lint.test.mjs:66-71) counts raw textual occurrences over an unmasked mitosis.js, so a twin's own definition line reads to it as a caller and it can never detect this. The four are deliberately NOT deleted here and are recorded as a named residual in the spec section 10 re-baseline: merge-watch.mjs is a whole twin, merge-watch.test.mjs carries real security coverage on planMergeWatch's repo-identity and option-injection guards, and folding that deletion into this MSP is the exact deletions-plus-behavior-change mixing M4 is threatened for. One genuinely dead import WAS removed: leases.test.mjs imported planMergeWatch and never used it after the poll deletion. REMEDIATION ROUND 2 CORRECTION: the five anchors above previously read :2806/:2775/:2789/:2790/:2743, which matched NEITHER 42e7d49 NOR the delivered tree — they were carried over from a pre-remediation intermediate that no longer exists, and they falsified the M5 row's blanket 42e7d49 provenance claim. Re-derived by grep against both trees and restated at 42e7d49; the substance of the row was re-verified unchanged (zero sibling references, the surviving importer still takes validateRepoIdentity/parsePrRef only, node --test dead-export-lint.test.mjs 3 pass / 0 fail on the final tree). The same stale numbers had been copied into the spec section 10 residual paragraph and were corrected there in the same pass"
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this artifact is itself the M1 mechanism, so the change cannot be called unengaged: all 12 registry ids carry a verdict here, once each, in a file written to be committed rather than in a PR body. Three residuals named rather than assumed away. First, scripts/invariant-coverage-check.mjs validates row shape, id set-equality and the two-valued verdict domain and contains no path that evaluates whether a check string is TRUE, so nothing mechanical stands behind these words. Second, and this is the OPEN LANDING OBLIGATION rather than a discharged check: the gate's pull-request clause diffs merge-base..HEAD and cannot see untracked paths, so this file must be COMMITTED in the same commit as the production change or it reads as no receipt and CI goes red. Adversarial review confirmed the failure mode by execution rather than by argument — node scripts/invariant-coverage-check.mjs --event pull_request --base-ref main exits 1 on this tree with 'no file under docs/invariants/coverage/ was added or modified between main and HEAD', because HEAD is still 42e7d49 and nothing is committed. Remediation round 1 was likewise instructed not to commit, so the most it could do is STAGE this path explicitly (git add docs/invariants/coverage/feat-m5-quiescent-exit.json, never git add -A), which it did; staging makes the path tracked and impossible to miss in a bare git commit, but it does NOT satisfy the gate, which needs the commit. The commit is owed by the agent that lands this work, and the verification it owes is: git diff --cached --name-only | grep feat-m5-quiescent-exit.json before committing, then node scripts/invariant-coverage-check.mjs --event pull_request --base-ref main exiting 0 after. Third, on what was and was not run: this author DID run node scripts/invariant-coverage-check.mjs --event push on this tree and it exited 0, printing 'invariant-coverage-check: ok' and enumerating this file among its entries, and separately validated by script that the 12 row ids set-equal the registry ids with no duplicates and only the two allowed verdicts. This author did NOT get --event pull_request --base-ref main to exit 0, and cannot without committing; the row says so plainly rather than implying a green PR-mode gate. REMEDIATION ROUND 2 STATUS, unchanged and still OPEN: two independent adversarial verifiers reported this as a blocker and both required fixes were 'commit the receipt in the same commit as the production change'. Round 2 was instructed, like round 1, not to commit and not to push, so it could not discharge it either; the obligation is real, correctly reported, and belongs to the agent that lands the work. What round 2 DID do rather than leave it silent: re-ran node scripts/invariant-coverage-check.mjs --event push on the final tree (exit 0), re-ran --event pull_request --base-ref main (exit 1, same message, because git rev-parse HEAD is still 42e7d49 and merge-base..HEAD is empty), re-confirmed this path is staged with git diff --cached --name-only, and re-validated that the 12 row ids still set-equal docs/invariants/registry.json under the two-valued verdict domain after round 2's edits. The landing sequence owed is exactly the one both verifiers named: stage explicit paths only (never git add -A/./-a), confirm git diff --cached --name-only shows feat-m5-quiescent-exit.json, commit, then confirm --event pull_request --base-ref main exits 0"
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged twice over, and both censuses were exercised by execution rather than trusted. The identifier census: see B5, where it halted on Date, NaN and arguments and was resolved by narrowing the engine rather than by widening the classification, which is the resolution M2 demands (adding rows to quiet a halting census is the sampled-allowlist failure mode). The mirror census: MIRROR_CENSUS in mirror-guard.test.mjs is the closed census over lib modules, and this change touches THREE rows classified whole (leases.mjs, run-log.mjs and — added in remediation round 1, which is why an earlier draft of this row said two — recovery.mjs, whose resolveRunIdentity had to carry quiescentExitAt onto the published path) while deliberately leaving merge-watch.mjs, window.mjs and reconcile.mjs alone. Measured rather than assumed: node --test mirror-guard.test.mjs reports 46 pass / 0 fail after every twinned edit, and it was re-run immediately after each twin edit (the leases rewrite, the run-log delta gate, the recovery carry-over) specifically to catch a one-sided edit early. One pinned count IS introduced into an existing test and is named here so it is not mistaken for a change-detector: the FLAGSHIP run-away test's total dispatch count moves 20 to 21. That number is the run's whole bounded dispatch budget, not a proxy for it, and it moved because the quiescent exit genuinely costs one additional run-level agent round-trip. REMEDIATION ROUND 2 re-entered the SAME three whole rows and added none: run-log.mjs (quiescentExitDelta gains an outstanding field, applyRunDelta folds it) and recovery.mjs (resolveRunIdentity carries it), each applied to the mitosis.js twin in the same edit pass, with node --test mirror-guard.test.mjs re-run to 46 pass / 0 fail after the twinned edits and again on the final tree. The identifier census was re-run on the final tree as well: workflow-sandbox-census.test.mjs plus mirror-guard.test.mjs together report 75 pass / 0 fail, unchanged, because round 2 introduced no new host intrinsic"
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "engaged, and every inertness claim below was discharged by mutation run by this author, never by reading for absence. Termination, the load-bearing new property: measured on a single-unit fixture whose runUnit always returns Built, HEAD terminates at exactly 3 dispatches, deleting maxSteps alone runs to the 5000-call runaway guard, and deleting maxSteps while ADDING the quiescent exit ALSO runs to 5001, so the spec's own justification for the deletion is false and the epoch guard is what actually terminates the loop (2 dispatches). The existing leases.test.mjs termination test discriminates: green on the shipped shape, red on a no-epoch-guard mutant reporting 65 dispatches. New test 1, frontier redispatch: green on the shipped shape, red on the rejected isDispatchable-exclusion mutant with actual 'built' expected 'done'. New test 2, the continuation block: green on the shipped engine, red on a mutant that omits the continuation key ('a run that stops short of all-shipped reports a machine-readable continuation block'), and separately red on a mutant that hardcodes identity to 'published' ('the continuation reports the identity assembleReport was handed, never a hardcoded literal'), which is the specific assertion the contract required to redden. The deletion receipt: merge-poll-characterization.test.mjs was run via MITOSIS_PATH against a scratch PRE-deletion engine copy giving 5 pass / 0 fail, and against the POST-deletion engine giving 0 pass / 5 fail with every failure landing on its ENTRY GUARD (merge-watch and review-decision dispatch counts of 0, and the poll count 0 against expected 6), which proves the pinned behavior is GONE rather than relocated. Named candidly: there is no red-on-parent acceptance test for the quiescent exit itself, because there is no reported symptom here; the asserted behavior is new capability, and it is pinned by the tests above and below. REMEDIATION ROUND 1 — the section-11 latency emitter was STRUCTURALLY DEAD and wholly uncovered, and this row's earlier draft did not say so because nothing reddened. Adversarial review named it, and it was re-derived by execution before being acted on: a probe importing the real recovery.mjs and run-log.mjs showed the local journal fold carrying quiescentExitAt '2026-07-31T10:00:00Z', the local-only identity path carrying it, and the PUBLISHED path carrying undefined — so the engine guard saw null on every run whose manifest ref existed, which is every run M6 published, and the emitter could never fire. Two further causes were measured beyond the reported one: the dispatch's closing 'Return ONLY: { written, detail }' omitted elapsedSincePriorExit, the field the emitter reads (captured verbatim through the AsyncFunction harness), and the shipped test fixture had no case for the quiescent-exit-checkpoint label, so createFakeAgent THREW on it and the whole persist path degraded silently in every scheduler test (log line captured: 'durable quiescent-exit-checkpoint failed (\"fakeAgent: unhandled label quiescent-exit-checkpoint:run\")'). All three are fixed and the chain is now pinned by mutation, control 1681 pass / 0 fail over the engine test files: deleting the emitter -> RED; reverting the read guard to the old length-only check -> RED; dropping the returnContract override -> RED; dropping the JSON schema -> RED; restoring the verbatim-append directive -> RED; deleting the placeholder read-back warning -> RED; hardcoding continuation.status to 'awaiting-approval' -> RED; returning null from continuationBlock on all-shipped -> RED. On the lib side, against a scratch copy of the whole lib tree: removing the recovery.mjs carry-over -> RED; making applyRunDelta accept any at, or always record null -> RED; loosening isIsoInstant to any non-empty string -> RED. Every one of those eight engine mutants and four lib mutants SURVIVED before this round. REMEDIATION ROUND 2 — the emitter recorded a quiescent exit on EVERY run and the next advance attributed that gap to section 3.6's human-wait residual even when the prior exit was an all-shipped run with nothing parked and nothing awaiting approval, so the one number deliverable 6 exists to produce mixed human-wait latency with post-completion idle time and could not falsify the section 3.6 claim it was built to test. Re-derived before acting: the two SHIPPED tests are themselves the proof, since the dispatch-contract test already asserted exactly one quiescent-exit dispatch on an all-shipped twoIndependentMsps fixture and the latency test already asserted the section 3.6 line on that same all-shipped fixture. The reviewer's proposed fix (write the exit only when awaitingApproval is non-empty) was REFUTED by mechanism rather than adopted: .mitosis/run.json is an append-and-fold journal, so a skipped write leaves the PRIOR instant in the fold and the next advance re-reports an already-consumed gap, replacing one falsehood with another. What shipped instead carries the outstanding-ness with the instant: quiescentExitDelta({ at, outstanding }), applyRunDelta folding quiescentExitOutstanding, resolveRunIdentity carrying it from the LOCAL journal only (still out of PUBLISHED_RUN_FIELDS, since publishing it is the same status leak the instant is kept out for), and the emitter labelling the gap HUMAN-WAIT with the section 3.6 attribution only when the prior exit stopped with an MSP awaiting a human merge, POST-COMPLETION IDLE otherwise. Every gap is still logged, so deliverable 6 is intact; only the attribution is conditional. Pinned by mutation, control 186 pass / 0 fail on mitosis-scheduler.test.mjs: restoring the unconditional section 3.6 attribution -> RED; recording outstanding as always true at the call site -> RED; hardcoding the prior-exit outstanding read to true -> RED; each reddening exactly the new attribution test and nothing else. On the lib side, against a scratch copy of the whole lib tree: dropping quiescentExitOutstanding from applyRunDelta -> RED; dropping the recovery.mjs carry-over -> RED (2 fail / 65 pass). Full suite after round 2: npm test reporting tests 1845, pass 1845, fail 0. TERMINATION RESIDUAL, DECLARED HERE AND NOT CLOSED. The quiescent exit's own failure mode is not a red. Measured during the 0193 audit and NOT re-run in this commit, on the tree as it stood before the new test existed: deleting the quiescent exit from BOTH twins manifests as a full-suite HANG with ZERO tests reddened, which is strictly worse than a red because nothing names the broken invariant and nothing ever prints. The mechanism, stated by SYMBOL rather than by :line because every :line in this artifact is stated at 42e7d49 and this is code the change introduces, and re-derived by grep at the current HEAD before being restated here: the scheduler loop's sole await is joinTick's Promise.allSettled over the units it just dispatched, and on the exit-deleted shape that array is EMPTY, so the loop awaits Promise.allSettled([]), which yields MICROTASKS ONLY and never returns control to the macrotask queue that node:test's { timeout: N } option depends on, so no per-test timeout can fire on this hang, ever. The supporting numbers are the 0193 audit's rather than this commit's: 3,000,001 iterations over 1941ms with ZERO macrotasks, against a probe self-validated at 26 macrotasks in 140ms of real event loop, so that zero is a starved queue rather than a dead probe. Suite-level invisibility is compounded, also measured in the 0193 audit and not here: the loop's unbounded ticks.push starves the node --test output flush, which makes a LONGER kill deadline strictly WORSE rather than better, buying more memory pressure and less output instead of a diagnosis. WHAT THIS CHANGE ADDS: the new test at .claude/lib/superpowers-parallel/tests/quiescent-exit-bound.test.mjs, committed as 2b936d7, pins termination DETERMINISTICALLY with no timers and no sleeps. It counts resolutions of a live window accessor that the loop re-reads every iteration and throws once they pass a bound of 64, so a non-terminating loop fails as a named assertion instead of spinning. Measured here across four variants, each run 20 consecutive times against its own scratch copy of the lib tree outside the repository, 80 runs total gated only on process exit: control 20/20 green, a behaviour-preserving refactor 20/20 green (so it is not a change detector), the exit-removed mutant 20/20 red tripping at exactly 65 resolutions, and the epoch-guard-removed mutant 20/20 red at exactly 65, for ZERO deviations across all 80 runs. Full suite with the test present: npm test reporting tests 1846, pass 1846, fail 0, read as text from both a standalone run and the pre-commit hook's own re-run rather than inferred from an exit code. Coverage COMPOSES rather than duplicates: mirror-guard.test.mjs reddens on any ONE-SIDED twin mutation, and this lib-side bound covers the BOTH-TWINS mutation that mirror-guard alone passes. THE LIMIT, stated plainly and not softened: the full-suite EXIT CODE is NOT recovered by this change. This MSP DECLARES the hang; it does not fix it. The mutation receipts just above were taken with node --test on this one file against scratch copies, which is where the bound is proven; the full-suite behaviour of a both-twins deletion WITH this test present was not measured here and is not claimed. Recovering a non-zero exit code for the whole suite remains owed to a later MSP"
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "the invariant this MSP is most at risk of violating, and it carries a DECLARED COVERAGE LOSS that must not be read as an oversight. This change does combine deletions with a behavior change in one reviewable range, which is why the characterization test existed: merge-poll-characterization.test.mjs was written in a prior MSP precisely so this one could run it before and after and then delete it whole, and that pre/post receipt is recorded under M3. Deleting a characterization test alongside the mechanism it characterizes preserves the record; rewriting its assertions would launder it, and no characterization assertion was rewritten here. THE COVERAGE LOSS THAT WAS DECLARED IN ROUND 1 IS CLOSED IN ROUND 2, AND THE REASONING THAT DECLARED IT WAS WRONG. Round 1 deleted mitosis-scheduler.test.mjs's T14(c) — which pinned the fail-closed sha verification reached with requireSha=true from the in-run frontier built-redispatch path (mitosis.js:4591) — and declared the behavior un-re-plumbable, on this measurement: a probe engine throwing on any entry into that block left the suite at 1831 pass / 1 fail, the sole failure being T14(c) itself, so NO test reached the path after the poll deletion. That measurement was CORRECT and this author reproduced it exactly (throw probe over the whole lib suite: 1681 pass / 0 fail once T14(c) was gone; probe mechanism validated by planting the same throw in the isBuiltResume branch, which reddens 10 tests, so a silent probe was ruled out before the null result was believed). The STRUCTURAL claim built on top of it was FALSE: round 1 wrote that reaching the branch 'requires an awaiting parent to become done mid-run' and that 'the merge poll was the only in-run mechanism that could do that', and concluded the requireSha=true check was UNREACHABLE in-run. An adversarial reviewer independently reached the same conclusion and recommended deleting the branch and its requireSha parameter. Deleting it would have removed LIVE, REACHABLE, SECURITY-RELEVANT code. Refuted by execution rather than by argument, twice over. First, an exhaustive simulation driving the real runSchedule with mitosis.js's own Built rule (Built iff some parent is not yet in doneIds) over every DAG of up to four units, every terminal-outcome assignment and three window sizes: 0 redispatches out of 16263 fresh-run combinations, which is why the round-1 claim looked right; the same simulation extended with relaunch-seeded unit states found 9 reaching combinations. The detector was itself validated (a rule that returns Built then Done fires it), so the 0 was not a broken probe. Second, end-to-end through the real engine: an a -> b -> c relaunch with b seeded built (relaunchStateFor, mitosis.js:4274, returns 'built' for a manifest-built unit) has c build ahead behind b, then a and b both reach done IN-RUN, and c is redispatched — the run logs 'mitosis[c]: frontier-train — every parent reached done', a line emitted only from inside that branch. The missing ingredient was never the poll: it is a BUILT parent reaching done in-run, which a relaunch supplies and a fresh run cannot. T14(c) is therefore RE-PLUMBED in round 2 onto that surviving route (no dependency on the deleted poll), with a companion test asserting the sha-matching path still ships. Receipts: the throw probe now reddens 2 tests where it reddened 0, and disabling the requireSha guard itself (if (false) in place of the guard condition) reddens the fail-closed test specifically. Residual 0188 keeps its round-1 disposition — declared again, not closed — but its facts are now stated correctly: the requireSha=true path is REACHABLE and TESTED on the frontier redispatch, and it is the isBuiltResume restore (mitosis.js:4586, passing builtRef alone) that still verifies nothing. Also verified rather than assumed, because this MSP shipped AHEAD of M4: all nine 'AIMD window W=' assertions in frontier-train-e2e.test.mjs still pass (55 pass / 0 fail for that file), so this change did not reach into M4's diff"
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "engaged and anchor-dense, so every anchor acted on was re-derived against the working tree by this author with grep before it was edited, and none was trusted from the dispatch brief. Re-derived and CONFIRMED at 42e7d49: leases.mjs awaitingUnits :146, progressPossible :150, markMerged :156, markAwaitingMerge :161, runScheduleTick :165, the poll-cycle guard :185, runSchedule :206, and the classifyMergeWatch import at :2; mitosis.js twins at :2358, :2362, :2368, :2373, :2377, :2397, :2418 (a constant +2212 offset, as the brief stated); MERGE_POLL_MAX_CYCLES :4989, MERGE_POLL_WAIT_SECONDS :4990, MERGE_POLL_INTERVAL_SECONDS :4991, pollRepoIdentity :4992, REVIEW_DECISION_SCHEMA :4994, reviewDecisionPrompt :5004, readReviewDecision :5016, resolveReviewEvent :5028, persistWindowCheckpoint :5035, mergePoll :5057 with its mergeWatchPrompt call at :5068, and the runSchedule call site :5099-5108; assembleReport :3220 and its report literal :3225; fatalReport :124 and fatalReportShipped :3237; publishedSpecPath :570; computeLogicalRunId :349; run-log.mjs delta constructors :4/:8/:12 and applyRunDelta :25 with the mitosis.js twins at :782/:786/:790/:803. Three residuals named rather than assumed away. First, the brief's own citations for fatalReport (:130 vs the measured :124) and the run-log twin offset did not survive re-derivation exactly, and the measured values above are what was edited. Second, every :line in THIS artifact is stated against 42e7d49, the pre-change tree, because the change is uncommitted and post-change line numbers will shift on landing; remediation round 2 made that claim true rather than merely asserted, by re-deriving the two places that violated it (B6's five merge-watch anchors, and the run-log applyRunDelta cite below) against 42e7d49 with grep and restating them. Third, the brief asserted the mitosis-only deletion set was six symbols and the implementation contract corrected it to nine; nine is what was measured and deleted, and each of the three the brief omitted (pollRepoIdentity, REVIEW_DECISION_SCHEMA, reviewDecisionPrompt) was confirmed to have all its consumers inside the deleted region before removal. REMEDIATION ROUND 1 anchors, likewise re-derived with grep before being edited rather than trusted from the review, and stated at 42e7d49 like the rest of this row: recovery.mjs resolveRunIdentity's local-carry-over block at :377-379 (window, parked, harnessRunId) with its mitosis.js twin at :722-724 — both confirmed byte-identical at 42e7d49 and on the delivered tree, since the edit is an insertion after them — and run-log.mjs applyRunDelta at :25 with its mitosis.js twin (an earlier draft cited applyRunDelta :29-43 and claimed it was measured on the post-M5 tree; both halves were wrong, since applyRunDelta is at :25 at 42e7d49 and at :35 on the delivered tree, and the claim was also the sole exception that made this row's blanket 42e7d49 provenance statement self-contradictory). REMEDIATION ROUND 2 anchors: these are stated by SYMBOL rather than by :line, deliberately, because every one of them is code this change introduces and therefore has no 42e7d49 line to state — inventing a post-change number for them is what broke the two citations corrected above. The symbols edited in round 2 are quiescentExitDelta and applyRunDelta's quiescent-exit clause in run-log.mjs with their mitosis.js twins, resolveRunIdentity's local-carry-over block in recovery.mjs with its mitosis.js twin, and in mitosis.js alone the priorQuiescentExitAt read guard, persistQuiescentExitCheckpoint and its single call site after the schedule loop. Every twinned one was applied to BOTH copies before mirror-guard.test.mjs was re-run (46 pass / 0 fail). RESIDUAL DISPOSITIONS, all now declared in a tracked artifact rather than left dangling: 0187 carried FORWARD to M4 (spec section 10; verified M5's diff does not touch that log line); 0188 declared AGAIN, and its facts CORRECTED in round 2 — round 1 recorded them as worsened by an unreachable requireSha path, which measurement refuted (see the M4 row); the residual is now stated as what it actually is, a weaker check inside the isBuiltResume call that does happen, with the requireSha=true path reachable and tested elsewhere; 0190 CLOSED, because M5 deleted the :5068 mergeWatchPrompt call site that carried it, with the surviving 300/30-defaults fact named in the spec section 10 re-baseline; 0185 re-baselined by execution across the spec diff. An earlier draft closed 0190 in code while declaring it nowhere, which is precisely the dangling the contract forbade"
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "nothing is built, deployed, vendored or hand-patched by this change. .claude/skills/mitosis/templates/receipts.yml, scripts/invariant-coverage-check.mjs and .github/workflows/test.yml are all absent from git status --short. Every mutation receipt cited in M3 and M4 was produced without patching any committed artifact: each mutant is a separate scratch file written outside the repository under this session's own scratchpad subdirectory, selected at run time through the MITOSIS_PATH environment indirection the tests already read, and git status --porcelain was re-checked afterwards confirming that the only modified tracked paths are the intended ones and that no UNINTENDED path was modified or staged. CORRECTION, restated rather than deleted so the false claim stays on the record: that clause originally ended 'and that the staged set is empty', which was FALSE at the moment it was written, because the 12 intended paths WERE staged right then; what the check actually established is the unintended-path claim above, and the intended set has since been committed as ba981cc. Two residuals named rather than passed over. First, that same MITOSIS_PATH indirection resolves the engine under test from the environment, so a stale or redirected value in a CI environment would silently characterize a different engine than the committed one, and nothing asserts the resolved path is the repo's own. Second, the pre-existing untracked .bak-pre-promptsfix-043a2526 copies of mitosis.js and engine-args.mjs remain in the tree and are exactly the hand-patched-deployed-file shape M6 forbids; they predate this change and were deliberately left untouched, and they are named here rather than silently tolerated"
    }
  ]
}
