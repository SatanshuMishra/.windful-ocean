{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "The change is two paths and neither is executable. `git diff --name-only -M --diff-filter=d da0cefd..HEAD` (da0cefd is the merge-base with main) lists exactly `.claude/CLAUDE.md` and `.claude/skills/pr/SKILL.md`, the second a rename of `.claude/commands/pr.md` at similarity index 54; `git ls-tree -r --name-only da0cefd -- .claude/commands/` shows that file was the only entry in that directory, which is why the directory no longer exists. Grepping the full diff body for DONT_CONTEXTIFY|createContext|runInContext|node:vm|SandboxViolationError|SANDBOX_VIOLATION|VALUE_GLOBALS|ALWAYS_DENIED|ALLOWED_GLOBALS|compileWorkflow returns 0 matches, and workflow-sandbox.mjs, the only module that constructs the realm, is absent from the path list. No realm global is constructed, replaced or reconfigured here, and no identifier reachable from a workflow body is added."
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "The same 0-match grep recorded under B1 covers SandboxViolationError and SANDBOX_VIOLATION over the whole diff body, so no denial path is touched and no proxy trap, ownKeys filter, freeze or seal changes shape. Both changed files are markdown consumed as agent guidance: nothing in the repository imports, compiles or evaluates either of them, so this range adds no throw site on any sandbox path, tagged or untagged, and no previously-tagged violation can degrade into a host error a workflow could catch as its own."
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "Guarded-intrinsic membership is declared in workflow-sandbox.mjs, which is not among the two changed paths, and none of workflow-sandbox-policy.test.mjs, workflow-sandbox-traps.test.mjs or workflow-sandbox.test.mjs is changed either. I checked de-collection as well as edits, because a denial assertion that silently stops running proves nothing: package.json is absent from the diff path list, so the three test globs at package.json:7 are byte-unchanged, and `.claude/lib/superpowers-parallel/tests/` still holds all four workflow-sandbox*.test.mjs files the first of those globs expands to."
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS each return 0 matches in the diff body under the grep recorded in B1, and both workflow-sandbox.mjs and workflow-sandbox-policy.test.mjs are absent from the two-path diff. Nothing here empties, shrinks or bypasses a policy constant, and per the collection check in B3 the assertions that would turn red if one were emptied are still collected by package.json:7."
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "workflow-sandbox-census.test.mjs is not in the diff, so no census row is added, removed or reclassified and no new host-reachable identifier enters the workflow-body position. The de-collection route is closed by the same package.json:7 check recorded under B3: no glob is removed or narrowed, so the census still runs and still halts on an unclassified identifier. Whether any census enumerates the newly added skill file is a separate question and is answered under M2."
    },
    {
      "id": "B6",
      "verdict": "not-threatened",
      "check": "The production caller is intact and I read it in the live tree: `.claude/lib/superpowers-parallel/mitosis-gate.mjs:1` is `import { compileWorkflow } from './workflow-sandbox.mjs';`. mitosis-gate.mjs is not among the two changed paths, and this range changes no lint artifact of any kind: dead-export-lint.test.mjs, mirror-guard.test.mjs and no-self-merge-consent.test.mjs are all absent from the diff. Sandbox liveness therefore still rests on the same real caller it rested on at da0cefd, with no self-referential substitute introduced."
    },
    {
      "id": "M1",
      "verdict": "not-threatened",
      "check": "Satisfied by this artifact. It records a verdict for all 12 registry ids, B1-B6 and M1-M6, and is committed to the repository rather than argued in the pull request body; I read PR #39's body and it carries no invariant verdict and makes no coverage claim. It exists because `scripts/invariant-coverage-check.mjs:175-177` fails any pull_request event when no path under `docs/invariants/coverage/` is added or modified between base and HEAD, and this branch touched zero paths there. The asymmetry in the check list is the same defect seen from both sides: the push-triggered run 30947234397 passed because it has no base ref and runs in push mode, while the pull_request-triggered run 30948247089 failed, with every other check green on both. The filename follows the branch-derived convention (feat/pr-guidance-global -> feat-pr-guidance-global.json) shared by the 20 pre-existing entries."
    },
    {
      "id": "M2",
      "verdict": "not-threatened",
      "check": "Engaged, and checked by execution rather than by reading a regex. The diff rewrites the one command string the environment's command classifier must exempt: `.claude/CLAUDE.md:12` and the fenced block at `.claude/skills/pr/SKILL.md:11-17` both now name `node ~/.claude/lib/superpowers-parallel/mitosis-git.mjs pr-create`, where the parent revision named the repo-relative `.claude/lib/...` form. If the selfwrap exemption at `.claude/hooks/block-destructive-bash.sh:43` did not classify the new form, this change would document a command the gate denies. I fed the hook the fenced block read verbatim out of the file, both as written with line continuations and collapsed to one line: the hook emitted nothing, ALLOW, in both cases. The parent revision's fenced block, read from `git show da0cefd:.claude/commands/pr.md`, is ALLOW too, and a control ad-hoc forge-CLI create string is deny. The classifier itself is unchanged, being absent from the diff, and this range introduces no pinned count and no sampled allowlist. On whether the new skill file needs a census row: exactly four collected suites enumerate a directory (`grep -rln readdirSync` across the three roots in package.json:7), and dead-export-lint.test.mjs:7, mirror-guard.test.mjs:7 and no-self-merge-consent.test.mjs:11 all scan only `.claude/lib/superpowers-parallel/`. The fourth, entrypoint-guard-lint.test.mjs:8, does scan `.claude/skills`, but it filters to ['.mjs', '.js', '.cjs'] at :11 through the endsWith test at :21, so a .md file is never enumerated, and it classifies by content pattern rather than by a per-file row, so there is no row for this file to be missing from. One pre-existing pinned count is named rather than passed over: PR_CREATE_SITES = 2 at gh-scope-lint.test.mjs:17, consumed at :90, :97 and :154; this change neither introduced nor widened it."
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "Threatened, stated plainly rather than argued away. This commit is remediation of a reported failure mode, a model reaching for an ad-hoc creation path before it ever finds the centralized tool, and it ships no acceptance test that is red on da0cefd and green here. There is no deterministic assertion surface for it in this suite: the payload is agent-facing guidance, nothing reads either file at test time, and the suite totals are identical on both sides of the change. The closest deterministic evidence available is the gate probe recorded under M2, and it is explicitly NOT a red/green pair, because the parent revision's documented invocation is ALLOW as well; the probe demonstrates preservation, not a fixed symptom, and no inertness mutation was run. What that leaves unpoliced: nothing asserts that `.claude/CLAUDE.md` carries the pull-request bullet, that `.claude/skills/pr/SKILL.md` exists, or that either names an invocation the gate exempts, so a regression on any of the three would be silent in CI."
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "Threatened, and the condition M4 guards is genuinely present. This single reviewable range combines a structural move, `.claude/commands/pr.md` to `.claude/skills/pr/SKILL.md` at similarity index 54, with a content change inside the moved file: the frontmatter gains `name: pr` at :2 and a rewritten description at :3, and the tool path changes from repo-relative to home-relative at :11 and again at :22, alongside the new bullet at `.claude/CLAUDE.md:12`. No characterization test pinning the surviving content was written before the move, and none exists, because no suite reads either file. I checked the surviving behavior by hand instead, through the probe recorded under M2, which shows the invocation exempted by the gate on both sides of the move; an ad-hoc probe run once is not the committed characterization test M4 requires, and it covers only the invocation line, not the frontmatter or the rest of the file."
    },
    {
      "id": "M5",
      "verdict": "not-threatened",
      "check": "Every path:line in this file was read in the live tree at the moment it was written, and copying was actively avoided rather than merely disclaimed. The demonstration is concrete: the most recent sibling entry, refactor-relocate-pr-tool.json, cites PR_CREATE_SITES at gh-scope-lint.test.mjs:20, but on this branch `grep -n PR_CREATE_SITES` puts it at :17, so carrying that citation across would have been wrong and this file cites :17. Each of the following was opened here: scripts/invariant-coverage-check.mjs:175-177 (the empty-touched failure), .github/workflows/test.yml:23-32 (the invariant-coverage job and its bare invocation), package.json:7 (the three test globs), .claude/hooks/block-destructive-bash.sh:43 (selfwrap), .claude/lib/superpowers-parallel/mitosis-gate.mjs:1 (the sandbox import), entrypoint-guard-lint.test.mjs:8 and :11 and :21 (scan roots, extension list, filter), dead-export-lint.test.mjs:7, mirror-guard.test.mjs:7, no-self-merge-consent.test.mjs:11, gh-scope-lint.test.mjs:17 and :90 and :97 and :154, protect-claude-config.test.mjs:16 and :183 and :194, .claude/CLAUDE.md:12, and .claude/skills/pr/SKILL.md:2, :3, :11-17 and :22. The two paths the diff itself cites were resolved as well: `~/.claude/lib/superpowers-parallel/mitosis-git.mjs` and `~/.claude/rules/common/git/pull-requests.md` both exist and `realpath` resolves each into this repository."
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "No gate-enforcing artifact is built, refreshed, vendored or hand-patched here: scripts/, .github/ and .claude/hooks/ return zero matches against the two-path diff list, and .claude/skills/mitosis/templates/receipts.yml is absent from it too. The clause this change does engage is the deployed-copy one, because the diff redirects the documented invocation off the repo-relative path and onto the installed `~/.claude/...` path, which would be a defect if the installed tree were hand-maintained copies. It is not: `realpath` resolves ~/.claude/skills/pr/SKILL.md, ~/.claude/lib/superpowers-parallel/mitosis-git.mjs, ~/.claude/rules/common/git/pull-requests.md, ~/.claude/hooks/block-destructive-bash.sh and ~/.claude/CLAUDE.md each to the corresponding file inside this repository, through directory symlinks for lib, skills and rules/common and per-file symlinks for the hook and CLAUDE.md, so the deployed artifacts ARE the committed source rather than copies of it. One exception is disclosed rather than glossed, and it is pre-existing: ~/.claude/settings.json is a real home-tree file whose realpath is itself, so the deployed permissions.deny is not the committed .claude/settings.json. That is exactly what the one failing test in the suite reports, protect-claude-config.test.mjs:194, and since its INSTALLED root is `join(homedir(), '.claude')` at :183 the assertion reads only the home tree and is branch-independent; I did not check out main to re-run it. This change neither causes nor fixes it, and .claude/settings.json is one of the pre-existing dirty working-tree paths deliberately excluded from this commit. The two untracked .bak-pre-promptsfix-043a2526 copies of mitosis.js and engine-args.mjs also remain in the tree and are the hand-patched-deployed-file shape M6 forbids; they predate this branch and are carried forward unchanged."
    }
  ]
}
