{
  "unit": "feat/config-entry-link-cutover",
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "`git diff --name-only origin/main` returns nine paths - .claude/hooks/protect-claude-config.sh, .claude/hooks/tests/protect-claude-config.test.mjs, and seven under scripts/config/ (manifest.mjs, paths.mjs, promote.mjs, receipt.mjs, release.mjs, tests/manifest.test.mjs, tests/promote.test.mjs) - plus two untracked files, scripts/config/cutover.mjs and scripts/config/tests/cutover.test.mjs. No changed path matches 'sandbox'. Grepping the whole diff and both untracked files for DONT_CONTEXTIFY, createContext, runInContext, node:vm and SandboxViolation returns 0 hits. No realm is constructed anywhere in this unit; every module is an ESM file the host imports directly with explicit node: builtin imports."
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "No sandbox denial mechanism is added or edited, and the unit constructs no proxy, no ownKeys filter and no freeze/seal barrier. The new verb signals failure by return rather than by raise across its whole public surface: planCutover (scripts/config/cutover.mjs:147), applyCutover (:458) and rollbackCutover (:583) each return a {status:'error', errors:[...]} record, the throws in placeLink (:392) and moveAside (:418) are caught by relink at :450, and restoreEntry is wrapped per entry at :587-591. Disclosed rather than claimed clean: lstatOrNull at :64-71 rethrows every non-ENOENT error and planCutover's call into classifyEntry at :185 is not wrapped, so an EACCES on a live entry escapes main (:715) as an uncaught host stack trace. That is an error-handling gap, not a sandbox one - B2's subject does not exist in this unit."
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "No guarded intrinsic and no member enumeration on an intrinsic is touched. The nearest shape is ENTRY_ACTIONS at scripts/config/cutover.mjs:47-52, the only object in this unit indexed by a token rather than searched. Both index sites were enumerated by grep: relink at :446 and describePlan at :673. Both read entry.state off records produced by classifyEntry (:117-127) or classifyNotes (:129-145), which return only the four frozen literals 'absent', 'real', 'already-linked' and 'link'. The journal-read path never indexes it - restoreEntry at :568-581 compares entry.state with ===, and its input is first gated by journalShapeErrors (:210-233) against ENTRY_STATES. A '__proto__' or 'constructor' state therefore cannot reach the lookup. Every other table the unit adds is a frozen array searched with Array.includes: ENTRY_STATES (:43), ENTRY_RECORDS (:45), PROMOTED_ENTRIES (scripts/config/paths.mjs:18-29), WITHDRAWN_GRANTS (scripts/config/manifest.mjs:32)."
    },
    {
      "id": "B4",
      "verdict": "threatened",
      "check": "Every constant this unit adds was mutation-probed against the shipped suites, each mutation applied to the live file, the suite run, and the file restored from a byte copy. Baselines: `node --test scripts/config/tests/*.test.mjs` is 137 pass / 0 fail, `node --test .claude/hooks/tests/protect-claude-config.test.mjs` is 37 pass / 0 fail, so load-bearing means fail>0. Load-bearing: ENTRY_STATES emptied -> fail=9; ENTRY_RECORDS emptied -> fail=9; DECLARED_SETTINGS_PATH (scripts/config/release.mjs:13) redirected to .claude/nope.json -> fail=7; NOTES_DIRNAME 'notes'->'notas' -> fail=6; CUTOVER_JOURNAL_FILENAME 'CUTOVER'->'CUTOVER2' -> fail=5; WITHDRAWN_GRANTS emptied -> fail=3; withdrawGrants (scripts/config/manifest.mjs:64-67) reduced to the identity -> fail=3; CUTOVER_ASIDE_PREFIX '.pre-cutover-'->'.x-' -> fail=3; ENTRY_ACTIONS emptied -> fail=2; NOT_ADOPTED_GRANTS emptied -> fail=2. That last pair settles decision 0288 mechanically rather than by assertion: NOT_ADOPTED_GRANTS (scripts/config/manifest.mjs:30) and WITHDRAWN_GRANTS (:32) hold identical membership today, yet emptying them turns disjoint assertion sets red (2 and 3), so they are not duplication. In the hook: removing note_repo_dir(receipt_repo_root(home_base)) at .claude/hooks/protect-claude-config.sh:80 -> fail=2; receipt_byte_limit 65536->1 (:24) -> fail=2; receipt_name \"LIVE\"->\"LIVE2\" (:23) -> fail=2. Inert, which is why this row is threatened: CUTOVER_STAGING_SUFFIX (scripts/config/paths.mjs:11) '.cutover.tmp'->'.zz.tmp' -> fail=0; CUTOVER_JOURNAL_VERSION (scripts/config/cutover.mjs:41) 1->2 -> fail=0; SETTINGS_INDENT (scripts/config/promote.mjs:39) 2->4 -> fail=0; UNRECOVERED (scripts/config/promote.mjs:51) emptied -> fail=0. One narrowing is inert the same way: re-adding \"settings.json\" to the probes tuple at .claude/hooks/protect-claude-config.sh:22, undoing this unit's narrowing, leaves the hook suite at 37 pass / 0 fail. Four named constants and one narrowing ship with no assertion behind them."
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "No code is added in the workflow-body position, so the census of host-reachable bare identifiers is unchanged. Every module in this unit is an ESM file node imports directly with explicit node: builtin imports (scripts/config/cutover.mjs:1-35), executed by the host, never evaluated inside the engine's sandboxed workflow scope. The one non-JS surface, the python program embedded in .claude/hooks/protect-claude-config.sh:8-128, runs as its own process under python3 -c and reaches no engine scope either."
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "scripts/config/cutover.mjs has no production caller. Grepping the worktree for 'cutover' returns hits only inside its own two files, an unrelated mkdtemp prefix at .claude/hooks/tests/protect-claude-config.test.mjs:266, and prose in .claude/docs/superpowers/ about a different ledger migration. It is also absent from BOOTSTRAP_ENTRIES (scripts/config/paths.mjs:31, which is ['promote.mjs','converge.mjs']), so install-bootstrap.mjs never deploys it to local/ and it can only be run out of a checkout. The chain it belongs to is dormant on this host: `ls -ld` reports ~/.claude/current and ~/.claude/LIVE both absent, ~/.claude/hooks a real directory, and ~/.claude/settings.json a real 0600 file. The release system has therefore never been cut over here, promote.mjs's inherited threat stands unchanged, and this unit adds a second verb whose liveness is proven only by its own 20 tests in scripts/config/tests/cutover.test.mjs."
    },
    {
      "id": "M1",
      "verdict": "not-threatened",
      "check": "Verdicts for all 17 registry ids read from docs/invariants/registry.json (B1-B6, M1-M6, G1-G5) are recorded in this committed file and nowhere else. The task brief withholds commit, push and PR creation, so no PR body exists that could carry a verdict table instead. The shape is the `rows` array required at scripts/invariant-coverage-check.mjs:126-129, carrying the id/check/verdict fields rowErrors demands at :110-121, with verdicts drawn from ALLOWED_VERDICTS at :10."
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "Two censuses in the new verb are genuinely closed and halt on the unclassifiable. journalShapeErrors at scripts/config/cutover.mjs:210-233 rejects any journal entry whose state falls outside ENTRY_STATES or whose record falls outside ENTRY_RECORDS, so an unknown state stops the rollback rather than falling through restoreEntry's default branch at :580. notesPlan at :340-349 refuses a notes source holding any non-regular-file entry rather than skipping it, pinned by 'a notes source holding a subdirectory is refused rather than partly copied' at scripts/config/tests/cutover.test.mjs:404. The path census is where the row fails. cutoverWritePaths at :93-106 is a hand-declared enumeration of six write kinds; nothing derives it from the verb's actual write sites, so a write site added later is invisible to the plan-time refusal in containmentErrors at :108-115. The test guarding it, 'every write path the verb uses is enumerated and proven inside the config root' at scripts/config/tests/cutover.test.mjs:262, asserts the six kind names by deepEqual at :267-270 - a pinned list, which is exactly the change-detector shape this invariant forbids. What actually carries containment is eleven independent isInside guards at the write sites themselves (scripts/config/cutover.mjs:110, :278, :330, :352, :361, :371, :395, :421, :537, :571, :606), each of which halts; those are hand-added too and no census discovers them. A third case is the hook's discovery ladder - receipt_repo_root (.claude/hooks/protect-claude-config.sh:63-78), then the resolved home base, then the probes tuple at :22 - which has no halt branch and exits 0 when every layer misses, by design and disclosed here rather than treated as closed."
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "Two of the three changes ship a verified red-on-parent test; the third cannot, and the inertness clause fails outright. Hook fix, verified: with .claude/hooks/protect-claude-config.sh and its test both replaced by their origin/main versions the suite runs 14 pass / 1 fail, the failure reading '/Users/satanshumishra/.claude/settings.json no longer points out of the home tree, so the guard discovers no repository base and stops protecting the checkout' - the exact symptom the previous docket recorded as M6 corroboration - and it is 37 pass / 0 fail on this branch. Settings reconciliation, verified: with promote.mjs, manifest.mjs, release.mjs, paths.mjs and receipt.mjs replaced by their origin/main versions, this branch's promote.test.mjs and manifest.test.mjs run 20 pass / 13 fail, and the restored tree runs 137 pass / 0 fail. Cutover verb, not verified and not verifiable in this form: against those same origin/main sources scripts/config/tests/cutover.test.mjs does not run at all, dying on `SyntaxError: The requested module './paths.mjs' does not provide an export named 'CUTOVER_ASIDE_PREFIX'`. That is an import crash, not a red assertion. It is a net-new verb with no parent-commit symptom to assert, so it carries no red-first receipt and is recorded as lacking one rather than credited with a green suite. The inertness clause fails independently: four constants and one narrowing move no assertion when mutated (see B4)."
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "The unit is one undivided reviewable range. `git log --oneline origin/main..HEAD` is empty and `git rev-parse HEAD` equals origin/main at a302e4c, so 878 insertions across nine tracked files plus 1209 lines in two untracked files sit in the working tree as a single block holding four distinct kinds of change: a net-new subsystem (scripts/config/cutover.mjs), a behavior change in promote() and rollback() (scripts/config/promote.mjs:255 and :337), a bug fix to a live guardrail (.claude/hooks/protect-claude-config.sh), and mechanical refactors riding inside the same functions - the settingsPathIn(configRoot) substitution inside promote and the discardStaging extraction at scripts/config/receipt.mjs:82-95. The surviving behavior was carried through rather than pinned first: `git diff origin/main -- scripts/config/tests/promote.test.mjs | grep -c '^-'` returns 5, four of which are the same rollback({configRoot, now}) call site rewritten for the widened signature at scripts/config/promote.mjs:337, so no prior assertion was deleted - but no characterization test was written before the refactor either, which is what this invariant asks for."
    },
    {
      "id": "M5",
      "verdict": "not-threatened",
      "check": "Every path:line in this record was re-derived against this worktree at HEAD immediately before it was written, never carried from an earlier docket. The constant and function anchors in scripts/config/cutover.mjs, paths.mjs, manifest.mjs, release.mjs, promote.mjs and receipt.mjs came from one `grep -n` pass per file; the .claude/hooks/protect-claude-config.sh anchors (:22, :23, :24, :63, :80) from another; the isInside site list under M2 from `grep -n 'isInside(' scripts/config/cutover.mjs`; the ENTRY_ACTIONS index sites from `grep -n 'ENTRY_ACTIONS\\[' scripts/config/cutover.mjs`. The three anchors this record cites in a file the unit does not change (.claude/hooks/block-destructive-bash.sh:17, :123, :169-172, :187) were read directly from that file. Every count quoted here - the 137/0 and 37/0 baselines, each mutation's fail count, the 14/1 and 20/13 red-before runs, the 47-entry deny comparison, the gate probe verdicts - is the output of a command run against this worktree during this session."
    },
    {
      "id": "M6",
      "verdict": "threatened",
      "check": "Narrowed by this unit but not closed. Narrowed: the live settings.json was previously reproduced by nothing, and promotion now recomputes it from committed source at a recorded sha - declaredSettings reads `<sha>:.claude/settings.json` through git ls-tree and git show at scripts/config/release.mjs:41-64, reconcileSettings folds it against live at scripts/config/promote.mjs:118-131, and applySettings writes it at :193, called from promote at :305 and from rollback at :404. The repo-owned section is therefore regenerable: REPO_OWNED_SECTIONS at scripts/config/manifest.mjs:27 is ['deny'], and the repo's and live's deny lists were compared entry by entry and are set-equal today (47 each, empty symmetric difference), so a hand-patched deny entry would be reverted at the next promote. Not closed, on four counts. The live-owned keys (LIVE_OWNED_KEYS, scripts/config/manifest.mjs:9) are preserved verbatim and reproduced by nothing. settings.json is still stripped from every release by stripSettings (scripts/config/release.mjs:15-22), so the deployed file carries no version of its own - decision 0287's intent, and still M6's shape. The CUTOVER journal and every `.pre-cutover-<sha8>` aside are deployed state with no recorded sha at all. And cutover.mjs, the artifact that relocates the guardrail files, is not in BOOTSTRAP_ENTRIES, so no deployed copy of it exists to version or refresh. Corroborating on this host: ~/.claude/current and ~/.claude/LIVE are both absent while ~/.claude/hooks/block-destructive-bash.sh and ~/.claude/hooks/protect-claude-config.sh are symlinks into this repo - the running gate is a live checkout, not a versioned build."
    },
    {
      "id": "G1",
      "verdict": "not-threatened",
      "check": "No merge path is added or weakened. Grepping the whole diff and both untracked files for `gh pr`, `gh api`, graphql, mergePullRequest, curl, wget and fetch( returns exactly one hit: the string 'Bash(gh pr merge:*)' used as a deny-list fixture in scripts/config/tests/promote.test.mjs. The only child-process shapes the unit adds are `git ls-tree` and `git show` at scripts/config/release.mjs:46 and :52. Recorded rather than passed over: promotion now writes the live permissions.deny list from the repo's declaration (REPO_OWNED_SECTIONS at scripts/config/manifest.mjs:27, applied at :122-125), and that list is where G1's 'Bash(gh pr merge:*)' and mcp__github__merge_pull_request denies live. As it stands that is a strengthening - the two lists were compared and are identical, so the next promote reproduces them exactly. The exposure it creates is that a future narrowing of the repo's deny list would silently narrow live's, leaving the origin-agnostic Bash gate at .claude/hooks/block-destructive-bash.sh:129 as the only surviving layer."
    },
    {
      "id": "G2",
      "verdict": "not-threatened",
      "check": "Nothing in this unit reads, composes or rewrites a pull request title or body, and no changed path touches .claude/lib/git/pr.mjs or .claude/hooks/block-destructive-bash.sh - `git diff --name-only origin/main` lists nine paths, two under .claude/hooks/ (protect-claude-config.sh and its test) and seven under scripts/config/, and the two untracked files are both under scripts/config/. The one indirect reach is the one recorded under G1: the denies that carry G2 (mcp__github__update_pull_request and its plugin twin, and the gh pr edit shapes) sit in permissions.deny, which promotion now rewrites from committed source. Verified present and identical on both sides today, so no G2 deny is dropped by this change as written."
    },
    {
      "id": "G3",
      "verdict": "not-threatened",
      "check": "This unit adds no PR-creating surface, and the task brief withholds commit, push and PR creation, so no pull request is opened from this branch by any path. The centralized `node .claude/lib/git/pr.mjs pr-create` route is neither bypassed nor modified. The deny entries backing G3 - 'Bash(gh pr create:*)', mcp__github__create_pull_request, mcp__github__create_pull_request_with_copilot and their plugin twins - were read out of both the repo's .claude/settings.json and the live ~/.claude/settings.json and are present in both, so the promotion rewrite recorded under G1 preserves them."
    },
    {
      "id": "G4",
      "verdict": "threatened",
      "check": "Threatened by design and confirmed by probe. PROMOTED_ENTRIES at scripts/config/paths.mjs:18-29 is ['skills','agents','lib','workflows','hooks','rules','docs','sounds','CLAUDE.md','keybindings.json'] - a superset of every name the Write/Edit guard treats as a guardrail file (exact and prefixes at .claude/hooks/protect-claude-config.sh:20-21) and of every name the Bash gate's guardpath matches (.claude/hooks/block-destructive-bash.sh:17). applyCutover (scripts/config/cutover.mjs:458) relocates a real ~/.claude/hooks to ~/.claude/hooks.pre-cutover-<sha8> through moveAside's renameSync at :430 and puts a symlink to current/hooks in its place through placeLink at :392. On this host that container holds the running gate: ~/.claude/hooks/block-destructive-bash.sh and protect-claude-config.sh are both symlinks into this repo. Probed at HEAD by feeding the gate a PreToolUse payload: `mv ~/.claude/hooks ~/.claude/hooks.pre-cutover-abc` returns permissionDecision 'ask' with reason 'Destructive command (shell write to Claude Code guardrail file)', while `node \"$HOME\"/.claude/local/cutover.mjs apply` and `node scripts/config/cutover.mjs --apply --config-root \"$HOME\"/.claude` both return empty output, which is allow. The same relocation is escalated through one verb and silent through the other, because guardverb at .claude/hooks/block-destructive-bash.sh:123 enumerates shell verbs (>, tee, sed -i, mv, cp, rm, chmod, truncate, perl -i, git checkout/restore) and an in-process renameSync is none of them; the Write/Edit guard cannot see it either, since it only inspects tool_input.file_path. The same gap covers promotion, which now writes ~/.claude/settings.json at scripts/config/promote.mjs:305 - a file still named in the guard's exact tuple at .claude/hooks/protect-claude-config.sh:20, confirmed still escalating on a direct Write payload, but reached here in-process. What is real and worth crediting, and is why the risk is bounded rather than open-ended: nothing is deleted (decision 0290) - moveAside renames and refuses outright if the aside already exists (:424-426), planCutover refuses when an earlier aside occupies the target (:191-196), plan is the default verb (DEFAULT_VERB at :633) so apply must be asked for explicitly, every write site is containment-checked against the config root, and the journal makes the move reversible (rollbackCutover at :583), pinned by 'a real directory in the way is moved aside intact, never deleted' at scripts/config/tests/cutover.test.mjs:104 and 'rollback restores a prior link, a moved-aside directory, and removes a created link' at :142. The withdrawal also narrows one live grant in the safe direction: 'Bash(ln -sfn:*)' is present in the live allow list and absent from the repo's today, so the next promote strips it (WITHDRAWN_GRANTS at scripts/config/manifest.mjs:32, withdrawGrants at :64-67). None of that satisfies G4's stated test, which is that the action be blocked or made deliberately harder; the verb makes it safer and reversible, which is a different property, so the row is threatened rather than clean."
    },
    {
      "id": "G5",
      "verdict": "not-threatened",
      "check": "G5's stated escalation condition is not met: the unit invokes no network-named command. The only commands it adds are `git ls-tree` and `git show` at scripts/config/release.mjs:46 and :52, neither of which appears in the gate's netbin list at .claude/hooks/block-destructive-bash.sh:187, and no argument either passes is credential-shaped. Two things are disclosed rather than claimed harmless. First, trust in the LIVE receipt's repo_root field widens to two new consumers in this unit: the Write/Edit guard reads it to derive the repository base (.claude/hooks/protect-claude-config.sh:63-80), and rollback hands it straight to declaredSettings, which runs git with `-C <that path>` (scripts/config/release.mjs:41-46). receiptShapeErrors at scripts/config/receipt.mjs:28-45 validates repo_root only as a non-empty string; the hook adds an os.path.isabs check at :76 and rollback adds an existsSync check, but neither constrains where it may point, so a writable LIVE receipt chooses the directory git runs in. Second, the silent-pass finding under G4 arguably touches G5's second clause, that an unrecognized command shape escalates to ask rather than passing silently - the `node .../cutover.mjs apply` shape passes silently. It is recorded once under G4 rather than double-counted here."
    }
  ]
}
