{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "the change is two added files and carries zero production change: git diff --name-only cad6ba2 -- .claude/workflows/mitosis.js .claude/lib/superpowers-parallel/leases.mjs .claude/lib/superpowers-parallel/reconcile.mjs printed nothing, and git status --porcelain lists none of the three, so no realm is constructed, removed or reached. grep over the added test finds zero occurrences of vm., DONT_CONTEXTIFY and createSandbox, and no workflow-sandbox file appears in git status --short. Named candidly rather than left implicit: the file DOES compile the engine body with the host realm's AsyncFunction constructor (merge-poll-characterization.test.mjs:27-28), deliberately running mitosis.js in the host realm OUTSIDE the sandbox, and the mutation work ran that same construction six more times over scratch engine copies. That is the harness pattern it inherits verbatim from frontier-train-e2e.test.mjs:26-28 and reconcile-only-advance-characterization.test.mjs:26-27, and it means the file proves nothing about B1 in either direction while leaving the production realm untouched"
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no denial path is added, removed or reached. grep over the added file finds zero occurrences of SandboxViolationError, and no workflow-sandbox file is in the change set. The only failures the new code can raise are node:assert assertions thrown in the test process by its five tests, plus the fixture's own deliberate entry guard, which throws a plain Error naming an unexpected stage label (merge-poll-characterization.test.mjs:257) inside the harness agent rather than inside any sandbox mechanism. The one catch-shaped construct the file relies on is the harness parallel shim at :30, which swallows a rejected thunk into null; it is inherited verbatim from the sibling e2e harness and governs the test's own dispatch, so no denial gains an untagged host-error route that a workflow could catch as its own"
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not read member by member for this entry. What was read is the whole change surface, which is one added test file and one added coverage entry, adding no member to and removing no member from any policy list, with every workflow-sandbox file absent from git status --short. No guarded intrinsic therefore gains or loses a denied member. The receipt is the full suite executed by this author on this tree: npm test reporting tests 1843, pass 1843, fail 0, which carries the existing guarded-intrinsic assertions unchanged, against a measured 1838 pass / 0 fail on the same tree with the new file temporarily moved aside"
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "checked by grep rather than assumed: VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS occur zero times in the added file, and no file defining or asserting them appears in git status --short. Emptying any of the three would not move an assertion in this file either, since its five tests assert only over dispatched stage labels (merge-watch:l2, review-decision:l2, window-checkpoint:l2, window-checkpoint:shepherd) and over two captured prompt strings, none of which reads sandbox policy. Load-bearingness stays proven exactly where it was proven before, and the 1843 pass / 0 fail suite carries those proofs unchanged"
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "re-checked rather than defaulted. mitosis.js IS the workflow-body position and it receives no line in this change, verified by an empty git diff --name-only against cad6ba2 for that path plus its absence from git status --porcelain, so no new free identifier or bare global reaches the host and the existing classification rows stay exhaustive. The new file does name the six workflow-body parameters when it constructs its harness runner (args, agent, parallel, log, phase, workflow at :28), but those are the engine's own declared parameter list, bound as parameters of a host-realm function rather than left free to resolve; the census reads the engine source, which is byte-unchanged. The six mutant engines are scratch copies written outside the repo and are never read by any census. Candidly: the census rows were not read one by one, and the receipt that the census still closes is the 1843 pass / 0 fail suite"
    },
    {
      "id": "B6",
      "verdict": "not-threatened",
      "check": "checked rather than assumed, because the new file DOES import a lib export and could in principle manufacture liveness for it. It cannot. dead-export-lint computes liveCallerCount as matches in mitosis.js plus sibling lib modules plus the defining module's own masked text (dead-export-lint.test.mjs:66-71), and its module set comes from libModuleNames(), a non-recursive readdirSync over the lib directory filtered to entry.isFile() and a .mjs suffix (:12-17). tests/ is a directory entry and is excluded, so no test file counts anywhere in that computation. The one export this change imports, computeLogicalRunId (declared recovery.mjs:4, imported at the new file's :4), already carries production liveness independent of this file: grep finds it twice in mitosis.js, declared at :349 and called at :3787. The change adds no export of its own, verified by grep -c for a leading export in the new file returning 0, so it raises no new liveness question"
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this entry is itself the M1 mechanism, so the change cannot be called unengaged: all 12 registry ids carry a verdict here, once each, in a committed artifact rather than in the PR body. Three residuals, named rather than assumed away. First, scripts/invariant-coverage-check.mjs validates row shape, id set-equality and the two-valued verdict domain (ALLOWED_VERDICTS at :9, row checks at :107-117) and contains no path that evaluates whether a check string is TRUE, so nothing mechanical stands behind these words. Second, the gate's pull-request clause reads git diff --name-only between the merge base and HEAD (:167-176), which cannot see untracked paths, so this file is committed in the same commit as the test rather than left on disk; the commit is what satisfies that clause, and this author verified the gate green in both --event push and --event pull_request --base-ref main modes AFTER committing, not before. Third, every execution receipt cited in this artifact was run by this author on this tree, and where a check was not run the row says so"
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged, and the honest verdict is threatened rather than vacuous. This change adds no gate, so no census of tokens, identifiers or paths is weakened directly, and no pinned count or sampled allowlist is introduced into any gate. What it does add is a file that no census classifies at all: mirror-guard resolves its MIRROR_CENSUS domain through libModuleNames(), a non-recursive readdirSync over the lib directory filtered to entry.isFile() and .mjs (mirror-guard.test.mjs:115-117, LIB at :7), so the tests/ directory falls outside it in exactly the way tests/ falls outside dead-export-lint. Measured by this author with node rather than inferred: 27 of the file's 31 top-level declarations share a name with frontier-train-e2e.test.mjs, and 243 of its 288 non-blank lines are byte-identical to some line in that sibling, about 84 percent, duplication policed by neither the shared-fixture-module precedent nor the policed-twin doctrine. The drift is silent by construction: a stale fixture that omits an upstream precondition resolves to a report rather than throwing, and this file's entry guards catch only the specific degradation where the polled unit never reaches merge-watch or review-decision, not every shape. One count in the tests is deliberate and is named here so it is not mistaken for a change-detector: the assertion that merge-watch:l2 is dispatched exactly 6 times pins MERGE_POLL_MAX_CYCLES, which is the bound itself and not a proxy for it"
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "the central invariant for this change, and engaged. There is no reported symptom and no production fix here, so the red-on-parent acceptance clause has nothing to attach to; the asserted symptom is the engine behavior itself, pinned before milestone M5 is allowed to delete or restructure the bounded merge poll. The inertness clause IS owed and was discharged by execution, by this author, against scratch engine copies selected through the MITOSIS_PATH indirection, with the repo engine never modified and git status re-checked clean for it afterwards. Baseline on the unmutated engine is 5 pass / 0 fail. Six single-site mutants were run, each anchored to a string this author confirmed occurs exactly once in mitosis.js, and the full 6-by-5 matrix was recorded from TAP output rather than sampled: MERGE_POLL_MAX_CYCLES 6 to 1 (:4989) gives 4 pass / 1 fail reddening only the bound test; the CHANGES_REQUESTED literal in resolveReviewEvent renamed (:5031) gives 4 pass / 1 fail reddening only the in-run halving test; the fallthrough after that arm changed from return null to return approved (:5032) gives 4 pass / 1 fail reddening only the unrecognized-verdict test; dropping the readError conjunct from the guard (:5029) gives 4 pass / 1 fail reddening only the failed-read test; MERGE_POLL_WAIT_SECONDS 300 to 120 (:4990) and MERGE_POLL_INTERVAL_SECONDS 30 to 7 (:4991) each give 4 pass / 1 fail reddening only the prompt-bound test. The matrix is a clean diagonal, every test is killed by its own mutant and by no other, and zero of the six mutants was inert. Named rather than hidden: the two constant mutants both kill the same fifth test, which pins two values in one test and is therefore the one place the diagonal is two-to-one"
    },
    {
      "id": "M4",
      "verdict": "not-threatened",
      "check": "this change is the artifact M4 demands rather than a change M4 constrains. Its whole purpose is to pin the surviving behavior of the bounded merge poll BEFORE milestone M5 restructures or deletes it, which is precisely the precondition M4 requires a refactor to satisfy in advance. The precondition M4 would otherwise test is absent here: the change carries zero production change, verified by an empty git diff --name-only cad6ba2 for .claude/workflows/mitosis.js, leases.mjs and reconcile.mjs plus their absence from git status --porcelain, so no behavior change and no structural refactor exist to share a reviewable range with anything. Suite accounting corroborates that nothing else moved: npm test reports 1843 pass / 0 fail on this tree and 1838 pass / 0 fail with the new file moved aside, a delta of exactly the 5 tests this file adds, so no existing test changed verdict. Stated candidly so the pin is not overclaimed: the file pins five behaviors of the poll, not the whole of it. Coverage deliberately NOT added, because it already exists and re-pinning it would fail the test admission gate, includes progressPossible and the poll-cycle guard (leases.mjs:150, pinned in leases.test.mjs), the fail-closed watch gates, onMerged's mutations, the sonnet model pin, and the APPROVED happy path"
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "engaged and anchor-dense, so every citation in this artifact was re-derived against the working tree today with grep and awk, and none was copied from the task brief that supplied them. Re-derived and confirmed: MERGE_POLL_MAX_CYCLES at mitosis.js:4989, MERGE_POLL_WAIT_SECONDS at :4990, MERGE_POLL_INTERVAL_SECONDS at :4991, readReviewDecision at :5016, resolveReviewEvent at :5028 with its readError guard at :5029, its APPROVED arm at :5030, its CHANGES_REQUESTED arm at :5031 and its fallthrough at :5032, mergePoll at :5057 through :5095, the mergeWatchPrompt call carrying both constants at :5068, the event resolution at :5075, the window-change guard at :5077 and persistWindowCheckpoint at :5079, runSchedule at :5099, WINDOW_FLOOR and WINDOW_CEILING at :2423-2424, nextWindow at :2427, windowDelta at :2434, mergeWatchPrompt with its internal 300 and 30 defaults at :2815-2818, computeLogicalRunId at :349 and :3787, and the reconcile-time CHANGES_REQUESTED site at :3138; recovery.mjs:4; leases.mjs:150; invariant-coverage-check.mjs:8, :9, :107-117 and :167-176; mirror-guard.test.mjs:7 and :115-117; dead-export-lint.test.mjs:12-17 and :66-71; frontier-train-e2e.test.mjs:26-28, :488 and :505; and the new file's own :4, :6, :27-28, :30 and :257. Two residuals named rather than assumed away. First, the brief supplied a line for computeLogicalRunId that did not survive re-derivation on this tree, so the call site recorded in B6 is :3787 as measured here and not the number handed to this author. Second, the mutant line numbers in M3 are the pristine engine's; a mutant that changes line count would shift them, and none of the six does, since every mutation is a same-line substitution"
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "nothing is built, deployed, vendored or hand-patched by this change: it adds one test file executed from the repo at its own SHA, and .claude/skills/mitosis/templates/receipts.yml, scripts/invariant-coverage-check.mjs and .github/workflows/test.yml are all absent from git status --short. The mutation receipts in M3 were produced without patching any committed or deployed artifact: each mutant is a separate scratch file written outside the repo, under this session's scratchpad, and selected at run time through the MITOSIS_PATH environment indirection the file reads at :6, with git status --porcelain re-checked afterwards confirming .claude/workflows/mitosis.js unmodified and the staged set empty. Two residuals named rather than assumed away. First, that same indirection resolves the engine under test from the environment, so a stale or redirected MITOSIS_PATH in a CI environment would silently characterize a different engine than the committed one, and nothing in the file asserts that the resolved path is the repo's own. Second, the pre-existing untracked .bak-pre-promptsfix-043a2526 copies of mitosis.js and engine-args.mjs sit in the tree and are exactly the hand-patched-deployed-file shape M6 forbids; they predate this change, were left untouched by it, and are named here rather than passed over in silence"
    }
  ]
}
