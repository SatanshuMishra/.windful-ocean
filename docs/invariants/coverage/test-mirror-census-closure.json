{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "the diff is one file under tests/ and ships no production change, so no realm construction is added, removed or reached: workflow-sandbox.mjs is byte-unchanged and no vm call or DONT_CONTEXTIFY reference appears in the diff. The new census does read workflow-sandbox.mjs, but only as text, classifying it 'standalone', which asserts its body does NOT appear inside mitosis.js. I read that module's export surface directly (ten export declaration lines, workflow-sandbox.mjs:18, :23, :25 among them, no export default and no export list), so the added export-line tripwire passes over it without constraining any realm code"
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no denial path is added, removed or reached. The added code raises nothing and catches nothing in sandbox scope: its only failures are assert.deepEqual over arrays of census failure strings, thrown by node:test in the test process, never inside a compiled workflow body. SandboxViolationError and SANDBOX_VIOLATION appear nowhere in the diff and workflow-sandbox.mjs is untouched, so no mechanism gains an untagged host-error route a workflow could catch as its own"
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not read member by member for this entry. What was read is the entire diff surface, which is one test file whose only operations are readdirSync/readFileSync over lib/*.mjs, string containment, and scanJsStructure. No member is added to or removed from any policy list, and workflow-sandbox.mjs is unchanged, so no guarded intrinsic gains or loses a denied member. The branch's full suite, which carries the guarded-intrinsic assertions, ran 1788 pass / 0 fail (orchestrator-executed)"
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "checked by grep, not assumed: VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS occur only in workflow-sandbox.mjs (:18, :23, :25) and in workflow-sandbox-policy.test.mjs and workflow-sandbox-census.test.mjs. No file in this diff reads or writes them. Emptying any of the three would not move a mirror-guard assertion either, because 'standalone' measures whether the module's text appears inside mitosis.js, never the contents of its policy arrays. Load-bearingness stays proven exactly where it was proven before"
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "zero production change is the defining fact of this PR: mitosis.js, which IS the workflow-body position, receives no line, so no new free identifier or bare global reaches the host and the existing classification rows stay exhaustive. B5's census reads workflow-sandbox.mjs and the engine source (workflow-sandbox-census.test.mjs:4, :6-7); both are unchanged here. The MIRROR_CENSUS this commit adds is a different census over a disjoint domain, lib module files and their export blocks rather than identifiers in the engine source, so it neither extends nor weakens B5's closure"
    },
    {
      "id": "B6",
      "verdict": "not-threatened",
      "check": "checked rather than assumed. The diff adds no export and removes none; mirror-guard.test.mjs lives under tests/, and the lint's domain is top-level lib/*.mjs only (dead-export-lint.test.mjs:12-17), with callers counted in mitosis.js, sibling lib modules, and the defining module's masked own text (:66-71) — test files count nowhere. The one consumer this change adds, scanJsStructure at mirror-guard.test.mjs:5, therefore manufactures no liveness for it; that export already has a non-test caller inside its own module at mitosis-gate.mjs:573"
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "candid reading per 0147 as relayed in the dispatch: this entry is itself the M1 mechanism, so the change cannot be called unengaged. All 12 registry ids carry a verdict here, once each, in a committed artifact and not in the PR body. Two residuals, both real and neither closed by this diff: the gate checks the SHAPE of every row and never the TRUTH of any check string, which is a permanent human gate; and every execution receipt cited in this entry was run by the orchestrator and supplied under 0143, not re-run by the author, who cannot see them"
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "this change IS the M2 mechanism, so it is engaged, not incidental. The parent's hand-typed 21-name array was precisely the forbidden sampled allowlist; the frozen MIRROR_CENSUS (mirror-guard.test.mjs:24) classifies a readdirSync domain in both directions, so an unclassified module halts the run and a row naming a deleted module halts it too. I re-derived the domain independently: 38 .mjs files in lib today, 37 at this commit once the later branch's status-facts.mjs is excluded, against 38 census rows = 23 whole + 3 partial + 12 standalone, i.e. 22+3+12 here. Receipt: truncating the census to the parent's 21 names fails naming exactly 16 unclassified modules, 21+16=37. Named residual: the domain filter demands a literal .mjs suffix at the top level, so a twin inlined from a subdirectory or a .js file is outside it, and the extractor tripwire still leans on a >= 200 floor plus five named anchors — a sample, but one guarding extractor emptiness, not classification"
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "engaged despite being test-only: the receipts M3 demands exist in the mutation form a guard change admits, and both were executed by the orchestrator, not relayed. Red on the parent: truncating the census back to the parent's 21 names gives 27 pass / 1 fail naming exactly the 16 unclassified modules. Inertness: perturbing the inlined msp-file-scope twin in a scratch mitosis.js turns the branch red at 43 pass / 1 fail while the parent's guard stays green at 22/22. That differential, not future-proofing, is the evidence a real hole closed — msp-file-scope.mjs was absent from the 21 names and now carries a whole-twin row (mirror-guard.test.mjs:33), while aggregateMspFileScope is declared at msp-file-scope.mjs:1 and duplicated byte-identically inside mitosis.js. Stated plainly: there is no production fix and so no runtime symptom to assert; the asserted symptom is the guard's blindness"
    },
    {
      "id": "M4",
      "verdict": "not-threatened",
      "check": "the precondition is absent rather than argued away: the diff carries zero production change, with mitosis.js verified clean against main and clean in the worktree, so no behavior change exists to share a reviewable range with the test restructure. The surviving behavior is pinned in place rather than replaced blind — the models-knob region twin test remains verbatim at the end of the same file, and the old whole-file check is subsumed by the 22 whole rows. The full-suite delta of +22 (1766 -> 1788) equals this file's own 22 -> 44, so nothing else in the suite moved. Caveat, not a defence: +221/-8 in one file is a large single reviewable range for a guard rewrite"
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "engaged, because the tree available to this author is one commit LATER than the change and the hazard was managed rather than ignored. mirror-guard.test.mjs in that tree carries one extra census row added downstream, so every line past it is off by one from this commit's file; citations to it are therefore confined to :5, :24 and :33, all above the inserted row, and later constructs are named without lines. mitosis.js is likewise a later revision, so no line in it is cited anywhere in this entry. Every other citation here was re-derived by grep or read in files this branch does not touch: mitosis-git.mjs:23 (the single bare re-export the tripwire's 289-blocks-plus-1 accounting depends on), msp-file-scope.mjs:1, mitosis-gate.mjs:573, dead-export-lint.test.mjs:12-17 and :66-71, workflow-sandbox.mjs:18, :23, :25, workflow-sandbox-census.test.mjs:4 and :6-7"
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "nothing is built, deployed, vendored or hand-patched: the gate is a committed test file executed from the repo at its own SHA, and the receipts CI template is untouched. One residual named rather than assumed away: the census resolves its domain by reading the on-disk lib directory at run time, so it measures the working tree and not the commit's git objects — an uncommitted .mjs dropped into that directory would enter the domain and demand a row. The one stray sitting there today, engine-args.mjs.bak-pre-promptsfix-043a2526, is excluded solely because the filter requires a literal .mjs suffix"
    }
  ]
}
