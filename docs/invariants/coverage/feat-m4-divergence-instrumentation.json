{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "checked by reading the added lines rather than waved through, because the workflow-body position IS edited here: b8c3542 touches .claude/workflows/mitosis.js. The positive distinguishing fact is that the identifier set resolvable in the body is IDENTICAL before and after, not merely a subset. The whole added surface across both twins is six lines each plus one log call, and it introduces exactly two new bindings, both function-local to planReconcile: invalidatingParents (mitosis.js:2980, reconcile.mjs:112) and invalidated (mitosis.js:2982, reconcile.mjs:114). Every other identifier the added lines reference already resolved at the base 58cb6ea — descendantsToInvalidate, manifest, parentId, verdicts, advance and log all appear in unchanged context, and descendantsToInvalidate was already imported at reconcile.mjs:2 and already called on the line the diff rewrites. No bare identifier, no host-realm intrinsic and no prototype-chain reach is added anywhere. workflow-sandbox.mjs and every workflow-sandbox test file are absent from git diff --name-only --diff-filter=d 58cb6ea HEAD, which lists exactly three paths: .claude/lib/superpowers-parallel/reconcile.mjs, .claude/lib/superpowers-parallel/tests/reconcile.test.mjs and .claude/workflows/mitosis.js."
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no denial path is added, removed or reached: SandboxViolationError occurs zero times across the whole 58cb6ea..HEAD diff, grepped over the diff text rather than inferred from the commit subjects. The positive distinguishing fact is that this change adds no failure surface of any kind — unlike its base commit, which added a halting arg-boundary validator, the added statements here are one let, one array-length comparison, one integer increment and one log emission. There is no throw, no catch, no rethrow and no error construction in the diff. The one member read on a value this change did not create is invalidated.length (reconcile.mjs:115, mitosis.js:2983), and its producer descendantsToInvalidate returns an array on both of its two paths (parking.mjs:52-55: a literal [] for a 'clean' verdict, otherwise transitiveDependents), so that read cannot raise. Nothing in these two commits crosses, tags or re-tags an error at the sandbox boundary."
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not re-read member by member for this entry. What was read is the entire change surface, which adds no member to and removes no member from any guarded intrinsic — workflow-sandbox.mjs is absent from the three-path change set. The positive fact is specific rather than a bare denial: the added lines contain zero global-intrinsic member accesses. The only property read introduced anywhere in the diff is .length on invalidated (reconcile.mjs:115, mitosis.js:2983), a locally-bound array, and the only assignment is an integer increment on a local. The one intrinsic member call in the surrounding function, Object.keys(verdicts) at reconcile.mjs:113 and mitosis.js:2981, is an unchanged context line the diff does not touch, so no guarded member's reach is created or orphaned. The receipt that no guarded intrinsic changed behaviour is the full suite, which carries workflow-sandbox-traps, workflow-sandbox-policy, workflow-sandbox-census and workflow-sandbox: npm test 1841 pass / 0 fail on this tree, run by the pre-commit hook on this commit."
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "checked by grep rather than assumed: VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS occur zero times across the 58cb6ea..HEAD diff, and no file defining or asserting them is among the three changed paths, so the load-bearingness proofs in workflow-sandbox-census.test.mjs and workflow-sandbox-policy.test.mjs are untouched and pass inside the 1841 pass / 0 fail suite. The positive distinguishing fact, and the reason this differs from the base branch's entry: this change introduces NO named constant at all, so it creates no new emptying-turns-red obligation to discharge. invalidatingParents is a per-invocation let re-initialized to 0 on every planReconcile call (reconcile.mjs:112, mitosis.js:2980), not a module-level constant, and the two literals it is compared against in tests are the counts themselves rather than references to a named policy value. B4's subject is therefore neither extended nor weakened here."
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "engaged rather than waved through, because mitosis.js IS the workflow-body position and this change edits it. Measured: the added statements introduce no free identifier that did not already resolve at 58cb6ea, so the host-reachable bare-identifier set at HEAD is IDENTICAL to the pre-change set, not merely a subset of it — every name in the added lines (descendantsToInvalidate, manifest, parentId, verdicts, advance, log) appears in unchanged context in the same functions, and the only genuinely new names are the two function-local bindings invalidatingParents and invalidated. A closed census cannot halt on an unchanged identifier universe. Contrast with the base branch's entry, which had to reason about a NEW read of external input at the arg boundary: nothing here reads process, env, argv or input at all. The receipt is the census's own execution rather than a reading of its rows: workflow-sandbox-census.test.mjs runs inside npm test at 1841 pass / 0 fail on this tree. Named candidly: this author did not re-read the classification rows one by one and did not run the census file standalone."
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "genuinely engaged, because a new field on a mirrored function's return shape is exactly the shape that ships as test-only cargo and then gets called live by its own assertions. Traced instead of assumed: invalidatingParents HAS a real production reader. planReconcile has exactly one non-test call site in the tree, mitosis.js:3928 (const advance = planReconcile(reconciledManifest, liveSignals)), and the field is consumed two lines into the same block at mitosis.js:3970, which logs it on the advance path — a production emission, not an assertion. Named candidly, because it cuts the other way in the lib copy: reconcile.mjs:132 returns invalidatingParents and NO module in .claude/lib/superpowers-parallel/ reads it; there its only readers are the four assertions at reconcile.test.mjs:137, :156, :163 and :183 plus three shape expectations at :206, :207 and :225. Its presence in the lib twin is compelled by mirror-guard's WHOLE classification of reconcile.mjs (mirror-guard.test.mjs:39), not by a lib-tree consumer. Nothing here moves dead-export-lint's subject: the change adds and removes zero exports — planReconcile was already exported at reconcile.mjs:101 and descendantsToInvalidate already imported at reconcile.mjs:2 — so no export's liveness arithmetic changes. Receipt: dead-export-lint.test.mjs runs green inside npm test 1841 pass / 0 fail."
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this artifact is itself the M1 mechanism, and its very existence is an M1 finding rather than a formality. All 12 registry ids carry exactly one verdict here, in a committed file under docs/invariants/coverage/ rather than in a PR body, named feat-m4-divergence-instrumentation.json after the branch per the convention all 16 pre-existing entries follow. The reason it is a SEPARATE file was measured, not assumed: this branch's PR targets feat/m4-fixed-build-ahead-cap, git merge-base feat/m4-fixed-build-ahead-cap HEAD returns 58cb6ea which IS the base tip (linear history), so branch 1's identically-named feat-m4-fixed-build-ahead-cap.json contributes ZERO diff lines in merge-base..HEAD and the gate's clause at scripts/invariant-coverage-check.mjs:167-177 would report no coverage file added or modified. Editing branch 1's file from this branch was rejected as breaking the file-per-branch traceability every existing entry follows. Two further precision points. resolveBaseCommit (scripts/invariant-coverage-check.mjs:150-159) tries origin/<ref> BEFORE the local ref; git rev-parse --verify origin/feat/m4-fixed-build-ahead-cap fails because branch 1 is not pushed, so the LOCAL ref resolves and pins the base at 58cb6ea — verified by running that rev-parse rather than inferred. And the known trap was avoided deliberately: a bare node scripts/invariant-coverage-check.mjs resolves the event to '' (line 192), fails the PULL_REQUEST_EVENTS membership test at line 194, and silently runs in PUSH mode where pullRequestErrors is never called — two documented incidents came from exactly that, so the verification run is GITHUB_EVENT_NAME=pull_request GITHUB_BASE_REF=feat/m4-fixed-build-ahead-cap. Candid limit: a file cannot record the exit status of the gate that validates it, because the gate reads the committed content; the executed command and its verbatim output are reported to the orchestrator alongside this commit rather than embedded here."
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "the invariant with the sharpest surface here, because this change literally ships a COUNT and M2 forbids a pinned count as a change-detector. Engaged on three fronts. THE COUNTER IS NOT A CLASSIFIER, which is the design decision that keeps it inside M2: reconcile.mjs:114-115 and mitosis.js:2982-2983 increment on descendantsToInvalidate's OUTPUT being non-empty, deliberately NOT on a re-derivation of verdict !== 'clean', so no second authority over the verdict is created and the counter follows the oracle if the fail-closed rule ever changes. The sole classifier remains assembleDivergenceVerdicts, whose verdict universe is closed at four values assigned on exhaustive branches — 'missing' (reconcile.mjs:91), 'indeterminate' (:93, :94, :95) and 'divergent' or 'clean' (:96) — and parking.mjs:53 returns [] if and only if the verdict is 'clean'. So invalidatingParents means exactly 'keyed parents with a non-clean verdict', with no token, path or identifier left unclassifiable. THE PINNED COUNTS, named so they are not mistaken for change-detectors: reconcile.test.mjs:137, :156, :163 and :183 assert the literals 1, 1, 0 and 1. Each fixture carries exactly one merged parent that gates a built subtree, so the asserted number IS the quantity under test rather than a proxy for it, and the :163 zero is a semantic assertion (the oracle returned an empty set) rather than a tally. The fail-closed case at :167-186 iterates a nine-case list covering absent/empty builtSha, missing fileScope, unknown/null/empty merged sha, missing verdict, probe failure and probe non-object, so it is an enumeration rather than a sample. THE MIRROR CENSUS: MIRROR_CENSUS classifies reconcile.mjs as WHOLE (mirror-guard.test.mjs:39) and this change edits BOTH copies on every hunk, so a one-sided edit was the standing risk throughout. Candid: mirror-guard.test.mjs was NOT run standalone for this entry; it is covered only by the whole-suite run at 1841 pass / 0 fail."
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "the invariant this branch exists to satisfy, and the receipt is a supervised execution cited rather than reproduced. The defect: b8c3542 shipped invalidatingParents with ZERO acceptance coverage — mutating the increment at reconcile.mjs:115 to if (false) left tests/reconcile.test.mjs GREEN at 23/23, so nothing pinned the quantity the divergence instrumentation exists to report. After 74962a6 the SAME mutation is RED: 23 tests, 20 pass, 3 fail, failing on 'the one keyed parent whose invalidation fired is counted once' (reconcile.test.mjs:137), 'the parent fired once — the count tracks parents whose invalidation fired, never the units that survived the already-done filter' (:156) and 'absent parent builtSha: counts the parent whose invalidation fired' (:183, the first label of the nine-case matrix). That is the inertness mutation M3 demands, run in both directions rather than asserted. The fourth assertion (:163) expects 0 and correctly REMAINS 0 under that mutation: its fixture is a clean probe whose oracle returns an empty set, so the increment never runs there even unmutated — that is by design, the deliberate pinning of the read-the-oracle's-output decision, and NOT a coverage gap. One honest limit on the shape: the acceptance assertions were added ABOVE the fix rather than red-on-the-parent-then-green, because the fix commit b8c3542 was already landed when the gap was found; the mutation receipt is what supplies the red, and it is a stronger probe than a parent-commit red would have been since the parent lacks the field entirely. Suite: 1841 pass / 0 fail. The test COUNT is unchanged from branch 1 because 74962a6 strengthens four existing tests rather than adding new ones — stated explicitly so a flat count is not read as 'no new coverage'."
    },
    {
      "id": "M4",
      "verdict": "not-threatened",
      "check": "checked against the actual commit boundaries rather than the branch as a whole, and they are cleanly separated by kind. b8c3542 is behaviour-only and 74962a6 is test-only, touching exactly one file (.claude/lib/superpowers-parallel/tests/reconcile.test.mjs). Across the branch, git diff --shortstat 58cb6ea HEAD is 3 files changed, 20 insertions, 9 deletions — no file is moved, renamed, split or restructured, no function is extracted or inlined, and no call site changes signature, so there is no structural refactor anywhere in the range for a behaviour change to be smuggled alongside. The positive distinguishing fact against the base branch's entry, which was threatened here: that one put a 102-insertion / 207-deletion AIMD removal in the same reviewable range as new validator behaviour; this one adds a field and reads it, and nothing else. Named candidly as the nearest thing to a mix: b8c3542 widened three pre-existing shape expectations (reconcile.test.mjs:206, :207 and :225) in the same commit as the behaviour change. Those are forced-by-construction deepEqual updates for a return shape that grew by one key, not a refactor and not a characterization written after the fact, and each still pins the same surviving behaviour it pinned at 58cb6ea."
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "engaged, and every path:line in this artifact was re-derived by grep or by reading the range against the live tree at HEAD = 74962a6 before it was written, never copied from the dispatch brief. Re-derived and CONFIRMED: reconcile.mjs:2 (the descendantsToInvalidate import), :101 (the planReconcile export), :103, :112, :114, :115, :116 and :132, which grep confirms is the COMPLETE set of invalidatingParents and invalidated occurrences in that module, plus :91, :93, :94, :95 and :96 for the four verdict values; parking.mjs:52-55 (the whole descendantsToInvalidate body, read rather than summarized, confirming [] iff 'clean'); mitosis.js:2969, :2971, :2980, :2982, :2983, :3000, :3928 and :3970, of which :3928 is grep-confirmed as the only non-test planReconcile call site in the tree; reconcile.test.mjs:137, :156, :163, :183, :206, :207 and :225, the complete grep set of invalidatingParents readers, with the nine-case matrix range :167-186 read line by line to confirm the case labels quoted under M2 and M3; mirror-guard.test.mjs:39 (the reconcile.mjs WHOLE row); and scripts/invariant-coverage-check.mjs:150-159, :167-177, :192 and :194 for the gate contract quoted under M1. One anchor arrived FROM a supplied receipt rather than from this author's own reading — the increment at reconcile.mjs:115 named as the mutation point — and it was re-derived by grep rather than trusted; it is the increment line. Named candidly: every line number above is stated at HEAD = 74962a6, the tree this artifact is committed onto. Both production commits are already landed, so their line numbers are final and no post-landing shift can falsify these."
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "nothing is built, deployed, vendored or hand-patched by these two commits. The change set is exactly three paths (git diff --name-only --diff-filter=d 58cb6ea HEAD): reconcile.mjs, tests/reconcile.test.mjs and mitosis.js. No gate-enforcing artifact is among them — .github/, scripts/ and .claude/skills/mitosis/templates/receipts.yml return zero matches against that list. The positive distinguishing fact for the one file that could be mistaken for a deployed copy: .claude/workflows/mitosis.js is committed source, not a hand-patched deployment of the lib modules, and its agreement with reconcile.mjs is enforced by a versioned check rather than by convention — MIRROR_CENSUS classifies reconcile.mjs WHOLE at mirror-guard.test.mjs:39 and the twin hunks at mitosis.js:2971-3000 are byte-parallel to reconcile.mjs:103-132. Two further receipts, both cited from supervised runs rather than re-executed. The rebase onto branch 1 replayed the payload BYTE-IDENTICALLY: git range-diff m4-anchor-c1..m4-anchor-c2 58cb6ea..b8c3542 printed fc035fc = b8c3542, with no conflict markers, so no hand-edit entered on the replay. And node .claude/lib/superpowers-parallel/mitosis-gate.mjs phase-parity returned ok: true, dead: 0 across 13 phases, so the engine's phase surface still reproduces from committed source. One residual is named rather than silently tolerated: the untracked .bak-pre-promptsfix-043a2526 copies of mitosis.js and engine-args.mjs remain in the working tree and are exactly the hand-patched-deployed-file shape M6 forbids; they predate this branch, they are among the five pre-existing dirty paths this commit was required to leave unstaged, and they are therefore carried forward unchanged rather than fixed here."
    }
  ]
}
