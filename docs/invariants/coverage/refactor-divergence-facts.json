{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "no realm construction is touched: workflow-sandbox.mjs is absent from the 4 changed paths, and the new module divergence.mjs, read in full, contains no vm call, no DONT_CONTEXTIFY reference and no global-object construction. The mitosis.js hunks are a one-line relocation of SHA_HEX_PATTERN from :2975 to :2987, an added ctx parameter with its destructure at :2989-2990, and the call site at :3871. Every one operates on run-local values - a manifest, an id list, a sha map, and an object literal of bindings already resolvable in the workflow body. Nothing in the diff reaches for a host-realm intrinsic or a host prototype chain"
    },
    {
      "id": "B2",
      "verdict": "threatened",
      "check": "engaged, and the honest reading is that it is carried rather than created. The extracted body contains two pre-existing catch blocks, moved verbatim: the checkpointRef catch at divergence.mjs:25 and the agent catch at :35. The second converts ANY thrown error, including a tagged SandboxViolationError raised inside the agent hook, into the string `divergence-probe threw: ${clean(err.message)}`, so the tag does not survive the boundary even though the message does reach probes[parentId].error and is therefore not silently discarded. This diff adds no catch, removes none, and alters neither one's text - byte identity with the prior mitosis.js body is what proves that - but the PR does introduce the module that now holds them, so the row is named rather than inherited quietly. Same error class as decision 0171's D5 finding, one degree less severe because this catch reports where D5's discards"
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "stated candidly: the guarded-intrinsic enumeration was not read row by row for this diff, and workflow-sandbox-policy.test.mjs was not opened. What was checked is the member surface the diff actually adds. The extracted text is byte-identical to what mitosis.js already executed, so it adds no member usage at all; the only genuinely new syntax is object destructuring of ctx at divergence.mjs:7 and the object literal at mitosis.js:3871, neither of which is a guarded intrinsic member. The receipt is the orchestrator-executed suite on this tip: 1818 tests, 1818 pass, 0 fail, exit 0"
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "VALUE_GLOBALS, ALWAYS_DENIED and ALLOWED_GLOBALS occur in exactly three files under .claude, located by content search: workflow-sandbox.mjs, tests/workflow-sandbox-policy.test.mjs and tests/workflow-sandbox-census.test.mjs. None is among the 4 changed paths. divergence.mjs was read in full and names none of them; it declares one constant, SHA_HEX_PATTERN at :4, which is a sha-shape regex and not sandbox policy. No assertion depending on a policy constant moves"
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "re-checked rather than defaulted, because the plan names this as the one B-track id worth re-deriving per extraction PR. The question is whether the diff adds a free identifier resolving in the workflow-body position. It does not: mitosis.js gains the parameter name ctx at :2989 and five destructured bindings at :2990, and a parameter and a const destructure both BIND names inside the function rather than leaving a free identifier to resolve against the host. The call site at :3871 references only agent, clean, logicalRunId, divergenceProbePrompt and DIVERGENCE_PROBE_SCHEMA, all of which already resolved at that position before this change - agent as the sandbox-injected hook enumerated in workflow-sandbox.mjs HOOK_NAMES, the other four as body-local declarations. The identifier surface of mitosis.js therefore loses nothing and gains no unclassified name. Candidly: the census rows in workflow-sandbox-census.test.mjs were not read one by one; the receipt that the census still closes is the 1818 pass / 0 fail suite"
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "engaged, and structurally so. divergence.mjs is a new lib module and NO non-test file imports it: the only importers are tests/divergence.test.mjs:3 and the census row at tests/mirror-guard.test.mjs:29, which reads it by path for containment rather than as a functional import. Production executes the byte-identical inline twin at mitosis.js:2987-3031 from the single call site at :3871, and that call site binds the twin's declarations, not the module's exports. dead-export-lint.test.mjs computes liveness as raw identifier matches in mitosis.js plus sibling modules, so the twin's own copied text makes both exports look live with zero real callers - exactly the self-referential lint artifact this invariant forbids. This is not a defect A7 introduces but the twin pattern's standing cost: mitosis.js runs inside a sandbox that denies import and require, so a production importer is impossible by construction. Identical shape to the status-facts.mjs row shipped under decision 0171"
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this entry is itself the M1 mechanism: all 12 registry ids carry a verdict here, once each, in a committed artifact rather than in the PR body. Standing threats, named rather than assumed away. The gate at scripts/invariant-coverage-check.mjs validates row shape, id completeness and verdict domain; it never checks whether a check string is TRUE, so nothing mechanical stands behind these words. Two receipts below are carried from the implementing agent and were not re-executed by the orchestrator: the inertness mutation that turned divergence test 1 red, and the scratch-copy mutation that made the census fail. What the orchestrator did execute on this tip: npm test at 1818/1818/0, the coverage gate in pull_request mode, an independent normalized-containment check of the twin, and repo-wide reference greps for both extracted names"
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged directly, because a new lib module is exactly what a closed census must halt on. mirror-guard.test.mjs enumerates the lib directory with readdirSync and asserts that no module lacks a MIRROR_CENSUS row, so divergence.mjs halted the gate until the single row added at :29 classified it WHOLE. That class is then verified by measured verbatim containment rather than by a pinned count or a sampled allowlist. The containment was re-verified independently of the guard: normalizing both files by the guard's own rules - strip a leading export, drop bare relative .mjs imports - the module's 2524-byte normalized body occurs inside normalized mitosis.js exactly ONCE, at a single offset. One occurrence, not zero and not two, is the fact that makes WHOLE the right class here"
    },
    {
      "id": "M3",
      "verdict": "not-threatened",
      "check": "A7 fixes no reported symptom - it is a pure extraction serving milestone M7 - so the red-on-parent acceptance clause has nothing to attach to and none is owed. Recorded anyway because it strengthens rather than weakens the picture: the implementing agent reported two executed red proofs, an inertness mutation of the built-status predicate that turned divergence test 1 red, and an identifier rename inside a scratch copy of the twin that made the census report the module's normalized body no longer appears verbatim. Both are CARRIED receipts under decision 0143; the orchestrator did not re-run either, and says so here rather than letting them read as its own"
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "engaged by this PR's central claim - that it carries no behavior change - so what establishes that claim must be named. Four things do. First, the extracted body is byte-identical to the text mitosis.js already ran, except for the added fourth parameter and the destructure line; the normalized-containment check proves the identity mechanically rather than by reading. Second, the relocation moved a const declaration past a function declaration within the same module scope, and a repo-wide grep shows SHA_HEX_PATTERN is referenced at only three sites, all inside the moved region, so nothing outside it can observe the move. Third, the sole call site was located by repo-wide grep and passes exactly the five bindings the function previously closed over, at the same point in the same scope. Fourth, the suite is 1818/1818 green. The threat that remains is temporal and real: M4's escape clause wants a characterization test written BEFORE the refactor, and runDivergenceProbes had zero test callers before this change, so the new unit tests were authored in the same commit range rather than pinning the behavior in advance"
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "anchor-dense, and the anchors provably moved, so every citation in this entry and in the PR body was re-derived against the working tree today and nothing was copied. The plan at docs/superpowers/plans/2026-07-31-m2-mirror-gaps.md cites this region as :2920 and :2934-2975; both were stale by exactly +55 lines because P2's status-fold twin landed above them, and neither number was reused. Current, re-derived: SHA_HEX_PATTERN at mitosis.js:2987 after relocation from :2975, runDivergenceProbes at :2989 with its destructure at :2990 and its body through :3031, the sole call site at :3871, the census row at mirror-guard.test.mjs:29, the module at divergence.mjs:1-49, the test at tests/divergence.test.mjs:1-93. Candidly: the plan document's OWN anchors were not rewritten, so that stale pair survives in the plan text and will mislead the next reader of section 5.2 unless a later PR re-baselines it"
    },
    {
      "id": "M6",
      "verdict": "not-threatened",
      "check": "this diff builds no artifact, patches no deployed file and creates no cache copy or vendored duplicate of any gate. Both copies of the divergence logic are committed source - divergence.mjs and the inline twin at mitosis.js:2987-3031 - and their byte identity is enforced by mirror-guard, which reports green for divergence.mjs on its new WHOLE row alongside the pre-existing rows for status-facts.mjs and parking.mjs. templates/receipts.yml and scripts/invariant-coverage-check.mjs are untouched, so no deployed enforcer is refreshed or regenerated by this change"
    }
  ]
}
