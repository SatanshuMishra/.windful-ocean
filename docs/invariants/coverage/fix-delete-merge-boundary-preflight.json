{
  "rows": [
    {
      "id": "B1",
      "verdict": "not-threatened",
      "check": "no realm construction is touched, checked rather than assumed. SCOPE FIRST, because the working tree carries drift that is not this change: `git status --short` also shows .claude/settings.json and .zshrc modified, and I read both diffs — settings.json changes a model string, two plugin registry names and effortLevel, .zshrc adds a zoxide chpwd shim. Neither is part of this change and both are excluded from every count below; I did not author or modify either. The change proper, re-measured today: 11 tracked paths between 106b253^ and the working tree (merge-boundary-preflight.mjs deleted, merge-boundary-preflight.test.mjs deleted, mitosis.js, SKILL.md, and the frontier-train-e2e, mirror-guard, mitosis-gate, mitosis-scheduler, no-self-merge-consent and reconcile-only-advance-characterization tests, plus feat-m5-quiescent-exit.json which nets to zero because 106b253 edited it and the uncommitted half reverts it) plus this untracked artifact. NOT ONE of those paths matches `hooks/|workflow-sandbox|registry.json|invariant-coverage-check|receipts.yml`; I state that as a claim about the PATH LIST only, because the same expression run as a CONTENT grep over the two diffs matches 24 lines — every one of them the identifier `sandbox` used as a local variable in the deleted preflight test, not a sandbox source. The mitosis.js half is pure deletion: `git diff 106b253^ HEAD -- .claude/workflows/mitosis.js | grep -cE '^\\+(const|function|async function|let|var) '` returns 0 and the removed side is exactly three (BOUNDARY_PREFLIGHT_CLI, readBoundaryPreflightVerdict, the boundaryVerdict call site). A content grep of both in-scope diffs for `DONT_CONTEXTIFY|createContext|runInContext|node:vm` returns 0, so no realm, context or global object is constructed, removed or reconfigured. Deleting readBoundaryPreflightVerdict removes reach toward the host rather than adding any. The readdirSync added by no-self-merge-consent.test.mjs runs in the node test process, never inside the sandboxed workflow body, so it is not a route from the workflow body into a host intrinsic."
    },
    {
      "id": "B2",
      "verdict": "not-threatened",
      "check": "no sandbox denial path is added, removed, or reached, and the one behavior change is strictly louder rather than a silent swallow. I read the full mitosis.js hunk: the deleted `haltReport('preflight-boundary', ...)` branch is engine halt REPORTING, not a sandbox mechanism, and it raised nothing a workflow could catch. Zero `node:vm` / `createContext` / `runInContext` references appear on either side of the in-scope diff, so no proxy invariant, ownKeys filter, freeze or seal is touched. Deleting the RECONCILE_SCHEMA `boundaryPreflight` property does change one observable behavior, and I checked the direction rather than assuming it: the schema still carries `additionalProperties: false` at mitosis.js:1604, so a reconcile agent that still returns boundaryPreflight now fails schema validation with the engine's own tagged validation error instead of having the field ignored. The change adds exactly two try/catch blocks, both in test-process code and both fail-loud: readTrackedSettingsDeny (no-self-merge-consent.test.mjs:154-164) converts an unreadable or unparseable settings file into `assert.fail`, and the argv scan's catch (:121-124) re-asserts that the thrown message matches the builder's own `refusing to build a gh argv` refusal before it will `continue`, so a broken builder surfaces instead of being read as an unsupported pair. No untagged host error gains a route to a workflow catch."
    },
    {
      "id": "B3",
      "verdict": "not-threatened",
      "check": "established by running the suites that own the enumeration and by locating the enumeration itself, with the limit of that stated. The policy module that holds the guarded-intrinsic surface is .claude/lib/superpowers-parallel/workflow-sandbox.mjs, which I located by grep; it is NOT among the 11 tracked paths this change touches, and neither are workflow-sandbox-policy.test.mjs or workflow-sandbox-census.test.mjs. The production side introduces no new member surface at all, because it is deletion only: merge-boundary-preflight.mjs removed entire, SKILL.md 0 insertions against 19 deletions, mitosis.js 0 added top-level declarations. The only added member calls anywhere in the change live in test files that run outside the sandbox (readdirSync/isFile/endsWith/map/filter/sort/basename/join in no-self-merge-consent.test.mjs, includes in mitosis-scheduler.test.mjs), all already executed elsewhere in this suite. Removing a guarded member's only call site cannot make a non-denied member start denying. Receipt I ran today, and stronger than the prior attestation's: `node --test` over ALL FOUR sandbox suites (workflow-sandbox-census, workflow-sandbox-policy, workflow-sandbox-traps, workflow-sandbox) reports 129 pass / 0 fail on this tree. Stated candidly and unchanged: I did not walk the guarded-intrinsic enumeration member by member myself; what I did is confirm the module defining it is outside the changed set and run every suite that asserts over it."
    },
    {
      "id": "B4",
      "verdict": "not-threatened",
      "check": "measured, not assumed. A content grep of the committed range and the uncommitted in-scope diff for `VALUE_GLOBALS|ALWAYS_DENIED|ALLOWED_GLOBALS` returns 0. All three live in workflow-sandbox.mjs and are asserted over by workflow-sandbox-policy.test.mjs and workflow-sandbox-census.test.mjs; none of those three files is among the 11 changed paths, so no assertion whose redness depends on emptying one of them moves. CORRECTION of a false statement in the prior version of this artifact, which claimed the change ADDS four named constants including MERGE_INVOCATION_PATTERNS: MERGE_INVOCATION_PATTERNS PRE-EXISTED at HEAD (it is at line 14 of `git show HEAD:.claude/lib/superpowers-parallel/tests/no-self-merge-consent.test.mjs`, holding five entries) and is MODIFIED, not added — it now holds six, two gained the `i` flag, and `enablePullRequestAutomerge` was respelled `enablePullRequestAutoMerge`. MITOSIS_PATH and SETTINGS_PATH likewise pre-existed and are modified. Re-derived by diffing module-level bindings between `git show HEAD:` and the working tree across BOTH changed test files, the change ADDS sixteen: SETTINGS_LABEL, LIB_DIR, DENY_CLASSIFIER_SHIM, SCAN_ANCHOR, ENGINE_BASENAME, ENGINE_NAME, libTopLevelModuleNames, mergeScanTargets, MERGE_PATTERN_SPECIMENS, MITOSIS_GIT_STAGES, ARGV_PROBE_OPTS, MERGE_ARGV_TOKENS, MERGE_ARGV_TOKEN_PATTERNS, REQUIRED_DENY_ENTRIES and readTrackedSettingsDeny in no-self-merge-consent.test.mjs, and FOLD_RUN_LOG_CLI in mitosis-scheduler.test.mjs. It DELETES six: PREFLIGHT_HALT_EXIT, PREFLIGHT_CONFIG_EXIT and PREFLIGHT_GH_MISSING_EXIT with their module, BOUNDARY_PREFLIGHT_CLI and readBoundaryPreflightVerdict from mitosis.js, and PROVEN_BOUNDARY from reconcile-only-advance-characterization.test.mjs. None of those twenty-two is a sandbox policy constant, so B4's enumerated subject is untouched. RULED ON HERE rather than deferred, because the prior version deferred it to M2 and M2 ruled on only some of them: I grepped an occurrence count for each of the sixteen added bindings and every one is referenced at least once beyond its own definition line. MERGE_ARGV_TOKENS is asserted at no-self-merge-consent.test.mjs:129 and MERGE_ARGV_TOKEN_PATTERNS at :131; both were inert when a reviewer looked and neither is inert now. No added binding in this change is dead."
    },
    {
      "id": "B5",
      "verdict": "not-threatened",
      "check": "mitosis.js IS the workflow-body position, so this was checked by execution rather than defaulted. `git diff 106b253^ HEAD -- .claude/workflows/mitosis.js | grep -cE '^\\+(const|function|async function|let|var) '` returns 0: the change adds zero top-level declarations and therefore zero new bare identifiers to the census, so no new census row is owed. It removes three bindings (BOUNDARY_PREFLIGHT_CLI, readBoundaryPreflightVerdict, boundaryVerdict), which SHRINKS the identifier surface; a removed binding cannot leave a free identifier behind because every reference to all three dies in the same hunk, which I read in full — the RECONCILE_SCHEMA boundaryPreflight property, the reconcile prompt's step 7 clause, the return-shape sentence, and the `if (!boundaryVerdict.proven)` halt branch. The rest of the mitosis.js delta is prompt string text and a step renumber 8 -> 7, which is data, not identifiers. No lib module is added, so no new module enters the census surface; the one lib module in the change is deleted. Receipt I ran: workflow-sandbox-census.test.mjs is green inside the 129 pass / 0 fail across all four sandbox suites on this tree, so the census still closes."
    },
    {
      "id": "B6",
      "verdict": "threatened",
      "check": "engaged directly, because this change DELETES a whole 410-line lib module and every export it had, which is the one operation that can destroy a production caller. Re-derived at the current tree rather than carried: `grep -rn \"from './merge-watch.mjs'\" .claude/lib/superpowers-parallel/*.mjs` returns exactly ONE hit, mitosis-git.mjs:6 (`import { validateRepoIdentity, parsePrRef } from './merge-watch.mjs';`). merge-boundary-preflight.mjs was the second production importer; after this deletion mitosis-git.mjs is the SOLE surviving one, so merge-watch.mjs still satisfies B6 through a real importer, on a thinner margin than before. The pre-existing residual is UNCHANGED and not cured here: classifyMergeWatch, planMergeWatch, mergeWatchPrompt and MERGE_WATCH_SCHEMA still have zero production callers, and dead-export-lint structurally cannot see it because liveCallerCount counts raw textual occurrences over an unmasked mitosis.js — the lint's own failure message (dead-export-lint.test.mjs:98) says mitosis.js and the sibling modules are counted raw and tests/ does not count, so a twin's own definition line reads to it as a caller. Nothing else is orphaned: PREFLIGHT_HALT_EXIT, PREFLIGHT_CONFIG_EXIT and PREFLIGHT_GH_MISSING_EXIT die with their module and their only importer, mitosis-gate.test.mjs, drops them in the same commit. LIVENESS CAVEAT this change ADDS: no-self-merge-consent.test.mjs:6 now imports MITOSIS_GIT_VERBS and buildGhArgv from mitosis-git.mjs, and a TEST importer is not a production caller by that same :98 rule, so the import manufactures no liveness for either symbol; buildGhArgv's production liveness rests on mitosis-git.mjs's own CLI entry. CARRIED and labelled: exporting MITOSIS_GIT_STAGES from mitosis-git.mjs was attempted and reverted because it reddens dead-export-lint with zero live callers outside tests. I did NOT run that mutation — I was constrained not to modify mitosis-git.mjs — so the redness is another agent's receipt, scoped to exactly that. What I verified myself is the end state it implies: mitosis-git.mjs exports no MITOSIS_GIT_STAGES (grep finds the name nowhere in that file), the test declares its own local list at no-self-merge-consent.test.mjs:94, and the mechanism supports the carried claim, since dead-export-lint filters exports to `liveCallerCount(...) === 0` at :93. Receipt I ran: 329 pass / 0 fail over no-self-merge-consent, dead-export-lint, mirror-guard, mitosis-gate and mitosis-git."
    },
    {
      "id": "M1",
      "verdict": "threatened",
      "check": "this file IS the M1 mechanism, and this is the change where M1 was VIOLATED, so the verdict is threatened with the violation named rather than a clean self-certification. The violation, measured: `git diff --name-status 106b253^ HEAD` lists `M docs/invariants/coverage/feat-m5-quiescent-exit.json`, and its diff is one string inside that older change's B6 row. Commit 106b253 therefore satisfied the PR-mode coverage requirement by EDITING an older change's attestation instead of authoring its own. The checker admits that: pullRequestErrors (scripts/invariant-coverage-check.mjs:161-179) filters `git diff --name-only` to lines beginning `docs/invariants/coverage/` and fails only when that filtered list is EMPTY (:174-176); it never checks WHICH file, so touching any historical entry satisfies it. That is exactly the assurance inflation this MSP deleted the preflight for. This entry is the repair, and both halves are verified: it is this change's own attestation, named for its branch, carrying all 12 registry ids once each in a committed artifact rather than in a PR body; and the retroactive edit is reverted, which I confirmed byte-exact — `git diff 106b253^ -- docs/invariants/coverage/feat-m5-quiescent-exit.json` produces EMPTY output, so that historical record again reads true for the tree it attested. The in-scope uncommitted surface is therefore 3 tracked files (no-self-merge-consent.test.mjs, mitosis-scheduler.test.mjs, feat-m5-quiescent-exit.json) plus this untracked artifact — the feat-m5 revert IS part of this change, and B1 and M4 count it the same way. The standing M1 threat, unchanged and not papered over: validateCoverageEntry (:119-139) checks that a `rows` array exists (:122), row shape (:106-117 — non-empty id, non-empty check, and a verdict drawn from ALLOWED_VERDICTS at :9), duplicate ids (:135), missing ids (:136) and unknown ids (:137), and it NEVER checks whether any check string is TRUE. Nothing mechanical stands behind these words except the mutation recorded under M3."
    },
    {
      "id": "M2",
      "verdict": "threatened",
      "check": "engaged in both directions and the net is still a threat, so both halves are stated and the census claim is scoped axis by axis rather than blanket. IMPROVED, and this is most of the uncommitted half: the merge-abstinence guard was a PINNED SINGLE PATH (at HEAD, five patterns read against mitosis.js alone, and a deny assertion naming three entries) and becomes a real census on the axes where a census is possible. Scan surface: mergeScanTargets reads the lib top level with exactly one NAMED exemption, gh-merge-shim.mjs, and the check at :65-72 derives an expected set from its OWN independent directory read and asserts `deepEqual(scanned, expected)`, so a filter regression surfaces as a named diff rather than a smaller count clearing a floor; two fail-closed anchors back it, gh-merge-shim.mjs must be present in the directory (:66) and mitosis-git.mjs must be in the scanned set (:69). I measured the resulting surface myself: 38 lib modules plus the engine, 39 targets, against 1 before, and the pattern count is 6 against 5. Pattern liveness: :75-83 asserts each of the six patterns matches a real-world specimen, so a pattern cannot rot into one that matches nothing. Env overrides: both `process.env.MITOSIS_PATH` and `process.env.SETTINGS_PATH` fallbacks are REMOVED — the file now contains zero `process.env` references (grep count 0), which closes a hole where pointing those variables at crafted files produced a full green over nothing; I confirmed no in-repo file SETS either variable (the only two matches anywhere are prose in .claude/reports/2026-08-01-permission-config-audit.md:368 and :420), SETTINGS_PATH is now named in this one file alone, and MITOSIS_PATH remains legitimately used by 8 other test files. CENSUS SCOPE, and the NAMED RESIDUAL this change DELIBERATELY does not close: the argv scan's VERB axis is a census (MITOSIS_GIT_VERBS imported from mitosis-git.mjs:30, not transcribed); its STAGE axis is NOT, and it stays open by ruling rather than by oversight. MITOSIS_GIT_STAGES at no-self-merge-consent.test.mjs:94 is a LOCAL list of three names that nothing keeps in sync with buildGhArgv, and the hole is demonstrated rather than theoretical: an EXISTING verb carrying a second branch at a stage the list does not name — pr-close at stage apply, returning a computed merge argv — is never read by the enumeration; a reviewer planted exactly that and it passed all 232 tests, a receipt CARRIED here and not mine. The per-verb completeness assertion (:137-138) does not close that axis and cannot, because it requires only that each exported verb build at least one argv at SOME listed stage, which such a verb already does. It DISCLOSES the axis; it does not census it. THE RULING that leaves it open, issued by the user and recorded as decision 0225: the merge-invocation pattern list is a denylist over an infinite string space and can never close — each round caught only a new spelling, Automerge against AutoMerge, then a computed join of the fragments me and rge — while what actually enforces merge abstinence is the runtime chokepoint. mitosis-git.mjs has exactly ONE spawnSync, which I re-derived myself: a grep of that file reports a single call site at :347 plus the import at :1, and ghExecTripwire(argv, classifyGhMerge) runs before it at :343, returning a frozen refusal when the classifier denies, when it throws, when its decision is not a usable object, and when refuse is anything but the strict boolean false (:299-320), so the gate is fail-closed however the argv was composed. Completeness comes from that chokepoint being singular, not from enumerating what flows through it. Refactoring production dispatch so a redundant static test could echo a working runtime gate is real blast radius for marginal assurance, and rebuilding an over-claiming gate is precisely the assurance inflation decisions 0219 and 0220 deleted the preflight for. CARRIED and labelled: a reviewer called the exported execGh directly with 8 merge argv shapes, including both demonstrated attack argvs, and ALL 8 were REFUSED. I did NOT run that; what I verified myself is the chokepoint shape those calls exercise — the single spawnSync, the gate ahead of it, and its fail-closed branches. Sizing what the enumeration does read, re-derived: buildGhArgv (mitosis-git.mjs:276-297) implements exactly five verb/stage pairs over the stages observe, converge and read, so the local list currently names every stage the builder serves — nothing enforces that, and this artifact makes no claim it will still hold for a stage added tomorrow. STILL THREATENED, found by reading the diff: mitosis-gate.test.mjs:484 moves `assert.ok(siblings.size >= 6)` to `>= 5`. That is a PINNED COUNT, the exact change-detector shape M2 forbids, and this change RE-PINS it downward instead of replacing it with a census over the sibling CLI surface. Not remediated here. Also still threatened: REQUIRED_DENY_ENTRIES (:141-152) is a hand-written list of ten deny strings, not a census derived from the rule that declares them, so a PR-writing MCP tool added server-side is not denied until a human types its name — .claude/rules/common/git/pull-requests.md says so itself, and the test cannot close what a remotely-versioned toolset leaves open. I verified the ten are all present among the 47 entries in the tracked settings.json's permissions.deny. `MITOSIS_GIT_VERBS.length > 0` (:113) and `built.length > 0` (:136) are anti-vacuity FLOORS, not censuses, and are honest about being floors; CARRIED from a reviewer and labelled as such, they replace an aggregate guard of the shape `built.length >= MITOSIS_GIT_VERBS.length` that passed `0 >= 0` on an empty verb table — I read the current expressions myself but did not observe the prior one execute."
    },
    {
      "id": "M3",
      "verdict": "threatened",
      "check": "the honest reading is that M3's red-before-green half does not apply cleanly and its inertness half is now WEAKER than the prior version of this artifact claimed. This change is a DELETION, not a bug fix: there is no reported symptom to reproduce, and the 668-line merge-boundary-preflight.test.mjs plus the scheduler coverage of the deleted gate are removed because their subject no longer exists, so nothing red-on-parent replaces that coverage and nothing could. What stands in its place is static pinning of the property the deleted gate stood in for: no-self-merge-consent.test.mjs asserts that no engine or lib top-level source carries any of six merge invocation patterns, that every argv buildGhArgv composes over its own exported verb table is free of every merge token, and that the tracked deny list still names all ten PR merge, create and edit surfaces; mitosis-scheduler.test.mjs restores the anchor that the reconcile prompt sources fold-run-log.mjs from the installed absolute path and never from `${repoRoot}/.claude/lib`. RECEIPT WITHDRAWN, and this is the correction: the prior version of this artifact carried a blanket claim that the implementing agent had demonstrated a mutation proof for EACH new or changed assertion. A reviewer demonstrated that the argv assertion had not earned that receipt, so the blanket form was false and is retracted rather than restated. I ran no mutation on any assertion in either test file, and this pass changed only assertion MESSAGE STRINGS and never an assertion expression, so I hold NO mutation receipt for any of them, and whether the reworked per-verb form at :137-138 would earn one is UNPROVEN here. TWO NARROWINGS land in this pass, and narrowing is all they are — no assertion in either test file is added, removed or reordered, and every line number this artifact cites is unchanged. FIRST, the argv token assertion's message (no-self-merge-consent.test.mjs:129) previously claimed that a merge token assembled at run time — from a variable, a template literal, a concatenation or a computed value — is caught there even though no scan of the source text can see it. That claim was FALSE and was demonstrated false: MITOSIS_GIT_STAGES is a hand-typed local list and the per-verb guard requires only one argv at SOME listed stage, so an existing verb (pr-close) with a second branch at an unlisted stage (apply) returning a computed merge argv passed all 232 tests. The message now claims ONLY the enumerated verb/stage pairs, names the local stage list as unsynchronised with buildGhArgv, and points at where the property is actually enforced — ghExecTripwire at mitosis-git.mjs:343 in front of the single spawnSync at :347, fail-closed — and at the source-text ban at tests/mitosis-git.test.mjs:969, which I re-derived and which is the declaration of the test whose banned-substring loop at :970-971 covers the wrapper source. SECOND, the neighbouring MERGE_ARGV_TOKEN_PATTERNS message (:131) carried the same shape, a pair-scoped check trailing the general guarantee that the wrapper may read and open pull requests but never merge one, and is narrowed the same way: pair scope stated, the guarantee reattributed to the runtime chokepoint. The stage axis stays open by ruling and is recorded as a named residual under M2. A THIRD NARROWING, closing a residual an earlier pass had handed back: the enclosing test's own title at :112 carried the same overclaim, reading that every argv the sanctioned gh wrapper can build is merge-free, and it printed on every passing line rather than only on failure. It is NARROWED in this change to \"argvs from enumerated verb/stage pairs are merge-free\", so no surviving line of this test asserts coverage of an argv built at a stage MITOSIS_GIT_STAGES does not name. Like the other two narrowings this changed a string and no assertion expression, and the test still reports 11 tests, 11 pass, 0 fail. The mutation I DID run and own is on this artifact, because an unproven gate is the defect this MSP is about: deleting the B1 row turned `node scripts/invariant-coverage-check.mjs` from ok to FAILED naming the missing id, and restoring it turned it green. Receipts, attributed by who ran them. RUN THIS PASS, which is why the whole-suite figure is no longer CARRIED: `npm test` on this tree reports 1776 tests, 1775 pass, 1 fail, exit 1, and the single failure is the one named below; `node --test` over no-self-merge-consent.test.mjs, re-run after the two message narrowings, reports 11 tests, 11 pass, 0 fail; `node scripts/invariant-coverage-check.mjs` exits 0 and `node .claude/lib/superpowers-parallel/mitosis-gate.mjs phase-parity` reports ok:true. Run by the earlier pass that authored the rest of this row and NOT re-run here: 329 pass / 0 fail over no-self-merge-consent, dead-export-lint, mirror-guard, mitosis-gate and mitosis-git; 251 pass / 0 fail over mitosis-scheduler, frontier-train-e2e and reconcile-only-advance-characterization; 129 pass / 0 fail over all four workflow-sandbox suites. I did re-derive the failure's anchor myself this pass, which the prior version left carried: .claude/hooks/tests/protect-claude-config.test.mjs:194 is `test('each installed probe file still resolves out of the home tree under its own name', (t) => {` and its body calls `t.skip` when no probe file is present, which is what makes it environment-dependent and skipped in CI. It is untouched by this change."
    },
    {
      "id": "M4",
      "verdict": "threatened",
      "check": "a violation with mitigations, not a pass, and the numbers are re-measured today because the prior version of this artifact contradicted itself between this row and M1. Size is NOT the problem. Committed, `git diff --shortstat 106b253^ HEAD`: 11 files, 38 insertions, 1386 deletions. Uncommitted in-scope, by `git diff --numstat` over the three tracked paths: 3 files, 165 insertions, 15 deletions — no-self-merge-consent.test.mjs +150/-14, mitosis-scheduler.test.mjs +14/-0, feat-m5-quiescent-exit.json +1/-1. Plus this one untracked artifact. Net over 106b253^ to the working tree, in-scope tracked paths only: 10 files, 190 insertions, 1388 deletions, feat-m5-quiescent-exit.json dropping out because the revert nets it to zero. The production side is a deleted 410-line module plus a net-negative mitosis.js and a SKILL.md that is 0 insertions against 19 deletions. Bundling is the problem, and there are THREE units here, not two. FIRST, the gate deletion and its mechanical fallout are one coherent change: the module, its test, the mitosis.js constant, reader, schema property, prompt clause and halt branch, the SKILL.md precondition and dispatch gate, and the call sites referencing the deleted exports all die together, and a deletion's fallout is not an independent refactor. SECOND, the no-self-merge-consent.test.mjs rewrite is independently shippable and shares the same reviewable range: it re-architects a surviving guard from a single-file scan into a directory census plus specimen liveness plus argv enumeration, and removes two environment overrides, work that would stand on its own with the preflight left in place. THIRD, the feat-m5-quiescent-exit.json revert is a separate one-line unit; it belongs with this artifact as the M1 repair rather than with the deletion, and it is bundled here too. Against M4's escape clause, no characterization test pinning the surviving merge-abstinence behavior was written BEFORE the rewrite — the five pre-existing single-file assertions were replaced in the same range rather than kept as the pin. The one characterization file in the range, reconcile-only-advance-characterization.test.mjs, changes by exactly one fixture (the 9-line PROVEN_BOUNDARY object and its spread at the withReconcileDefaults merge), a fixture deletion forced by the schema change, not an assertion pin written beforehand. Mitigations, named as mitigations and not as compliance: the replacement guard is strictly WIDER than what it replaced — 6 patterns over 39 sources against 5 over 1, and 10 asserted deny entries against 3 — and the specimen guard proves no pattern went dead in the widening, so the rewrite cannot have quietly weakened the property it inherited."
    },
    {
      "id": "M5",
      "verdict": "threatened",
      "check": "every path:line in this file was re-derived by me today against 106b253 plus the working tree, with Read or grep, and not one was copied from the dispatch, a commit message, a reviewer note or the artifact's own prior version. Re-derived this pass: docs/invariants/registry.json, confirmed to declare exactly twelve ids B1-B6 and M1-M6; scripts/invariant-coverage-check.mjs read for :9, :106-117, :119-139, :122, :135, :136, :137 and :161-179; mitosis.js:1604 (`additionalProperties: false` on RECONCILE_SCHEMA); mitosis-git.mjs:6, :30 and :276-297; no-self-merge-consent.test.mjs :6, :65-72, :66, :69, :75-83, :94, :113, :121-124, :129, :131, :136, :137-138, :141-152, :154-164; dead-export-lint.test.mjs :93 and :98; mitosis-gate.test.mjs:484; mitosis-git.test.mjs :969-971; block-destructive-bash.sh :32 and :46; .claude/rules/common/git/pull-requests.md:3; .claude/hooks/tests/protect-claude-config.test.mjs:194, which the prior version of this artifact carried and I re-derived here. Marked threatened rather than clean for four live provenance failures I found and am recording rather than burying. FIRST: pull-requests.md:3 states that post-creation title/body edits — `gh pr edit --title/--body`, the matching `gh api --method PATCH`, GraphQL, and MCP `update_pull_request` — are denied 'the same way' as the create surfaces, that is at the hook AND in settings. Measured, both halves are wrong for the edit surfaces: the tracked settings.json carries 47 permissions.deny entries and ZERO of them match `gh pr edit`, and block-destructive-bash.sh contains ZERO occurrences of `updatePullRequest`. The doc's claim is the thing that is wrong; it was deliberately NOT added to REQUIRED_DENY_ENTRIES, because asserting a claim the tree does not satisfy would be red on arrival, and it is not corrected in this change. SECOND: THREE committed documents still name the deleted module as live, one more than the prior version of this artifact found — docs/superpowers/plans/2026-07-31-m2-mirror-gaps.md:219 lists it in a mirror census, docs/superpowers/specs/2026-07-31-b6-harness-liveness-implementation.md:349 cites merge-boundary-preflight.mjs:7-10 as a source of the sibling exit-code set, and docs/superpowers/specs/2026-07-28-mitosis-quiescent-advance.md:318 names it as one of the two production importers of merge-watch.mjs. All three are stale as of this change and none is corrected here. THIRD: the restored feat-m5-quiescent-exit.json states its anchors at 42e7d49 and likewise still names merge-boundary-preflight.mjs as a live importer; I restored that file byte-exact and re-derived NONE of its anchors, because restoring a historical attestation is not re-attesting it, and this row makes no claim about whether those line numbers still resolve or about whether its importer sentence describes the current tree. FOURTH, named because it is a provenance claim I cannot close: I did not observe the four measured bypasses of block-destructive-bash.sh recorded under M6 execute; I re-derived only the regex shape they exploit."
    },
    {
      "id": "M6",
      "verdict": "threatened",
      "check": "flipped from the prior version's not-threatened, because this change REMOVES a deployed gate artifact from the installed surface and leans on the remaining layers, and both facts bear on this invariant directly. The removal is clean and at the source, measured rather than reasoned: `ls -ld ~/.claude/lib` shows it is a SYMLINK to this repository's .claude/lib (and ~/.claude/workflows likewise), so the absolute installed path the deleted preflight was invoked from resolves into committed source, and `ls ~/.claude/lib/superpowers-parallel/merge-boundary-preflight.mjs` now reports No such file or directory — deleting the tracked file deleted the installed artifact, with no stale deployed copy and no hand-patch anywhere. `grep -rn 'merge-boundary-preflight|BOUNDARY_PREFLIGHT|boundaryPreflight|MITOSIS_BOUNDARY' .claude docs scripts` leaves zero references under .claude/ or scripts/ outside a stale worktree checkout, so no surviving instruction points at a gate that no longer exists; the three stale doc references are recorded under M5. The gate artifacts that enforce this repository are untouched: no changed path matches `hooks/|workflow-sandbox|registry.json|invariant-coverage-check|receipts.yml`, so the checker, the registry, the receipts CI template and every hook are unchanged, and mirror-guard's census loses exactly the row for the deleted module and ran green inside the 329 pass / 0 fail. CORRECTION, and the reason this row must not be read as an all-clear: nothing here establishes that the layers this change now leans on are SUFFICIENT, and one of them has MEASURED bypasses. A security review measured four live bypasses of .claude/hooks/block-destructive-bash.sh — `gh -R owner/repo pr merge 5`, `\"gh\" pr merge 5`, `gh pr \"merge\" 5` and `$(which gh) pr merge 5` are all ALLOWED, as is the `gh alias set m \"pr merge\"` route, and `gh api graphql` carrying `updatePullRequest` is allowed so a PR body can be rewritten after review. Those executions are that review's receipt, CARRIED and not mine; what I re-derived myself is the shape that permits them, `ghtok` at block-destructive-bash.sh:32 requiring `gh` followed immediately by whitespace and the guard at :46 matching `${ghtok}pr[[:space:]]+merge`, which does require `pr merge` adjacent to the gh token, and the zero `updatePullRequest` occurrences in that file. Those defects are PRE-EXISTING, are filed as separate follow-up work, and are not this change's to fix — but any reading of this artifact that treats the bash gate as a sufficient merge barrier is false. A second measured limit of the same kind: ~/.claude/settings.json is a REGULAR FILE and NOT the same file as the tracked .claude/settings.json (checked with `-ef`; unlike lib and workflows it is not a symlink), so the deny list this suite asserts over is the tracked, diffable copy and not the installed one the permission system actually reads. no-self-merge-consent.test.mjs:169 says exactly that in its own failure message, which is honest, and the gap is real and uncovered. The standing tension, unchanged and named rather than assumed away: mitosis.js is a hand-maintained twin of the lib modules rather than a versioned build of them, and what keeps it on the right side of this invariant is that no generator exists whose reproduction could diverge and that mirror-guard fails on any one-sided edit. If a generator is ever introduced for mitosis.js, this row must be revisited."
    }
  ]
}
