---
Status: accepted
Date: 2026-08-02T02:49:30.110Z
Thread-Id: 01KYR405KFXHM15J5XXK5BXTVT
---

# 0193. The termination remedy is the in-process window-accessor bound, and BOTH audited routes independently refuted the payoff the recommendation rested on

## Context

0192 required the recommended termination remedy be audited before implementation. Two dedicated test-engineer auditors ran in parallel, fenced to scratch with MITOSIS_PATH mutants and forbidden to touch the repo; both verified the tree untouched at ba981cc afterwards. Auditor A took the child-process route, auditor B the in-process observable bound. PRECONDITION DISCHARGED FIRST: the 12 staged paths were committed as ba981cc before either agent ran, which protected the 3.55M-token asset, dissolved 0191's two manufactured findings, and turned invariant-coverage-check --event pull_request --base-ref main from exit 1 to exit 0, discharging the M1 row's open landing obligation. The pre-commit hook ran the full suite on this tree first-hand: 1845 pass / 0 fail. BOTH auditors re-derived every anchor at ba981cc rather than trusting the brief; the quiescent exit is leases.mjs:160 and mitosis.js:2386, and the two loops are byte-identical modulo the export prefix. B refined the premise it was given by mechanism: the spin is NOT literally synchronous, there is an await on Promise.allSettled([]), but it yields microtasks only - measured 3,000,001 iterations over 1941ms with ZERO macrotasks, against a self-validating probe that saw 26 macrotasks in 140ms of real event loop. So node:test's { timeout: N } genuinely cannot fire, now established rather than inherited.

## Options

- Adopt auditor A's child-process liveness test with a hard-kill deadline
- Adopt auditor B's in-process window-accessor bound, an existing pinned public contract
- Adopt B's mechanism but keep A's aaa- filename prefix for full-suite log visibility
- Take the 0192 fallback and declare the limit in the M3 receipt row with no new test
- Add a separate npm script and CI step so the liveness test runs outside the hanging files

## Outcome

SHIP AUDITOR B'S IN-PROCESS BOUND, WITH AN ORDINARY FILENAME, PLUS A CANDID M3 RESIDUAL. THE DECISIVE FINDING IS ONE NEITHER AGENT WAS ASKED FOR AND BOTH REACHED INDEPENDENTLY: neither route makes the exit-removed mutant redden the full npm test invocation. A measured it hardest - full 62-file mutant suite, notOk=0 at 75s, 3/3 reps - and found the counter-intuitive cause by mechanism: ticks.push allocates unboundedly under the mutant, and the resulting memory pressure stalls node --test's output flush, so a LONGER kill deadline is strictly WORSE (1 hanger at 20000ms prints nothing at 60s; at 5000ms it prints at 6.0s). B independently hit the same wall from the other side: the suite still never exits, no summary and no rc=1, because the sibling scheduler tests spin regardless. The orchestrator's original recommendation rested on 'a RED test rather than a suite-wide hang', and that payoff does not exist on either route. 0192's method rule is vindicated on the orchestrator's own recommendation, which is the tenth instance. RULING ON THE THREE PILLARS, Quality first: (1) B has ZERO wall-clock dependence - a pure integer counter on opts.window, which runScheduleTick already resolves once per iteration BEFORE the exit check, and whose per-iteration cadence is ALREADY a pinned public contract at leases.test.mjs:350. No production machinery is added, so this is not maxSteps growing back, which was the disqualifying condition. A's hard-kill deadline IS a wall-clock bound that A itself concedes could false-red, and testing discipline forbids sleeps and flake outright; a 35x margin is a good margin, not the absence of the dependence. (2) B proved NON-CHANGE-DETECTOR status by mutation - a behaviour-preserving refactor stays green - which A did not test. (3) B reddens the epoch-guard mutant too. (4) B is 500x cheaper in test-body time, 346ms against 592ms green and 3507ms red, and pays no timeout on the red path. A's ONLY advantage over B was suite-level visibility, and A's own measurements refuted it. ADOPTED FROM A, because it is the real synthesis win from running two auditors: A's mirror-guard composition argument is the coverage rationale for B's test, since B also drives runSchedule from the lib twin. A measured mirror-guard at lib-twin-only mutant RED, engine-only mutant RED, BOTH-twins mutated 46 pass / 0 fail - so mirror-guard covers every one-sided mutation and the lib-side bound covers the both-sided one, and the three shapes compose to full coverage without the ~250 lines of createFakeAgent/buildInput replication that reaching mitosis.js in a child would have cost. REJECTED: the aaa- filename prefix, because the full-suite visibility it buys is exactly what both auditors showed is unreliable, and B named the prefix brittle and unpoliced - paying a permanent brittleness for a benefit that does not survive measurement is the wrong trade. REJECTED for this MSP: the separate npm script and CI step, which is real scope creep into receipts.yml and .github/workflows/test.yml, all three of which the M6 row certifies untouched; it is recorded here as the known way to recover suite-level visibility if it is ever wanted. THE M3 ROW THEREFORE DECLARES, MEASURED: exit removal manifests as a full-suite hang rather than a red, the new test gives a deterministic isolated rc=1 and a diagnostic that names the starvation mechanism, and the suite-level exit code is NOT recovered. That is the 0192 fallback kept ALONGSIDE the test rather than instead of it. Also owed and unchanged: the M6 row's 'the staged set is empty' sentence is false and is a one-line fix.
