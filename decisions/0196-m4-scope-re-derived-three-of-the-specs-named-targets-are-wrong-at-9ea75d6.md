---
Status: accepted
Date: 2026-08-02T04:44:06.491Z
Thread-Id: 01KYR405KFXHM15J5XXK5BXTVT
---

# 0196. M4's scope is re-derived by measurement at 9ea75d6; three of the spec's named deletion targets are wrong and one deletion is not pure AIMD

## Context

M4 was dispatched against spec section 3.3, which names its deletion targets with cites stated at 42e7d49 or earlier. The M4 dispatch brief required re-deriving every anchor by grep because all cites are stale. A read-only grounding pass at 9ea75d6 measured the actual surface rather than trusting the spec text, and four of its findings change what M4 may safely do. (1) readReviewDecision and resolveReviewEvent, named by section 3.3 and by mechanism-ledger row 9 as M4's deletion target with cite :4730-4747, DO NOT EXIST at HEAD: git log -S proves M5's own commit 9ea75d6 deleted them, because their only caller lived inside the nine-symbol merge poll M5 removed. The still-live pipeline that actually feeds AIMD is differently named - the reviewDecision capture at mitosis.js:3089 and the events loop inside buildReconcileLiveSignals at :3106-3121, a mitosis.js-only function with no lib twin. (2) clampWindow was never part of the window.mjs twin; it exists only at mitosis.js:2420-2422, so it carries no mirror obligation, contrary to the spec grouping it with the mirrored constants. (3) WINDOW_FLOOR is not pure AIMD plumbing: leases.mjs:111 uses it as buildAheadWindow's generic fallback when windowSize is not an integer, a non-tuning path, so a literal delete silently moves that fallback from 3. (4) The nine 'AIMD window W=' assertions in frontier-train-e2e.test.mjs were independently re-counted and the figure is confirmed exactly nine, at :477 :502 :586 :732 :733 :854 :1106 :1311 :1335. Separately, window.test.mjs and run-log.test.mjs import deleted-to-be exports directly and would fail at LOAD, the progressPossible failure mode this thread has already paid for once.

## Options

- Implement M4 literally as spec section 3.3 words it, deleting the symbols it names
- Re-derive the surface by measurement at 9ea75d6 and let the measured surface govern, recording where the spec is wrong
- Halt M4 and rewrite spec section 3.3 first so the text matches the tree

## Outcome

THE MEASURED SURFACE GOVERNS; the spec text is treated as stale where it disagrees, and this record is the correction rather than an edit to the spec. This is decision 0148's standing posture (re-baseline by execution per MSP, never rewrite the anchors as a standalone effort) applied to M4, and 0151's incomplete-list finding recurring for the fifth time in this thread. Four consequences bind M4's implementation. Its review-decision deletion retargets from the two non-existent symbols to buildReconcileLiveSignals' events loop at mitosis.js:3106-3121 plus the :3089 capture, which is mitosis.js-only and therefore carries NO twin obligation. clampWindow likewise carries no twin obligation. WINDOW_FLOOR's fate is an explicit, named decision in the M4 diff and not an incidental deletion, because leases.mjs:111's generic fallback is a real non-AIMD consumer whose value would move silently. And the test work is larger than a log-string edit: nine e2e assertions, two test files that fail at IMPORT rather than at assertion, and three reconcile.test.mjs deepEqual shape checks that break on dropping nextW from planReconcile's return even though they are not about AIMD. TWO ITEMS ARE DELIBERATELY LEFT OPEN for the implementing session rather than pre-decided here, because both are genuine scope calls the grounding pass correctly refused to make: whether resolveRunIdentity's window carry-over at mitosis.js:722 (twin in recovery.mjs, pinned by recovery.test.mjs:639 under the label 'the persisted AIMD window is carried') dies with AIMD or survives to persist an engine-arg override across relaunch; and whether the section 3.3 divergence and rebuild-unit counters are per-run locals logged once at mitosis.js:3949-3967, or a durable new applyRunDelta kind requiring a case in both twins. NOT RE-OPENED: the cap value. Spec section 13 still lists BUILD_AHEAD_CAP as open decision (b) gating M4, and the grounding pass correctly flagged that as a live discrepancy, but 0086 confirmed K=8 on the user's 2026-07-28 instruction; the spec's approval section is simply stale on that point and M4 does not re-litigate it.
