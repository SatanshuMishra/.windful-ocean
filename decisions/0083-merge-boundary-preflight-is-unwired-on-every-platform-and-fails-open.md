---
Status: accepted
Date: 2026-07-28T19:57:52.342Z
Thread-Id: 01KYJTVMQ06B0NY6ENYKZCRYBP
---

# 0083. The 20 red tests are a severed preflight on every platform, not a test artifact and not a portability bug

## Context

The thread left the 20 red tests at beca874 UNVERIFIED between hypothesis A (test artifact) and hypothesis B (portability bug dropping the preflight on Linux). A debugger measured the suite at beca874 in a detached worktree - tests 1540, pass 1520, fail 20, confirming the 18 BOUNDARY PREFLIGHT + 2 guardrail split - then traced the code instead of reasoning from the path literal. In .claude/workflows/mitosis.js at beca874, BOUNDARY_PREFLIGHT_CLI occurs exactly once (its definition at :24), readBoundaryPreflightVerdict exactly once (:127), and the string preflight-boundary zero times. grep for existsSync/accessSync/statSync returns zero hits, so no path-existence conditional exists to gate anything; grep for homedir/process.env.HOME/__dirname/fileURLToPath returns zero hits, so the emitted prompt is byte-identical regardless of HOME or OS. Under HOME=/home/runner the scheduler tests reproduced the identical 18 failures with identical names, and in that same run the only absolute path emitted into any prompt was the hardcoded fold-run-log.mjs path - interpolation demonstrably working and HOME-independent - while merge-boundary-preflight.mjs occurred 0 times across every emitted prompt. Provenance: 7e2e7d7 (PR #8) deleted both call sites that 457d6fa (PR #5) added - the step-7 prompt paragraph and the readBoundaryPreflightVerdict halt returning fatalReport(preflight-boundary). Reference counts run 457d6fa 3/2/1, 7e2e7d7 1/1/0, beca874 1/1/0. RECONCILE_SCHEMA at :1357 still declares boundaryPreflight as optional and absent from its required array, so a reconcile result with no verdict validates cleanly - the enforcement was removed while its shape survived, which is why the regression was silent.

## Options

- A - test artifact: tests assert an author-machine absolute path; the engine still runs the preflight on Linux
- B - portability bug: the preflight instruction is dropped on Linux, so the check runs only on the author's laptop
- C - the wiring is severed on EVERY platform; both call sites deleted by 7e2e7d7 and the engine fails open

## Outcome

C for the 18 BOUNDARY PREFLIGHT failures. The preflight is dead code on every host including the author's macOS laptop; the check runs nowhere. The engine FAILS OPEN, proven by actual 'all-shipped' versus expected 'failed' with boundaryPreflight absent - the run reaches decompose and ships with zero boundary proof and zero diagnostic. Since 7e2e7d7 the server-side boundary invariants (branch protection, required approving review, machine-user identity, base-branch and repo-slug attestation, gate provenance) are never corroborated engine-side, and a verdict produced by a preflight copy vendored inside the repo being merged into - the exact self-authorization attack 457d6fa closed - is undetectable. The gate itself is healthy: merge-boundary-preflight.mjs still exists and is exercised by merge-boundary-preflight.test.mjs and frontier-train-e2e.test.mjs; only the engine's invocation is severed. The hardcoded /Users/satanshumishra literals at mitosis.js:21-25 ARE a real portability defect for running mitosis on Linux, but they are orthogonal, and restoring the wiring would fail CLOSED there because ENOENT yields boundaryPreflight=null, which readBoundaryPreflightVerdict (:127-131) treats as unproven. The 2 guardrail failures are A, a genuine test artifact: protect-claude-config.sh:20-33 builds its protected-base list only from ~/.claude while protect-claude-config.test.mjs:8 derives REPO_CLAUDE from import.meta.url; these coincide only because ~/.claude is a symlink into this repo, and diverge in any worktree or CI checkout. Fixes recorded and deliberately NOT applied: restore the step-7 paragraph before the 'Return ONLY the structured object' line at mitosis.js:3720; restore the verdict halt after the targetRepoHost line at mitosis.js:3746, placement load-bearing because 16 tests assert decompose is not dispatched; seed protect-claude-config.sh:22 bases with the hook's own enclosing .claude directory. The 18 existing tests are already the red proof, so no new test is admitted. Test :1716, asserting the prompt emits the anchored read-only preflight, would have caught 7e2e7d7 at landing and must not be deleted. Applying the fix is owned by the mitosis-preflight-hardening thread, and a guard hook holds .claude/lib, .claude/workflows and .github/workflows edits for a human. NOT DETERMINED: whether 7e2e7d7 dropped the wiring deliberately or via a bad merge resolution, and whether fix/mitosis-boundary-preflight (c59ca79) already addresses it - worth checking before anyone implements the fix.
