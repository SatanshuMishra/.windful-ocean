{
  "name": "windful-ocean",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic test backstop for the mitosis parallel-execution engine",
  "scripts": {
    "test": "node --test $(ls .claude/lib/superpowers-parallel/tests/*.test.mjs | grep -v generate-run-script.test.mjs)"
  }
}
