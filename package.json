{
  "name": "windful-ocean",
  "version": "0.0.0",
  "private": true,
  "description": "Personal Claude Code configuration and dotfiles repository, with a deterministic test backstop for its hooks and scripts",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test .claude/lib/*/tests/*.test.mjs .claude/hooks/tests/*.test.mjs .claude/skills/*/tests/*.test.mjs scripts/repo/tests/*.test.mjs"
  }
}
