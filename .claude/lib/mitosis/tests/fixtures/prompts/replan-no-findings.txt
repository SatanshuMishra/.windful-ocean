You are revising the implementation plan for MSP "fx-unit" (fx unit title) of a mitosis run after an adversarial review returned needs-changes. You have NO Skill tool.

Current plan: /fx/repo/.mitosis/fx-unit.plan.md. Scope of this MSP: fx rationale sentence. Earlier MSPs already planned/merged it may depend on: fx-dep-one, fx-dep-two.

Review findings to remediate:
(no structured findings supplied; the review was a non-approval — re-examine the plan against necessity, regression-risk, over-scope and parallel-safety yourself)

Address EACH finding minimally. Do NOT over-correct and do NOT expand scope: fix exactly what the finding names and nothing more, keeping the plan the smallest correct plan that satisfies the pillars (Quality > Optimization > Speed). Overwrite the SAME plan file idempotently at /fx/repo/.mitosis/fx-unit.plan.md (create the .mitosis directory if absent).

Return ONLY: { planPath: "<absolute path to the revised plan you wrote>", summary: "<one sentence on what you changed>" }.