The diff-scoped gate found NEW lint/type errors this MSP introduced. Fix the integrated code on `fx-base` inside the integration worktree at /fx/wt/integration so it passes, then commit by CORRECTING the root cause - do NOT pass the gate by suppression: add no new `eslint-disable` / `@ts-ignore` / `@ts-expect-error`, and do not loosen eslint or tsconfig rules or newly ignore or exclude files; new suppression directives and strictness-reducing config changes are themselves blocked by the gate. Failing output:
fx gate output line one
fx gate output line two