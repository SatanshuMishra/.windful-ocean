BUILD session. Commits 1-3 of 7 landed on fix/pre-tool-use-guard in continuity-ledger-plugin. Nothing merged, nothing pushed, nothing published; the 0016 merge block was respected throughout. Working tree clean, no background agents left running.

SHIPPED.
Commit 1 b920ddd fix(hooks): fail closed on oversized Bash input. MAX_COMMAND_BYTES 16384 seated after the type/roots guard and before scanSegments, so the frozen shell-tokens.mjs never receives unbounded input. Oversized command that names a root by substring returns deny; oversized command that names no root returns allow. Genuine red first: at the 16385 root-referencing cell the pre-change code returned false. Green after at 515 pass 0 fail (baseline 513 + 2). Comparison basis is command.length in UTF-16 code units, which is exactly what scanSegments iterates and therefore the true cost driver; the constant keeps the MAX_COMMAND_BYTES name because 0018 locks it.

Commit 2 11cfa0e test(hooks): tokenizer characterization for the frozen shell scanner. New test/unit/hooks/shell-tokens.test.mjs, 16 tests, 16 pass. LOAD-BEARING RESULT: all 11 probe-table facts were re-verified empirically against the frozen tokenizer and ALL ELEVEN MATCHED, zero discrepancies. The entire inversion rests on that table, so this was the cheapest place for the design to be wrong and it was not. Confirmed: ampersand, parens and braces are ordinary word chars; semicolon splits; glued ampersand welds tokens; open-paren fuses into the head; 2-redirect plus ampersand-1 word; sh -c keeps one opaque token; single-quote unresolvable false vs double-quote-with-dollar true; less-than is operator; heredoc yields 3 segments with heads cat/line/EOT; dollar-quote yields dollar-rm; pipe splits.

Commit 3 b60ac56 test(hooks): corpus for deny-by-default guard inversion. New test/unit/hooks/pre-tool-use.corpus.test.mjs, COMMITTED DELIBERATELY FAILING so the red state stays reachable. Verbatim: tests 126, pass 77, fail 49, duration_ms 65.17825. Full suite at red: 657 tests, 608 pass, 49 fail, with all 531 non-corpus tests green so nothing pre-existing regressed. Failing ids: G1 G2 G3 E1 E2 E4 E5 P1 P2 P3 E18 E20 E21 S1 S2 F1 F2 GIT1 GIT2 E8 E9 X1 A1 E3 E6 B4 O1 O2 E16 E22 E23 FP1 J1 OB1 OB2 OB3 OB4 OB5 OB6 OB7 OB8 OB9 OB11 OB12 X2 O2OV HEREDOC1 AWKPIPE SUBST. Four deny cases already pass at red because the TIP guard catches them: N2 N3 E17 OB10. All 71 allow cases pass at red, zero allow-side false failures. ACTUAL counts 48 deny / 5 ask / 71 allow, 124 cases; 12 and 41 were asserted as floors only and never presented as measurements, per the honesty requirement.

KEY DESIGN CALL MADE THIS SESSION. The test-engineer flagged six ambiguities. All six resolve inside the locked design with NO amendment; recorded as decision 0020. One was a subagent misread that I overruled: E12 diff process-substitution does NOT need a carve-out, because splitControl emits the post-paren REMAINDER as the new head, giving heads diff/cat/cat (all allowlisted) where E1 gives rm (not allowlisted). The post-paren head IS the E1-versus-E12 discriminator and it is already in the design; a carve-out would be redundant and would weaken G1's cheapest detector.

RISK ITEM CORRECTED, not resolved. The inherited risk 'the design spec is UNTRACKED, commit it or it dies' was written without knowing that .claude/docs/superpowers/ is DELIBERATELY gitignored at .claude/.gitignore:44 and that ZERO of the 22 specs there are tracked. Tracking it needs git add -f against standing policy, so it is a user call, not a chore. User was asked and chose to LEAVE IT AND DECIDE LATER. Durability risk is largely retired anyway because 0018 and 0020 now carry the load-bearing design content in the synced ledger.

INCIDENTAL FIELD EVIDENCE. The installed LIVE guard 0fe1c02 denied one of my own pure read commands during this session purely because the text carried 2-redirect-to-dev-null plus a root substring. That is a second first-hand reproduction of the canonical false positive this work exists to kill, independent of the one captured during the design session.

NOT DONE, and why. Commits 4-7 remain: allowlist tables, splitControl, the inversion, the narrow runGuardEntry. Stopped at 80 percent orchestrator context rather than start the inversion, which is the most delicate commit in the sequence and rewrites roughly 46 test assertions. No work was attempted and abandoned; the stop is a clean boundary between commit 3 and commit 4. Criterion 3 is satisfied by commit 1 and is flipped to done. Criteria 1, 2, 4, 5 remain open; criterion 2 is half-satisfied, red evidence recorded, green half pending.