SESSION CLOSE. Eighteen commits landed above b60ac56, suite green at 735/735 verified independently, nothing pushed or merged. CRITERION 4 FAILS A SECOND TIME: both re-reviews over f95bdcb..HEAD returned BLOCK. Every finding below was demonstrated by EXECUTING the payload, not by reading the diff. No subagents or background tasks are still running.

WHAT SHIPPED. Commits 4-7 completed the original build (c304b7c allowlist tables, 025a5a3 splitControl, 1668944 the inversion, f95bdcb runGuardEntry). Then the user-authorized hardening round: c3f8137 RED corpus (715/688/27); round A on clearance 4d9193d, 8c2e8c2, b41bebd, 616d5b4 (719/708/11); round B1 on the engine ae7b975, 05dfa77, 937b66b, dd89bf2, a0125ea (719/719/0); round B2 3cb599d, 4b54a46, 635ad98, b21ab86 (735/735/0).

PRIOR FINDINGS ALL CONFIRMED CLOSED by re-execution: C1 write-capable heads, C2 sed script body, C3 git past the subcommand, C4 oversize both directions, H5 both cd desync forms, H6, H7 lexical dotdot, the ampersand shredding of quoted literals, prefix-word stripping, and the opener stack overflow at 50000 openers. The hardening round did what it was authorized to do.

NEW SECURITY FINDINGS, all in code the hardening round ADDED plus one surface never inspected on either pass. CRITICAL-1 UNTERMINATED QUOTE FAIL-OPEN: both scanners resolve an unmatched single quote as quoted-to-end-of-input, so ANSI-C quoting and hash comments emit a quote that is not an opener; the phantom quote swallows every remaining separator into one word token under a cleared head. cat dollar-quote-backslash-quote semicolon rm -rf ROOT returns ALLOW and executes under real bash 3.2 and zsh. Fires with semicolon, and-and, pipe, newline and redirects, not only ampersand - the new ampersand gate WIDENED an existing hole rather than creating it, because the unconditional split used to rescue the ampersand variant. CRITICAL-2 env -S: env is a PREFIX_WORD, skipPrefixOperands eats the whole -S payload as a flag, the index runs off the end and normalizeHead returns assignment-only, which unitOf treats as CLEARED. CRITICAL-3 ENVIRONMENT ASSIGNMENTS ARE SKIPPED WITHOUT INSPECTION: PATH=/tmp cat ROOT/f returns ALLOW and was verified to DESTROY a stand-in ledger via a hijacked cat. Same gap re-opens the -c bounding escape through GIT_CONFIG_COUNT/KEY/VALUE (documented by git as exactly equivalent to -c), GIT_EXTERNAL_DIFF, GIT_PAGER and RIPGREP_CONFIG_PATH, all confirmed to run. HIGH-4 bare dash counts as a flag, so uniq - ROOT/f and xxd - ROOT/f leave the ledger path as the OUTPUT operand; all four forms wrote the target. HIGH-5 sort accepts any unambiguous long-option prefix, so --out= and --outp= both wrote the file, and --compress-program hands its value to sh (proved by the sh error message). HIGH-6 isUnderRoot calls resolve() BEFORE canonicalizing, collapsing dotdot lexically while POSIX resolves the symlink first, so link/../PROJECT.md escapes; the symlinked-ROOT direction works, so the fix is half-complete. MEDIUM-7 file --comp abbreviates past the exact --compile match. MEDIUM-8 tree uses exact -o while its sibling sort uses the bundled form - inconsistent regardless of how tree parses (unverified, tree not installed). LOW-9 the size gate measures UTF-16 units, not bytes.

NEW CODE-REVIEW FINDING, HIGH-1, AND IT WAS MY ERROR: I instructed the implementer to accept the mixed-ampersand residual as fail-CLOSED. It is fail-OPEN. Shredding a token DESTROYS the root substring, which removes the unit from scope and therefore ALLOWS. With a root containing an ampersand, appending 2-greater-ampersand-1 or and-and-echo bypasses the guard, and the two corpus cases assert it closed only because those exact strings carry no other ampersand - the same test-proves-two-strings pattern the reviewer flagged the round before. FIX WITHOUT TOUCHING THE FROZEN TOKENIZER: compute SCOPE on the ORIGINAL unsplit segment tokens (so a quoted root substring survives) while still deriving HEADS from split sub-segments. Inheritance already shares scope across sub-segments, so this is contained, and it makes the residual genuinely fail-closed as I wrongly claimed it already was.

OTHER CODE-REVIEW FINDINGS: MED-1 O1 drops git/find by NAME, so cat ROOT/f and-and git push, git init, git hash-object -w and find -delete all went from ask to ALLOW while rm still asks - fix by gating on whether the other unit is CLEARED, not on its name. MED-2 removing -c, -p, --paginate, --namespace, --super-prefix, --exec-path from the git globals hard-DENIES legitimate reads like git -C ROOT -c color.ui=false log; GIT_CFG_BENIGN only appears to pin this because it names no root and never reaches clearance. MED-3 GIT_EXEC_CONFIG_KEY misses pager.SUB, alias.NAME, gpg.program, credential.helper, core.gitProxy, uploadpack.packObjectsHook, core.fsmonitor, init.templateDir, include.path and filter.CLEAN - invert to a small inert-key allowlist, which is what deny-by-default implies. MED-4 uniq/xxd count a valued flag's value as an operand, over-denying uniq -f 1 and xxd -c 16. MED-5 the unbounded deny fires before the scope gate and emits a reason claiming the command targets the ledger store when it does not, offering unactionable remediation. MED-6 corpus why fields now mix two conventions, several describing the pre-fix defect in present tense while asserting the fixed verdict, and three cite sedAllows which no longer exists. LOWs: no direct tests for shell-source.mjs or the splitsAmpersand=false branch; isUnderRoot now THROWS on a non-string root where it used to return false; classifier latency 630 ms on an 8000-ampersand in-cap command because canonicalPath re-walks per root per token; shell-source.mjs duplicates the frozen tokenizer's quote logic; shellCwd applied twice; several predicates exported with no cross-module consumer.

ONE ITEM NEEDING AN EMPIRICAL CHECK, flagged speculative by the reviewer: the H6 fix is correct as code, but its effectiveness depends on Claude Code actually reporting a cwd that tracks intra-session cd. The docs say it does; nobody has proven it against the live harness. If the harness always reports the project dir, the fix is inert against the original H6 scenario.

NON-FINDING worth keeping: cat /tmp/a hash-quote ampersand rm -rf ROOT returns ALLOW but real bash treats the tail as a comment, so nothing executes. The reviewer discriminated this correctly rather than padding the count.

CLEAN ON RE-AUDIT: the iterative opener strip (50000 openers deny, no throw), the ampersand over-split direction, git glued -c and -C forms (rejected by git itself), and a fresh sweep of all 38 ALLOW_HEADS members finding no further writers beyond uniq/xxd. The reviewer's closing point is the important one: the residual risk in that list is not a missing flag, it is CRITICAL-3 - a bare command name is not a stable capability when the environment is attacker-supplied.

TEST-COVERAGE GAP: grep across test/ for dollar-quote, hash-quote, env -S, split-string, PATH=, GIT_EXTERNAL_DIFF, GIT_CONFIG_, uniq dash, xxd dash, compress-program, dotdot and LD_PRELOAD returns ZERO hits for every one. Green at 735/735 proves the fixed vectors stay fixed and says nothing about these.

MERGE BLOCK STANDS. Handing off for round C in a fresh session per user instruction.