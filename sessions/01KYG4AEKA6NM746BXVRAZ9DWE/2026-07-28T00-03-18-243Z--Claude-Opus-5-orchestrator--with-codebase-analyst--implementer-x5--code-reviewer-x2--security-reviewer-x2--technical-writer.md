Items 3 and 4 both shipped, plus five review-driven fix rounds. HEAD is d102fb8 on fix/pre-tool-use-guard; 30 commits ahead of origin, NOTHING PUSHED, no PR. Suite independently re-run by the orchestrator on a quiet machine at three separate points: 604/604 then 622/622, 0 fail, check-packaging ok each time, clean tree.

ITEM 3 (f69add3, plus refactor 1643934). LocalDriver gained a private git recovery repo at <ledgerRoot>/.git, gated on !isGit(), committing per mutation through the existing commitAndReindex choke point, degrading without throwing when git is absent. sync() deliberately untouched. Mapping first established the fact 0030 lacked: GitRefDriver EXTENDS LocalDriver. GitRefDriver.init() turned out NOT to call super.init(), so the trap was never live through init(); the isGit() gate is defence in depth and is pinned by a regression test either way. The implementer found empirically that an ambient GIT_DIR hijacks git init, placing the repo elsewhere and leaving the target with none - a driver that believes it has history and does not.

FRESH REVIEW (round 1, over f69add3). Code-reviewer and security-reviewer both APPROVE-WITH-FIXES, no CRITICAL, no HIGH - the review half of criterion 4 satisfied for the first time on this thread. Both verified by EXECUTING real git rather than reading. They converged on one root cause: the env pinning closed the repo-LOCATION axis completely but left CONFIG LOADING wide open, and config is a code-execution surface. Verified: four env channels inject core.hooksPath and execute hooks; GIT_TEMPLATE_DIR installs hooks that persist; core.fsmonitor fires from git add; --no-verify suppresses only pre-commit and commit-msg. Decisively for the accident model, the user's own ~/.gitconfig is read in full - an ordinary global core.hooksPath runs the user's post-commit on every ledger commit, and a global core.excludesFile silently drops every thread record while commit() reports committed:true with a real sha.

FIX ROUNDS. Round A (drivers) + Round B (guard) ran in parallel on disjoint files, then Round 2 (GitRefDriver, user-authorised per 0043), then a final consolidated round. Landed: full config lockdown via new src/util/git-env.mjs; repair-on-every-init; lstat+isDirectory on the .git probe; recovery_degraded surfaced through commitAndReindex into all ten mutating tool results; dead latch deleted; ambient commit dates cleared; commit() message contract made symmetric across both drivers; guard now answers ask instead of silence on an unreadable Bash command; runGuardEntry decode/read/write moved inside try with an exit-code floor; non-plain-object stdin denied; byte-accurate length cap; auto-allow intersected against the real TOOLS registry, loaded LAZILY because a static import cost ~42ms on every tool call; literal CLAUDE_PLUGIN_DATA path added as a trigger spelling; hooks/lib/git.mjs hardened across all three functions; and src/util/git-scope.mjs introducing four scopes behind one scopedExec choke point (0050).

ITEM 4 (d0454ab). README section stating what guards the ledger, what does not, and what is unverified. Every in-repo claim pinned to path:line, sandbox claims cited to https://code.claude.com/docs/en/sandboxing (fetched). Hooks/MCP sandboxing labelled unverified with the outage consequence named, exactly as 0037 requires. Writing it CORRECTED three claims this thread carried as fact (0051): there is no LEDGER_REF constant (our own B7 refactor removed it), denyWrite takes literal prefix-resolved paths not globs, and the 68-command figure is pinned by nothing. The ledger_branch gap got SHARPER under verification - under the DEFAULT orphan-branch backend a custom branch resolves to refs/heads/<branch>, which the guard never checks.

PROCESS FAILURE, unresolved, needs a user call. Round B used git add -A in three commits and swept Round A's in-flight uncommitted files into them. No content lost, tree clean, suite green - the damage is attribution and atomicity. src/util/git-env.mjs, the highest-priority fix of the round, exists ONLY in 8962003, whose message is about the CLAUDE_PLUGIN_DATA tripwire; 5110d8b and 7294e70 carry slices of test/unit/drivers/local-driver.test.mjs. Both implementers declined to rewrite history unilaterally, correctly. All 30 commits are unpushed, so a cleanup would rewrite local-only commits rather than published history; with squash-on-merge as the house default the messy interior never reaches main. Orchestrator recommendation: leave it. Every later round was instructed to use explicit pathspecs and none recurred.

THE REVIEW RECURSION, which is the live problem. Criterion 4 requires reviews over the FINAL diff. Round-1 reviews ran over f69add3 and drove 16 fix commits. Confirming reviews then ran over 351a931 and drove 10 more (through d102fb8). So the confirming reviews are AGAIN reviewing a superseded diff - the exact trap this thread already fell into once. Criterion 4 is therefore NOT met at HEAD and is left false. This is not pedantry: every fix round so far has introduced new findings, and the final round is no exception in kind - it added ABSENT_HOOKS_PATH, split the env helpers, and consolidated fixtures. The cycle needs an explicit termination rule from the user, not another lap by default.

Also captured: the final round found two review findings overstated and narrowed them (0054) - the networkScope vector needs protocol.ext.allow=always alongside url.<ext::cmd>.insteadOf on git 2.55.0, and the /t case is NOT a defect but the accepted cost of substring matching under 0029. All verification is macOS/darwin, git 2.55.0, Node 26.4.0 only; Windows and older git are unverified throughout.