CRITERION 4 FAILS. The security review returned BLOCK: four CRITICAL and three HIGH findings against the branch at f95bdcb. The orchestrator INDEPENDENTLY REPRODUCED every headline claim by calling classifyBashCommand directly with roots ['/repo/.git/ledger'] and baseDir '/repo'. These are verified, not relayed.

VERIFIED BYPASSES, all returning null (allow):
C1 five ALLOW_HEADS members write files with no redirect - sort -o ROOT/f, uniq IN ROOT/f, xxd IN ROOT/f, tree -o ROOT/f, yq -i ROOT/f. Decision C justified these as "stream filters, no file-write flags in common use"; that premise is factually FALSE for at least five of the thirty-eight members. sort -o, uniq IN OUT and xxd IN OUT were execution-confirmed by the reviewer to replace file contents.
C2 sedAllows models only -i, but sed carries write and execute capability in the SCRIPT BODY - sed -n 'w ROOT/f' and sed 's/a/b/w ROOT/f' both write; GNU sed's e command executes arbitrary shell. The script is one quoted token, so the option parser never sees it.
C3 git clearance stops AT the subcommand - everything after it is unexamined. git diff --output=ROOT/f writes (execution-confirmed as a truncate). Worse, -c and -p are BOTH on the approved global lists, so git -p -c core.pager='rm -rf ROOT/sessions' log turns an allowed read subcommand into arbitrary execution. --exec-path and --config-env are the same class.
C4 the 16 KB gate FAILS OPEN for relative paths. Verified boundary: an oversized command with an ABSOLUTE root path denies, the identical command with a RELATIVE path (rm -rf .git/ledger/sessions padded past 16385) ALLOWS, while the same relative command UNDER the cap denies correctly. Decision G accepted this residual on the stated ground that it "requires the tracked cwd to already be inside the store, which never occurs for an agent operating from the project directory" - that justification is WRONG whenever the ledger root sits inside the project, which is the normal git-project layout.
H5 cd tracking is unconditional and has no shell semantics - a cd that FAILS at runtime (cd /nonexistent; joined by semicolon) and a cd inside a SUBSHELL ((cd /tmp)) both move the guard's tracked cwd while the real shell stays in the root, walking later units out of scope. Both verified null.
H6 the tracked cwd RESETS to projectDir every invocation while the Bash tool's shell persists cwd across calls. cd ROOT in call 1 allows correctly; a bare rm -rf sessions in call 2 then allows because nothing names the root and baseDir is the project. Verified null. No exotic syntax required - ordinary agent work could hit this.
H7 isUnderRoot is LEXICAL only (path.resolve plus startsWith), so a symlinked path component aliases past both the Bash guard AND the Write/Edit branch. Reachable without attacker setup wherever /tmp, /var, home or the checkout sits under a symlink, which is the macOS default for /tmp.
M8 an ANCESTOR of the root is out of scope: rm -rf of the parent destroys the ledger. Narrower than the tracked worktree exclusion in one respect but BROADER in another - under the CLAUDE_PLUGIN_DATA layout, rm -rf dataRoot destroys every project's ledger at once.
L9 roots are re-resolved by a git subprocess on EVERY guarded tool call inside a 10-second hook budget, and gitCommonDir swallows failures into null - so the deferred empty-roots bypass is reachable TRANSIENTLY from a git hiccup, not only from missing configuration.

HELD UNDER DIRECT ATTACK, explicitly confirmed sound: the fd-dup exemption is not a smuggling vector (the exempt token is anchored to ampersand-digit so it can carry no payload, and the one structurally interesting variant lands on an obfuscated head); prefix stripping never exposes an ALLOWED head for a destructive command; redirect handling is solid across every form tried including pre-command, mid-word, heredoc and ampersand-greater; find action-flag coverage is complete for GNU and BSD; sed -i SHAPE coverage is complete; runGuardEntry fails closed on every malformed input shape tried. The structural half of the control - tokenizer compensation, splitControl, head normalization, redirect scoping, fail-closed IO - survived attack. The failures are concentrated in what counts as read-only.

WHY THIS IS NOT RE-LITIGATION AND WHY IT STOPS HERE: every C1/C2/C3 remedy CHANGES A USER-LOCKED TABLE, and each one would also flip a case in the user-approved corpus, which commit 6 was forbidden to touch - ALLOW_CORPUS asserts sort, uniq, xxd, tree, yq and SED_READ as allow. Fixing the proven bypasses therefore REQUIRES editing the locked corpus and the Over-Block Bill. That is a user call, not an implementer call, so no second build round was started. C4's remedy (deny or ask unconditionally on oversize) likewise contradicts Decision G's explicit verdict table. H7 alone is a pure implementation defect fixable without touching any locked artifact.

STATE: seven commits on fix/pre-tool-use-guard, full suite 688/688 green, nothing pushed. MERGE BLOCK STANDS and now has a second independent reason. The code-reviewer pass was still running when this entry was written.