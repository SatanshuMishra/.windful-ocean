HARDENING ROUND COMPLETE AND GREEN; RE-REVIEWS DISPATCHED. Eighteen commits now sit on fix/pre-tool-use-guard above b60ac56. Nothing pushed, nothing merged, RED still reachable at b60ac56.

ROUND SHAPE: c3f8137 RED corpus for the hardening round (715 tests / 688 pass / 27 fail). Then round A on clearance - 4d9193d output-capable heads, 8c2e8c2 sed removal, b41bebd git clearance past the subcommand, 616d5b4 untrusted prefix words - taking it to 719/708/11. Then round B1 on the engine - ae7b975 unquoted-ampersand gating, 05dfa77 iterative opener strip, 937b66b oversize fail-closed both directions, dd89bf2 monotonic cwd, a0125ea O1 sink narrowing - reaching 719/719/0. Then round B2 - 3cb599d shell-cwd plumbing, 4b54a46 symlink canonicalization, 635ad98 git config-injection denial, b21ab86 dead-code and dedup cleanup - finishing at 735 tests, 735 pass, 0 fail, verified INDEPENDENTLY by the orchestrator, not just reported.

ORCHESTRATOR PROBE, re-run against the same attack strings that reproduced the original bypasses: sort -o, uniq two-operand, xxd two-operand, yq -i, tree -o, sed w, sed s-slash-w, git diff --output, git -p -c core.pager, failed-cd desync and subshell-cd desync ALL now DENY. The oversized relative-path command now ASKS where it silently allowed. Controls still behave: literal rm denies, relative rm denies, cat reads allow.

TWO WRITERS THE SECURITY REVIEW MISSED, found by the mandated positive audit of all 38 ALLOW_HEADS members: file -C/--compile writes a compiled magic file, and rg --pre=COMMAND spawns an arbitrary command once per file traversed - arguably worse than anything in the original C1 set. Both are now conditional with corpus cases FILE_COMPILE and RG_PRE. The audit also proved a generic -o rule would have been WRONG: od, strings, cksum and join all have an -o that is not a file (octal shorts, print offset, historic algorithm, field format list), which is why the predicates are per-head. Seven of thirty-eight members were writers, not the five reported.

CORRECTION MADE TO A REVIEW RECOMMENDATION: the code reviewer proposed fixing the ampersand shredding by restricting the split to token-boundary ampersands. That would have REOPENED corpus case E5 (ls ROOT-ampersand-rm), whose ampersand is MID-token. Implemented instead as a quote-aware raw-source scan in the new hooks/lib/shell-source.mjs that answers whether the command contains any UNQUOTED ampersand, gating the split on that. shell-tokens.mjs stayed frozen.

EMPIRICAL FINDING BEYOND THE BRIEF: git --config-env accepts a SPACED form that its own --help does not document, verified against git 2.55.0. Handling only the documented equals form would have left a live bypass; corpus case GIT_CFG_ENV_SPACED covers it. Glued -c-name-equals-value is rejected by git itself and needs no coverage.

INHERITANCE VINDICATED: the code review measured that scope inheritance buys only FP1, and recommended dropping it. That measurement was corpus-limited. The new INHERIT_EVASION case (ls ROOT ampersand rm -rf dollar-D) PASSED on landing, proving inheritance already denies a real evasion the corpus never covered. Kept, and now pinned on both sides with INHERIT_OVERBLOCK carrying the over-block price.

ACCEPTED RESIDUALS FROM THIS ROUND, all fail-closed: a command mixing quoted and unquoted ampersands over-denies the quoted one, because the frozen tokenizer emits no source offsets; monotonic cwd marks units textually BEFORE a cd into the root as in scope; O1's git/find exemption is name-based rather than clearance-based, so cat ROOT/f then an out-of-scope git clean is now null where it was ask; uniqAllows and xxdAllows count a valued option's value as an operand, so xxd -c 16 ROOT/f over-denies.

STILL OPEN AND UNAUTHORIZED: M8 ancestor deletion (rm -rf of the CLAUDE_PLUGIN_DATA root destroys every project's ledger at once), L9 per-call git subprocess inside a 10s hook budget with transient empty-roots reachability, the deferred roots-length-zero bypass, and a flaky gpg-signing fixture noticed during a test run that lacks the commit.gpgsign false the other fixtures set.

MERGE BLOCK STILL STANDS. Fresh security-reviewer and code-reviewer passes over the full f95bdcb-to-HEAD diff were dispatched and had not returned when this entry was written. Criterion 4 cannot be marked until both come back clean of CRITICAL and HIGH.