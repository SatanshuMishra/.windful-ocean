Session resumed this thread on an explicit go and cleared the blocking branch-topology question, then shipped items 1 and 2 of the four-item plan. Items 3 and 4 were NOT started; the session stopped at the context wrap-up threshold, not on a blocker.

SHIPPED: c25516c on fix/pre-tool-use-guard, pushed. 11 files, +181/-1305. Deleted command-allowlist, command-scope, shell-tokens, shell-source, token-access, the unit/scope/overlay machinery, and splitControl. Replaced by a parse-free substring tripwire. Also pushed the 22 previously local-only commits so the audit trail for why the parser died is no longer single-machine.

splitControl was deleted BEYOND the letter of 0029, which never named it. The map showed it was delete-set-independent, and the analyst proposed rewriting its test to hand-built token literals. Rejected: it is a 59-line token-list segmenter whose only consumer was classifyBashCommand, so keeping it would leave a tokenizer with no caller in a codebase that just ruled tokenizing out. Its test was deleted rather than rewritten, since a test for a function with no production surface fails the admission gate.

TOPOLOGY (0032): both options carried in the spine were wrong, and the git evidence settled it rather than preference. Re-forking off b60ac56 would have silently dropped f95bdcb, 3cb599d and 4b54a46, which harden the KEEP set (hook-io fail-closed, hook-io cwd resolution, ledger-roots symlink canonicalization) rather than the parser. Reverting would have restored the pre-inversion allow-by-default guard, a state no decision endorses. Chose forward deletion on the existing branch.

REVIEW: code-reviewer and security-reviewer both returned APPROVE-WITH-FIXES; every finding was applied before the commit. The two highest-value ones were not anticipated by the brief. First, the project-relative spelling: rm -rf .git/ledger had degraded from deny to no-opinion, and that root holds the live active-thread pointer written by src/util/active-thread.mjs:20; from repo root the relative form is the spelling an agent naturally types. Closing it also closed the cd-then-rm-rf-dot case, which turned out to be the same gap. Second, $HOME was uncovered while ~ was covered, and ~ does not expand inside double quotes, so a quoting agent writes exactly the missing form. Also fixed: CLAUDE_PLUGIN_DATA as a trigger (larger blast radius than any covered command - it names the parent of every project store), the oversize branch bypassing the trigger list, a trailing-slash HOME silently disabling the home spellings, a phantom baseDir parameter, and a lost test for the shellCwd-to-baseDir wiring that made line 91 mutable to undefined with the suite still green.

EVIDENCE DISCIPLINE: the first implementer proved RED by extracting HEAD to a scratch tree and running the new tests against the old parser (7 of 18 failed there, including the ref-kill case). The second ran seven mutation checks (M1-M7) and honestly reported M7 as 0 failures, meaning fix 8 has no observable surface once fix 7 lands, and declined to manufacture a test for an unreachable state. The security reviewer ran the deleted 164-case corpus through both implementations side by side rather than reasoning about the delta.

TEST TRAJECTORY: 735 baseline -> 523 after deletion -> 532 after review fixes. Final state independently verified by the orchestrator on an unloaded machine: 532 tests, 532 pass, 0 fail, check-packaging ok.

CORRECTIONS MADE THIS SESSION: the corpus file was reported as 2 tests; it is 2 test() blocks but 164 counted subtests, which is most of the drop. A first-run claim of 0 failures was stated more firmly than the evidence then supported; a reviewer measured 1 failure, which reproduced as environmental (gpg cannot allocate memory under concurrent agent load, passes 6/6 in isolation, file untouched by the diff).

NOT DONE, and why. Item 3 (LocalDriver recoverability) and item 4 (README) were never started. Item 3 was deliberately serialized behind item 1 rather than run in parallel, because both agents would have run the same full suite and a mid-edit tree from one would have produced false failures in the other's report. Item 4 carries a genuine blocker discovered this session: 0030 instructs documenting that sandbox.filesystem.denyWrite does not sandbox hooks or MCP servers, but the official docs do not confirm that and state only that the sandbox covers Bash commands and their child processes. See the decision recorded this session.

COMPLETION CRITERIA WERE NOT TOGGLED, deliberately. Criteria 1 and 2 are marked done but describe a parser that no longer exists. Criterion 3 is parser-framed (it names scanSegments, now deleted) though its fail-closed intent is met with boundary tests at 16383/16384/16385. Criterion 5 asserts a live evasion is DENIED, which the tripwire answers with ask. Criterion 4 is the only one satisfiable as written and is NOT yet satisfied: the suite is green, but the two reviews ran against the PRE-FIX diff and returned two HIGH findings; no review has run over the final committed diff. A fresh review pass over c25516c is required before criterion 4 can be honestly checked.

BACKGROUND WORK: none running. All six dispatched agents completed and were collected. No shell ids to carry forward.

ASKED AND UNANSWERED: whether to fix the stop.mjs defect (exits 2 unconditionally, never reads stop_hook_active, so the Stop hook nags every turn). Surfaced twice; no user reply. Left untouched per the standing do-not-fix-without-explicit-go fence.