GREEN EVIDENCE CAPTURED. All seven commits are on fix/pre-tool-use-guard; the build half of the thread is complete. Commits 4-7 as landed: c304b7c refactor(hooks) extract head normalization and allowlist tables; 025a5a3 fix(hooks) split shell control characters the tokenizer treats as words; 1668944 fix(hooks) invert the PreToolUse Bash guard to deny-by-default; f95bdcb fix(hooks) fail closed on unreadable PreToolUse input. One commit each, no push, no squash, no amend; commits 1-3 untouched so the RED state stays reachable at b60ac56.

VERBATIM FULL-SUITE GREEN, run independently by the orchestrator at f95bdcb (not the implementer's number): tests 688, suites 0, pass 688, fail 0, cancelled 0, skipped 0, todo 0, duration_ms 17841.180875. RED baseline at b60ac56 was tests 657, pass 608, fail 49. The corpus file alone went from 126 tests / 77 pass / 49 fail at 025a5a3 to 126 / 126 / 0 at 1668944, with no case, id, command, expectation or floor assertion edited.

CORPUS COUNTS AS MEASURED: 48 deny, 5 ask, 71 allow, 124 cases, plus 2 floor assertions = 126 tests. The 12 and 41 figures remain FLOORS with no upstream provenance and are asserted only as floors.

Suite total moved 675 to 688 across commits 6 and 7 for two accounted reasons, not coverage loss: commit 6 deleted the whole test that the awk assertion emptied (corpus OB3 owns it), and commit 7 added 12 tests plus one file-level entry, because node --test executes the new child-process fixture at test/unit/hooks/fixtures/guard-entry.mjs as a test file, exactly as the pre-existing fixtures/fake-cli.mjs already did in the 675 baseline.

STRUCTURE AS BUILT: classifyBashCommand(command, roots, baseDir) returns deny, ask or null at pre-tool-use.mjs:187, composed of scanCommand (segment walk, cwd carried across segments), scanSegment (two-pass scope inheritance), unitOf, headClears, advanceCwd, unitDenies, overlaysAsk and sinkElsewhere. Scope helpers live in the new sibling hooks/lib/command-scope.mjs; tables and normalizeHead in hooks/lib/command-allowlist.mjs; pre-tool-use.mjs is 248 lines. mutatesUnderRoot and the corpus legacyVerdict adapter are gone repo-wide (grep clean), so the corpus can no longer silently grade the old boolean guard. The dash-prefix operand filter is deleted, which is what puts dd of= and cp --target-directory= in scope.

VERIFIED HARNESS FACT: permissionDecision accepts allow, deny, ask and defer, and a decision is returned with exit code 0 and hookSpecificOutput JSON on stdout - confirmed against https://code.claude.com/docs/en/hooks.md this session. The docs do NOT define behavior for an unrecognized permissionDecision value, so the tests assert the literal strings deny and ask; a typo there would fail open silently.

COMMIT 7 SCOPE HELD: runEntry is byte-for-byte unchanged and still serves the other five entry scripts; only hooks/pre-tool-use.mjs moved to runGuardEntry. The roots-length-zero bypass was left untouched, still deferred. Unreadable input denies unconditionally (the tool is unknowable in that state); a handler throw denies only for Bash, Write, Edit, MultiEdit and NotebookEdit, and stays silent for other tools. The implementer mutation-tested that predicate: forcing it true fails exactly the unguarded-tool test, forcing it false fails exactly the three deny-on-throw tests, so the branch is load-bearing rather than vacuous. The deny payload is built locally in hook-io.mjs, so no import inverts the module layering.

OPEN ITEM FOR THE USER, commit 7 only: the new tests use a child-process harness rather than an in-process stdout mock, because mocking process.stdout.write captured the node test runner's own reporter frames and corrupted the no-output assertions (a real, reproduced failure). Cost is about 30 ms per test. Keeping the silence assertions exact was judged worth it; moving them in-process would weaken them to a heuristic filter.

MERGE BLOCK STILL STANDS. Nothing was pushed, merged, published or reinstalled. Criterion 4 reviews (code-reviewer plus security-reviewer) were dispatched over the full branch diff and are still outstanding at the time of this entry.