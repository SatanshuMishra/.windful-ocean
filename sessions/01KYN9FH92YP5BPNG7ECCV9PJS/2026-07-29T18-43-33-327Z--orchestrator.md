Ledgerize, 2026-07-29. Formal wrap-up on top of the two narrative entries already appended this session. No subagents or background tasks were left running: all three (two Fable design passes, two Fable implementers) completed and their results were collected and independently re-verified before use. Nothing needs killing and no shell ids carry forward.

USER DIRECTIVE recorded for the next session: step 2 is to be started in a FRESH session, not continued here. Do not treat the remaining steps as mid-flight work; they are unstarted and fully specified.

Completion criteria audited against evidence rather than optimism. The 0094 criterion is now flipped to DONE: the config guard covers git worktrees, asserted by a test that builds an actual git worktree fixture ("a write to a guardrail file inside a linked worktree is held for human confirmation"), and it merged to main as #13 at d77346d. Every other criterion stays UNCHECKED, including the final one requiring origin/main to be verified fully green re-derived FROM THE REMOTE after each merge - I did not do that re-derivation, because both 1559/1559 runs were on branches pre-merge. Leaving it unchecked is the honest state; the next session should re-derive before claiming it.

Straggler decision sweep: 0102 was captured at decision time and is the only genuine decision locked this session. Two items were deliberately NOT written as decision records because they are open questions, not rulings: (a) whether a ratified decision record by itself authorizes a destructive git operation without separate user confirmation, raised by the git rm in step 1 and due to recur at the chmod in step 3 and the migration in step 4; (b) the ~/.zshrc coupling, which after the pin becomes the last live-config symlink tracking an unpinned branch. Both belong to the user to settle.