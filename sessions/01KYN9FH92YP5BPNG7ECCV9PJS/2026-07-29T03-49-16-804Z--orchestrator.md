SESSION 2026-07-28. SHIPPED: MSP A is CLOSED and independently verified. Two atomic commits on fix/mitosis-boundary-residuals in worktree .claude/worktrees/boundary-residuals - 2e6fd57 (worktree-aware base derivation in protect-claude-config.sh) and 58cc2d3 (lib/ and workflows/ added to the block-destructive-bash.sh guardrail-write clause). The orchestrator did NOT trust the agent's self-report: re-ran node --test .claude/hooks/tests/*.test.mjs itself (122/122 pass) and re-ran the live probes itself - worktree hook path returns ask, worktree lib path returns ask, .claude/skills/mitosis/SKILL.md correctly stays silent, and a shell write to .claude/lib returns ask. All four were unprotected before. Worktree clean, zero further changes; primary checkout byte-identical to session start at 450804e. MSP A is NOT pushed and has NO PR - that is deliberate, pending review. The agent chose git worktree list --porcelain -z enumeration over per-candidate toplevel resolution because git -C <dirname> fails when a Write creates a file in a not-yet-existing directory, which would push a routine case into the fail-closed branch; fail-closed verified live with git off PATH. NOT DONE: the migration was NOT applied, MSP B and MSP C were not started. The migration script was authored and dry-run clean (38 planned repoints, exit 0, machine untouched, ~/.claude-install absent); its author found and fixed a genuine macOS trap - BSD mv -f FOLLOWS a symlink that resolves to a directory, so lib and rules/common would have survived untouched while stray temp symlinks landed inside them; the fix is mv -fh in both the forward repoint and the generated rollback. CRITICAL CAVEAT: that script lives at an ephemeral per-session scratchpad path and will almost certainly NOT survive into the next session. Re-authoring is cheap and the design is fully captured here and in decisions 0100 and the safeguards record - preserve the mv -fh fix, the 38-link inventory, the rollback location ~/.claude-install.rollback/<UTC>.sh written and verified non-empty BEFORE any mutation, the auto-rollback on mid-loop failure, and the abort when ~/.claude-install exists but is not a registered worktree. TOOL QUIRK worth knowing: record_decision rejected two calls with "must have required property X" where X was whichever parameter immediately followed the very long context value; placing context LAST made it succeed. NEW FINDINGS: (a) .drift-state.json prerequisite - superpowers-drift-check.sh:4 writes $HOME/.claude/lib/superpowers-parallel/.drift-state.json, a TRACKED file, so post-migration it dirties the install worktree permanently and destroys the cleanliness signal; must be relocated to ~/.claude/state/ and untracked BEFORE the pin. Note docs/superpowers/specs/2026-07-27-centralized-pr-creation.md:679 marks that file "do not touch, owned by no unit", so it needs a deliberate owner. (b) MSP A residual: the fail-closed branch matches only the four prefixes, not the four exact filenames, so <worktree>/.claude/settings.json escapes that branch; the normal path still covers it and widening is free since the decision is ask not deny. (c) ~/.zshrc still symlinks into the PRIMARY checkout, so shell config keeps tracking whatever branch is out - same coupling class, outside R4 scope, undecided. (d) UNVERIFIED, agent-reported only: the pre-commit hook ran the full suite at 1558/1558 green inside a worktree, which sits oddly against the mitosis-nonstop-shipping-architecture thread's recorded "worktree fails 2 more than main at the same commit"; counts have moved so it may simply be stale. The orchestrator did not verify this and it belongs to that thread.