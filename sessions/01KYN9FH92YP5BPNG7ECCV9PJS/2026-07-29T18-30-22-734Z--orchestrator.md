Session 2026-07-29. MSP A SHIPPED AND MERGED: PR #13 (fix/mitosis-boundary-residuals -> main), now origin/main d77346d. Before pushing it I folded in its one known residual - the fail-closed branch matched only the four directory prefixes, so <worktree>/.claude/settings.json escaped it when the worktree set was underivable; fixed by testing membership against prefixes + exact. TDD red proven first (assertion "null !== 'ask'"), then green. Re-verified the diff and both suites MYSELF rather than trusting the implementer report, per this thread's standing rule: 15/15 protect-claude-config, 84/84 block-destructive-bash, and 1559/1559 full suite via the pre-commit hook.

DESIGN PHASE COMPLETE for all four 0101 steps. Two Fable design passes produced docs/superpowers/specs/2026-07-29-install-pin-safeguards.md, carrying both scripts in full, the freshness-check outcome matrix (rows A-J with exit codes and streams), the mkdir-based locking protocol with race-free stale claim by rename, and the verified man mv quote establishing that -h, not -f, is the guard against following a symlink-to-directory.

Three findings changed the plan. (1) Recorded as decision 0102: 0101's chmod-plus-brief-restore leaves the install writable if a session dies mid-window, and a trap cannot survive SIGKILL, so the real guarantee is an unconditional next-session audit-and-reseal running before anything else. (2) Step 3's deny silently upgrades ask to deny for the ~/.claude symlink paths post-pin, because every entry realpaths into the install root - the everyday edit flow becomes checkout, PR, merge, auto-refresh, and the user must be told rather than discover it. (3) Step 4 needs a new artifact, ~/.claude/state/install-pin-applied, without which an absent install root cannot be told apart from a destroyed one.

STEP 1 STARTED. Live drift state preserved to ~/.claude/state/superpowers-drift-state.json (version 6.2.0, copied not moved, so the still-old live hook does not churn the working tree before cutover). Worktree .claude/worktrees/drift-state created on branch fix/drift-state-relocation off origin/main d77346d. An implementer is mid-flight there: copy the untracked spec into the branch, replace the drift hook with the fail-loud rewrite, add the gitignore line, and git rm the tracked .drift-state.json. The removal is authorized by 0101 with content preserved in two places (the copy above and git history). NOT yet committed, pushed, or PR'd at the time of this entry.