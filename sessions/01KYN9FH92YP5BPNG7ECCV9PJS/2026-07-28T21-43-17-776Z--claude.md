REPRODUCTION COMPLETE for 0092; 0093 and 0094 mapped. No code changed. Pristine worktree created at .claude/worktrees/boundary-residuals on branch fix/mitosis-boundary-residuals, cut from origin/main a229c9c (primary checkout is dirty on feat/centralized-pr-creation and holds another thread's work on mitosis.js - untouched).

0092 VERDICT: PARTIALLY CONFIRMED; the ledger's MECHANISM WAS WRONG.
- REFUTED: "the engine execs the CLI". mitosis.js has ZERO child_process calls. "Step 7" is item 7 of a natural-language prompt string (mitosis.js:3720) handed to a reconcile subagent (agentType implementer), which runs it in its own shell.
- CONFIRMED by execution: exit 31, stdout 0 bytes (verified via wc -c and xxd).
- REFUTED: "prints nothing". 242 bytes on STDERR naming all four missing vars (merge-boundary-preflight.mjs:372). Discarded by the caller, which only parses stdout.
- ROOT CAUSE = UNSATISFIABLE CONTRACT: SKILL.md:43 mandates the four values be set INLINE and forbids ambient export; mitosis.js:3720 needs the same CLI to read them from the environment and attaches none. Inline vars do not propagate into a subagent's separately-spawned shell. Following SKILL.md correctly GUARANTEES layer-2 failure. There is no world where both layers pass and the docs are obeyed - which is why a week-long severance was invisible: it never worked.
- AMPLIFIER: exits 0 and 30 emit the stdout verdict line; exits 31 and 127 emit none. So config-missing, gh-missing, file-missing and agent-failure all collapse into one generic message at mitosis.js:130-131. Symptom, never cause.
- NEW FINDING (not in the ledger): the gate file DOES NOT EXIST on this machine. ~/.claude/lib symlinks to the primary checkout, which sits on feat/centralized-pr-creation, 8 commits behind origin/main and predating 457d6fa which created merge-boundary-preflight.mjs. Both call sites currently fail node exit 1 MODULE_NOT_FOUND, not 31. Structural hazard: checking out a branch changes the live security tooling. merge-boundary-preflight.test.mjs:667 compares two path STRINGS and never calls existsSync, so no test catches it.

0093 VERDICT: CONFIRMED. invokedAs is raw process.argv[1] (merge-boundary-preflight.mjs:369), string-compared at mitosis.js:150 to a hardcoded constant; neither side realpath-resolved. Detects a DIFFERENT COPY at a different path; cannot detect a TAMPERED file at the same path, since tampered code reports the same argv[1] and can print any verdict. Both layers resolve through the symlink to ONE physical file in the managed repo, so "two independent layers" is false. STRUCTURAL TRUTH: no self-hosted gate can attest to its own integrity - any self-check is deletable by whoever edits the file. The real mitigation is 0094, not more code in the gate.

0094 VERDICT: CONFIRMED. protect-claude-config.sh:20-53 derives exactly two bases (~/.claude and its realpath target) and prefix-matches; no git rev-parse, no worktree awareness, never shells to git. A worktree path matches neither base, so settings.json, CLAUDE.md, hooks/, rules/, lib/, workflows/ are all Edit/Write-able with NO prompt from inside any worktree. mitosis fans every wave task into one (run-engine.mjs:96-97, 1077-1078). COMPOUNDING: the 0093 gate file is freely rewritable from any fan-out worktree. ANCILLARY: block-destructive-bash.sh:91-93 omits lib/ and workflows/ entirely, so those two trees have exactly ONE guard and it is the one with the hole. protect-claude-config.test.mjs uses real fixtures and real symlinks but builds NO worktree - the gap is untested.

RECOMMENDED ORDER (presented to user, not yet approved): (1) 0094 worktree guard via git rev-parse --git-common-dir - unblocked, no trade-off, and every later fix lands inside this hole until closed; (2) 0093 realpath both sides AND downgrade the documented claim from "detects poisoning" to "detects a different copy"; (3) emit the verdict line on EVERY exit path with a distinct reason code, plus existsSync on the gate; (4) compose the four values inline into the step-7 command string (none is a secret; the PAT is not among them); (5) posture; (6) receipt that execs the real CLI and is proven RED by actually severing the call site.

PENDING USER DECISION - posture. Recommended: degrade on exit 31 (unconfigured) with a loud recorded warning, halt hard on exit 30 (configured and UNSAFE) - a distinction the code cannot currently make downstream. Rationale: under 0095 layer 1 always exits 31, so strict fail-closed makes the engine permanently dead code - an outage wearing a security justification. CAVEAT, EXPLICITLY UNVERIFIED: this rests on the inference that the boundary protects an autonomous-merge capability mitosis does not actually have (gh pr merge and both merge GraphQL mutations are denied; 0095 cancelled the machine user). If the receipts CI enforcer can complete a merge without a human, the recommendation FLIPS. Verify that in code before implementing step 5 - do not assume it.

A first AskUserQuestion on posture + layer-2 disposition was rejected; the user asked for a step-back explanation first, which was delivered. Awaiting their ruling.