Session closed the posture question that had blocked this thread across two sessions, and shipped no code.

WHAT LANDED: decision 0099 (merge-boundary posture) and a full spine rewrite. Nothing else. The worktree .claude/worktrees/boundary-residuals is VERIFIED clean at a229c9c with zero commits ahead of origin/main - checked directly with git status and rev-parse, not taken from a subagent report.

THE AUDIT THAT UNBLOCKED IT: an independent read-only audit dispatched by the previous session returned early this session. It answered the prerequisite the spine demanded (can the receipts enforcer merge without a human): NO coded path can, in the engine or in CI - merge-policy.mjs:1-13 returns only human-gated, mitosis.js:3098/3103/3116 forbid self-merge, merge-watch.mjs:53-61 is a read-only poller, saga.mjs:54/63 never pushes the base, receipts.yml has no merge step, and the pinned enforcer requests pull-requests:write only for its report comment. But live probes found the hard boundary absent: gh api rules/branches/main returns [], the credential is owner SatanshuMishra with permissions.admin true, and the gh merge-deny shim is NOT on PATH (command -v gh gives /opt/homebrew/bin/gh), so its classifier guards only mitosis-git.mjs's own execGh calls. Exposure is capability, not code.

THREE CORRECTIONS TO THE INHERITED RISK LIST, all confirmed: (1) protect-claude-config.sh:36 DOES list lib and workflows - the hole is the two-base prefix derivation at :20-33 that no worktree path matches. (2) run-engine.mjs:1077-1078 does not exist at origin/main (585 lines); the real cite is :96-98. (3) "inert for over a week" was wrong and the truth is worse - 457d6fa merged 2026-07-27, but the installed path has NEVER held the file, so the gate has been inert its entire existence. Also newly found: superpowers-drift-check.sh:7 opens with [ -f "$RESOLVER" ] || exit 0, a third instance of the silence-as-success idiom behind this whole incident.

THE FINDING THAT RESHAPED THE RULING: layer 1 carries FOUR gating invariants, not one. identity must equal a configured machine user (merge-boundary-preflight.mjs:153), repository must report admin exactly false (:171), base branch must require >=1 approving review with require_last_push_approval exactly true (:236-241), and the rule must bind an active repo-owned ruleset (:262). The first three are unsatisfiable BY CONSTRUCTION in a single-owner personal repo: the owner is structurally admin on their own repo, and GitHub forbids self-approval, so one human account can never satisfy a one-approval rule on its own PRs. The gate was built for a two-identity world; descoping the machine user removed the ground under three of four checks at once. Creating the ruleset fixes only the fourth. The user's first ruling (ruleset + keep layer 1) was therefore unimplementable as stated, which is why a second question was asked and answered.

WHAT FAILED / WAS STOPPED: MSP A (0094 worktree guard) was dispatched to an implementer and then KILLED during wind-down at the continuity guard's insistence, while still in its exploration phase. It made no edits - the clean worktree is independent proof. Its one useful finding, worth not rediscovering: the hook tests live at .claude/hooks/tests/, which already contains block-destructive-bash.test.mjs and entrypoint-guard-lint.test.mjs. MSP B and MSP C were scoped but never dispatched.

NOT DONE, DELIBERATELY: the ruleset itself. Payload written to scratchpad main-ruleset.json (pull_request, required_approving_review_count 0, bypass_actors [], enforcement active, ~DEFAULT_BRANCH, allowed_merge_methods squash). The agent must never create it - on the gate's own logic at merge-boundary-preflight.mjs:270, write access to the boundary would let the engine edit the boundary it rests on. As of this hand-off the user had NOT yet run it, so assume no ruleset exists until re-probed.

UNRULED AND DOMINANT: R4 deployment pinning. Until ~/.claude's code entries point at a tree pinned to origin/main, every fix in MSP B and C lands in the repo and changes nothing observable. Raise this before spending effort on B or C.