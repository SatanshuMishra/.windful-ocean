HAND-OFF. Session ends with the posture ruling still open. No code was written this session and nothing was committed or pushed; the only artifacts are the ledger writes and one pristine worktree.

SHIPPED: reproduction of 0092 (mechanism REFUTED, symptom CONFIRMED), mapping of 0093 and 0094 (both CONFIRMED), decision 0098 recording the corrected 0092 mechanism, and a spine correction. The spine's active_goal had asserted "step 7 execs the merge-boundary preflight" - reproduction refuted that, and the false claim was rewritten rather than left standing. The predecessor lost a week to a spine that asserted a false mechanism; that was not repeated.

DID NOT SHIP: every one of the six completion criteria is still unchecked, correctly. No fix was implemented because criterion 2 (posture) gates 0092's implementation and the user has not ruled.

FAILED / BLOCKED: an AskUserQuestion on posture and layer-2 disposition was REJECTED by the user, who asked for a step-back explanation first. That explanation was delivered in chat covering all five faults in plain language with a recommended fix order. The user did not then rule - two consecutive turns arrived as Stop-hook feedback with no user message, which is what triggered this hand-off. The posture question is UNANSWERED, not abandoned.

NO BACKGROUND WORK IS RUNNING. Two subagents completed and returned; nothing was left in flight and no shell needs killing.

WORKTREE LEFT IN PLACE DELIBERATELY: .claude/worktrees/boundary-residuals on branch fix/mitosis-boundary-residuals, cut clean from origin/main a229c9c, zero commits, clean status. It exists because the primary checkout is dirty on feat/centralized-pr-creation with another thread's uncommitted work on the very file this thread must change (mitosis.js). Reuse it; do not work in the primary checkout. If this thread is abandoned instead, remove it with: git worktree remove .claude/worktrees/boundary-residuals && git branch -D fix/mitosis-boundary-residuals.

RECOMMENDED FIX ORDER as presented to the user (steps 1-2 need no ruling; 5 does): (1) 0094 worktree guard, resolving ownership via git rev-parse --git-common-dir rather than enumerating worktrees, plus adding lib/ and workflows/ to the Bash-side regex so those trees stop having exactly one guard; (2) 0093 realpath both sides AND downgrade the documented claim from "detects a poisoned gate" to "detects a different copy", because no self-hosted gate can attest to its own integrity - any self-check is deletable by whoever edits the file, so the real tamper mitigation is 0094, not more code inside the gate; (3) emit the verdict line on every exit path with a distinct reason code, plus assert the gate file exists; (4) compose the four values inline into the step-7 command string, which satisfies SKILL.md's inline mandate and makes corroboration possible for the first time; (5) posture; (6) the receipt, proven RED by actually severing the call site rather than asserting it would be red.

THE ONE THING TO VERIFY BEFORE STEP 5, flagged as an explicit unverified inference and NOT to be treated as established: the recommendation to degrade on exit 31 rests on the reading that the boundary guards an autonomous-merge capability mitosis does not actually possess, since gh pr merge and both merge GraphQL mutations are denied at the gate and 0095 cancelled the scoped machine user. If the receipts CI enforcer CAN complete a merge without a human, that reading is wrong and the recommendation flips to fail-closed. Verify it in code first; do not assume it, and do not let the recommendation harden into a premise.

ALSO WORTH DISTINGUISHING in whatever posture lands: exit 31 (nobody configured it) and exit 30 (someone configured it and it is UNSAFE) are different signals that the code currently cannot tell apart downstream. 30 should halt hard regardless of the posture chosen for 31.