RULESET NOW EXISTS - the brief's instruction to "assume it does not exist" is superseded. Probed 2026-07-28: ruleset id 19939922 "Require PR for Merge" on SatanshuMishra/.windful-ocean, created 2026-07-28T19:57:30-06:00, i.e. after the previous session closed. It conforms to 0099 on every point: enforcement=active; source_type=Repository (repo-owned, not org); conditions.ref_name.include=["~DEFAULT_BRANCH"] so it binds main; rules[0].type=pull_request with required_approving_review_count=0; bypass_actors=[] (empty, load-bearing per 0099). Additionally current_user_can_bypass="never", so the owner cannot bypass the PR requirement even though repos API reports permissions.admin=true - this is stronger than 0099 assumed and means direct push to main is blocked server-side for the owner too. NOT verified by attempting a push (that would be an outward-facing mutation); this is config-verified only. allowed_merge_methods=[merge,squash,rebase], so squash-on-merge per the branching rule remains available but is not enforced. CONSEQUENCE FOR MSP C: of layer 1's four gating invariants, the ruleset-binding one (merge-boundary-preflight.mjs:262) is now SATISFIED and verifiable; the other three remain unsatisfiable in this single-owner repo - identity=machine user (:153), admin===false (:171, contradicted by permissions.admin=true), and review>=1 with require_last_push_approval (:236-241, actively contradicted by the ruleset's deliberate 0 and require_last_push_approval=false). MSP C's reshape therefore has a live, passing reference case to build against rather than a hypothetical one. Branch protection API reports "Branch not protected" for main, which is expected: the protection lives in the ruleset, not in the legacy branch-protection surface, so any check that reads /branches/main/protection will see nothing and must read /rulesets instead.