Shipped criterion c4 in four commits on fix/gate-hardening-followups. Suite verified by the orchestrator, not just reported: 160 pass / 0 fail on the committed tree.

725db81 - G4, four named holes: git checkout and git restore, perl in-place edits, and a copy whose destination is the bare .claude directory (so a dropped file becomes settings.json without ever naming it). The bare-directory case was fixed on the PATH side, which gave every existing verb the coverage at once. chflags nouchg got its own branch and its own reason string, because clearing immutable-flag protection is not a shell write and the human answering the prompt needs to be told which one happened. No chflags flag was applied to any file; that waits for c7.

3f16c18 - G1. The probe found the goal OPEN, which is the session's main result. ghtok hard-coded gh immediately followed by its subcommand, so any flag between them defeated every clause built on it: gh -R owner/repo pr merge 12, gh --repo=owner/repo pr merge --admin, gh -X PUT api repos/o/r/pulls/12/merge and gh --hostname github.com api ... all returned no opinion. Probing gh 2.97.0 showed cobra resolves a pre-subcommand flag against the subcommand's own flag set and that only value-taking flags are reachable there. Also closed: the leading-slash /graphql endpoint spelling, and enqueuePullRequest, which reaches a merged PR through the merge queue. The same shared local left gh -R owner/repo pr create --fill and gh -R owner/repo pr edit 5 --title x allowed, so the fix necessarily advanced G2 and G3 too. The widening is monotone - zero flag repetitions reproduces the old pattern character-for-character - so nothing previously denied was lost.

0640017 - a fifth G4 hole the first round found and did not close: a guardrail subdirectory named without a trailing slash. mv .claude/hooks /tmp/stash and rm -r .claude/hooks both passed, because the path list required hooks/ with the slash.

bd9cbef - the threat model, amended to match. G1's control cell claimed a primary control outside the agent's reach; reconnaissance under 0255 had already falsified that, and the cell now names the hook deny as the sole control and records why settings.json's Bash(gh pr merge:*) prefix rule is not a second layer - prefix matching cannot express a flag between gh and its subcommand, so both G1 layers failed on one input. Section 6 marks the fail-open row closed by c3 and the guardrail half of row 2 closed by c4, refines the false-positive row with what was measured rather than assumed, and logs six evasions that fall to stated non-goals so no later round reopens them as fix targets.

Two decisions recorded at decision time: 0257 (widen ghtok generically rather than enumerating gh's flags - both candidates scored identically across the probe corpus, so the tie broke on drift) and 0258 (extend G1's enumeration to name enqueuePullRequest). 0258 amends a document the owner ratified on 2026-08-05 and was flagged in chat as reversible.

What did not go clean. The gate denied its own commit message: git commit -F - with a heredoc containing a literal merge phrasing tripped the merge clause, which is accepted risk 4 (unanchored has()) firing on a command that executes nothing. Worked around with git commit -F <file> from the scratchpad. That is incidental live proof the symlinked hook is the running guard, and it gives c5's false-positive work a measured cost rather than a hypothetical one. Two knowingly-accepted new false positives came with the bare-directory path: ls .claude > /tmp/list.txt and echo "see .claude for config" > /tmp/note.txt now ask, logged as section 6 row 10. One pre-existing unrelated test failure remains in protect-claude-config.test.mjs (it asserts ~/.claude/settings.json resolves out of the home tree; the threat model itself records it as a real regular file) - not touched by this work, not caused by it.

Left open deliberately: gh --paginate api .../pulls/N/merge, on the premise that gh rejects a boolean flag in that position, which was verified against gh 2.97.0 with real output but is version-dependent. Also gh --repo "owner repo" pr merge, unreachable because no owner/repo contains a space.