Shipped c3, the fail-closed harness, as commit 97b3e8b on fix/gate-hardening-followups. Not pushed; c7 still owns the push because CI fires on bare push and the invariant-coverage debt is unresolved. block-destructive-bash.sh is 214 lines, inside the 150-250 target. 103 tests pass in the gate suite; npm test is 1794/1795, the one failure being the pre-existing protect-claude-config.test.mjs:194 case that reproduces on pristine main and was left alone.

All three fail-open paths from threat model section 6 row 3 are closed, plus a fourth that was not on anyone's list. The case prefilter was deleted outright (0254). The python3 extractor's three collapsed sub-cases were split: an unrunnable interpreter and an unparseable payload each escalate to ask, while a valid payload with an absent or empty command is the explicit no-opinion token. The intended no-opinion exit became that token, validated by name at a single emit_verdict installed as an EXIT trap with INT TERM HUP routed into it. The fourth path: with grep unavailable every matcher returned "no match" and a destructive command produced no output at all, a silent allow on a broken classifier; match_status now separates grep's exit 1 from exit >1 and escalates an unevaluable matcher to ask. That is a Definition-of-done item 2 defect rather than a stated non-goal, so it was fixed rather than logged.

Verified independently rather than taken on the subagent's report: the only two exit statements sit inside the emitter, matcher_fault is genuinely consumed at block-destructive-bash.sh:205 rather than set and forgotten, and no allow decision is emitted on any path. That last check was the one that mattered, because a hook allow bypasses the normal permission prompt, so "make no-opinion explicit" done naively would have auto-approved everything the gate had no objection to.

The prefilter deletion rested on a finding worth remembering: the prefilter had never once short-circuited in production, because transcript_path always lives under ~/.claude and matched its .claude arm, while the test harness sent a bare tool_input object with no transcript_path and so did short-circuit. The suite was exercising a code path production never took. Demonstrated live: a spaced fork bomb was a silent allow under a minimal payload and an ask under a realistic one, same gate. The harness now sends realistic payloads and a corpus test asserts the verdict is identical either way.

c4 was started but deliberately stopped before any code, at roughly 80 percent context, to avoid abandoning a large dispatch mid-verification. What it produced is decisions, not diffs. Reconnaissance falsified a claim in the ratified threat model: G1 named GitHub branch protection as its primary control, "server-side, outside the agent's reach", but gh auth status shows the token carries repo scope, which is admin on an owned repo, so the agent can delete the protection rule as easily as it can merge. The repo is public (SatanshuMishra/.windful-ocean), so branch protection was available on the free plan and the solo-repo constraint would have required zero required approvals. The owner chose to skip it; the hook deny is now G1's sole control (0255), and no protection-tampering deny was added, because with no rule deployed it closes nothing and .claude/hooks/CLAUDE.md forbids hardening beyond what G1-G5 requires. The condition for any future revisit is recorded in 0255.

Second c4 conclusion, recorded as 0256: keep the verb denylist for G4 rather than inverting it to a reader allowlist, because .claude/hooks/ matches the guardrail path list and the inversion would prompt on every node --test run against the gate's own tests.
