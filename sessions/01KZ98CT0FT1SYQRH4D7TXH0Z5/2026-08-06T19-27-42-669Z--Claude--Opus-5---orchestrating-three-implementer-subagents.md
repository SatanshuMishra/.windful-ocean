c7 shipped; PR #42 merged as squash commit 0451220 on main, closing the thread's last criterion.

0251's deferred choice was settled by READING rather than by picking one of its two named options. The ratified oracle at docs/superpowers/specs/2026-07-30-two-track-invariant-plan.md:121 already scopes set-equality to the entry a change "adds or updates". The checker applied it to every file in docs/invariants/coverage/ instead (invariant-coverage-check.mjs:131 reached from :203-207), so the checker was stricter than its own spec, and THAT over-strictness — not the registry append — was the cause of the 22-artifact fallout. Recorded as 0264. Backfill rejected on the same honesty ground as the pr-create rule: 110 verdicts authored today about changes made before G1-G5 existed, in records that are receipts of past sessions. Track-aware rejected because the same spec line forecloses self-selected applicability as the enumerated allowlist M2 forbids.

Four commits. 74da24f scoped completeness to changed entries while keeping shape, duplicate, unknown-id, stray-file and registry validation over every entry in every mode. 9c4755f appended G1-G5, lifted verbatim from threat-model section 4. 4e425ff keyed the gate suite to a registry-driven goal census that halts both directions — a registry goal with no corpus, and a corpus naming a goal the registry does not carry. 620916b recorded seventeen measured verdicts in docs/invariants/coverage/fix-gate-hardening-followups.json.

Measured, not asserted: npm test 1937 tests / 1936 pass / 1 fail; gate suite 242/242; both M3 red-green pairs re-run independently in throwaway worktrees (exit 1 on parent 4fddf3b, exit 0 on HEAD); the goal-map mutation proof (emptying G4 turns two named assertions red); the census falsified in both directions with a fabricated G6 in a scratch registry and a fabricated G9 corpus; the corpus multiset checked HEAD vs working copy at 219 vs 219 with zero differences, proving the test relabel changed no case.

Three findings that deliberately did NOT become gate changes. (1) The single npm test failure is pre-existing and unrelated: protect-claude-config.test.mjs:194 asserts ~/.claude/settings.json is a symlink into this repo; it is a separate real file, and the test fails identically at parent 4fddf3b. Filed as its own task rather than widened into this branch. (2) .claude/hooks/CLAUDE.md has no deployed counterpart, because ~/.claude/hooks symlinks each child individually — recorded in the M6 row as a found difference, not a threat. (3) M4 is not a clean pass and the artifact says so: the merge-base characterization suite run against the HEAD gate gives 83/85, two deliberate deltas — the issues/ versus pulls/ split under 0261, and a fixture whose gh pr create phrase sat inside a quoted --why value, which c5's command-position rule correctly reads as no-opinion.

Stated as not verified in both the PR and the artifact: the remote branch-protection state, and the GitHub MCP pull-request tools (the MCP half rests on deny lists present in both settings.json files).

Unrelated working-tree drift persisted throughout and was never staged: .claude/settings.json, .zshrc, two .claude/sounds/*.mp3, and three untracked paths (two .bak-pre-promptsfix files and .claude/skills/context7-mcp/).