Closed c10 and c8. All eight MSPs of the frozen testing-architecture SPEC are now implemented and shipped.

c10. Confirmed pull request 279 merged by reading the trunk tree rather than the label: the dispatch recorder is present in the run store and the planning-phase recorder is wired in the command line entry point on the trunk. Fast-forwarded the primary checkout six commits; none of the six touched any of the eight dirty documentation files, and all eight survived. Confirmed the recorder live in the working tree.

c8. Built the harvest machinery BEFORE spending, which turned out to be the decisive sequencing call. Three preparation agents ran in parallel and found that the sweep driver hard-refuses any recorded cassette on sight, that the source-run field appears nowhere in the dispatch record and must come from the sibling plan document, and that the recorder emits eight dispatch kinds while the replayer can route only four, so a cassette keyed with an unroutable kind loads, does nothing, and still passes the exhaustion census. Any of the three discovered after the run would have wasted it.

Built and proved for free: a harvester that folds the recorder kinds onto the routable four in file line order, excludes the two payload-bound scripts, and takes the source run from the plan document; and driver changes giving a cassette directory override, accepting a recorded cassette with a non-null source run while still refusing every dishonest combination, and failing a scenario whose scripted kind is never consumed. The consumption census was proved red before green: an unroutable scripted kind passed at exit 0 on the unmodified driver, then failed naming that kind.

Sent the harvester back for a second round after its first receipt, because its smoke fixture exercised none of the fold branches. A synthetic fixture with deliberately non-monotonic iteration values proved the fold and proved ordering comes from file lines, not the iteration field. The real run then produced two revision dispatches, so that fold was load-bearing rather than theoretical.

The billed run was blocked by the permission classifier and the user ran it. It completed in 1134 seconds and exited 1 at the crash-resume leg, the accepted outcome. Eight dispatches recorded. Engine provenance confirmed the primary checkout, so the stale-engine trap that made the previous billed run record nothing did not recur.

Findings. The hand-authored findings payload shape was confirmed exactly correct. The real gap is that no authored scenario describes what the live reviewer did: it rejected twice and approved on the last allowed revision, while the authored set covers approval first time, one rejection, and rejection to exhaustion. The recording was probed against all three rather than assumed into one; it fails two on their own dispatch-count checks and passes the third while contradicting its name. Third finding: the dispatch kind is computed from schema identity and call ordering rather than declared, so a retry is recorded as a revision, a second judgment dispatch is always labelled the security one, and four kinds carrying no unit are never recorded at all.

Pull request 280 is open and awaits a human merge. Its diff is four test and evidence files with no source, so the enforcer will short-circuit and return a pass with the mutation gate never running. That is the enforcer declining to referee, not a cleared gate.