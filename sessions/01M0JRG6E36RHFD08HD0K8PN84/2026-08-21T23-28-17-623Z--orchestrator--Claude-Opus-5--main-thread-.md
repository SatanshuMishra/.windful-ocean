Resumed this child thread from the parent on explicit instruction, fast-forwarded the primary checkout from 05b29b5e to c2992f78, and confirmed all four 2026-08-21 macOS temp evidence directories were still alive before anything else.

SHIPPED, all merged to main by the user during the session: M1 as pull request 275 (run-store gains recordDispatch; the four evidence directories committed first), M2 as 273 (cassette.mjs with loadCassette, scriptFor, CASSETTE_SCHEMA and five rejection tests), M5 as 274 (the prompt side-effect harness), M6 as 276 (the predictiveness census). M3 shipped with no pull request because its whole fileScope is the harness directory, which is not a git repository; that is correct, not an omission. M4 shipped as 277.

FAILED, and why. Three repair cycles ran after the six MSPs, and the user correctly identified the pattern as the thing to eliminate rather than to keep paying for. Each cycle had one cause, and the causes compound:
1. M2's cassette.mjs uses a provenance field the frozen SPEC mandates verbatim, and the repository's attribution census bans that token; M2 also created tests/cassettes/, which halts the dispatch-binding census on any undeclared subdirectory. Both fired in CI, not locally.
2. The reason they did not fire locally is the root cause of the whole pattern: attribution-census.test.mjs carries /worktrees/ in its own EXCLUDED_SEGMENTS, and every agent worktree here lives under .claude/worktrees/, so the census excludes every candidate file and passes over an empty set. The same blind spot was observed in agent-schema-lint, determinism-lint, mitosis-gate, mitosis-gate-red-cases, name-integrity-census and retirement-census. Every agent verified in the one place its guards are blind, reported an honest green, and CI then reported the truth. The review cycles did not introduce these defects; they were the first instrument able to see them.
3. Repairing that surfaced a third, independent failure: pull request 277 had been merged into feat/mitosis-cassette-loader, which was never deleted, so GitHub never retargeted it and its content never reached the trunk. Verified by content with git cat-file, never by the MERGED label. Merging that stale base branch would have been destructive, because it was cut before 274, 275 and 276 landed and its diff against main deletes the preserved run evidence, prompt-side-effects.test.mjs and run-store-dispatch-record.test.mjs and restores a deleted test. Recovered instead by cherry-picking forward onto a fresh branch from main, with the diffstat asserted to touch exactly eight paths before pushing, as pull request 278. That pull request also carries the two census fixes, so it greens the trunk and lands M4's stranded content in one change.

MEASURED STATE OF THE TRUNK. main at f3304b86 fails 17 tests when the suite is run from a foreign-path clone. Two of those are the census guards 278 fixes. The remaining 15 are pre-existing and unrelated to this thread's diffs, spread across agent-schema-lint (4), determinism-lint (3), mitosis-gate (3), mitosis-gate-red-cases (2), name-integrity-census (1), retirement-census (1) and prompt-side-effects (1). The prompt-side-effects failure is M5's test correctly reporting a live defect: pull request 271 fixed the boundary gate's comparison worktree, not the implement-task worktree the child builds from prompt text, and prompt-execute.mjs is unchanged across it. The test job is not a required status check, which is how 15 failures accumulated while every contributing pull request reported green.

M4 CORRECTED THE SPEC'S OWN WORKED EXAMPLE. Run 4's lost unit is not a resume defect. Run 4's stdout shows resume carrying the unit correctly; the drop is between Integrate and Ship, caused by a leaked boundary worktree whose collection was refused. The SPEC's verbatim unit test asserts a behaviour that is already correct and could never go red, so it was re-aimed at integrateBuilt and integrateSummary under the mandated filename. Writing it as specified would have manufactured exactly the vacuous green this SPEC exists to cure.

NOT DONE: M7 and M8. M8 is the only billed MSP and nothing billed ran at any point this session.

OPEN WORKTREES left in place, all clean, none abandoned mid-write: m1-dispatch-record, m2-cassette, m3m4-sweep, m5-prompt-side-effects, m6-census, census-recovery. No background shells or tasks are running.