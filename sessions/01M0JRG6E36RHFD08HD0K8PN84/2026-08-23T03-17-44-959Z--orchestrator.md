Verified 278 landed, closed M7, and opened the recorder fix M8 turned out to need.

MERGE VERIFICATION. 278 is merged into main directly, not into a live base branch. The ancestry check that catches the stacked-PR trap does not apply to a squash merge, since the head SHA never becomes an ancestor; content parity is the right instrument, and `git diff origin/main 8493797` is empty. Trunk is 14c4b268.

THE TRUNK WAS NEVER FIFTEEN FAILURES. A fresh-clone baseline reported 15, but CI at the same SHA reports 3202 tests, 3199 pass, 1 fail. Fourteen of the fifteen are one environmental artifact: canonical-config-dir.mjs derives the canonical agent directory two ways and halts when they disagree, which they do in every checkout that is not the primary one, because the live configuration symlinks into the primary checkout. The one real failure is the node_modules trailing-slash defect, already known and correctly red. So the planned triage of fifteen failures was work that did not exist. Consequence worth carrying: there is no local location where this suite can be read honestly except the primary checkout, since a linked worktree instead makes the census tests pass vacuously. CI is the only trustworthy reader.

M7 COMPLETE. Five mutation experiments, one agent per failure branch, each in its own clone at trunk, each diffing its failure set against the noise floor and confirming a clean revert. Plan revision rejection: COVERED, 15 to 17. Park on NeedsHuman: COVERED, 15 to 20, all five reddening tests firing the same assertNever. Park on dispatch outcome: PARTIALLY COVERED, 15 to 23. Resume carry of a built unit: COVERED, 15 to 72 across unit, integration and six end-to-end layers. Refuse re-ship of a shipped unit: COVERED, both guard sites independently, with an end-to-end witness whose dispatch count rose from zero to three.

M7 shipped as an evidence document alone, at the m15 harness directory, with no repository change: receipts.config.json already carries G14 at block with max_mutants twelve and has no field for registering branch targets. The acceptance clause requiring the local G14 replica to pass before push is vacuous when there is nothing to push, and that is recorded rather than papered over.

M8 BLOCKED, AND WHY. Measurement before spending found the recorder blind to the whole planning phase: the wrapper is installed only inside runUnit and the kind tracker cannot name the plan family, so across every dispatches.jsonl on this machine there are 9 implement, 6 review, 3 diagnose and zero plan, plan-review or replan, including on a scenario that drove four plan-review dispatches. M8's acceptance was unsatisfiable for the plan family. The user chose to fix the recorder before spending.

The fix is PR 279. Three changes: planning dispatches carry the unit id, the run observer is created before planning rather than after (without which the recorder's handle does not exist yet and every record silently no-ops), and the kind tracker learned both plan schemas, naming a unit's first draft plan and later ones replan. Receipt: red on the parent with the assertion quoted, green after, inertness confirmed by reverting the implementation and watching the same red return, and a full suite from a fresh clone showing the noise floor unchanged. CI on 279 reports 3203 tests, 3200 pass, 1 fail, the same known node_modules failure, with receipts and all six mitosis-gate jobs green.

PRECONDITIONS MEASURED. Substrate is clean, zero pull requests ever and only main and _ledger. M4's sweep is green ten of ten at trunk. The primary checkout FAILS its precondition: it is five commits behind and grep finds no recordDispatch in the live run-store.mjs, so a billed run today would execute pre-M1 code and record nothing. The fast-forward is safe, none of the incoming commits touch any of the eight dirty documentation paths.

COST ANCHOR. The preserved 2026-08-21 single-unit run recorded total_cost_usd of 1.2518 for one unit, one dispatch, fourteen turns. That is a per-unit sample and was deliberately not extrapolated.

FILED, NOT FIXED. Ten of thirteen dispatch outcome values have no reddening test, and the null-verdict path has none at all; if that guard were deleted a failed dispatch would report the unit Done with a null sha. An unassigned sixth guard site sits at parking.mjs selectPreservedBuilt. The SPEC misattributes branch 4's test to resume-state-carry.test.mjs, which never imports parking.mjs. All above M7's ceiling, so all new items.

NOT DONE, DELIBERATELY. No branch or worktree was deleted; the stale refs are hygiene only, since no child pull request is waiting to retarget. The primary checkout was not fast-forwarded and its dirty documentation work was not touched.