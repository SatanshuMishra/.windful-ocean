2026-08-04. Read phase of the 8-defect fix round. NO EDIT APPLIED to either document; nothing in .windful-ocean created, edited, staged or committed. Session ended on context exhaustion at the point the first write would have begun, deliberately rather than incidentally: defects 1, 2, 7 and 8 each need a matched edit in BOTH the re-baselined SPEC and the docket, so a half-applied set worsens invariant I5 instead of repairing I4.

SHIPPED: artifacts/ROUND-4-PLAN.md in the handoff dir. It carries (a) the recall-verification setup the handoff demanded, (b) both missing decisions read in full, (c) a per-defect edit list of 14 sites across the two documents with file and line for each.

RECALL VERIFICATION SET UP, NOT COMPLETED. The handoff's named method fix was to enumerate the band rather than search it. Band 0130-0227 enumerated: 98 records, re-runnable command and all titles recoverable from it. Round 4 still owes the bearing/non-bearing mark against each of the 98 - that is the work itself and it was not done here.

0130 read in full. Its two missing rulings sit in the Outcome tail, after the false-clean-path narrative: Resume stays at meta.phases index 10 rather than execution order (display-only, MSP-13 owns Resume), and the CLI having no in-repo caller is accepted since enforcement is the test file under npm test. It also carries a NAMED RESIDUAL LIMIT worth quoting at D-06 (a concatenated key ['pha'+'se'] reads clean; the literal ['phase'] form halts) and a fact bearing on the 0128 row (the SPEC omitted a required edit at frontier-train-e2e.test.mjs:489, found by grep before dispatch, not by the spec).

0210 read in full. Operative green test verbatim, the wording rule forbidding "the suite is green", CI baseline 1832/1831/0 fail/1 skipped, and a closing sentence that belongs in the SPEC: the environment defect is UNFIXED and the guard's own error text says it discovers no repository base and stops protecting the checkout, so that protection is inert on this machine.

TWO CORRECTIONS TO THE HANDOFF'S OWN PICTURE, both verified this session.

1. Corpus size and store. 0209 rules the live ledger is the ledger git ref, not the plugin-data copies. REGROUND-0130-0227.md section 1 read the logbook-inline copy and printed 227; the logbook-logbook copy now holds 230. The 0130-0227 band is unaffected either way, but any recount must state which store it read, and the "full 227-record corpus" phrasing in SPEC :149 and :740 is now a stale absolute.

2. The worktree arithmetic is NOT unresolved in the SPEC. The handoff says neither document resolves it. SPEC section 2 row 0 at :219 already carries the full reconciliation (12 claimed / 13 measured by 0195 / 4 held / 6 at HEAD, explicitly not a like-for-like delta), matching REGROUND section 3. Only the DOCKET Section A row 0 still needs checking - the fix-pass agent reported it still states "Leak count 12 -> 6".

NOT DONE, carried whole to round 4: all 8 defects, and the bearing/non-bearing marking of the 98 records.