Repair round 4. All 8 residual defects APPLIED as one set across both artifacts under ~/.claude/projects/-Users-satanshumishra-Documents-DevLabs--windful-ocean/handoff-2026-08-03-mitosis-core-rebuild/artifacts/. Nothing in the repository was touched or staged (0229 holds).

SITES: 21 applied, against ROUND-4-PLAN.md's list of 14. Defect 1 (0130's two rulings): SPEC new §16 row 0130 + MSP-0 note; DOCKET D-05, D-06, D-08 ratified context. Defect 2 (0210 green test): SPEC new §16 row 0210 + §14 statement; DOCKET corpus-grounding preamble. Defect 3: SPEC :149 bearing set + §15 provenance rows. Defect 4 (D-02 misclassified): DOCKET header, options table, BLUF settled table, arithmetic, consequence table, round-3 changelog; SPEC START HERE §6 + changelog line. Defect 5: DOCKET D-08 [RATIFIED CONTEXT] narrowed. Defect 6: SPEC §8.1 + MSP-0; DOCKET D-08 question. Defect 7: SPEC §16 row 0128 quote restored + false assertion withdrawn; DOCKET D-03. Defect 8: SPEC §0; DOCKET D-06.

SEVEN SITES BEYOND THE PLAN, each because fixing only the planned site would have created a NEW I5 violation: the docket's D-08 carried defect 6's flawed grep (plan listed SPEC only); D-02's reclassification cascaded into the BLUF settled table, the arithmetic sentence, the consequence table, the round-3 changelog table, and both SPEC arithmetic sites. Open surface arithmetic restated 16 = 9 OPEN + 6 PARTIALLY CLOSED + 1 CLOSED; the user-facing count is UNCHANGED at fifteen.

MEASURED THIS SESSION at HEAD da0cefd, replacing evidence that did not establish its conclusion: `git grep -n phase-parity` -> 48 hits across 13 files; restricted to an invocation (`git grep -n "mitosis-gate\.mjs phase-parity" -- .github .githooks scripts package.json .claude/settings.json`) -> exactly one, .github/workflows/test.yml:21. `.githooks/pre-commit` body is `npm test` and nothing else. `git config --get core.hooksPath` -> the logbook-inline plugin's managed githooks dir, which is the live shape of 0131's capture race. Decision 0131 read in full: its three-part scope is (1) close the stale task, (2) make capture logic incapable of recording a managed dir, (3) resolve fail-loud -- phase-parity wiring is in NONE of the three, which is defect 5 confirmed from the source.

STALE-STRING CHECK (not the I1-I8 gate) surfaced two LIVE residuals created by my own pass and both were closed: SPEC §16 intro still read "all 227 decision records", and the docket's round-3 changelog still asserted "fifteen questions = 10 OPEN + 5 residues". Remaining matches of withdrawn phrases were verified to sit inside explicit withdrawal context.

VERIFIED STALE, no action needed: the handoff's claim that the docket's Section A row 0 still states "Leak count 12 -> 6" is FALSE at :75 -- it already carries the full 12/13/9/4/6 reconciliation and names round 1's figure as not like-for-like. Both documents now resolve the worktree arithmetic.

NOT DONE, and these are the whole of what remains for READY: (1) the 98-record bearing/non-bearing marking over band 0130-0227 was never started -- it is a separate deliverable, not part of the edit set; (2) the I1-I8 gate has NOT been run since these edits. Status is NOT READY per 0230 -- not because a defect failed, but because the gate has not been run. No test suite was run this session; no PR, no commit, no branch.

BUDGET NOTE for the next session: 0231's gate was tested for real. The edit set was started at 72% context and finished at ~90%. What made that safe was that the read phase was already cached in-context -- the gate is about an unpaid read phase, not a raw percentage. The next session dispatches subagents, so this arithmetic does not transfer.