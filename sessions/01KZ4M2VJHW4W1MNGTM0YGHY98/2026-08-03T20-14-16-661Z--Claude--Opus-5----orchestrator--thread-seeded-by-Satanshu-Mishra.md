THREAD OPENED, NO WORK DONE. This entry is the dispatch brief for the FIRST session. Nothing implemented, nothing staged, no branch cut, no agent dispatched. The seeding session did read-only measurement only. This thread replaces 01KZ4KGFM462TZZQBKDJCR0PAM (same content, name carried "step6"), which was abandoned minutes after creation with zero work performed.

THE SPEC. docs/superpowers/specs/2026-07-30-mitosis-core-rebuild.md, "Mitosis core rebuild - step 6: fix pipeline, phase collapse, gate model". Status line: "approved architecture, not implemented". Only committed copy: branch docs/mitosis-core-rebuild-spec at d444797, NOT on main. Binding decisions 0115, 0117, 0118, 0119, 0120, 0121; governing 0106-0114 and 0116; all under thread mitosis-architecture-rebuild-exploration (01KYR405KFXHM15J5XXK5BXTVT), which stays open for its own residuals and is NOT this thread's parent.

MEASURED STATE AT SEEDING (main, da0cefd, working tree clean but for the five known dirty paths):
- MSP-0 SHIPPED as PR #15 / 12053dc. Final review deleted, phase('Shepherd') renamed and declared as Resume, phase-parity gate live.
- MSP-1..MSP-13 NOT STARTED. mitosis-gate.mjs exists with no budget verb. MITOSIS_GIT_VERBS is still Object.freeze(['pr-create','pr-close','compare']) at mitosis-git.mjs:30, so none of the ten new verbs exists. Reconcile and Prepare still dispatch separately; there is no run-probe.
- Declared phase set is still the full 13, not the collapsed eight.
- Nothing staged anywhere: neither stash touches phases; no worktree carries a mitosis.js modification; every branch ahead of origin/main that differs on phases carries an OLDER set, never a collapsed one.
- mitosis.js is 5,514 lines. The SPEC's anchors were taken at 4,851 lines.

WHY IT STALLED. Not failure. After MSP-0 the predecessor thread pivoted to the quiescent-advance spec (M0-M8, delivered) and the boundary-preflight deletion. Step 6 has sat at MSP-0 since 2026-07-30, so decision 0114's measured defect is still fully live: 8 sequential model round-trips before the first line of code, and the repo explored from scratch three times with the first two discarded.

THE PRECONDITION PROBLEM, AND WHY THE AUDIT IS IN SCOPE. Decision 0116 fixed an eight-entry landing order: (0) worktree reaper per 0113; (1) a test harness reproducing the real sandbox global surface per 0107; (1.5) streaming-scheduler flip plus run-setup parallelization; (2) kill the 21-module twinning tax per 0112's revised order; (3) durable state model absorbing the journal-append verb and the piggyback rule per 0108; (4) restack as a deterministic activity, where unit-prep IS the primitive; (5) speculation depth per 0109; (6) this SPEC. Steps 0-5 have NO spec document - none exists on main or on any branch. They exist only as decision records. There is therefore no written acceptance criterion for any of them, and the work that plausibly discharges several landed under entirely different numbering (quiescent-advance M0-M8, run-readiness-repair MSP-0..MSP-10, two-track-invariant-plan, b6-harness-liveness). Nobody has ever recorded "step 2 is done". SPEC section 2 states step 6 has no fallback for an absent precondition, so starting on an unproven foundation is the single most expensive mistake available in this thread.

PHASE A - AUDIT AND VALIDATE. Read-only, parallel, before anything else. Nothing may be implemented until A and B have returned and C has landed.

A0 series, ONE DEDICATED AGENT PER STEP, seven agents, steps 0, 1, 1.5, 2, 3, 4 and 5. Each agent must, in this order: (i) read the decision(s) that define its step and DERIVE the acceptance criteria from them - where the decision states no acceptance criterion, say so explicitly rather than invent one; (ii) determine by MEASUREMENT whether that step is discharged, partially discharged, or absent, citing path:line, commit sha, or a test that runs; (iii) map any work that landed under other numbering onto this step, naming the commit or PR; (iv) name precisely what remains, and estimate it; (v) state whether step 6's dependency is genuinely blocking or has a workaround, quoting SPEC section 2's stated reason for the dependency. A verdict of "discharged" must carry evidence that would survive a hostile reading. Absence of evidence is reported as unproven, never as absent and never as done.

A1 codebase-analyst: re-derive EVERY line anchor the SPEC cites against mitosis.js at HEAD - section 4's 14-row fate table, section 5's two findings, every anchor inside section 12's MSPs, section 14's six do-not-regress rows. Return old anchor, new anchor or NOT FOUND, and what now sits at the old line.

A2 codebase-analyst: per MSP-1..MSP-13, does its named target still exist; has any part already landed by other work; does each deletion target still exist. Flag every MSP whose premise is now false.

A3 researcher: has the harness contract the SPEC assumes changed since 2026-07-30 - agent() as sole effector, opts.phase as the third parity surface, Workflow script capabilities, schema-forced structured output. Cite the current contract.

ADVERSARIAL VERIFICATION IS MANDATORY on every step reported discharged, and on the highest-impact A1/A2 findings. Dispatch a second agent prompted to REFUTE, defaulting to unproven when uncertain. The asymmetry is deliberate: a false "absent" costs re-work, a false "discharged" costs a rebuild on a missing foundation.

PHASE B - CHOOSE THE REMEDY. For each unmet precondition and each SPEC gap: solution-architect with 2-3 options against trade-offs and one recommendation, grounded in current code. An unmet precondition is a decision point, not automatically a build: the options include landing it, scoping step 6 around it, or recording why step 6 may proceed without it. Where a gap touches gate trust, fail-closed halting or the effector boundary, add a second lens rather than a second correctness reviewer.

PHASE C - MAKE THE CHANGES. Land the prerequisite work that phase B decides to build. Re-baseline the SPEC: correct every stale anchor, amend any MSP whose premise A2 refuted, and record a decision per material change rather than silently editing. Record a decision per precondition disposition - discharged, landed here, or deferred with reason. Decide and record whether the SPEC lands on main. Commit before dispatching implementation.

PHASE D - IMPLEMENT. One DEDICATED small dynamic Workflow, not ad-hoc subagent dispatch. Bottom-up ordering; MSP-1 MUST precede MSP-2, or the before-and-after replay 0115 owes is unpayable forever. Stacked PRs are permitted to keep working through a blocking PR. Test MSP-12's falsifier EARLY rather than at the capstone: if a stage needs a closure over run state, pipeline-as-data is refuted and the fix pipeline stays imperative. Expect the human write-approval hook to serialize every write under .claude/{hooks,rules,lib,workflows}; design around interactive approval, not unattended fan-out.

USER DIRECTIVES CARRIED VERBATIM. No debrief was run on the seeding conversation. The predecessor thread will be restarted separately for its own remaining work. This thread's primary focus and completion criteria are this SPEC and the preconditions it rests on, and nothing else; other specs are evidence for the precondition audit, never work items.