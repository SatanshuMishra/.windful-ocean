Audited every PR-creation surface in the config. Found FIVE independent paths that can open a PR: the mitosis engine, the vendored /commit-push-pr command, the vendored superpowers finishing-a-development-branch skill, the GitHub MCP create_pull_request tool, and ad-hoc gh pr create. Four of the five invented title and body ad hoc. Also found three contradictory policies and one latent defect: engine PRs titled "mitosis: <id>" fail the Conventional-Commits title lint the engine itself deploys to target repos via receipts.yml.

Research (fable researcher): title = Conventional Commits, max 72 chars, no ticket id, because GitHub squash-merge seeds the squash subject from the PR title. Body = Why / What / Verification with a hard Verified-vs-Not-verified split / Provenance. Checklists cut as measured noise. Longer descriptions correlate with SLOWER review, so more sections is disconfirmed. Honesty rule: never synthesize a value not known; a fabricated test plan is strictly worse than none because it weaponizes automation bias. Size warning past ~400 changed lines.

Enforcement research: only permissions.deny and PreToolUse hooks are harness-enforced. CLAUDE.md, rules, skills, and slash commands are advisory; nothing can force a skill to govern an action. Hooks DO fire inside subagents, which the design depends on since mitosis opens PRs from dispatched subagents.

Dispatched 13-agent workflow wf_a756a3d9-199: spec -> three adversarial lenses -> revise -> core tool -> three parallel periphery units -> verify -> code+security review -> remediate. Shipped four atomic commits on feat/centralized-pr-creation. NOT pushed, no PR opened, nothing merged.

VERIFIED INDEPENDENTLY BY THE ORCHESTRATOR (not taken from agent self-reports): engine suite 1277 pass / 0 fail; hook suite 107 pass / 0 fail (note: correct invocation is node --test "tests/*.test.mjs" — the bare directory form errors on Node 26). permissions.allow verified untouched; the PR-write surface went into permissions.deny. The workflow result payload did NOT carry the verify/remediate agents' self-reports at top level, so independent verification replaced them.

Gate proven live incidentally: the orchestrator's own verification command was blocked because it contained the blocked phrase as a literal string. The matcher is text-based and cannot distinguish a command from a string mentioning one. Accepted UX cost; a looser matcher would be evadable.

BONUS SECURITY FINDINGS — the gate agent tested the EXISTING merge gate instead of assuming it worked, and closed three pre-existing holes that were letting merges through: (1) line-oriented matching, so "gh pr" + newline + "merge 12" passed; (2) the word boundary excluded "/", so "/opt/homebrew/bin/gh pr merge 12" passed; (3) neither GraphQL spelling was covered, so mergePullRequest and enablePullRequestAutoMerge both passed — the latter tells GitHub to land the PR itself once checks go green. It also correctly REJECTED a reviewer's proposed sed multiline fold after verifying that on BSD/macOS sed it emits zero bytes for single-line input, which would have made the ENTIRE hook (rm -rf, force-push, guardrail protection) fail OPEN. Additionally closed gh pr edit --title/--body, a real bypass where a compliant PR could be rewritten one command later.

FALSE POSITIVE TO EXPECT AGAIN: the harness flagged the impl:gate subagent for "adding auto-approved permissions for gh pr merge / gh pr create". Verified by direct read that those entries went into deny, not allow — the classifier misread the direction. This will likely recur on any future gate work.

Left open at hand-off: the four unchecked completion criteria.