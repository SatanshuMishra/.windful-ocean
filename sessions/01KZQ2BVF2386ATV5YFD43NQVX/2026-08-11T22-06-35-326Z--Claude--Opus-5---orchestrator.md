Started the clock-bound cache-expiry probe (the c5 item the last hand-off ranked first because it is bound by wall time, not effort). Nothing else was started: no SPEC text, no implementation, and the orchestrator context-curve measurement and c1's figure re-derivation remain untouched.

WHAT SHIPPED. A three-turn probe built into the existing harness at artifacts/2026-08-11-sdk-probe-harness/ (new files q11lib.mjs, q11a.mjs, q11b.mjs, q11-driver.sh, q11-state.json, q11-driver.pid, plus a durable node_modules pinned to SDK 0.3.228; q1-q10 and results.jsonl untouched). One session id eccac898-613f-4a7f-9530-f548112e460a, all turns on claude-haiku-4-5-20251001, CLI 2.1.228, settingSources [] and tools [], service_tier standard. Results append to q11-results.jsonl, deliberately NOT the scratchpad, which is ephemeral and nearly cost the prior run its raw data.

Design point worth preserving: the expiry wait is timed from the CONTROL RESUME, not from session creation, because a cache hit refreshes the entry's TTL. Timing from creation would have measured a still-warm cache and produced a false expiry price. T2 and T3 are the identical code path and options, differing only in elapsed time, so any cost delta is attributable to expiry alone.

RECORDED SO FAR. T1-CREATE: input 10, cache_creation 0, cache_read 70,368, output 92, sum 70,378, $0.0783058, 3,876 ms. T2-CONTROL-RESUME: input 10, cache_creation 110, cache_read 70,368, output 43, sum 70,488, $0.0074818, 1,512 ms. Elapsed T1 to T2: 30.1 s. T2 reproduced the prior run's in-window result within 0.5% (Q4B: 70,368 read, 105 creation, $0.0074468), so the in-window case is now anchored inside a single run rather than across two.

STILL RUNNING - DO NOT ASSUME IT DIED. Detached waiter PID 99761 (PPID 1, own session leader, absolute-epoch deadline with 60 s polls so machine sleep cannot make it under-wait). Deadline epoch 1786489644 = 2026-08-11T23:07:24Z = 17:07:24 MDT. It runs T3-EXPIRY-RESUME and appends to q11-results.jsonl; on error it retries once after 180 s as T3-EXPIRY-RESUME-RETRY and writes the error object either way. Kill command if it must be stopped: kill 99761. Highest failure risk is OAuth expiry, since the detached process is no longer under a host offering token refresh.

CONFIRMED. The SDK requests the ONE-HOUR TTL, not the 5-minute default: both turns carry ephemeral_1h_input_tokens and ephemeral_5m_input_tokens verbatim, and T2 shows all 110 created tokens on 1h and zero on 5m. This pins an assumption the prior artifact had only inferred from an observed window, and it is why 4,500 s is the correct wait rather than a guess.

UNPLANNED FINDING, decision-grade. T1 was supposed to pay the ~$0.21 creation cost and instead reported cache_creation 0 and cache_read 70,368: a brand-new session id in a new process read the prefix Q4A wrote 35.7 minutes earlier, because the mandated byte-identical filler produced a byte-identical prefix. The Anthropic prefix cache is therefore keyed by CONTENT, not by session. Read directly off the usage fields, not inferred from cost arithmetic. Two consequences for SPEC B: favorably, N orchestrator sessions sharing a system prompt and loaded context do not each pay a cache write, which bears on any fan-out or rotation design; hazardously, any cost figure benchmarked on a fixed payload can be accidentally warm and under-report creation cost. That is a defect in measurement METHOD, and it applies to how figures are gathered going forward, including c1's re-derivation.

UNRESOLVED, blocks a number entering the SPEC. T1 billed $0.0783058 and T2 $0.0074818 for the SAME 70,368 cache_read tokens, differing only trivially in output tokens. A 10x gap the usage fields as reported do not account for. Not explained away here; flagged so no cache-read price is quoted until it resolves. It does not affect the T2-vs-T3 comparison, which is two resumes on one session over one prefix.

CONTAMINATION RISK on the pending result: because the prefix is content-keyed and shared with the older Q4A lineage, anything re-sending that identical 70,368-token payload before the deadline refreshes the TTL and T3 would report a warm read. Nothing scheduled will. T3 records elapsedSecSincePrevAtStart and cacheWindowLapsed (strict > 3600) and refuses to run at all if the T2 completion timestamp is missing, so a warm or short result is detectable rather than silently mistaken for an expiry price.

LEDGER HYGIENE CAUGHT IN PASSING, not acted on. Two standing risks still cite .claude/lib/superpowers-parallel/supervisor.mjs:11, but 0340's rename of that tree to lib/mitosis has since merged (see 709ff7f, 1ecacc7). The substance of both risks stands; the path refs are probably stale and should be re-checked against the tree before either is acted on. Restated verbatim this session rather than silently rewritten to an unverified path.

NO decision record promoted. Nothing was ratified by the user this session: the probe design was mine and the instruction was to start it. The content-keying finding and the cost discrepancy are measured facts and an open question, not chosen approaches, so they are carried as risks. If the measurement-hygiene constraint should be locked as a decision, that is the user's call to make explicitly.