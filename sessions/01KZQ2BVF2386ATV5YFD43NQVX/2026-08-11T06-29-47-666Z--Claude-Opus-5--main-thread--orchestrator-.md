Both open research passes ran and both re-asks were settled. The deliverable changed shape mid-session by user direction.

WHAT RAN. Two researcher agents dispatched concurrently and synchronously, both returned: context-usage-versus-accuracy scaled to window size, and the enforcement surface that makes instructions binding. Both returns persisted verbatim to artifacts/ under handoff-2026-08-10-spec-b-rewrite so the next session reads them from disk rather than from a narrated summary. No repository file was written and no implementation was started, per c4.

THE WINDOW-SCALING PREMISE DID NOT SURVIVE. The user directed thresholds scaled to window size, so 250K and 1M would differ. The evidence points the other way: RULER found models claiming 32K to 1M cluster at 4K-64K ABSOLUTE effective context; NoLiMa found 11 of 13 models below half their own baseline at a fixed 32K regardless of claimed window; Anthropic's own compaction triggers at an absolute 150,000 input tokens with a 50,000 floor and does not scale for the 1M beta. Put to the user with the contradiction stated plainly; the user withdrew window scaling and chose an absolute trigger. Recorded as 0329.

ORCHESTRATOR AUTHORITY NARROWED. The user chose the narrowest of three options: the orchestrator judges inside a phase, halts and asks a human, and never reorders, skips or re-runs. The phase graph belongs to the toolkit. Recorded as 0330, which also carries the enforcement construction down to bare-name denies, dontAsk mode, the explicit spawn-depth pin, and rejecting bypassPermissions at host startup.

BOTH BANKED FIGURES VERIFIED. arXiv 2606.22528 (constraint drop 30-59%, verbatim pinning restores 0% at under 0.5% of context) and arXiv 2607.02294 (boundary violations 55.8-67.8%, refusals at or below 2.5%) were each checked against the primary source this session and both hold.

DELIVERABLE CHANGED BY USER DIRECTION. The /report flow was started and then explicitly stopped by the user, who directed the visual-explainer skill be used directly to diagram the architecture at a fundamental and detailed level. Recorded as 0331. A five-diagram self-contained HTML page was produced: containment tree, full-run sequence, fan-out with the in-flight registry, the rotation handoff, and the six-step permission precedence. Written to ~/.agent/diagrams/mitosis-rearchitecture-2026-08-11.html and copied to artifacts/DIAGRAMS-mitosis-rearchitecture.html.

WHAT FAILED OR WAS LEFT UNDONE. (1) A verification pass covering the carried engine census, the three harness blockers and the JS-versus-Python findings was dispatched and rejected by the user mid-flight; those figures remain carried-and-unverified and the page says so in its footer. (2) The rendered page could not be verified — it lives outside the project folder, so the preview pane renders it as a static snapshot and the browser tools report no site open, leaving console output and layout unchecked. (3) A gap-extraction script wrote zero rows because open_risks sits under spine rather than at top level; not retried, since the spine already holds them durably.

NOTED. The primary checkout is on chore/licensing-polyform-noncommercial at 75b90a3 with a clean tree, not feat/invariant-inert-registry as the session-start snapshot claimed. No branch was switched. SPEC B was read from main via git show into the scratchpad.