Preflight, then the user ruled on all seven open questions left by the prior session, then the report was corrected to match.

RULINGS AND RECORDS. Q1: graphify ALONE determines parallelizability — the bounded-oracle confirmation pass is cut. 0364 is superseded by 0365 and removed from the spine index; its file remains on disk. The user's reason: graphify exists in the design for robustness, speed AND efficiency together, so if the graph is current its file relationships are trusted, and spending agents and tokens to re-verify them defeats the point. Q2: the directive's five-step flow was never all-inclusive and there is no missing step 5; the phase list stays the eight-phase model of 0338, and the rework's target is cutting pre-implementer audits that exist only because earlier phases were not trusted (0366). Q3: the 2026-08-11 directive and the design brief are demoted as outdated rather than corrected; current decision records govern (0367). Q4: no inlining, the report ships CDN-loaded. Q5: c5 rewritten to drop the hard diagram count. Q6: renumber. Q4-Q6 are recorded together in 0368. Q7: the pillars.md edit is deferred and added to out-of-scope.

REPORT CORRECTED (implementer subagent, backup at scratchpad/report-backup-pre-0365.html). Every trace of the confirmation pass is gone: the S4 CONFIRM CUTS table row and mermaid node, the recall-instrument node and paragraph, the confirm-cuts verb-catalogue row, the "precedent that proves confirm-cuts is buildable" card, the confirm-cuts message in the partition sequence diagram, and the R2 and G1 prose. Pipeline stages renumbered S0-S6 to S0-S5 with edges rewired. The boundary-set and graph-slice mechanism was KEPT deliberately — 0363 still stands, cut edges still reveal coupling, they are simply no longer re-confirmed. Figures renumbered 7-12 to 5-10; captions now run 1 to 10 contiguously.

TWO FINDINGS. The "dangling Figure 6" was not a defect: line 561 reads "the rotation handoff (prior Figure 6)", a deliberate cross-reference to the superseded page whose machinery is now deferred out of scope. Nothing to repoint; left untouched, and the risk is retired as a false premise. Separately, the section heading "the five steps" sat over a seven-row table before the edit and was already wrong; it now reads "the six steps".

NOT DONE. Browser render verification was not performed — the subagent has no browser, and two diagrams were edited by text only. Structural checks did pass: zero remaining "CONFIRM CUTS" or "bounded oracle" strings, 10 figcaptions numbered 1 to 10, a full-document HTML nesting parse clean, and zero undeclared edge endpoints in the edited mermaid block. Mermaid's own parser never ran, so no claim is made that any diagram draws. c5's user review is still unmet.