User reviewed the diagram page and returned four gaps rather than approval. Four research passes ran to completion (2 researcher, 2 codebase-analyst), all dispatched run_in_background false; findings presented inline and NOT yet written into the page. Approval to rewrite is pending on two open questions.

GAP 1 - rotation threshold. User directs 200K absolute, superseding 0329's provisional 100K. Research materially changed the evidence picture: NEITHER anchor paper measures the 100K-300K band (RULER stops at 128K, NoLiMa anchors at 32K), so the prior 100K was itself extrapolation, not measurement. Two defects surfaced that outrank the number. (a) Anthropic defines input_tokens as EXCLUDING cache reads; true occupancy is input_tokens + cache_creation_input_tokens + cache_read_input_tokens, and an orchestrator's stable prefix lands mostly in cache_read, so a supervisor reading input_tokens alone undercounts and the trigger fires late or never. (b) Summing input_tokens across turns double-counts, since each turn already carries the whole prior conversation - the trigger must read the LATEST turn's three-field total. (b) is my analysis from the field definitions, not a cited finding. Also corrected: the page misstates constraint-pinning as "30-59% of runs"; 2606.22528 measured 0-59% across 7 models, pooled average 30%, restored to 0% at ~47 tokens.

GAP 2 - orchestrator autonomy. User's goal (orchestrator resolves SPEC-vs-reality conflicts and dispatches its own research; human only when it cannot resolve reliably) is supported by precedent but NOT by the mechanism it implies. Anthropic's multi-agent research lead agent already decides whether more research is needed and dispatches subagents - direct precedent for mid-run research dispatch. But verbalized confidence tracks commitment, not correctness (2606.29490), so "escalate when not confident" is not implementable as self-assessment. UnderSpecBench nuance worth keeping: refusal <=2.5% but clarification requests reached 44.5% - agents ask, they just do not decline. Design consequence: gate autonomy on EVIDENCE CLASS, not self-assessed confidence, with a default-to-escalate fallthrough (the only mechanism with direct evidence). Taxonomy drafted: auto-resolve when a deterministic artifact settles it or an in-repo convention covers it or independent research converges; escalate on irreversible/externally-visible consequence, novel edge case with no in-repo precedent, disagreeing or single-source research, and everything reaching fallthrough.

GAP 3 - phases. All 12 phases mapped (cadence, input, process, output, dispatch count, halt sites). Order: Reconcile/Decompose/Prepare once per run, then per MSP Plan, Plan review, Parallelize, Branch (embedding Waves, Integrate, Boundary, then the CI-fix loop), Ship; Remediate re-entered from inside any stage. Carried claims corrected: mitosis.js:4288-4290 is a soft refusal that continues, not a halt; :5169 is a .catch swallowing a probe failure, not a halt; :4739-4744 sits in Prepare not Ship; a 13th label phase 'Resume' exists at :3314; *_SCHEMA count is 25, not the 26 in SPEC B section 0.6. LARGEST correction: mitosis.js never imports run-engine.mjs - it defines its own runEngine at :1272 that has diverged ~77 lines from run-engine.mjs:296, so SPEC B section 0.3's guard.dispatch census describes a forked copy the engine does not execute.

GAP 4 - the two scripts. supervisor.mjs is 61 lines, pure, per-unit: one unitId+stage remediation cycle, verbs resume/retry/stop/escalate. Its ESCALATE never reaches a human mid-run - STATUS_FOR_VERB.escalate is 'parked' and the park surfaces only in the post-run report. The proposed supervisor process (Node host, SDK session, reads ResultMessage.usage, owns rotation) is a NAME COLLISION, not an extension; nothing in-repo is that concept. Composition measured: 25 whole-mirrored lib files total 2,952 of 5,515 lines; backtick literals 144,863 of 362,969 chars - overlapping estimates, not a partition. supervisor.mjs and remediation.mjs survive a move to real Node untouched; the Workflow ambients (agent/parallel/log/phase) are moot and must be rebuilt as SDK calls.

NOT DONE: the HTML page is unchanged. The planned edit set is section 05 rewritten to 200K with the corrected counter and the pinning figure fixed, three NEW sections (autonomy boundary with taxonomy + sequence diagram, phase anatomy with tree diagram splitting per-run from per-MSP, the two scripts), section 07 decisions extended, and a footer stating the census is now re-derived rather than carried.

Session ended on context pressure at ~87% with two questions open to the user: whether to amend 0330 to permit cross-phase re-entry for one MSP under a typed conflict class with a hard attempt budget, and whether the 200K counter reads three-field total occupancy or literal input_tokens as directed.