Executed the recorded next step in full: rewrote ~/.agent/diagrams/mitosis-rearchitecture-2026-08-11.html with the four approved gaps closed. No new decisions were ratified; no criterion closed.

SHIPPED. Three new sections. (1) Section 03 "Phase anatomy" — the eight-phase model of 0338 as a tree diagram (Probe, Decompose, Prep, Execute, Integrate, Ship on the happy path; Resume relaunch-only, Remediate failure-only), replacing the old "Phases 04-13" placeholder. Carries all four amendments as a table, both corrections as cards, and the deleted Plan-review / Final review fates. (2) Section 05 "The autonomy boundary" — 0337 as a sequence diagram: the evidence-class gate, mid-phase research dispatch with cross-run convergence, typed one-MSP re-entry under a budget held outside the model, and unconditional escalation on irreversible consequence / novel edge case / fallthrough. Reorder and skip drawn as still forbidden. (3) Section 08 "Two scripts, one name".

CORRECTIONS APPLIED. Rotation is now 200K measured as the latest turn's input_tokens + cache_creation_input_tokens + cache_read_input_tokens, in Figure 6, a new callout, and the table row; the row also now states that nothing published measures the 100K-300K band and that the 200K-300K completion band is reasoned margin, not measured. Constraint pinning corrected from "30-59%" to 0-59% across seven models, pooled average 30%, restored to 0% at roughly 47 tokens. The hero BLUF and Figure 3's alt block were rewritten off the pre-0337 "judges only inside a phase" rule. Figure 1's launcher node no longer asserts the name supervisor.mjs. Decisions table extended to 0324-0338 with supersession and amendment marked inline; footer refreshed. Sections renumbered 01-11 and figures 01-07, verified sequential by grep, and the one stale cross-reference (Figure 3 -> Figure 5) fixed.

TWO-SCRIPTS FINDINGS, RE-DERIVED LIVE RATHER THAN TAKEN FROM THE SESSION LOG. A grep of the session archive did not surface them, so both were verified against the repository directly. (1) .claude/lib/superpowers-parallel/supervisor.mjs already exists and is unrelated to the proposed Agent SDK host, so the launcher cannot be named supervisor.mjs. (2) supervisor.mjs:3 declares SUPERVISOR_VERBS = resume, retry, stop, escalate, and supervisor.mjs:11 maps STATUS_FOR_VERB escalate -> 'parked'. This is the more dangerous of the two: section 05's safety rests on default-to-escalate-at-fallthrough BECAUSE it is deterministic rather than judged, but that only holds if escalate reaches a human. If the new host inherits this repo's verb vocabulary — the natural move, since the words match — every fallthrough silently parks the MSP and the one mechanism with direct evidence of reliability becomes a no-op. Written into the page as requiring an explicit rename-or-redefine in the SPEC.

NOT DONE, AND WHY. The page was NOT regenerated through the visual-explainer skill. Context was already at 73% when writing began, so it was edited in the page's existing idiom instead — its CSS/JS scaffold, Okabe-Ito classDefs and mermaid zoom shell were already the skill's output, and loading the skill would have cost the budget needed to finish. This is a session expedient, not a ratified decision, and it was disclosed to the user in chat.

NOT VERIFIED. The two new mermaid graphs have never been rendered. The page lives outside the project folder, so the preview pane shows only a static snapshot and the browser tools cannot reach it; a mermaid syntax error fails silently to an empty box rather than an error. The mitosis.js line citations (:1191, :1503-1531, :4817) were carried from the 2026-08-11 audits and not re-confirmed; this is now stated in the page footer. The phase-literal count (48 vs 50) and SPEC B section 0.4's prompt-byte figures remain unreconciled and are flagged in-page as not-to-be-cited.

USER DIRECTION AT HAND-OFF. The fresh session is to WAIT for specific instructions. The goal is to continue understanding and modifying the report; the final SPEC is not to be started until that is settled.

No background agents, tasks or shells were running at wrap-up.