Preflight on this thread. The user sequenced the directory reorganization FIRST, as the stated prerequisite to updating mitosis, and asked for a dedicated orchestrator subagent to carry it end to end.

Because 0324 and this thread's criteria forbid implementation here, and 0340 had already scoped the rename's execution to a branch outside this thread, the work was NOT taken on inside this thread. A child thread was opened instead: directory-reorganization-lib-mitosis-rename (01KZS3HZHY8T9JTPQS3CGH29T9), with six completion criteria of its own. Decision 0344 was recorded there covering how it executes.

Two constraints shaped the dispatch. The primary checkout must never switch branches, since live config resolves through ~/.claude/current into a release directory rather than the working tree, so the work runs in a git worktree. And 0340's 17-site enumeration was demoted from worklist to cross-check: the orchestrator re-derives the census over the current tree and must explain every difference from 17 rather than absorb it, because a pinned count standing in for a census is what this repository's own testing rule forbids.

Nothing in this thread's own criteria advanced. c1 remains the current step. After the child thread closes, work returns here to continue defining scope against the recorded decisions before any SPEC text is written.