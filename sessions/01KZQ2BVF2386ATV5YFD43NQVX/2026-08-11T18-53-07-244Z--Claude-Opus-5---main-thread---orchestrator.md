Deep-dive session on two fronts. No implementation, per this thread's criteria.

(1) .claude ORGANISATION AUDIT (unplanned work, recorded as a detour).
lib/superpowers-parallel holds 38 .mjs files / 5,247 lines, of which exactly ONE (resolve-superpowers.mjs) concerns the vendored Superpowers plugin; the other 37 are the mitosis engine. Same pattern at larger scale in .claude/docs/superpowers (58 files, only 20 mitosis-titled), which duplicates the already-existing docs/plans and docs/specs hierarchies. Blast radius enumerated exhaustively: 17 load-bearing path strings across 12 files - hook superpowers-drift-check.sh:6, CI test.yml:22, package.json:8, three imports in the mandated lib/git/pr.mjs, a SELF-REFERENTIAL hardcoded path at resolve-superpowers.mjs:79 that will not follow its own rename, and mitosis.js:23 LIB_DIR which is interpolated into subagent PROMPTS. Release machinery names no subdirectory (paths.mjs:18-29 promotes `lib` whole), so an ordinary release+cutover suffices. No orphan directories or files found.

CORRECTION TO A STANDING RISK: supervisor.mjs is NOT an unrelated parallel-execution helper. It IS the mitosis fault-recovery supervisor, imported by remediation.mjs:2, which is imported by parking.mjs and run-log.mjs. escalate -> parked confirmed exactly at supervisor.mjs:11. The launcher-filename advice stands but for a STRONGER reason: the collision would be inside the same system, not with a stranger. mitosis.js carries a hand-duplicated inline twin at :2107-2165 guarded by mirror-guard.test.mjs.

(2) ROTATION-HANDOFF ARCHITECTURE. Four investigations: logbook 0.2.4 implementation audit; evidence pass on summarization fidelity; evidence pass on long-horizon agent memory architectures; run-log substrate audit.

Key verification: arXiv:2606.22528 RESOLVES and quote-grounds 0336's figures EXACTLY - "Governance Decay", 1,323 episodes, ConstraintRot benchmark, seven model families, 0-59% range, 30% pooled violation rate, restored to 0% by ~47-token verbatim pinning. 0336's evidence base is sound.

Other load-bearing findings: logbook has NO summarization algorithm (update-thread.mjs:68-81 is a flat object spread); its documented demote-with-pointer is NOT implemented (caps.mjs:22-35,143-145 rejects instead); every logbook write is a git commit (897 for this project). Anthropic's compaction docs make NO preservation guarantee; the memory tool auto-injects "ASSUME INTERRUPTION". run-log.mjs's fold is deterministic, total and additively extensible - proven live by an orphaned {"kind":"window"} record in fireplace/.mitosis/run.json that no code constructs and the fold correctly ignores - but its durability layer is not built for this purpose (append-only is a prompt instruction, no atomicity, .mitosis/ gitignored, refs/mitosis-manifest/ write-once and strips mutable fields).

Reliability evidence for EVERY handoff pattern surveyed is DESCRIBED, not MEASURED - no vendor publishes handoff success rates. That negative finding is why further literature research is closed as saturated.

NOT DONE / OPEN. No SPEC text written. The diagram page has STILL never been rendered in a browser. The instrument source in SPEC B section 2.2 - which section 8 names the single largest open question - was NOT addressed. The "hold less" alternative (Cognition's published position that handoffs should be avoided entirely) was flagged and deliberately NOT investigated. And the soft centre of my own recommendation is unresolved: Tier 2 still asks the orchestrator to CHOOSE to emit a typed record, which is the same class of voluntary judgment as the summariser it replaces.

SPINE NOTE: open_risks for scope c2 were replaced in full this session. Any prior c2 risks concerned questions that these four decisions settle.