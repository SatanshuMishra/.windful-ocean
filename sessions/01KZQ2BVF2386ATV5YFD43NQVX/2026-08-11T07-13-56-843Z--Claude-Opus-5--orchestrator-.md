CORRECTION to this session's earlier claim. I stated the new phase sequence did not exist as a written artifact. That was wrong. It exists, designed, at docs/superpowers/specs/2026-07-30-mitosis-core-rebuild.md section 4 "The phase model after collapse", reachable as git show docs/mitosis-core-rebuild-spec:docs/superpowers/specs/2026-07-30-mitosis-core-rebuild.md - it is NOT on main, which is why a working-tree search missed it.

THE DESIGNED PHASE SET, exactly eight: Probe, Decompose, Prep, Execute, Integrate, Ship, Resume, Remediate. Six on the happy path; Resume entered only on relaunch, Remediate only on failure. Derived by collapsing the current engine's 13 declared titles: Reconcile and Prepare fuse into Probe; Decompose kept and absorbs the task cut; Plan deleted, surviving only as a needsPlan escalation; Plan review deleted, auto-approving by construction; Parallelize deleted as a dispatch, its edges becoming a verb; Branch deleted for root MSPs with frontier-only prep surviving as Prep; Waves renamed Execute; Integrate kept and absorbs Boundary as gate-lint; Boundary folded into Integrate; Final review deleted as declared-but-never-entered; Ship kept; Remediate kept; undeclared Shepherd declared honestly as Resume. Two ambiguities the spec closes: Cut is not a title (decompose-and-cut is ONE dispatch under Decompose), and phase parity is a STATIC source property, never an assertion that a run entered every phase.

CONSEQUENCE FOR THE ARTIFACT: the diagram page is wrong on this point. Figure 1 shows "Phases 04-13 fixed sequence" and Figure 2 dispatches "phase 01 Decompose". The design says EIGHT phases, not thirteen, and Probe precedes Decompose. The 13 was carried from the current engine's declaration count, not from the design.

CAVEAT the rewrite must carry: that phase table's line citations were measured against mitosis.js at 4,851 lines; it is 5,515 today. Decision 0128 already re-derived the drift as four bands (+59 model policy through Boundary, +73 Reconcile/Remediate/Shepherd, +78 Decompose through Parallelize, +74 Branch/Ship) and confirmed the census SHAPE survives unchanged - 13 declarations, 13 call sites, 45 phase: literals over 13 titles. So the design holds and every :NNNN in it needs re-derivation before use.

INTERACTION with SPEC B that nobody has reconciled: the collapse DELETES Plan review and Parallelize as dispatches. SPEC B's H4 hypothesises about the plan-review payload and H2 about the Decompose/Parallelize dependency overlap - both are moot or transformed if those phases cease to exist. Also, SPEC B's preamble explicitly disclaims all citation authority from the 2026-07-30 document, so the rewrite must RE-ADOPT this phase design deliberately rather than inheriting it silently.