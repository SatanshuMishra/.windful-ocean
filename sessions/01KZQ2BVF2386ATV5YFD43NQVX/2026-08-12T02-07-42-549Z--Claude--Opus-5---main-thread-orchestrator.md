Worked the 2026-08-11 directive's seven items. Two read-only censuses grounded everything: a graphify capability audit and an exhaustive pre-implementer query-site census of mitosis.js.

SHIPPED. Report rewritten at ~/.agent/diagrams/mitosis-rearchitecture-main-thread-2026-08-11.html, 116,736 -> 139,647 bytes. Section 11 "The SPEC trust gate" became "Admitting a SPEC": T1 GROUNDED deleted outright, P1-P7 checks installed, a per-failure consequence table closing G8. Section 12 became "Graphify: SPEC to sealed partition": three fact callouts, the S0-S6 pipeline, the enforcement inversion, the file-granularity requirement. Section 17 re-cut to 22 gaps, G1/G8/G11/G12 marked Closed in place, G13 downgraded, four new gaps added. All ten mermaid diagrams render-verified in two independent browser engines over a local HTTP server; zero failures. Decisions 0361-0364 recorded at decision time.

FOUR CORRECTIONS TO PRIOR WORK. (1) The claim that no numeric graphify recall figure exists in-repo is FALSE - specs/2026-06-29-mitosis-design.md:60 records Graphify 0.9.1 on a 1,624-file TypeScript project at ~13% average false negatives, 25-32% on critical shared utilities, 8.7% silent zero-output. The falsehood appears in the directive, the design brief and the old section 12; I propagated it before catching it. (2) The recall claim lives at tool-routing.md:7, not :8, in all three. (3) DECOMPOSE_SCHEMA closes at 1586 not 1585; dependsList embeds at :4830 not :4829; PARALLELIZE_SCHEMA is 1723-1748. (4) The report artifact size is 9.1 MB by du -h, not 9.5 MB.

THE FINDING THAT MATTERS MOST, not in the directive or the brief: the SPEC is never handed to any phase as trusted content. Decompose gets a path (:4173), Plan gets the same path again via planGroundTruthSeed (:1206 used at :4831) plus four scalar fields whose only free-text member is capped at 200 chars. Every phase re-reads the document and forms its own reading; nothing detects divergence. So provenance trust cannot be declared - admission must emit an artifact or trust is discarded one line after it is established. That drove 0362.

SECOND SHARPENING: the re-audit is three sites, not a pervasive disease. The census classified every pre-implementer site: DELETE 3 (planGroundTruthSeed, and the Serena/Read/Grep halves of :4175 and :4181), KEEP-INDEX 3, KEEP-INDEPENDENCE 2, KEEP-OTHER 7. Two of the directive's four "load-bearing citations" are something else - :3525/:3529 is deliberate adversarial plan review, and :1344 is post-implementer entirely. A blanket "no codebase queries" rule would break the Prepare prerequisite probe and the Reconcile run-state check.

USER PUSHBACK, ACCEPTED. I asked whether to attach the recall measurement to pillars.md. The user refused: pillars are guiding principles and must not become anything else; a principles document that carries a fact rots when the fact does. Correct, and the deeper defect is that the worked example adjudicates two named tools inside a tool-agnostic document - which is exactly why both I and the directive cited it AS the rule. pillars.md left untouched (0361).

NOT DONE. c5's user review has not happened, so c5 stays open. c1 stays open: this session corrected the REPORT's citations, not SPEC B line by line. Work items 3 and 5 are specified, not implemented - the successor thread owns DECOMPOSE_SCHEMA, the slice function and deleting planGroundTruthSeed. The user's step 5 was left as "..." and I assumed review -> integrate -> ship without instruction.

Nothing left running. The verification agent stopped its HTTP server and confirmed git status clean.