Shipped: the revised SPEC. Read github.com/shaheershoaib/fanout in full at commit 06ae1921 (1,929 lines, cloned to a session scratchpad, never executed) and censused mitosis's dispatch surface. Central finding that reframed the thread: fanout's PRIMARY mode is the inversion - the model calls it as a calculator and dispatches with its own primitive, and its Claude Code adapter names the Workflow tool's pipeline()/parallel() as the preferred dispatcher (adapters/claude-code/SKILL.md:196-199), with --exec the fallback for runtimes lacking a subagent primitive (:50-52). So fanout is not evidence for leaving a sandbox; its transferable thesis is "no sampler in the deterministic path", which still forces the move because 24 of mitosis's 38 dispatch sites (~63%) exist only because the sandbox has no fs/exec.

Closed 0369's blocking question empirically against Claude Code v2.1.228: claude -p reproduces the whole hook surface (--agent, --model, --effort, --json-schema, -w), preserves agent()'s internal HTTP-transient retry, and keeps settings hooks and permissions.deny firing. Only opts.phase is lost and it is cosmetic. Measured a trap worth carrying: --json-schema silently degrades to prose when the named agent's tools frontmatter omits StructuredOutput, returning subtype success with no structured_output key.

User corrected the MSP premise mid-session: MSP is only for shipping, PRs stacked and opened, nothing released until the engine work completes. That killed the strangler options and made a clean bottom-up rebuild legitimate, and it collapsed Part III to a git rm.

Recorded 0370-0374 at decision time. Wrote and committed a 584-line SPEC to .claude/docs/specs/2026-08-12-mitosis-os-process-rearchitecture-design.md on branch docs/mitosis-os-process-spec at 05f1523, superseding the 2026-08-06 SPEC B: 18 MSPs in four clusters (A0-A4, B1-B3, C1-C7, D1-D3), six binding laws each naming its unreachable-path mechanism, and a named residual list.

Not done, and why. c3 remains OPEN: the SPEC is written but NOT user-approved, and approval is half the criterion. The c5 report render check is still unrun. Three deviations flagged to the user and unresolved: the SPEC went to .claude/docs/specs/ rather than the spine's stated docs/superpowers/specs/ (commit 693d54c merged the former); the StructuredOutput remediation was folded into A4 as a third gate verb rather than becoming a 19th MSP; and the SPEC's own section 0.2 requires a citation re-verification pass before admission, because the ~60 path:line references were derived by this session's three audits rather than personally re-opened at write time. Nothing was implemented. writing-plans was NOT invoked - it is gated on user approval of the written spec.