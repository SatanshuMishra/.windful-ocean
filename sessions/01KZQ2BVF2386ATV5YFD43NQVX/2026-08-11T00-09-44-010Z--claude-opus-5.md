Thread opened at the close of the 2026-08-10 session, succeeding the SPEC A implementation thread, which closed done at 11 of 11. No work has been done on this thread yet beyond locating, reading and diagramming the document it exists to revise.

WHAT THIS THREAD INHERITS. SPEC B, committed on main at docs/superpowers/specs/2026-08-06-mitosis-core-cost-decomposition-stacking-SPEC.md, 340 lines, status designed-not-implemented, authored and approved 2026-08-06. Its gating precondition per 0271 - SPEC A must LAND first, because SPEC B writes into .claude/lib, .claude/workflows and .claude/hooks, all live-linked, and rebuilding the engine under that linkage hot-swaps the engine and its guard hooks underneath the session doing the rebuild - is SATISFIED as of the 2026-08-10 cutover.

STRUCTURE, so it need not be re-derived. Four parts in a fixed order the SPEC argues for rather than merely prefers. Part I, the token and tool-call instrument, lands ALONE and produces a baseline; nothing may be tuned before it exists, because no token or tool-call accounting exists anywhere in the engine today - the only tokens identifier in mitosis.js is a glob tokenizer at :988-997. Part II, cost reduction as five falsifiable hypotheses H1-H5, each with claim, mechanism, measurement, falsifier and disposition-if-falsified, all governed by one universal falsifier: total input plus output tokens across the affected phase must fall against baseline, or the hypothesis is refuted regardless of what happened to dispatch count or wall-clock. Part III, decomposition of the 5,515-line monolith by codegen, opening with a byte-identity proof - regenerate from current inline content and match the committed mitosis.js before any module moves to a canonical source. Part IV, stacked PRs replacing the frontier-train, whose acceptance signal is DELETION of three compensation mechanisms (divergence detection, the supersede path, the unmergeable-parent re-park cascade) rather than coexistence with them.

TWO KNOWN DEFECTS, found by re-derivation against the working tree on 2026-08-10, and the starting point for c1. First, the *_SCHEMA count does not reproduce: section 4.3 claims 26 scattered declarations, a strict grep finds 25. Both span endpoints (:977 STATUS_SCHEMA and :5425 QUIESCENT_EXIT_SCHEMA) and the scattering re-verify, so the load-bearing point stands and the count is a candidate seventh entry for the SPEC's own section 0.6 list of citations that did not survive re-derivation. It was deliberately NOT silently corrected, since the SPEC's counting method may differ. Second, several load-bearing figures are reproducible from nothing committed: the section 0.4 prompt-byte table (26,354 / 14,826 / 4,183 / 3,704 / 3,365 / 2,558 / 1,648 / 346), the ~24 pre-code dispatches on a 6-MSP run, the 37-census-row denominator, and the 4,717-to-5,515 growth across 30 commits. Also unverified: deleteBranchOnMerge false, which needs a GitHub API call.

WHAT DID RE-DERIVE, so it need not be re-checked: mitosis.js 5,515 lines, divergence.mjs 75, gh-merge-shim.mjs 357, normalizeMergePolicy returning MERGE_POLICY_HUMAN_GATED from a one-line body, the four opus pins, agent( 31, parallel( 1, pipeline( and workflow( 0, guard.dispatch( 8, Date and Math.random zero occurrences in mitosis.js, phase: 'Ship' 15, HOOK_NAMES omitting budget at workflow-sandbox.mjs:36, the three loop bounds, MITOSIS_GIT_VERBS at pr.mjs:30 confirming no retarget verb exists, the gate's pr edit conjunction at block-destructive-bash.sh:134, and gh 2.97.0 with no extensions installed.

ARTIFACTS. The visual explanation at ~/.agent/diagrams/spec-b-mitosis-core-cost-decomposition-stacking.html carries 11 sections and 8 Mermaid diagrams, paired before/after, under one convention: solid borders mean measured-today, dashed mean proposed-and-never-built. It is outside the repo and survives the session. A copy of the SPEC is at the session scratchpad as SPEC-B.md; the canonical copy is on main and is absent from feat/invariant-inert-registry only because that branch diverged at 0451220, which predates the spec landing - nothing was deleted, so read it with git show rather than switching branches.