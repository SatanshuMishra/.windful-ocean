Architecture review conducted entirely in chat. NO files changed, no commits, no PRs. The recorded next step — serving the report over HTTP and render-verifying the ten diagrams — was NOT run; the session went elsewhere on user direction and c5 remains untouched.

WHAT WAS DONE. Read section 14 of the report (mitosis-rearchitecture-main-thread-2026-08-11.html:1244-1372) and the engine, and explained on request: how the engine works today, then how the proposed toolbox would work, then how enforcement and failure handling would actually be implemented.

VERIFIED ENGINE FACTS (re-derived this session, all against .claude/workflows/mitosis.js). 25 named *_SCHEMA constants. Thirteen phases in meta.phases:4-18, and phase() is a progress-display grouping call that gates nothing — order today is plain JS statement sequence. Scheduler is runScheduleTick:2552-2574, a dispatch-tick loop exiting on quiescence. runUnit:4691 is the per-MSP pipeline. Every side effect is a dispatched agent that self-reports and is re-validated in JS (persistCiAttemptCheckpoint:4517-4539). Two-layer trust: schema forces the shape, JS checks semantics a schema cannot express (tasks/waves set equality at 4916-4938). In-memory closures at 4403-4410 are the live state; the journal is explicitly "a hint, not the skip authority" (4510).

TWO FRAMINGS I GOT WRONG, BOTH CORRECTED UNDER USER CHALLENGE. (1) I presented precondition refusal as the routing mechanism. It is not; that would be a search loop costing a model turn per wrong hop. Refusal is the floor. The router must be a computed next field returned by every verb, and the router and the gate must read ONE predicate table — separate tables drift and produce the worst case, router says go and gate says no. (2) I framed caller-supplied state as a trust problem. Wrong frame: the threat is staleness, context compaction and reconstruction, none of which are dishonesty, and "re-verify per verb" collapses into re-running verification everywhere. The correct mechanism is a version token / compare-and-swap — check one token, then trust the whole payload wholesale. The idiom is already native to the engine (--force-with-lease at 5337; content-keyed ref re-derived character-for-character at 3972, mismatch treated as a failed probe rather than an answer).

FINDINGS NOT IN THE REPORT. (a) Rehydration tax: the sandbox cannot read a file, so every fresh invocation pays an agent dispatch just to load state; reconcile's prompt (3961-3982) is several kilobytes across seven reads. The report prices this in neither column. Chaining verbs amortizes it, which reframes run-to-next-gate as a cost mechanism rather than a convenience against tedium. (b) Subagents are NOT sandboxed — they hold full Bash/Read/Edit; only the orchestration JS is capability-stripped (HOOK_NAMES is seven hooks, workflow-sandbox.mjs:38). The report's wording led the user to the opposite conclusion. (c) The seven hooks belong to the Workflow tool, so widening them with a narrow readRef() — the textbook sandbox extension that would dissolve both the tax and the trust question — is not a lever this project holds. (d) Artifact lifecycle: worktrees ARE cleaned (1485, 1518, 2794); refs/mitosis/* are never deleted by anything the greps found; refs/mitosis-manifest/ is explicitly write-once-forward-only (4301) and content-keyed, so every spec edit mints another permanent remote ref; .mitosis/run.json has no rotation or compaction anywhere and is OVERWRITTEN at genesis (4268), destroying prior-run history rather than archiving it.

FANOUT COMPARISON (github.com/shaheershoaib/fanout, fetched this session). A single-file Python CLI run as a real OS process. No LLM in the control loop at all: it computes clusters, waves and ready_after deterministically, then dispatches subprocesses via --exec. Being a real process deletes every problem above — no rehydration tax, no dispatch-to-write, no trust question, no routing question. Failure marks dependents blocked and exits non-zero. Two specifics worth arguing with: it claims a shared working tree is safe when a wave is file-disjoint by construction, against mitosis forcing isolation:'worktree' unconditionally (4906); and its trajectory store is strictly additive, "history only ever raises caution, never relaxes it", which is exactly the monotonicity the overwritten journal lacks.

THE OBSERVATION THAT DROVE THE PIVOT. Mitosis already contains fanout. derive-edges.mjs, generate-run-script.mjs (validateGraph to waves), derive-clusters.mjs and route-planner.mjs are deterministic Node CLIs computing what fanout computes — and the Parallelize stage reaches them SIDEWAYS, by dispatching an agent to shell out to them and report back through a schema (4897-4901). The difference between the designs is not capability; it is where the outer loop lives. User then directed the next session to shift focus to the OS-process approach.