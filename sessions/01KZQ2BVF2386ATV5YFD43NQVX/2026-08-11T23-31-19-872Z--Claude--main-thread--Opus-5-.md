Session opened on preflight for this thread and ended with the thread's architecture materially narrowed by user decision.

WHAT FAILED FIRST. The T3-EXPIRY-RESUME probe never fired. Its detached driver (PID 99761) died at 23:01:07Z, 377 seconds before its scheduled 23:07:24Z firing; the process is gone and q11-results.jsonl still holds only the T1 and T2 rows. Nothing is waiting to land. The cache-expiry number it would have produced was never measured, and is now out of scope anyway.

WHAT WAS ESTABLISHED FROM THE ROWS THAT DID EXIST. T1-CREATE cost 0.0783058 USD, T2-CONTROL-RESUME 0.0074818 USD for the same ~70,368-token context, a 10.5x gap. Both turns read the prefix warm (cache_read 70,368, cache_creation 0 and 110), so neither is a cold-creation measurement. The gap is the auto-title tax recorded in 0351, not a cache effect: strip it and T1's own turn costs what T2's resume costs. The user then judged further probing not worth the time and directed conclusions be drawn from research instead of measurement.

THE SCOPE CUT (0352). The user stopped all agents and cut the supervisor process and the dispatched orchestrator agent from scope, deferred until after the mitosis engine is updated. The main thread is the orchestrator: responses, clarifications and permission requests surface to the user there, and mitosis JS remains the dispatch and fan-out engine. Analysis found the cut dissolves B1 (a workflow cannot ask a human), defers B2 with the rotation it measured, and relaxes but does not verify B3 (subagent nesting depth).

WHAT SHIPPED. mitosis-rearchitecture-2026-08-11.html was COPIED, not edited, to mitosis-rearchitecture-main-thread-2026-08-11.html and rewritten to the narrower scope: new hero and section 01, redrawn topology showing the removed layer as a dashed deferred band, rewritten run sequence, escalation boundary, fan-out re-pointed at the main thread, a new section 07 marker enumerating what was cut (per 0343), updated decision table and cost/security. Section 03 phase anatomy carried VERBATIM as an engine fact unaffected by the cut. Five mermaid diagrams, down from seven.

WHAT WAS NOT VERIFIED. The page was never rendered. The preview pane static-snapshots file:// URLs so its JavaScript never runs, and a local http server was denied at the bash gate. Three constructs written into the new diagrams and not present in the rendering original were rewritten defensively to proven forms without ever being seen: a bidirectional flowchart arrow, parentheses in a sequence participant alias, and an edge terminating on a subgraph id. The file was opened in the default browser at the end of the session but its diagrams were not inspected.

ALSO NOT DONE. The SPEC B citation census was dispatched and then stopped by the user along with everything else; it produced no artifacts for the second session running. The *_SCHEMA count, the 48-versus-50 phase-literal split, the four mitosis.js line citations, the supervisor.mjs escalate-to-parked mapping and the prompt-byte figures all remain unreconciled, and the new page's footer says so explicitly rather than citing them.

OPEN TENSION FOR THE NEXT SESSION. Criteria c2 and c5 both reference work that 0352 deferred (the rotation and cost research passes). Neither was rewritten or struck here, deliberately: the user has said the next session clarifies requirements for the narrower scope, and re-cutting the criteria is that conversation, not a call to make unilaterally beforehand.