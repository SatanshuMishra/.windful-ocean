RE-AUDIT of the eight-phase collapse design against mitosis.js at 5,515 lines (designed against 4,851). VERDICT: the design SURVIVES, with four amendments and two corrections. It is not invalidated.

RE-DERIVED phase() call sites, today: Waves :1448, Integrate :1458, Boundary :1533, Remediate :3603, Reconcile :3955, Decompose :4167, Prepare :4352, Plan :4821, Plan review :4845, Parallelize :4890, Branch :4976, Ship :5319. All twelve exist; the declaration block at :5-17 is still contiguous.

PARTIALLY LANDED ALREADY - two of the fourteen fates shipped in commit 12053dc. 'Final review' is GONE from the declaration block entirely (not merely unentered: the string is absent from the file) and slot :15 now reads 'Resume', the honest name the design assigned to the undeclared Shepherd. That commit also added the mitosis-gate.mjs phase-parity check, which closes declared/called/assigned in both directions and makes an undeclared phase structurally unreachable going forward. NOTE the new wrinkle: 'Resume' is declared but has ZERO phase() call site - it exists only as a dispatch-options tag at :3314 inside divergedParents(), reached from Reconcile at :4102. Resume has inherited exactly the declared-but-never-entered shape that Final review was deleted for; parity passes only because the phase: literal satisfies it.

CORRECTION 1, material. The design justifies deleting Plan review with "auto-approves by construction at :889-899". FALSE as cited - :889-899 today is deriveClusters topology code, unrelated, not a line-drift near-miss. The real construct is resolvePlanReview at :1191, specifically `reReviewed ? 'approve' : 're-review'` at :1200. And it is NOT unconditional: a first ambiguous verdict yields 're-review' and triggers a genuine second adversarial dispatch (:4845-4879); auto-approve fires only as the bounded second-pass fallback. The deletion rationale is weaker than the design states and must be restated honestly.

CORRECTION 2. "Boundary's prompt at :1206-1228 is already fully mechanical" - the CLAIM holds, the cite is stale. Today it is firstPassGate/recheckGate at roughly :1503-1531, a deterministic multiset count-and-compare ("an identity BLOCKS iff its HEAD count EXCEEDS its BASE count"). Genuinely a lint verb, so folding Boundary into Integrate as gate-lint stands - but it now spans two functions, not a 23-line block.

FOUR AMENDMENTS to the eight-phase text:
1. Name the CI-to-green loop explicitly under Ship/Remediate. It is the single largest addition since the baseline (commit 4fd03c2, net +481 lines): six-class failure classifier, ciProbeAttempt/ciFixAttempt at :5143-5343, dispatch labels ci-probe/ci-fix/ci-diff/ci-publish/ci-publish-verify all carrying phase 'Ship', and two new park kinds CI_RED_EXHAUSTED_KIND/CI_HUMAN_GATE_KIND at :3663/:3665.
2. Drop or replace "survives only as a needsPlan escalation" - NO needsPlan identifier or construct exists anywhere in mitosis.js (zero grep hits). The nearest analog is the plan-artifact-missing park at :4817, which is a resume-time NeedsHuman, not a plan-time escalation.
3. Soften "Parallelize's edges become a verb". True only of the edge-derivation sub-step (derive-edges.mjs). The dispatch also does LLM semantic discovery via LSP/Graphify (:4896), wave computation via validateGraph (:4900), lane routing via planRoute (:4901), prompt resolution (:4902) and 14-key engineArgs assembly (:4906) - none of which a verb replaces. Also record that derive-clusters.mjs is DEAD from the engine's perspective: nothing in mitosis.js imports it, only its own tests reference it.
4. Clarify that "Prep" is a design-level label with no code-level counterpart. Nothing in the engine is named Prep; it spans the declared 'Prepare' site (prepare-probe :4367) plus only the frontier slice of 'Branch' (frontier-compose :4988-5000), while the plain root branch-prep at :5027 sits in the same declared 'Branch' phase. Separately, frontier WIDTH machinery SHRANK rather than grew: commit 6a83b1d deleted the self-tuning AIMD window wholesale and replaced it with fixed BUILD_AHEAD_CAP = 8 at :2582.

COVERAGE: no gap. Every phase: literal in HEAD resolves to one of the 13 declared titles; every new dispatch attaches to an already-declared title (ci-* to Ship, manifest-publish/checkpoint-init to Reconcile, which the design folds into Probe). One un-narrated asymmetry: the quiescent-exit write at :5507-5508 fires outside any dispatch and carries no phase literal at all - it is the counterpart to "Resume entered only on relaunch" that the eight-phase text never mentions.

UNRESOLVED: the two audits disagree on the phase: literal count, 48 versus 50. Re-count before the figure is published.