Session opened as a question about the repo's licensing state and ended with PR #62 green and awaiting a human merge.

FINDINGS. The repo was PUBLIC with NO license file: `gh api repos/...` reported `license: null`, and `git ls-files` matched exactly one license-ish path, `.config/nvim/LICENSE.md`. The only license signal was one README sentence ("MIT License - feel free to use my configurations for your own setup!", added 2025-07-28 in 1f84917). That sentence was defective as a grant: no copyright holder or year anywhere in the repo, so MIT's sole condition ("include the above copyright notice") was unsatisfiable. Practical state was all-rights-reserved with an ambiguous overlay. Also established: 369 of 449 tracked files (82%) are `.claude/`, so the README's "my configurations" wording arguably did not even reach the engine code. Sole author across 2023-12-17..2026-08-09 (two email identities, same person), so relicensing had zero CLA friction. `.config/nvim/` descends from kickstart.nvim; its LICENSE.md was verified byte-shape-identical to upstream, which itself carries NO copyright line.

RESEARCH (done inline via WebFetch/WebSearch; the Agent tool was barred by session policy until the user explicitly requested subagents). OSD clause 6 forbids field-of-endeavor restriction, so BOTH the noncommercial and the anti-AI goals disqualify a license from being OSI open source. US case law is adverse: Bartz v. Anthropic (Jun 2025) held training on lawfully-acquired works fair use ("spectacularly transformative"), pirated acquisition not (~$1.5B settlement); Kadrey v. Meta (Jun 2025) went to Meta only on failure to prove market harm. If training is fair use, a license never binds the trainer. The one lever with teeth is EU DSM Art. 4(3) rights reservation, enforced via AI Act Art. 53(1)(c) - but a Hamburg ruling (10 Dec 2025) held natural-language opt-outs INSUFFICIENT; the reservation must be machine-readable.

SHIPPED. Branch chore/licensing-polyform-noncommercial off origin/main, three atomic commits, seven files: LICENSE (verbatim PolyForm Noncommercial 1.0.0 + three propagating `Required Notice:` lines), NOTICE, robots.txt (28 crawler blocks), ai.txt, .well-known/tdmrep.json, README License section rewritten, package.json `license` key. Four subagents used (3 parallel implementers on disjoint file sets, 1 for the coverage receipt).

WHAT WENT WRONG, and the corrections.
(1) I asserted CI would demand an invariant coverage receipt. WRONG - I had read `.github/workflows/test.yml` and `docs/invariants/registry.json` while sitting on the feat/invariant-inert-registry branch. On origin/main neither the checker nor the registry exists and test.yml has a single `test` job. A subagent had already authored a full 14-row receipt before this was caught; the file was deleted rather than shipped as an orphan pointing at an absent registry. LESSON: re-verify branch-local facts after switching branches; a read is only true of the tree it was taken from.
(2) The three parallel implementers each reported the other two's files as "unexpected modifications by some hook". Benign - they were correctly scoped and correctly refused to touch out-of-scope paths. When fanning out on a shared worktree, tell each agent which sibling paths will appear.
(3) A subagent's prompt carried a stale `scripts.test` expected value (taken from the feat branch). The agent correctly refused to force the live value to match and reported the discrepancy instead. Same root cause as (1).

CI. secret-scan FAILED on the pull_request run (31465362753) while PASSING on the push run (31465330001) for the identical commit. Not a leak: gitleaks-action calls api.github.com to classify individual-vs-org, hit a self-signed-certificate error, failed closed and demanded a GITLEAKS_LICENSE secret. A bare re-run went green with no code change. Filed as a detour criterion - a security gate that fails randomly trains people to ignore it. Final: all 7 checks pass (test 2188/2188, secret-scan, sast, sca, receipts, pr-title-lint, label).

NOT DONE. Merge (denied to agents at the bash gate by design - human action). Two background task chips were spawned: the gitleaks determinism fix, and a stale-README fix (the documented clone URL `github.com/satanshumishra/windful-ocean.git` 404s; the real remote is `SatanshuMishra/.windful-ocean` with a leading dot, and the README omits `.claude/` entirely).