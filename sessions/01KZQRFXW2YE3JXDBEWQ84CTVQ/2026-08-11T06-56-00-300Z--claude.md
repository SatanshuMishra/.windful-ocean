c8 diagnosed, read-only, no fix applied. ROOT CAUSE: gitleaks-action pinned at ff98106e (v2.3.9) sets `var shouldValidate = true` at src/index.js:73 and clears it ONLY inside the success branch of `GET api.github.com/users/<owner>` (line 93); the .catch at 102-106 merely warns and the .finally hits `process.exit(1)` at 113, BEFORE `start()` at 116. So any failure of that one classification call hard-exits the job with "missing gitleaks license" and the scanner never runs. Attempt 1 failed on a TLS chain error ("self-signed certificate") reaching api.github.com.

PREMISE CORRECTED: the pair recorded in the earlier risk was wrong. 31465330001 was a push event on the branch head; 31465362753 was pull_request on merge ref 152608a. The real apples-to-apples control is attempt 1 (failure) vs attempt 2 (success) OF RUN 31465362753 — same event, same merge ref, same workflow bytes, same action SHA, 2 minutes apart. Workflow content verified identical (md5 4b7cd0675a792c075d09658696c6ba15) across 152608a, 4a6bd77, c7ecacb and local HEAD.

REFUTED: real secret (binary never ran on att.1; both passes scanned 1cde39c^..4a6bd77 clean), rate limit / token auth (TLS-layer, not an HTTP status — though a 429 WOULD produce the identical failure, so the fragility is real), unpinned action, differing event types, and "upgrade the action" (v3.0.0 e0c47f4 and master bcfb9cc carry byte-identical gate logic at the same line numbers). Frequency: 1 in the last 100 security.yml runs.

INCIDENTAL: real github-app-token at .config/github-copilot/apps.json — untracked and gitignored, so the gitignore entry is load-bearing. Tracked hit at .claude/lib/superpowers-parallel/tests/reconcile.test.mjs:78 is a false positive (synthetic hex standing in for git OIDs). Also noted: the CI scan is diff-scoped, not a full-history secret gate.

UNDETERMINED: why TLS interception hit that specific runner (westus failed, centralus/northcentralus passed; n=1, not causal). No full-history scan run; local gitleaks 8.30.1 != CI 8.24.3.