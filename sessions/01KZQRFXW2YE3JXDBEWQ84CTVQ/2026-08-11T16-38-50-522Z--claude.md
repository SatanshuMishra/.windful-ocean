SESSION CLOSE. PR #63 merged by the user; verified against the remote rather than taken on report, per this thread's own standing risk. gh reports MERGED at 2026-08-11T16:36:27Z, merge commit b49de71 on base main. On origin/main the string "gitleaks-action" now appears 0 times in .github/workflows/security.yml and "gitleaks git -v --redact" appears 3 times, so the wrapper is genuinely gone and every scan arm redacts.

Both CI paths are now proven on real runners, not just locally: the pull_request run took the resolved-baseline arm with BASELINE_REF=c7ecacb (a non-empty range, so the green is non-vacuous), and the post-merge push run on main at b49de71 also passed, exercising the github.event.before arm, which is a different code path. Honest limit: I confirmed the range and exit code, not gitleaks' "N commits scanned" summary line, which goes to stderr and was outside my log filter.

All nine criteria are now complete. NOT closed as done, deliberately: the active goal has two halves and only one shipped. The license and the machine-readable reservation are in effect on main; the mirror to an owned domain, which is the step that actually activates the EU Art. 4(3) mechanism, was never a completion criterion and remains untouched. Closing on a satisfied criteria list alone would declare a half-met goal achieved, so the disposition is left to the user: either close this thread and open a successor scoped to the mirror, or reopen it with a mirror criterion. Flagging rather than deciding.

Housekeeping left for the user, both requiring confirmation since they are destructive: chore/licensing-polyform-noncommercial and ci/gitleaks-direct-binary are both merged and still exist locally and on the remote. Also note the thread's vcs_ref still names the first of those, now stale.

Filed out of scope and still open: task_9ce31094, the osv-scanner download in the sca job that has no checksum at all and is chmod +x'd, which is the same class of hole PR #63 just closed for gitleaks.