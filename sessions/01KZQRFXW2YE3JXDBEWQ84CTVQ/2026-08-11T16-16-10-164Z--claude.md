c9 implemented and shipped to review as PR #63 (https://github.com/SatanshuMishra/.windful-ocean/pull/63), branch ci/gitleaks-direct-binary off c7ecacb, commit 31c983b. NOT merged and CI had not been read at the time of writing.

Licensing prerequisite cleared before implementing (decision 0335): core gitleaks is MIT at v8.30.1 and never carried a commercial or org/individual restriction (GPLv3 until 2018, no LICENSE for ~17 months, MIT since 2019-12-04); the proprietary EULA belongs to the Action wrapper and scopes itself by its own text to "GITLEAKS-ACTION", which "runs as a GitHub Action". Inference from two individually unambiguous documents, not a clause addressing this substitution; moderate-high confidence.

Implementation reuses the sast job's baseline-resolution step verbatim, pins v8.30.1, verifies SHA-256 551f6fc8... before extraction (confirmed three independent ways: gh asset digest, published checksums.txt, sha256sum of the tarball), and drops GITHUB_TOKEN from the job.

Security review returned APPROVE-WITH-FIXES; both ship-blockers were applied before commit, and both were empirically demonstrated rather than asserted. HIGH: `-v` without `--redact` printed a full plaintext secret into the job log, which on this public repo is world-readable, retained ~90 days, and survives the history rewrite done to remediate — worse than under the wrapper, since the direct binary emits no report artifact and the log is now the only output. MEDIUM: the first-draft `--log-opts="-1"` fallback exited 0 on a real leak two commits back (job GREEN on a secret), and it fires on the force-push-rewrite path too, not only new branches. Replaced with a three-arm fallback against refs/remotes/origin/<default>, verified available under fetch-depth 0. Two rejected alternatives the reviewer tested: `--not --remotes=origin` scans 0 commits green on a new-branch push, and `origin/<default>..HEAD` is empty when HEAD is the default tip.

Local verification actually run: python3 yaml.safe_load valid with three jobs and permissions still contents:read; actionlint exit 0; diff confined to one file, 43 insertions 2 deletions, sast and sca byte-identical; no continue-on-error or `|| true` anywhere; all three gitleaks arms carry --redact. NOT verified: runner behavior (only a real CI run establishes it) and whether secret-scan is a required status check (no ruleset file in .github/, repo settings not inspected).

Filed separately, deliberately out of this diff: the sca job downloads osv-scanner v2.4.0 with no checksum and chmod +x's it, giving code execution to anyone who can serve or MITM that URL. Pre-existing on main, spawned as task_9ce31094.