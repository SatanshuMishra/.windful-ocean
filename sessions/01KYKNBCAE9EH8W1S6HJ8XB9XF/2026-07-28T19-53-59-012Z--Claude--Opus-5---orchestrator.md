Session 2026-07-28. Approved scope: implement the spec as M1->M2->M6->M3->M5->M7 via the mitosis skill. THE RUN WAS NOT DISPATCHED. No code modified, no commits, no pushes. Four blockers were found in preflight, each of which would have corrupted the run.

BLOCKER 1 - the branch was spent. feat/centralized-pr-creation was squash-merged as PR #8 at 2026-07-28T04:43:01Z. Working from it would have replayed an already-resolved diff (the exact §3.7 hazard the spec describes). USER RULING: base = origin/main.

BLOCKER 2 - every spec line cite is stale. The spec is grounded at 450804e (engine 4847 lines); origin/main's engine is 4920 lines, moved by 457d6fa (merge-boundary preflight, PR #5). 7 of ~60 cites were verified wrong by direct read: :1909 isBuildable -> 'switch (supervised.verb) {'; :2021 maxSteps -> '}'; :2131 STREAMING_DISPATCH_ENABLED -> 'const next = new Map();'; :3394 shippedCount===0 -> an unrelated ternary; :4703-4705 MERGE_POLL constants -> rb.compare/rb.readError; :2141 WINDOW_FLOOR -> a for loop. This matters most for M3/M5, which are DELETION MSPs under the mirror-guard byte-identity constraint: wrong cites means deleting the wrong block twice. A codebase-analyst was dispatched to produce the full mapping; it drifted off-task (began creating worktrees and running suites) and was stopped without returning a result. THE NEXT SESSION MUST RE-RUN THE MAPPING.

BLOCKER 3 - guardrail hole. .claude/hooks/protect-claude-config.sh computes bases = [~/.claude, realpath-of-symlinked <repo>/.claude] and matches by exact path prefix on {hooks,rules,lib,workflows}. A git worktree anywhere else matches NEITHER base, so mitosis worktrees would edit mitosis.js and lib/ entirely unheld. Verified by direct probe of the hook. USER RULING: close the hole first, then grant an explicit declared carve-out for the run's worktree root. Branch fix/guard-any-claude-root created at beca874; NO commits on it. Note corrected mid-session: ~/.claude/hooks/* ARE symlinks into the repo's tracked copies (an earlier inode read misled me), so there is ONE file to edit and the edit goes live immediately.

BLOCKER 4 (NEW, not in the spec) - the suite is RED and NON-HERMETIC. Measured origin/main @ beca874: 1540 tests, 1520 pass, 20 FAIL. Spec §11's "1415 pass / 0 fail" is stale on both figures and must be corrected during re-grounding. Failures: 2x protect-claude-config + 18x BOUNDARY PREFLIGHT. Non-hermeticity is PROVEN, not inferred: protect-claude-config.test.mjs passes 9/9 in the canonical working tree and fails 2/9 in a worktree at the SAME commit, because the test derives REPO_CLAUDE from its own file location while the guard matches fixed bases. The first preflight failure shows the engine emitting the INSTALLED /Users/.../.claude/lib/superpowers-parallel/fold-run-log.mjs path rather than the tree under test. CONSEQUENCE: mitosis runs MSPs in worktrees, so a run would verify against a signal that is red for reasons unrelated to its own diff - "each MSP leaves the branch green" is currently unverifiable. This is a Pillar-1 problem sitting underneath all of M1-M7. Evidence: scratchpad/main-baseline/baseline.tap.

ALSO OBSERVED: origin/main advanced during the session from 7e2e7d7 to beca874 (PR #9 merged). It does not touch the engine, so the cite-mapping target is unaffected.

REVISED ORDER: hermeticity -> guard fix -> spec re-ground + COMMIT -> mitosis dispatch. (Was: guard -> spec -> dispatch.)

The spec file is still UNTRACKED. It MUST be committed before any worktree-based run or every implementer agent is blind to the authority document.

SIDE EFFECTS LEFT ON DISK: branch ref fix/guard-any-claude-root @ beca874 (no commits); a scratchpad worktree at scratchpad/main-baseline holding baseline.tap; possibly a stray worktree created by the stopped analyst - run `git worktree list` and prune. The user's deliberate uncommitted changes (.drift-state.json, tests/no-self-merge-consent.test.mjs +73 lines) were left untouched as instructed.