Session 2026-07-28 (second). Executed decision 0065: three Fable subagents dispatched IN PARALLEL - code-reviewer (audit), researcher (industry research), solution-architect (design). All three completed. No code changed, nothing committed or pushed, nothing left running.

REPORT PERSISTENCE CAVEAT: the three reports exist ONLY in this session's transcript. They were deliberately NOT written to .claude/reports/ because that directory is git-TRACKED and carries an unresolved leak caveat on the mitosis-resilience-implementation thread. All load-bearing content is therefore distilled below; a future session cannot recover the originals.

TEST SUITE (first run for this thread): `npm test` from repo root -> 1415 pass / 0 fail / 0 skip, 6.4s, exit 0. The dirty working tree (.drift-state.json, tests/no-self-merge-consent.test.mjs) is NOT implicated; the modified test passes as modified.

LOAD-BEARING CORRECTION VERIFIED: optimistic building already exists and is default-on. isBuildable (:1905-1915) admits parents in built|awaiting|done (check at :1909); frontier compose (:4491-4529) stacks children on unmerged parent checkpoint tips, moving fresh onto origin/<base> at :4507 and recording builtAgainst at :4525-4529. NO line drift - every cite from the 2026-07-27 review matches the current file exactly.

SEVEN-FINDING ADJUDICATION: (1) CONFIRMED but a DESIGN COMMITMENT, not a bug - test-pinned in 4 places (frontier-train-e2e.test.mjs:301,621; mitosis-scheduler.test.mjs:668). (2) CONFIRMED, with a nuance the review missed: the window PERSISTS across relaunch (:3975, :4049-4050), so "cold run builds 3 then idles" holds only for a genuinely cold run. (3) CONFIRMED, but the stop is a clean awaiting-approval report, not a failure (:3391); test-pinned as intended (leases.test.mjs:375). (4) PARTIAL - the arithmetic at :2021 is real, the mislabel-as-failed consequence is REFUTED: state 'awaiting-merge' (:2013) has ZERO consumers in mitosis.js. (5) see below. (6) CONFIRMED real defect, impact far narrower than claimed; ZERO test coverage - no test references progressPossible at all. (7) PARTIAL - mechanism true, but "full decompose" is wrong (decompose is skipped with a reusable manifest, :3794-3797) and the path still opens deferred PRs, so the cost is fan-out machinery, not lost function.

FINDING 5 - THE THREE-WAY ADJUDICATION (most important artifact of this session; settled by main-thread direct read, not by any agent). The original finding reached the RIGHT CONCLUSION by the WRONG REASONING. The audit correctly demolished the reasoning - the git-ref rescue at :3702-3714 runs on every relaunch before the resume selectors, reads live refs from `git ls-remote origin refs/mitosis/*`, and promotes cascade-parked units back to built, firing on exactly the trigger the finding described - then OVER-GENERALIZED to refute the conclusion. The design agent found the actual live vector. Verified directly by reading :473-479, :2230-2237, :3702-3714, :3706:
  step 1: unit parks at stage 'plan' (divergence-condemned) -> resumePoint.stage='plan'
  step 2: unit is later rebuilt -> applyBuiltTransition (:473-479) returns {...msp, status:'built', ...}; the spread PRESERVES the stale resumePoint. Nothing clears it.
  step 3: an ancestor parks -> cascade (:2230-2237) sets status back to 'parked' and PRESERVES {...msp.resumePoint}; it substitutes {branch:null,ref:null,stage:null} ONLY when the field is absent.
  step 4: relaunch -> guard at :3706 (status==='parked' && resumePoint.stage==='plan') returns early -> git-ref rescue SKIPPED -> full re-plan and re-execute despite a live checkpoint ref.
ROOT CAUSE is one line: a state transition that fails to invalidate a field meaningful only in the prior state. This is a standalone bug fix, independent of any redesign. NOT VERIFIED: how often divergence-invalidation fires (:3757/:3773), so frequency is unknown.

THE REFRAME (changes what the whole effort is for): finding 1 was misattributed as the cause of the halt from the outset. Between human events there is NO agent work at a review boundary, and build-ahead already runs during review. The halt is findings 2/3/4 - window floor, the 6-cycle bounded poll, and failure-shaped reporting of review latency. Changing PR-open policy buys parallel human review; it does not fix the stop.

RESEARCH HEADLINES (all URLs verified by the researcher): production stacked-PR tools target the child PR at the PARENT's branch, not base and not deferred - Git Town (git-town.com/stacked-changes), Graphite, Aviator; parent-merge is detected via the FORGE API because squash-merge makes git-level detection impossible; then retarget + rebase --onto. Sapling is the only tool doing superset-diff child PRs and its own docs concede it "will not look right on GitHub". GitHub auto-retarget MISFIRES and CLOSES child PRs when the parent branch is deleted via git CLI instead of the API (github.com/orgs/community/discussions/131045). NO production system fix-forwards a published branch autonomously: rerun-once (Azure DevOps, Google), quarantine+ticket (Meta PFS), revert-first (Chromium sheriffs), eject-to-author with zero retries (GitHub merge queue, bors-ng, Mergify). Repairnator: 4-5 human-accepted patches across ~30,000 failing builds. Overfitting literature (Smith et al. FSE 2015) - patches satisfying a failing test routinely break unasserted behavior, making green CI a WEAK ORACLE. Meta's SapFix is hard-gated on human review. NO production merge queue documents adaptive window sizing; all use fixed caps. Escalation ladder is serial -> batching -> fixed parallel speculation -> ML speculation, each rung bought only when the prior ceiling is hit (Uber SubmitQueue, EuroSys 2019, at thousands of commits/day); this system is on rung one.

NEW DEFECTS the seven findings missed: (a) MEDIUM - in-run AIMD re-counts a persistent APPROVED on EVERY poll cycle with no dedup (:4789-4794), inflating the window 3->8 from a single approval; the shepherd path dedups exactly this (frontier-train-e2e.test.mjs:383) so the control law is incoherent with itself; untested. (b) MEDIUM - computeMergePolicyStatus:3394 returns 'failed' whenever shippedCount===0, so a first run with an open PR awaiting approval plus one unrelated park reports failed despite durable built work. (c) LOW - blockedByApproval (:4694) is never pruned on in-run merge (:4802). (d) INFO - 'awaiting-merge' (:2013) is a write-only state.

DESIGN PROPOSAL SHAPE (not yet a spec): "quiescent-advance engine" - derive unit state from durable facts (forge PR state + content-hash-named git checkpoint refs) rather than the journal fold; advance every actionable unit; exit honestly at quiescence; move the non-stop property out of the engine into an orchestrator-level background event watch. Mechanism ledger 18 -> 11, net -7 INCLUDING the new capability. Three options weighed: A quiescent-advance (recommended), B resident supervisor (rejected - keeps the whole fragility inventory and adds to it), C open-early stacked PRs = the industry pattern (rejected on force-push grounds; ratified as 0066).

STRUCTURAL GOTCHA for any implementation: mitosis.js INLINES copies of the lib modules (window at :2141-2158 duplicates window.mjs; planReconcile at :2710 duplicates reconcile.mjs) with a drift checker at .claude/lib/superpowers-parallel/.drift-state.json. Every lib deletion must land TWICE or the drift check fails.

PROCESS NOTE worth repeating: when the audit landed refuting finding 5, the design agent was still running with that finding in its brief. It was deliberately NOT interrupted, to preserve the independence of its verdict. That paid off - the design independently declined to accept the brief's framing and found the vector the audit had missed. Interrupting would have destroyed the signal that made the correct adjudication possible.

WHAT WAS NOT DONE: no architecture spec written (deliberate - context reached 81%, and the spec deserves a full window); no code changed; the stale-resumePoint fix NOT applied; nothing committed or pushed; no PR touched.