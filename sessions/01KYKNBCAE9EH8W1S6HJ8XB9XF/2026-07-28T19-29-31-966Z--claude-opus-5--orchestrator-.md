Invoker exploration under 0069, then the single spec pass. Six-agent bounded fan-out (4 researcher, 2 codebase-analyst); all returned; none abandoned.

SHIPPED: docs/superpowers/specs/2026-07-28-mitosis-quiescent-advance.md rewritten in one pass. Status PROPOSED -> APPROVED (architecture). 0069 added to binding decisions. New goal (iii) resume-from-any-clone. Old 3.5 (watcher liveness) DELETED and replaced by 3.5 Durable run identity; new 3.6 The invoker (one command, three callers); new 3.7 Restack semantics after a squash-merged parent. Continuation block loses `watch`, gains `identity: published|local-only`. Section 5 gains the ordering clarification (a child PR can never pre-exist its parent's merge). Section 8 gains row 24 and is recounted 22->14, net -8. M6 rewritten from watcher to manifest ref and reordered to depend on M2. Section 11 gains fresh-clone, idempotence and latency-instrumentation coverage; section 12 gains risk 5 (identity/status drift); section 13 rewritten from five asks to ratified-plus-three-open.

RESEARCH FINDINGS THAT DROVE IT (all cited in-spec):
- Polling was never expensive: 5,000 REST req/hr, 304s free. The defect was polling INSIDE the engine, burning maxSteps. Recorded in section 1 so the diagnosis is not re-litigated.
- The advance needs almost no model judgment. runReconcileOnlyAdvance's agent() dispatches at :2984-2995 and :3023-3034 exist because the workflow script is sandboxed with no fs/shell access, NOT because judgment is required. Every pr-create field is a stored manifest field or a pure function (prTitleFor :3186-3190, prProvenanceFor :3198-3200, PR_NOT_VERIFIED_OPEN_CI :3177); only --changed-lines is computed at runtime. planReconcile is pure and mirror-enforced.
- BLOCKER FOUND, now fixed in spec: the MSP table (id, dependsOn, fileScope, changeType, scope, title, rationale) lives only in the genesis line of .mitosis/run.json (:3900-3912), never committed (engine's own prose at :4329). Verified at HEAD: nothing under .mitosis/ is tracked. So old 3.5 tier-1's "by anyone, at any later time" was FALSE from a fresh clone. That is what M6 now fixes.
- merge_commit_sha semantics VERIFIED (was an open unverified risk): on a MERGED PR it is the squashed commit's sha. Recorded in 3.1 and consumed by section 7.
- Squash-merge restack: append-only merge-of-base-into-child is correct; rebase would silently drop already-upstream commits. Phantom conflicts auto-resolve in the three-way merge. At restack time the child PR does not exist, so conflict must be detected via unmerged paths, not exit code. Forge auto-retarget only fires on head-branch deletion and never repairs content.

CORRECTION LANDED: the previous draft cited .drift-state.json as the mirror/drift checker for the inlined lib copies. That file is a SessionStart hook cache for four vendored Superpowers prompt files and never runs in CI. The actual byte-identity guard for the inlined copies is tests/mirror-guard.test.mjs. Section 10's gotcha now cites the right guard.

COUNT RECONCILED HONESTLY: the prior -9 was wrong in the design's own favour (the watcher triad never got a table row, undercounting additions by one). 0069 deletes the watcher, clearing that debt; 3.5's replacement adds exactly one row. Corrected figure -8, stated as arithmetic on the table plus an explicit note.

NOT DONE / CARRIED: completion_criteria were NOT toggled this session - the resume brief is spine-only and did not return their exact texts, and update_thread matches a criterion by exact text. Guessing the wording would have written a false done flag. Criterion 6 (approve the architecture) is satisfied in substance and should be ticked next session once the texts are read.

NO CODE TOUCHED. No commit, no push, no PR. The spec is the only file written.