Session 2026-07-27/28. Read-only architecture review of the mitosis engine, prompted by an extract from another project claiming mitosis halts whenever a blocking MSP's PR is open but unmerged. No code changed; nothing committed or pushed.

WHAT WAS READ (direct, not delegated): .claude/workflows/mitosis.js (4847 lines, the real orchestrator) — scheduler core :1852-2160, park/recovery :2189-2318, reconcile/frontier planning :2604-2742, reconcile-only shepherd :2904-3048, merge-policy :3361-3400, relaunch seeding :3740-3879, runUnit + frontier compose/ship :4227-4700, poll config :4703-4707, end-of-run classification :4824-4847. Also .claude/lib/superpowers-parallel/{merge-policy,merge-watch,wave-planner}.mjs. NOT read: the test suite (not run), .claude/skills/mitosis/SKILL.md, and no comparison against whatever vendored mitosis copy the other project actually ran.

CORRECTION TO THE PROMPTING EXTRACT (load-bearing): the extract asserts the scheduler "keys strictly on merged-status" and that there is "no knob, optimistic or otherwise" — and admits it never read the scheduler. That is wrong here. isBuildable (mitosis.js:1905) admits parents in built | awaiting | done, and the frontier-train compose stage (:4500-4529) stacks a child on its parents' UNMERGED durable checkpoint tips, recording builtAgainst. Optimistic BUILDING already exists and is default-on. What is unconditional is the PR-OPEN policy, not the build policy. Any redesign must start from that corrected premise or it will re-solve a solved problem.

SEVEN FINDINGS (ranked; all from direct reads, none verified by execution):
1. PR-open gated on every parent MERGED — :4606-4611 (parentsDone -> else return Built) and :4305-4311. Child builds, boundary-validates, pushes a checkpoint, then withholds its PR until the parent merges. At most ONE PR per dependency chain is ever open for human review. This is the direct cause of the reported behavior.
2. Build-ahead window floors at 3 — WINDOW_FLOOR :2141, refusal at :1914, AIMD grows only on approved/merged :2147, and those events are read only inside mergePoll.watch :4789 which runs only when the scheduler is already idle. Cold run builds 3 ahead, then idles. Ceiling 8.
3. Bounded poll -> hard stop — MERGE_POLL_MAX_CYCLES=6 x MERGE_POLL_WAIT_SECONDS=300 (:4703-4705); pollsUsed resets on merge (:2048), so 30 min of human INACTIVITY terminates the run. Module constants, not engine-args: no knob.
4. Step-budget exhaustion mislabels a healthy run as failed — maxSteps = units.length * (maxPollCycles + 2) + 1 (:2021); ticks AND polls both burn steps. On exhaustion the loop exits WITHOUT markAwaitingMerge, leaving units 'planned', which falls to halted.push (:4844); computeMergePolicyStatus treats haltedCount>0 as a fault (:3386) -> reports partial/failed instead of awaiting-approval.
5. PARK CASCADE DESTROYS FRONTIER-BUILT WORK (worst) — park() :2212-2241 sets status:'parked' on the unit AND every transitive dependent unconditionally, including dependents already 'built' with a pushed checkpoint. buildInitialManifest carries no resumePoint (:406-431), so the cascaded dependent gets {branch:null,ref:null,stage:null}. On the COMMON identical-spec relaunch only selectResumeBuilt (needs status==='built', :2271) and selectResumeUnits (:2251) run; the cascaded unit fails the first, passes the second with stage:null -> resumeStartIdx=-1 -> skipPlan false -> full re-plan and re-execute while its checkpoint ref still exists in git. selectPreservedBuilt (:2289), which recovers built state from git refs rather than manifest status, is wired ONLY to the !reusable && isRelaunch path (:3864) — the spec-CHANGED case, not the common one. Net: one parent ship failure discards every child built ahead of it.
6. progressPossible uses the wrong window — :2001-2005 calls planTick(hypothetical) with no window arg, falling back to WINDOW_FLOOR=3 (:1963). With AIMD grown to 8 it under-reports buildable work and can terminate while the live window would still admit builds.
7. One parked unit disables the cheap relaunch path — shouldReconcileOnly requires buildableWorkRemains===false (:2675) and hasBuildableWork counts anything not built/shipped (:2679), so any parked unit forces a full decompose/plan/fan-out instead of the reconcile-only shepherd (:3006-3044) that exists precisely to open deferred PRs after a human merge. Compounds finding 5.

ON THE SQUASH-MERGE OBJECTION: the extract's strongest argument (stacked PRs are incoherent under squash-merge) does NOT block fixing finding 1. The engine never stacks PR BASES — both the frontier compose (:4507) and the deferred open (:3028) move the integration ref FRESH onto origin/<base> and replay. The stale merge-base artifact is already engineered around. The genuinely open question is narrower: may a child PR open against base while carrying its parents' commits (a superset diff — the real MSP-invariant objection), or is deferral the only correct answer? Unresolved; it is the central design question for the redesign.

GAP NOT COVERED BY ANY FINDING: nothing in the current engine drives a red CI run back to green. ship treats non-merged/non-awaiting as halted (:4669-4671) and parks. The user's target capability (auto-remediate CI to green, then continue) has no existing surface to extend.

USER DIRECTIVE AT SESSION END: do the audit/research/redesign in a FRESH session via dedicated Fable subagents in parallel; governing constraint SIMPLE + ROBUST beats FRAGILE + COMPLEX. Recorded as decision mitosis-redesign-via-fable-fanout.

NOTHING LEFT RUNNING. No background shells, no subagents, no open PRs touched.