Item 2 RESOLVED WITH EVIDENCE; the "Build A is SAST-UNEVALUATED" risk is refuted, not merely aged out.

Sequence executed: human pushed origin main (c1d2606..cd5c65d, plain fast-forward) per 0034. Verified consequences -- PR #5 is now exactly 1 commit (merge-base(origin/main, head) = cd5c65d, rev-list --left-right --count = 0 1, MERGEABLE); GitHub's API still reports commits:36 from a stale cache, cosmetic only. Run 30303354357 on main: secret-scan/sca/test green, sast's SOLE failed step is "Fetch and verify p/default ruleset" -- confirming 0034's accepted red-main cost is upstream drift, not repo code.

Pin integrity established. CI recomputed the current canonical hash at 2026-07-27T20:37:38Z: d9f73571cb16f43a3a51b5c9c29d712a77bfe5133f684bd7d713347205a55c96, identical to the value 0033 recorded a week ago, so upstream has NOT drifted again. Human fetched the ruleset via `! curl` (Bash(curl:*) is a deny rule at .claude/settings.json:56 -- not routed around; WebFetch rejected because a mangled fetch yields a hash CI can never reproduce). Local `python3 .semgrep/canonicalize.py | shasum -a 256` on macOS/homebrew reproduced d9f73571... EXACTLY, cross-validating the canonicalizer across two toolchains. New p/default carries 1074 rules; all four suppressed rule IDs are still PRESENT (no pragma is dead by rule-deletion).

Scan evidence, semgrep 1.170.0 against a clean local clone at c59ca79 (413 tracked files, 294 scanned, matching CI's checkout rather than the dirty working tree):
- A, --disable-nosem, full tree: 25 findings, ALL 25 located at the 21 pragma lines. ZERO findings anywhere else in the repo.
- B, normal, full tree: 0 findings. Every finding is covered by an existing pragma.
- C, --baseline-commit cd5c65d (exactly what CI runs on PR #5): 0 findings, 4 paths scanned.

Therefore Build A needs NO code change for sast. merge-boundary-preflight.mjs and mitosis-git.mjs -- the gh-argv/untrusted-JSON surface 0033 flagged -- produce zero findings under the new ruleset. All 21 pragmas are LIVE (each rule still fires at its line) and all are rule-ID-scoped with zero bare pragmas, which structurally forecloses the "suppression silently covers a different rule" risk.

Caveats carried forward: (1) 9 PartialParsing warnings, all pre-existing and none on a pragma-bearing file -- .claude/docs/msp-driven-approach-report.html, six .claude/hooks/*.sh, and .claude/skills/mitosis/templates/receipts.yml x2; semgrep's coverage of those shell hooks is incomplete and always has been. (2) Four lines report the same rule twice at identical column spans (live-accept.mjs:366, live-wrap.mjs:606/607/608) -- same rule, same span, so the pragma covers it; cause unexplained, not adjudication-relevant. (3) What is VERIFIED is that each rule exists, still fires, and is ID-scoped; what is NOT verified is whether each pragma's written justification still holds against the code. That per-justification review is the human step 0033 reserves and it remains OUTSTANDING. The pin has NOT been bumped.