BUILD A IS BUILT AND GREEN, in worktree .claude/worktrees/boundary-preflight (branch fix/mitosis-boundary-preflight, base cd5c65d). NOT committed, NOT pushed, NO PR. NOT YET REVIEWED - code-reviewer and security-reviewer have not run on it.

CREATED: .claude/lib/superpowers-parallel/merge-boundary-preflight.mjs (332 lines); tests/merge-boundary-preflight.test.mjs (532 lines).
MODIFIED: mitosis-git.mjs (one word - `execGh` exported at line 262, so the preflight reuses the single tripwire-guarded spawn path); mitosis.js (`boundaryPreflight` added to RECONCILE_SCHEMA, pure `readBoundaryPreflightVerdict` above `resetPreamble`, gate seated after `targetRepoHost`, reconcile prompt step 7 + return contract); tests/mitosis-scheduler.test.mjs (PROVEN_BOUNDARY fixture + 10 engine-gate tests); tests/frontier-train-e2e.test.mjs (one-line fixture default).

EVIDENCE: worktree baseline at cd5c65d = 1216/1216. Final = 1316/1316, 0 fail (+100). RED confirmed first via ERR_MODULE_NOT_FOUND on the absent module. Gate mutation-tested: replacing `if (!boundaryVerdict.proven)` with `if (false)` produced exactly 8 fail / 160 pass (precisely the deny cases), then restored. The gate is load-bearing, not decorative.

BASELINE CORRECTION: the recorded 1229 figure is NOT reproducible from committed state. 13 of those tests come from the UNCOMMITTED working-tree edit to no-self-merge-consent.test.mjs (+77 lines) that belongs to the mitosis-preflight-hardening thread. True committed baseline at cd5c65d is 1216. Use 1216, not 1229, as this branch's reference.

*** OPEN ARCHITECTURAL CONCERN - NEEDS A DECISION, DO NOT LET THIS SCROLL AWAY ***
Because mitosis.js has zero exec surface, a gate "seated" in it can only CONSUME a verdict, never observe GitHub itself. The implementer routed the verdict through the EXISTING `reconcile` dispatch (label 'reconcile', agentType 'implementer') rather than inventing a stage - a reasonable choice that introduced no new agent label. But the consequence is that the gate's strength is bounded by the dispatched agent reporting honestly. mitosis.js validates the verdict fail-closed, yet cannot verify it independently. That is materially WEAKER than the runbook's "the GitHub server is the boundary" framing, and it is the same class of self-reported, client-side trust that decision 0009 tore down MSP-2/MSP-3 for being undecidable and fail-open. This needs an explicit ruling before Build A ships: either accept the limitation with eyes open and record why, or move the probe execution somewhere the engine can trust (the exec-side module invoked directly rather than via an agent report).

SECOND ITEM WORTH ITS OWN DECISION: if `bypass_actors` were ever returned to the engine's token, that fact ALONE would prove the token holds ruleset write access - itself a boundary violation. It sits outside the three gated invariants so it was deliberately not gated, but it is a free, high-signal tripwire.

IMPLEMENTATION NOTES: preflight is a SIBLING module, not a 4th mitosis-git.mjs verb - that file's FLAG_SPEC/verb machinery is shaped for PR mutation and 5 probes would have pushed it past 750 lines. It reuses `execGh` literally but has its OWN pure argv builder rather than extending `buildGhArgv` (deliberate deviation from the spec's literal wording, flagged by the implementer). Checks carry tri-state passed (true/false/null) plus a `required` flag; the collaborator read halts only on a positive `admin`, unreadable is null and does not gate - which is why the maintain-maps-to-write trap is harmless, since no equality check against `write` exists. An org-only ruleset HALTS (repository-owned active ruleset treated as required). `describeBypassListGap()` takes NO arguments so it structurally cannot read bypass_actors; bypassVerified is a hardcoded false with a fixed gap string. `fatalReport('preflight-boundary', reason, 0)` used with no crashed flag, asserted `result.crashed` is []; `failedRun` never reached for. Seating initially broke 178 pre-gate full-run fixtures; resolved by giving the two fake-agent factories a conformant default rather than weakening the gate.

CORRECTION TO MY OWN AMENDMENT: mirror-guard.test.mjs iterates a HARDCODED 21-entry list (line 19), so an unlisted new module is not checked at all and IS effectively exempt. The seat region is not inside any twin - `targetOwnerRepo` appears in zero twin modules. My hazard warning was wrong as written. No prompt-snapshot test broke; the four snapshots cover implementer/reviewer prompt files, not the reconcile prompt.

THE FIVE BUILD A SPEC FIXES WERE NEVER RECOVERED. The recovery agent could not enumerate sessions/ - no Glob in its toolset, Serena unbound (No active project), Bash hook-blocked against the store, and the ISO-millisecond filenames are unguessable. Build A was built instead from the runbook Section 7 plus decisions 0017/0019, which proved more specific than the spine's summary of the fixes. Whether any of the five contradict what was built is UNKNOWN and unverified. Recovering them needs a session whose agent has Glob, or direct orchestrator Reads once filenames are known.

NEXT: (1) get a ruling on the honest-reporting limitation above; (2) run code-reviewer + security-reviewer on the worktree diff; (3) then commit/PR per the per-MSP policy; (4) then Build B, whose blocker is that reconcileShippedSet (mitosis.js:334-351) drops mergedSha - thread it through or the restack driver has nothing to rebase onto.

MSP-4 REMAINS UNDEFINED in this spine. It traces to a v1 plan where it meant the target-aware ref-parsing hook (RC2), which decision 0009 most likely made moot by tearing down the MSP-2 hook layer it extended. Needs the user's definition before it can be scheduled.