Session goal was the thread's four-item next step. Items 1 and 4 SHIPPED; items 2 and 3 (the two builds) NOT STARTED, stopped deliberately on context budget rather than begun half-way.

ITEM 1 - RESEARCH, COMPLETE. Four agents ran (three researchers on independent doc directions, one codebase-analyst on the engine), plus one follow-up round to the rulesets researcher. Roughly fifteen claims verified or refuted against live docs.

THREE RUNBOOK CLAIMS WERE WRONG. (a) The "Checks" permission is NOT assignable to a fine-grained PAT - GitHub Support, March 2025, disabled it; GitHub Apps only. (b) Personal-account repositories have no five-role model at all - owner and collaborator only, and collaborators cannot even be read-only. The target repo is personal-account, so this applied directly. (c) `dismissal_restriction` does not exist on the ruleset pull_request rule; that is the classic branch-protection API.

TWO LOAD-BEARING CLAIMS CONFIRMED. Author self-approval is blocked, verbatim and not plan-dependent, so 0009's "why the server is complete" survives. And both ruleset endpoints AND both identity endpoints need only Metadata:read, NOT Administration - the deliberately-narrow token can read them, which was the design-breaking risk going in and it resolved in our favour.

ONE STRUCTURAL GAP FOUND, now decision 0017. `bypass_actors` is returned only to a caller with ruleset WRITE access; Administration read-only is confirmed insufficient. Every alternative was checked and is also gated - version history needs Administration write, rule-suites audit needs Administration read. So the empty-bypass invariant is unverifiable by the engine's identity, and an absent key carries zero information. Compounding it: self-approval blocking does NOT alone force a human approval, because an admin can merge with zero approvals unless bypassing is disallowed - two independent controls, and the token sees only one.

ONE DESIGN-BREAKING CODE FINDING, now the second decision 0017. mitosis.js has ZERO imports and ZERO child_process calls - it is a Workflow script with only agent/phase/log/parallel injected. It cannot shell out, so 0012's "BUILD in-engine, run git rebase --onto" describes a capability that does not exist. User chose to build the driver as a mitosis-git.mjs verb instead, which keeps 0012's intent and only relocates it.

ITEM 4 - COMPLETE, but not as the ledger described it. The teardown was ALREADY DONE and left nothing to commit: the deleted detector and its test were untracked (so no git-status trace) and the two block-destructive-bash mods already matched HEAD. The ledger's claim that the teardown sat uncommitted was wrong. So item 4 reduced to the runbook alone. Thirteen edits applied across nine corrections, 156 -> 199 lines, then committed as a0bb6e1 on a new branch docs/mitosis-merge-boundary-runbook (branched from main at cd5c65d; nothing committed to main, and no branch predating cd5c65d was checked out). Leak gate clean, twenty placeholder occurrences intact. A pre-commit hook ran the full suite and passed.

CORRECTION TO THE LEDGER, worth carrying forward: the no-self-merge-consent.test.mjs working-tree change is NOT unrelated as the prior spine claimed. It hardens the no-self-merge invariant, runs 5/5 green, and almost certainly belongs to the mitosis-preflight-hardening thread whose next step is isolating two fixes into atomic commits. Left untouched. The other two dirty entries (.drift-state.json version bump, the context7-mcp skill) are genuinely unrelated and also untouched.

WHAT DID NOT HAPPEN. The two builds. Both were blocked on user decisions that only landed late in the session, and both turned out materially larger than the spine assumed - the preflight seat has to move because the repo slug does not exist before Reconcile resolves it, and the restack verb needs a plumbing gap closed first. Starting either with the remaining context would have produced a half-finished multi-file edit. Left clean for a fresh session.

ALSO NOT RESOLVED. Whether the engine reads CI status via the Checks API. A grep of mitosis.js and the superpowers-parallel .mjs files found NO check-status call anywhere, so either it lives somewhere unsearched or it does not exist. This gates which PAT permission the human should grant, so it must be answered before Section 3 is applied.

HOUSEKEEPING. Thread 01KYG4AEKA6NM746BXVRAZ9DWE was found ACTIVE again and the Stop hook flagged it repeatedly. It was transitioned active -> paused ONLY - the prescribed reversible session-end transition. Its work was NOT disposed and remains the human's call; no session log was written to it, deliberately, since this session's work belongs here and misattributing it would corrupt the ledger. Separately: the two decision records written this session were BOTH assigned number 0017 by the server, a real numbering collision - both files exist and are distinct by slug.

No background tasks or shells remain running.