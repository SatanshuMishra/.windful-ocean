Executed items 1-3 of the four-item order. Items 4 (engine preflight) and MSP-4 NOT started. NOTHING COMMITTED - all output sits in the working tree.

ITEM 1 - RESEARCH (done, 3 parallel researchers).
(a) The 0010 auto-retarget lead is VERIFIED but only PARTIALLY true, and it does NOT dissolve restack. GitHub does retarget an open same-repo PR when its base branch's PR is merged and the branch deleted (docs.github.com "Creating and deleting branches within your repository"; changelog 2020-05-19 "Pull Request Retargeting"), retargeting to the merged PR's own base. BUT only a true MERGE COMMIT leaves the child diff clean; SQUASH and REBASE merges rewrite the parent's SHAs, break the child's merge-base, and force an explicit restack. Multi-hop cascade (A<-B<-C) is UNDOCUMENTED, with community reports (community discussions 58773, 131045, 176698) of the child being AUTO-CLOSED instead of retargeted. Confidence: High on the single-hop doc claim, Low on cascade.
(b) NEW HAZARD, previously unknown: auto-retarget fires pull_request activity type `edited`, which is EXCLUDED from GitHub Actions' default opened/synchronize/reopened. A retargeted PR keeps displaying its STALE check result - it can show green against a base it was never tested against. Runbook requirement, now written in.
(c) Also surfaced: GitHub shipped a native Stacked PRs private preview (~Apr 2026, roadmap #1218). Not evaluated; a future option.

ITEM 2 - TEARDOWN (done, verified with real evidence, reversible).
Backed up to scratchpad/teardown-backup-0009/ with SHA-256 verified BEFORE deletion, including working-tree.patch (24KB of discarded diff). Deleted block-destructive-bash-detector.py + test_block_destructive_bash_detector.py; removed the now-dangling global symlink ~/.claude/hooks/block-destructive-bash-detector.py; path-scoped `git restore` of block-destructive-bash.sh + block-destructive-bash.test.mjs to HEAD. VERIFIED: git diff --stat HEAD empty on both; no residual `detector` reference; merge-denial logic SURVIVES (block-destructive-bash.sh:28); bash -n clean; hook suite 25 pass / 0 fail; the 3 unrelated dirty entries untouched; stash@{0} INTACT.
DISCOVERY not previously recorded: ~/.claude/hooks/block-destructive-bash.sh is a SYMLINK into this repo, so the repo working tree IS the live hook. The restore reverted live behavior instantly - the grep-denying harm 0009 cited is gone. MSP-3b needed no artifact edit: it has no standalone plan file, and 0009 already records its retirement. (The archived 2026-07-09-mitosis-resilience-3b-plan-approved.md is a DIFFERENT 3B, from the mitosis-resilience thread - do not conflate.)

ITEM 3 - RUNBOOK (done, authored, NOT applied).
docs/superpowers/specs/2026-07-26-mitosis-merge-boundary-runbook.md (155 lines, untracked, will commit). Placement forced: BOTH .claude/docs/superpowers/ and docs/superpowers/plans/ are gitignored; docs/superpowers/specs/ is the only TRACKED doc location (verified via git check-ignore).
LEAK GATE PASSED: 0 hits for the confidential codename, 0 for satanshumishra/bitcap/windful; placeholders throughout (15 <ORG>, 13 <REPO>).
CAVEAT, material: the authoring agent had NO network access. Its GitHub docs URLs are from training knowledge, NOT live-fetched. It flagged ~10 items for apply-time confirmation - exact fine-grained-PAT permission names, current UI labels, the bypass_actors JSON shape, whether "do not allow bypassing" is its own API field or implied by an empty bypass list, and whether Checks vs Commit statuses is the right read permission. The Section 7 verification table is INTENDED as the preflight's contract, so those gh api field names MUST be confirmed before the preflight hard-codes them.

ENGINE GROUNDING (read-only; 0010's line numbers had drifted as the brief warned).
Restack is dispatched as prose in TWO sites, not one: mitosis.js:2964-2974 (reconcile-only shepherd RESTACK stage) and mitosis.js:4452-4453 (frontier-train). NO preflight exists anywhere - no gh auth, identity, or ruleset check; item 4 is fully greenfield. It would seat before phase('Reconcile') (mitosis.js:3591) and halt via the existing failedRun shape (mitosis.js:123). Park machinery exists (park, ParkRecord, parkUnit, shepherdPark, persistParkCheckpoint).

LIVE DEFECT FOUND, NOT FIXED - independent of the rewrite. Both prose sites use `git merge-base --is-ancestor` as their ONLY containment check. Under a SQUASH merge the parent's commits are not ancestors of the squashed base, the check returns false, and the engine RE-APPLIES content that already landed. This bites today.

FAILURES / FRICTION. The first teardown dispatch was BLOCKED by the auto-mode permission classifier (correctly - it discards uncommitted work); the user switched out of Auto-Mode and it succeeded on retry. I did NOT route around the denial.

NOT DONE / DELIBERATELY UNTOUCHED. stash@{0} still awaits explicit human confirm (its MSP-2a tokenizer contents are retired under 0009, so it is very likely droppable). An unrelated thread 01KYG4AEKA6NM746BXVRAZ9DWE (pre-tool-use-guard-deny-by-default-inversion) was found ACTIVE mid-session; it was created by a CONCURRENT OR CRASHED session, not this one. I refused to dispose it - writing a disposition for work never seen would corrupt that session's continuity. It needs the human's call.