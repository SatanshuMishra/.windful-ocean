ITEM 1 PASSED (2026-07-27, human-run). The human re-ran the p/default curl; canonical hash = d9f73571cb16f43a3a51b5c9c29d712a77bfe5133f684bd7d713347205a55c96, matching the target exactly. Commit 1 (pin bump 39e9e106 -> d9f73571) is UNBLOCKED. Ledger value had already been independently corroborated against CI run 30303354357's log before the curl.

ITEM 2 CLOSED with evidence; see decisions 0038 and 0039. Human ruled via two explicit choices.

REAL-RULESET VERIFICATION (new, supersedes the replica-based reasoning in 0039 by confirming it): the curl left the genuine ruleset at /tmp/p-default.yml (1074 rules). Confirmed all four suppressed rule IDs are PRESENT in it (detect-non-literal-regexp, prototype-pollution-loop, wildcard-postmessage-configuration, dynamic-urllib-use-detected), so every pragma is live. Strip test against the REAL rule: run-engine.mjs with pragma -> 0 findings; with the pragma removed -> 1 finding at line 22 detect-non-literal-regexp. This proves the 0039 mechanism on real ground, not a replica.

CAVEAT FROM 0038 IS NOW RESOLVED, NO ACTION NEEDED: the 3 unadjudicated dynamic-RegExp sites (run-engine.mjs:98, mitosis.js:789, design-parser.mjs:166) return ZERO findings under the real p/default. The local replica rule was broader than the real rule. There is no unadjudicated-site gap and item 4 is not at risk from them.

WORKTREE STATE: .claude/worktrees/semgrep-pin on branch fix/semgrep-pin-readjudication, cut clean from origin/main @ cd5c65d. The parent working tree's three unrelated changes (.drift-state.json, no-self-merge-consent.test.mjs which belongs to mitosis-preflight-hardening, untracked .claude/skills/context7-mcp/) are deliberately NOT in it.

IN FLIGHT AT SESSION END: an implementer was dispatched to replace globToRegExp with a linear DP matcher in run-engine.mjs and its twin mitosis.js, delete both pragmas, and retarget scope-covers.test.mjs:32's `instanceof RegExp` change-detector assertion to observable behavior. It was instructed NOT to commit, so its output sits UNCOMMITTED in the worktree and must be reviewed before being trusted. Its result was not verified by the orchestrator before session end -- VERIFY IT, do not assume it succeeded.

REMAINING: commit 1 (pin bump + honest-text rewrite of the 2 CONTESTED-PREMISE pragmas at live-inject.mjs:233 and live.mjs:216, dropping the false "caps bound backtracking" claim); commit 2 (the matcher, once verified); item 4 re-run PR #5 sast; item 5 disposition via create_successor. Per 0039 the PR is TWO commits, consciously superseding item 3's "ONE commit" wording. Never push main (0031).