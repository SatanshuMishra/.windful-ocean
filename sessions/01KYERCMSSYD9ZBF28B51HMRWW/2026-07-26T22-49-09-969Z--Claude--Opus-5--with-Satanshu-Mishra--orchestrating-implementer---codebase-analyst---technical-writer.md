Session goal was the two remaining BUILDS. NEITHER shipped. Build A was blocked by a permission deny before writing a single line; Build B was never started (deliberately - it is the larger of the two and context was spent). What DID ship is three things the builds depended on, plus a materially better Build A spec.

BUILD A - BLOCKED, ZERO LINES WRITTEN, TREE CLEAN. .claude/settings.json denies Edit on .claude/lib/** and .claude/workflows/**; every file Build A needs sits inside those globs. The implementer refused both writes and did NOT route around them - no Serena symbol-edit, no worktree, no settings edit. Correct behavior. CRITICAL MECHANIC for the next session: these are DENY entries and the ask list is EMPTY. Deny rules refuse outright and NEVER prompt, so "approve each write interactively" is NOT achievable as configured. The user SELECTED interactive approval as the intent; the only mechanism that delivers it is moving those two entries from deny to ask. That settings edit was NOT made and was NOT consented to - it is hook-guarded by protect-claude-config.sh, and commit a923763 shows Write denies on this file have broken Claude Code startup before. Get an explicit call before touching settings.json.

FIVE SPEC DEFECTS the blocked implementer found - fold these into the Build A prompt or the build will thrash. (1) BIGGEST, and missed by the prior design pass: mitosis-scheduler.test.mjs and frontier-train-e2e.test.mjs execute mitosis.js as a LIVE workflow with fake agents that THROW on an unrecognized label (createFakeAgent default; inline agents at 543, 559, 1069; createFrontierAgent at 218; shepherdAgent at 177). A new required preflight-boundary dispatch breaks all of them until each stub learns the label, and the stub must PARSE the slug from the prompt because the two files use different slugs (me/target vs o/repo). (2) mitosis-git.test.mjs:47-52 asserts "exactly three verbs" - edit that test in place, do not add alongside. (3) The prior spec's error routing was genuinely ambiguous; the coherent split is wrong TOP-LEVEL shape to readError/exit 20, absent or wrong-typed NESTED fields to invariant failure/exit 22. (4) mitosis-git.test.mjs:765-771 forbids the substrings merge, approve, --squash, --rebase, --admin, --auto anywhere in the wrapper source - new failure strings must dodge them. (5) agent() returns a RAW value, not a tagged outcome; a Done-check only means anything wrapped in runStage() (mitosis.js:1671).

CONFIRMED STILL TRUE against live code: fatalReport at mitosis.js:121 (failedRun does not exist), the seat at 3630-3631, reconcileShippedSet at 334, runReconcileOnlyAdvance at 2884, the four is-ancestor sites at 1175/2971/4452/4593, and classifyGhMerge will NOT refuse the three planned bare-GET reads.

CLEAN BASELINE: npm test = 1229 tests, 1229 pass, 0 fail, 4603ms. Any implementation must beat that number.

LEDGER INTEGRITY HOLE FOUND AND REPAIRED. The prior session log asserted both 0017 records "exist and are distinct by slug". FALSE. The server's numbering collision CLOBBERED the preflight record; only the restack one is on disk. The ruling survived in three places (the spine's own key_decisions slug, runbook Section 7, the session log) and is restored as decision 0019 - verified present on disk after writing. Treat decision numbers as untrustworthy; verify a record exists after every record_decision call.

ENGINE-BREAKING RISK CLOSED, and the runbook was wrong in a way nobody predicted. The engine reads CI status at exactly ONE site, mitosis.js:4596 (prompt built 4586-4599, consumed at 4584), via gh run list / gh run view - the Actions Workflow Runs API. NOT the Checks API and NOT the Commit Statuses API. So the fine-grained-PAT needs Actions:Read; the runbook prescribed Commit statuses, which would have left pushes and PRs working while the ship-stage CI poll silently failed. The Checks-permission threat was never real - the engine never called that API. Also confirmed: the engine has NO token logic of its own (zero hits for GH_TOKEN, GITHUB_TOKEN, gh auth across mitosis.js and the .mjs libs), so every gh call rides the ambient identity - which is what makes the Section 3 permission set load-bearing. Runbook corrected and committed as 4575265; leak-checked (no confidential codename, no real slug, placeholders intact); pre-commit hook ran the full suite green.

NOT PUSHED, by explicit choice. Two commits sit on docs/mitosis-merge-boundary-runbook: a0bb6e1 and 4575265. Both pass the leak gate. The repo is PUBLIC and this thread carries a standing leak caveat, so the push was left as the human's call rather than taken autonomously.

WORKING TREE at hand-off: only the three pre-existing unrelated entries - .drift-state.json, no-self-merge-consent.test.mjs (belongs to mitosis-preflight-hardening), and the untracked .claude/skills/context7-mcp/. None were touched.

No background tasks or shells remain running.