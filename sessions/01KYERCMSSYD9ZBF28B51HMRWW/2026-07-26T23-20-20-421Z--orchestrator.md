2026-07-26 session: the BLOCKER WAS A MISDIAGNOSIS. Build A is not blocked and never needed a settings.json change. See decision 0021.

EVIDENCE GATHERED (all verified this session, not inferred):
- ~/.claude/{lib,workflows,skills,agents} are symlinks INTO this repo's main checkout; ~/.claude/{settings.json,CLAUDE.md,keybindings.json} likewise. ~/.claude/{hooks,rules,plugins} are real dirs. So the two Edit denies freeze the LIVE engine this session runs on - a real self-modification guardrail, correctly kept.
- The denies are absolute-path-anchored to the main checkout only. Worktrees hold real files (no symlinks back), at paths lacking the denied prefix.
- EMPIRICAL PROOF, both directions: Write into .claude/worktrees/boundary-preflight/.claude/lib/superpowers-parallel/ SUCCEEDED; the identical Write into the main checkout's .claude/lib/superpowers-parallel/ was refused ("File is in a directory that is denied by your permission settings").
- Official docs (code.claude.com/docs/en/permissions.md): Edit(path) rules govern Write and NotebookEdit too; a bare Write(path) rule is accepted but NEVER matched and only emits a startup warning. Precedence is deny -> ask -> allow; deny is a silent hard block in every mode incl. bypassPermissions; deny rules match either the literal path or the symlink target.
- CONSEQUENCE: commit a923763's carried-risk note ("file creation via the Write tool in the main checkout's frozen engine directories is ungated") is FALSE. The paired Edit deny already covers Write. There is no hole; the risk register entry should be dropped, and dropping the Write denies was still correct since they were inert warning-generators.

STATE CREATED:
- Worktree .claude/worktrees/boundary-preflight on new branch fix/mitosis-boundary-preflight, based at cd5c65d (main) - satisfies the do-not-check-out-anything-predating-cd5c65d constraint. Worktree verified clean (permission probe file removed).

BASELINE: npm test in the main checkout = 1229/1229 pass, 0 fail, exit 0 - green WITH the mitosis-preflight-hardening thread's no-self-merge-consent.test.mjs working-tree change still applied.

RE-VERIFIED FACTS FOR THE BUILDS:
- The four merge-base --is-ancestor containment sites are still exactly at mitosis.js:1175, 2971, 4452, 4593. All four are inside prompt template literals, i.e. instructions emitted to subagents - corroborating 0017's finding that mitosis.js cannot exec.
- mitosis-git.mjs (484 lines) imports spawnSync but uses it ONLY inside execGh (line 262) to spawn the gh binary. No git exec primitive exists. Build B must add one with its own fail-closed guard, as the register says.
- No preflight implementation exists anywhere in .claude/lib, .claude/workflows or .claude/skills/mitosis - Build A is greenfield.

BUILD A SPEC SOURCE: docs/superpowers/specs/2026-07-26-mitosis-merge-boundary-runbook.md Section 7 carries the exact read-only gate commands and, importantly, the traps a naive implementation would fall into: use .permissions.push / .permissions.admin for booleans and .permission for the string (there is NO .permissions.write key, so a check against it fails silently); never hard-code role_name; the rulesets-list endpoint defaults includes_parents=true so filter source_type=="Repository"; prefer /rules/branches/<base> (server-computed effective rules) as the primary approvals check; and NEVER treat an absent bypass_actors key as proof the bypass list is empty. Per 0019 the gate is the three Metadata:read-provable invariants only - bypass-emptiness stays human governance.

LEDGER TOOL FAULT RESOLVED: record_decision succeeded this session (wrote 0021). Last session's required-property failure did not reproduce; the tool is healthy. The CI-status decision that failed last session is still unrecorded and could now be retried.

STILL OPEN: the five Build A spec fixes recorded in the previous session's log were being recovered by a subagent that had not reported when this session wound down. Build A was deliberately NOT dispatched without them, since re-dispatching without the fixes repeats a known failure - the live-workflow fake agents that throw on unknown labels being the significant one.