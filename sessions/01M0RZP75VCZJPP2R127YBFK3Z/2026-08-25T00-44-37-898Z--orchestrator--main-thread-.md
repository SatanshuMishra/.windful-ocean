Measured U2.3's gate instead of assuming it, and the unit did not survive the measurement.

**The gate.** On a fresh clone at trunk 9392037, `npm test` exits 1: `tests 2299, pass 2256, fail 41, skipped 1, todo 1`. U2.3's criterion 1 - the lane completes with conclusion `success` - was therefore falsified, not merely unverified, and every route to a green lane was on the forbidden list. A census of the 41 against what U3 and U4 declare they own put roughly 17 cases across 13 files outside both ceilings, refuting the plan's own premise that those two units make the suite green, and exposing that no unit's acceptance criterion asserted `fail == 0` at all. Nothing in the plan could fail for leaving the suite red.

**The user chose the split and the widening.** Recorded 0725 (split U2.3), 0726 (widen U3's ceiling to assert the suite exits zero), 0727 (omit `verify.suite_command`).

**The enforcer question, settled by running the pinned action against disposable synthetic repositories.** Landing `receipts.yml` with `verify.suite_command` set would have blocked the very units meant to green the suite: gate G9 runs the suite on the head commit only, has no base-side run anywhere, cannot attribute a failure to the diff, and blocks while reporting a pre-existing red as a regression. A synthetic unit carrying a genuine red-on-base green-on-head receipt still exited 1 because two unrelated tests failed. Omitting the key degrades G9 to a printed warning naming it; `gates.disabled` reaches the same pass silently and was rejected for that reason. Also established: the pinned enforcer is `receipts/gates@1.0` with fourteen gates, not the eighteen-gate `@1.1` the global rule cites; G9 has no `mode` key and a `gates.G9` entry is silently ignored; G14 skips silently when the receipt is red, so an absent `gates.G14` artifact entry means "did not run", never "clean"; and configuration is read from the base commit, so a config change never governs its own pull request.

**Shipped U2.3a as pull request 3** - `.github/workflows/receipts.yml` (enforcer at the host's pin, `pr-title-lint` verbatim, no `mitosis-gate`, no `repo-checks`, no duckdb) and `receipts.config.json`. All four acceptance criteria met. The inertness mutation was observable: the intact title lint exits 1 with its message, the assertion-deleted copy exits 0 on the same input. Both checks green, `CHECKS=0`, enforcer verdict `WARN` in 55 seconds - a downgrade-tag short-circuit clears in about 129 milliseconds, so that run was real.

**Merged by the user, branch deleted, verified on the trunk by content rather than by status.** `compare/main...2bd43500` returns `behind`, `main` is the only branch, and the committed config carries neither `verify.suite_command` nor `gates.disabled`, with the enforcer pin and `pr-title-lint` present and duckdb, `mitosis-gate` and `repo-checks` absent.

**Plan edits.** U2.3 became two briefs, U2.3a and U2.3b. U3 gained the suite clause and two steps: clear the residue named in filed item 1, and restore `verify.suite_command`. Waves renumbered to 22 waves and 25 pull requests. The previously unstated U3 -> U2.3b edge was added to the hard-edge table. Thirteen items were filed with owners. Brief lint was zero before and zero after; 24 briefs became 25.

**A defect I introduced and corrected the same session.** The U2.3a brief's criterion 1 used `grep -q` against a rollup that emits `SUCCESS` in upper case, so a passing check read as a failure. Now `grep -qi`, reproduced both ways against pull request 3: the original pattern exited 1, the corrected one exited 0.

**Filed rather than fixed.** Two SPEC section 5.5 defects: `verify.on_load_error_red` is a boolean where the enforcer expects a string, and since the enforcer reads it as `value || "warn"` the key is inert; and `agent.tripwires.g11_live` is `"off"` with no recorded rationale while its continuous-integration twin `gates.G11` blocks. A reviewer proposing to set `verify.suite_command`, add `gates.disabled`, or add `fixed` to the downgrade tags is arguing against a recorded decision.

Nothing was left running. No background task, no open subagent.