Repaired the migration plan end to end. No unit was started and no code was written in the extraction repository.

WHAT THE SESSION FOUND FIRST. Re-measured the landed import against a fresh clone rather than inheriting the prior session's numbers. The shipped test script names the test directory as a bare path, which this Node version resolves as a single file, so the suite runs zero tests and reports one synthetic failure. With a glob it runs 966 tests: 871 pass, 95 fail, of which 69 are whole files that cannot load. All 112 unresolvable specifiers sit under the test tree and none under the engine source: the import moved modules into subdirectories and rewrote the source importers, but no rewrite rule ever covered the tests or the test helpers. 102 of the 112 point at a file that exists at exactly one other path, with zero ambiguous.

WHAT WAS ADDED TO THE PLAN. Three units between the import and the plugin unit, numbered by the plan's own split convention so ship order and name order agree, with the old second-stage name retired rather than reassigned so no existing reference silently changed meaning. Carry the six modules the closure missed; make the suite executable; then bootstrap continuous integration. The continuous-integration unit had to move behind the other two because its receipt requires observing a green that could not exist while the suite ran nothing.

THE INSTRUMENT KEPT BEING THE DEFECT. The seven missing modules I reported were produced by a scan blind to a fifth loading form, a module spawned as a driver with its path built from its own location; that form is how the subsystem root is reached, which is exactly why it was never carried. Recomputing over every path-shaped literal instead of over loading syntax returned nine candidates, of which six are carried and three are host-owned or not references at all. The brief lint I wrote to gate the repair had the same fault twice: it matched the prose describing a defect, and read a vertical bar inside a query expression as a shell pipe. Both fixed by parsing only fenced blocks.

THE REPAIR. All 24 briefs now pass the lint, with four exemptions each carrying a written reason, and one of those reasons was made true by adding the check it claimed rather than left standing. The publication unit was worst and went first: its three checks printed counts beside expectation comments that nothing compared, all six criteria were negative, and a licence naming a wrong repository with the machine-training reservation gutted satisfied every one. Its sixth criterion rested on a gate the same unit authors. Every criterion is now positive, the reservation is compared byte for byte against the source, and four mutations run before publication rather than after. The universal defect across the remaining twelve was a captured exit code with no stated disposition. One acceptance criterion was demonstrated already satisfied on the parent commit: it grepped the pull request tool for a path string that file does not contain, because it reaches the engine by relative specifier.

THE ONE CONSTRAINT CONFLICT, MEASURED AND RESOLVED. The plugin unit's criterion mandates a minimal-mode flag whose own help states that authentication becomes strictly an API key and that the keychain is never read. An API key is not permitted here. Pointing the configuration directory at an empty path isolates correctly but the session reports not being logged in, because the account record resolves through that directory while the credential sits in the keychain. Both measured live. The flag is dropped and the proof rebuilt around identifying the plugin by its resolved load path, with a second session pointed at an empty directory whose assertion must fail. That control is stronger than the isolation it replaces, and it closes a hole the flag never addressed, since the host ships a skill of the same name.

NOTHING LEFT RUNNING. Two background probes completed. PLAN.md went from 1432 to about 1700 lines; the pre-repair copy is beside it.