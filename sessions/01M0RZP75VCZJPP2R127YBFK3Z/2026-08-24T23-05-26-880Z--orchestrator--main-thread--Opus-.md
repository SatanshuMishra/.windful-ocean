Shipped U2.2, make the suite executable. Pull request 2 merged; content landed and was verified by ancestry rather than by the MERGED label (merge commit 9392037, shipped head dc01ac1, LANDED=0), and the remote branch ref confirmed gone.

WHAT SHIPPED. The test script now enumerates instead of resolving a directory as a single file. 128 stale specifiers across 80 files were rewritten by resolution against the tracked file list, with zero ambiguous halts: 102 import specifiers (100 found by Node's own resolution, 2 dynamic imports inside test bodies the load probe is structurally blind to) plus 26 file-locator literals, including the spawned-driver path that no import-shaped rewrite would find. A load census over every tracked test file was added, plus main guards on the three modules that ran their command-line program at import time. retired-roster.json was carried byte-identical (blob 8046cb850082434d7d66457204eb542025e1cf17, matching the host at the import commit). Three allowlist entries were deleted as idle, not one: the brief named the retired-roster entry, I flagged a second from reading the trunk, and the agent found a third of the same class in lead-agents.test.mjs.

MEASURED. Suite went from 113 failures of 1030 cases to 41 of 2299. Receipt exit codes on the shipped commit: SUITE_EXIT=1 (expected, proves nothing either way), TOTAL_CHECK=0 at TOTAL=2299, LOAD=0, PATHS=0. Red-on-parent load census: 75 failing files, where the brief predicted 69. The measurement stands; the prediction is superseded, not reconciled.

THE MUTATION THAT PAID. Under the enumeration mutation the suite exited 1 in BOTH states, so the exit code was uninformative exactly as the brief warned. Only the count fired: the runner reported 1 test for 128 tracked test files. That is the defect that had survived until now.

WHAT FAILED, AND HOW. The delivery-lead dispatched its implementer as a background agent and then ended its own turn believing it was waiting for a result. The turn genuinely ended; no result would ever have arrived. The implementer had in fact run to completion, leaving four commits and an unstaged 81-file rewrite on disk. I re-derived that state from git as a liveness question only, did not audit the work, and resumed the lead by message so its context survived, with a standing instruction to dispatch every remaining child with run_in_background false. It then completed the unit in one turn. This is the second thread to hit this trap.

HONESTY NOTE FROM THE AGENT, WORTH KEEPING. Its first run of the total-tests parser referenced an environment variable it never exported. That run exited 1 and would have read as a legitimate red. It caught this itself, corrected the invocation, and re-ran; the reported figure is from the corrected run. An exit code alone could not have distinguished the two.

NOT MEASURED. Whether any of the 41 remaining assertion failures is a regression from the rewrite. Review read all 122 changed lines and found each differs only inside a quoted string with an unchanged basename, which is strong evidence against one, but the failure set was not diffed against the parent's and no claim is made.

RECORDED. Nine items filed into the plan with owners assigned, before the Rollback line of the U2.2 brief, following U2.1's convention. Brief lint read FAIL (0) exit 0 both before and after the edit. Decisions 0723 and 0724 recorded at decision time.