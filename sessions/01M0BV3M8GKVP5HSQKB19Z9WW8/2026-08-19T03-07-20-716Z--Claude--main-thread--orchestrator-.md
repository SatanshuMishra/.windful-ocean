Session spanning the close of the predecessor thread and this one's reframing. Three streams.

SHIPPED — predecessor thread agent-architectural-review, now done. PR 218 confirmed on origin/main by merge-base and its branch deleted. A delivery-lead settled the agent-id reuse question that had blocked c5: multi-start groups are RE-ENTRY duplication, not reuse, so dispatch grain was sound all along (2139/2139 sidecars with a toolUseId are Agent calls; zero ids shared across dispatches). Round 1 could only bound Lead share by hand at a 38.24 percent ceiling. The user rejected a hand-derived negative and ruled the instrument be repaired first. Round 2 fixed a LEFT JOIN cartesian fan-out reporting 2872 start rows against a true 667 and emitting a negative minimum duration, shipped a halting agent-type census, unblocked the Lead numerator hardcoded at contract.mjs:43, and fixed an npm test glob that excluded the agent-audit suite. Result: Lead share 24.23-26.60 percent at n of 421 against a 50 percent bar. FAIL, decided, instrument-produced. c5 struck rather than reworded; thread closed done.

SHIPPED — the routing fix, PR 238, OPEN and awaiting a human merge. The user ruled the whole Lead-share statistic non-diagnostic: a share sums over all four routing outcomes, so any value is reachable with wholly wrong routing, and the observed architect scope-growth failure was never a triggering defect but a missing stopping condition. A measurement round building three tree-based signals was killed mid-flight on that ruling. The fix is text: architect now carries a stopping condition of decision 0468's shape (a decision closes the design question; read it rather than re-dispatching), plus when-NOT-to-use clauses on all four Leads. Edited in agent-specs/*.spec.json and regenerated, because the .md files are generated. Receipt is a natural red-then-green: drift check exit 0 baseline, exit 1 naming exactly the four changed files, exit 0 after regeneration, with 97 pass 0 fail across nine spec suites.

SHIPPED — the ledger investigation, and it REFUTED its own premise. There is no store divergence. The twin data roots are git worktrees of one _ledger ref in one repository, 614 files and 613 numbers each, all byte-identical, one an ancestor of the other. There were no gaps either: 0001-0613 is contiguous and this session's 0611-to-0613 jump was another concurrent session consuming 0612. All eleven of this session's decision records are present and identical in both roots. Three real defects were found instead and are documented in an upstream report delivered to the user: an unlocked max-plus-one number allocator that has ALREADY collided at 0521 with two different decisions from two threads 35 seconds apart; an active-thread pointer at gitCommonDir/ledger/active-thread that no tool can clear because transition_thread throws illegal_transition before reaching its release line; and Claude Desktop registering every plugin twice via an app-composed --plugin-dir flag. The stale pointer was deleted, which is the supported remedy. Nothing in any ledger store was merged, renumbered or deleted.

FAILED, and worth carrying forward. Three errors of mine, all corrected in flight but each cost real work. I asserted that subagents hold SendMessage and told a Lead its own report was wrong; they do not, and it burned three of its calls proving me right to doubt myself only afterwards. I framed the decision-number gap as the hard evidence for store divergence; the inference runs the other way, since independent stores predict duplicate numbers rather than gaps, and a worker had to kill that premise for me. And my first memory write generalised "the twin roots converge" from this project, which is false: a second project on this machine holds 70 decision records in only one root, so collapsing the roots there would strand them. That memory is corrected.

STILL OPEN. PR 238 needs a human merge, and its agent definitions are in force ONLY while the checkout sits on fix/lead-agent-stopping-condition, because the agents directory symlinks into this working tree. Nothing is running in the background; no shell ids to kill.