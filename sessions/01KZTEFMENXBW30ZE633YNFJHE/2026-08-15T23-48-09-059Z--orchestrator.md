All six CP pull requests are now merged into feat/mitosis-os-process, base at 995616e6, and content was asserted present by file for each rather than inferred from a MERGED status: the disposition record, the prompt data-block tests, the coupling carry test, and the couplingResolution carrier across generate-run-script.mjs, mitosis-execute.js and run-engine.mjs. CP is closed. RULING on two items proposed for the thread. Neither becomes a completion criterion. The engine-side coupling consumer question is real and survives D2, because run-engine.mjs is surviving substrate, and couplingSerializeViolations at run-engine.mjs:11 called from :386 is verified as the ONLY engine-side reader - nothing reads decision, source, signals or rationale to influence dispatch. But adding it as a criterion would treat the acceptance list as a floor, which is what makes done unsatisfiable under receipts/gates@1.1; and no-further-consumer is a legitimate resting state, so the stack can ship without settling it. The nine-field twin is verified real at generate-run-script.mjs:48-49 against the prompt literal now at mitosis.js:5117, shifted from the filed 5084 by PR 131. Its premise expires: D2 deletes .claude/workflows/mitosis.js at design.md:550, removing the second copy, so a census test written now is work D2 deletes, and building one also cuts against 0439's retirement of census apparatus. The hazard is genuine only across the C7 to D1 to D2 window, which is exactly when C7 is likeliest to touch those fields, so it is carried as a discipline - touch both copies in the same commit until D2 - rather than as a test. Both remain filed in the committed disposition record and are now carried as spine risks with explicit triggers. record_decision is still refusing every call with missing_parameter on options and outcome, so this ruling is captured here rather than as a numbered decision.