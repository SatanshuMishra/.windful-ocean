Stack progress, overnight run. Two MSPs shipped and both CONFIRMED GREEN ON CI, not just locally.

PR #138 feat/d2a-unwire onto feat/mitosis-os-process, tip fd6d5dd3. Suite 3327 to 3334, 0 fail. All 13 CI checks SUCCESS including receipts, the three mitosis-gate matrix verbs, test, sast, sca, secret-scan, pr-title-lint. Nine of nine criteria, no downgrades. Red-on-parent and inertness both proven for the coarse-scope wiring.

PR #139 feat/d2b-delete onto feat/d2a-unwire, tip 35fc8f3b. The legacy engine is DELETED. Suite 3334 to 2839, 0 fail, delta minus 495 reconciled file by file. All 13 CI checks SUCCESS. Nine of nine criteria including the amended 3.2.9 that removed compileUnderSandbox and the workflow-sandbox import from mitosis-gate-core, without which every gate verb would have failed at module load.

c23 is in flight on feat/c23-park off 35fc8f3b, base feat/d2b-delete.

Decision 0465 re-scoped D3 on reconnaissance findings. The critical fact: NO ENGINE RUN HAS EVER EXISTED. captureEnvelope at dispatch.mjs:570-593 is complete and captures both cache fields and total_cost_usd, but pool.mjs:290 narrows the verdict to ok and outcome and all three production consumers discard it; run-store's recordStart, recordOutput and commitState have zero non-test callers; and not one journal byte exists in the repo, in any of 27 worktrees, or in any external checkout. So the 10-dispatch falsifier has no denominator.

Two SPEC claims proven false and superseded by the ceiling doc and 0465. First, .claude/reports/ is NOT git-tracked: .gitignore:11 ignores it, so a report written there is invisible to the enforcer, to the PR and to a fresh clone; D3's report goes under .claude/docs/ instead. Second, dead-export-lint.test.mjs is absent at the base and at both shipped MSPs but still PRESENT on origin/main, because retirement commit 80de7fa9 is an ancestor of the base only. That confirms 0460 rather than undermining it, and it flags a modify/delete conflict hazard for the M1 main fold.

Filed above ceiling, not fixed: a composed path is invisible to a grep-based census (phase-model.test.mjs builds its target with join and template interpolation, so the static sweep missed it) — worth naming as a class; the transcription-fidelity census lost 21 tests and cannot be restored without splitting a 694-line module; GATE_COMPILE_EXIT is now dead; the phase-parity declaredNeverCalled direction is dropped pending a C1 follow-up; the dispatchable-agent census narrowed from 7 to 5 because debugger and solution-architect were named only in mitosis.js string literals.

OPEN FOR THE USER, deliberately not decided: the c7 release gate says the base merges to main only if D3 clears the falsifier and that a failure is recorded as falsified. The falsifier cannot be EVALUATED at all, and unevaluable is not falsified. Whether the base may merge to main with the falsifier unevaluated is a human ruling and is being left framed rather than guessed.