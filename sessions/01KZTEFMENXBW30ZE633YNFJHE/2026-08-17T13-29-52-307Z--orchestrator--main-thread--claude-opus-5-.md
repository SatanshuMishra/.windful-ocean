Scoped the c27-c33 gap against the SPEC, designed a linear MSP stack, and executed it overnight by plain subagent dispatch under 0378.

SHIPPED AND MERGED (7 pull requests): #156 scope-fence settles without a null sha, #157 run-engine prompt twins deleted, #158 the eight-phase driver, #159 the disposable end-to-end substrate, #160 the g11 live tripwire disabled, #167 durable dispatch-start and state records (reopened from the closed #162), #165 journal resume, #168 reconcile outranks the journal. c32 and c33 closed.

SHIPPED AND OPEN (15 pull requests, #172 through #187): compose prompts at dispatch; integrate with the real boundary gate; the engine opening one pull request per MSP through pr-create; serialized ship order behind the merge gate; park with transitive dependents; bounded redispatch; poisoned-lock recovery; the end-to-end decompose-to-pull-request test; phase-parity added to the CI gate matrix; judgment record emission; the fix kind retired; diagnose and redispatch in remediate; plan, plan-review and replan dispatched from Prep; prep record emission; the ci-to-green loop. c27 CLOSED at the stack tip: all twelve registered prompt kinds reachable on a real run, fix explicitly retired, verified both by an import-closure walk and by a real substrate run in which the stub claude actually received each composed prompt. c28 through c31 have their receipts on the stack and close on merge.

WHAT FAILED, AND WHY. The receipts enforcer draws its G14 mutation sample AFTER a pull request exists, and every implementer finished before that ran, so the one-attempt policy in 0512 never triggered for them and ten pull requests accumulated blocked. I misreported several as green from the agents' local runs; CI is the authority and disagreed. Six were then cleared, every one on the first push, after an agent reproduced the enforcer's G14 locally from the plugin's own source and the replica matched CI's twelve-mutant picks exactly. Three remain red as tracked downgrades rather than false greens: #172 carries one non-equivalent survivor at cli.mjs:364 whose one attempt is spent, #181 needs a receipt-cmd line in a body immutable at creation, and #183 hits the G11 test-removal wall from 0492.

THREE CORRECTIONS THE AGENTS MADE TO MY OWN DISPATCH PROMPTS, each of which would have shipped a defect: a function I called dead had a live call site at run-engine.mjs:412; C7 obligations J1 and J2 are phantom, because appendRunJournal exists nowhere in this tree and the only reference is a string literal citing a file that is not in this repository; and an uncapped attempt ordinal in the epoch key destroyed the termination argument, which surfaced as engine.test.mjs running 114 seconds and failing until the ordinal was capped. One agent also stopped and reverted a written module rather than half-ship it, handing over a verified design instead.

A LIVE HAZARD FOUND AND FIXED: unit-verdict-sha.test.mjs drove runCli in-process with no dispatch dependency, so once Prep had a record to plan against it fell through to the real dispatcher and SPAWNED THE REAL CLAUDE BINARY. Caught mid-run at pid 66165 holding a live plan prompt, killed, and now stubbed.

Nothing was merged, force-pushed or deleted after the branch cleanup that reopened #162.