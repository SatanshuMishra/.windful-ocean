D1 shipped as PR #135 against feat/mitosis-os-process, awaiting a human merge. One filed item turned out to be a hole in the stack's shape rather than a loose end.

HYGIENE, both on the user's explicit go-ahead. The primary checkout moved off chore/receipts-global-standard to main and fast-forwarded nine commits, so the live global configuration is current again - receipts.md and the new "Trust the result" section of delegation-discipline.md are now in force through the ~/.claude symlinks. The merged feat/c7-loop worktree was removed and its local branch deleted; the remote branch survives because the bash classifier blocks push --delete.

SCOPE RULING BEFORE DISPATCH. Orientation found that cli.mjs reads --spec --run-id --at --repo-root --journal --repo-slug --integration-branch and optional --window, parsing --spec as a JSON document carrying specs and manifest, while SKILL.md collected a markdown spec path plus verify, build, models, worktreeRoot and fixLoopMax. Swapping the invocation alone would satisfy the SPEC's written D1 acceptance while leaving the entry point non-functional. The user ruled to WIDEN D1 rather than file the gap (0454), accepting the larger diff.

DISPATCH SHAPE. One Opus MSP owner in worktree .claude/worktrees/d1-entry-point off fd84e889, authorized to dispatch Sonnet children under a strict contract - exactly-one-correct-output tasks only, no judgment delegated, run_in_background false, results trusted rather than re-run. It was given the five criteria as a ceiling, the honesty ladder, and the tree's named hazards (raw control bytes, the SKILL.md:35 pack wording, the INLINED_TWINS list, the bash gate, the exec-policy census, G11). It was forbidden to open the PR, on the ground that a title and body are immutable after creation.

WHAT SHIPPED. Commits f0b48f56 and 9085793b, 3 files, +108/-30. The hook now resolves mitosis.js and mitosis-execute through one BLOCKED_WORKFLOW_NAMES set and matches scriptPath by extracted basename rather than a hardcoded name, so every shape already proven for mitosis-execute applies to mitosis without duplication. SKILL.md drops the workflows-enabled precondition, dispatches node .claude/lib/mitosis/cli.mjs through Bash, collects exactly the eight flags the CLI reads, and keeps the declare-or-pass-or-ASK branch contract.

RECEIPT. The worktree sat at fd84e889 with the hook untouched, so the red was a true parent-commit red rather than a stash simulation: exactly four failures, all four the new mitosis.js legs, and 26 pass 0 fail 0 skipped after the fix. The parent hook is itself the inertness mutation. Full suite exit 0, 3271 tests, 3270 pass, 0 fail, 0 skipped, 1 pre-existing todo.

WHAT DID NOT CLOSE. Criterion 4 split. Two clauses are fixed - every required flag has a declared source, and all seven orphaned inputs were removed with their homes named. The third clause, that the printed invocation is one a reader could actually run, landed unverified-reasoned: nothing on this base produces the {specs, manifest} run document. The agent did not invent a decomposer; the skill now declares the run document an explicit precondition and forbids hand-decomposing in main. That downgrade is visible on the PR as its own not-verified line.

THE FORK. F1 is not a loose end. The only decomposer lives inside mitosis.js at :4409-4466 (schema :1786-1818, manifest builder recovery.mjs:108-130) and is not liftable - it emits msps[] keyed on dependsOn with no request, and never serializes, handing its table straight to its own in-process engine at :5148. D2 deletes that file. The user ruled to insert a decompose-and-emit MSP before D2 (0455), so the capability never dies without a replacement.

ALSO FILED, NOT FIXED. F2 SKILL.md's run-identity section documents identity/shipped/overallStatus, which runEngine does not emit; left byte-identical rather than deleting documented behavior on an agent's own judgment. F3 branch-contract.mjs is unreachable from the CLI, so the contract is skill-side prose with no machine enforcement. F4 baseBranch has no flag and cannot travel into a run. F5 --run-id, --repo-slug and --integration-branch validate only after work starts, discarding a whole run's summary on a typo. F6 dead-export-lint does not exist in this tree yet D2's acceptance at SPEC:555 requires it green. F7 run-engine.mjs and engine-args.mjs are dead relative to the OS-process path.

The provenance flag rejected model=claude-opus-5[1m]; the window suffix is not part of the model string the tool accepts.