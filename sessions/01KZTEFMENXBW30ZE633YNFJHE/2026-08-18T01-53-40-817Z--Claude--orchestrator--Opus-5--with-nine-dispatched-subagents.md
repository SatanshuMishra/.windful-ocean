Read the e2e audit findings, reported them, and then implemented all six of the audit's minimum change set under four user directives issued in response.

WAVE 1 (four read-only agents) overturned three of the audit's own conclusions. (a) Both options the audit named for the one-invocation fix are wrong: re-snapshot is INCORRECT because Execute truncates the journal it writes into, losing prior-invocation builds and emptying the shipped set, which silently disables the divergence guard; loop-to-fixpoint is non-terminating and destructive because a shipped-but-unmerged unit is neither settled nor built, so the work set regrows to everything and every pass truncates the journal. A third option won: advance the resume view in memory from the deltas Execute already recorded. (b) The ship defect is worse than reported: two DISJOINT branch namespaces exist, the child commits to one name and Ship composes a PR head from another, so the head never existed anywhere and publishing alone would not have fixed it. (c) There is nothing real to derive green from, because the child's schema sets additionalProperties false and the scoped check is never executed host-side.

WAVE 2 (three parallel implementers, isolated worktrees, disjoint file fences): deleted the fabricated green claim and its verified line; added resume-advance.mjs as a pure in-memory fold; built ship-publish.mjs as an ordered ten-step publish sequence plus a compose-head git verb that joins the two namespaces. All three merged into feat/mitosis-ship-live with zero conflicts, and the merged COMPOSITION was verified green rather than assumed from three green branches.

WAVE 3 (two parallel): wired stacked pull requests through the centralized pr-create tool with engine-side retirement of a provably-merged parent branch; and built a live disposable-repo harness that REALLY RAN - created SatanshuMishra/mitosis-live-pr-harness, pushed a real branch, opened a real pull request, read it back independently, and cleaned up. Its load-bearing assertion is the negative case: pr-create for a branch never pushed exits 21 with real GitHub stderr, the precondition the fake never checked.

WAVE 4 (sequential): derived the exit code from shipping rather than unit disposition and corrected the SKILL.md exit table; wired mergedShas through to the divergence guard; retired the fake gh binary.

WHAT FAILED, HONESTLY. Criterion 3 of the exit-code unit came back unverified-reasoned, and the reason is a defect the audit never found: divergence.mjs destructures agent, divergenceCheckPrompt and DIVERGENCE_CHECK_SCHEMA from its ctx and NONE OF THE THREE EXISTS ANYWHERE IN THE TREE. Every keyed parent therefore throws, is caught, and folds closed, so a dependency chain structurally cannot resume. Measured directly: the merged map changes nothing observable while the probe is unwired. Wiring the map was the necessary prerequisite and is in place; closing it needs a new adjudication mechanism and was left as filed work.

Retiring the fake gh nearly caused a worse failure. Both gh-merge-shim.mjs and pr.mjs fall back to hardcoded absolute paths when nothing resolves on PATH, so naive deletion would have made every affected test silently reach the real authenticated gh on this machine. The fix routed the CLI in-process through its existing dependency-injection seam instead, making gh and the pr.mjs subprocess unreachable as spawn targets rather than merely shadowed. No test was dropped; every behaviour survives at the same layer.

ONE DISPATCH ERROR OF MINE: I fenced agents one and three by explicit file list but omitted that list for agent two, which then rewrote six end-to-end files and overlapped agent one on e2e-ship-pr.test.mjs. It merged clean, but the fence gap was luck rather than design.

STATE AT HAND-OFF: 12 commits on feat/mitosis-ship-live, 59 files, +3394/-1388, tree clean, suite exit 0 at 2212 passing, determinism and phase-parity green. Six branches exist LOCALLY AND UNPUSHED. No review has run. No pull request has been opened. The engine has never been driven end to end on a real run against a disposable repo - the live harness proves the pr-create path, not the engine reaching it.