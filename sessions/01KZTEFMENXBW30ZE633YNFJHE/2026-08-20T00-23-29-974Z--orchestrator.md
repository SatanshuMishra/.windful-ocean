THE LIVE RUN EXECUTED, on the user's own trigger after three agent-side refusals. Run a15d0001 against the disposable harness. The engine reached Ship and opened two REAL pull requests, harness 3 and 4, each body ending with the pr-create machine trailer exactly. 24 of 28 declared checks matched.

All four mismatches trace to ONE cause: numbers-clamprange, the dependency-edge unit, parked at Ship rather than opening its pull request. Consequences, not separate defects: two PRs opened where three were declared; that unit absent from ship.mergeOrder; the parent's deleteAfterMerge false because no stacked child survived; and verdict status blocked rather than all-integrated-opened. That last one refutes the phase-1 prediction that the verdict could never read blocked - the prediction was reasoned over Execute-time parks, and a SHIP-time park does count toward genuineParkedCount.

What the run proved, each against a state declared before it started:
- C1 dependency edge: the built record for strings-titlecase at journal line index 1 precedes numbers-clamprange at index 3, and the unit table reads that unit done.
- C2 induced failure: parked, the exact stderr line, and NO journal park record.
- C3 retry path entered: the unit appears in two distinct ticks.
- C4 park: diagnose-dispatch-failed, and the detail confirms the filed defect verbatim - the diagnose child outran its 1ms budget and took SIGTERM, which is the inherited-timeoutMs trap.
- C4b: blocked-by-parked-prerequisite naming dates-isleap.
- C5 resume: restarted false at attempt 2, and the invocation-1 journal is an exact ordered prefix of the final one, 2 lines growing to 10.
- C6 poisoned lock: exit 1, the run-store stderr prefix, empty stdout, retire refusing without --force and clearing with it.
- Run level: exit 3 as declared, quiescent true.

The falsifier is NOT triggered. The run completed, reached quiescence, resumed rather than restarted, and opened real pull requests. c29 and c30 clear on their own declared words; c28 does not, because one PR per MSP failed at two of three and the merge half is still with the user.

Instrument anomaly to hand the lead: engine-pin-post-check printed DIVERGED-CHECKSUM and then result=ok on IDENTICAL hashes. The agents and prompt-snapshot checksums did move during the run, and the second line reporting ok looks wrong.