CLOSE-OUT of the live end-to-end audit, 2026-08-17. User approved a thorough e2e test with disposable repositories and minimal intervention, then mid-run redefined the stop line to root-cause depth and a live-usability verdict (0539), deferred the fourteen-MSP scale probe, and directed that NO fixes be made this session.

VERDICT. Mitosis would NOT work if used live today. The engine structurally cannot open a pull request: Ship composes a head branch that nothing in the wired pipeline ever creates or pushes, because the ship git site carrying publish has no caller and shipPhase wires no git port. Verified end to end on both substrates, not inferred. The cause is architectural but not complexity: the system is well factored and was built module-first and never integrated. Seven findings classify as architecture, eight as localized bugs, one mixed.

WHAT SHIPPED. A disposable tier-1 substrate and a realistic tier-2 substrate (2439 LOC, 31 modules, 138 tests, two seeded bugs proven invisible to the suite); seven SPECs with expectations pinned before every run; an observability harness (27/27, two inertness mutations); a transparent passthrough timing shim (19/19, 13/13 mutations detected); guardrail, fault-injection and resume suites; live decompose, full-engine and hidden-coupling probes. 343 evidence files preserved outside the repo, plus 77 more from the coupling run and the manual collision proof. FINDINGS.md written as the durable deliverable. Two auto-memory entries added so a future session cannot route real work through the skill unwarned.

HEADLINE DEFECTS. The ship git site is unreachable (0540). green is a hardcoded literal at cli.mjs:534 that becomes a --verified line in a real PR body (0541) and MUST be fixed before ship is wired, or the engine becomes an automated fabricator of verification claims. One cli.mjs invocation advances exactly one stage while SKILL.md prescribes one call (0538). Exit zero is computed over unit state rather than shipping (0536). An unread CI reports all-shipped and a tracked test pins it (0526). integratePhase never passes mergedShas so dependency chains never resume (0526). No machine-derived dependency truth is on the engine path at all (0535). Decompose cannot represent no-work so a junk spec yields a dispatch-ready fabricated unit (0529). The exec-allowlist gate attests a mocked refusal (0525).

WHAT HELD, and should be preserved. Merge refusal at four layers, all ten specimens. The scheduler is genuinely parallel, three children within a 13ms spread. Lock survives SIGKILL with the exact remedy, retire clears it, relaunch resumes without redispatching. Redispatch budget is exactly 1. The c23 stderr contrast holds. Most importantly the model caught an undeclared dependency no static mechanism in this engine could have found, and the plan reviewer then refused the hazardous unit the decomposer invented, twice, escalating rather than approving (0542). Review is load-bearing, not ceremonial.

MEASURED. Three trivial units cost 6.49 dollars and 24 minutes; mean concurrency 1.14x because planning is strictly serial at about four minutes per unit while only Execute parallelizes. Decomposition costs about 0.70 dollars per spec and is non-deterministic, dropping a unit in two separate runs.

NOT DONE. c31 never started. The scale, refactor and bugfix probes were not run. No pull request was ever opened so PR lint, body honesty and CI-on-PR were never graded live. Tier-1 grading was partly confounded by the audit's own pin assuming a greenfield substrate, recorded rather than widened after the fact.

HOUSEKEEPING. A tier-2 cli.mjs left running past the probe ceiling was found still spending tokens with no collector and was killed by process group; its lock remains in an ephemeral directory. Every preserved script carries absolute paths into the destroyed scratchpad and needs re-pointing before reuse. A GitHub outage ran through the middle of the session; on recovery the status page still reported major_outage while every endpoint returned 200, so the forge was verified with the engine's own argv instead. The two disposable repositories hold zero pull requests and nothing was ever merged.