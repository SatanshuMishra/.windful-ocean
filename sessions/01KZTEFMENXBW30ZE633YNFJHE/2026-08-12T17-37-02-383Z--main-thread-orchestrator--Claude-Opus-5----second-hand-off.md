Continued after the first hand-off. c8 (the scalar fileScope fail-open fix to main) was built; A1 was NOT started.

c8 STATE AT HAND-OFF. Branch fix/filescope-array-guard, worktree .claude/worktrees/filescope-array-guard, three commits ending 9cb7469, cut from main at f52ed9e. Commit order is test-then-fix (85b4bba tests + todo, e4860e4 the guard, then a third commit for two HIGH review findings). Full suite 2220 = 2218 pass / 0 fail / 2 todo, exit 0, re-run and read by the orchestrator itself. The PR was being opened at hand-off — VERIFY with gh pr list before acting on it.

WHAT c8 CONTAINS. Fail-closed guards at two places: the planWaves ingestion boundary (message `task <id> fileScope must be an array`, allowing undefined/null so the existing no-scope path is preserved) and inside scopesOverlap itself (message `fileScope must be an array`). The second is what covers derive-edges.mjs, which imports scopesOverlap from wave-planner. Both messages contain the substring A0's todo test matches on. Element-level entries are refused too, not just the array shape. The A0 test file was ported whole (+244 vs A0's +201) rather than shipping only the scalar test; the reconciliation plan for the resulting collision with PR #69's file is in the PR body and the docket.

TWO REVIEW FINDINGS THAT CHANGED THE FIX. (1) Bare Array.isArray was shallow: an element like [123] only failed closed incidentally, via a raw TypeError from .replace, and this repo's own idiom for the field elsewhere (run-engine.mjs:228, divergence.mjs:37, recovery.mjs:93) is to silently FILTER non-strings — so the plausible rewrite reopens the hole with every test green. Element checks are now explicit and mirrored. An empty-string entry turned out to be ALREADY fail-open rather than incidentally closed, since ''.replace is valid and pathsOverlap('', x) simply returns false. (2) The ported A0 file asserted pathsOverlap('*.js','a.js') === false GREEN, as correct behaviour — pinning a fail-open as the contract. Both such assertions were converted to todo tests stating the desired contract. The underlying matcher was NOT changed.

MIRROR QUESTION, RESOLVED. The +1 line in .claude/workflows/mitosis.js against +3 in lib/mitosis/wave-planner.mjs is CORRECT, not a partial mirror: mitosis.js has no planWaves twin at all — it works at MSP granularity, and task-level wave planning exists only in wave-planner.mjs, reached via generate-run-script.mjs:58. Only pathsOverlap and scopesOverlap are mirrored, and mirror-guard.test.mjs:26-66 carries wave-planner.mjs as a PARTIAL row naming exactly those two exports, checked BIDIRECTIONALLY, so a later inlined planWaves copy would red on the unlisted branch. Known blind spot in that census: a NON-exported helper is uncensused, so a mirrored body could reference a helper the mirror never defines and still pass — which is why both new guards were written inline with no helper extracted.

SECOND LIVE FAIL-OPEN FOUND, NOT FIXED, DECISION PENDING. Wildcard-leading globs in fileScope match nothing. globPrefix (wave-planner.mjs:8-12) returns the text BEFORE the first wildcard, so '*.js' and '**/*.js' both yield an empty prefix; the two glob branches at :18-21 then compare against '' and fail, and the directory-prefix branch at :22 fails too, so pathsOverlap returns false. Globs WITH a literal prefix work correctly ('src/*.js' yields 'src'). Security-reviewer proved end-to-end that '**/*.js' and 'src/shared.js' co-schedule. Same class and same reachability chain as the scalar fail-open, but the fix is materially larger — correct glob matching is a feature, not a guard.

USER INSTRUCTION AT HAND-OFF. The user declined to rule on the glob fix here and asked that the NEXT session brief them first, assuming no domain knowledge: explain the problem in plain language, say whether it can be deferred or must be fixed, and answer why fileScope overlap uses wildcard PREFIX matching at all rather than a real matcher — they suspect the approach is fragile and want that assessed honestly. A1 does not start until that briefing happens and they rule.

Three fix options were drafted and put to them, and remain the live menu: refuse un-analyzable wildcard-leading globs (small, no glob engine, consistent with the module's refuse-rather-than-permit stance, and plan-to-task-graph already tells authors to prefer exact paths); implement correct glob matching (closes it properly but is a behavioural change to a safety-critical matcher with real regression risk); or ship the todo tests and defer (cheapest, but main carries a second live fail-open for the life of the stack and A2's ready_after work lands on top of it).

STILL TRUE: 1 of 18 MSPs shipped (A0, PR #69, open and unmerged). Stack base feat/mitosis-os-process untouched at 482dc7d. Nothing from the stack has reached main.