2026-08-20, live e2e phase. The c28 live run passed at the merged engine pin: failures=0, three units shipped as three real pull requests, ship.parked empty, every declared expectation green, and the retarget instruction emitted correctly. The merge half then completed for the first time without losing a stacked child: pull requests 1 and 2 merged, pull request 3 retargeted onto the trunk with the engine's own emitted instruction, the parent branch deleted and confirmed gone, pull request 3 still open and mergeable, then merged, with all three units' content asserted present on the substrate trunk rather than inferred from a MERGED label.

Then the harness itself was put on trial and failed the user's bar. Four blockers were found between that green result and a trustworthy live run. Blocker 1: the primary checkout does not contain the tested engine, so a live invocation today emits no retarget instruction at all - safe to fix, since the checkout branch is already an ancestor of main and its dirty files are identical branch-versus-main. Blocker 2: the CI loop reads a workflow conclusion before the run settles, so ci-unwatched is the expected outcome of every live run; measured live, all three pull requests the engine opened were BLOCKED by the receipts enforcer on G13 and the engine never noticed, and two were then merged red on merge commands that carried no check-read step. Blocker 3: three contracts disagree on the rationale that becomes the pull-request why value, so a live decomposer can park a fully built and billed unit at Ship. Blocker 4: the run document has never gone from the decomposer into the engine - the Decompose phase inside the engine is a no-op, the real producer is a separate executable outside the engine's import closure, and the hand-authored document omitted prep, judgment, isolation and task, which is why the plan, review and security dispatches have never fired and why measured cost read one dispatch per shipped unit.

Blocker 3 shipped as pull request 256, all seventeen checks green. Blocker 2 shipped as pull request 257, green everywhere except G14, driven from nine survivors to one. A first test-prune batch shipped as pull request 259. All three await a human merge. A fresh substrate was created and confirmed pristine, and a new spec-to-trunk harness was authored with twelve declared criteria, including that every opened pull request's checks are green with receipts included, that the engine observed CI, and that the plan, review and security dispatches actually fired. The harness runs the engine from the live checkout rather than a pin, and gates on capability rather than a commit id.

A third defect surfaced while building it: the engine discards the plan and judgment dispatch envelopes before they reach the usage recorder, so its own cost accounting under-reports; the harness reports the engine's number alongside an honestly derived minimum. The test audit classified all 124 mitosis test files and found only one that fails the user's bar, which reframes the diagnosis: the suite is not mostly wrong, it is mostly narrow, and no amount of pruning would have caught these defects - only the end-to-end instrument can.