Cleared the three filed defects as three independent MSPs off base origin/feat/mitosis-os-process = ed5ccbb9, stopping before the restack by instruction. G0 acceptance was pinned per MSP before any code, in handoff-2026-08-12-mitosis-os-process/G0-ACCEPTANCE-three-defects.md.

First correction: the local feat/mitosis-os-process ref was 114 commits stale. The real base is origin/feat/mitosis-os-process = ed5ccbb9, which absorbed PRs 111 through 117. All work branched from the remote ref, never the local one.

PR 118, fix/determinism-lint-nul-escape, commit b1df769b. Escaped three raw C0 bytes: the NUL separator at determinism-lint.mjs:319, a BEL hostile-input specimen at tests/ci-escalation.test.mjs:268, and a NUL inside a copy-verbatim fenced block at docs/plans/2026-06-29-mitosis-02-plan-to-task-graph.md:176. Added a closed census over every tracked file. The reported symptom half-reproduced: git renders these files as TEXT because its binary heuristic scans only the first 8000 bytes and the NUL sits at offset 14232, but grep genuinely skipped the module, returning no output and exit 1 against ten real matches. Fixed file returns 10 and exit 0. CI fully green including receipts and test.

PR 119, fix/exec-chokepoint-spawn-sites, commit b04c4659. Routed run-store.mjs:343, generate-run-script.mjs:86 and :90, dispatch.mjs:299 and lib/git/pr.mjs:347 through assertSpawnAllowed before any child starts. gh-merge-shim.mjs:369 stays unrouted by design as the enforcement backstop. Corrected three prose copies of the stale five-sites claim under G15. Correction to the filing: no currently reachable attacker-controlled argv exists at any of the five sites, so this closed an architectural gap, not a live exploit.

PR 120, fix/gate-module-load-diagnosis, commit 0468d1cb. The defect as filed does not reproduce at all, recorded as decision 0440. Shipped the real adjacent property instead: the gate body moved to mitosis-gate-core.mjs and mitosis-gate.mjs became a 73-line entry that loads it through createRequire inside a guard, framing a load failure with the verb and the module. runMitosisGate stayed synchronous, so the filed async ripple across 20 test blocks never happened.

Suites re-run independently per branch with unmasked exit codes: 3037, 3039 and 3029 tests, exit 0 each, one pre-existing todo at wave-planner.test.mjs:137.

The bound receipts enforcer ran for the first time and PASSED on all three PRs under claim.require_receipt_for any-source-change with G11 and G14 at block. That open unknown is now closed.

Known conflict, documented in handoff-2026-08-12-mitosis-os-process/MERGE-SEQUENCE-three-defects.md: PR 119 and PR 120 both touch mitosis-gate.mjs and conflict there. PR 118 is clean against both. Merge 118, then 119, then 120, and when resolving 120 carry 119's one-line EXEC_ALLOWLIST_NOT_ATTESTED correction into mitosis-gate-core.mjs. It is prose inside an 805-line move and nothing turns red if it is dropped, so assert it after merging with git grep for "no verb of this gate censuses".

Still open and out of scope by instruction: the base is 54 commits behind origin/main (G8), C5b restacks onto the cleaned base, and C6 is still parented on the orphaned 3ff7c990 and needs an explicit rebase --onto.

Tooling hazard worth carrying: the agent Write path converts a typed backslash-u escape into a raw control byte, and the Bash gate rejects a command carrying one. That is the plausible recurrence mechanism for this defect class; build such escapes through perl chr or String.fromCharCode and verify with od before running.