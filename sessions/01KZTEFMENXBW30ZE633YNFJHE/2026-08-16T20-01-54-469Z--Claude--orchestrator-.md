Session opened to rule c7 and found the ground under it false, so nothing was ruled and two units shipped instead.

WHAT THE SESSION FOUND. 0466 had closed c6 believing the D3 envelope survived the pool seam and run-store could write per-attempt usage. Mapping what a real run needs showed the instrument was never connected: run-store.recordUsage had no non-test caller at all, engine.mjs tickDispatcher discarded the outcome object before pool.mjs could carry the envelope, and cli.mjs realPorts.runUnit forwarded it on the success path only. A run commissioned as planned would have dispatched real children and written no token, cost, or dispatch data anywhere. Separately, no durable dispatch record existed either, so the count was recoverable only from a live stdout capture that counts units rather than dispatches and misses every retry.

WHAT SHIPPED. c24, commit 5c7c509a on feat/d3-instrument-wiring: the envelope now survives tickDispatcher, the engine path calls recordUsage, and usage.jsonl carries one line per started dispatch. Red on parent 2b9e8f0c with three failing assertions, three scoped inertness mutations, suite 2607 to 2610 with zero failures, determinism verb unchanged. An advisory review then tried to construct a miscount and could not, confirming the count logic sound, but found a reachable path where a billed dispatch records as unbilled. Filed as c25 rather than reopening c24, and shipped as commit 1b87838a: post-dispatch record failure keeps the envelope under its own POST_DISPATCH_RECORD_FAILED label, observers are individually isolated with the durable writer first, the summary names runKey and attempt, and both envelope variants carry one key set. Four new tests red on 5c7c509a, inertness mutation reddened only its own assertion, suite 2614 with zero failures.

NEITHER BRANCH IS PUSHED. feat/d3-instrument-wiring sits 2 commits ahead of feat/mitosis-os-process locally, and no pull request exists.

WHAT DID NOT HAPPEN. No engine run. No clone. c7 stays open and the falsifier stays unmeasured.

IN FLIGHT AT HAND-OFF. An implementer is working fix/secret-scanner-anthropic-keys in .claude/worktrees/scanner-keys, off main at 343ff14a, fixing a confirmed live gap: secret-scanner.sh matches sk- followed by alphanumerics, so the hyphens in sk-ant-api03 keys mean an Anthropic key writes through unblocked, reproduced at exit 0 where 2 is required. Its acceptance is the repro at exit 2, a first-ever test suite for the hook including a clean-input case, red on parent, and an inertness mutation. Check git -C .claude/worktrees/scanner-keys log --oneline to see whether it committed; there is no shell to kill, and its result was not read before this hand-off.

WHAT THE NEXT SESSION INHERITS. A survey of six candidate units for the measurement run, of which the flat triad D, E and F was chosen: tests for the semgrep canonicalize script, for impeccable isGeneratedFile, and for impeccable path helpers. The only genuine dependency edge the survey found was the scanner fix paired with its own test suite, and it left the run with the fix, so this run exercises dispatch volume without scheduler ordering.