C7 shipped as PR #134 (feat/c7-loop into feat/mitosis-os-process), 11 commits, 1372 insertions and 162 deletions across 23 files. Acceptance was pinned before work started in .claude/docs/specs/2026-08-15-c7-scope-and-ceiling.md, committed as 33f0f277.

Two recon agents ran first. One mapped the substrate and found engine.mjs and cli.mjs did not exist at all, that the SPEC's and the disposition record's line citations had drifted, and that "ports onto A2's pool" was not a port: pool.mjs settles each node exactly once over a static DAG while the incumbent scheduler re-dispatches per epoch, serializes dynamically on overlapping fileScope.edit sets and gates on a live build-ahead window. The other found redispatch was already a registered prompt kind, making residual 7's option (b) free, and that SPEC section 2.3's row was factually wrong about it.

Four decisions were ratified: 0450 redispatch stays a judgment kind with the six mechanical remediation attachments detached; 0451 the engine keeps the tick loop and composes runGraph per tick; 0452 the phase order inverted after phase 1 proved obligations are downstream of the process move; 0453 the three prompt-contract mismatches resolve without breaking the verify.scopedCheckCmd surface.

Three Opus leads executed in sequence, each fanning out Sonnet workers. Phase 1 landed B3-remainder, the redispatch detachment and a test-terrain fix, and empirically established the sandbox wall that forced 0452. Phase 2 built the substrate and cleared all ten of its items, including the ceiling's one unverified question: dispatch.mjs:653 already refuses a schema request answered without structured_output. Phase 3 landed 0453's three resolutions, closing two live defects, corrected the SPEC, and honestly re-filed seven of nine obligations to D2 rather than forcing them through by weakening mirror-guard.

Verified at f8bebb41: npm test exit 0 with 3265 tests and 0 failures, all four gate verbs exit 0, mirror-guard green. Every fix shipped red on its parent commit, green on its own, with an inertness mutation observed directly.