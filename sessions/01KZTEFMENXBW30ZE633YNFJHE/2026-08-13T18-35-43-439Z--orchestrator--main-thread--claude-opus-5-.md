Shipped A4 and B2 through one dynamic Workflow, per the standing directive in 0385. Eleven agents, 894 tool calls, 2.81M subagent tokens, ~2h05m wall clock, zero agent errors.

SHAPE. Two lanes pipelined so B2 never waited on A4: plan (writes a durable plan file, zero repo edits) -> implement (TDD, rewrite-simulating mutations per 0380, mirror in the same commit) -> split-role review in parallel -> one bounded fix round, only if a CRITICAL or HIGH exists -> a fresh verifier that re-runs the suite, the gate verbs and at least two mutations itself before pushing and opening the PR. Second review lens chosen per lane risk: security-reviewer for A4 because it IS the exec and merge boundary; codebase-analyst for B2 because its dominant failure mode is an incomplete migration, not an incorrect one. Worktrees .claude/worktrees/a4-guarantees and .claude/worktrees/b2-context-packs were cut by the orchestrator from base 7d6fb4e before launch; the primary checkout was fenced out of every prompt.

SHIPPED. PR #82 feat/a4-guarantees head 788390b, 31 files +1744/-264 across 14 commits, 2541 tests 0 fail, all four gate verbs exit 0. PR #81 feat/b2-context-packs head a2348d1, 33 files +1281/-633 across 6 commits, 2495 tests 0 fail. Both OPEN, not draft, base feat/mitosis-os-process, base unmoved at 7d6fb4e. Nothing merged.

ORCHESTRATOR-VERIFIED, not taken on the workers' word: both remote refs equal the claimed SHAs via ls-remote; both worktrees git-status clean; both diffstats read directly; PR state, base, head and draft status read via gh; hygiene scanned over added lines only, finding zero emojis, zero authored code comments, zero AI attribution in diffs or commit messages, and the only non-ASCII being em-dashes in prose and error strings. NOT re-verified by the orchestrator: the suite counts, the gate verb exits and the mutation matrices, all of which came from the per-lane independent verifiers rather than from me.

A4's substantive result. The SPEC never defined "engine source" for the determinism census and SPEC 2.1 names cli.mjs and engine.mjs, neither of which exists until D1. Scoped honestly the verb was red on the base: derive-edges.mjs:220 and generate-run-script.mjs:136 both called new Date(), ledger-lint.mjs defaulted options.now to a wall-clock read, workflow-sandbox.mjs carried the identifiers only inside denial-reason string literals, and mitosis.js had zero hits. The lane removed the entropy rather than drawing the boundary around it. See the decision recorded this session.

Evidence quality worth keeping. A4's verifier ran a false-proof probe nobody asked for and found a real residual: narrowing MERGE_REFUSAL_SPECIMENS from seven probes to one leaves exec-allowlist green, caught by only two unit tests. It is in the PR risk line, not buried. It also DISPROVED an implementer self-report - mitosis.js was claimed untouched and had in fact changed in two places, with ci-escalation.mjs changing too; the mirror was nevertheless byte-identical in the same commit and mirror-guard is green. B2 drew zero CRITICAL and zero HIGH across both reviewers, so no fix round ran; A4 drew five HIGHs, all addressed or defensibly declined in one round.

WHAT DID NOT HAPPEN. Nothing failed, but four gaps are honestly open. Receipts CI has never run on a real GitHub runner for either branch, and A4 adds three new gate legs to the deployed receipts template that were only exercised locally. Per-commit greenness was measured on neither branch, only at the PR heads; B2's implementer states commits 3 and 4 leave two test files red until commit 5. Seven of A4's thirteen mutations and seven of B2's ten were not independently re-run. And no live claude -p schema probe was spawned, by design, to keep the suite deterministic and offline.

MERGE FACTS ESTABLISHED FOR THE NEXT SESSION. A dry-run merge-tree of the two branches, touching no refs, conflicts on exactly one file: .claude/skills/plan-to-task-graph/SKILL.md. mitosis.js, derive-edges.mjs, generate-run-script.mjs and their tests all auto-merge. Auto-merge is NOT proof the mirror survives, because git merges each file independently while mirror-guard compares the pair, so the second PR to take the base in must re-run npm test and mirror-guard.

ESCALATED, NOT DECIDED. Both PRs edit .claude/skills/plan-to-task-graph/SKILL.md, which the SPEC schedules for C5 - A4 to fix the stale --at recipe (a HIGH review finding), B2 to state the pack contract. B2's shipper asked for a veto or a blessing rather than deciding it. It is also the only conflicting file, so one ruling settles both questions. Separately, both PRs run roughly three times the review-size target and neither was split, because splitting is a decomposition change that belongs to the user under the 0381 precedent.

ENVIRONMENT NOTE, transient and not a project risk. A parallel session holds the primary checkout on docs/max-autonomy-permission-spec with three commits and owns an active logbook thread, 01KZY5ARMRK0S390J8Y25X8Z72 (maximum-autonomy permission architecture), whose Wave 0 measurement agents were still running. The stop hook named that thread as the one to hand off. It was deliberately NOT touched: it is another session's live work, debriefing it would have falsely attributed this session's work to it and paused a thread mid-flight. The active-thread pointer therefore still names it, which is correct.
