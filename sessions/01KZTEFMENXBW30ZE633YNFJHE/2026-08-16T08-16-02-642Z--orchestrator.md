Overnight run COMPLETE. All six remaining MSPs shipped as one linear stack, every PR 13 of 13 green on CI and MERGEABLE, nothing merged.

#138 feat/d2a-unwire onto feat/mitosis-os-process. #139 feat/d2b-delete. #140 feat/c23-park. #141 feat/c4-gate-red. #142 feat/m1-main-fold. #143 feat/d3-measure, tip c546ebb5.

The legacy engine is deleted, origin/main is folded in and confirmed an ancestor, all three structural gates now provably fail on their own violation, the silent park reports its cause, and the dispatch envelope survives the pool seam.

Two agent corrections to the orchestrator worth carrying. First, M1 proved the four-file resurrection list was INCOMPLETE: dead-export-lint.test.mjs was deleted before the base, so a two-dot range diff cannot see it while main still carries it. The right instrument is a tree comparison, which found 17 files, 14 must-stay-absent, all verified absent post-merge. Second, the receipts red on #143 was misdiagnosed as the gates.G17 unknown-key warning; a child refused to act, made no commits, and showed the real cause was G14 with 2 of 12 mutants surviving at pool.mjs:284 and :289. Warnings render inside the BLOCK verdict line and can never block. Fixed by adding the two deny-case assertions, each proven to redden only its own mutant.

A subtlety in that G14 gap worth keeping: pool.test.mjs ALREADY asserted state failed on both paths, but it was not in the PR diff, so it was not part of the receipt G14 runs. The gap was in the RECEIPT, not the suite, which is why the fix had to land in the seam file specifically.

OPEN, requiring the human. c3 stays open until the six PRs are merged; a MERGED status is not evidence, so assert ancestry of c546ebb5 afterwards. c6 stays open as a tracked unverified-reasoned downgrade per 0466 — the falsifier is UNEVALUABLE, neither cleared nor falsified, because no engine run has ever existed to give the ratio a denominator. c7 needs a ruling: the gate's text covers cleared and falsified but not unevaluable.

Merge runbook is at the session scratchpad MERGE-RUNBOOK.md. Remove the six worktrees first or gh refuses the branch deletions; all six were verified clean with nothing unpushed. Merge bottom-up, delete each parent branch, confirm the ref is gone before the child, per 0445.

Filed and NOT fixed: gates.G17.downgrade_threshold is inert because the pinned enforcer predates the key, and nothing reconciles config schema version against the pinned enforcer SHA; the phase-parity declaredNeverCalled direction is dropped pending a C1 follow-up; the dispatchable-agent census narrowed from 7 to 5; the transcription-fidelity census lost 21 tests needing a 694-line module split; a composed path is invisible to a grep-based census; whole-value deep-equal snapshots are a change-detector class worth a sweep; 1099 ledger rows carry a windful-ocean cwd with a mismatching project label, so project alone is not a safe filter key.