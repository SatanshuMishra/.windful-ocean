Shipped three MSPs and landed five PRs. Base `feat/mitosis-os-process` moved 38be95c -> 0a0f0e3; zero PRs open at close.

ENGINE CHANGE. The nested-orchestrator impasse was resolved by driving each MSP lane from a deterministic Workflow script authored by the main thread (0385). Fifteen agents, zero errors, ~2.9M subagent tokens: one shared ruling, then per-MSP plan -> implement -> split-role review -> ship, pipelined so lanes never waited on each other.

ADMISSION TRAP FOUND BEFORE FAN-OUT. Measured every identifier A2/A3/B1 planned to export against the base: runGraph, retire, runStore, openRun, couplingReview, deriveEdges all had ZERO raw occurrences, so `dead-export-lint` would have reddened all three while SPEC 4.1 demands each be independently green. A1 had passed the gate only because "dispatch" collides with a common word in four siblings — an accident, not a precedent. Settled once as 0386 (real CLI entrypoint per module), which introduced no new shared file.

SHIPPED. A2 DAG pool PR #74 (8 findings, 7 fixed), A3 run store PR #75 (13 findings, 13 fixed), B1 coupling review PR #73 (8 findings, 7 fixed). Agents corrected their own plans rather than following them: A2 found a determinism guard comparing a value identical across runs, and a mutation that spins synchronously and hangs instead of asserting; B1 found its planned mutation inert because detectCycle runs first; A3 found two mutations its tests survived and strengthened the tests.

THEN FOUR REPAIRS. (1) run-store.test.mjs was 844 lines, over the 800 cap — split into four files plus a fixtures module, proven a pure move by diffing test-title sets and extracted bodies. (2) #73's CI failed on one Semgrep `detect-non-literal-regexp`; fixed by eliminating the dynamic RegExp, with a differential harness over 627,555 comparisons proving equivalence — and the agent correctly refused toLowerCase because regex /i uses legacy uppercase canonicalization (U+212A, U+0130 diverge, and U+0130 lowercases to two code units, corrupting index arithmetic). (3) The pre-existing derive-edges re-run defect fixed and landed. (4) #75's predicted MIRROR_CENSUS conflict resolved as one added line.

THE MISS WORTH REMEMBERING. PR #76 was stacked on `feat/b1-coupling-review`. #73 merged that branch into the base first, the branch was not deleted, so GitHub never retargeted #76 and it merged into an already-merged branch — MERGED, CI green, payload absent from the trunk. Caught only by an ancestry check run while verifying something else. Re-landed as PR #77. Root cause is structural and will recur for any future stacked PR whose base branch survives its own merge. Saved as memory `stacked-pr-merges-into-a-live-base-branch`.

DEPENDENCY STATE AT CLOSE. A0, A1, A2, A3, B1 all on the base. A4 depends on A3 (met) and B2 depends on B1 (met), so both are unblocked and mutually independent — the next parallel pair. B3 depends on A2 and B2, so it waits on B2. Cluster C untouched.

VERIFICATION. Every subagent claim was re-checked against git rather than trusted: PR states, head SHAs vs local and remote, worktree cleanliness, file line counts against the 800 cap, comment counts, and payload presence on the base by `git cat-file`. Primary checkout never left `main` and was clean throughout.