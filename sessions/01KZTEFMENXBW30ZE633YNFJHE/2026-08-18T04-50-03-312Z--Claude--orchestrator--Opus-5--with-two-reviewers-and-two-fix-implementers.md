Continuation of the same session: ran both reviewers over the ship stack, cleared what they blocked on, opened pull request 209, and the user merged it.

BOTH REVIEWERS RETURNED BLOCK, converging independently on the same defects from different angles. That convergence, not either report alone, is what made the fix round non-negotiable.

The branch-deletion power the stack introduced was unsafe in four ways. (1) The candidate set was every merged pull request under the source prefix, because the ownership check round-tripped a string the same function had just composed, so it always held; a human branch matching the shape was deletable, up to 200 per run. (2) The gate proved the FORGE-NAMED merge commit was on the trunk, never the tip of the branch being deleted - and under this repo's squash-merge default the tip is never an ancestor, so the check was structurally incapable of proving containment, and blind to any commit pushed after the merge. (3) mergeCommit was validated only as a ref token, so an oid of origin/main made the ancestry check compare a ref with itself and exit 0, meaning the safety question was never asked; the identical field was already sha-validated on the READ-ONLY resume path, so only the destructive consumer was weaker. (4) An already-merged forge status reported SHIPPED without proving this run's built content was in that merge - the inferred-from-upstream-success class this entire stack exists to close.

Separately the new live workflow handed a repository-scoped credential to code authored in a SAME-REPO pull request, reachable by the engine itself now that it opens pull requests autonomously. No token existed, so nothing needed rotating.

FIXES were dispatched against five named invariants rather than a finding list, because this project's recorded lesson is that finding-list rounds each introduce a new defect on an unnamed path. Retirement is now bound to units in this run's manifest; the branch's CURRENT REMOTE TIP must be proven contained in the trunk as a further conjunct; every sha on the destructive path is sha-validated; already-merged parks unless this run's content is proven landed; the hand-built deletion argv became a transcribed ship/retire-head verb emitting a fully qualified refs/heads ref, closing a tag-deletion ambiguity. The workflow dropped its pull_request trigger, gained least-privilege permissions, non-persisted credentials and a concurrency group, and the harness now refuses to reset a repository lacking its sentinel. Both fixes shipped tests red on their parent plus independent inertness mutations.

FRESHENED onto main (9 behind at the time) with zero conflicts, then verified the composition: suite exit 0 at 2238 passing, five of six gate verbs green. The sixth, retirement-census, exits 41 - and I established by running it on a PRISTINE origin/main checkout that it fails there identically, same nineteen sites, ZERO of them in this diff. It was declared as not-verified in the pull request body rather than allowed to read as caused by this work.

FILED, not fixed: no spawn deadline on the ship git steps, verifiedLines narrowing to its head element, an unfrozen manifest out of resume-advance, the in-process test runner intercepting above the exec chokepoint, and derived-command frozen vectors that no test executes against a real incumbent source.

PULL REQUEST 209 opened through the centralized tool and MERGED by the user. Merge verified BY CONTENT rather than by label: ship-publish.mjs and resume-advance.mjs are both present on origin/main, both new ship verbs are there, and zero occurrences of the hardcoded green literal remain.

STATE AT HAND-OFF. The primary checkout sits on main 28 commits BEHIND origin/main, so the merged engine is NOT the running engine - global lib, rules, skills and agents symlink into that working tree. Seven worktrees and six branches from this session remain and are now merged. No live end-to-end run has ever happened; the engine can open a pull request but has never been observed doing so.