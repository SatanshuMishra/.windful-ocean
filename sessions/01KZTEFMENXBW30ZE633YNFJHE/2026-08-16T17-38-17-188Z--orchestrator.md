Session close. The stack is MERGED and verified, and the branches are cleaned up.

All six pull requests merged onto feat/mitosis-os-process by the human. Base tip 2b9e8f0c. The merges went through GitHub's asynchronous stack-merge API, which REWROTE the commits: PR #143's head became fb7febde where the branch tip had been c546ebb5, so `git merge-base --is-ancestor c546ebb5` correctly returns NO. That is not a lost merge. Verification therefore had to be by CONTENT, not by SHA ancestry.

Content verified present on the base: .claude/docs/2026-08-16-d3-dispatch-measurement.md, tests/usage-envelope-seam.test.mjs, tests/mitosis-gate-red-cases.test.mjs, tests/dispatch-failure-report.test.mjs, recordUsage in run-store.mjs, and the envelope carried through pool.mjs. All four resurrection candidates confirmed still absent: workflows/mitosis.js, workflow-sandbox.mjs, mirror-guard.test.mjs, dead-export-lint.test.mjs. origin/main is an ancestor of the base, so the M1 fold survived the merge intact.

Cleanup done: six worktrees removed, six local branches deleted, six remote branches deleted. push --delete was NOT blocked, contradicting the long-standing risk entry that said the bash classifier refuses it; the permission rework main brought in through M1 appears to have lifted that. The risk is retired.

One fix round happened before the merge and it was mine. PR #143 went red on receipts and I misread the cause as the gates.G17 unknown-key warning. A child refused the dispatch, made zero commits, and showed the real cause was G14 with 2 of 12 mutants surviving at pool.mjs:284 and :289. Warnings are concatenated onto the BLOCK verdict line by emit() and can never block; only a BLOCK exits 1. Fixed by adding two deny-case assertions, each proven to redden only its own mutant, and #143 went to 13 of 13 green in one push.

The subtlety in that gap is worth keeping: pool.test.mjs ALREADY asserted state failed on both error paths, but it was not in the PR diff, so it was not part of the receipt G14 runs. The gap was in the RECEIPT, not in the suite, which is why the fix had to land in the seam file the PR introduced.

Remaining open: c6 as a tracked unverified-reasoned downgrade, and c7 awaiting a human ruling on a falsifier that is unevaluable rather than cleared or falsified.