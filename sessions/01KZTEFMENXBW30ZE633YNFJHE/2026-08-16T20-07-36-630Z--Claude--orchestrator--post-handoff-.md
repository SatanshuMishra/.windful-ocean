The in-flight secret-scanner fix named in the hand-off returned after the debrief. Recording its outcome so the next session does not have to re-derive it.

SHIPPED. Commit ac4ba5ff on fix/secret-scanner-anthropic-keys, parent 343ff14a, NOT pushed and no pull request. The repro that returned exit 0 now returns exit 2 with a named anthropic_key verdict.

THE BUG CLASS WAS WIDER THAN THE ONE INSTANCE. The briefed defect was that sk- followed by alphanumerics cannot cross the hyphen in sk-ant-. The same blindspot covered sk-proj-, the current OpenAI project key format, and github_pat_, the fine-grained personal access token format, both of which carry a hyphen or underscore inside the prefix and were therefore also unmatched. Six patterns were added in total, the three above plus xoxp- as the plain sibling of the already-present xoxb-, npm_ and sk_live_.

EVIDENCE. 25 tests. On the parent, 25 run with 16 passing and 9 failing, and the nine reds are exactly the nine new-pattern assertions, which means the sixteen green ones are genuine regression coverage over the pre-existing families rather than fresh claims. After the fix, 25 of 25 pass. The inertness mutation deleted only the anthropic_key table line and reddened exactly the four Anthropic tests while the other five new patterns stayed green, proving each assertion binds to its own table entry rather than to a shared side effect. Suite 1979 to 2004, delta exactly the 25 added, exit 0 both times. Six clean-input cases assert exit 0, so the hook is proven not to block indiscriminately.

A PRACTICAL DETAIL WORTH KEEPING. Every test fixture is built by string concatenation rather than written as a literal, because the live PreToolUse scanner runs on the write of the test file itself and would block a literal fixture. Anyone extending this suite must keep that convention or the file becomes unwritable.

FILED, NOT FIXED. First, .claude/lib/mitosis/tests/wave-planner.test.mjs:140 prints a full TypeError stack under a failing tests heading while the run still reports zero failures and exits 0; it is present identically on the parent and unrelated to this change, but it makes a green suite look red to anyone skimming the tail. Second, the length thresholds are a judgment call rather than a measurement: the tails are set to stay clear of short documentation placeholders, and the one in-repo occurrence at .claude/rules/typescript/security.md:16 does not match, but a realistic full-length fake key written into documentation would be blocked.