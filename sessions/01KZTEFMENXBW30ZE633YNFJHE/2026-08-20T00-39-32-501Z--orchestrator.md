The user merged harness PRs 3 and 4 at 00:38, in position order, as merge commits 91f99f21 and e42d02a7. assert-ancestry.sh then exited 0 with ANCESTOR_OF_MAIN for both head shas, 8480d1da for strings-titlecase and 72a9a685 for arrays-partition, read from a fresh bare mirror rather than from any cached state.

So M15's ceiling check 4 CLEARS OBSERVABLY, and the unverified-reasoned downgrade its own honest-ladder flag anticipated is NOT taken. The user performed the merges, as ruled, and the engine never merged anything.

M15's four checks, final:
1. One PR per MSP plus the pr-create trailer - SPLIT. The trailer half passed outright: both bodies carry the tool trailer and end with the machine trailer exactly, two distinct PR urls, no duplicate heads. The count half FAILED at two of three.
2. Journal retention and resume.restarted false - PASSED.
3. Six conditions reaching their pre-declared terminal states - PASSED, all of them.
4. Ancestry after the human merge in ship.mergeOrder order - PASSED, exit 0.

c28 therefore stays OPEN on exactly one thing, and it is worth stating precisely so the next session does not re-derive it: the merge-serialization half of c28 is now proven end to end on a live run, and the one-pull-request-per-MSP half is not, because numbers-clamprange parked at Ship rather than opening its pull request. Everything else that criterion asks for has been observed.

Nothing was dispatched to root-cause that park yet, by user instruction to hold.