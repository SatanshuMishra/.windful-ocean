ADDENDUM after the orchestrator's final checkpoint.

c8 SHIPPED. PR #70 https://github.com/SatanshuMishra/.windful-ocean/pull/70 — fix/filescope-array-guard, head 9cb7469, base main, 3 commits, OPEN and unmerged. Orchestrator-re-run evidence: full suite 2220 = 2218 pass / 0 fail / 2 todo exit 0; mitosis-gate phase-parity ok exit 0; the silent-filter mutation reddens all 5 element tests while mirror-guard holds 46/46, green BECAUSE both copies changed identically, which is what makes it a behavioural mutation rather than a drift artifact; reverting Guard 2 alone reddens only derive-edges while planWaves stays green, proving the two guards cover different paths. No existing caller passes a non-array: zero in tracked JSON, and all 8 scalar sites are negative tests.

BLAST RADIUS WAS WIDER THAN BRIEFED. scopesOverlap has FOUR consumers, not two, and mirror-guard.test.mjs compares normalized SOURCE TEXT, so a one-sided edit reddens at :214. Three live paths: derive-edges.mjs:78, the wave-planner.mjs:66 CLI, and mitosis.js:935 fresh-decompose clustering. generate-run-script and leases were already guarded.

THE SECOND FAIL-OPEN IS NOT A GLOB BUG, IT IS A CANONICALIZATION BUG. This supersedes the narrower glob-only framing in the previous session entry. normalize (wave-planner.mjs:4-6) is a two-regex cosmetic tidy, not a path canonicalizer: it strips ONE leading ./ via a non-global regex and trailing slashes, and nothing else. Security-reviewer executed end-to-end proof that planWaves CO-SCHEDULES two tasks that both write src/shared.js when declared as any of: src/./shared.js, src/../src/shared.js, src//shared.js, "src/shared.js " with a trailing space, or **/*.js. The glob failure is one instance of the general problem — the glob branches force a / separator so a mid-segment star never matches, and a root-level glob yields an empty prefix matching nothing. **/*.js is the worst case: the broadest-scope task receives the least protection.

INHERITED DEFECT IN THE STACK. A0's test file on the stack (PR #69) still asserts pathsOverlap('*.js','a.js') === false GREEN at lines 31 and 57 — a fail-open pinned as the contract. PR #70 converted its own copies to todo. The stack was deliberately NOT altered. Pre-agreed reconciliation, recorded in the docket: take main's copy, which is a strict superset.

ORCHESTRATOR'S RECOMMENDATION on the pending question: fix pathsOverlap in its own PR before A1 (option a). Same defect class the user just ruled worth fixing on main, live and proven, and PR #70 has already written the desired contract down as todo — leaving it unfixed ships a stated contract nobody intends to meet. Cost: a real path-matching behaviour change, mirrored into mitosis.js, affecting waves, clusters and leases, and it could change existing clustering outcomes. Alternatives it weighed: defer into the stack, where no MSP owns it today but SPEC D2 deletes mitosis.js so the mirror obligation disappears and the fix gets cheaper; or log it as an accepted risk, leaving PR #70's two todo tests red indefinitely.

Docket is 283 lines and current, carrying blast radius, the mirror constraint, all verified evidence, and open findings 3-6 (leases element types, derive-clusters id naming, overlapHolder empty-map fail-open) which are smaller and could ride along with a pathsOverlap fix.

A1 NOT STARTED, deliberately. Stack worktree clean and untouched at 9d95065.