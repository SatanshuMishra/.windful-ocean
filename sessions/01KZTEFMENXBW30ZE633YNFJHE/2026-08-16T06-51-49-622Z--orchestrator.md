Overnight autonomous run, in flight. Settled the two D2-boundary questions and a third the briefing had not caught, ratified by the user: 0460 (dead-export-lint is not revived; D2 acceptance amended by a G0 ceiling doc), 0461 (composability halt dropped, coarse-scope lint wired), 0462 (D2 splits into un-wire and delete; the reference clause is scoped to executable references). Then 0463 (one linear stack, main fold below D3) and 0464 (D3 measures the falsifier from shipped journals and downgrades the rest).

The ceiling doc is .claude/docs/specs/2026-08-16-d2-scope-and-ceiling.md, authoritative for D2 over the SPEC, and it corrects four drifted SPEC citations: mitosis.js is 5762 lines not 5515, the four sandbox tests are 129 not 28, mirror-guard is 31 not 7, and CI_ENFORCER_CHECK_TOKENS is at 2500 not 2264.

c5 verified SATISFIED and closed: redispatch has a determinate home in the frozen PROMPT_KINDS registry with the six mechanical attachments detached, recorded in PR #134's body; the three hybrid b2 sites are each named with a disposition in PR #108's body.

D2a shipped as PR #138 (feat/d2a-unwire onto feat/mitosis-os-process, MERGEABLE, NOT merged). Suite 3327 to 3334, 0 fail. Four gate verbs green, semgrep 0 findings on 17 files. Red-on-parent and inertness-mutation both proven for the coarse-scope wiring. Nine of nine criteria met, no ladder downgrades.

D2b is in flight on feat/d2b-delete off fd6d5dd3, with an amended criterion 3.2.9 pinned before it started: mitosis-gate-core.mjs:1 imports compileWorkflow from workflow-sandbox.mjs which D2b deletes, so the import and compileUnderSandbox must go in the same commit or every gate verb fails at module load.

Planned stack order, each PR based on the previous, nothing merged: base, D2a (#138), D2b, c23, c4, M1 (fold origin/main), D3. Merging is denied to the agent on every path and stays human. A merge runbook ships with the stack because 0445 is live here: a child merged while its parent branch still exists reports MERGED into a dead branch and never reaches the trunk.

Filed above the ceiling, not fixed: the phase-parity gate needed a redesign rather than a path swap and its declaredNeverCalled direction is dropped pending a C1 follow-up; the dispatchable-agent census narrowed from 7 agents to 5 because debugger and solution-architect were named only in mitosis.js string literals, so if they are meant to be dispatchable on the OS-process path some lib module must name them.

Ledger correction: the standing watch-out saying verify, models, worktreeRoot and fixLoopMax are consumed only by run-engine.mjs and engine-args.mjs is wrong on three of four. worktreeRoot, scopedCheckCmd and models are live on decompose-emit.mjs today; only fixLoopMax and verify.fullValidationCmd are genuinely dead.