2026-08-20, second half. The merge half of the earlier live run completed for the first time without losing a stacked child, and all three units' content was asserted present on the substrate trunk. Then the four blockers were closed: the primary checkout moved to main and was proven by capability rather than by a merge label, the rationale repair and the bounded CI wait shipped as two pull requests, and a fresh substrate was staged for a spec-to-trunk harness.

The first spec-to-trunk attempt crossed the decompose seam for the first time - a human-written spec produced a four-unit run document that validated, carried prep, judgment, isolation and task, and whose four rationales all passed the pull-request validator, which settles the rationale question behaviourally rather than by reasoning. The attempt then aborted because the harness waited twenty minutes for a built record that cannot appear until the whole prep phase finishes; the engine was healthy throughout and wrote a plan document 38 seconds after teardown. That teardown also leaked a billed grandchild, because it enumerated children only one level deep.

The user rejected the first repair - raising the ceiling to eighty minutes - on the grounds that ten debug rounds at eighty minutes is thirteen hours before any fixing starts. The harness was rebuilt into three cost-ordered lanes with streamed events, a live status file, liveness-based aborts and first-violation evaluation. The zero-cost smoke lane now crosses the entire pipeline in two seconds and found four contract mismatches plus an engine-side silent park on its first run.

The second live attempt failed for a reason no amount of harness work could prevent: the account crossed its provider spend limit at 20:48:36 UTC and every subsequent child was rejected with HTTP 429. Six children succeeded, four were rejected, and the limit holds until 2026-08-22 21:00 UTC. The engine handled the outage correctly - it parked every unit with a reason, wrote a complete journal and exited cleanly - but it discarded the child's own error text and its status code, writing only that the child exited 1, and it never retries a plan-stage dispatch though it retries an execute-stage one. Both are now fixed and merged. Two design questions were surfaced and deliberately left to the user: whether a dependency between two units that merely share a file should block at start or only order the merge, and whether planning should run concurrently rather than at roughly eight minutes per unit in sequence.