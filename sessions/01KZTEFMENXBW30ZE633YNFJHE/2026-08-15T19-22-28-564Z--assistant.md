Close-out. All three defect PRs merged; base moved ed5ccbb9 -> aec7d253 (#118 at 11bc4725, #119 at 5c88ed82, #120 at aec7d253). No open PRs remain. Content-landed asserted by ancestry for every merged head, never inferred from MERGED status.

Ancestry survey run at close-out, which answers the standing question about whether C5a and C5b need a verification round before C6:

- feat/c4b-git-sites: ahead of base by 0. Content landed.
- feat/c4c-gh-sites: ahead of base by 0. Content landed.
- feat/c5a-coupling-enforcement: ahead of base by 0. CONTENT ALREADY LANDED - no verification round is needed and no restack applies. Every commit on it is reachable from the base.
- feat/c5b-coupling-parity: ahead by 12, behind by 14. Real unlanded work, including 6c2d1d04, 1b8f22f4, 69634eca and the apparatus retirement merge 62200593 from PR #114.
- feat/c6-boundary-program: ahead by 71, behind by 77. Includes C5b's 12, its own roughly 9, and history inherited from the orphaned parent 3ff7c990.

So the order is forced: C5b restacks onto aec7d253 first, then C6 restacks onto the MOVED C5b. C6 must never take a bare rebase - explicit --onto only, because a bare rebase replays dozens of commits rather than its own nine, and because restacking C5b gives it new SHAs that C6 must then be re-pointed at.

Criteria c18, c19 and c20 inserted as detours and closed. Decisions 0441 and 0442 recorded this session, alongside 0440 recorded mid-session.

Four c3 risks retired as resolved or false: the raw NUL at determinism-lint.mjs:319 (fixed, c18), the exec-policy bypass framed as live arbitrary code execution (fixed and the framing corrected, c19), the ERR_MODULE_NOT_FOUND claim (does not reproduce, 0440), and "the receipts enforcer has never run under the bound config" (it has now run and passed 13 checks on each of three PRs).

One thread risk was CORRECTED rather than retired: the blanket claim that mitosis.js has zero imports and calls its own inlined twins, so a lib citation always describes a mirror. tests/mirror-guard.test.mjs:25-54 defines INLINED_TWINS as 25 whole plus 3 partial, and exec-policy, dispatch, run-store, journal-store, mitosis-gate and determinism-lint are NOT among them. For those modules the lib file IS the executing path, and citing mitosis.js instead would be wrong.

No background work left running. Worktrees defect-recon, nul-escape, exec-chokepoint and gate-load-diag remain on disk under .claude/worktrees/ and are safe to remove; their branches are merged.