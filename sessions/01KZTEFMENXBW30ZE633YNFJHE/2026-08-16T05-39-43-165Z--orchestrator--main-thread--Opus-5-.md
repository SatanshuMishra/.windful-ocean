Asserted D1's arrival by ancestry (merge commit 50cd0185 is an ancestor of origin/feat/mitosis-os-process, head diff empty), then cut the decompose-and-emit MSP that 0455 inserted, splitting it per 0456.

D1b1 - the pure run-document composer at .claude/lib/mitosis/run-document.mjs, 16 tests, no fs/spawn/clock/randomness. Shipped as PR #136, all 13 checks green, merged by the user. Arrival re-asserted by ancestry: e0c7bcc3 IS an ancestor of the base, i.e. merge-commit style not squash, which is why D1b2 could stack cleanly and base its PR directly on the trunk. Mutation M3 in that unit found a test that SURVIVED bypassing the fileScope guard - validatePromptInput was refusing the same pack downstream - so the test proved "refused somewhere", not "refused by this guard". It was strengthened to stub the composer, then went red.

D1b2 - decompose-schema.mjs and decompose-emit.mjs plus 34 tests, and the SKILL.md correction (line 13 had instructed the skill to STOP on the grounds that no step turns a spec into a run document). Eleven flags all shape-checked before any child spawns, staged exit codes 2-6, and every failure path asserts nothing was written. Opened as PR #137 against the trunk, commit b791c6ab.

FAILED, then fixed. sast blocked #137 on Semgrep detect-non-literal-regexp at decompose-schema.mjs:90, new RegExp(node.pattern). Cause of the miss: my acceptance ceilings for both units enumerated the four mitosis-gate verbs and npm test and never named sast, so it could only surface at CI. Fixed at 644fe670 by validating through a deep-frozen table of RegExp literals, guarded by a closed census that halts in BOTH directions (a patterned node with no literal, a literal claiming no node), rather than by a nosemgrep pragma - this repo already lost a round to a ReDoS in the receipts enforcer. Semgrep turned out to be installed locally, so the before/after was reproduced on disk rather than inferred from CI. DECOMPOSE_SCHEMA stayed byte-identical at 980 bytes so the --json-schema contract with the child is untouched. #137 is now green on all 13 checks and MERGEABLE/CLEAN, awaiting a human merge.

Filed above the ceiling, not fixed: the PR-composability halt and the coarse-scope lint that D2 deletes with mitosis.js and nothing carries forward; buildInitialManifest truncating title/rationale so the manifest disagrees with the untruncated unit prompt in the same file; no durable manifest-ref publish, so every emitted run is identity local-only; --out refusing a missing parent directory while cli.mjs's --journal creates one; the decompose child inheriting the emitter's cwd rather than --repo-root; the lazy pattern-halt for a foreign schema whose branch is never visited; validateRequest and validateGraph being unexported so proving acceptance needs injected doubles; buildInitialManifest not defaulting parked, replicated at three call sites.

Left alone deliberately: feat/d1b1-run-document still exists locally and on the remote, held by the d1b-decompose-emit worktree. It gates nothing now that D1b2 bases on the trunk. Branch deletion is destructive and push --delete is blocked by the bash classifier, so it joins the batch cleanup rather than being forced here.