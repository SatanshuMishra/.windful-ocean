PR #120 conflict resolved; all three defect MSPs now merged or mergeable.

User merged #118 (fix/determinism-lint-nul-escape) and #119 (fix/exec-chokepoint-spawn-sites) into feat/mitosis-os-process, which moved ed5ccbb9 -> 5c88ed82. Content-landed asserted with git merge-base --is-ancestor for both heads (b1df769b, b04c4659), never inferred from MERGED status.

#120 (fix/gate-module-load-diagnosis) then conflicted exactly as the pre-written MERGE-SEQUENCE doc predicted. Cause: #120 moves the 805-line body of mitosis-gate.mjs into a new mitosis-gate-core.mjs, leaving a 75-line CLI entry, while #119 changed one line INSIDE that moved block - the first element of EXEC_ALLOWLIST_NOT_ATTESTED, corrected under G15.

Resolved by MERGE (git merge origin/feat/mitosis-os-process), never a rebase and never a force-push, since the PR was open. Merge commit 2a57a52c, parents 0468d1cb (original, unamended) + 5c88ed82. mitosis-gate.mjs resolved to the branch's thin entry; #119's corrected string carried into mitosis-gate-core.mjs:47, copied byte-identically from the base rather than retyped.

THE TRAP, worth carrying forward: that G15 correction is one line of PROSE inside an 805-line move. A wholesale resolution from either side drops it SILENTLY and nothing turns red - the assertion reading it matches on /spawn site/, which both the stale and corrected text satisfy. A green suite is NOT evidence the correction survived; it has to be grepped for explicitly. Verified independently by the orchestrator: exactly one hit for "no verb of this gate censuses" in production source (mitosis-gate-core.mjs:47), zero hits for "every live spawn site imports" and "five live spawn sites", and #119's test assertion intact at tests/mitosis-gate.test.mjs:639.

Final state: origin/feat/mitosis-os-process = 5c88ed82 carrying #118 and #119. PR #120 at 2a57a52c, MERGEABLE/CLEAN, 13/13 CI pass, base fully contained as ancestor. runMitosisGate still synchronous at mitosis-gate-core.mjs:753; all four gate verbs exit 0. Full suite 3051 tests, 0 fail, 1 pre-existing todo at wave-planner.test.mjs:139.

Two observations filed, not fixed: (1) the receipts commit tripwire read edit state from a DIFFERENT session id and classified a scratchpad shell script as production source; (2) node's test runner prints a "failing tests:" section header even at fail 0 when a todo exists, so any CI gate grepping that pattern misfires.

Still open and untouched by instruction: the restack. Base is 54 commits behind origin/main (G8), C5b needs restacking onto the cleaned base and C6 onto C5b with an explicit --onto, since C6 is still parented on the orphaned 3ff7c990.