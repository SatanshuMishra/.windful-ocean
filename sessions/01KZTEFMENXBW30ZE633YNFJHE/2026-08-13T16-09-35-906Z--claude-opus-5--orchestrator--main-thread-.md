Verified the base, cleared the five merged MSP branches, cleared A1's style debt, and closed the three termination holes 0387 filed. Base ended at 7d6fb4e with zero open PRs.

BRANCH CLEANUP. Verified origin/feat/mitosis-os-process was 0a0f0e3 with zero open PRs and pool/run-store/coupling-review all present. Deleted nine branches (a0, a1, a2, a3, b1, derive-edges-carryforward, plus the filescope, pathsoverlap and spec detours) with their worktrees, after proving each merged. origin/feat/b1-coupling-review was one commit ahead at 4fd1138; git diff base 4fd1138 returned 2627 deletions and ZERO insertions, proving its tree a strict subset of the base, so deleting it lost nothing. Also found the local base 40 commits stale at aff3314 and fast-forwarded it.

A1 STYLE DEBT (PR #78, squashed as bf7c2db). runChild went from 190 lines / 6 nesting to 21 lines / depth 3; dispatch.test.mjs (838 lines, over the 800 cap) split into five files plus fixtures. dispatch.mjs is STANDALONE in MIRROR_CENSUS with zero references from mitosis.js, so there was no mirror obligation.

THE INVERTED PREMISE. 0384 deferred the refactor "guarded by the existing dispatch tests". An inertness battery of eight plausible unsafe rewrites measured that FALSE: seven stayed green. Only the escalated flag reddened. M2/M3 are redundant-by-construction and M7 provably inert, but M1/M4/M5/M8 are genuine holes. Recorded as 0387. The refactor shipped on other evidence: line-by-line review, the PARENT's unmodified 95-test file run against the refactored dispatch.mjs, and 185 unchanged string literals. Closed M1 in #78 since the extraction created that seam.

TERMINATION GUARDS (PR #79, re-landed as #80, squashed as 7d6fb4e). Added guards for liveGroups release, abort-listener removal and drain-timer registration; 96 to 99 tests, no production code touched. An independent agent reproduced all three mutations and REFUTED the author's explanation of the M8 collateral: the orphaned drain timer kills the group at t=2504ms, before the at-exit sweep runs, so the assertion was reworded to name the broader oracle. It also found four environment couplings, all fixed (bare PATH node, anchor orphaned on early throw, an 8s/15s band giving false reds, alive() throwing on EPERM).

WHAT FAILED. PR #79 merged into a live already-merged base and never reached the stack base - the #76 incident exactly. #78 had been merged before the merge command ran, so --delete-branch had nothing to delete and GitHub never retargeted #79. My own verify step made it worse: I gave an ancestry check on the pre-squash SHA, which a squash merge guarantees will fail, so it printed a false STOP that was correctly overridden. Recovered by cherry-picking the stranded 393b67e onto the base as PR #80, tree byte-identical. Root cause found: every branch here is worktree-held, and gh pr merge --delete-branch fails on those and leaves the REMOTE alive too. Swept the whole repo afterwards and deleted eight more merged-but-alive branches; no merged-but-alive remote branch remains.

Seven orphaned local branches remain with zero unique commits; a delete command was handed to the user, unrun at hand-off. Three pre-existing grandchild-spawning tests in dispatch-termination.test.mjs leak setInterval processes and never assert the grandchild dies - noticed, not touched.