Ran the whole-solution stack's first two waves by lead-agent fan-out. SHIPPED NINE UNITS: M1, M10, M12a, M14 merged to origin/main at c29829a2 (verified by content); M0 (PR 219), provenance fix (220), M12b-1 (221), M2 (222), M12c (223), M7 (224) open and green, all awaiting human merge. Every worktree ends with zero uncommitted and zero unpushed work.

WHAT WENT WRONG AND WAS FIXED MID-SESSION. The stack grew 16 units to 24 because the orchestrator routed every blocked lead to the architect, and an architect asked to resolve a block returns design, which is more units. Four of five additions came from that loop. The user called it and the stack was FROZEN at 24 (0595): architect retired, blocks ruled by the orchestrator in one exchange, discoveries go to BACKLOG-not-this-stack.md and never become units, one reviewer and one round per unit. A RUNBOOK.md was written so a dispatch is "read the Standing Brief and your row" rather than the ~800 words retyped into every earlier dispatch.

TWO TOOLING DEFECTS THAT SILENTLY DISABLE VERIFICATION. All four receipts ladder tags are matched as text anywhere in a PR body and short-circuit all eight re-run gates - PR 215 returned PASS in 129ms, PR 221 withheld the tags and ran a real 67s. Filed as a gap against the standard, never patched locally. Second: the PR tool could not express this session's own model id, so every machine PR carried a false model string; fixed in 220 after an investigation proved the bracket ban was never an injection control - working Markdown links already render in five sibling fields of the same immutable document.

THREE LEADS BLOCKED AND ALL THREE BLOCKED CORRECTLY, none widening and none faking a green. M12a found a test pinning a file by name; M2 found the acceptance leg unsatisfiable from its own file set, which produced the phased expand-migrate-contract rename; M0 proved its ceiling self-contradictory by running three designs. M7 refused a direct instruction to ship and was right: ship-with-downgrade covers a verification gap, never breaking the target branch, and an agent cannot waive a user-level invariant on another agent's word (0596).

WHAT THE LEADS CORRECTED IN THE ORCHESTRATOR. M2 measured that this repo merges with merge commits, not squashes, contradicting the routing rationale given to it. M12c measured that the "undetectable" vocabulary collapses were detectable repo-wide and undetectable only inside two files. M7 measured both trees rather than assuming its 22 failures were inherited. The architect corrected its own importer inventory from five to eight after its grep was piped through a line-oriented filter that showed only a multi-line import's closing brace - the third short inventory of the programme.

A session usage limit tore down four agents mid-work; state was re-derived from git rather than from their last words, nothing was lost, and all four resumed.

NOT DONE: waves 3 through 9, nineteen units. Nothing was dispatched after the wind-down instruction.