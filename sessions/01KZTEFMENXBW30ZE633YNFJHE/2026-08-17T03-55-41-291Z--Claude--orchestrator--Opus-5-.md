Shipped seven pull requests, all merged: #145 secret-scanner key formats (c26); #146 D3 instrument wiring (c24, c25); #147 the unit verdict sha contract (0484); #148 the StructuredOutput grant to main (0488); #150 the dispatch-time schema-capability assert; #151 the D3 measurement republished with real figures; #152 the 303-commit integration of the whole stack into main, content-verified with zero reversions and the legacy .claude/workflows/mitosis.js retired. The primary checkout was fast-forwarded so all of it is in force, not merely merged.

Root-caused three defects that each independently made a run impossible, none of which any prior session knew about. First, the engine could not complete a single unit: decompose-emit never set request.schema, so no --json-schema reached the child, no structured_output came back, shaOfVerdict returned null and requireSha threw. Fixed by a default UNIT_VERDICT_SCHEMA attached only under worktree isolation (0484). Second, --json-schema is silently dropped unless the dispatched agent's tool set admits StructuredOutput; the fix for that had existed on a dozen feature branches and had never reached main, and ~/.claude/agents is a symlink into the primary checkout, so it was in force nowhere (0488). Third, the dispatchable-agent-schema-capable gate censused the directory beside its own module rather than the one the CLI loads, which is why #147 could truthfully report the gate green while real dispatch degraded to prose; closed by a fail-closed assert at dispatch time.

The first real engine run in the project's history then completed: two units done, quiescent, two billed usage lines, ratio 1.0 against a ceiling of 10, cold 85853 and 87587 against warm 1151513 and 1328844, about 2.50 USD across three dispatches. Recorded CLEARED at n=1 with the vacuity caveat inside the verdict (0489).

What FAILED is the thread's actual goal, and it is the important part of this log. The engine reaches only checkpointed and green. cli.mjs imports no prompt registry, so all thirteen registered prompt kinds are unreachable; the engine never opens a pull request and gh-merge-shim refuses every merge verb; only Decompose and Execute of four modeled phases were exercised; scope-fence isolation cannot complete a unit at all; and among 101 test files there is no automated end-to-end test. The eighteen-MSP stack shipped by manual subagent dispatch under 0378, so the staged-pull-request workflow has never once been run by the engine. c7 cleared a falsifier that could only ever return the architecture's structural floor, so clearing it proved nothing. The user ruled this a thread failure rather than a successor task; 0494 re-scopes the thread and supersedes 0475, and c27 through c31 now carry the remaining engine work with c32 and c33 as deferred follow-ups.

Three separate receipts G14 blocks were cleared the same way each time, by adding a test that pins an exact value instead of asserting not-the-old-value; no production code was touched and no gate was weakened. #152 merged with receipts red on G11 by admin override at the user's ruling, with the per-file justification for the eleven retired test suites recorded in 0492 instead of the pull request body, because pr-create has no flag that emits a test-removal line and a body is immutable after creation.