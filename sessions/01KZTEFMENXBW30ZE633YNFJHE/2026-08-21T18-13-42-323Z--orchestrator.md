Session 2026-08-21: four billed single-lane runs, three shipped fixes, and a SPEC that reframes the whole effort.

WHAT RAN. Free smoke against trunk 05b29b5e passed. Billed probe confirmed the spend cap had already reset, so the lanes ran a day earlier than the ledger expected. Four billed single-lane runs followed, each surfacing exactly one terminal defect.

Run 1 (757s): harness built-wait heartbeat watched .mitosis/plans/, which nothing writes during Execute. It killed a healthy engine 16s into a review dispatch, 17s after the implement child had successfully committed real code. Fixed: watch dir moved to the whole .mitosis tree, idle limit raised to 900s (above the engine's own 600s dispatch timeout). Proven with an offline fake-process check, red then green.

Run 2 (787s): heartbeat fix held, no false stall. The implement child found a genuine hole in the fixture spec (truncate formula goes negative when suffix exceeds maxLength) and parked NeedsHuman. Engine behaved correctly. Fixed by disambiguating both fixture specs to throw RangeError, grounded in the specs' own stated convention.

Run 3 (621s): parked plan-unapproved. Root cause was not the budget value: PLAN_REVISION_BUDGET existed as a constant but runPlanning was hardcoded to exactly two rounds, so changing the constant alone would have been a silent no-op. Loop rewritten to honour the constant. Shipped as PR 272, merged.

Run 4 (984s): first run to traverse the entire pipeline with no abort marker. Planned through three revisions, implemented, reviewed, recorded built - then the resumed invocation reported nothing-pending and opened no pull request. A built unit was dropped at the resume boundary. Six of eleven declared criteria failed, all downstream of that one loss. Filed as 0662; NOT fixed this session.

ALSO SHIPPED. PR 271 (node_modules symlink) merged, with the caveat recorded below. PR 272 merged. Both are on trunk c2992f78; the primary checkout is still at 05b29b5e and must be fast-forwarded before anything reads the engine.

WHAT FAILED, AND THE HONEST CAVEATS. PR 271 does not demonstrably fix what it was filed to fix: its own implementer reported that addedWorktree is the only worktree creation the engine owns, while the child creates its worktree from prompt text. The test is real and red-then-green, but on a path that may not be the one that committed the symlink. M5 of the new SPEC exists partly to settle this. Run 4's receipt describes a worktree at cafae8fb, not trunk, so nothing in it is evidence about main.

THE TURN. The user identified the pattern: each run reveals one new defect, we fix it, the next run reveals another. Diagnosis reached this session, recorded as 0663: every automated test of mitosis as a whole replaces the model with a stub that always succeeds, instantly, and does nothing. The smoke stub is 123 lines with a verdict vocabulary of approve, pass, and a canned implement success. Because it cannot produce a failing input, missing engine code and missing coverage became indistinguishable, and the engine was written to satisfy the stub. All five defects found this session are explained by that one cause.

A measured constraint reshaped the fix: recordUsage persists only cost and tokens, so what a model actually returned is never written to disk. No cassette can be harvested from any past run, which is why the SPEC's first MSP teaches the engine to record verdicts.

DELIVERABLE. m15/TESTING-ARCHITECTURE-SPEC.md, 542 lines, eight MSPs, no placeholders. Self-review corrected four claims that would have forced a rewrite: run-store.test.mjs does not exist (tests are split per verb), the dispatch-kind enum was partly invented and is now marked derive-before-implementing, planResume is async with five required request fields, and the row-8 fixture now sources from the real preserved journal rather than an authored shape.

EVIDENCE AT RISK. Four preserved scratch directories under /var/folders/tz/.../T/m15-single-live.{XVIlQv,JfZ9lA,qtphU4,MjnrRd} hold the only journals and usage records from these runs. They are macOS temp directories and will be garbage collected. The SPEC's M1 step 0 copies them into the repository before any code is written; if they are gone by then, the JfZ9lA park record is the one that matters most.

NOTHING LEFT RUNNING. No lanes, no engine processes, no monitors. Substrate SatanshuMishra/mitosis-full-e2e-substrate is clean: zero pull requests, only main and _ledger.