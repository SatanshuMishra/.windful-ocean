Resumed at C4c per preflight. Dispatched orchestrator run 6 (general-purpose, opus, backgrounded) at ORCHESTRATOR-BRIEF.md + STACK-STATE.md. Verified the whole chain against git before dispatching: linear f252fef7 -> 18d5e4c9 -> 65a696a4 -> fb195e47 -> 4656b8ad -> 7f0e7513 -> 6acc7171, all local == origin, six PRs open with the recorded bases.

SHIPPED. C4c: branch feat/c4c-gh-sites, head 3ff7c990, PR #108, base feat/c4b-git-sites. 38 files, +3468/-103, all inside .claude/lib/mitosis/. Suite 3123/3122 pass/0 fail/1 todo (true exit 0, parent 3050/3049 so +73). All seven gate verbs exit 0. Census 18 target sites / 18 converted / 0 unconverted. mitosis.js and run-engine.mjs byte-identical to parent; allowlists unchanged at five binaries. Both reviewers APPROVE-WITH-FIXES, 0 CRITICAL / 5 HIGH, seventh consecutive independent convergence, now recorded on disk in C4c-SHIPPED.md rather than only in chat.

The five HIGHs were real. The worst: a reviewer rewrote a git diff read into `git push --force origin HEAD` and all seven verbs plus the full suite stayed green. Fix round framed as seven invariants rather than eleven findings; censusing found +4 declaration classes, +1 counted claim, +1 program-path binding and +1 re-declared copy beyond what was reported. The orchestrator then found one more survivor the fix round missed (the shared-step comparison sourcing `expected` from the sibling builder rather than the frozen fixture, making it f(x) === f(x)), fixed and re-killed at exit 41.

RESTACKED. The user merged #100-#103 mid-run, moving the base from f252fef7 to d11f7693 and leaving #105 CONFLICTING. Run 6 rebased the C4 chain onto the merged base with --onto, replaying only each branch's own commits: c4a bdada212, c4b e78a5ced, c4c 3ff7c990. Zero conflicts, all three trees byte-equal to their pre-rebase values (90d630d9, 247c5314, af115150), pushed with --force-with-lease. #105 flipped CONFLICTING -> MERGEABLE. I verified every head, ancestry link and tree SHA myself.

TWO STUDIES COMMISSIONED, both of which corrected me. (1) A measurement across all seven shipped MSPs FALSIFIED my claim that MSP size is the structural cost driver: at n=7 the only relationship reaching p<0.05 is stack position, and size, position and gate-verb count are collinear at rho 0.857 and inseparable. What did hold: review is a FLAT ~42-minute cost per MSP, varying 1.6x while size varies 9.6x, rho = -0.252. Consequence: splitting an MSP multiplies review cost rather than dividing it, so splitting is a quality move and never a speed move. (2) External research found the project's own 200-400 LOC rule is weakly sourced -- the SmartBear breakpoint is ~200-250 with the 300-400 half an explicit hedged inference, the famous "70-90% defect discovery" figure is marketing and absent from the study, Google's guidance is ~100 lines, measured medians across major orgs are 11-44, and the DORA citation is a genuine mismatch measuring delivery performance rather than review defect detection. Corrected in PR #109, merged.

ALSO CORRECTED. My instruction to the orchestrator pinned C4c's restack target tree to 3426c09e, measured at ce5a40a8 BEFORE the fix rounds. The orchestrator caught it: using that value would have halted the restack spuriously or, worse, certified the branch against a pre-fix tree, silently attesting that the five HIGH fixes had been dropped. It re-measured at the head it was actually rebasing (af115150). Separately I misdiagnosed a live fix round as dead because the worktree was clean and HEAD stable -- it was a lull between commits, and the child had been resumed via SendMessage, which has no run_in_background parameter and genuinely runs in the background. Memory corrected.

RULED. Three C5/C6 lane rulings given and written to RULINGS-2026-08-14-C5-C6.md: C5 splits into C5a/C5b and ships the eighth verb; C6 does not extend the CI verb matrix; .claude/skills/ byte-cleanliness holds through C6 with D1 as the designed write point.

ENDED BLOCKED, and this is the open problem. The user merged #105 and #107 as well, force-pushing c4b from e78a5ced to 7dfd0757 before merging it. Both SHAs carry the IDENTICAL tree 247c5314 -- the content is the same. But c4c is built on the old SHA, which is no longer an ancestor of the base, while the new SHA is. Git therefore computes a distant merge-base, tries to re-apply c4b's changes onto a base that already has them, and reports PR #108 as CONFLICTING. Zero content conflict; pure ancestry. Run 7 had already cut feat/c5a-coupling-enforcement and feat/c6-boundary-program from 3ff7c990, so both inherit the same orphaned ancestry and must ride along with c4c's rebase.