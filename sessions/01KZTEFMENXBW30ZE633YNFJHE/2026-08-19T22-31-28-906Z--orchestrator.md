Sessions 2 and 3 of RUNBOOK AMENDMENT F, run as pure orchestration: one delivery-lead per unit, one reviewer one round, ladder statuses returned to the orchestrator and never written into a PR body.

MERGED TO MAIN, each proven by content rather than by a MERGED label:
- M6 one terminal-state function, PR 245. Red on parent: merge-policy returned 'partial' where nothing-pending was required. Ruling 4 (a run with unwatched CI no longer reports as shipped) was ambiguous on the exit code; ruled ci-unwatched is a hand-off status - word withheld, exit stays 0 - because the exit-3 reading flipped 14 tests across 5 files and violated ruling 4's own bounded-lines text.
- M6's G14 pins, PR 246. Its receipts run BLOCKed at 137s naming 6 surviving mutants in describeValue; the remediation was pushed 53 seconds AFTER the human merged, so it never reached the trunk. Landed as a fresh branch off the updated trunk.
- M13 decompose no-work, PR 247. The enforcer independently re-ran the receipt on both shas.
- M11 argv path validation, PR 248. Added a second inertness mutation because the spec's named one did not exercise the one-resolver criterion.
- M17 status-mirror contraction, PR 249, receipts a real 103s run.

M17 STOPPED FIRST, correctly. Built to A6's letter the literal census halted on 45 of 75 closed-set literals, and worse was blind to the three mirror writes carrying no literal - so zero occurrences was reachable with the mirror intact. The user moved the ceiling to the read census. On the second round the reviewer proved the retained literal census falsifiable by turning it red against a probe tree, which is what makes it a census rather than a tally.

M15 phase 1 authored, reviewed twice (a BLOCK with three CRITICALs that would each have wasted the run, then an APPROVE) and validated 12 items offline. Two findings outrank the script: the 2026-08-18 live run invoked the engine through ~/.claude/lib and therefore tested the primary checkout's branch, 84 files and +3275/-4217 from main; and the run prompts had no credential clause, while the gh token is user-scoped with delete_repo and repo. Both closed - a SHA-pinned detached worktree, and a PreToolUse deny hook proven by a real child's write being denied.

Four leads independently measured that mitosis-gate.test.mjs:529 reddens npm test locally on an unmodified parent while CI is green. Local-only noise; cause unverified.