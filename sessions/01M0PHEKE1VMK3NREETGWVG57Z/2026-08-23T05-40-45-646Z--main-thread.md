2026-08-22: pull request 281 merged mid-flight and swallowed the follow-up commit.

## The sequence

05:33:20 the test job failed on head 5e483633, on two pinned-byte fixture tests, not on the fix itself. 05:34:38 the pull request was merged by the human at head 5e483633, before the fixture re-pin existed. The implementer then pushed 48f6a3045fc03df6947f9d08a58b5c49db7c0eab to the same branch.

That commit reached origin/fix/mitosis-implement-prompt-artifact and never reached the trunk. A merged pull request's head is frozen, so a later push to its branch is silently swallowed and the API keeps reporting the stale head.

The implementer OBSERVED the exact symptom and misread it. It checked git ls-remote, saw 48f6a304 present on the remote, saw the pull request API still reporting the prior sha, and attributed the disagreement to a brief API cache lag. It was not lag. It was the merge. The observation was correct and the diagnosis was wrong, and nothing in its instructions told it the pull request could be merged out from under it mid-round.

## Verified state

- git ls-remote origin refs/heads/fix/mitosis-implement-prompt-artifact = 48f6a304.
- Merge commit bfd3d733 carried prompt-execute.mjs and prompt-side-effects.test.mjs only.
- git diff 48f6a304 origin/main -- .claude/lib/mitosis/tests/fixtures/prompts/ is non-empty: two files.
- CI run 32620697557 on main at bfd3d733 fails on exactly the two fixture tests and nothing else in that job.

## What the merge did achieve

The trailing-slash node_modules artifact test is GONE from main's failure list. The fix itself works on the trunk; only its fixture collateral is missing.

The security workflow also went green on main at bfd3d733. That is the self-clearing behaviour predicted in 0676, not a fix: the scanner diffs the previous trunk sha to the head, and the offending commit now falls outside that range. A full-history scan still reports it, and every future recorded run reproduces it, so c1 remains real work.

## Remedy dispatched

A fresh branch test/mitosis-repin-implement-fixtures cut from origin/main with 48f6a304 cherry-picked, as a new pull request. Pull request 281 cannot be reused; its head is frozen. The old branch is NOT deleted yet, because it is the only place that commit currently exists.