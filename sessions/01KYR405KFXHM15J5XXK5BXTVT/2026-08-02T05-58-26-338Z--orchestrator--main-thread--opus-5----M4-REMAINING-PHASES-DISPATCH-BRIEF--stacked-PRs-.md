M4 REMAINING PHASES. COMMIT 1 IS DONE - DO NOT RE-DO IT, do not re-cut the branch, do not revisit the deletion scope.

STATE. Branch feat/m4-fixed-build-ahead-cap, checked out in the MAIN worktree, ONE commit 777617b (13 files +57/-207). NOT PUSHED, no PR. Suite 1839 pass / 0 fail measured LOCALLY; CI is green on 9ea75d6 but has NEVER run on 777617b. Working tree carries ONLY the five pre-existing dirty paths. Four dirty worktrees remain by design. Nothing running.

USER DIRECTIVE 2026-08-02, SUPERSEDING THE SINGLE-PR SHAPE: run ALL remaining phases as ONE dedicated SMALL DYNAMIC Workflow, and ship via STACKED PRs. That directive is the explicit opt-in the Workflow tool requires; it is in the user's own words and needs no further gate.

STACK SHAPE. PR 1 = feat/m4-fixed-build-ahead-cap -> main, carrying 777617b alone. PR 2 = a NEW branch cut off feat/m4-fixed-build-ahead-cap carrying the instrumentation commit, opened with --base feat/m4-fixed-build-ahead-cap. Both satisfy the green-branch invariant standalone: commit 1 is green at 1839/0 and commit 2 is purely additive. Merge stays HUMAN-GATED for both. GitHub retargets PR 2's base to main when PR 1 merges, so do not hand-edit it - and PR scope is frozen at creation anyway.

PHASE 1 - COMMIT 2, THE INSTRUMENTATION, per 0198. Per-run locals at the existing per-id reset loop (mitosis.js:3949-3967 AT 9ea75d6 - RE-LOCATE BY SYMBOL, commit 1 moved every line number in that file), logged ONCE, emitted ONLY on the path where planReconcile actually ran. A fresh run never enters that block and must not print counters it never measured - that is the section 1 defect class the M5 latency emitter already had to be corrected for. rebuildUnitCount is available mitosis.js-only as advance.toParkSubtree.length; if the divergence count is not derivable from planReconcile's existing return shape, adding ONE integer to that return is acceptable and lands in BOTH twins in this same commit. NO TEST - section 11 refuses one and a log-string assertion is a change detector. Do not let an agent add one.

PHASE 2 - ADVERSARIAL VERIFY, fenced read-only, re-running the decisive mutations itself rather than trusting the implementer's report. PRIMARY TARGET: the three e2e tests commit 1 DELETED rather than updated (H2 repro, H2 dedup + foreign filter, bullet 6). Prove the claimed replacement coverage at frontier-train-e2e.test.mjs:1148/:1167/:1267 actually asserts the duplicate-accepted and foreign-branch-filtering invariants, and prove by mutation that the two KEPT-and-retitled WINDOW_CEILING clamp tests still redden on a real regression rather than passing trivially. SECOND: the input.buildAheadCap override at mitosis.js:3483 is reachable and its present-but-invalid refusal actually fires. THIRD: mirror-guard green and the full suite green on EACH commit separately, not only on the tip.

PHASE 3 - SHIP, HARD-GATED IN THE SCRIPT on the verifier's verdict so nothing pushes if verification fails. Push both branches, then open both PRs through pr-create ONLY.

STANDING CONSTRAINTS. 0194 governs ordering absolutely: NEVER instruct an agent not to commit; every lens runs AFTER the commit producing what it measures. Stage explicit paths only, NEVER git add -A; the five pre-existing dirty paths stay untouched. Distinct scratch subdirectory per agent. Never patch a committed artifact to run a mutation - mutants live in scratch copies selected through MITOSIS_PATH. The engine is .claude/workflows/mitosis.js, NOT under .claude/lib/superpowers-parallel/. pr-create is the only PR path, requires --provenance with --origin machine, rejects angle brackets in --what, and never write a Verified: line for a check that was not run.

EXPLICITLY NOT M4'S, all measured this session and left deliberately: the dead reviewDecision payload at mitosis.js:1667/1671 and :3800; advance.toRestack/toOpen dead weight; the .mitosis/ gitignore defect; the two-ledger-stores config defect; the M3 termination residual.