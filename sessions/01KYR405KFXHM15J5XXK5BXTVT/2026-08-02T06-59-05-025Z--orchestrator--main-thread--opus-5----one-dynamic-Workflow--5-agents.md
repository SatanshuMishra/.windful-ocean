M4 COMMIT 2 LANDED; THE SHIP GATE CLOSED ON THREE FAILS. NOTHING PUSHED, NO PR OPENED, NOTHING RUNNING.

WORKFLOW wf_3d4cd7c3-1b5 ran all remaining M4 phases per the 2026-08-02 stacked-PR directive: 1 implementer, 4 adversarial lenses in parallel, ship hard-gated DETERMINISTICALLY in the script on all-four-PASS. 5 agents, ~587k subagent tokens, 39 min. Journal: ~/.claude/projects/-Users-satanshumishra-Documents-DevLabs--windful-ocean/83cd5055-.../subagents/workflows/wf_3d4cd7c3-1b5/journal.jsonl. Script: .../workflows/scripts/m4-remaining-phases-wf_3d4cd7c3-1b5.js (resumable by scriptPath + resumeFromRunId).

SHIPPED. Commit fc035fc "feat(mitosis): ..." on NEW branch feat/m4-divergence-instrumentation, cut off 777617b. Three files: mitosis.js, reconcile.mjs, tests/reconcile.test.mjs. Purely additive, no comment, no new test, no applyRunDelta change. Suite 1839/0 local, twice (npm test + the pre-commit hook). Only explicit paths staged; the five pre-existing dirty paths untouched.

TWO ORCHESTRATOR MEASUREMENTS FED INTO THE DISPATCH, both confirmed by the implementer. (1) The rebuild-unit count §3.3 asks for was ALREADY emitted at mitosis.js:3966 as toParkSubtree.length; no second emitter was added, so only the divergence count is new and the diff is smaller than 0198 assumed. (2) The divergence count is NOT derivable from planReconcile's return shape, so 0198's pre-authorized ONE-integer addition applies and landed in both twins with reconcile.test.mjs:202/203/221 updated.

THE JUDGMENT CALL I FLAGGED WAS RESOLVED WELL. invalidatingParents counts merged parents whose invalidation ACTUALLY FIRED (invalidated.length > 0), taken from descendantsToInvalidate's OUTPUT rather than re-deriving its rule as verdict !== 'clean' — so the counter follows the oracle instead of becoming a second authority for it. Mechanism read at mitosis.js:2452-2455 + parking.mjs:52-55: [] iff clean, so missing and indeterminate invalidate exactly as divergent does. Coverage stated in the log line's words rather than as a second field. Proven by mutant: emits 1 on the :571 two-parent scenario (one divergent, one clean); moving it outside the isRelaunch block reddens frontier-train-e2e.test.mjs:1260, so the honesty fence is live, not assumed. Mirror-guard proven able to redden by running one twin unchanged.

THE GATE CLOSED: 3 FAIL / 1 PASS. All four lenses proved their probes could redden.

FAIL 1 — DELETED e2e COVERAGE (decision D's primary target, and D's suspicion was CORRECT). Two of the three deletions are sound. The third dropped the FOREIGN-BRANCH-FILTERING invariant, and NOTHING in the suite asserts it today. The claimed replacement cites :1148/:1167/:1267 are WRONG — none touches either invariant; the same-url dedup actually survives at M2b. Lens drafted the restoring test (next to M2b, existing helpers, no window fixture) and proved it green on the engine and red on a classifyRunOpenPRs mutant that falls back to the first built unit when branchToMspId returns null.

FAIL 2 — PER-COMMIT GREEN, and this is the one that would have shipped red. BOTH 777617b and fc035fc FAIL the required invariant-coverage CI job ON THE pull_request EVENT: neither adds a file under docs/invariants/coverage/. A BARE invocation of scripts/invariant-coverage-check.mjs silently takes the weaker PUSH path, which is why every prior green reading — including the lens's own first pass — missed it. Re-verify only with GITHUB_EVENT_NAME=pull_request GITHUB_BASE_REF=<base>. Remedy must be an AMEND to each commit, not a follow-up, or 777617b itself lands red. Shape: docs/invariants/coverage/<branch-slug>.json, {"rows":[{id,verdict,check}]} covering all 12 registry ids B1-B6/M1-M6 from docs/invariants/registry.json; the gate validates shape and id-set completeness, so a stub fails. npm test, phase-parity and mirror-guard ARE green on both commits independently.

FAIL 3 — OVERRIDE REFUSAL, narrow but real. input.buildAheadCap: reachable, valid overrides work, no durable read survives (0197 held). But null is refused SILENTLY — an explicit `&& buildAheadCapOverride !== null` clause at mitosis.js:3475 suppresses the log. Blast radius exceeds literal null: the engine JSON.parses its args at :3460-3461 and JSON.stringify maps NaN/Infinity to null, so a computed garbage cap (parseInt(env)*2) is swallowed with zero operator signal. Safety never violated — the cap holds 8 in every invalid case — so this is the observability/honesty class. Remedy VERIFIED BY EXECUTION: delete ONLY the null clause, KEEP the undefined guard; a mutant removing both guards CRASHED on the absent case, so the naive edit is wrong. Cosmetic sibling at :3476: clean() already stringifies, so clean(JSON.stringify(v)) double-encodes.

PASS — KEPT CLAMP TESTS. Both genuinely redden on a real cap regression; retention holds, no deletion warranted. BUT the compound claim is half false: the `window: 9999` premise both titles rest on is provably INERT (the engine reads manifest.window nowhere), so the fixtures are dead data and the titles overclaim a property the tests cannot observe. What they actually pin is BUILD_AHEAD_CAP == 8 end-to-end. Fix is tests-only: drop the inert fixture arg (:894, :1247) and the unused frontierManifest window param, retitle both, update the assertion messages at :913 and :1268. Do NOT try to restore a "persisted window is ignored" test — under the new design that property has no engine surface and any such test would be unfalsifiable by construction; the guarantee is now structural, which is stronger.

NUMBER CORRECTION THAT PROPAGATES BACKWARD. 1839 is ENVIRONMENT-LOCAL: it includes ~9 tests from a gitignored file present in the main worktree. A clean checkout (the lens used fresh git worktrees) and CI measure 1830. Both lenses reported this independently. Every 1839/1846 figure in this thread's prior entries is a main-worktree figure, not a CI figure.

WHAT I DELIBERATELY DID NOT DO. No fix round was dispatched. Four findings across three surfaces is exactly the shape the standing memory names — five prior rounds each introduced a new defect on an unnamed path — and remediation must be planned against invariants, not against this finding list. Context was also at 81%.

USER DIRECTIVE FOR THE FRESH SESSION, issued 2026-08-02 after reading the verdicts: for EACH issue above, dispatch DEDICATED AUDIT AND RESEARCH SUBAGENTS FIRST to fully understand the CORE issue and its solutions, and only THEN dispatch implementing subagent(s). Audit-then-implement is the ordering; do not go straight to the drafted remedies even though three of four are already drafted and mutation-proven.