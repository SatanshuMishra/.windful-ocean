CORRECTION AND THE CONTROLLING FACT FOR THE NEXT SESSION. The user merged PR #25 at 22:57:54 UTC, so all three PRs are merged. But #25's base was still test/mirror-census-closure, so it merged THERE, not into main. Verified by execution, not inferred: merge commit 99eb137 is now the tip of refs/heads/test/mirror-census-closure on the remote; origin/main is still 8933c2c; and a path-scoped diff of origin/main against 05bfa7f over status-facts.mjs, parking.mjs, mitosis.js and the P2 coverage entry shows 4 files, +197/-28, i.e. main does NOT carry P2's content.

So M2's MSP is currently stranded on a side branch. P1 and P0 did land in main (squash-merged as 438ac8c and 8933c2c).

THE TRAP FOR WHOEVER PICKS THIS UP: do not just open test/mirror-census-closure -> main to fix it. That branch's tree never contained P0's work, so the diff would carry the DELETION of docs/superpowers/plans/2026-07-31-m2-mirror-gaps.md, the .gitignore negation hunk and docs/invariants/coverage/docs-track-m2-mirror-gaps-plan.json out of main. This is the same squash-merge asymmetry that stranded P2 in the first place: 7b8acd2, fc418de, 731a0d1 and now 05bfa7f are none of them ancestors of origin/main, so content that IS in main is not in these branches' histories and every cross-diff reads as a revert.

RECOMMENDED REMEDY, not yet ratified by the user: cut a fresh branch off origin/main at 8933c2c and cherry-pick P2's nine commits onto it in order - f04b8de, 9363558, 6b11f25, 9e36674, dcb1a46, b7b54b0, 5684271, ef92657, 05bfa7f - then open ONE new PR to main. This needs no rebase and no force-push, so it clears the destructive-git gate on its own, and the resulting PR diff is exactly P2's content. The alternative, merging origin/main into test/mirror-census-closure first and PRing that, also works but ships a merge commit and a wider diff. A rebase of the existing branch is the one option to avoid without explicit confirmation.

All three head branches survive on the remote (test/mirror-census-closure at 99eb137, docs/track-m2-mirror-gaps-plan at 731a0d1, fix/m2-monotone-status at 05bfa7f). Delete none of them until main is proven to carry P2.

A note on how this was caught: gh pr list returned only #25 as open, which looked wrong, and querying each PR number directly is what surfaced that #23 and #24 had already merged. The instinct to explain a surprising listing rather than accept it is what turned up the mis-targeted base too. Sixth instance this thread of a reported or apparent state needing independent verification.