AUDIT F3 (buildAheadCap silent refusal) RETURNED. Only F2 remains outstanding. Nothing implemented; thread stays paused. THIS IS THE RESULT THAT JUSTIFIES THE WHOLE AUDIT-FIRST DIRECTIVE.

HEADLINE, AND IT IS NEW: buildAheadCap: 9999 IS SILENTLY ACCEPTED, widens the build-ahead frontier with NO upper bound, and emits NO log at all. The FAIL 3 report never mentioned it. It contradicts the headline of the very commit under audit -- 777617b calls itself "a FIXED build-ahead cap", and the cap is not fixed. The drafted remedy makes the SAFE direction (refusal, cap holds at 8) louder while leaving the DANGEROUS direction silent.

IT IS A CLASS, NOT A CLAUSE. buildAheadCap is the ONLY scalar knob at the arg boundary that accepts invalid input and proceeds. fixLoopMax, retry.maxAttempts, retry.runBudget, models, the ref fields and the path fields ALL halt via haltReport('input', ...) -- measured by executing the real validator lines (mitosis.js:3716-3755; fixLoopMax :3743-3745, retry.* :3746-3751). So the invariant at stake is the boundary's own halt convention, and this knob opted out of it.

BEHAVIOUR MATRIX, produced by running source lines EXTRACTED FROM THE FILE, not a hand copy: absent -> 8, no log. null -> 8, NO log. NaN/Infinity via JSON round-trip to null -> 8, NO log. "banana"/0/-1/1.5 -> 8, LOG fires. 4 -> 4, no log. 9999 -> 9999, NO log, frontier widened. The 9999 path is proven LIVE, not theoretical: running the COMMITTED leases.mjs planTick with 8 units already built, window=8 withholds the child and window=9/9999/1e6 admit it. buildAheadWindow consumes windowSize verbatim with no clamp (leases.mjs:110-112, whole twin at mitosis.js:2335).

THE CRASH MECHANISM IS NOW NAMED. Deleting both guards throws because JSON.stringify(undefined) returns the VALUE undefined and clean(undefined) then calls .replace on it. Absent is the HOT PATH -- every run without the override -- so the naive edit crashes every normal run, not an edge case.

THE DOUBLE-ENCODE IS NOT COSMETIC. Measured: 0 renders "0", 1.5 renders "1.5", "4" renders "\"4\"". The log that exists to be honest about operator input cannot distinguish the number 0 from the string "0" -- it degrades the exact signal this finding is about. And clean(undefined) throws with OR without it, so removing the double-encode does not make the undefined guard droppable.

TWO STRUCTURAL FACTS THAT NARROW THE FIX. (1) NO TWIN: buildAheadCap appears only in mitosis.js repo-wide; the remedy mutant passes mirror-guard, so no twin edit is required -- BUT buildAheadWindow IS a whole twin, so any read-site clamp would need both files. (2) NO EMITTER: buildEngineArgs feeds a different program and ENGINE_ARG_NAMES omits buildAheadCap; args arrive via the sandbox hook from a hand-authored Workflow invocation. So the NaN-to-null vector the finding leaned on has ZERO in-repo instances and is hypothetical. The realistic vector for a literal null is a human or LLM writing "buildAheadCap": null to mean "no override".

THE null CONVENTION IS SPLIT, so the drafted remedy invents a third behaviour. Collapse camp (null equals absent): engine-args.mjs:48, fixLoopMax via ??, verify/build/models via ||{}. Distinguish camp (null halts): retry.maxAttempts and retry.runBudget. Deleting only the !== null clause yields log-then-proceed, matching NEITHER camp.

RECOMMENDATION IS OPTION B, NOT THE DRAFT. Fold buildAheadCap into the existing validator: keep mitosis.js:3473, DELETE :3474-3477 entirely (the eager const, the guard, and the log with its double-encode -- the cosmetic issue disappears with it), and add a retry.*-shaped check at :3752 before modelsKnobCheck, declaring buildAheadCap there. Safe because the only read is at :5050, far below. mitosis.js only. Verified green on the full 447-test MITOSIS_PATH suite including mirror-guard. Zero tests reference buildAheadCap today; 777617b deleted tests/window.test.mjs.

A USER DECISION IS REQUIRED AND MUST NOT BE MADE BY AN IMPLEMENTER. The recommended accepted range is 1..BUILD_AHEAD_CAP, so the knob may only NARROW the frontier. That upper bound is a POLICY CHOICE: 777617b's BODY says the override is "refused and logged when it is not a positive integer", which implies unbounded acceptance of any positive integer was INTENTIONAL, while its TITLE says the cap is fixed. Title and body contradict each other and the auditor could not resolve authorial intent. Option D -- delete the override entirely, since it is undocumented, untested, has no emitter and no caller -- was judged legitimate and simplest but explicitly a product call for the user, not an auditor.

PLACEMENT: F3 says AMEND 777617b (it introduced the clause; fc035fc's diff touches buildAheadCap zero times), and requires the rebase. TALLY IS NOW F3 AMEND, F4 AMEND, F1 NEW-COMMIT-ON-TOP. F3 adds a DECISIVE argument for amend that F1 could not have known: it wants 777617b's COMMIT MESSAGE corrected, which is inaccurate in both directions (null is refused but not logged; 9999 is not refused at all). A follow-up commit cannot fix a prior commit's message; only an amend can. Weigh that against F1's SHA-preservation preference at reconcile time.

F2 IS THE LAST OUTSTANDING AUDIT AND IS THE TIEBREAKER on whether the invariant-coverage gate evaluates per-commit or per-PR-head, which decides whether 0201's "amend, not a follow-up" forbids F1's shape.

RE-VERIFY AFTER (F3's list): npm test at 1839 local / 1830 CI with 0 fail; mirror-guard under MITOSIS_PATH; per invariant M3 an acceptance test RED on the 777617b parent and GREEN on the fix, asserting null, a non-integer, 0 and 9999 each HALT; then npm test at BOTH commits independently after the rebase.

F3's FALSIFIERS: a real operational need to set the frontier above 8 kills the upper bound (the commit body arguably already asserts that need); an out-of-repo caller passing null to mean "no override" makes halt-on-null a breaking change; a fourth proceed-on-invalid knob would shift the framing from "one outlier" to "the validator shape is wrong", favouring a table-driven validator. It did not run a live end-to-end orchestration.