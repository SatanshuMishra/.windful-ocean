Resumed on user go. Asked the 0138 PR question once; user authorized PRs and chose plan-lands-first. Decisions 0139, 0140, 0141 recorded.

CI archaeology corrected two brief claims before any branch was proposed. (1) "NO CI RUN EXISTS for any branch" is FALSE - test and security runs exist on all three branches and invariant-coverage already ran green in push mode on feat/invariant-coverage-gate (run 30585613111). Only the pull_request-event path stays unproven. (2) The sast red on the docs and gate branches is NOT branch-dependent and NOT a regression: security.yml takes its diff baseline from github.event.before, which is all-zeroes on a branch's FIRST push, so the baseline resolves empty and semgrep full-scans, surfacing 3 findings pre-existing on main. Later pushes to the same branch get a real baseline and pass. A pull_request event uses pull_request.base.sha, so PR-event sast is diff-aware and will not reproduce them.

REALIZED RISK, now closed. The pinned-realm risk fired on its first exposure to a second Node. Local node v26.4.0 has Temporal compiled out of V8 (--harmony-temporal already defaults on, still absent); CI's node-version 26.x resolves to v26.5.1, whose realm global carries it. The totality check added by 3e59d05 refused to build the sandbox. Fixed by classifying Temporal in ALWAYS_DENIED, committed as 2f4ee4d and pushed. Verified by a mutation harness that installs Temporal on the realm global with a real global's descriptor shape: RED reproduced the CI message byte-for-byte, GREEN showed a bare Temporal inside the sandbox raising ReferenceError, which proves the name was actually deleted and passed the CONTEXT_INSTALLER survived-check rather than merely being filtered out of the classification error. Local suite 1741/1741 before and after; no test added, because prunePlan's totality check is already the regression detector.

Opened PR #16, docs/two-track-invariant-plan -> main, via pr-create. Spec grepped clean of the confidential codename; secret-scan and sca green on that head.

NOT DONE: the registry source rewrite on the gate branch, the gate PR, and B-6.