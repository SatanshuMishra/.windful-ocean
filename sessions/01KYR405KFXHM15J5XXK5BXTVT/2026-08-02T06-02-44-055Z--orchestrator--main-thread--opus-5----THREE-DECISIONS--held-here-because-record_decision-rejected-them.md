TOOL DEFECT, READ FIRST. record_decision rejected six consecutive well-formed calls with "must have required property 'options'" and/or "'outcome'", while a minimal probe with tiny values succeeded and wrote decision 0199. DECISION 0199 IS A VOID PROBE ARTIFACT - title "probe", body "probe". It carries no meaning, must NOT be linked in key_decisions, and should be superseded or deleted when the tool is fixed. The three real decisions below were locked this session and are recorded HERE, in the append-only session log, because that path works. They should be promoted to numbered decision records once record_decision accepts long field values. Treat them as binding regardless of where they live.

DECISION A. M4 SHIPS AS TWO STACKED PRs, RUN BY ONE SMALL DYNAMIC WORKFLOW.
Context: M4 was contracted as two commits on one branch behind one PR. Commit 1 (777617b) is landed and green; commit 2, verification and ship remain. The user directed on 2026-08-02, after commit 1 landed, that the fresh session dispatch a dedicated small DYNAMIC workflow for ALL remaining phases and ship via STACKED PRs. 0198 already required the instrumentation to be a second purely additive commit so each commit leaves the branch green alone.
Options weighed: single PR with both commits behind one review; two stacked PRs; two independent PRs off main.
Outcome: TWO STACKED PRs. PR 1 carries 777617b alone to main; PR 2 carries the instrumentation on a NEW branch cut off feat/m4-fixed-build-ahead-cap, with that branch as its base. The stack makes 0198's reviewability rationale real rather than notional - a 13-file deletion and a small additive emitter are reviewed as separate units. Both hold the green-branch invariant standalone. INDEPENDENT PRs OFF MAIN ARE REJECTED because commit 2's counting site sits inside the block commit 1 rewrites, so a main-based branch could not be green before commit 1 lands. Merge stays HUMAN-GATED for both; GitHub retargets PR 2's base when PR 1 merges; PR scope is frozen at creation. The user's directive is also the explicit opt-in the Workflow tool requires.

DECISION B. THE reviewDecision CAPTURE DIES WITH AIMD; THE GROUNDING MAP'S RATIONALE IS REFUTED.
Context: the dispatch brief said KEEP the reviewDecision capture at mitosis.js:3089 UNLESS grep proved no other consumer, because the grounding pass believed it also served provenance/contested logic. The implementer ran that grep. The accepted map is written at :3089 as { url, reviewDecision }; every other read site (:3091, :4290, :4297-4298) touches only entry.url, and the sole reads of entry.reviewDecision are at :3118-3120, both inside the events loop M4 deletes. The provenance and contested branches read pr.isCrossRepository, pr.url, pr.headRepositoryOwner and pr.headRepository, never pr.reviewDecision.
Options weighed: keep it on the stated rationale; delete it because the UNLESS clause fired on measurement; delete it and narrow the schema and gh fetch in the same commit.
Outcome: DELETED. The UNLESS clause fired exactly as written and the map's causal claim is REFUTED - the sixth instance in this thread of a confident finding failing against the mechanism, and the reason the brief carried a measurement escape rather than a flat instruction. The now-dead external payload is deliberately NOT narrowed in M4: mitosis.js:1667/1671 still requires reviewDecision in the openPRs schema and :3800 still asks gh to fetch it. That is the external-payload contract, a different layer from the accepted map, with its own blast radius. It belongs to a later MSP and is named so it is not rediscovered as a surprise. The grounding map remains the anchor authority for line cites but is now known wrong on this one causal claim.

DECISION C. THE .mitosis/ GITIGNORE PREMISE IS FALSE AND THE INERTNESS RESULT SURVIVES IT.
Context: 0197's second precondition and the dispatch brief both justified proving applyRunDelta's behavior on an unknown record kind by asserting .mitosis/ is gitignored and per-machine, so pre-M4 journals carrying window records exist here. The implementer measured the premise: find returns nothing for .mitosis/ and git check-ignore -v .mitosis/run.jsonl prints NOT IGNORED, because .gitignore has no .mitosis entry. mitosis.js itself logs the gitignored claim twice in the quiescent-exit latency messages.
Options weighed: treat the false premise as invalidating the precondition and re-open the deletion; accept the measurement because it was proven by execution and record the premise as false; fix .gitignore and the log strings inside M4.
Outcome: THE MEASUREMENT STANDS, THE PREMISE IS RECORDED FALSE. The precondition was discharged by two-arm execution against a scratch copy - unknown kinds fall through to return manifest with no throw and no mis-fold, and every record after a legacy window record still applies - so the result never depended on such a journal existing. The premise only motivated the check; the check answered itself. Nothing re-opens. TWO RESIDUALS NAMED AND LEFT, neither M4's: .mitosis/ is not gitignored, so an engine run would leave untracked journal files in the working tree, and mitosis.js logs an unbacked claim to the contrary twice. Fixing them inside M4 would widen a frozen scope. This is the second stale claim this session that a log string or comment asserted and the code did not back, after the frontier-train-e2e rationale about hardcoded events.

DECISION D. THE THREE DELETED e2e TESTS ARE THE VERIFIER'S PRIMARY TARGET.
Context: the brief enumerated nine AIMD window assertions and said all nine are UPDATED. The implementer instead deleted three whole tests and kept two it was not told to keep. Deleted: H2 repro and H2 dedup plus foreign filter, because both also assert on the window-checkpoint prompt, undefined once persistWindowCheckpoint is deleted, so assert.match throws and the tests lose their subject; and bullet 6, whose only remaining assertion duplicates the C1 frozen-PR test verbatim. Kept and retitled: two WINDOW_CEILING clamp tests, argued to still assert that a persisted window of 9999 never widens the frontier past 8. Claimed replacement coverage is at :1148/:1167/:1267. The suite is green at 1839/0 with the delta accounted, but green proves the suite loads and passes, not that coverage was preserved.
Options weighed: accept on the reported rationale; accept provisionally and make it the verifier's primary target; reject and require all nine updated in place.
Outcome: ACCEPTED PROVISIONALLY, SETTLED BY MUTATION NOT ARGUMENT. The rationale is defensible - a test whose subject was deleted is not preserved by rewriting its assertions, and the admission gate would refuse a stub - but this is the largest judgment call in commit 1 and was made by the agent that also benefits from the suite being green, which is exactly the shape this thread's method rule exists for. The verifier must prove by measurement that :1148/:1167/:1267 actually assert the duplicate-accepted and foreign-branch-filtering invariants the deleted tests carried, and that the two kept-and-retitled clamp tests still REDDEN on a real regression rather than passing trivially under a fixed cap. A probe that cannot be shown capable of reddening proves nothing. If either proof fails, the remedy is a targeted test in the instrumentation PR or a follow-up, never silent acceptance.