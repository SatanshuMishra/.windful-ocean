M5 WAS DISPATCHED AND HALTED BEFORE SHIP. The change is LANDED IN THE WORKING TREE, STAGED, UNCOMMITTED, and must not be discarded. Workflow wjy3c80jv / run wf_eb7805da-fe1: 19 agents, 3,554,732 subagent tokens, 1268 tool uses, 291 minutes, 0 agent errors. It ran Ground x3 / Design / Implement / Verify x4 / Remediate x2 / Ship, exhausted the remediation cap with four blocker-or-major findings outstanding, and correctly refused to ship. Script preserved at ~/.claude/projects/-Users-satanshumishra-Documents-DevLabs--windful-ocean/9dd630b0-4d10-40a1-a7ad-cee59a0ef0f2/workflows/scripts/m5-quiescent-exit-wf_eb7805da-fe1.js; journal at the sibling subagents/workflows/wf_eb7805da-fe1/journal.jsonl; full 69k-char result at /private/tmp/claude-501/-Users-satanshumishra-Documents-DevLabs--windful-ocean/9dd630b0-4d10-40a1-a7ad-cee59a0ef0f2/tasks/wjy3c80jv.output. Nothing is running.

PREFLIGHT, done in the main thread and all of it confirmed rather than assumed. origin/main re-derived at 42e7d49, both required checks green (test, security), read after the merge. Every anchor the dispatch brief named was re-confirmed against the origin/main BLOB, not the working tree, and all matched exactly: mergePoll :5057, MERGE_POLL_MAX_CYCLES/WAIT/INTERVAL :4989-4991, readReviewDecision :5016, resolveReviewEvent :5028, progressPossible mitosis.js:2362 + leases.mjs:150, maxSteps :2382/:2384 + :170/:172, poll-cycle guard :2397 + :185, orphaned toRestack log :3996, reconcile.mjs toRestack :122/:130, the 4586/4592 restore asymmetry, and spec lines 75/292/296/306/314. ONE CORRECTION THE BRIEF CARRIED WRONG, and it matters for every future dispatch: the engine is at .claude/workflows/mitosis.js, NOT under .claude/lib/superpowers-parallel/. mirror-guard.test.mjs classifies BOTH leases.mjs AND reconcile.mjs as WHOLE, confirming the twin hazard was real. Branch feat/m5-quiescent-exit cut off origin/main in the main thread before dispatch; all five pre-existing dirty paths survived untouched.

WHAT IS STAGED, verified first-hand by the orchestrator with git diff --cached --name-status rather than accepted from the report: 12 paths. Modified — leases.mjs, recovery.mjs, run-log.mjs, mitosis.js, and the test files frontier-train-e2e, leases, mitosis-scheduler, recovery, run-log; DELETED — tests/merge-poll-characterization.test.mjs (the A5 instrument, deleted with the mechanism it pinned, which is the CORRECT act and not the M4 trap); ADDED — docs/invariants/coverage/feat-m5-quiescent-exit.json; modified — the quiescent-advance spec, re-baselined by execution per 0185/0148. git grep confirms MERGE_POLL_MAX_CYCLES, const mergePoll, progressPossible and maxSteps are at ZERO occurrences across both twins. HEAD is still 42e7d49. The five dirty paths (.claude/settings.json, .zshrc, two .bak files, .claude/skills/context7-mcp/) are present, unstaged, unchanged.

THE FOUR FINDINGS ARE NOT FOUR PROBLEMS. They are one substantive finding, one real one-line honesty defect, and TWO THAT THE WORKFLOW MANUFACTURED AGAINST ITSELF.

(1) SUBSTANTIVE, and it is a genuinely good catch. Termination on the no-actions path is NOT observable. The inertness verifier deleted only `return { units, ticks, quiescent: true };` from BOTH twins (mitosis.js:2384-2387, leases.mjs:158-161), keeping markAwaitingMerge, and ran ALL 60 test files under its own harness with a 240s hard cap: TIMEOUT, zero tests reddened. Repeated against leases.test.mjs alone at 45s: same TIMEOUT. The spin is SYNCHRONOUS — no await is reached when dispatch is empty — so it blocks the event loop and node:test's `{ timeout: N }` can never fire, even though the project already uses that option idiomatically at mitosis-scheduler.test.mjs:354,378,406,459,3242. The OTHER half of termination IS properly pinned: removing the epoch-guard filter reddens 'runSchedule terminates unconditionally...' cleanly, and keying the epoch on id only reddens 3 tests. So M5 as it stands deletes maxSteps while leaving the replacement guarantee unprovable by the suite, in the exact place the deletion made it load-bearing for liveness. The verifier also noted the receipt's M3 row describes only the epoch-guard mutant and does not disclose that exit-removal manifests as a hang rather than a red.

(2) REAL, one line, and mechanically uncatchable. The receipt's M6 row asserts 'git status --porcelain was re-checked afterwards confirming ... that the staged set is empty' while 12 paths are staged, and it contradicts the SAME file's M1 row, which correctly records that the receipt path was staged deliberately. Confirmed first-hand: the string is in the file and `git diff --cached --name-only | wc -l` returns 12. The M6 VERDICT survives independent check (no gate, CI or deployed artifact is among the staged paths; every mutant lived outside the repo). Only the sentence is false. Nothing mechanical catches this because scripts/invariant-coverage-check.mjs never evaluates whether a check string is TRUE (rowErrors/validateCoverageEntry, :107-137) — decision 0143's point, now demonstrated live.

(3) and (4) ARE THE SAME ISSUE AND ARE AN ARTIFACT OF THE WORKFLOW SCRIPT I WROTE, NOT A DEFECT IN THE CHANGE. I instructed the implementer and the remediator NOT to commit, and I ordered Verify BEFORE Ship. So when the verifiers ran `invariant-coverage-check --event pull_request --base-ref main` it exited 1 with 'no file under docs/invariants/coverage/ was added or modified between main and HEAD' — because merge-base..HEAD is empty when HEAD is still 42e7d49 and the receipt is staged-but-uncommitted. Push mode exits 0. The receipt's own M1 row DECLARES this candidly as an OPEN LANDING OBLIGATION and reproduces the exit-1 message verbatim, which is exactly the honesty the gate cannot enforce. The receipt gate is only meaningful AFTER the commit, so placing that lens before the Ship phase measures a condition the harness itself created. The receipt content is independently confirmed correct: its 12 row ids set-equal docs/invariants/registry.json (B1-B6, M1-M6), no duplicates, two-valued verdict domain only. A verifier even rebuilt the gate's PR clause in an isolated scratch repo and proved staged-uncommitted exits 1 while committed exits 0.

WORKFLOW-HARNESS LESSON FOR THE NEXT DISPATCH, and it is the reusable one: never place a lens that measures committed state before the phase that commits. Either move the receipt lens after Ship, or have the implementer commit and let Ship only push and open the PR. Two of four blockers, and the entire second remediation round, were spent on a condition the script manufactured. The A5 shape did not expose this because A5's receipt lens happened to pass in push mode.

USER DIRECTIVE AT CLOSE, given explicitly after reading the above: do NOT act on the orchestrator's recommended remedy on its own authority. In a FRESH session, first dispatch DEDICATED subagent(s) to AUDIT, RESEARCH and VERIFY the recommended solution; only then re-dispatch a SMALL TARGETED dynamic workflow to complete M5 and ship it. The recommendation under audit is: fix the termination-observability gap with a real bounded test rather than declaring the limit — drive runScheduleTick in a child process with a hard kill and assert normal exit, or assert an observable bound such as dispatched-epoch set size or tick count on a fixture whose planTick output is non-empty forever. The audit exists because that remedy is unproven: a child-process test is a new pattern for this suite, it must be proven able to FAIL against an exit-removed mutant, and it must not itself become a flaky or sleep-dependent test. The alternative the verifier offered — declare the limit candidly in the M3 receipt row — remains on the table and is what the audit should weigh the recommendation against.

CONTEXT NOTE. This session reached ~75% context before the hand-off, which is why the remediation was not attempted here. The remaining work is small and well-scoped: one bounded termination test, one M6 string correction, commit, push, PR. Four agents, not nineteen.