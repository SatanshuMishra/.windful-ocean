M4 IMPLEMENTED IN FULL, LOCALLY. All seven commits of the 0204 sequence are landed across the two stacked branches and independently verified. NOTHING PUSHED, NO PR OPENED. Only the ship phase remains. Nothing is running; no agent or background task is in flight.

FINAL STACK. feat/m4-fixed-build-ahead-cap over origin/main 9ea75d6: 68ee1bb (amended feat) -> d5ccf5d (F1 test) -> da36e4f (F4 cleanup) -> 58cb6ea (coverage). feat/m4-divergence-instrumentation over 58cb6ea: b8c3542 (replayed fc035fc) -> 74962a6 (0205 assertions) -> b5c34c0 (coverage). Recovery tags m4-anchor-c1 = original 777617b and m4-anchor-c2 = original fc035fc are INTACT and must survive until both PRs merge. The five pre-existing dirty paths were never touched; every commit staged explicit paths only.

0203 DISCHARGED. buildAheadCap folded into the arg-boundary validator immediately before modelsKnobCheck: halts via haltReport('input', ...) unless the value is an integer in 1..BUILD_AHEAD_CAP, absent still yields 8 on the hot path with no log, and the clean(JSON.stringify(...)) double-encode died with the deleted clause as predicted. The commit message was rewritten and is now true in both directions. Acceptance test E10 in frontier-train-e2e asserts absent->8, 4->4, and null / 'banana' / 1.5 / 0 / -1 / 9999 each halting at stage 'input'. RED on the pre-amend tree, GREEN after; the pre-amend probe printed buildAheadCap=9999 -> frontierWidth=12 against a cap of 8, which is the hole the original four-lens gate never named.

0204 DISCHARGED AS RULED. One amend for F3 plus its test; F1, F4 and the coverage file each landed as their own atomic commit. RULING MADE THIS SESSION AND RECORDED AS A DECISION: 0203's mandated acceptance test rides INSIDE the amend rather than as a fourth separate commit, because a behaviour change and the test that pins it are one atomic change; 0204's three-separate-commits enumeration covers the three UNRELATED remedies and did not contemplate this test.

0205 DISCHARGED. Four assertions added at reconcile.test.mjs:137, :156, :163 and :183. The scope fence held: the returned integer is asserted, the emitted log string is not, so section 11's refusal is narrowed exactly as ruled and not overturned. THE ORCHESTRATOR RAN THE MUTATION ITSELF rather than trusting a report: `if (false)` at reconcile.mjs:115 was 23/23 GREEN before and is 20 pass / 3 fail AFTER. The fourth assertion expects 0 and correctly stays 0 under that mutation because that case's oracle returns an empty set — by design, not a coverage gap, and the coverage row says so.

F4's DECLARED VERIFICATION GAP IS CLOSED. The F4 auditor asserted the two retitled clamp tests still redden on a real cap regression but never ran it and said so honestly. They were run: both go RED at actual 12, expected 8 under an injected cap regression. F1's mutant likewise reproduced the documented contrast exactly (no ship:l2, l2 awaiting on foreign pull/7686, status flipped) while recovery.test.mjs's branchToMspId tests stayed green on that same mutant, re-confirming by execution that the unit layer cannot express the wiring.

VERIFICATION ACTUALLY RUN, EACH STATED HONESTLY. npm test 1841 pass / 0 fail at both tips, re-run by the live pre-commit hook on every commit, never bypassed. mirror-guard 46/46 standalone. mitosis-gate.mjs phase-parity ok:true, dead:0, 13 phases. THE SANDBOX-COMPILE GATE F1 DEMANDED AND NO PRIOR AGENT EVER RAN: 129/129 across the four workflow-sandbox test files, all of which compile mitosis.js as one script body. Rebase fidelity proven the strongest available way: git range-diff m4-anchor-c1..m4-anchor-c2 58cb6ea..b8c3542 printed `fc035fc = b8c3542`, an identical replay, with no conflict markers. Invariant-coverage gate run in PULL_REQUEST mode on BOTH branches against their REAL bases (branch 1 vs origin/main 9ea75d6, branch 2 vs local 58cb6ea), both exit 0; branch 2's was RED before its coverage commit and green after, which is a genuine red-then-green on the gate itself. NOT RUN: CI on either head SHA — that is the first act of the ship phase and is deliberately outstanding.

THREE FRAMING FACTS CORRECTED BY MEASUREMENT THIS SESSION. (1) THE PRE-COMMIT CHAIN IS ALIVE, NOT DEAD — the unverified pointer the 07-35 entry flagged resolves in the thread's favour. core.hooksPath routes to the continuity plugin, which chains through continuity.priorHooksPath=.githooks to an executable .githooks/pre-commit running npm test. Caveat worth keeping: it tests the WORKING TREE, so 'npm test + the hook' is two runs of one tree, not two independent signals. (2) THE STALE-MAIN TRAP IS NOT A TRAP FOR THE GATE. Local main is 3 commits behind origin/main, but resolveBaseCommit at scripts/invariant-coverage-check.mjs:151 tries origin/<ref> BEFORE the local ref, so GITHUB_BASE_REF=main correctly pinned 9ea75d6. It matters in the other direction for branch 2: origin/feat/m4-fixed-build-ahead-cap does not exist yet, so the local ref won — which will change once branch 1 is pushed. (3) THERE ARE 15 EXISTING COVERAGE ARTIFACTS, NOT THE 16 F2 CITED. Harmless, since the gate does no filename validation, but do not quote 16.

TWO SUBAGENTS DIED MID-TASK TO API STALLS, AND THE RECOVERY IS THE REUSABLE LESSON. Both had burned their whole budget rebuilding scratch mutants for receipts that had ALREADY been executed under supervision earlier in the same session. Both times the repo was left clean and was verified so before re-dispatch. The third and fourth dispatches carried an explicit fence — cite the supplied receipts, re-run nothing — and both completed comfortably. When an agent died after doing real work (the rebase and the 0205 commit landed but its report was lost), the orchestrator RE-DERIVED the receipts itself with range-diff and its own mutation run rather than assuming the work was sound.

DELIBERATELY NOT DONE. No push, no PR, no CI read, no merge. The stop was taken at the ship boundary on the Quality-over-Speed rule for a concrete reason, not as a general caution: PR titles and bodies FREEZE at creation and are denied edit by the project's own gate, so composing them without room to read CI first would permanently bake a not-run verification line into a document a reviewer trusts by default.