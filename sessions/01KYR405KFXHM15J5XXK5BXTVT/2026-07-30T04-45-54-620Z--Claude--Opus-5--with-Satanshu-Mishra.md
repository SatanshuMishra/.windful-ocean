SHIPPED: the empirical test 0103 left open, and it REVERSED 0103. Four throwaway zero-agent Workflow probes (~30ms total, scratchpad only) tested every invocation path: inline script, on-disk scriptPath, file:// URL import, and child workflow via the in-script workflow() hook - the last being the path block-inline-engine.mjs:12-19 MANDATES for the production engine. All four returned the identical hard error "import() is not available in workflow scripts." child_process does not merely fail to execute, it does not resolve. Measured enumerable global surface: log, phase, console, budget, setTimeout, clearTimeout, Date, agent, parallel, pipeline, workflow, args. Absent: process, require, Buffer, fetch, XMLHttpRequest, WebAssembly. new Function blocked ("Code generation from strings disallowed for this context").

CONSEQUENCE: agent(prompt: string) is the orchestrator's ONLY effector; parallel/pipeline/workflow are combinators over it. Prose IS the engine's syscall ABI and the prose-as-syscall layer is FORCED, not chosen. The structural pinch: agent() exists only inside the sandbox, I/O only outside it, so the two capabilities are in disjoint compartments and prose is the only bridge either way. There is also no signal/suspend primitive, which is what forces end-and-relaunch rather than an orchestrator that sleeps.

TWO SYSTEMIC FAILURES FOUND, both live defects on the CURRENT engine: (1) frontier-train-e2e.test.mjs:16-17 reconstructs the engine body with new AsyncFunction(...) in REAL Node, where import()/process/require all work - so the suite evaluates the orchestrator in a strictly MORE PERMISSIVE context than production and is structurally incapable of failing on any sandbox violation. It has been certifying code that cannot run. (2) The await import('node:os') at parallel-plan-execution.js:27-28 is dead code on that line and cannot ever have executed under this harness; 0103 read its presence in the tree as evidence of execution.

SIDE EFFECT: the 2026-07-29 run-readiness-repair spec's section 1.5 zero-imports premise is VINDICATED - it was correct all along; 0103 was the error. That spec remains untracked in the working tree.

DECISIONS RECORDED (6): 0107 sandbox is a real harness limit, supersedes 0103. 0108 durable-execution STATE MODEL only, no runtime - the decisive argument being that every win 0106 attributes to durable execution is a state-model property, not a runtime property. 0109 speculation depth DERIVED from restack capability (depth 1 without automated restack, default 2 with, hard ceiling 3, first pipeline at 1, subtree-local pruning), supported by p^d survival arithmetic that kills the old ceiling of 8. 0110 fix work-type pipeline first, feat second, chosen on ORACLE STRENGTH because the receipt is machine-checkable and review is not. 0111 REBUILD AUTHORIZED by the user; polling removal in scope and load-bearing, with the target sharpened to "any wait that costs tokens or holds a run alive" and three categories resolving differently. 0112 robustness redefined as per-action legitimate value, with four operative rules (gates over reviews; second reviewer only for a different lens; falsification before elaboration; one oracle per property at the cheapest deciding layer).

USER DIRECTION THIS SESSION: rebuild approved. Robustness explicitly does NOT mean repeated review - reviewing something ten times is excessive; robustness means every action carrying legitimate value. Current approach is too slow at development. This sharpened pillar 1 and is captured in 0112.

NO CODE WRITTEN, by design - the rebuild is authorized but landing-order step 1 touches .claude/lib/**, where the write guard holds edits for a human. Probes were scratchpad-only.

FAILED / CORRECTED IN FLIGHT: the first probe tested globals via eval(), which is itself blocked by the code-generation guard, so it measured the wrong thing and reported a false "THROWS" for every global; re-run without eval gave the clean enumeration. Also attempted to read the decision store with Bash and was correctly refused by the ledger guard - Read is the right tool there.

INFRASTRUCTURE DEFECT observed, not yet fixed: this thread's decision history is now SPLIT across two ledger stores. 0103-0106 live under plugins/data/session-continuity-continuity-ledger/...; 0107-0112 wrote to plugins/data/session-continuity-inline/.... get_resume_brief read the spine correctly, so nothing is broken today, but a future read against the wrong store silently loses half the architecture record.