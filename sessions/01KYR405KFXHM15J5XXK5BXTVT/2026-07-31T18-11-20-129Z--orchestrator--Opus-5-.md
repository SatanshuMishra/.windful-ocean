M1 DELIVERED and merged: PR #22, branch fix/m1-honest-terminal-states, commits a07fbc4 (engine + tests) and 01636cc (coverage entry). Second mitosis engine spec MSP ever implemented, after M0.

Re-grounding first: origin/main re-derived as 60f4217, local main advanced onto it, merged M0 branch deleted with -D (it was squash-merged so it is not an ancestor; content diff of ab33673 against origin/main was EMPTY, verified before deleting).

Scope: spec section 10's M1 row (honest terminal states + blockedByApproval prune), NOT section 6's full prose. Three changes shipped. (1) computeMergePolicyStatus in merge-policy.mjs:23 and its inline mitosis.js twin at :3450 - the shippedCount === 0 -> failed branch is DELETED, a genuine fault returns blocked, awaiting work returns awaiting-approval regardless of shippedCount, ci-red-exhausted is added, and partial survives only as the no-fault residual. (2) onMerged at mitosis.js:4875 recomputes the still-blocked set from the live awaitingApproval array. (3) the end-of-run classifier at mitosis.js:4906-4923 stops writing resumePoint.stage plan into report-only park records for units already built, via a new reportOnlyResumePoint helper.

TWO SPEC CLAIMS REFUTED BY EXECUTION, the 0137/0144/0145 class again. Section 6 calls markAwaitingMerge zero-consumer: it has four call sites (leases.mjs:204, :278, mitosis.js:2124, :2198) and roughly fourteen disposition assertions in leases.test.mjs. Section 11 calls progressPossible test-free: leases.test.mjs imports it at :16 and asserts it at :284, :290, :297, :303. Both deletions were therefore EXCLUDED from M1 and left to M3/M5, which section 10's table already assigns them to; landing them here would have broken the branch on tests those MSPs own. See decision 0152.

0151 CONFIRMED FOR A SECOND CONSECUTIVE MSP: section 10's mirror-twin gotcha list names only M3/M4/M7, and merge-policy.mjs is a policed twin. The twin list at mirror-guard.test.mjs:19 was grepped BEFORE any edit and the change landed in both copies.

Orchestrator-executed verification, not relayed (0143 held): full suite 1766 pass / 0 fail exit 0 on the branch (baseline 1762, net +4 tests); red-on-parent proven by reverting merge-policy.mjs and mitosis.js to 60f4217 with the new tests kept - 218 tests, 195 pass, 23 FAIL, exit 1; mirror guard 22 pass / 0 fail; scripts/invariant-coverage-check.mjs --event pull_request --base-ref main reports ok. The pre-commit hook's own inline full suite passed on both commits.

WHAT FAILED / WHAT IS WEAKER THAN COMMISSIONED. The CHANGE B acceptance test asserts the released ids in the onMerged log line, NOT a false blocked-pending-approval park record. The implementer reported the report-level symptom is unreachable through today's classifier (a non-terminal unit always retains a live blocker) across 16 DAG and merge configurations, and the orchestrator did NOT reproduce that probe. So the prune lands on a genuinely stale Set and on a log line that claimed an unblocking it never performed, while the report-level symptom stays unproven in both directions. Recorded as the M3 threat in the coverage entry and as a --not-verified row on the PR.

Coverage entry docs/invariants/coverage/fix-m1-honest-terminal-states.json verdicts all 12 registry ids. M1, M3, M4 and M5 are threatened on the candid reading. M4 owns that three behavior changes share one reviewable range. B3 candidly states the guarded-intrinsic enumeration was not read row by row.

Background task hygiene: bdkdl7yo1 was a poll loop whose sentinel was the TAP form ^# duration_ms while node's default reporter emits the info form, so it could never exit and was killed by its own 15-minute timeout with an empty output file. No result depended on it - the suite summary was read directly from the redirect file. TaskList now reports no live tasks. task_70509bf0 remains a spawned-task CHIP the user alone can dismiss; its premise is still dead.

Working tree left carrying only the pre-existing dirt it started with (.claude/settings.json, .zshrc, three untracked paths). Nothing of it was ever staged; explicit paths only, never git add -A. The two parked stashes are untouched at depth 2.