AUDIT F1 (foreign-branch-filtering coverage) RETURNED, mutation-proven by execution. F2 and F3 still outstanding. Nothing implemented; thread stays paused.

HEADLINE: THE GAP IS REAL, LIVE AND CURRENTLY UNCAUGHT. Confirmed independently, not inherited.

THE INVARIANT, stated properly for the first time in this thread: a PR whose branch does not match `<sourcePrefix>/<mspId>-integration` must be INVISIBLE to the run -- never attributed to a unit, never freezing one, never becoming an operator's merge target.

MECHANISM at fc035fc. branchToMspId is mitosis.js:359-367, mirrored at recovery.mjs:14-21 and already unit-tested at recovery.test.mjs:49-58. But the LOAD-BEARING LINE is the wiring, mitosis.js:3025 `if (id === null) continue;` inside classifyRunOpenPRs (:3014-3085). classifyRunOpenPRs has NO LIB TWIN -- grep for it, prHeadOwnerRepo and emptyOpenPrClassification across .claude/lib/superpowers-parallel/*.mjs returns zero hits. That single fact decides the layer question: e2e is not a shortcut here, it is presently the ONLY layer that can express the behaviour, which satisfies the testing rule rather than violating it.

PROVEN BY MUTATION, run in scratch via MITOSIS_PATH, tracked worktree untouched. Mutant makes an unresolved id fall back to the first built unit. Real engine: ship:l2 dispatched, l2 absent from awaitingApproval, status all-shipped. Mutant: ship:l2 NOT dispatched, l2 IN awaitingApproval, l2's reported prUrl is the FOREIGN pull #7686, status flips to awaiting-approval. The production-shaped consequence is not a crash -- it is a wrong PR silently substituted as an operator's merge target while the unit's real work freezes behind it.

SUITE-WIDE SWEEP CONFIRMS "TRULY UNASSERTED". Every headRefName literal in all three files carrying openPRs fixtures (frontier-train-e2e, mitosis-scheduler, reconcile-only-advance-characterization) is built from ${SOURCE_PREFIX}/...; zero non-matching fixtures exist anywhere. The claimed replacement cites are re-confirmed WRONG at current line numbers: :1148 is M3b with openPRs: [] about a manifest prUrl fallback, :1167 is L2 asserting a do-NOT-merge string on a contested unit, :1267 is the capped-build-path test with openPRs: [].

TWO ALTERNATIVES REJECTED WITH REASONS. Extracting classifyRunOpenPRs into a lib module to unit-test it is a production refactor creating a NEW mirror-maintenance obligation -- legitimate, but a named follow-up, and exactly the blast-radius-multiplying shape the method rule warns about. Relying on the existing recovery.test.mjs:49-58 is insufficient: it pins the pure function's contract, and the mutant above breaks the WIRING while leaving branchToMspId and that unit test green.

RECOMMENDATION: e2e test next to M2b (~:1104) in .claude/lib/superpowers-parallel/tests/frontier-train-e2e.test.mjs -- note F1 gives that fuller path where F4 cited tests/frontier-train-e2e.test.mjs; confirm they are the same file before editing. Assert operator-visible output (unit still dispatched, never in awaitingApproval) PLUS a broader "no unit at all is falsely frozen or awaiting when the only open PR is foreign-shaped", so it pins the invariant rather than killing one mutant.

PLACEMENT CONFLICT -- F1 AND F4 DISAGREE, DO NOT RESOLVE BY PICKING THE LATER REPORT. F4 recommends AMENDING 777617b. F1 recommends a NEW COMMIT ON TOP of 777617b, advancing feat/m4-fixed-build-ahead-cap's tip and preserving 777617b's SHA. Both still force a rebase of feat/m4-divergence-instrumentation, because either way commit 2's parent moves and fc035fc's SHA changes. F1 verified mechanically that fc035fc touches zero lines this insertion would touch, so the replay is clean.

THE CONFLICT HAS A THIRD PARTY: 0201 ruled the coverage remedy must be an AMEND to each commit "rather than a follow-up". If 0201 meant a follow-up PR/MSP, F1's same-branch follow-up COMMIT may satisfy the gate anyway, since the invariant-coverage job runs on the pull_request event against the PR's head, not per-commit. If 0201 meant any non-amend, F1's shape fails it. F2 is the authority on the gate's actual evaluation unit and MUST settle this before any implementer runs. Do not assume either reading.

SCOPE CONSEQUENCE FOR DECISION A. Decision A fixed PR 1 as "feat/m4-fixed-build-ahead-cap -> main carrying 777617b ALONE". F1's recommendation makes PR 1 carry 777617b plus a test commit. That is a deliberate amendment to decision A, not a detail -- decide it explicitly rather than letting an implementer drift into it. F1's own falsifier (is commit 1 really its own PR?) is CLOSED by decision A: yes, it is.

RE-VERIFICATION F1 DEMANDS AND THE PRIOR PLAN DID NOT NAME: after the rebase, (1) no conflict markers, (2) FULL suite at the new commit-1 tip, (3) FULL suite at the rebased commit-2 tip, (4) tree diff of rebased vs original fc035fc excluding the new test to prove the payload replayed unchanged, and (5) RE-RUN THE SANDBOX-COMPILE GATE (per commit 95ef8e1) because mitosis.js is eval'd as one script body, so a bad hunk boundary surfaces only there or in the full suite, never in a narrow diff read.

F1's DECLARED GAPS: it did not run the full 1830 suite, and it could NOT see the lens's actual drafted test -- no artifact exists in the repo -- so its mutation-proved test is its own reconstruction from the finding's description, not a verification of the lens's literal draft.