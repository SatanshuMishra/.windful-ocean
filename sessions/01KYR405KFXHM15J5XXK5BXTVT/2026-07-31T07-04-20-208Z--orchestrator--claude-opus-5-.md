Resumed via lift-off on the named thread. Re-grounded against the tree first, which overturned two ledger claims: PR #14 was recorded as unconfirmed but is merged (6d19499), and local main was 5 commits behind origin/main. Fast-forwarded main to 95ef8e1, later to 11421ef.

ANSWERED THE STANDING QUESTION. No mitosis spec MSP has ever been implemented. PRs #1-#19 are all Track B invariant/gate/boundary hardening, not engine work. Three specs are approved-and-unimplemented: run-readiness-repair (MSP-0..MSP-10, header literally reads "Status: approved, not implemented"), quiescent-advance (M0..M8), and the core-rebuild spec that lives only on branch docs/mitosis-core-rebuild-spec. Proved the engine is untouched by execution, not by reading the ledger: mitosis.js still defines progressPossible (:2074), computes maxSteps (:2094) and carries the shepherd probe stage (:2924) - the three things M3 and M5 exist to delete.

SHIPPED PR #20, merged as 11421ef. Two commits on docs/mitosis-engine-specs: a60e411 commits both untracked specs verbatim, 9832a2f records the invariant coverage entry the PR owed. Leak-checked before staging (zero codename hits, no secret shapes, no /Users paths in either file); staged explicit paths only, leaving the seven dirty paths untouched. Suite 1761/1761 on both commits via the pre-commit hook, output redirected and tail-read. invariant-coverage-check --event pull_request exited 0. The standing risk "both specs are one session from being lost" is now DISCHARGED.

THE ANCHOR INVESTIGATION, and a correction I made mid-session. Measured against current main, 22 of 23 named anchors in the quiescent-advance spec appeared drifted, which read as the 0137/0144/0145 hand-enumeration failure mode repeating. It was not. Section 8 declares its derivation baseline as commit 450804e. Re-derived by execution against git show 450804e:.claude/workflows/mitosis.js: all 23 named anchors resolve at that SHA - 19 to the symbol definition line, and 4 to the cited usage site rather than the definition (isBuildable :1909, WINDOW_FLOOR :1963, planTick :1966-1981, PR_NOT_VERIFIED_OPEN_CI :4655), which was my verifier's heuristic mismatching, not the spec erring. The specs are honest documents that declare their own baseline. Stated the correction plainly to the user rather than letting the first framing stand. Chose NOT to rewrite the 78 anchors: doing so by hand is exactly the failure mode 0137/0144/0145 record. The gap is recorded in the M5 coverage row and the PR risk field instead.

DID NOT START M0, deliberately. 0085 authorizes it and 0086/0087 clear M4 and M8, but 0085 carries a caveat: M0 sits behind the hermeticity blocker because its red-then-green proof needs a trustworthy suite signal. Starting a red test on an untrusted signal is what that caveat forbids. New evidence gathered instead: the full suite is 1761/1761 in the PRIMARY working directory, not the 1520/1540 the ledger records - that figure is simply stale, the suite grew. The blocker's actual claim is that a worktree fails 2 more at the same commit, and NO worktree run was performed this session, so the blocker is neither cleared nor confirmed.

WHAT FAILED. record_decision was rejected twice on a malformed call (the options property was passed without the tool's parameter prefix, so it never reached the server); recovered at wrap-up. Two lift-off tool calls were also rejected for passing an empty argument object before succeeding with no arguments.

Nothing is running in the background. No worktree was created. Both stashes remain parked at depth 2. No file under .claude/{hooks,rules,lib,workflows} was written this session.