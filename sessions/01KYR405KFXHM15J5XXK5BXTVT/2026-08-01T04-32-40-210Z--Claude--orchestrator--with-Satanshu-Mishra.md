MSP M6 shipped end to end. PR #28 open: https://github.com/SatanshuMishra/.windful-ocean/pull/28 (branch feat/m6-durable-run-identity, cut off origin/main at 5dd7176, four commits abde750 / 1ba18e1 / 174a0dc / e516fc9). Suite 1820 -> 1846, green at every landing, confirmed first-hand in the main thread twice, not relayed. Mirror guard 46/46 in isolation. invariant-coverage-check ok in push mode. No forbidden path was ever staged; the five pre-existing dirty paths are untouched. Worktree count unchanged at 17 - nothing leaked.

A2 IS KILLED, high confidence, and Gap 1's extraction inventory is therefore fully closed (A7 shipped, A4/A6 dropped, A2 killed). The verdict rests on byte-identity, not argument: the 102-line A2 region is byte-identical from 8933c2c (pre-M2) through HEAD across both engine commits, and M2's five hunks (old-lines 536, 2336, 3776, 3845, 3868) do not intersect old-range 2817-2918. The "MSP M2 (fact source)" justification is falsified by dataflow DIRECTION, not merely by non-use: foldObservedStatus runs at :3831 sourcing reconcile.mjs, while classifyRunOpenPRs does not run until :3860 - the fold cannot consume A2's output. The spec names no A2 function anywhere. Stale-anchor delta was a uniform +55 (51+4 from M2's two insertions above the region); live at mitosis.js:2872-2973. A3 and A5 characterization-test debt before M3/M5 is SEPARATE and still owed.

M6's three recorded preconditions all cleared. Hermeticity REFUTED at today's HEAD: root 1820/1820 vs temp worktree 1811/1811 at the same commit - a 9-test COLLECTION delta (64 vs 65 matched files), zero failures either side; the probe worktree was removed. The guard fix on fix/guard-any-claude-root was NEVER WRITTEN - the branch is an empty stale pointer at beca874 - and M6 does not depend on it. Cite mapping re-derived live.

Sequence: one dynamic workflow (10 agents, ~1.55M subagent tokens, 3.5h) did ground/design/implement/verify/review/fix and landed abde750 + 1ba18e1. Review returned 17 findings, 5 blocking. The fix round grouped by invariant rather than working the list, and caught itself breaking the twin - it had put `identity` directly into fatalReport, which is an outcome.mjs twin; the mirror guard went red, the twin was restored byte-identical, and an engine-local haltReport wrapper replaced it. That is the guard doing exactly its job.

Then four residual decisions were ratified by the user and implemented in 174a0dc. The FIRST implementer dispatched for them changed NOTHING and returned an honest orientation result at ~85% context, having found a real blocking defect in the ratified Change 1: the engine composes manifestRef at mitosis.js:3959-3961 and bakes it into the reconcile prompt, but observedSpecHash does not exist until :4021, arriving from that same agent's step 4. The engine has no shell, so publishedManifestRef(runId, specContentHash) could not be called at the composition site. Its two resolutions were ratified and re-dispatched: (a) the engine composes only the ref PREFIX, the reconcile agent appends its step-4 hash and reports the ref it actually probed in a new RECONCILE_SCHEMA field publishedManifestRefProbed, and the engine re-derives and COMPARES - any mismatch is probeFailed, never absence; (b) resolveRunIdentity's envelope comparison had to move to repo-relative too, or a relative payload would report a permanent false disagreement on every run.

Landed in 174a0dc: content-keyed ref refs/mitosis-manifest/<logicalRunId>/<specContentHash>; repo-relative published spec path (reader rejects leading /, .., drive prefix, and backslash - the implementer added the backslash rule beyond ratified scope because a UNC path passes the three named checks and still leaks); transient-publish retry gated fail-closed behind the shared pre-publish validator; confirmed one validator serving both publish paths (parsePublishedManifest, 4 call sites, no second copy).

Spec 3.5 amended in e516fc9 by the orchestrator (spec = orchestrator artifact) with an "As shipped" block recording four deliberate deviations plus the accepted sourcePrefix limitation and the reconcile-only gap. The original prose is left intact so the delta stays legible.

NOT DONE, deliberately: sourcePrefix does not WIN its logged disagreement (needs the reconcile pipeline reordered - declined as exactly the "new defect on an unnamed path" failure mode this codebase has been bitten by); computeLogicalRunId still hashes spec path + baseBranch only, so a superseded manifest ref is never reaped; reconcile-only relaunch returns before the publish stage so it does not retry a lost publish; inertness mutations not run this round; invariant-coverage-check pull_request mode not run before PR creation. All are stated in the PR body under Not verified, and in the spec amendment.

pr-create rejected the first attempt because a --what value contained angle brackets, read as an HTML tag opener. Reworded, not forced.