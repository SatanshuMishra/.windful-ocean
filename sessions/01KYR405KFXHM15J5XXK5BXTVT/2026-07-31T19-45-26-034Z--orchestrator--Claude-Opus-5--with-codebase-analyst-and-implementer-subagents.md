Session goal: read the machine-local M2 mirror-gaps plan, settle the two owed rulings, then implement P1 and P2. Outcome: both rulings settled on verified evidence, P1 implemented and committed green, P2 materially re-scoped before any of it was written. P2 NOT started. No push, no PR opened this session.

SHIPPED

1. Read docs/superpowers/plans/2026-07-31-m2-mirror-gaps.md in full (369 lines, 37KB). Internally consistent: 22 whole + 3 partial + 12 standalone = 37 lib modules, matching its own section 2 measurement. Baseline confirmed by execution: main at 1bb149d, the five known pre-existing dirty paths present and untouched all session.

2. Ruling 2 settled -> decision 0158. Track the plan. .gitignore:22 is a whole-directory exclude (/docs/superpowers/plans/) while docs/superpowers/specs/ carries 9 tracked specs. Chosen form is the /* rewrite plus an explicit negation, because git refuses to re-include a file whose PARENT DIRECTORY is excluded, so the trailing-slash form cannot be negated at all. Leak scan for the confidential codename returned 0 hits, so tracking is safe. NOT YET IMPLEMENTED - this is P0.

3. Ruling 1 settled -> decisions 0159 and 0160, after two investigation rounds plus orchestrator verification.

   H-C REFUTED (0159). Not a defect, not deferred; MSP M2 ships nothing at mitosis.js:4058-4061 and the plan's section 9 done-union receipt row is struck. Producer chain traced end to end, every step gated on a live merged-PR read: recon.mergedPRs (gh read at :3718) -> reconciledShipped :3760 -> reconciledMergedIds :3777 -> newlyMergedIds :3778 -> persistShipCheckpoint call :3019 -> shipDelta write :4242 -> .mitosis/run.json -> fold-run-log.mjs CLI -> run-log.mjs applyRunDelta -> applyShipTransition. Narrowing would re-break HIGH-C at frontier-train-e2e.test.mjs:860 (fixture sets mergedPRs to empty deliberately) and strand descendants at parentsDone :4687, a liveness regression.

   H-A REFRAMED (0160). The veto at mitosis.js:3787 is the resurrection guard, covered by H4 at frontier-train-e2e.test.mjs:578 with its assertion at :596. It STAYS. Plan section 9's monotone-forward receipt row is MISWORDED - it asks for a test red-on-parent BECAUSE the veto exists, which asserts the exact regression 0155 already refused. Correct property per 0154 is monotone-forward EXCEPT via two named logged vetoes.

   H-B is the single uncontested behavioural gap remaining, and narrower than the brief described: selectResumeBuilt at parking.mjs:111-132 takes no builtUnits parameter at all (only its sibling selectPreservedBuilt at :134 does), so it is an API-level omission, not a skipped in-body check.

4. P1 implemented and committed: 7b8acd2 on branch test/mirror-census-closure, message "test(mitosis): close the mirror census over all lib modules". Hand-typed 21-name array at mirror-guard.test.mjs:19 replaced by a frozen MIRROR_CENSUS over a readdirSync domain, three classes, asserted in both directions. Receipts: seeded with only the 21 declared names it fails naming exactly 16 unclassified files (matches plan section 9's prediction); all 37 classified goes 44/44; perturbing the inline twin at mitosis.js:68 turns it red; three fixture-driven negative tests cover mis-declared standalone, a shrunk partial row, and the renamed-export message pair. Suite 1766 -> 1788, 0 fail, delta +22. Zero disagreement with plan section 6.1 on any of the 37 rows. Implementer added one assertion beyond section 6.2 that the orchestrator judged load-bearing and kept: the tripwire also asserts every top-level export line is brace-matchable or a bare re-export (290 lines = 289 blocks + 1 re-export in mitosis-git.mjs:23), without which an export default would be invisible to the extractor - the same omission hole this PR closes.

VERIFIED BY THE ORCHESTRATOR, NOT RELAYED
Commit 7b8acd2 present; git diff --stat main..HEAD shows exactly one file (mirror-guard.test.mjs, +221/-8); mitosis.js clean vs main AND clean in the worktree, so the inertness perturbation was reverted; scoped node --test on mirror-guard.test.mjs returns 44 tests / 44 pass / 0 fail, exit 0. Separately verified before ruling on H-C: HIGH-C exists verbatim at frontier-train-e2e.test.mjs:860; applyShipTransition at :470 is the sole writer of status shipped (writes at :473 and :485, all other hits are reads); the --limit 200 cap is real at :3718; liveSignals is block-scoped to :3819-3863 and NOT reachable at :4058, while reconciledShipped IS. Registry confirmed at 12 ids (B1-B6, M1-M6); coverage rows are exactly {id, verdict, check}; CI runs npm test + phase-parity + invariant-coverage as separate jobs on push and PR.

WHAT FAILED, AND WHY IT MATTERS
Investigation round one refuted H-C on a SOLE-CALLER claim - that applyShipTransition is invoked only from the fold at :3781. A two-second grep falsified it: there is a second caller at :562 inside applyRunDelta, which is journal replay, and that is precisely the path the H-C framing named. The orchestrator caught it and sent it back rather than accepting the conclusion. Round two traced the second path to its single producer and the refutation survived, strengthened. Method rule recorded in 0159: trace producers, never assert sole callers. This is the same failure mode as the "fix rounds need invariants, not finding lists" memory - a plausible finding that would have passed review on its narrative rather than its evidence.

NOT DONE, DELIBERATELY
P1 is committed but NOT pushed and has NO PR: opening it requires the 12-id coverage entry, which is orchestrator-authored per 0143 and was not affordable at 87% context. P0 not implemented. P2 not started and must be re-scoped first. No worktree was created (plan section 8 forbids parallel worktrees for P1-P5, and 12 leaked worktrees already exist). Parallelism this session was in investigation and authoring, not in landing - the two subagents ran concurrently on disjoint files.