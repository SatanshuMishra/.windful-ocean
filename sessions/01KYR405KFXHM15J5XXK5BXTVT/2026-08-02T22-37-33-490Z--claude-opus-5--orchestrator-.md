M7 SHIPPED AND MERGED. origin/main is e9306e6, tree ee06be3d, PR 36 squash-merged 2026-08-02T22:35:19Z. All CI green on the PR: test, invariant-coverage, sast, sca, secret-scan, label. Orchestrator verified PR state and CI directly; it did NOT read the PR body or the coverage artifact itself.

WHAT SHIPPED. Three commits: 275505c refactor(mitosis) collapse divergence to one fail-closed predicate; e662e20 test(mitosis) pin the divergence-check target payload composition; 23ed834 docs(mitosis) record the m7 invariant verdicts. runDivergenceProbes, assembleDivergenceVerdicts, descendantsToInvalidate, DIVERGENCE_PROBE_SCHEMA and divergenceProbePrompt are all deleted, replaced by needKeyedParents + divergedParents: two states, ONE batched read-only dispatch (label divergence-check, phase Resume, agentType implementer), every cannot-tell folded to diverged. Comparison stays CONTENT-based over declared fileScope between parent.builtSha and merge_commit_sha. The batch parser is positive-match: not-diverged requires exactly one matching entry with an error-free empty changedPaths; missing entry, duplicate entry, top-level error, non-array results and non-object response all fail closed. divergedParents is total, so the old outer catch was DELETED rather than inverted into a silent clean. planReconcile now holds no divergence logic. Three WHOLE twins edited alongside their mitosis.js copies (divergence.mjs, parking.mjs, reconcile.mjs). Suite 1832 -> 1836 with zero engine regressions; mirror-guard 46/46; phase-parity ok true dead 0.

HOW IT RAN. Workflow 1 (wf_86a9cc67-84d, 10 agents planned) completed 8 agents then died when the application quit mid-Verify. Recovery was by JOURNAL EXTRACTION, not by resumeFromRunId: the orchestrator parsed journal.jsonl, wrote the 7 recovered agent results to a scratchpad, and hand-authored a continuation workflow (wf_467dadfa-e30, 5 agents) whose prompts referenced those files by path. No completed agent was re-run. Total across both runs: 13 agents, ~694k subagent tokens in the continuation alone.

TWO ORCHESTRATOR CORRECTIONS THAT CHANGED THE OUTCOME. (1) The plan audit's single BLOCKING finding was REFUTED: it claimed the plan fabricated a citation to Decision 0207 because "the ledger runs 0001-0206", having read a stale copy under .claude/plugins/data/session-continuity-inline/. The live ledger is the _ledger git ref and does carry 0207 and 0208. Uncorrected, the ship agent would have rewritten a CORRECT citation into a wrong one. The fix agent and the ship agent each re-verified this independently rather than taking it on assertion; the fix agent additionally observed that 0207's own Options section cites 0172 as the prior pattern it reaffirms, proving they are two distinct events. (2) The receipts verdict of "red" was a FALSE HALT that would have blocked the ship: the sole failing test is protect-claude-config.test.mjs:194, proven pre-existing by running it on the main worktree at a618338, and M7's nine-file diff touches zero files under .claude/hooks/. The continuation encoded an operative green test - zero failures OTHER than that named test - and forbade ever restating it as "the suite is green".

THE HIGH FINDING, AND WHY IT MATTERS. A deleted test pinned SURVIVING behavior. The old per-parent test asserted the exact dispatch payload (ref, builtSha, mergedSha, fileScope); that composition survives the collapse verbatim, but after the rewrite nothing in the tree read those four values. The reviewer supplied a mutation (builtSha := mergedSha) under which every dispatched diff compares a commit against itself, the positive-match parser confirms EVERY parent not-diverged, and no descendant of a genuine squash rewrite is ever invalidated - the precise fail-open direction M7 exists to prevent - with all 1836 tests still green. This is spec section 11 line 330's own recorded lesson recurring. The fix agent applied the mutation itself before agreeing, added a test-only pin (production byte-identical to 275505c, verified by git diff --quiet over mitosis.js and lib excluding tests), then proved the new assertion non-inert with three separate fail-open mutations each reverted, then re-checked the other nine invariants rather than stopping at the finding.

JUDGMENT CALLS, ALL UPHELD BY BOTH REVIEWERS INDEPENDENTLY. The implementer overrode the APPROVED plan on needKeyedParents: the plan required manifest presence, which would have dropped a merged-but-absent parent that still gates built work out of the keyed set, shrinking the diverged set and breaking the safety direction. Verified against a618338 by reading transitiveDependents' seeding, not by reading the plan. Also upheld: dropping clean from the predicate ctx (no verdict or probe error string ever reached a user-visible surface), and narrowing one e2e assertion whose original form would have failed on the manifest-publish prompt, where the :(exclude)* token legitimately appears.

SHIPPED UNREMEDIATED, NAMED IN THE PR BODY AND THE COVERAGE ARTIFACT: 4 MEDIUM and 4 LOW. The two that compound are both consequences of the batching decision itself - (a) a batch entry is bound to a target only by a parentId the agent echoes back, with nothing binding it to the endpoints it was told to diff, a mis-keying route to a false not-diverged that the per-parent prompt structurally could not have; (b) every diagnostic on the destructive path is discarded, so a whole-batch dispatch failure is indistinguishable in the logs from N genuine divergences. The named cheap fix for (a) is echoing checkedBuiltSha/checkedMergedSha in the per-entry schema and folding unless both match the target.

COVERAGE ARTIFACT IS DELIBERATELY NOT A CLEAN BILL: 7 of 12 rows threatened (B2 B6 M1 M2 M3 M4 M5), 5 not-threatened (B1 B3 B4 B5 M6). M1 is threatened in a sharp form because the reviewers were fenced from re-running receipts, so every test, gate and mutation result in the artifact is CARRIED and each row names its source. M4 is threatened because the commit is typed refactor while the reviewable range carries behavior change with no characterization test preceding it. The ship agent proved the pull_request-mode gate is live by running it BEFORE committing the artifact (exit 1) and after (exit 0), rather than trusting the bare push-mode invocation that silently reports a false green.

NOT RUN, STATED PLAINLY: no agent ran semgrep, osv-scanner or secret scanning locally (CI ran all three and they passed post-push). The reviewers re-executed no handed receipt, by instruction.

CLEANUP OUTSTANDING, NOT DONE: the branch refactor/m7-single-divergence-predicate (local and remote) and the worktree at .claude/worktrees/m7-divergence-predicate both still exist. Local main is still a618338, one behind origin. None of this was touched because destructive git operations require explicit user confirmation. The five pre-existing dirty paths in the main worktree are byte-for-byte unchanged and were never staged.

ENVIRONMENT FINDING, OUT OF SCOPE AND UNADDRESSED: protect-claude-config.test.mjs:194 fails locally because ~/.claude/settings.json is a regular 9.8KB file rather than a symlink resolving out of the home tree. The guard's own error text says the consequence: it discovers no repository base and stops protecting the checkout. That protection is currently inert on this machine.