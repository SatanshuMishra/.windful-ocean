DELIVERED the recorded task, then the user's critique overturned its premise and the thread pivoted from explanation to a scoped deletion.

DELIVERED (task from the prior brief): the minimal-domain-knowledge explanation of merge-boundary Preflight invariants 1, 2, 3 and the bypass advisory - what each says, its mechanism, and the test that reddens if removed. Re-derived at 4fd03c2. The TEST half, flagged UNDERIVED, is now derived: tests/merge-boundary-preflight.test.mjs (668 lines) and tests/mitosis-gate.test.mjs (519 lines) were both opened and mapped mechanism-to-test.

TWO RISKS FROM THE PRIOR BRIEF CLOSED. (1) Exit code 30 is CONFIRMED IN SOURCE: PREFLIGHT_HALT_EXIT = 30 at merge-boundary-preflight.mjs:8, returned at :388; 31 is PREFLIGHT_CONFIG_EXIT, a pre-network config rejection. It was previously only the user's reported number. (2) The test half is no longer underived.

ANCHOR CORRECTIONS at 4fd03c2: PREFLIGHT_PROBES is :19 (recorded :18); PREFLIGHT_CHECK_IDS is :21-29 (recorded :20-27). Every other recorded anchor re-verified clean.

CORRECTION TO THE RECORD: tests/mitosis-gate.test.mjs carries ZERO coverage of the three invariants. It tests phase-parity, an unrelated static gate. Its ONLY preflight tie is the exit-code collision test at :479-496, which imports PREFLIGHT_HALT_EXIT / PREFLIGHT_CONFIG_EXIT / PREFLIGHT_GH_MISSING_EXIT and asserts no phase-parity code collides with them, plus asserts at :492 that the sibling codes were imported rather than transcribed. That is a real guarantee (the caller distinguishes a halted boundary from an unrelated tool failure by exit code alone), but it is not invariant coverage. The prior brief's expectation that this file held half the test story was wrong.

THE PIVOT. The user rejected all three invariants as unsatisfiable for their dominant workflow - solo repos where they are the sole owner. Investigation confirmed the critique and found the decisive fact by grep: mitosis.js contains ZERO merge invocations (no `gh pr merge`, no pulls/*/merge, no mergePullRequest, no enablePullRequestAutomerge, no --squash). Merging is independently denied at three local layers: permissions.deny in settings.json (Bash(gh pr merge:*) plus both GitHub MCP merge tools), the Bash hook block-destructive-bash.sh:46-49 (gh pr merge, the pulls/*/merge REST endpoint, both GraphQL mutations), and the engine itself which has no merge call to deny. The hook's own deny message states the design in words: "mitosis never merges PRs ... a human merges via the PR after review."

So the preflight proves a server-side boundary against a capability the engine does not have and never exercises, and it gates the WRONG EVENT - SKILL.md gates DISPATCH on exit 0, blocking the start of work on the grounds that finishing it might be unsafe, when the engine never finishes it.

INVARIANT 3 IS WORSE THAN STRICT ON A SOLO REPO. GitHub does not permit a PR author to approve their own pull request, so required_approving_review_count >= 1 with enforcement active means NO pull request can ever merge - not by the engine and not by the human. The only escape is adding oneself as a bypass actor, which is precisely the one thing the gate declares unverifiable (:270). Invariant 3 pushes a solo operator into the exact blind spot the bypass advisory admits to. CAVEAT: the self-approval constraint was NOT verified against GitHub docs this session; it is load-bearing for the deadlock claim and must be confirmed before anything is built on it.

BLAST RADIUS measured by grep, worktrees excluded, 9 live files: merge-boundary-preflight.mjs (32 refs, delete), tests/merge-boundary-preflight.test.mjs (84, delete), tests/mitosis-gate.test.mjs (7, drop the PREFLIGHT_* imports from the exit-code collision test), mitosis.js (3, remove BOUNDARY_PREFLIGHT_CLI at :24 plus the reconcile re-run), skills/mitosis/SKILL.md (2, remove the "Prove the merge boundary" section), and one ref each in tests/mitosis-scheduler.test.mjs, tests/reconcile-only-advance-characterization.test.mjs, tests/mirror-guard.test.mjs, tests/frontier-train-e2e.test.mjs. Plus 3 spec/plan docs under docs/superpowers/ and docs/invariants/coverage/feat-m5-quiescent-exit.json - that last one may feed an invariant-coverage checker, and the standing "coverage-checker false green" residual means it must be CHECKED, not assumed.

NOT DONE, BY DESIGN: nothing was written to the repo. This session was explanatory under the prior directive, and the deletion is a real change awaiting the user's go. No implementation, no branch, no commit.