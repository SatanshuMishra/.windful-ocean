CLOSING STATE. The user merged PR #31. origin/main re-derived at 42e7d49 with BOTH required checks green (test success, security success), verified after the merge rather than assumed. Both A5 artifacts are present on main: .claude/lib/superpowers-parallel/tests/merge-poll-characterization.test.mjs and docs/invariants/coverage/test-a5-merge-poll-characterization.json. Nothing is running; workflow wi3nyl4z1 completed and needs no wind-down. The local checkout is parked on the now-merged branch test/a5-merge-poll-characterization with the same five pre-existing dirty paths - RE-DERIVE origin/main before cutting anything.

USER DIRECTIVE FOR THE NEXT SESSION, given explicitly at close: dispatch a DEDICATED SMALL dynamic Workflow to FULLY IMPLEMENT AND SHIP M5, in the fresh session. The brief below is written to be lifted almost verbatim into that dispatch. Do not re-derive it from the specs first; re-confirm the anchors it names, then dispatch.

=== M5 DISPATCH BRIEF ===

WHAT M5 IS. The quiescent exit plus the continuation block, and the deletion of the mechanisms it replaces. Spec docs/superpowers/specs/2026-07-28-mitosis-quiescent-advance.md section 3.4, landing table row M5 at :292, which reads 'quiescent exit + continuation block; delete the bounded poll and progressPossible', depends on M3 which has landed. The spec's own pseudocode at :75 states the exit condition as 'if actions is empty: exit quiescent'.

M5 SHIPS SIX THINGS, NOT TWO. The landing table undercounts. Derive from these, and treat the table as stale per 0148's re-baseline-by-execution rule:
1. The quiescent exit itself: when a tick produces no actions, the run exits QUIESCENT - a labelled, honest terminal state - instead of polling or reporting a mislabelled failure. Section 1 of the spec exists to delete exactly that class of a run reporting something false about itself.
2. The continuation block in the report, INCLUDING the identity field. Spec :306. M6 landed the durable content-keyed identity manifest specifically so this field is written from a real value rather than a placeholder (spec :296), so M6 is the source - do not invent a new identity mechanism. Note 0178: identity is THREE-valued and the published spec path is relative; and 0176: computeLogicalRunId hashes only spec path plus baseBranch.
3. Delete the bounded merge poll: mergePoll at mitosis.js:5057-5095 and its two helpers readReviewDecision :5016-5026 and resolveReviewEvent :5028-5033, plus the constants MERGE_POLL_MAX_CYCLES :4989, MERGE_POLL_WAIT_SECONDS :4990, MERGE_POLL_INTERVAL_SECONDS :4991. These live ONLY in mitosis.js and have NO twin (0189).
4. Delete progressPossible - mitosis.js:2362 AND leases.mjs:150 - and the poll-cycle guard that consumes it at mitosis.js:2397 and leases.mjs:185.
5. Delete maxSteps - leases.mjs:170/172 and mitosis.js:2382/2384. 0185 moved this row from M3 into M5 deliberately: the step bound is the ONLY thing bounding the tick loop until the quiescent exit exists, so it may be deleted only in the same change that lands the exit. Deleting it earlier trades a bounded wrong stop for an unbounded hang.
6. The instrumentation section 11 REQUIRES rather than suggests: log, per quiescent exit, the wall-clock between that exit and the next advance (spec :314). Section 3.6 asserts residual latency is the design's only cost, and section 11 requires that assertion be MEASURED rather than assumed. Shipping M5 without this emitter leaves the design's central claim unfalsifiable.

WHY IT IS ONE MSP AND NOT SEVERAL. The green-branch invariant: a PR merged into any branch must not break that branch. Deleting the poll without the exit leaves the engine with no terminal behavior; deleting maxSteps without the exit leaves an unbounded loop. They are not independently shippable, so they are one MSP with one PR.

THE DOMINANT RISK, AND IT IS NEW FOR THIS RUN: M5 IS THE FIRST MSP HERE THAT TOUCHES POLICED TWINS. leases.mjs is a WHOLE mirror - its entire normalized body must appear verbatim inside mitosis.js, CI-enforced by mirror-guard.test.mjs. progressPossible, maxSteps, the poll-cycle guard AND the tick loop itself (runScheduleTick, runSchedule, joinTick, isDispatchable) are all inside that mirror, so the quiescent exit almost certainly lands in BOTH copies. Every twin edit lands in both files in ONE commit. reconcile.mjs is also a WHOLE mirror - check whether the change reaches it. Landing stays SERIAL for mitosis.js and the mirror census: parallelize investigation, never landing. Re-derive the twin surface per MSP; 0151 established the gotcha list is incomplete.

THE M4 TRAP, WHICH M5 IS POSITIONED TO REPEAT. M4 is still THREATENED because three behavior changes plus a structural rename shared one commit range AND three characterization tests were rewritten inside it - which destroys the tests' evidentiary value. M5 must NOT rewrite a characterization test's assertions in the same commit as the behavior change. DELETING a characterization test wholesale alongside the mechanism it characterizes is different and is CORRECT here: merge-poll-characterization.test.mjs was deliberately kept in its own file so M5 can delete it with the production change (0189, 0190). The distinction is the whole point - deletion preserves the record, rewriting launders it.

THE RECEIPT THAT MAKES M5's DELETION HONEST. A5's test is the instrument. Before deleting it, run it via MITOSIS_PATH against a scratch PRE-deletion engine copy to confirm it still passes, then against the post-deletion engine to confirm the pinned behaviors are genuinely gone rather than merely relocated. That is the non-inertness proof for the deletion, mirroring what M3 did. Then delete the test file in the same commit.

TERMINATION MUST BE PROVEN, NOT ASSERTED. Spec section 3.2 justifies deleting maxSteps by claiming the loop cannot spin because every iteration either performs an action or exits. Once maxSteps is gone that claim is load-bearing for liveness. M5 owes a test that the loop terminates on the no-actions path - a hang is worse than the bounded wrong stop it replaces, and 0185 already flagged this exact trade as a Quality-pillar regression if taken carelessly.

FOUR RESIDUALS ASSIGNED TO M5, all already adjudicated - read the records, do not re-litigate:
- 0187: advance.toRestack is computed at reconcile.mjs:122/:130 plus its twin and logged at mitosis.js:3996 with ZERO consumers. M5 rewrites this region, so M5 fixes the line. The same log string also carries the AIMD W= clause that M4 deletes; prompt-snapshot.test.mjs may pin the string, unverified.
- 0188: the isBuiltResume restore call at mitosis.js:4586 passes only builtRef, so the sha-verification block at :4572-4579 never runs there, while the frontier redispatch at :4592 passes requireSha true. A weaker check inside a call that does happen, not a bypass. Decide explicitly whether M5 closes it or declares it again.
- 0190: G5's wiring gap. mergeWatchPrompt defaults to 300/30 internally at mitosis.js:2817-2818, so the call site at :5068 is unpinned. If M5 deletes that call site this residual dies with it - say so rather than leaving it dangling.
- 0185: spec section 10's M3 row and section 8 row 2 are inaccurate as written. Re-baseline them BY EXECUTION as part of M5 rather than as a standalone doc rewrite, which 0148 rejected.

WORKFLOW SHAPE, unchanged from the shape that shipped A5 with zero remediation rounds: Ground x3 / Design / Implement sole-writer / Verify x3 / Remediate cap 2 / Ship. Keep it SMALL, under 15 agents. One change from A5, and it is mandatory: GIVE EACH AGENT A DISTINCT SCRATCH SUBDIRECTORY. A5's inertness verifier had its own scratch file overwritten mid-run by a sibling sharing the path; its results survived only because they were printed first. The three verify lenses that worked: mutation inertness re-derived with the verifier's OWN harness rather than the implementer's, coverage-receipt completeness and honesty, and scope plus admission gate. For M5 add a FOURTH lens: twin parity, asserting mirror-guard passes and that every twin edit landed in both copies in one commit.

NON-NEGOTIABLES TO CARRY VERBATIM INTO THE DISPATCH PROMPT:
- COMMIT docs/invariants/coverage/<branch>.json with a row for ALL 12 registry ids, verdict domain two-valued (threatened / not-threatened). The PR-mode gate diffs merge-base..HEAD and CANNOT see untracked paths, so an uncommitted receipt is an invisible receipt and CI goes red. The gate never evaluates whether a check string is TRUE, so every string must state what was actually run and say so plainly where something was not.
- NEVER invoke the compiled workflow. Use the AsyncFunction harness with a stub agent, MITOSIS_PATH for mutants.
- NEVER git add -A. Stage explicit paths only; the five pre-existing dirty paths must survive untouched.
- No code comments, no emojis, no AI co-author attribution.
- PR only through node .claude/lib/superpowers-parallel/mitosis-git.mjs pr-create. Title grammar type(scope): lowercase imperative, max 72 chars; suggested type feat, scope mitosis, since M5 introduces new terminal behavior rather than only removing mechanism - confirm against the diff before composing. Scope is FROZEN at creation: title and body edits are denied at the gate, later facts go in a PR comment. Angle brackets are rejected in a --what value; reword, never force. Honesty rule on --verified lines is absolute. Do NOT merge - merge is human-gated.
- Cut the branch in the MAIN thread BEFORE dispatching; never pair a branch-switching agent with one reading the working tree. Suggested branch feat/m5-quiescent-exit.
- Writes under .claude/{hooks,rules,lib,workflows} need per-write approval, so the run WILL prompt the user.

METHOD RULE, now eight instances deep and the single highest-yield habit in this thread: verify a confident finding against the mechanism before acting on it. Every A5 scope change came from mutation rather than from reading for absence, and reading would have produced two redundant tests and missed two real holes. A red result from a hand-rolled shell one-liner is suspect BEFORE the code is - re-run it in node.