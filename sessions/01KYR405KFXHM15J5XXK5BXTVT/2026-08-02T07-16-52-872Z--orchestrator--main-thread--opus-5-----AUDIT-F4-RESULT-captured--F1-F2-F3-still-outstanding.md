AUDIT F4 (inert window:9999 clamp premise) RETURNED. F1, F2 and F3 were still running. Nothing implemented; thread remains paused; repo untouched. Captured here so the result survives the session.

HEADLINE: THE FINDING IS NOT INVERTED. manifest.window is read NOWHERE. The premise holds.

MECHANISM, measured at fc035fc. The only live `.window` consumer in the engine is runSchedule (mitosis.js:2397, reading opts.window at :2399), and its ONLY call site passes { window: buildAheadCap } at :5050, where buildAheadCap comes from input.buildAheadCapOverride or the constant BUILD_AHEAD_CAP = 8 at :2403. Nothing routes a parsed manifest into it. applyRunDelta (mitosis.js:814) has cases for ship/built/park/quiescent-exit and NO 'window' case -- 777617b's own diff deleted that case plus priorManifest.window, clampWindow, nextWindow, windowDelta and persistWindowCheckpoint. Twins agree: leases.mjs:173-176 same shape, run-log.mjs:35 applyRunDelta has no window case, window.mjs is a one-line stub exporting BUILD_AHEAD_CAP = 8.

THE BLAST RADIUS IS LARGER THAN THE FINDING STATED, and this is the part worth keeping. frontierManifest is called ~25 TIMES in tests/frontier-train-e2e.test.mjs with window: 3/4/5/9999, not the two sites the finding named -- every one of them is the same dead data. Separately, a byte-similar but TEXTUALLY INDEPENDENT copy of frontierManifest exists at tests/reconcile-only-advance-characterization.test.mjs:96 with four more call sites (window: 3 at :233/:279/:305/:328); editing the e2e helper does not touch it. So "drop the frontierManifest window param" is a ~30-line, two-file change, not the local tidy the finding implied.

A LOWER-LAYER ASSERTION ALREADY EXISTS and does NOT make the e2e pair redundant. tests/recovery.test.mjs:639 asserts resolved.manifest.window === undefined. The audit REJECTED relying on it: that test only proves resolveRunIdentity does not copy the field into its output, while buildAheadCap is computed on a wholly separate CLI-driven path (:3473-3476) that the unit test never exercises. Deleting the e2e pair would leave the user-visible guarantee -- dispatch never exceeds 8 regardless of manifest content -- unasserted anywhere. Two layers, two different wiring gaps.

RECOMMENDATION: NARROW FIX, AMENDED ONTO 777617b, NOT fc035fc. The justification is strong and is new information: 777617b ALREADY retitled these two tests (E9 old title said "clamped at every read site ... WINDOW_CEILING", new title says "ignored outright ... BUILD_AHEAD_CAP") while leaving the adjacent frontierManifest({ msps, window: 9999 }) fixture untouched. That commit's own diff therefore contains a line its own retitle contradicts. Finishing the edit inside that commit is completing an edit already open there, not new scope. fc035fc has an empty diff intersection with this file. Edits: :894 and :1247 drop the fixture arg; :913 and :1268 restate the assertion messages; retitle at :886 and :1239 to pin BUILD_AHEAD_CAP alone and drop persisted/clamped/ignored language implying a read-then-discard behaviour. The wide fix (all 25 + 4 call sites, helper signature) was judged the WRONG SURFACE for M4 and belongs in a named test-hygiene follow-up.

VERIFICATION GAP THE AUDITOR DECLARED HONESTLY, AND IT MATTERS. It did NOT run the mutation itself, did NOT run the suite, and did NOT reproduce the lens's "both tests genuinely redden" proof. The conclusion rests on exhaustive static analysis plus 777617b's own diff. Under this thread's method rule that is NOT sufficient on its own for the retention half of the claim -- the inertness half is well proven statically, but "these tests still redden on a real cap regression" remains lens-reported and unreproduced. Any implementer dispatch must close that gap or state it as not-run.

ONE FALSIFIER THE AUDIT LEFT OPEN IS PROBABLY MOOT AND MUST BE CHECKED AT RECONCILE TIME. It flagged the risk that a future M5 already plans to delete these two tests, inferring futurity from the commit message "pin the bounded merge poll before m5 deletes it". M5 IS ALREADY MERGED as 9ea75d6 and re-opening it is out of scope, so that inference is backwards -- but confirm against the M4/M5 spec text rather than assuming. Its other falsifier stands: a dynamic manifest[key] access resolving to 'window' would invert §2.

RECONCILE-TIME NOTE. F4 now argues for amending 777617b. F2's coverage remedy also amends 777617b. If F1 or F3 also land on commit 1, that is one amend carrying several unrelated changes -- decide deliberately whether they ride together or whether some defer, because PR 1's scope freezes at creation.