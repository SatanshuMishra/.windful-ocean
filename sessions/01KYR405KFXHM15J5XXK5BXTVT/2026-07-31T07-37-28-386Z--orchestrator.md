Hermeticity gate executed and cleared; M0 implemented, verified and shipped as PR #21.

GATE (0149). Worktree at 11421ef: 1752 tests / 1752 pass / 0 fail. Primary at the same commit: 1761 / 1761 / 0 fail. The blocker is refuted on both halves - nothing fails in either environment, and 1520/1540 was stale. The 9-test delta was localized by diffing test-name SETS: all 9 belong to .claude/hooks/tests/session-config-drift-check.test.mjs, UNTRACKED because .gitignore:18's `*session*` pattern matches its filename, so a clean checkout cannot contain it. Recorded as 0150.

M0. Branch fix/m0-built-transition-clears-resumepoint, two commits (db9ac15 fix, ab33673 coverage), PR #21 open against main.

RE-DERIVATION FINDING, load-bearing and NOT in the spec: applyBuiltTransition is a mirror twin. recovery.mjs is listed in mirror-guard.test.mjs:19, so the fix had to land byte-identically in BOTH recovery.mjs:175 and the inline copy at mitosis.js:514. Spec section 10's gotcha list names only M3, M4 and M7 and omits M0 - the same incomplete-hand-enumeration class as 0137/0144/0145. Assume that list is incomplete for M1, M2, M5, M6, M8 too; re-derive per MSP.

Anchors re-derived at 11421ef (spec is pinned to 450804e): applyBuiltTransition mitosis.js:509 / recovery.mjs:170; park cascade mitosis.js:2303-2310, twin parking.mjs:75-82; relaunch guard mitosis.js:3784; parkDelta null convention mitosis.js:555.

Verified first-hand by the orchestrator, not taken from the subagent: full suite 1762/1762 0 fail; mirror-guard 22/22; red-on-parent by reverting both twin edits and re-running (43/44, exactly the new test); green 44/44; invariant-coverage-check in pull_request mode ok across all 12 ids. The coverage entry docs/invariants/coverage/fix-m0-built-transition-resumepoint.json was orchestrator-authored per 0143, with M1, M3 and M5 recorded threatened.

HOUSEKEEPING. The scratch worktree was removed after the run, so the leak count is unchanged at 12. .claude/settings.json and .zshrc were left dirty and unstaged throughout; only explicit paths were staged. Two parked stashes untouched.

NEXT. PR #21 awaits human merge. Then M1, and the order M2 -> M6 -> M3 -> M5 -> M4 -> M7 -> M8. Separately, the gitignored drift-check test file is a real spec-loss-class exposure belonging to the boundary-residuals thread's SessionStart freshness work, not to the engine MSPs.