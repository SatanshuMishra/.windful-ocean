Two MSPs shipped and merged this session. (1) P2 re-landed: cut fix/m2-monotone-status-onto-main from origin/main 8933c2c, cherry-picked the nine commits f04b8de..05bfa7f in order with zero conflicts, opened PR #26, merged by the user as e7e4025. (2) A7 implemented from scratch: branch refactor/divergence-facts off e7e4025, four commits, 245 lines - divergence.mjs extracting runDivergenceProbes and SHA_HEX_PATTERN as a WHOLE policed twin, the constant relocated below divergenceProbePrompt for contiguity, a ctx seam carrying the five deps plan section 5.3 refuses, six behavioural tests, and a 12-row coverage entry at docs/invariants/coverage/refactor-divergence-facts.json. Opened as PR #27, merged as 5dd7176. Main is green at both. Receipts executed by the orchestrator: npm test 1813/1813 then 1820/1820, both exit 0; the coverage gate in pull_request mode ok on all 12 ids at each landing; nine identical patch-ids for the re-land; twin byte-identity re-derived INDEPENDENTLY of mirror-guard, the normalized module body occurring in normalized mitosis.js exactly once. Established facts worth keeping: origin/main was byte-identical to the P2 branch point on all five touched files, so the feared cross-branch deletion was a two-dot-diff artifact only; scripts/invariant-coverage-check.mjs keys entries by registry-id completeness and never by branch name; and .githooks/pre-commit runs the full suite on every commit but is NOT triggered by cherry-pick. What went wrong and how it was caught: my first byte-identity check was a malformed shell one-liner (BSD sed plus unquoted zsh globs) that reported a false DIFFERS and had to be redone in node - the lesson is that a negative result from a hand-rolled shell check deserves suspicion before the code does. The coverage entry shipped its first draft with four stale suite counts of 1818 that the two added tests invalidated; caught on re-read and corrected to 1820 before push, since a committed honesty artifact cannot carry a number I know to be wrong. Several record_decision calls failed validation on malformed parameter emission until the field order was changed. Code review returned approve-with-fixes: the untested-error-branch finding was accepted and fixed (ctx.clean had zero assertion coverage, the exact failure this refactor risks), the shadowing rename was declined on record because it would destroy byte-identity with the text production already ran. Nothing is left running; no background shells, no live agents.