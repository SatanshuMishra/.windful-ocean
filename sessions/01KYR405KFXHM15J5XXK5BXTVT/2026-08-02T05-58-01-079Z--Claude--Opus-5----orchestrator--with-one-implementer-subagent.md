M4 COMMIT 1 IS LANDED AND GREEN. One implementer subagent dispatched, returned, nothing running.

SHIPPED. Commit 777617b "feat(mitosis): replace the aimd window with a fixed build-ahead cap" on feat/m4-fixed-build-ahead-cap, 13 files +57/-207, verified first-hand by the orchestrator (git log, git show --stat, git status). NOT PUSHED, no PR. Suite 1839 pass / 0 fail LOCAL, down from the 1846 baseline: -8 deleted, +1 added, fully accounted. Working tree still carries ONLY the five pre-existing dirty paths; git add -A was never used.

BOTH 0197 PRECONDITIONS DISCHARGED BY MEASUREMENT, AND 0197 HELD. (a) manifest.window had no surviving reader - the grep covered bracket notation, destructuring and dynamic access, not only dotted reads; the non-deletion hits were writers, a different object (opts.window at leases.mjs:175, scheduler opts not the manifest), and two tests. (b) applyRunDelta proved INERT on an unknown record kind by a two-arm execution against a scratch copy selected through MITOSIS_PATH, so the 'window' case was deleted as dead code rather than kept as an explicit ignore; the inertness is now pinned by a rewritten run-log.test.mjs. Bonus defect the deletion removes: at HEAD the fold wrote {"kind":"window","size":"not-a-number"} straight through to window:"not-a-number" with no validation.

SCOPED TDD WAS RUN on the three behavior changes and all three failed first for the right reason (planTick fixed-cap fallback; the I3 precedence carry-over; the legacy-window-record inertness).

THREE FINDINGS THAT CHANGE THE MAP, each recorded as a decision this session. The reviewDecision capture at :3089 was DELETED, not kept - the brief's UNLESS clause fired because grep proved the provenance/contested branches never read that field. The .mitosis/ gitignore premise stated in 0197 and the dispatch brief is FALSE on this machine. And three e2e tests were DELETED rather than updated, with two WINDOW_CEILING clamp tests conversely kept and retitled - the largest judgment call in the commit.

CONFIG DEFECT FOUND WHILE READING IN, NOT FIXED, NOT M4'S. There are TWO ledger stores on disk for this project and they disagree. session-continuity-inline/.../ledger-worktree is LIVE (carries 0196-0198, the grounding map, this entry). session-continuity-continuity-ledger/.../ledger-worktree is STALE, frozen at 2026-08-02T04:23, its copy of this thread stopping at 0195 and still asserting "CI HAS NEVER BEEN READ" and "THE MAIN WORKTREE IS STILL ON THE MERGED feat/m5-quiescent-exit" - both false now. The MCP server serves the live one, so the resume brief was correct, but the stale copy is internally consistent enough to be believed by anything that reads the ledger BY FILE PATH. Read the ledger through the MCP tools only.

FAILED / NOT DONE: nothing failed. Commit 2, verification and ship were deliberately not started - the session stopped at the atomic green boundary on the context budget, and the user then issued the stacked-PR directive for the fresh session.