ALL FOUR AUDITS ARE IN. F2 returned last and is the most decisive: it BUILT the amend-and-rebase scenario in throwaway worktrees and measured it. Nothing implemented; thread stays paused; no implementer dispatched. Repo untouched, worktrees removed, main worktree HEAD still fc035fc.

F2's LOAD-BEARING MEASUREMENT: TWO COVERAGE ARTIFACTS ARE REQUIRED, NOT ONE. It amended commit 1 with a coverage file (new SHA), rebased commit 2 cleanly onto it, and re-ran the gate in PR-2 shape: STILL RED. Because the rebased history is linear, merge-base(base, HEAD) == base, so a file present identically in both trees yields ZERO diff lines. Adding a second distinctly-named file on commit 2 turns it green. Editing commit 1's file from commit 2 also works mechanically but was REJECTED as the wrong surface -- it breaks the file-per-branch traceability all 16 existing coverage files follow and puts PR 1's file identity inside PR 2's diff.

THIS CORRECTS 0201's RATIONALE. The gate evaluates the PULL REQUEST'S DIFF against merge-base, NOT each commit in isolation. So 0201's stated reason -- "777617b itself fails the pull_request gate, so the remedy must be an AMEND rather than a follow-up" -- rests on a per-commit reading the measurement contradicts: a follow-up COMMIT on the same branch would satisfy PR 1's gate just as well as an amend. 0201's CONCLUSION may still stand, but not on that ground. Recorded so the next session does not inherit the wrong mechanism.

THE PLACEMENT CONFLICT RESOLVES TO AMEND ANYWAY, on a reason F1 could not have known. F3 requires 777617b's COMMIT MESSAGE corrected -- it is wrong in both directions (null is refused but not logged; 9999 is not refused at all) -- and only an amend can rewrite a message. F1's sole argument for a new commit was SHA preservation, which buys nothing here: the rebase of commit 2 happens under either shape, nothing is pushed, and no PR exists. AMEND BOTH COMMITS. F1's own recommendation is otherwise adopted unchanged; only its placement is overridden.

EXACT GATE CONTRACT, from a full read of all 229 lines of scripts/invariant-coverage-check.mjs. Verdict domain is exactly two-valued, threatened / not-threatened (:9) -- 0142 CONFIRMED against code and against all 16 artifacts. Rows need non-empty id, non-empty check, allowed verdict (:106-117). Id-set must equal the registry's 12 ids exactly: no missing, no unknown, no duplicate (:130-138). Coverage dir admits only .json and must be non-empty (:93-103). EVERY existing coverage file is shape-validated on EVERY run, push or PR (:203-207) -- so a malformed stub cannot pass silently. pull_request requires a non-empty base ref with NO fallback (:162-164); unresolved base is a hard fail, never degrades to push semantics (:150-159). The diff check is git diff --name-only --diff-filter=d merge-base..HEAD filtered to docs/invariants/coverage/ (:167-177). THERE IS NO FILENAME OR BRANCH-SLUG VALIDATION ANYWHERE -- the branch-slug naming is house CONVENTION (16/16 precedent), enforced by nothing.

TWO CORRECTIONS TO THIS THREAD'S FRAMING, both verified live. (1) GITHUB_BASE_REF is a GitHub-native primitive auto-populated only on pull_request/pull_request_target, so a stacked PR resolves its real base without workflow config. (2) "REQUIRED job" is SOFTER than assumed: gh api shows NO classic branch protection on main and exactly one ruleset, "Require PR for Merge" (id 19939922), carrying NO required_status_checks. CI red would not physically block the merge button today; the requirement is this project's documented human-gated process, not a GitHub-enforced gate.

A FIFTH FINDING, DISCOVERED BY F2 AND MISSED BY THE ORIGINAL FOUR-LENS GATE. fc035fc's new counting logic has ZERO acceptance-test coverage, proven by mutation: replacing the increment at reconcile.mjs:115 with `if (false)` leaves reconcile.test.mjs at 23/23 GREEN. The two tests the commit touched (:202-203, :221) assert invalidatingParents === 0 only in NO-INVALIDATION cases; none of the four tests that exercise a real invalidation (:133-139, :142-155, :157-162, :164-183) assert the counter at all. NOTE THE TENSION: the REMAINING-PHASES BRIEF ordered "NO TEST -- section 11 refuses one and a log-string assertion is a change detector." That refusal was aimed at asserting the LOG STRING. The counter is a returned integer on planReconcile's return shape and is unit-assertable without any change-detector coupling, which section 11's refusal did not contemplate. This needs an explicit ruling, not an implementer's guess.

THE AMEND TO 777617b NOW CARRIES FOUR UNRELATED CHANGES: F2's coverage file, F3's validator move, F4's clamp-test cleanup, F1's restored foreign-branch e2e test. DECISION A SAID PR 1 CARRIES "777617b ALONE". That is no longer true under any reading, and it is a deliberate scope amendment requiring the user, not a detail to absorb.

F2 DRAFTED FULL 12-ROW CONTENT for both files, each row grounded in what it measured. Highlights: B6 not-threatened WITH a positive distinguishing fact -- BUILD_AHEAD_CAP's export has a genuine non-test importer at leases.mjs:2, so it does not repeat M6's self-referential-lint standing cost. M3 for commit 1 is threatened WITH an executed red-then-green receipt: 777617b's own changed test files overlaid on parent 9ea75d6 give 165/170 with the 5 failures naming the fixed-cap behaviour, and 170/170 on HEAD. M3 for commit 2 is threatened and carries the fifth finding above, candidly stated per 0143. M1 and M5 are threatened as the standard self-references.

BASELINE RE-CONFIRMED INDEPENDENTLY: 1830/1830 on BOTH commits in clean throwaway worktrees. The 1839 local figure is main-worktree inflation, as 0201 said.

BARE-INVOCATION ERGONOMICS: named as a REAL and RECURRING defect -- a local run with no env vars silently resolves to push mode and reports green, which has now caused two documented incidents (M6's disclosed-but-unactioned coverage caveat, and this M4 finding across three green readings). F2 judged the workflow trigger config itself NOT a defect (push genuinely has no base ref to diff). Remedy belongs to its own follow-up against the checker script, e.g. a --strict flag or a warning whenever mode resolves to push implicitly. EXPLICITLY OUT OF M4's SCOPE.

RE-VERIFICATION CHECKLIST, measured green by F2 after its own two-file remedy: npm test on C1' alone (1830/1830), npm test on C2' alone after rebase+amend (1830/1830), mirror-guard 46/46 on each, and the gate in pull_request mode on each against its REAL base (C1' vs main, C2' vs C1'). F2 flags mitosis-gate.mjs phase-parity (test.yml:21) as part of the same job set but NOT run by it -- the implementer must include it. F1 additionally demands the sandbox-compile gate (commit 95ef8e1) because mitosis.js is eval'd as one script body.

THREE RULINGS REQUIRED FROM THE USER BEFORE ANY IMPLEMENTER RUNS: (1) the buildAheadCap upper-bound policy from F3 -- bound to 1..BUILD_AHEAD_CAP, leave unbounded-but-halting, or delete the override entirely; (2) whether PR 1 carries all four changes or the scope splits, amending decision A either way; (3) whether to assert invalidatingParents in the four real-invalidation tests despite the earlier no-test ruling.