M8 SHIPPED, MERGED, AND ITS DISCLOSED TEST-INERTNESS RISK IS NOW DISCHARGED BY MEASUREMENT.

WHAT LANDED. Dispatched the dedicated small dynamic Workflow 0213 called for (run wf_9b4f284a-0f5, 13 agents, 0 errors, ~7.2h, 3.25M subagent tokens): 3 parallel grounding agents -> plan -> adversarial plan audit -> reconcile -> 1 implementer in an isolated worktree -> 3 fenced review lenses -> 1 bounded remediation round -> 1 independent receipts executor -> ship. PR 37 opened, CI FULLY GREEN (11/11 check runs incl. `test`), merged by the user as 4fd03c2. Local main fast-forwarded to 4fd03c2; the five deliberate dirty paths are untouched. 15 commits, 16 files, +1889/-34.

Mechanism: the CI-to-green loop hosted on the EXISTING remediation supervisor (no new supervisor), 3 attempts per PR, each requiring a new failure fingerprint, recorded durably in a dedicated `ciAttempts` field BEFORE dispatch (moved out of triedSet in remediation, because park() replaces triedSet wholesale). All six escalation classes in a new WHOLE twin `ci-escalation.mjs` (MIRROR_CENSUS 46 -> 47 rows). Class 6 at FILE granularity per 0087. Exhaustion parks `ci-red-exhausted`, finally giving M1's producerless status a producer. Both M7 residuals landed in both halves per 0212.

WORKFLOW QUALITY SIGNALS, recorded candidly. Plan audit: 17 findings, 0 blocking, 3/138 anchors wrong. Review: 35 findings across 3 lenses, 12 CRITICAL/HIGH. Remediation: 16 fixed, 0 refuted, 7 open residuals (all disclosed in the PR body). ZERO REFUTATIONS ACROSS BOTH ADVERSARIAL PHASES (0/17 and 0/35) despite both agents being explicitly fenced to reproduce-before-agreeing and to refute with evidence. On this project's history that reads more like agreeableness than 52 correct findings; it is not a claim anything is wrong, it is a reason to read the remediation commits with more suspicion than the green suite invites.

BASELINE CORRECTION, measured by the orchestrator directly. The spine recorded 1836/1835/1 at e9306e6. Actual measurement of main at e9306e6 was 1845/1844/1/0. The 1836 figure was WRONG and has been corrected in the spine. Post-merge baseline at 4fd03c2 is 1887/1886/1/0.

INERTNESS PROOF (the session's second half, on explicit user instruction after the merge). 5 subagents in parallel, each in its own auto-cleaned isolated worktree so mutations to the two shared mirror halves could not collide. 40 mutations total, every one applied to BOTH halves, every one run against the full 1887-test suite, every one restored with a verified-empty diff plus a re-run landing on exact baseline.

VERDICT: ALL FOUR DENY-CASES (classes 2,3,4,5) PROVEN NON-INERT, at BOTH the unit and the engine level. The scoping insight that made the proof real: these are not one mechanism per test. Class 2 rests on 3 mechanisms (predicate, class label, and an 8-element whitelist whose elements are 8 INDEPENDENT data limbs - node assert is fail-fast in the loop, so all 8 needed separate mutations). Class 3 rests on 6. Class 4 on 2. Class 5 on 2 directions. A one-mutation-per-test approach would have proven one limb each and missed every gap below.

TWO ORCHESTRATOR PREDICTIONS REFUTED BY MEASUREMENT. (1) I predicted class-0 shadowing would make class 4's test non-discriminating. Shadowing IS real - deleting 'codeql' from the constant returns class 0, not class 4 - but the test survives it because it asserts `.class === 4` rather than truthiness. Written as "escalates", it would have been satisfied by the census and proven nothing. (2) I predicted classes 2-5 might be pure-function-only, with the engine never consulting the classifier. REFUTED: mitosis-scheduler.test.mjs:4696 (`CI-CLASS-DENY`) drives all five classes end-to-end through the real engine via invokeMitosis. Proven per class by making the engine honour every class EXCEPT N; each reddened on the load-bearing assertion (zero ci-probe/ci-fix/ci-diff/ci-publish dispatches). Where guards were broken the tests caught the REAL consequence: the autonomous fixer actually running against a failing CodeQL check, a red receipts enforcer, and a cancelled CI run.

SEVEN GAPS, EACH MEASURED GREEN (mutation applied, full suite stayed at exact baseline). These are measurements, not suspicions. In every case the SHIPPED CODE IS CORRECT; what is missing is any test that would catch the guard's removal.
  1. `'invariant-coverage'` enforcer token pinned by NOTHING. Delete it -> suite at exact baseline. Not rescued by the class-0 census either, since the string contains the ordinary token 'coverage', so removing it sends a failing invariant-coverage check straight to an autonomous fix attempt. That is this repo's own coverage gate.
  2. CI_SECURITY_CHECK_TOKENS unpinned for MIXED-TOKEN check names. For every name the suite exercises, class 0 alone suffices to park - the token list only buys the correct label. It becomes load-bearing exactly where no test looks: names carrying both an ordinary and a security token. Measured: `dependency-review lint` is class 4 today; empty the list and it is `escalate:false`, silently, nothing red. Real check names have this shape (`Trivy / test`, `CodeQL build`, `security scan (ubuntu-latest, build)`).
  3+4. `'d6'` and `'cluster-boundary'` each separately deletable - the fixture "D6 cluster-boundary interaction tests" contains both, so it pins only "at least one still matches".
  5. Case-insensitive matching for ENFORCER names rests solely on class 4's 'CodeQL'; class 3's uppercase 'D6' is rescued by the lowercase 'cluster-boundary' substring.
  6. Class 2's PRECEDENCE over classes 3-5 unpinned: move the class-2 line below class 5 and the whole suite stays green. A cancelled run with a red receipts enforcer reclassifies 2->3 silently. Both still park, so consequence is confined to label and reason.
  7. mitosis.js:5209 (the probe/publish classifier call site) deletable outright with no test reddening. TRACED AND JUDGED REDUNDANCY, NOT A HOLE: every fix dispatch is gated by the other call site at :5327, pinned for all five classes, and an unescalated probe result flows back as evidence into the next cycle's diagnose. Caveat left standing: the redundancy is undefended.
WHAT CLOSES 1-5: roughly six assertions in the existing class-3 and class-4 tests (a bare `invariant-coverage` name, a mixed-token name, separate bare `D6` and `cluster-boundary` names, an uppercase enforcer name). 6 needs one fixture where two classes are simultaneously true, asserted as class 2. 7 is a design question, not a test gap. TEST FILES ONLY, no production code. NOT AUTHORIZED - offered and the user did not take it up this session.

DEPLOYMENT QUESTION ANSWERED BY INODE, not by reasoning. ~/.claude/workflows and ~/.claude/lib are symlinks into this repo; ~/.claude/workflows/mitosis.js and the repo copy are the SAME INODE (130074761), so the merged M8 engine is LIVE with no install step. The command that makes a merged change live is `git pull --ff-only` on main, not an installer - and before the orchestrator fast-forwarded, the symlink was still serving pre-M8 code despite the PR being merged. EXCEPTION: ~/.claude/settings.json is a REAL FILE (inode 129889333) not a symlink to the repo copy (129839372), so repo edits there do NOT propagate. That is the known-broken topology leg, the reason .claude/settings.json shows permanently modified, and the reason protect-claude-config.test.mjs fails locally.

HOUSEKEEPING LEFT: the mitosis-ci-to-green worktree and branch are merged and eligible for reaping (squash-merge makes `git branch -d` refuse; the 0207 criterion is an empty residual diff against origin/main, verified BEFORE -D). All 5 mutation worktrees auto-cleaned. Nothing running at hand-off.

M8 WAS THE LAST MSP of the quiescent-advance landing plan. Only M0 remains and it is still UNAUTHORIZED.

ARTIFACTS (EPHEMERAL - /private/tmp session scratchpad, a fresh session gets a different dir; re-derive from code if gone): .../scratchpad/m8/{plan.md, plan-final.md, plan-audit.md, implementation.md, remediation.md, receipts.md, ship.md, ground-*.md}. plan-final.md section 1 carries the ten M8 invariants I1-I10 with their headings at lines 77, 106, 127, 147, 170, 184, 206, 227, 243, 257.