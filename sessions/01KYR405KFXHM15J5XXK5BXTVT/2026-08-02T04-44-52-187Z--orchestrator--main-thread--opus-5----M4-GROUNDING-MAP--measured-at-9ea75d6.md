M4 OPENED. Nothing implemented; no edits, no commits beyond the branch cut. This entry is the machine-derived anchor set so the next session does NOT re-run the 159k-token grounding pass. Every cite below was grepped at 9ea75d6 this session. Scope corrections are decision 0196.

STATE AT HAND-OFF. Branch `feat/m4-fixed-build-ahead-cap` cut off origin/main at 9ea75d6 and CHECKED OUT IN THE MAIN WORKTREE (the main worktree is no longer on the merged feat/m5-quiescent-exit; that branch ref survives). Zero commits on it. Working tree carries ONLY the five known pre-existing dirty paths. No worktree was created, so nothing new can leak.

CI IS NOW READ, closing the thread's longest-standing risk. On 9ea75d6: workflow `test` completed/success, workflow `security` completed/success. `git diff --stat 556706c 9ea75d6` is EMPTY, so M5's local 1846-pass measurement and this CI green are on an identical tree, and every anchor below holds at both commits. The repo runs three workflows only (test, security, Pull Request Labeler); there is no pr-title-lint job here.

A. AIMD SURFACE. WINDOW_FLOOR window.mjs:1 (=3) / mitosis.js:2405; WINDOW_CEILING window.mjs:2 (=8) / mitosis.js:2406; WINDOW_INCREMENT window.mjs:3 (=1) / mitosis.js:2407; nextWindow window.mjs:5-10 / mitosis.js:2409-2413, consumed at reconcile.mjs:106 and its twin mitosis.js:2992 inside planReconcile; clampWindow mitosis.js:2420-2422 ONLY (no lib copy, no twin obligation), consumed at mitosis.js:4253 and :4339-4340. WINDOW_FLOOR has a NON-AIMD consumer: leases.mjs:111 buildAheadWindow's fallback when windowSize is not an integer (twin mitosis.js:2336-2337).

B. DURABLE DELTA. windowDelta window.mjs:12-14 / mitosis.js:2416-2418, sole caller persistWindowCheckpoint. persistWindowCheckpoint mitosis.js:5026-5033 ONLY (no lib copy). Exactly ONE live production call site, mitosis.js:4342, inside the relaunch-seeding block at :4339-4346. The string 'shepherd' there is a unitId LABEL, a naming fossil of the deleted shepherd path, not a separate call path.

C. REVIEW-DECISION READ — RETARGETED, see 0196. readReviewDecision and resolveReviewEvent DO NOT EXIST; M5 deleted them in 9ea75d6 (proved by git log -S, two hits: d0ad328 introduced, 9ea75d6 removed). The live pipeline is mitosis.js:3089 (reviewDecision capture in the open-PR classifier) -> buildReconcileLiveSignals :3106-3121 events loop (APPROVED->approved, CHANGES_REQUESTED->changes-requested), defined mitosis.js-only, single call site :3938 -> planReconcile's nextW reduce. That events loop is AIMD's ONLY consumer.

D. 0187 RESIDUAL. Log line mitosis.js:3988 still ends `; AIMD window W=${advance.nextW}`. toRestack computed in planReconcile mitosis.js:2988-3019 / reconcile.mjs:102-133, assigned at :3947-3948. MEASURED: toRestack AND toOpen both have ZERO consumers other than that one log string. (Contrast: toParkSubtree consumed at :4272, nextW at :4339-4340.) M4 owes only the AIMD clause; the broader toRestack/toOpen dead-weight question is not assigned to M4 by the spec.

E. MIRROR CENSUS. MIRROR_CENSUS at tests/mirror-guard.test.mjs:24-64; libModuleNames() at :115-120 is readdirSync(LIB).filter(isFile && .mjs), NON-RECURSIVE — tests/ entries are directories and are filtered out, so the tests-are-not-governed claim is RE-CONFIRMED at this tree. Governed `whole` twins M4 touches: window.mjs (:48), reconcile.mjs (:39), leases.mjs (:31), recovery.mjs (:40), run-log.mjs (:44). NOT governed: clampWindow, buildReconcileLiveSignals, persistWindowCheckpoint (all mitosis.js-only), and every test file.

F. PINNING TESTS. LOAD-TIME failures if exports go without the file changing in the same commit: window.test.mjs:3 imports {nextWindow, windowDelta, WINDOW_FLOOR, WINDOW_CEILING}; run-log.test.mjs:6 imports {windowDelta} (used :160). reconcile.test.mjs:201-205 is the one genuinely AIMD test, BUT :208-209 and :227 assert planReconcile's whole return object via deepEqual and break on dropping nextW despite not being about AIMD. recovery.test.mjs:639 pins the window carry-over under the label 'the persisted AIMD window is carried'; :600 pins 'a top-level window is rejected' and SURVIVES regardless. frontier-train-e2e.test.mjs: the nine 'AIMD window W=' assertions independently re-counted and CONFIRMED at exactly nine — :477 :502 :586 :732 :733 :854 :1106 :1311 :1335 (7 presence, 2 absence). That file loads mitosis.js via new AsyncFunction, so it will NOT fail at import; it fails at assertion when the log format changes, which is the intent.

G. INSTRUMENTATION SITE. descendantsToInvalidate parking.mjs:52 / mitosis.js:2471, called only inside planReconcile (mitosis.js:2996-3000 / reconcile.mjs:110-114), driven by assembleDivergenceVerdicts (reconcile.mjs:76 / mitosis.js:2962). planReconcile has exactly ONE production call site, mitosis.js:3947, inside the isRelaunch && reusable && builtUnits.length>0 block :3936-3989 — it is a pre-loop one-shot, not called from the scheduler loop. Natural counting site is the existing per-id reset loop :3949-3967, which already logs 'RESET by divergent-invalidation' per unit; both counters are already-computed integers (verdicts keys; parkSubtreeSet.size at :3979), NO new traversal and NO timestamp needed. Sandbox ban re-verified live: workflow-sandbox.mjs:39,46 traps the whole Date global, and grep -c Date mitosis.js is 0 — the engine is genuinely Date-free today. applyRunDelta mitosis.js:815-829 / run-log.mjs:35-49 keys on record.kind ('ship'|'built'|'park'|'window'|'quiescent-exit'); a durable counter would need a case in BOTH twins, a per-run local would need none.

H. WINDOW CARRY-OVER, LEFT OPEN BY 0196. mitosis.js:722 `if (Number.isInteger(local.window)) manifest.window = local.window;` (twin in recovery.mjs, resolveRunIdentity at mitosis.js:642 / recovery.mjs:297), siblings at :723-726 (parked, harnessRunId, quiescentExitAt, quiescentExitOutstanding). window is excluded from PUBLISHED_RUN_FIELDS (:547). The carried value feeds clampWindow at :4253 and planReconcile's persistedWindow read (:2990 / reconcile.mjs:104).

TWO STALE ARTIFACTS FOUND, neither fixed here. frontier-train-e2e.test.mjs:477 carries a rationale comment claiming buildReconcileLiveSignals hardcodes events:[] so reviewDecision is discarded — FALSE at HEAD, the function populates events; the assertion itself still passes, only the causal claim is wrong (code wins per no-comments). And spec section 13 still lists BUILD_AHEAD_CAP as open decision (b) gating M4, which 0086 closed on 2026-07-28.

NEXT SESSION STARTS HERE. Read 0196, then this entry, then the M4 DISPATCH BRIEF entry, then 0187/0086/0151/0194. Resolve the two open scope calls in H and G first (they change the file set), then dispatch the four-agent serial shape the brief recommends: implement-and-commit, receipt-and-commit, adversarial verify fenced read-only, ship hard-gated on the verdict. 0194 governs ordering: never tell an agent not to commit.