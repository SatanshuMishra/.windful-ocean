M4 IS DISPATCH-READY AND WAS DELIBERATELY NOT DISPATCHED, on explicit user instruction to complete everything up to the dispatch and stop. Read 0196 (scope re-derived), 0197 (durable window state), 0198 (instrumentation), then the M4 GROUNDING MAP entry for anchors, then 0187/0086/0151/0194. The grounding map is the anchor authority; do NOT re-run that pass and do NOT trust the spec's :line cites.

STATE. Branch feat/m4-fixed-build-ahead-cap, cut off origin/main at 9ea75d6, checked out in the MAIN worktree. ZERO commits. No worktree created, so nothing new can leak. Working tree carries ONLY the five known pre-existing dirty paths. CI on 9ea75d6 is READ and green (test + security both success) - that risk is closed and must not be re-listed as open. K=8 is closed by 0086; spec section 13's open decision (b) is stale text, not a live gate.

WHAT M4 SHIPS, with scope now fully closed:

COMMIT 1 - THE DELETION, one interlocked commit. It cannot be split: the mirror guard forces each `whole` twin edit into both copies, and window.mjs's exports are imported by reconcile.mjs and leases.mjs, so any partial landing is red mid-branch and violates the green-branch invariant. Introduce BUILD_AHEAD_CAP = 8 with an optional engine-arg override. From window.mjs AND its mitosis.js twin delete WINDOW_FLOOR, WINDOW_CEILING, WINDOW_INCREMENT, nextWindow, windowDelta. Point leases.mjs:111's buildAheadWindow fallback at BUILD_AHEAD_CAP in BOTH copies (fallback moves 3 -> 8; intended per 0197). Strip the events/nextW computation from planReconcile in BOTH reconcile.mjs and its mitosis.js twin, DROPPING nextW from the return shape. mitosis.js-only, no twin: delete clampWindow (:2420-2422) and its call sites (:4253, :4339-4340); delete persistWindowCheckpoint (:5026-5033) and its single call site (:4342); delete the events loop inside buildReconcileLiveSignals (:3106-3121) but KEEP the reviewDecision capture at :3089 unless grep proves it has no other consumer, because grounding found it also serves provenance/contested logic; strip the `; AIMD window W=${advance.nextW}` clause from the log line at :3988, which discharges 0187. Per 0197 delete the resolveRunIdentity carry-over at :722 and its recovery.mjs twin. Tests in the SAME commit: window.test.mjs and run-log.test.mjs:6 fail at IMPORT, not assertion, so they must be deleted or rewritten here or the suite does not load; reconcile.test.mjs:201-205 goes and the deepEqual shapes at :208-209 and :227 are updated for the dropped nextW; recovery.test.mjs:639 goes, while :600 SURVIVES untouched; all nine 'AIMD window W=' assertions in frontier-train-e2e.test.mjs (:477 :502 :586 :732 :733 :854 :1106 :1311 :1335) are updated. While at :477, DELETE its stale rationale comment - it claims buildReconcileLiveSignals hardcodes events:[], which is false at HEAD; per no-comments, a comment contradicting the code being changed is deleted, never updated.

TWO PRECONDITIONS FROM 0197, discharged by MEASUREMENT before the matching deletion, never assumed. Prove by exhaustive grep that manifest.window has NO surviving reader once clampWindow and persistedWindow are gone; a surviving reader REFUTES 0197 and must be reported, not worked around. And prove by execution what applyRunDelta does with an UNKNOWN record kind before deleting its 'window' case (mitosis.js:815-829 / run-log.mjs:35-49) - .mitosis/ is gitignored and per-machine, so pre-M4 journals carrying 'window' records exist on this machine. Inert means delete the case; throws or mis-folds means the case STAYS as an explicit ignore.

COMMIT 2 - THE INSTRUMENTATION, purely additive, per 0198. Per-run locals at the reset loop (:3949-3967), logged once, emitted ONLY on the path where planReconcile actually ran - a fresh run never enters that block and must not print counters it never measured. rebuildUnitCount is available mitosis.js-only as advance.toParkSubtree.length; if the divergence count is not derivable from planReconcile's existing return, add ONE integer to that return shape and land it in BOTH twins in this commit. NO TEST: section 11 refuses one and asserting on a log string is a change detector. Do not let an agent add one.

DISPATCH SHAPE, unchanged from the recommendation M5 Stage 2 proved: a SMALL serial run of about four agents - implement-and-commit, receipt-and-commit, adversarial verify fenced read-only and re-running the decisive mutations itself, then ship - with the ship phase HARD-GATED on the verifier's verdict so nothing pushes if verification fails. 0194 governs ordering absolutely: NEVER instruct an agent not to commit; every lens runs after the commit producing what it measures. Standing constraints: distinct scratch subdirectory per agent; stage explicit paths only, NEVER git add -A; never patch a committed artifact to run a mutation, mutants live in scratch copies selected through MITOSIS_PATH; the engine is .claude/workflows/mitosis.js, not under .claude/lib/superpowers-parallel/; PR scope is frozen at creation and merge stays human-gated; pr-create requires --provenance with --origin machine and rejects angle brackets in --what.

WHAT M4 DOES NOT TOUCH. advance.toRestack and advance.toOpen have zero consumers besides the :3988 log line, measured this session - that dead weight is NOT M4's, which owns only the AIMD clause per 0187. The four production-dead merge-watch symbols stay per the spec's named residual. M5 is merged and closed; the declared M3 termination residual belongs to a later MSP.