2026-08-23. The user cut the scope to one goal: only work that moves the engine toward a completed live run. Recorded as 0679, which defers making the four unit-less dispatch kinds recordable and keeps the predecessor's required-status-check item out.

SHIPPED

Harness repository provisioned. Reading the provisioning function settled the cheap-versus-expensive question: it early-returns when the repository is already visible, so creation is reached only on a first run. Created mitosis-live-pr-harness by hand, private, README-initialized, description matching the source sentinel byte-for-byte at 147 characters, verified programmatically rather than by eye. The README is functional: it makes the default branch exist, which sends the harness down its already-seeded path so it never pushes workflow files, which is what removes Workflows write from the token requirement. Recorded as 0680.

Crash-point race fixed, outside the repository in the untracked m15 harness. The engine writes built and quiescent-exit 3 milliseconds apart while the harness polls on a one-second floor, then kills unconditionally, so the kill always landed about 1.5 seconds after the run was already over. The engine died during shutdown, never reached ship, and opened nothing. This is why the last billed run charged full price for zero pull requests. The fix stops racing: read both records afterwards, kill only when the crash point is genuinely still open, otherwise record that the proof was not exercisable and wait for the engine to finish. Proven by a new check whose scenario one was red with the assertion quoted, green after, and red again under an inertness mutation; a second scenario guards against the fix degenerating into never killing. Recorded as 0681, including the honest cost.

Teardown containment shipped as pull request 285. The bound sits at the single shared function all three call sites route through, including the dependency-injection port. Red before, green after, inertness mutation confirmed, 238 of 238 passing on the narrow suite.

Collection-refusal guard shipped as pull request 286. Recorded as 0682.

FAILED, and why it matters

The live lane was re-triggered as CI run 32656517961 and failed with a message byte-identical to the previous run: could not create the repository, resource not accessible by personal access token. The repository was re-confirmed present immediately afterwards. The cause is different from last time: the lookup returns the same empty result whether the repository is missing or merely invisible to the caller, so an unauthorized token falls into the creation branch and dies there. Recorded as 0683 precisely because the identical wording across two runs with two different causes is what would send a fresh session to re-create a repository that already exists.

NOTED

Two separate tests were found asserting the vulnerable behaviour as expected — one expecting an out-of-repository deletion to succeed, one expecting a dispatch on the refusal path. Neither was deleted or skipped; both were corrected in place, and the second is called out in its pull request's risk field so a reviewer does not misread it as a weakened test.

Nothing is left running. No billed run was triggered at any point.