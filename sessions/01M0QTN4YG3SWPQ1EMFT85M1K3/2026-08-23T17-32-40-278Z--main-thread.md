2026-08-22: thread opened, one real result, two dispatches killed. Work resumes in a FRESH session by explicit user instruction.

## The one result: the live GitHub lane ran for the first time

Triggered as run 32654935845 once MITOSIS_LIVE_GH_TOKEN was set. It got PAST the credential gate, which has never happened before, then failed at a single specific point:

    Error: live-github-harness: could not create SatanshuMishra/mitosis-live-pr-harness:
    GraphQL: Resource not accessible by personal access token (createRepository)

This is progress, not a regression. The lane has never previously executed at all.

## My token guidance was wrong, and this is the correction

I advised scoping the token to the EXISTING repository mitosis-full-e2e-substrate with Contents and Pull requests read and write. That is not what the harness does. It creates its OWN disposable repository named mitosis-live-pr-harness, so a token scoped to one pre-existing repository cannot serve it.

Two candidate remedies, and the cheap one is UNVERIFIED:

1. Pre-create mitosis-live-pr-harness by hand once and grant the token access to it. The harness test imports a function named ensureRepoExists, whose name implies it reuses an existing repository rather than always creating one. I did NOT read that function's body before the session ended. Verify the implementation before advising the user; the name is a hint, not evidence.
2. Grant the token repository-creation rights. Higher privilege, and it should not be chosen until remedy 1 is actually ruled out.

Whoever picks this up: read ensureRepoExists first. It decides which remedy is correct and the answer is one file read.

## Two dispatches killed mid-flight, with nothing salvageable

The user stopped both deliberately, to move this work into a fresh session rather than continue near a compaction boundary.

- The teardown-bounding implementer had not begun editing. Its last output concerned skill routing.
- The dispatch-kind investigator was mid-diagnosis. Its only visible output was a progress note claiming a mechanism confirmed for three kinds, with decompose, the replay stub and the decision records still outstanding. That is a progress line, NOT a finding. Nothing from it may be cited or carried forward; the diagnosis must be re-dispatched from scratch.

Liveness verified from git rather than assumed: no branch fix/mitosis-bound-teardown-removal locally or on the remote, no new worktree, no open pull request, and the user's eight uncommitted working-tree files unchanged.

## Order for the fresh session

c1 first, because it is one file read away from being decided and it costs nothing. Then c2's diagnosis, re-dispatched whole. Then c4, the teardown hazard. Then c3, the built-wait race. The billed run belongs to the parent's c45 and starts only after these.