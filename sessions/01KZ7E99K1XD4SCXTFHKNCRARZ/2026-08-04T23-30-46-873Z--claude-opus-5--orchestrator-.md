Confirmed the shipped relocation on main (27419a4), then cleared the four follow-ups with parallel subagents. Branched fix/gate-hardening-followups off main.

SHIPPED (committed):
- 86a57e8 c7. no-self-merge-consent.test.mjs keyed its deny-classifier exemption on entry.name alone, so any same-named file in lib/git inherited it. Agent PROVED the bug by injection: byte-identical violating content named self-merge.mjs failed 2 assertions; named gh-merge-shim.mjs it passed 11/11 silently. Fixed to the sibling LIB_TREES prefix+dir idiom. Verified 11/11.
- 6d02c05 c6. Original premise FALSE: ~/.claude/lib is a whole-dir symlink, all six relocated files share inodes with the repo, no deployed copies exist. Real gap found instead: ~/.claude/hooks and ~/.claude/rules are the only two Stow-UNFOLDED trees, so a new TOP-LEVEL file there has no live symlink until scripts/install_config.sh reruns. Live instance: .claude/hooks/session-config-drift-check.sh (inert today, gitignored by *session*, unreferenced). Documented in docs/TROUBLESHOOTING.md. I corrected the writer's stated CAUSE myself: the install script pre-mkdirs EVERY subdir, so that is not what singles these two out; the real cause is live-only content (graphify-out caches in both, rules/context7.md) that Stow cannot fold around.

c4 + c5 GREW FAR BEYOND THEIR FILING and were merged into one fix wave (they share one file and one root cause).
Security review returned VERDICT: BLOCK with a CRITICAL confirmed by execution: normalizer at :15 collapses newlines BEFORE the :44 chained guard reads them, so wrapper+NEWLINE+denied-command was silently ALLOWED. Laundered every hook-gated PR sink including `gh api -f body=@<path>`, a file-exfiltration primitive defeating both Read(~/.ssh/**) and Bash(curl:*). CRITICAL not High because settings.json permissions.deny names no gh api and no gh pr edit rule, so the hook is the SOLE gate there. Nine further classes confirmed: token splitting ("gh", g\h, $X) defeats every gh rule; total fail-open if python3 absent; gate self-disable via node -e and ln -sfn, both PRE-APPROVED in permissions.allow.
Second agent independently found the same file's boundary regex ([[:space:]]|$) causes BOTH the filed false-positive AND a false negative: `gh pr create;echo pwned` was ALLOWED.
12 invariants derived. Only 4 are regex-reachable; the two carrying the CRITICAL (INV-3 evidence preservation, INV-4 per-command exemption scoping) are not. This is why five prior patch rounds each closed a finding and opened a new one.

USER DECISIONS THIS SESSION: full structural rewrite approved over minimal patching; narrowing permissions.allow approved.

IN FLIGHT AT HAND-OFF: rewrite implementer still running. It has ALREADY swapped the live hook and added .claude/hooks/lib/shell_parse.py and .claude/hooks/lib/bash_gate.py (untracked). I smoke-tested the live gate before writing this and it is in a GOOD state, not a broken one: raw create denies, benign allows, the CRITICAL newline payload now DENIES, "gh" pr merge 12 now DENIES, prose now ALLOWS, `create;echo pwned` now DENIES, and the hook suite is 189/189 (was 85). Nothing is committed and settings.json narrowing is unconfirmed. Agent transcript: /private/tmp/claude-501/-Users-satanshumishra-Documents-DevLabs--windful-ocean/88d0e893-9f2a-4926-9a3e-fd53b96e2e63/tasks/a921ae3777c61ba09.output (do not cat it whole).

Baseline before any work: npm test 1776 pass / 1 fail, the known protect-claude-config.test.mjs:194 failure, which reproduces on pristine main and is caused by ~/.claude/settings.json being a real file rather than a symlink out of the home tree. Not mine, not fixed, belongs to permission-config-audit.