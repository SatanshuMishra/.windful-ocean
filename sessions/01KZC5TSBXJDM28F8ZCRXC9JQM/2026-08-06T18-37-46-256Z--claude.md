GOAL set by user: revisit the 2026-07-30 mitosis core rebuild SPEC (~/Downloads/2026-07-30-mitosis-core-rebuild.md), modify SCOPE, author a BRAND NEW SPEC with ZERO dependency on it. Brainstorming explicitly deferred to a FRESH session via superpowers:brainstorming. User instruction this session: wait for specific instructions.

MEASURED BASELINE (static census, 2026-08-06). No run telemetry exists: no .mitosis journals, no token/tool-call accounting anywhere in the engine (the `tokens` hits at mitosis.js:988-1003 are a glob tokenizer). Method: reused scanJsStructure() from .claude/lib/superpowers-parallel/mitosis-gate.mjs to enumerate dispatch sites rather than regex-guessing.

- 39 dispatch sites: 31 agent() in .claude/workflows/mitosis.js + 8 guard.dispatch() in .claude/lib/superpowers-parallel/run-engine.mjs. runEngine is duplicated inline in mitosis.js (the twinning tax).
- Pre-code path, 6-MSP run: 3 run-level explorations (Reconcile, Decompose, Prepare) + ~3-4 per MSP (Plan, Plan review, Parallelize, Branch) = ~24 dispatches that independently open the repo before the first line of code.
- Carried prompt bytes by phase (sites): Ship 26,354 (13); Reconcile 14,826 (3); Branch 4,183 (2); Parallelize 3,704 (1); Decompose 3,365 (1); Prepare 2,558 (1); Plan 1,648 (2); Plan review 346 (2).
- Decompose and Parallelize BOTH derive dependency facts via Serena/LSP - overlapping work.
- The plan-review prompt is 171 chars: too small to carry its subject, so the reviewer must fetch everything.
- reviewLoop at run-engine.mjs:395-411 is a while(true); every iteration re-runs `git diff <launchCommit> -- <fileScope>` from scratch. Confirms the old spec's section 10.4 claim, at a different line than it cites.
- COUNTER-FINDING: task.fullText is passed INLINE as payload to implementer and fix (run-engine.mjs:339, :386), so implementers do NOT re-read the plan; an explicit anti-re-derivation instruction already exists at :329. The pathology is concentrated in the PLANNING STACK, not universal.

DRIFT vs the 2026-07-30 document: mitosis.js is 5,515 lines (doc cites 4,851); 'Final review' deleted and 'Resume' declared; phase-parity ships in mitosis-gate.mjs (MSP-0 appears LANDED); mitosis-git.mjs moved to .claude/lib/git/pr.mjs with MITOSIS_GIT_VERBS still at :30 and PR_TITLE_PATTERN at :152. Every line citation in the old document needs re-derivation before reuse.

ASSESSMENT: the old spec's phase collapse is genuinely effective on the pre-code path (~24 exploring dispatches -> 2) and is deletion, not relocation. Three masking risks remain, ALL invisible to its own MSP-1 instrument, which counts dispatches and wall-clock only: (a) Decompose absorbing cut + derive-edges is the only load-bearing allocation in the document with NO falsifier; (b) Ship budgeted at 1 dispatch vs 13 conditional sites observed, with no happy-path subset established anywhere; (c) 3 design reviews per MSP each re-derive the diff - only the loop re-review is addressed by 10.4.

Census script (regenerable, session scratchpad, will not survive): dispatch-census.mjs - enumerates agent() sites via scanJsStructure, extracts label/phase/agentType/model, measures carried prompt bytes, flags exploration signals.