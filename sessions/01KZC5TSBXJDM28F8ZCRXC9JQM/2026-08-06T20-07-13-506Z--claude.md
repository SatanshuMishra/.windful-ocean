CORRECTION to the 2026-08-06T20-05 event of this session, on the ledger infrastructure anomaly only. Everything that event records about the SYMLINK PROBE and the THREE APPROACHES stands unchanged.

That event claimed the ledger MCP tools operate on the logbook-logbook data root while the Stop hook reads logbook-inline. That is FALSE, and it was falsified by the very write that recorded it: append_session_event returned a path under logbook-inline. The MCP server and the Stop hook read the SAME root, ~/.claude/plugins/data/logbook-inline/. The logbook-logbook root (21 thread files, no 01KZCAGBAH55F8AR1ZXJQT8JRP) is a stale second copy from a second install and is not what the tools use. Do not act on the split-root theory.

WHAT IS ACTUALLY TRUE. The Stop hook reported thread 01KZCAGBAH55F8AR1ZXJQT8JRP (invariant-coverage-tax, a CHILD of 01KZ98CT0FT1SYQRH4D7TXH0Z5) as "still active" while that thread's own JSON records "status": "paused". So the thread state and the active-thread pointer disagree: a paused thread left the pointer set. The count reconciles with children being excluded from the roster - rebuild_index reported 21 threads and children 1 against 22 thread files on disk.

CONSEQUENCE FOR THE NEXT SESSION. This is a logbook pointer-hygiene bug, NOT a SPEC A finding, and it was not fixed here beyond attempting to clear the pointer. If the Stop hook keeps demanding a debrief for a thread that is already paused, that is this bug, not a real unfinished thread. Deciding whether to prune the stale logbook-logbook root needs the user.

METHOD NOTE, worth keeping: update_thread replaces every scope its risks array MENTIONS. Submitting four new c3 risks silently retired the six c3 risks already on the spine, including the 340k-token do-not-re-run warning. They were restored in a second call. When adding a risk to a scope, resubmit that scope in full.
