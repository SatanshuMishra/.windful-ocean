SPEC A WRITTEN, COMMITTED AND APPROVED BY THE USER. c3 and c4 both close this session. Decision recorded: 0271 (authoring order). Nothing failed; nothing left running.

ARTIFACT: docs/superpowers/specs/2026-08-06-config-staging-live-promotion-SPEC.md, 234 lines, commit 606c761 on branch chore/config-drift. Sections: 0 verified ground truth, 1 what it changes, 2 layout, 3 the promote verb, 4 enforcement, 5 safety, 6 settings.json, 7 migration, 8 out of scope, 9 residuals, 10 acceptance criteria. Written standalone per 0268 - no inherited decision numbers, no step-N chain, no citation of the 2026-07-30 document as authority. The brainstorming spec self-review was run and fixed three things inline: a miscount (65 -> 64), an unspecified GC depth (now the 5 most recent releases), and a vague bootstrap location (now ~/.claude/local/).

=== CORRECTIONS TO THE 2026-08-06T19-31 CONFIG INVENTORY, all re-measured today ===
Re-derivation was mandatory under 0268 and it falsified three items in the earlier inventory. The SPEC carries the corrected figures; the earlier event is append-only and stays wrong, so trust these.
1. LIVE HOOKS. 26 symlinks plus exactly ONE live-only entry (the generated graphify-out/), NOT "4 live-only files". Drift also runs the other way: .claude/hooks/session-config-drift-check.sh and its test exist in the checkout and are NOT live.
2. LINK CENSUS. 37 symlinks into the checkout: 9 at depth 1 (CLAUDE.md agents docs keybindings.json lib notes skills sounds workflows), 26 under hooks/, 2 under rules/ (common, typescript). ~/.claude/hooks and ~/.claude/rules are REAL directories.
3. settings.json. The `model` key is now REPO-ONLY, not live-only as the earlier event recorded; the repo copy moved under 652c9af on this branch. Live-only key is pluginConfigs. Live grants Bash(ln -sfn:*) at ~/.claude/settings.json:20; the repo copy grants no ln form at all.

=== THE FINDING THAT RESHAPED THE MIGRATION SECTION ===
COVERAGE GAP. A release is built by `git archive`, which carries ONLY TRACKED content, and the tracked set does not cover the live set. Measured today: 64 live files under .claude/ are untracked once generated artifacts are excluded - 56 under .claude/docs/superpowers/ (35 plans, 21 specs), 1 under .claude/docs/analysis/, 5 under .claude/notes/, and 2 hooks files. .claude/notes/ is 0 TRACKED against 5 on disk, so a release built today delivers an EMPTY notes/ and drops 61 of 72 files under docs/. Cutover performed today would silently destroy live config. This did NOT overturn 0269 - the mechanism is unchanged - it became a hard precondition in section 7, a coverage check in pre-swap validation section 5.1, and acceptance criterion 10 which verifies by diffing live against the release rather than asserting.
THE IGNORE RULE BEHIND IT. .gitignore:19 is a bare unanchored `*session*`, so it matches any path containing that substring - which is why .claude/hooks/session-config-drift-check.sh is invisible to git. Until it is narrowed, ADOPTING that file APPEARS TO SUCCEED and the file still never enters a release. Narrowing it is precondition 2 in section 7, ordered BEFORE adoption.

=== TWO CONSEQUENCES THE APPROVED DESIGN HAD NOT REACHED, both now in the SPEC ===
1. GENERATED OUTPUT INSIDE PROMOTED SUBTREES. ~/.claude/hooks/graphify-out/ and ~/.claude/rules/graphify-out/ exist because those directories are writable today. Collapsing hooks and rules to whole-directory links makes them read-only release content and the graphify hooks would fail on write. Relocation is a cutover precondition (section 2.1).
2. protect-claude-config.sh DEGRADES. That PreToolUse(Edit|Write) guard finds the checkout by realpath'ing ~/.claude/CLAUDE.md (:64-68); after cutover that resolves into ~/.claude/releases/<sha>/, which is not a git worktree, so `git worktree list` fails, `derived` stays false, and it falls through to its textual .claude/<prefix> heuristic (:99-104). The paths that matter still prompt, so this is a WEAKENING not a hole - it can no longer confirm a .claude path belongs to a real checkout. Fix named in section 9: read repo_root from the LIVE receipt, which is why the receipt schema in section 2 carries repo_root.

=== POSITIVE PROPERTY WORTH KEEPING ===
All 26 registered hook commands address hooks as $HOME/.claude/hooks/<name>; none hard-codes a checkout path. So a single entry link hooks -> current/hooks preserves every registered path and the 26 per-file links collapse to one. NO hook registration in settings.json changes as part of SPEC A.

=== WHAT SPEC A DELIBERATELY DOES NOT CLAIM ===
Section 9 writes the unmeasured swap-visibility question as OPEN, not designed around: whether a pointer swap reaches an already-running session is still unmeasured because headless claude cannot authenticate under a scratch config dir. The SPEC states it changes the promotion-TIMING contract in section 4, not the choice of mechanism. Cross-file mixing is carried as accepted and unmitigated.