ADDENDUM to the 2026-08-06T19-31 session event: the parallel-plan-execution dead-code finding, recorded in full because it was otherwise held only on an ephemeral task chip that does not survive dismissal. Decision 0267 states the finding; this event carries the citations and the disposal instruction.

THE DEFECT. ~/.claude/workflows/parallel-plan-execution.js cannot execute. Verified by direct grep 2026-08-06, exact lines:
  parallel-plan-execution.js:27  const { homedir } = await import('node:os');
  parallel-plan-execution.js:28  const { runEngine } = await import(`file://${homedir()}/.claude/lib/superpowers-parallel/run-engine.mjs`);
Both sit at the top level of the workflow body, and the Workflow runtime answers dynamic import() with a hard error (see 0267 and the probe output in the prior event). The script therefore throws on invocation before reaching its return.

WHY IT IS UNREACHABLE ANYWAY. mitosis.js has zero workflow() call sites, so nothing routes to it. mitosis.js instead uses its own verbatim inline copy of run-engine.mjs: the inlined definition begins at mitosis.js:1272 (`async function runEngine(engineArgs, ctx) {`), running to roughly :1561 per the codebase-analyst's mapping, and is invoked in-process at mitosis.js:5040 (`const engineResult = await runEngine(`). CITATION CORRECTION: the analyst reported that call site as :5039-5042; direct grep places `await runEngine(` at :5040. The :1272 definition line is confirmed by grep; the :1561 range end is the analyst's figure and is NOT independently confirmed.

WHY IT IS WORTH CLEARING RATHER THAN LEAVING. `parallel-plan-execution` is still advertised as an available skill, so a user or an agent can invoke it today and get an immediate hard failure with no hint that the entry point is structurally broken.

DISPOSAL INSTRUCTION. .claude/hooks/block-inline-engine.mjs exists for the sole purpose of blocking Workflow calls whose scriptPath matches this filename (match at :13, block at :16-18, with a reason string directing the caller to run /mitosis instead). If the script is removed, that hook becomes dead too and must be removed in the same change, along with its PreToolUse Workflow registration in settings.json.

FENCE. Do NOT touch mitosis.js or the inline-twin pattern while clearing this. That surface is governed by 0267 and belongs to SPEC B.