SPEC B WRITTEN, SELF-REVIEWED, COMMITTED AND APPROVED BY THE USER. c5 and c6 both close, so the thread completes at 6 of 6. Nothing failed; nothing left running. Decision recorded: 0272 (SPEC A implementation strategy for the next session).

ARTIFACT: docs/superpowers/specs/2026-08-06-mitosis-core-cost-decomposition-stacking-SPEC.md, 340 lines, commit f86c92a on branch chore/config-drift. Sections: 0 verified ground truth, 1 what it changes and in what order, 2 Part I the instrument, 3 Part II cost as falsifiable hypotheses, 4 Part III decomposition by codegen, 5 Part IV stacked PRs, 6 sequencing, 7 out of scope, 8 residuals, 9 fourteen acceptance criteria. Standalone per 0268 - no inherited decision numbers, no step-N chain, no citation of the 2026-07-30 document as authority. Written to the 0271 content constraint: Part I is the instrument, nothing may be tuned before it lands, and every cost target is a hypothesis with claim/mechanism/measurement/falsifier/disposition under one universal falsifier (total input plus output tokens must fall, whatever happened to dispatch count).

=== FIVE CITATIONS FALSIFIED BY RE-DERIVATION, all recorded in SPEC B section 0.6 ===
Re-derivation was mandatory under 0268 and it falsified five. The SPEC carries the corrected figures; the earlier events are append-only and stay wrong, so trust these.
1. BUILD_AHEAD_CAP is IMPORTED at leases.mjs:2 from window.mjs; canonical declaration is window.mjs:1. NOT leases.mjs:110-112. Inline twin at mitosis.js:2582.
2. There are FOUR explicit `model: 'opus'` pins, not two, and none at the cited :5176/:5329. They are mitosis.js:1374, :5000, :5027, :5187. Separately mitosis.js:1156-1176 are model-policy functions that RETURN 'opus' as a conservative default - a different mechanism from a pin.
3. THE SCHEMA FREE WIN DOES NOT EXIST. The claimed ~445 contiguous lines at 1562-2007 is refuted: 26 top-level *_SCHEMA declarations scattered from mitosis.js:977 to :5425. The "near-risk-free, 8% of the file" framing is WITHDRAWN in section 4.3; it is a normal seam of ordinary risk.
4. PR_TITLE_PATTERN is imported at pr.mjs:11, re-exported :23, APPLIED :152 - not declared at :152.
5. reviewLoop spans run-engine.mjs:397-409: while(true) :397, review dispatch :400, fix dispatch :409.
6. SHIP'S DISPATCH COUNT IS UNSETTLED, not merely uncited. Three figures disagree because they count different things: 11, 13, and the 15 occurrences of `phase: 'Ship'` measured today (a superset - remediation descriptors carry a phase too). SPEC B adopts NONE of them and makes measuring it a precondition of H3.

=== THREE FINDINGS THAT CHANGED THE DESIGN, not just the citations ===
1. THE RUNTIME ALREADY EXPOSES TOKEN ACCOUNTING THE ENGINE NEVER TOUCHES. All 20 `budget` hits in mitosis.js are the engine's OWN remediation counter (mitosis.js:1381-1384, :2117-2216, REMEDIATION_BUDGET :2109), not the runtime global. The live Workflow tool contract documents budget.spent() returning output tokens. BUT the 2026-08-06 probe never tested it, and workflow-sandbox.mjs:36 HOOK_NAMES omits it. Section 2.2 carries it as the leading OPEN question with both branches designed (Source A in-sandbox budget, output-only and unattributable under parallel(); Source B out-of-band transcript records read by a hook in real Node). Confirming it by zero-agent probe is the first act of the instrument MSP. Do NOT read it as settled.
2. gh pr edit --base PASSES THE BASH GATE TODAY. block-destructive-bash.sh:134 matches `pr edit` only alongside --title/--body/--body-file. With pr.mjs:30 carrying only pr-create/pr-close/compare, base retargeting would route a PR mutation around PR centralization by ACCIDENT. Section 5.5 makes it a pr-retarget verb plus a gate deny in the same change.
3. SPEC B REJECTS ONE OPTIMIZATION IN ADVANCE (section 3.3). Caching reviewLoop's `git diff <launchCommit>` across iterations looks free and is not: the fix dispatch at run-engine.mjs:409 runs BETWEEN iterations and mutates the tree, so a cached diff is stale by construction. The recomputation is NECESSARY. If loop cost is a target, the target is iteration count. Recorded so a future session does not rediscover this as an opportunity.

=== TWO DEFECTS THE SELF-REVIEW CAUGHT IN THE DRAFT ===
1. Acceptance criterion 9 originally banned `import` and `require` textually, directly contradicting criterion 7's byte-identity proof. Verified: those bare words appear 11 times in mitosis.js today - :144-154 as config key names, :1313 :1322 :3464 :3528 :4900-4906 inside prompt strings including instructions telling a subagent to write its own import statements - and all of it passes the validator. Corrected in section 4.4 and criterion 9 to ban only module-level import/export statements and the import() call form. Date and Math.random verified at ZERO occurrences today, so the textual ban on those two is compatible with byte-identity.
2. The Ship dispatch-count conflict above was silently picking 13; now stated as unsettled in both 0.6 and H3.

=== WHAT SPEC B DELIBERATELY DOES NOT CLAIM ===
The instrument's own source is unsettled and is the single largest open question (section 8). A baseline of one run is a sample of one; variance must be recorded. Fusion hypotheses can be right about cost and wrong about quality - the instrument counts tokens, not whether a fused Decompose produces worse cuts. runUnit may not be safely decomposable at all; if byte-identity cannot be held across a split, leave the 700-line function whole. H5 CAN KILL PART IV's justification: if the baseline shows engine-active time dominates rather than waiting-on-human, stacked PRs lose the latency argument and must stand on the deletion of the compensation machinery alone, with the latency claim explicitly withdrawn rather than quietly kept.

=== USER INSTRUCTION FOR THE NEXT SESSION, verbatim intent ===
Debrief now; in a FRESH session dispatch a dedicated SMALL dynamic workflow to FULLY implement and ship SPEC A; where changes block each other use stacked PRs (https://docs.github.com/en/pull-requests/get-started/about-stacked-prs). Captured as decision 0272 and as the successor thread's plan.