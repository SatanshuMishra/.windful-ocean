Audited git tracking across the whole repository and separated AI workflow output from real configuration. Of 774 tracked files on main, 92 were assistant-generated documents: 77 under the in-repo Claude docs tree (design specs, implementation plans, two task graphs, two run ledgers, six rendered HTML reports) and 15 under the superpowers docs tree. Everything else — the mitosis engine and its tests, hooks, rules, agents, skills, editor and shell config, install scripts, CI — is genuine config and was kept.

Shipped, two commits on chore/untrack-ai-workflow-docs, both pushed:
- ac100df3 untracked all 92 documents with git rm --cached, leaving every file on disk. Added /.claude/docs/ to the root ignore file, replaced the two narrow superpowers-plans ignore lines with the whole tree, removed the now-pointless un-ignore of the session-continuity-redesign directory, and dropped the one live rule sentence that pointed into the untracked tree. Tracked file count 774 to 682.
- 68aacf5d repaired the consumer that untracking broke.

The failure and its repair. The audit's first pass wrongly reported that no executable read those documents. The grep that produced that claim was truncated at twenty lines and the hit fell past the cut; the consumer also builds its path from two separate string literals, so a literal path search would not have found it regardless. A design document turned out to be the sole surviving carrier of the nine retired agent names — the nine agent definition files were deleted earlier, so tracked config held only the surviving thirteen. Three tracked consumers parsed it: two tests and the retirement-census gate. A pristine clone with HOME overridden reproduced exactly two test failures and gate exit 43.

The repair moved the nine names into a tracked JSON data file and re-grounded the retained roster on the already-tracked agent-spec store rather than declaring it a second time, which also turned a degenerate cross-check into one that actually fires. The markdown parser that had read the design document is deleted. Pristine clone after: npm test exit 0, 3084 tests, 0 failures; gate exit 0. Inertness mutation (empty retired list) turned both red, exit 1 and exit 42, so the new file is load-bearing rather than decorative.

Also cleaned two stray screenshots of a rendered report from the repository root, moved to Trash rather than deleted outright.

Not done, deliberately: history was not rewritten, so the blobs remain in past commits. PR 252 is open and unmerged; merge is human-gated. A follow-up unit is filed but not started — enumerate tracked programs no CI job invokes (a static path scan cannot close this; only a fresh checkout can), and repair two tracked prose files that cite now-absent paths. One unrelated working-tree change, a modified sound file, predates this work and was left untouched throughout.