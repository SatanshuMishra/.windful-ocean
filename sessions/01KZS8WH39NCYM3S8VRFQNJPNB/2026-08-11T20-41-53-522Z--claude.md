Thread opened out of the rename thread's final gating question. Read-only code trace, no mutation.

Two independent rollback verbs share the name. promote.mjs rollback (promote.mjs:337) reads ~/.claude/LIVE via readReceipt (promote.mjs:338) and is what 0323 governs; CUTOVER appears zero times in promote.mjs or converge.mjs. cutover.mjs rollback calls rollbackCutover (cutover.mjs:954-1022), whose only inputs are the journal (readJournal, cutover.mjs:955) and its aside container ~/.claude/.cutover/<journal-sha>/. readJournal has exactly two call sites repo-wide: cutover.mjs:712 (forward path) and cutover.mjs:955 (rollback).

Observed live state: CUTOVER names sha 75b90a3, applied_at 2026-08-10T21:55, 11 entries all recorded "performed". LIVE names 1ecacc7 with previous b49de71. ~/.claude/current resolves to releases/1ecacc7 and every promoted entry is a symlink to current/<name>. The aside container .cutover/75b90a3/ is intact: 9 symlinks pointing directly at the repo checkout, plus hooks and rules as real directories.

Traced consequence of running cutover.mjs rollback against that state: corroborationVerdict (cutover.mjs:333-369) checks only that each aside exists with the predicted node kind, so all 11 corroborate; restorePreserved (cutover.mjs:814-821) unlinks each live entry and renames the aside back. Nine entries become symlinks straight into the repo checkout, bypassing the release system; hooks and rules become directories frozen on 2026-08-10. Neither 1ecacc7 nor 75b90a3. Nothing compares the journal sha to liveSha, so no refusal fires; CUTOVER is then deleted (cutover.mjs:1004) and the aside containers reclaimed.

Apply early return confirmed at cutover.mjs:710 (plan.actions.length === 0 returns 'unchanged'), which makes everything from cutover.mjs:712 through :765 unreachable once all entries are already-linked. converge.mjs imports only from promote.mjs (converge.mjs:9), so the promotion flow cannot keep the journal current.

Reachability: cutover.mjs is absent from BOOTSTRAP_ENTRIES (paths.mjs:31, only promote.mjs and converge.mjs) and from ~/.claude/local/, so no SessionStart or Stop hook can invoke it. No npm script and no CI job references it. Both verbs are direct CLI only.

Undetermined and deliberately not guessed: whether apply was re-run after 2026-08-10 and hit the early return, or was simply never invoked again - both fit the evidence. Also undetermined, and now c6: whether the bash gate catches the command string "node scripts/config/cutover.mjs rollback", since its guardname regex matches CUTOVER and .cutover as path segments and that string carries neither.