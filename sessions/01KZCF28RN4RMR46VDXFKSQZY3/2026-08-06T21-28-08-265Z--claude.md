PLANNING ONLY - NO IMPLEMENTATION. The user clarified that workflow is used where possible AND appropriate with the cutover serial, then instructed: update logbook decisions, then stop. Decision recorded: 0275 (the workflow fan-out boundary). No code was written, no workflow was dispatched, nothing is running.

=== A CLAIM FROM THE PRIOR HAND-OFF IS FALSIFIED ===
The closing chat summary of the predecessor session and 0274's outcome both state that SPEC A section 7's five preconditions are "genuinely independent" and "parallelize cleanly". Measured 2026-08-06, that is WRONG in two ways, and 0275 supersedes the claim (not 0274 itself, whose core the user confirmed).

1. FILE CONTENTION. The ignore rules live in TWO files, and preconditions 1 and 2 collide on one of them.
   - Root .gitignore: /.claude/docs/analysis/ at :12, *claude-session* at :15, *claude_session* at :16, the unanchored *session* at :19.
   - .claude/.gitignore (separate file): notes/, docs/superpowers/, plus graphify-out/ and **/graphify-out/.
   Closing the coverage gap edits BOTH files; narrowing *session* edits the root file. Concurrent units conflict on root .gitignore. They are ONE unit, not two, with the narrowing applied BEFORE adopting any path the pattern hides.
   Subtlety: :15-16 already carry the narrower *claude-session* and *claude_session*, so narrowing :19 must not un-ignore what those two do not cover.

2. LIVE-LINKED WRITES INSIDE THE "SAFE" SET. Precondition 3 (relocate generated output) edits .claude/hooks/graphify-provision.sh, .claude/hooks/graphify-common.sh and .claude/hooks/graphify-refresh.sh - all reached through the 26 per-file hook symlinks. A half-saved edit there breaks every running session instantly. That is the cutover's hot-swap hazard at smaller scale, sitting inside the precondition set previously described as safe to fan out.

=== THE CORRECTED BOUNDARY (0275) ===
FAN OUT (no live-linked write, no shared file): bootstrap placement under ~/.claude/local/ (verified ABSENT today, so it is a greenfield write); adopt rules/context7.md (~/.claude/rules is a REAL directory holding only the common and typescript symlinks, so the repo write reaches no running session); author the promote verb with validation, rollback and the LIVE receipt; author the settings.json ownership manifest and the capture verb.
SERIALIZE (live-linked write): the graphify output relocation; registering the SessionStart and Stop converge hooks in live settings.json.
SERIALIZE AND ALONE, LAST: the cutover.
The gitignore/coverage unit is serial within the fan-out band by contention, not by hazard.

Efficiency is bounded and should not be oversold: the fan-out band is roughly four units of authoring work. The workflow buys concurrency on AUTHORING; the hazardous writes were always going to be serial.

=== STILL TRUE, CARRIED FROM 0274 ===
Stacking is manual via the convention - pr.mjs pr-create --base <parent-branch> - because the engine has none (merge-policy.mjs:15-17 returns HUMAN_GATED unconditionally, every PR opens on baseBranch at mitosis.js:5338, pr.mjs:30 carries only pr-create/pr-close/compare). deleteBranchOnMerge is false so auto-retarget never fires and each child needs explicit retargeting once its parent merges.