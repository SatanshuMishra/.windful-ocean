SHIPPED NOTHING TO LIVE. Verified three times during the session that ~/.claude is untouched: no releases/, no current, no LIVE, no CUTOVER; hooks/ and rules/ still real dirs at 27 and 4 entries; notes still points into the checkout; settings.json still a real file with converge still unregistered; local/ still the 6 bootstrap files. Primary checkout still shows only the two files belonging to the invariant-coverage-tax thread. Nothing committed, nothing pushed.

BUILT, in the worktree .claude/worktrees/spec-a-cutover on branch feat/config-entry-link-cutover off origin/main a302e4c: scripts/config/cutover.mjs (plan/apply/rollback with a crash-safe journal), settings reconciliation wired into promote with WITHDRAWN_GRANTS, protect-claude-config.sh reading repo_root from the LIVE receipt with probes narrowed to CLAUDE.md and keybindings.json, and docs/invariants/coverage/feat-config-entry-link-cutover.json at 17 rows. Full npm test in the worktree: 2107 pass, 0 fail, 6 suites.

TWO REVIEW ROUNDS BOTH RETURNED BLOCK. Round 1 found 2 CRITICAL plus 4 HIGH: the pointer and settings.json were treated as independent, so rollback swapped the pointer without reverting settings and a settings-write failure after a swap returned promoted with a receipt recording unqualified success; and the cutover journal described a plan rather than performed work, so an aborted apply deadlocked rollback forever and a second apply overwrote the first apply's aside record. Round 2 was planned against ten named invariants and still returned BLOCK with 3 HIGH.

THE ROUND 2 LESSON, which matters more than the findings. Round 2's INV-4 said prove containment at the write site, and the implementer did exactly that at all six sites, but isInside at paths.mjs:50-56 compares lexically with no realpath, so every one of those six proofs is defeated by a single symlink. Verified by the reviewer: with ~/.claude/local made a symlink to an outside directory, applyCutover returns applied and writes notes outside the config root. An invariant must constrain the property, not the placement of a check. A check that exists but cannot fail is worth nothing. Round 3 was restated at the property level and dispatched.

ROUND 3 WAS STILL RUNNING AT HAND-OFF. Its instructions are at ~/.claude/projects/-Users-satanshumishra-Documents-DevLabs--windful-ocean/artifacts/2026-08-08-cutover-round3-invariants.md and it was told to write its report to 2026-08-08-cutover-round3-report.md beside it. If that report file is absent, the round did not finish and must be re-dispatched from the invariants file. It covers: INV-4' sound containment through realpath on both sides, INV-2' rollback consumes the journal only when every entry is verifiably restored, INV-11 every restore path gates on ownership (restoreLink computes the ours flag and never uses it, so rollback replaces any symlink at the path), INV-12 no live setting is removed without being reported (withdrawGrants strips silently).

TWO PRE-EXISTING BUGS FOUND INCIDENTALLY, both worth keeping. First, a real silent bypass in the live guard: when a probe realpath resolves into a release inside a version-controlled HOME, the derived worktree set collapses and a write to the real checkout is allowed through rather than merely falling back to the textual match. Fixture is red against the unmodified script. Second, a placeLink path that would rename a symlink onto real content and silently destroy it. Also resolved a test that was already red on main, the installed-probe assertion, which failed precisely because live settings.json is a real file, which is what 0287 ruled it should be.

DOCKET IS HONEST AT 7 OF 17 ROWS THREATENED. G4 is the one to act on: the bash gate blocks mv ~/.claude/hooks but returns empty for node scripts/config/cutover.mjs --apply --config-root $HOME/.claude, because guardverb enumerates shell verbs only. The cutover verb and promote's new settings write both relocate or rewrite guardrail files past the gate. Reversibility and plan-by-default are credited but are a different property than blocked. Four constants came back inert, and re-adding settings.json to the guard probes tuple is also inert, so nothing in the suite would catch that narrowing being undone.

REJECTED TWICE, DO NOT RAISE A THIRD TIME: both reviews asked to collapse WITHDRAWN_GRANTS into NOT_ADOPTED_GRANTS because they are byte-identical today. 0288 already rejected that; divergence is the intended behavior, and the second review stated the intent as the defect.