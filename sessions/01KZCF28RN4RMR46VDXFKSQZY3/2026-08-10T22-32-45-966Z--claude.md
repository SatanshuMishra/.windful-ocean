Hand-off state for the c6 verification session.

Nothing is left running. Both test-engineer agents completed and returned in-band; no background shells, no worktrees created, no branch switched. The primary checkout is still on feat/invariant-inert-registry with a clean working tree, and the cutover still holds: current resolves to releases/75b90a3d.

Nothing was committed or pushed this session. The verification wrote no repo content at all — it read main through git show and git archive, and every behavioural probe ran under a sandbox config root. Live ~/.claude was read-only throughout.

Artifacts, all under the session scratchpad at .../4dbf2bb1-5701-49a4-b2e3-a413e93edaaa/scratchpad/c6/: tooling/ holds the git archive extract of scripts/config at 75b90a3d that both agents ran against; a1/scripts/ holds the criteria 1-5 probes (c1_behavioral, c2_idempotence, c4_rollback_no_rebuild, c5a_bootstrap_inside_release, c5b_live_positive_readonly); a2/ holds the criteria 6-10 sandboxes (c6-, c7-, c8-, c9-sandbox, c10, repo-mirror). Scratch is session-scoped and will not survive; anything worth keeping must be re-derived, and all of it is re-derivable from the extract plus the probe scripts.

Two process notes worth carrying. First, the harness refused both agents' attempts to write a REPORT.md, so each returned its findings as text instead of the requested file; the dispatch prompt asked for a written report and did not get one. Second, the initial suite failure was an extraction artifact rather than a defect: converge.test.mjs reads .claude/settings.json three levels above tests/, which a scripts/config-only extract does not carry. Supplying that one file read-only turned the suite green at 188/188. A future extract for this suite needs .claude/settings.json alongside it.

Left undone: c7, the PR-discipline criterion, is the only open criterion and was not started. It needs the #43-#61 PR range enumerated and each PR checked for having been opened by pr.mjs pr-create with --base pointed at the parent branch where one change blocked another. It is not a formality: c8 already established that five of the seven unit PRs merged into stack parents rather than main and needed re-integrating bottom-up.