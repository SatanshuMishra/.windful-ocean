c5 shipped: the entry-link cutover ran on the live root and every check passed.

Preparation. Read cutover.mjs at main (75b90a3d) rather than in the working tree, which sits on feat/invariant-inert-registry and carries no scripts/config at all. Established the live pre-state: 8 entries symlinked into the primary checkout, hooks and rules as real directories, notes symlinked to the checkout, current -> releases/75b90a3d with a matching LIVE receipt.

Provenance check, unplanned and load-bearing. The briefing warned the swap would change live content, so every differing file was traced to a side. Live hooks/ was byte-identical to this branch, and this branch has made ZERO commits to those files since the merge-base while main has made 1 to 5 on each. Live was stale and the release is newer, so the swap was an upgrade, not a regression. It adds session-config-drift-check.sh, hooks/CLAUDE.md and the context7-mcp skill; it drops two branch-only test files under lib/superpowers-parallel/tests that remain in git on the unmerged branch.

The binding 0281 rehearsal. A dispatched agent built a faithful scratch root: live settings.json copied in, the bootstrap installed through the primary install-bootstrap path against a git-archive extract of the sha, HOME overridden so the root sat at $HOME/.claude, all 9 live symlink targets reproduced verbatim by readlink, and hooks and rules cp -a'd as real directories. 13 assertions, all PASS. apply relinked all 11 entries, copied 5 byte-identical notes, wrote a well-formed journal; the acceptance property held at 27 of 27 registered hook references resolving through the swapped links, 0 unresolved; rollback restored the root to a byte-for-byte match of the pre-apply manifest, with hooks and rules returning at their original mtimes, so they were moved back rather than recopied. A second apply/rollback cycle was equally clean, proving re-cutover is repeatable. One finding: after a rollback the local/notes copies are deliberately retained, so editing a note afterwards makes the next apply fail closed with "already holds different bytes"; it aborts before any link is replaced, and the remedy is a manual removal that the rollback output never mentions.

What failed. The dispatch of the dedicated fresh-context swap agent that 0317 requires was denied by the auto-mode permission classifier. It was not retried and not routed around, and the swap was not performed from the orchestrating context, which by then sat at 80 percent and was exactly the degraded operator 0292 exists to keep away from an irreversible live migration. The user ran the apply by hand instead. Recorded as a divergence.

Verification after the live apply. All 10 promoted entries are symlinks -> current/<name> resolving into the release; notes -> local/notes with all 5 files byte-identical to source; the journal names the sha with all 11 records performed; 28 hook commands and 27 unique references all resolve; current, LIVE, settings.json and the 7 bootstrap .mjs files were untouched. The asides preserve the pre-cutover state of all 11 entries, including the only two that were irreplaceable: hooks at 57 files and rules at 31 files. One check as first written was vacuous and was not counted: diffing each entry's realpath against the release compares the release with itself now that the entries are links into it. It was replaced with the meaningful form, diffing the release against a git archive of 75b90a3d, which came back clean on all 10 entries; the only top-level difference is settings.json, which the release deliberately does not carry per 0287 and 0291. That is the evidence c6 criterion 10 asks for, so criterion 10 is discharged ahead of c6.

Live evidence beyond the static checks: the bash gate now resolves through the new symlink into main's newer block-destructive-bash.sh and has been admitting tool calls since the swap, so the hook surface is functioning, not merely present on disk.