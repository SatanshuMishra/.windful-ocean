Built the cutover unit in the worktree .claude/worktrees/spec-a-cutover on branch feat/config-entry-link-cutover, based on origin/main a302e4c. Nothing committed, nothing pushed, and live ~/.claude is verified untouched: no releases/, no current, no LIVE, no CUTOVER; hooks/ and rules/ still real dirs at 27 and 4 entries; notes still points into the checkout; local/ still the 6 bootstrap files.

Three build tracks ran in parallel against the spec at scratchpad/CUTOVER-UNIT-SPEC.md. Track A added scripts/config/cutover.mjs (plan/apply/rollback, journal, notes handling) plus its test file and 6 lines of constants in paths.mjs. Track B wired settings reconciliation into promote per 0291 and added WITHDRAWN_GRANTS per 0288. Track C fixed protect-claude-config.sh to read repo_root from the LIVE receipt and narrowed probes to CLAUDE.md and keybindings.json per 0287.

Two decisions recorded this session: 0290 (the cutover verb moves real content aside and never deletes, which dissolves the graphify-report ruling instead of forcing it) and 0291 (promotion applies reconciled settings, because resolveSettings had zero production callers and 0288's withdrawal set would otherwise be inert).

A code review of the scripts/config half returned BLOCK with 2 CRITICAL and 4 HIGH. Both CRITICALs share one root cause, the pointer and settings.json treated as independent: rollback swapped the pointer without reverting settings, and a settings-write failure after a successful swap returned promoted with a receipt recording unqualified success. The HIGHs were all cutover journal recoverability - the journal described a plan rather than performed work, so an aborted apply deadlocked rollback forever, and a second apply overwrote the first apply's aside record and stranded the real hooks directory.

The fix round was planned against ten named invariants at scratchpad/CUTOVER-FIX-ROUND-INVARIANTS.md rather than against the finding list, per the standing lesson that five prior rounds each satisfied their findings and each introduced a new defect on an unenumerated path. Tracks A2 and B2 were dispatched against those invariants and were still running when this event was written; their results are not yet known.

One reviewer finding is deliberately NOT fixed and is recorded as accepted: WITHDRAWN_GRANTS duplicating NOT_ADOPTED_GRANTS verbatim. 0288 already rejected collapsing them; identical membership today is coincidence, not shared definition.

Track C surfaced a real pre-existing security finding beyond the specified scope: when the probe realpath resolves into a release inside a version-controlled HOME, the guard's derived worktree set collapses and a write to the real checkout is silently allowed through rather than merely falling back to the textual match. Its new fixture is red against the unmodified script. Track C also resolved a test that was already red on main before this work - the installed-probe assertion failed because live settings.json is a real file, which is exactly what 0287 ruled it should be.

Verified by hand after Track C reported an editor-injected NUL byte: both guard files scan clean at 0 NUL, 0 non-ascii, no stray control bytes, with bash -n and node --check passing.

Not yet done: collect the A2/B2 results, re-review the full diff including the guard, author the invariant coverage docket at docs/invariants/coverage/feat-config-entry-link-cutover.json, commit, open one PR through pr.mjs pr-create, then the 0281 rehearsal against merged main, then the live swap.