Closed the ungated-main emergency, then audited the cutover unit and stopped short of building it.

SHIPPED. The receipts gate is on main. Diagnosed the c8 recurrence exactly: PR #57 merged chore/retire-invariant-machinery into main at 06:12:23Z and PR #58 merged ci/deploy-receipts-gate into that same parent 9 seconds later at 06:12:32Z, so c78c934 landed on a branch main had already taken. Both PRs still report MERGED. Re-opened from the UNCHANGED branch with --base main rather than cherry-picking, so the branch tip stayed c78c934 and the post-merge check is a hash-identity proof instead of a content comparison - the same weak evidence class as the MERGED badge that caused this (0315). Opened PR #59 through pr.mjs, all 10 checks green including receipts itself running with main as base. User merged; verified twice - merge-base --is-ancestor c78c934 origin/main exits 0, and both blobs on main (d3899f1, 829cb2e) are byte-identical to c78c934. Merge commit 0ba81ae preserved the hash as predicted.

AUDITED, NOT BUILT. A read-only codebase-analyst pass over feat/config-entry-link-cutover found two things the briefing did not have. First, the branch is BROKEN IN ISOLATION: committed cutover.mjs imports CUTOVER_ENTRIES, CUTOVER_ASIDE_PREFIX, NOTES_DIRNAME and isInsideResolvedContainer from paths.mjs, and those exports exist only in the UNCOMMITTED diff - swapping dependencies for origin/main versions throws SyntaxError, already recorded as row M3 of the worktree's own coverage audit. cutover.mjs was carved out of a coherent block and committed alone, stranding its prerequisites, which plausibly explains why review rounds kept surfacing new findings: they were reading half a feature. Second, the rollback hole is now precisely located rather than vague - corroborationVerdict and restoreEntry key off the journal's top-level sha while mergeJournal carries records forward keeping their ORIGINAL sha, so a record from an earlier release is refused forever as foreign-release, its aside never restored and never scanned as orphaned, and restoreEntry maps per-entry without short-circuiting so same-generation entries are physically renamed while the call reports error. No test cuts over to two releases before rolling back, which is why nothing caught it.

CORRECTED TWO STALE BRIEFING CLAIMS. cutover.mjs and its test ARE tracked now (bd63ca2, ee20348), not untracked. The branch is 12 commits behind main, not 64.

DECIDED. Finish the branch rather than rebuild (0316): the audit locates exactly one real defect and it sits on the record-sha versus journal-sha axis, orthogonal to the entry-state axis the design and its test matrix are built around, so a rebuild would re-derive the same model and reproduce the same blind spot at full cost. The rest is sound - ENTRY_STATES total over 4 states with both maps asserted complete by deepEqual, suite 2161 of 2161 green in that worktree, dirty diff coherent WIP rather than debris.

DID NOT DO, DELIBERATELY. Steps 1-4 (commit the stranded block atomically, fix the sha-axis defect test-first, rebase, review to SHIP) were left for a fresh session at user direction, on a context already past 70 percent. The live swap was never a candidate here: 0292 reserves it for a session doing nothing else, and this session had already shipped a PR.

TEST-RESULT DISCREPANCY, RESOLVED. npm test in the retire-invariants worktree failed one guard at protect-claude-config.test.mjs:194 on live ~/.claude/settings.json state; the same suite is fully green in the cutover worktree, whose uncommitted diff rewrites that guard and its test by +232 lines. Both readings are correct and of different file versions; that rewrite is itself review surface.