Landed step (1) of the round-6 order: the Q4 deletions. Four atomic commits on feat/invariant-inert-registry - be0f3b9, 1147b53, 1c5a91d, f8c81aa. Tree clean, nothing pushed, no PR opened.

WHAT SHIPPED. M2, M3 and M4 are gone from docs/invariants/registry.json; the registry now holds 14 ids (B1-B6, M1, M5, M6, G1-G5). UNWITNESSED_IDS lost the three ids and INERT_BARRED_IDS reduced to ['M5']. The barred-id test at invariant-coverage-check.test.mjs:369-379 was repointed from M3 to M5 with its assertions intact, and carries a mutation proof: emptying INERT_BARRED_IDS turns it red specifically on the /inert_when/ assertion, so it still exercises the barred path rather than passing off the witness-waiver error. M2's sentence went into testing.md's Quality Bar, M3's into testing.md's TDD (scoped) section, and M4's discharge half - a characterization test written before the refactor - onto the Atomic commits bullet in git/commits.md. 23 historical coverage entries were deleted per 0312.

THE FIRST ATTEMPT FAILED, AND THAT WAS THE VALUABLE PART. A writer dispatched against the original plan stopped at its trap gate with zero files changed. It measured that committing a 24-file coverage sweep pulls all 24 entries into diff scope, switching on totality for the 22 that predate the G track, demanding 110 rows of verdict prose about already-merged diffs. It refused to author them. It also found a defect in the receipt I had specified: scope reads the committed diff at invariant-coverage-check.mjs:396-397, so a working-tree run reports a false ok and cannot see a failure that only appears at commit time - the green half as specified would have certified a red branch. The second attempt took its green post-commit, and additionally under --event pull_request --base-ref main.

MEASURED FACTS WORTH KEEPING. Only the checker consumes coverage entries; the eleven registry hits are inert_when pattern strings and the ci-escalation.mjs / mitosis.js hits are CI check names containing the substring, not readers. An empty coverage directory is a hard failure at invariant-coverage-check.mjs:213-215, so at least one entry must always survive. The deletion set was chosen by git ancestry, not by any list supplied to the writer, and that mattered: fix-gate-hardening-followups.json was one of only two entries carrying all 17 ids, but its introducing commit 0451220 IS the merge-base, so the measured test correctly removed it where a hand-written keep-list would have kept it for the wrong reason.

VERIFICATION. invariant-coverage-check green in both push and pull_request modes; invariant-coverage-check.test.mjs 38/38; invariant-shape-check.test.mjs 12/12; block-destructive-bash.test.mjs 242/242 (the registry consumer, unaffected because its id filter is /^G[0-9]+$/); full npm test 1968 pass with 1 failure, the pre-existing protect-claude-config.test.mjs:194 case, in a file this diff does not touch.

SCOPE TAKEN BEYOND INSTRUCTION, disclosed by the writer: commit f8c81aa corrects the M1 row in the surviving receipt, which still asserted a verdict over all 17 ids in a file that now holds 14. The falsehood was created by the deletion itself, in the one artifact whose purpose is honest attestation.

NOT FIXED, needs a decision. The surviving receipt's M5 row cites four constants at scripts/invariant-coverage-check.mjs:21-24 and claims they are working-tree-only and uncommitted. Both halves are false - they sit at :28-31 and were committed in a4f0d41. That is a live M5 breach inside the row attesting to M5. The writer declined to rewrite another commit's attestation prose, which is the right call; it is not a subagent's decision to make.