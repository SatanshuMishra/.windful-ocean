c6 discharged: all 10 of SPEC A's acceptance criteria verified by observation, not assertion.

Method. Tooling re-extracted from main at 75b90a3d into scratch rather than trusted from the working tree — the primary checkout sits on feat/invariant-inert-registry, which is 101 commits behind main and does not contain scripts/config at all. Two test-engineer agents ran disjoint criteria (1-5, 6-10) in separate sandbox config roots, both dispatched run_in_background false. Live ~/.claude was read-only throughout; every behavioural probe ran under an explicit sandbox --config-root.

Results. 10 of 10 pass. The load-bearing ones:

- c1: swapPointer is symlink-to-current.tmp then renameSync onto current, with no unlink of current on the swap path; man 2 rename on this machine confirms an instance of the target always exists. A stale current.tmp planted mid-flight left current untouched.
- c3: all four rejection classes refused and left live untouched — missing entry, present-but-empty entry, unresolvable hook, non-executable hook, and invalid syntax for both bash and node.
- c7: the drift report was OBSERVED firing for the first time, closing the standing watch-out. Sandbox with LIVE at sha X and main at sha Y, invoked through the real hook entry run({argv, env, stdout, stderr}); stdout carried hookSpecificOutput.hookEventName SessionStart with additionalContext naming both shas, exit 0, stderr empty. It reaches session context, not stderr.
- c6: proven against the live machine as the experiment, since the checkout is on a non-main branch right now. All 10 promoted entries realpath inside releases/75b90a3d. Two files that differ between main and the branch — hooks/protect-claude-config.sh and rules/context7.md, the latter deleted outright on the branch — are live in main's version. converge refuses any non-main ref outright.
- c8: an unrecognised key survived promotion and was flagged rather than dropped, live-owned model preserved, repo-owned env applied.
- c10: re-verified rather than inherited. Fresh git archive of 75b90a3d diffed against both the live tree and the deployed release; diff -r byte-zero on all 10 entries, matching file counts. notes is correctly absent from PROMOTED_ENTRIES and links to local/notes by design.

Suite: 188 pass, 0 fail. The single initial failure was an extraction artifact — converge.test.mjs reads .claude/settings.json three levels above tests/, which a scripts/config-only extract does not carry; supplying it read-only turned the suite green. Not a product defect.

One verdict overturned. Agent 1 returned c4 as FAIL, reporting that rollback shells to git and requires receipt.repo_root on disk, and read that as contradicting SPEC 5.2's rename-never-rebuild. Verified against the source instead of accepting it: rollback refuses when the target release is absent, with an error that says in as many words that rollback is a rename and never rebuilds. The repo_root dependency is settings recomputation, not a release rebuild — settings.json is excluded from releases by section 6, so rollback must re-derive what the target sha declares and refuses rather than leave live settings reconciled for the newer sha. Criterion 4 as written passes. The dependency is real and is filed as a risk instead.