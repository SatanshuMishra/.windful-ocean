PR #54 IS MERGED. Verified rather than taken on report: gh pr view 54 -> MERGED at 2026-08-07T22:11:40Z, merge commit 1e84dd1; f14fb9d is an ancestor of origin/main; scripts/config/ is present on origin/main including the new _interpreters.mjs helper. origin/main = 1e84dd1. LOCAL MAIN IS STILL STALE AT 0451220 and can clean-fast-forward.

The whole promote/validation unit is now on main. That closes c2 (promote verb with pre-swap validation, rollback by rename, LIVE receipt, bootstrap outside releases) and the hardening detour. Nothing live has been touched: across three faithful rehearsals the real ~/.claude mtime never advanced past a time predating each run, and releases/, current, current.tmp and LIVE are all still absent there. The live config is still 37 symlinks straight into the primary checkout, exactly as at session start.

WHAT THIS SESSION ACTUALLY DID. Fixed the three defects that rejected every candidate release permanently (notes in PROMOTED_ENTRIES; hook syntax dispatched on extension so a python file named .sh got bash -n; settings.json shipped inside releases against 0270), then hardened the validator across two independent reviews and three fix rounds, then shipped it as one merged PR.

THE CRITICAL, AND WHY IT MATTERS BEYOND ITSELF. The security review returned BLOCK on a flaw THIS BRANCH INTRODUCED: python3 -c sets sys.path[0] to the inherited cwd, and ast is a shadowable pure-python stdlib module, so a repo containing a root-level ast.py got that file EXECUTED during validation AND the shadow parse() returned instead of raising, so the entire python syntax gate silently passed. Proven end to end through the real hookFailures() entry point, then verified closed adversarially with a control showing plain python3 -c from that same directory DOES load the shadow on this machine while the fixed path does not. Four hooks route to the python checker in the live settings today. The branch's own invariant row G5 had cleared this spawn on the grounds that nothing from the hook file is interpolated into the program text — correct, and beside the point, because the risk was the sys.path entry -c implies. Fixed with -I plus a PATH-only env, a per-validation empty sandbox cwd, a spawn timeout and output caps.

FIX ROUNDS WERE PLANNED AGAINST INVARIANTS, NOT FINDING LISTS, per the standing lesson from this project. It worked and it was not sufficient: the round satisfied I4 (everything validated lives inside the release) for hook paths and left jsonParseFailures walking through committed symlinks OUT of the release, emitting file-content prefixes into the drift report that converge injects into session context — the sibling code path right next to the one that was fixed. Same for I6: stripping landed, the cap bound only the checker reason. SHARPENED LESSON FOR NEXT TIME: when an invariant is declared, enumerate every code path it applies to BEFORE dispatching, because a fix round will satisfy it where the finding pointed and nowhere else.

CI THEN FAILED, AND THE FAILURE WAS MORE INTERESTING THAN THE BUG. A zsh end-to-end test silently required zsh on the validating host; the GitHub Ubuntu runner has none. The sweep found the same fragility in four more tests, and TWO WERE PASSING WITH python3 ENTIRELY ABSENT — a missing interpreter raises the same hook-syntax rule those tests asserted, so they were green while proving nothing. That is the vacuous-pass failure mode living in the SUITE rather than the validator. Tests are now split: checker SELECTION asserted through resolveChecker with zero spawns, versus genuine syntax rejection guarded on availability with a visible skip reason. Verified PATH-stripped rather than by local green: normal 98 pass / 0 skip, no-zsh 97/1, no-python 94/4, 0 fail throughout. Production code byte-identical across that fix (git diff 58a5085..f14fb9d -- scripts/config/validate.mjs empty).

MY OWN TWO ERRORS, recorded because they cost real time. (1) I dismissed the code review's LOW about the suite depending on python3 as "portability, no action"; that exact class then broke CI on zsh and hid two vacuous tests. (2) I described the suite as effectively green when the one local failure and CI's failure were different things — the local one does not occur on CI at all — so "one known local failure" was never evidence CI would pass.

ONE PROCESS JUDGMENT worth knowing when reading the history: the hardening was NOT split into its own PR even though it made #54 21 commits and ~1500 lines. Splitting would have meant knowingly opening a PR containing a live code-execution path. Production surface was ~425 lines; the rest is tests and the invariant record.

DELIBERATELY NOT FIXED, disclosed in the invariant record under 0283 rather than dropped: hookRegistrations silently discards malformed registrations (needs malformed live settings.json, outside the hostile-commit threat model); checkerEnvironment passes PATH through, so an attacker-writable ABSOLUTE PATH entry hijacks every checker (anyone with that write already controls every command the user runs, and pinning risks breaking the homebrew-vs-/usr/bin python3 selection the rehearsal depends on); and a same-user rename race between realpathSync and the spawn, unavoidable without fd-based checking Node does not expose.

STATE AT HAND-OFF: nothing running, no background shells, no agents. The CI watch shell b6gk6b9xi was orphaned by a process exit and is moot — CI was verified directly afterwards. Worktree .claude/worktrees/config-release-fixes still exists at f14fb9d and can be removed. Primary checkout untouched on feat/invariant-inert-registry at 369490f with the same two modified files it had at session start (unrelated inert-registry work, per 0280 off this phase's critical path).