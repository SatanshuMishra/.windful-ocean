Presented the five round-6 open questions per the recorded next step, re-explained them from zero on request, and took the user's answers on all five plus three follow-ups that changed scope. NO CODE WAS WRITTEN. Nothing pushed, no PR opened. The three commits on feat/invariant-inert-registry from the prior session remain unpushed.

VERIFICATION DURING PRESENTATION FOUND THE ROUND-6 ARTIFACT WRONG IN TWO PLACES.

First, artifacts/2026-08-10-open-questions-for-round-6.md Q5(a) states that three overlapping constants were collapsed into one named GUARDED_CONFIG_ENTRIES homed in scripts/config/paths.mjs, and that paths.mjs exists only on the cutover branch. Neither holds. No symbol of that name exists anywhere in the primary checkout or the cutover worktree, including uncommitted edits. scripts/config/paths.mjs is present on main and byte-identical to feat/config-entry-link-cutover. The collapse was described but never landed.

Second, that artifact's Q3 recommendation reads, taken literally, as an instruction to add a permit-set clause as prose. Doing so would move the drift surface rather than remove it. This was corrected in decision 0304.

THREE GUARD LISTS DO EXIST AND ARE MATERIALLY DIFFERENT: PROMOTED_ENTRIES (10 members) and CUTOVER_ENTRIES at scripts/config/paths.mjs:18 and :33; GUARDED_FILENAMES (4) and GUARDED_PREFIXES (4) at scripts/config/capture.mjs:141 and :148; and a hand-typed 7-entry GUARDED list at .claude/hooks/tests/protect-claude-config.test.mjs:97 which imports nothing from capture.mjs. That third list is the actual defect and is the subject of decision 0306.

CENSUS RUN LIVE. node scripts/invariant-shape-check.mjs reports FAILED with 2 domain-anchoring findings over 1 declared domain and 52 vocabulary-census findings over 52 candidate domains in 141 tracked source files. The 2 anchoring findings are B4 naming POLICY_LISTS at .claude/lib/superpowers-parallel/workflow-sandbox.mjs:70, which is module-private and therefore not iterable by a witness in another module, and B4 hand-listing 3 of that constant's 4 members with BOUND_DENIALS left unquantified. 28 of the 52 are emitted discriminants. 6 come from the vendored .claude/skills/impeccable/ tree.

CI STATE ESTABLISHED. .github/workflows/test.yml:32 runs invariant-coverage-check.mjs with NO --event and NO --base-ref, though the checker accepts both and refuses to let a pull-request run fall back to push mode. The workflow triggers on an UNFILTERED push plus pull_request, so every PR runs the same commit twice under different diff scopes. Because the checker's inert-basis proof requires a diff, its verdict varies with the change set and trigger rather than with whether the invariants hold. This is the mechanism behind the reported intermittent failures; the failing run logs themselves were NOT read, so the mechanism is named rather than confirmed against a specific failure. invariant-shape-check.mjs is not wired to CI at all.

FIVE ANSWERS TAKEN: Q1 proceed as recommended; Q2 exclude vendored code; Q3 amend the contract not the code; Q4 yes, delete before authoring witnesses; Q5 clean up and resolve robustly rather than accept as residuals. The Q5 answer overrides the recommendation to accept the cross-release aside case as a stated residual, putting N2 in scope. Recorded as a deliberate scope addition in decision 0307.

AGREED EXECUTION ORDER, CI FIRST: (0) fix the workflow per 0305; (1) Q4 deletions, sole writer under .claude/rules/; (2) census admission rule per 0303; (3) wire invariant-shape-check.mjs and ratchet UNWITNESSED_IDS; (4) I3a permit-set as a fixture per 0304; (5) Q5(a) per 0306; (6) Q5(b) per 0307; (7) witnesses last, against the smaller registry.