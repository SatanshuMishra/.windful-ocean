Second half of the session, after c7 closed: located SPEC B, opened it, and diagrammed it.

WHERE SPEC B WAS. The user asked where SPEC B is, since the predecessor thread mitosis-core-respec aimed to produce two SPECs. It is committed on main at docs/superpowers/specs/2026-08-06-mitosis-core-cost-decomposition-stacking-SPEC.md, alongside SPEC A at 2026-08-06-config-staging-live-promotion-SPEC.md. Both were authored and approved 2026-08-06, closing the re-spec thread at 6 of 6. The search cost several passes because the ledger cites SPEC B only by section (Part IV, section 5.5, section 5.6) and never by path, and the handoff artifacts directories hold the core-rebuild spec lineage rather than the A/B pair.

NOT DELETED. SPEC B is absent from this branch's working tree only because feat/invariant-inert-registry diverged at 0451220 (#42), which predates the spec landing. Confirmed by git cat-file against HEAD and against the merge-base, and by a diff-filter=D log showing no commit on this branch removed it. Read it with git show rather than switching branches; a local copy is at scratchpad/SPEC-B.md.

WHAT SPEC B IS. 340 lines, status designed-not-implemented. Four parts in a fixed order that is an argument rather than a preference: Part I the token and tool-call instrument, landed alone, producing a baseline; Part II cost reduction as five falsifiable hypotheses H1-H5; Part III decomposition of the 5,515-line monolith by codegen; Part IV stacked PRs replacing the frontier-train. The ordering fact is that no token or tool-call accounting exists anywhere in the engine today, so every cost claim is unmeasurable until Part I lands. Its own gate, per 0271 - SPEC A must LAND before any SPEC B unit, because SPEC B writes into .claude/lib, .claude/workflows and .claude/hooks, all live-linked - is now satisfied.

THE VISUAL EXPLANATION. Dispatched a general-purpose agent (run_in_background false) to run the visual-explainer skill against a diagram plan I specified from having read the spec. Output at ~/.agent/diagrams/spec-b-mitosis-core-cost-decomposition-stacking.html: 11 sections, 8 Mermaid diagrams, paired before/after, with solid borders meaning measured-today and dashed meaning proposed-and-never-built, carried into the classDefs so a diagram read in isolation cannot be mistaken for current fact.

TWO FINDINGS ABOUT SPEC B ITSELF, from the agent's re-derivation pass against the working tree. First, the *_SCHEMA count does not reproduce: the spec says 26 scattered declarations, a strict grep finds 25. The span endpoints (:977 to :5425) and the scattering both re-verify, so the load-bearing point stands, but the count is a candidate seventh entry for the spec's own section 0.6 list of citations that did not survive re-derivation. It was deliberately not silently corrected. Second, several load-bearing figures are not reproducible from anything committed: the prompt-byte table (26,354 / 14,826 / 4,183 / 3,704 / 3,365 / 2,558 / 1,648 / 346), the ~24 dispatches on a 6-MSP run, the 37 census rows denominator, and the 4,717 to 5,515 growth across 30 commits. SPEC B's own evidence base therefore sits inside the measurement vacuum the SPEC exists to close, which is an argument for Part I rather than against the SPEC. Separately, deleteBranchOnMerge false is a GitHub repo setting needing an API call and is carried as a spec claim, not re-verified.

Everything else the agent cited was re-derived: mitosis.js 5,515 lines, divergence.mjs 75, gh-merge-shim.mjs 357, normalizeMergePolicy returning HUMAN_GATED from a one-line body, the four opus pins, agent( 31 / parallel( 1 / pipeline( 0 / workflow( 0, guard.dispatch( 8, Date and Math.random zero occurrences, phase: 'Ship' 15, HOOK_NAMES omitting budget, the three loop bounds, MITOSIS_GIT_VERBS at pr.mjs:30 confirming no retarget verb exists, and the gate's pr edit conjunction at block-destructive-bash.sh:134.

Nothing left running. Audit script at scratchpad/c7-audit.mjs and the spec copy at scratchpad/SPEC-B.md are both re-runnable or re-readable; the HTML report is outside the repo and survives the session.