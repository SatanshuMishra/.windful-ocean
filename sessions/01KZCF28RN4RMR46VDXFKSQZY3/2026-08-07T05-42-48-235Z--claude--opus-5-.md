Session of 2026-08-06. Resumed this thread by explicit slug, user approved "Go". No commit reached main; nothing irreversible was done.

RECON (main thread, read-only, measured against the live tree rather than trusting SPEC A's prose). Confirmed the linkage census: ~/.claude holds 37 symlinks into the checkout (9 at depth 1, 26 under hooks/, 2 under rules/); releases/, current, LIVE and local/ do not exist. Confirmed the coverage gap: 56 files under .claude/docs/superpowers/ (plus one generated manifest.json), 1 under docs/analysis/, 5 under .claude/notes/, and the 2 session-config-drift-check files. MEASURED the *session* pattern at .gitignore:19 precisely, which SPEC 7.2 leaves qualitative: it uniquely hides exactly those 2 files. Every other path matching the substring is either re-included by the negations at .claude/.gitignore:45-48 or sits inside the tpm submodule, so narrowing is a two-file delta and is verifiable as such. SPEC A itself is NOT on main - it exists only on the unpushed, unmerged, PR-less branch chore/config-drift, which also already deletes the Bash(ln -sfn:*) grant that SPEC section 3 flags as the wrong form.

THE BLOCKER FOUND. The repository is public (SatanshuMishra/.windful-ocean, isPrivate=false) and 5 of the 64 files SPEC 7.1 would adopt carry the confidential cross-project codename - 15 occurrences total, 6 across three docs/superpowers/plans files and 9 across two .claude/notes files. Adoption is publication and is irreversible. Halted and asked rather than assuming a disposition. Resolved by 0276.

THE COST FOUND. docs/invariants/registry.json declares 17 invariants and scripts/invariant-coverage-check.mjs fails any pull_request that adds or modifies no file under docs/invariants/coverage/, so SPEC A at roughly seven PRs implied about 119 written verdicts, most about a vm sandbox this work never touches. Resolved by 0277, which also fixed the stack root.

DECISIONS RECORDED AT DECISION TIME, not at wrap-up: 0276 (three plans scrubbed at source, .claude/notes/ reclassified live-only and never release content) and 0277 (chore/config-drift is the stack root; the inert basis lands before SPEC A's units). Landing order refined beyond the literal answers to inert basis -> chore/config-drift -> SPEC A units, so config-drift's own coverage file is not written at full freight.

UNIT DISPATCHED AND WHAT WENT WRONG. One implementer built feat/invariant-inert-registry at 369490f (pushed): inert_when on B1-B6 and G1-G5, cross-assigned so sandbox invariants go inert for bash-gate-only paths and vice versa; M3/M4/M5 barred by the checker because they assert HOW a change was made, not WHERE it landed; M1/M2/M6 left prose-only. The agent caught a genuine self-inflicted bug mid-run - its first glob set could never prove inert because every PR must touch docs/invariants/coverage/, which always lands in scope.

THE UNIT WAS NOT SHIPPED, and the reason is the important part. I briefed it "data only, do not change checker semantics" on the belief that the inert machinery was already committed. It is NOT. git show main:scripts/invariant-coverage-check.mjs contains zero occurrences of INERT_WHEN_FIELD; the implementation exists only as uncommitted working-tree edits to that file and to .claude/lib/superpowers-parallel/tests/invariant-coverage-check.test.mjs. My earlier grep read the working tree and I mistook it for committed code - that briefing error is mine. The consequence is worse than the error: the branch as it stands adds registry data that main's committed checker never reads, and a dormant basis is worse than none, because a later PR could write basis "inert" rows that pass unexamined. No PR was opened.

PROVENANCE UNRESOLVED AND CARRIED FORWARD. The working tree was CLEAN at session start (verified) yet now shows those two files modified. The inert checker implementation therefore appeared during this session and is unattributed - it may be the invariant-coverage-tax thread's c5 work surfacing, or the agent editing the checker after being told not to. It is unreviewed code sitting in the gate that guards every PR in this repository, so it was deliberately left uncommitted rather than committed on an assumption.

ALSO OBSERVED. npm test is 1949/1950 with one failure identical on main and on the branch: protect-claude-config.test.mjs asserts ~/.claude/settings.json is a symlink when it is a regular file. Environmental and pre-existing, not caused by this work, but it will mask a real regression while it stays red.

NO CRITERIA AMENDED. The inert-basis unit is unplanned relative to c1-c7 but was not closed this session and already has a home as criterion c5 of the paused invariant-coverage-tax thread, so it was not duplicated here as a detour; the dependency lives in 0277 and in next_step instead.

NOTHING LEFT RUNNING. The single dispatched agent completed and its claims were independently verified before being relayed.