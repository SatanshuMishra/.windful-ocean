Round 5 implemented against the stated invariant contract. All ten contract steps except step 10 are done; the work is GREEN and UNCOMMITTED in .claude/worktrees/spec-a-cutover on feat/config-entry-link-cutover, which still sits at origin/main.

VERIFIED BEFORE ACTING (zero-assumptions pass). Every line anchor in artifacts/2026-08-08-cutover-control-file-invariants.md was re-checked against the tree and held: protect-claude-config.sh:20-21 guarded set, the derived suppressor at :109-125, guardpath at block-destructive-bash.sh:17, lexical isInside at paths.mjs:50-56, git call sites at release.mjs:27/46/55/83. Live pre-state re-measured by readlink: nine of eleven entries are outward symlinks into the primary checkout, hooks/rules/local are real dirs, current/releases/LIVE/CUTOVER absent. Step 1 (F4) had NOT landed - classifyEntry still compared realpaths.

EXECUTION. Three implementers in parallel on disjoint file fences in one worktree. Lane A steps 1-6 (paths.mjs, cutover.mjs, cutover.test.mjs). Lane B steps 7-8 (both hooks + hook tests). Lane C step 9 (receipt.mjs, release.mjs + tests). Each was told its traps up front: no round-4 created-derivation remedy, no re-unifying the write-kind containment split, the two protect-claude-config tests WILL invert and preserving the defect to keep them green is forbidden, fsmonitor was never measured and must not be reported as if it were.

WHAT SHIPPED. I1-I4: classifyEntry compares literal target to linkTargetFor(name); moveAside runs for link as well as real; restoreLink deleted, rollback restores only by rename(derivedAside -> entryPath) plus unlink; entry.target/entry.aside/entry.created/journal.notes have zero remaining uses; journal record shape is now {name, state, sha, recorded}. I5: lexical floor unconditional and ordered BEFORE discovery, derived flag and its gate deleted, discovered root must contain .claude, floor stays ask. I6: CUTOVER, LIVE, releases, current, *.pre-cutover-* added to both gates, guardpath composed from one guardname list. I7: repoRootErrors boundary predicate (absolute, realpath-resolving, directory, not inside config root, contains .claude), all four git sites via one hardened runGit.

EVIDENCE. Lane A: 9 red before, 34/34 after. Lane B: 21 red before, 346/346 after (baseline 323). Lane C: 11 red before, 16/16 after. Full repo suite over the combined tree: 2159 tests, 2159 pass, 0 fail, exit 0, 74s. Lane A did not trust its own reds - it ran a sandboxed exploit probe against the OLD code and reproduced the attack (rollback planted a directory at CLAUDE.md, repointed sounds at an attacker path), then showed the new code refuses it with fully well-formed planted records. Lane C caught two of its own tests passing for the wrong reason (git error text coincidentally contained .claude and the ref string) and rewrote them into real falsifiers before implementing.

TWO CORRECTIONS TO THE CONTRACT ARTIFACT. (1) core.fsmonitor is no longer unverified: measured on git 2.55.0/darwin with a repo-local hook wired to a sentinel log, rev-parse/ls-tree/show/archive invoked it zero times; only a git status control fired once. One git version, one fsmonitor configuration - this narrows F5, it does not settle it. (2) There is no git worktree list call anywhere under scripts/; the artifact's Not-verified note named a subcommand that does not exist in this codebase.

WHAT DID NOT HAPPEN, and why. Step 10 (re-derive docs/invariants/coverage/feat-config-entry-link-cutover.json) not started - the contract orders it last so it cannot go stale. The fresh independent review was NOT run: context ran to ~80% and a truncated review is precisely what produced the last four BLOCK verdicts. Nothing committed, pushed, or PR'd - the thread's own order puts the review before the commit. Nothing under ~/.claude/ was written and the primary checkout was never entered.

RESIDUALS THE FRESH REVIEW SHOULD START WITH, not rediscover. (1) ln is absent from guardverb, so ln -sfn releases/<evil> ~/.claude/current re-points the whole live config unremarked; adding current to guardpath does NOT close it, only a verb-set change would. (2) classifyNotes still classifies by realpath; the naive fix routes a self-referential notes symlink into copyFileSync with src==dest and truncates the file. (3) cutover.mjs is 836 lines, over the house 800 cap; the journal extraction was deferred because commit rules forbid mixing a move with a behavior change. (4) resolveRef and declaredSettings cannot run the containment clause - needs configRoot threaded from promote.mjs:259/:273 and converge.mjs:62. (5) Discovery in protect-claude-config.sh is now decision-inert (the floor is a strict superset and runs first); deleting that block would remove a subprocess and ~40 lines, but it deletes a mechanism a prior round built deliberately. (6) Dead branches at promote.mjs:352 and converge.mjs:54. (7) GIT_CONFIG_GLOBAL=/dev/null also discards global safe.directory entries, so a differently-owned checkout the user whitelisted is now refused by git's ownership check. (8) Lane C added a leading-dash refusal to promote --ref, beyond I7's literal enumeration - a scope call for the reviewer, trivially revertible. (9) The docs/security/bash-gate-threat-model.md section 6 row recording the new I6 patterns is unwritten. (10) Pre-existing fail-open at protect-claude-config.sh:2 (input="$(cat)") - the hook goes silent rather than asking if cat is unavailable; it has no fault verdict, unlike the bash gate.

THREAT-MODEL CHECK. Lane B checked before touching the gate: no collision with a stated non-goal. The I6 gap is a finding against G4, a stated GOAL whose path list simply missed the aside namespace, so it is a real defect rather than an accepted risk. Corpus registered under corporaByGoal.G4; the row-17 pr-create collision warning was re-measured and passes.

CAVEAT ON I6, stated as Lane B stated it: porous defence-in-depth. python3 -c "open('~/.claude/CUTOVER','w')..." still slips past both gates. I1-I3 hold without I6 and must never lean on it.