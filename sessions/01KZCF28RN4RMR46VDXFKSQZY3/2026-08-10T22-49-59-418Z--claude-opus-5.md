Closed c7 by auditing the whole #43-#61 PR range rather than spot-checking it.

Identity proof: for each PR, parsed the body back into renderer fields and re-rendered it through the real renderPrCreateBody from .claude/lib/git/pr-format.mjs, comparing byte-for-byte. 17/17 round-trip exactly, so block separators, bullet prefixes, section ordering and the origin-consistent trailer all line up. A pasted trailer does not survive that. Titles: 17/17 match PR_TITLE_PATTERN. Mandatory fields complete throughout, with --origin machine paired with provenance on every one.

No bypass: every first-parent commit on origin/main from 098f228 (#43) onward is either a squash commit ending (#NN) or a merge commit naming a PR. Nothing landed outside a PR. #41, #50 and #53 are not PRs at all and issues are disabled, so they are not missing units.

Stacking: compared per-PR createdAt against each predecessor's mergedAt. #45/#46/#47 were opened on chore/config-drift while #43 was unmerged; #48/#49 on feat/config-promote-verb while #45 was unmerged; #58 was opened 52 seconds after #57 and correctly stacked on chore/retire-invariant-machinery rather than main. Every base:main PR was opened only after its predecessor had landed. The only cross-PR commit-set overlaps are #51 containing #48+#49, #52 containing the whole stack, and #59 repeating #58's single commit c78c934 - the three documented bottom-up re-integrations, each re-landing commits genuinely not yet on main. No undeclared stack.

Why the expected gaps did not materialize: c8's defect was a merge TARGET (five PRs merged into stack parents instead of main). c7 asks about the creation shape - base selection and tool provenance - which was never violated. The two are different axes.

Finding outside SPEC A: feat/invariant-inert-registry, the branch this session sat on, has never had a PR and carries 7 of 8 commits not upstream by patch-id. It is not a SPEC A unit (c8 already established the seven units are all on main). Inference, not verified: 0313 retiring the homegrown invariant system whole mooted it, and the invariant-coverage-tax thread owns its disposition.

Audit script retained at scratchpad/c7-audit.mjs; it is re-runnable.