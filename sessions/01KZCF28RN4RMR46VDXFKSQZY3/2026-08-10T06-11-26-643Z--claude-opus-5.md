Directive reversal session. The round-6 invariant work was abandoned mid-flight and the whole homegrown invariant system retired per 0313; receipts adopted as sole owner per 0314-equivalent user ruling.

What happened. An orchestrator was dispatched to finish old steps (2)-(7). It stopped twice without landing anything: first returning only "Waiting on the review" after backgrounding a review it never waited on, then, after being resumed with a synchronous-dispatch correction, reporting that two reviews converged on APPROVE-WITH-FIXES but had found a demonstrated live false exclude in the census admission gate (gh-merge-shim.mjs:171 emits a kind discriminant through a ternary so only 'file' registers, while :178 branches on 'stdin'). That finding is as reported and was never independently verified, because the user then halted the work entirely.

Teardown SHIPPED as two stacked PRs, both open, neither merged:
- PR #57 chore/retire-invariant-machinery -> main. 41 files, +6/-3259, six commits d72073c 45e9cb1 e6302c8 9f67137 b3466f2 41569c1.
- PR #58 ci/deploy-receipts-gate -> chore/retire-invariant-machinery. One commit c78c934.

Verification is real, not asserted. Baseline on pristine origin/main was 2056 tests / 1 fail; after teardown 2034 / 1 fail, the same pre-existing protect-claude-config.test.mjs:194 live-config failure. The 22-test delta is fully accounted: 3 ci-invariant-wiring + 18 invariant-coverage-check + 1 registry cross-check. Zero new failures. test.yml parses with jobs exactly [test].

Two implementer deviations, both defensible and accepted. ci-invariant-wiring.test.mjs was folded into commit 1 rather than commit 3 because it asserts the CI job's own wiring and reds the instant that job is unwired; the checker was likewise paired with its test. The CI-first ordering constraint was preserved.

Two invariant- greps deliberately survive: CI_ENFORCER_CHECK_TOKENS in ci-escalation.mjs:11 and its mirror mitosis.js:2264 hold the string 'invariant-coverage' as a substring classifier over CI check names on OTHER repos mitosis drives. Nothing dereferences it, and removing it would make a surviving invariant-coverage check elsewhere unclassifiable and halt the closed census.

PR #58 caveats, disclosed not hidden: the D6 step was omitted (scripts/d6-check.cjs absent here) and npm ci was omitted because this repo has NO LOCKFILE and that step would have failed outright. The enforcer's verify.js was run locally against this exact base..head, exit 0 with WARN - not a fix-claim. The pinned action SHA running on real GitHub Actions is NOT verified.

Cutover recon completed and the headline is that the cutover CANNOT be performed. scripts/config/cutover.mjs does not exist on origin/main at all; it lives only on unmerged local branch feat/config-entry-link-cutover in worktree .claude/worktrees/spec-a-cutover, 929 lines, with uncommitted edits to six sibling files, nothing pushed, no PR, no CI, and an unresolved symlink-target validation gap where restoreLink reaches placeLink and passes target unchecked to symlinkSync - a hole in the ROLLBACK path.

Live topology measured today: 37 symlinks under ~/.claude, 9 depth-1, 26 under hooks/, 2 under rules/, ALL resolving into this repo checkout. No releases/, no current, no LIVE receipt - no promote has ever run against real config. ~/.claude/local/ holds six modules byte-identical to main's tip, so install-bootstrap has run. ~/.claude/settings.json is a regular file, not a symlink.

Two silent gaps found: manifest.mjs resolveSettings is DEAD CODE in production, promote() only strips settings.json and never recomputes it, so acceptance criterion 8 has no code path; and converge is registered in main's tracked settings.json but NOT in live ~/.claude/settings.json, so it never runs automatically.

Housekeeping. stash@{0} holds the orphaned round-6 census/shape-check work. An unrelated commit 41de7ca landed on feat/invariant-inert-registry mid-session from another session, committing the settings.json enabledPlugins reorder. Primary checkout never left its branch.</body>
</invoke>
