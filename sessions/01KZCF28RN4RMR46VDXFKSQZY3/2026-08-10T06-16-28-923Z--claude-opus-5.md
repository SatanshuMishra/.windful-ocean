Merge verification session. Both PRs merged, but the stack mis-merged and the receipts gate never reached main.

VERIFIED ON MAIN (origin/main now 39b161b):
- PR #57 MERGED into main at 06:12:23Z. Zero files matching docs/invariants or scripts/invariant-* survive on main, confirmed by git ls-tree count = 0. The teardown is complete.
- The three relocated prose rules DID carry onto main: testing.md on main contains the inertness-mutation paragraph, confirmed by grep count = 1.

THE DEFECT. PR #58 MERGED at 06:12:32Z but its base was chore/retire-invariant-machinery, NOT main. That parent branch had already merged into main nine seconds earlier, so #58's commit c78c934 landed on an already-merged branch. git merge-base --is-ancestor c78c934 origin/main returns FALSE. .github/workflows on main are exactly labeler.yml, security.yml, test.yml - no receipts.yml.

Consequence: main today has NO invariant machinery and NO receipts gate. The repo is ungated for fix-verification, which is the opposite of what 0314 ratified. The orphaned content is two files, .github/workflows/receipts.yml and receipts.config.json, 43 insertions, alive on remote branch ci/deploy-receipts-gate at c78c934.

This is the SAME failure already recorded in criterion c8, where five PRs merged into stack parents rather than main and needed bottom-up re-integration through two further pr.mjs PRs. It recurred because a stacked PR whose parent merges first is left pointing at a dead base, and GitHub reports it MERGED either way. The merged-state report is not evidence the content reached main; only ancestry is.

FIX IS MECHANICAL: open one pr.mjs PR re-basing c78c934 onto current main. Do not cherry-pick by hand and do not force-push; the branch and its commit are intact.

Carried forward: stash@{0} in the primary checkout holds the orphaned round-6 census and shape-check work if the invariant redesign thread wants prior art. Primary checkout still on feat/invariant-inert-registry, tree clean, three stashes intact, never switched branches.