SPEC A's implementation units shipped and merged to main; the cutover was rehearsed and correctly refused. Nothing live was touched all session - ~/.claude/{releases,current,local,LIVE} are all still absent and the 37 symlinks are intact.

RESOLVED THE BRIEFED NEXT STEP, BOTH HALVES. Provenance of the uncommitted edits to scripts/invariant-coverage-check.mjs and its test is CONFIRMED, not inferred from resemblance: they are byte-identical to artifacts/2026-08-06-c5-checker-mechanism.patch (zero-line diff, identical numstat), so they are the invariant-coverage-tax thread's c5 mechanism. The merits review 0279 asked for was already written on that thread - artifacts/2026-08-06-inert-glob-audit-BLOCK.md says "Do not ship this data" - and 0278 requires a checker-enforced kind rule with the sets re-derived. Measuring 0278's narrowing showed the inert basis returns ZERO verdicts to any SPEC A PR, since a PR is inert only when every changed path matches and every unit here touches .sh, .mjs, .gitignore or settings.json. Recorded 0280: the basis leaves this phase's critical path, feat/invariant-inert-registry stays parked at 369490f with no PR, and this stack pays the full seventeen-verdict prose tax instead.

SHIPPED. A 13-agent dynamic workflow (author then review-and-ship per unit, every author stage worktree-isolated) opened seven PRs, all CI-green: 43 chore/config-drift as stack root onto main, then 44 context7 adoption, 45 promote verb, 46 graphify output relocation, 47 coverage-gap closure with the narrowed session ignore, 48 converge hooks, 49 settings manifest and capture verb. The review stages caught four HIGH defects the author stages missed - the pointer swapping before the LIVE receipt was known writable (no rollback target), an fs throw escaping the SessionStart hook contract, capture adopting live's permissions.deny wholesale over this repo's own merge controls, and a swallowed non-zero status that skipped the whole settings comparison while still reporting no drift. U1 adopted 59 of the 64 untracked files; the 5 under .claude/notes/ stayed untracked per 0276, and the leak scan confirmed two of them do carry the confidential codename.

MERGE TOPOLOGY WENT WRONG AND WAS REPAIRED. All seven PRs reached MERGED state but only 43 and 44 reached main - GitHub retargeted 44, while 45/46/47 merged into chore/config-drift and 48/49 into feat/config-promote-verb, the latter two AFTER 45 had already merged that branch upward, so the parent carried neither. Opened PRs 51 (feat/config-promote-verb onto chore/config-drift) and 52 (chore/config-drift onto main) through pr.mjs to re-integrate bottom-up. Both merged. Verified by merge-base ancestry that all seven merge commits are now on main at b24b266, with test and security green.

CUTOVER REHEARSED AND REFUSED. The promote CLI has no build-only mode, so the rehearsal ran the real verb against a scratch config root, then re-ran it under an overridden HOME so the registered $HOME/.claude hook paths mapped into the candidate. That second run isolated 2 real failures out of 27; the other 24 were scratch artifacts. F1: scripts/config/paths.mjs PROMOTED_ENTRIES still lists notes, stale against 0276, so coverage validation rejects every candidate release permanently. F2: validate.mjs syntaxCheckFor dispatches on file extension, so .claude/hooks/secret-scanner.sh - which is Python behind a .sh name - gets bash -n and fails, rejecting every candidate permanently. F3, non-blocking: git archive puts .claude/settings.json inside the release, which SPEC section 6 excludes entirely. F1 is the stale SPEC section 2 layout coded into a constant, and it is a gap in the U2 dispatch prompt, which never told the implementer that notes had been declassified as an entry link.

STOPPED SHORT OF THE SWAP for three independent reasons, any one sufficient: the two blocking defects above, two other claude CLI sessions running (pids 19750 and 48947) against SPEC section 9's unmeasured swap visibility, and this session reaching its compaction threshold. Local main is stale at 0451220, so the rehearsal had to pass --ref origin/main; a real cutover must fast-forward first or repeat that flag.