The session ran the fresh independent review c10 has been waiting for, got BLOCK, and then went up a level: it diagnosed WHY five rounds have each failed the same way, derived the shape a correct invariant must have, and re-graded every invariant in the system against it. Nothing was committed, nothing was implemented, no code changed anywhere. Every lane was read-only and probe-fenced to the scratchpad. The worktree is exactly as it was found: branch feat/config-entry-link-cutover at origin/main, 14 modified plus 4 untracked, uncommitted.

VERIFIED FIRST. The orchestrator re-ran the full suite itself rather than inheriting the claim: 2159 of 2159, 0 fail, exit 0, 71s. Worktree state confirmed against the briefing. The git-diff-covers-less-than-half warning confirmed by line count: cutover.mjs 838, cutover.test.mjs 883, receipt.test.mjs 154, coverage JSON 90, all untracked.

THE REVIEW. Four parallel lanes, each with a disjoint lens, each handed the ten known residuals up front so none of them spent the round rediscovering known ground. Adversarial BLOCK, correctness BLOCK, over-strength SHIP, test-quality SHIP. The split was not disagreement: every BLOCK finding landed in an area the SHIP lanes named in their own WHAT I DID NOT CHECK sections. Consolidated 1 CRITICAL, 4 HIGH, 8 MEDIUM, 9 LOW, recorded as 0299.

The CRITICAL: corroborated (cutover.mjs:288-289) demands disk evidence only for state in {link, real}; already-linked and absent are honored with none, and firstByName makes carried records win. A journal planted once before any apply survives a legitimate apply, turns rollback into an eleven-entry no-op reporting success at exit 0, and unlinks the journal, orphaning every aside including the two real directories hooks and rules that exist nowhere else. Reproduced by executed probe.

Two of the four HIGHs are regressions round 5 introduced, both proved by running old code against new in the same scenario. repoRootErrors was wired into readReceipt, so a relocated checkout refuses cutover plan, cutover apply and promote rollback, verbs that never read the checkout; and the process.cwd() fallback at promote.mjs:492 that compensates for it promotes an unrelated repo at exit 0 with empty stderr.

THE PERMIT-SET CHECK, and why it mattered. The adversarial lane proposed a remedy. Rather than accept it, the over-strength lane was resumed and told to attack it. It reversed its own SHIP to BLOCK and proved the remedy DESTRUCTIVE: it would unlink ~/.claude/rules on the legitimate partial-apply resume the shipped suite already encodes, orphaning graphify-out content that exists nowhere else, failing that test 33 of 34. It also proved the remedy insufficient, and proved that drop-the-contradicted-record is not a fix either, because a name with no record orphans exactly as silently as a name with a false one. It then designed, built and measured a correct pair of properties. Recorded as 0300. That gate is the single most valuable process change this session made: three prior remedies in this unit were wrong, and this is the first that was attacked before being written rather than after.

THE DIAGNOSIS. The user asked why the loop keeps repeating. The mechanism is now identified and is supported by every family of invariants in the system. Each invariant states a property in its heading, then restates it in its body as an ENUMERATION of sites, states or variables. The implementer implements the body, because the body is concrete. The enumeration is always narrower than the property. The review then finds the gap, which is by construction a new defect on a path nothing named. I1 is the control case: it is the only cutover invariant whose body quantifies over a closed set (four syscalls) and the only one that survived a dedicated attack lane. I3, I6 and I7 all enumerate, and all three were violated. The smoking gun is that I4 of the same contract cites the real code constant ENTRY_STATES while I3 hand-lists three of its four members, dropping already-linked, which became the CRITICAL.

THE REGRADE. Two dedicated lanes re-graded every invariant in the system against a four-part shape, writing full output to artifacts rather than into context.

Cutover I1-I7: NOTHING CONFORMED. Seven became ten after four splits, plus two new, minus four sections that were never invariants. Written to artifacts/2026-08-09-cutover-invariants-v2.md. The biggest change is I7a inverting from deny-list to allow-list on the subprocess environment, because the environment-variable universe is OPEN, so a deny-list has no closed domain and no complete witness can exist for it. The correct allow-list implementation already exists in a sibling file at validate.mjs:313-316 and release.mjs never adopted it. Three defects four review lanes had missed fell out purely from asking what the closed set is: local/ holding the rollback bootstrap is in neither hook's guarded set; ln is absent from guardverb while manifest.mjs:30-32 actively withdraws Bash(ln -sfn:*); repoRootErrors is called without configRoot at release.mjs:62 and :80.

System-wide: written to artifacts/2026-08-09-invariant-regrade-system.md. Families B1-B6 sandbox, M1-M6 method, G1-G5 gate goals, DoD1-4, A1-A6, the CLAUDE.md globals, and mitosis.js:4582. Three M-invariants deleted as procedures rather than properties; the checker already conceded this with INERT_BARRED_IDS = ['M3','M4','M5']. A1-A6 govern a repo with no registry and no checker, six invariants with zero enforcement. The CLAUDE.md globals and the green-branch invariant are rules, not invariants, and were deleted from the registry with a stated home. One live contradiction surfaced: mitosis.js:4582 declares a published PR branch frozen while saga.mjs:61-66 permits --force-with-lease on exactly that ref. B4 is the disease caught in the act and not yet exploited: workflow-sandbox.mjs:70 declares POLICY_LISTS with four names and B4 hand-lists three, omitting BOUND_DENIALS.

THE REGISTRY FINDING, which reframes all five rounds. The invariant registry is a COVERAGE tracker, not a witness. REGISTRY_FIELDS is ['id','statement','source'] with no witness slot, and rowErrors only checks that check is a non-empty string. The checker never evaluates any property. What is genuinely closed is the id set, so a PR cannot skip an id, and that is worth keeping. But the cases behind every verdict come from a human's memory, which the spec itself concedes at two-track-invariant-plan.md:124. So for five rounds there were two green signals, CI and registry, and neither could ever have caught the CRITICAL.

WHAT DID NOT HAPPEN. No commit, no PR, no merge, no 0281 rehearsal, no 0292 swap. No code was changed anywhere. The registry was not rewritten and no witness was implemented; this was a documents exercise producing a corrected map, not a corrected system. Nothing under ~/.claude was written and the primary checkout was never entered for anything but reads. Context ran to the ceiling, which is why the regrade lanes wrote to disk and returned compact verdicts.