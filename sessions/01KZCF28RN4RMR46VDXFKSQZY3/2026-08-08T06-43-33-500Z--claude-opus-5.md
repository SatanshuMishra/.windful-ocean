ADDENDUM, written after the hand-off: round 3 finished. Its report exists at artifacts/2026-08-08-cutover-round3-report.md, so the next_step's "if absent, re-dispatch" branch does NOT apply. Read the report and go straight to the re-review.

Round 3 claims all four restated invariants closed. npm test in the worktree: 2116 pass, 0 fail, against a 2107 baseline, so 9 new assertions, all of which it reports as red before the fix. Two quoted: "a grant taken out of live must be reported, never silently dropped" and "a hand-set target must survive a rollback".

The finding worth carrying, because it corrects the invariant I wrote rather than merely satisfying it: rename, symlink and unlink do NOT follow the final path component, while copyFile and writeFile DO. So INV-4' as I stated it, resolve both sides through symlinks everywhere, would have refused every real deployment, because pre-cutover entries are legitimately symlinked outside the config root and that is exactly the state the verb exists to change. Round 3 split containment by write kind instead: container-resolution for link writes, full resolution for content writes, dispatched through one table that fails closed on an unknown kind. cutover.mjs no longer imports isInside at all. It enumerated all 14 isInside call sites across capture, validate, install-bootstrap and promote first and left their semantics unchanged, so those callers hold by construction.

Deliberate non-closures it reported, none of which I have reviewed: cutover.mjs is now 785 lines, under the 800 cap and past the 400 target, left unsplit because an unrequested refactor mid-fix-round is what caused earlier regressions; copyFile retains a genuine TOCTOU between check and copy that COPYFILE_EXCL cannot close without breaking the byte-identical-overwrite case; install-bootstrap.mjs:50,53 stay lexical because they gate a module closure rather than a write; no concurrency lock, replaced by a wx flag on the journal staging write so a race fails loudly; and the second-rollback refusal was kept after grep found no idempotence claim at the verb level to preserve.

State verified again at this point: live ~/.claude untouched, with no releases/, current, LIVE, CUTOVER or local/notes, hooks/ and rules/ still real directories and settings.json still a real file. The worktree holds 9 modified and 3 untracked files including the docket, all uncommitted. Primary checkout still shows only the two files belonging to the invariant-coverage-tax thread.

STILL REQUIRED BEFORE ANY LIVE WRITE, unchanged: an independent re-review of the full diff returning SHIP, since every round so far has been reviewed and two of three returned BLOCK, and round 3 is the first that is entirely unreviewed. Then commit, one PR via pr.mjs, merge, install-bootstrap re-run, the 0281 rehearsal against merged main, and the swap alone per 0292.