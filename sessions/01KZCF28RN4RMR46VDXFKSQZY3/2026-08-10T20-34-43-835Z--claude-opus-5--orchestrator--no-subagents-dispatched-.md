SHIPPED. The cutover unit is on main. Pushed feat/config-entry-link-cutover at 700878d from its own worktree (.claude/worktrees/spec-a-cutover), opened PR #60 through node .claude/lib/git/pr.mjs pr-create, and the human merged it. Merge commit bdbb01a; verified by `git merge-base --is-ancestor 700878d origin/main`, which passes, so the unit landed whole rather than squashed.

VERIFIED BEFORE ACTING, because the brief is a hint. Fresh fetch showed the branch clean, 22 ahead and 0 behind origin/main, exactly as the brief claimed. The branch lives in a dedicated worktree, so nothing had to switch branches in the primary checkout — the primary checkout never left feat/invariant-inert-registry and stayed clean throughout, which keeps the live-symlink rail intact. pr.mjs is byte-identical on both branches, and SatanshuMishra/.windful-ocean passes validateRepoIdentity despite the leading dot (checked directly, since a leading-dot repo name is exactly the shape a naive owner/repo regex rejects).

THE PR BODY. Carried the round-3 review's prescribed disclosures verbatim: the planted-.cutover-symlink apply risk as --risk, and its two --not-verified lines. Added a third --not-verified line, "0281 scratch-root promote rehearsal at this sha - not run at open", because the approved order runs the rehearsal after the PR and a body is never rewritten, so silence would have read as an unstated pass. Four --verified lines, each from a check the previous session actually ran. --changed-lines 4476 fires the tool's own SIZE warning in the body, which is the honest rendering for a 17-file unit.

A LEAD FOR THE REHEARSAL, verified not assumed. The live ~/.claude/local/ bootstrap is the Aug 7 copy and holds six modules: converge, paths, promote, receipt, release, validate. The merged promote.mjs imports ./manifest.mjs (scripts/config/promote.mjs:32), which is not among them. The bootstrap on disk would therefore throw on first run. The rehearsal should install the bootstrap the same way the live swap will, so this surfaces there rather than mid-swap.

NOT DONE, deliberately. No rehearsal, no live swap, no entry link moved. Live config verified untouched: no LIVE receipt, no current link, no releases/ directory in ~/.claude, and CLAUDE.md still symlinked into the primary checkout. The session stopped here on context pressure (81%) rather than starting the rehearsal with a quarter tank, which is the boundary at which this thread has lost three orchestration attempts.

No subagents were dispatched this session, so the run_in_background failure mode did not arise.