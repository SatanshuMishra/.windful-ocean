Thread opened and the rename executed end to end by a dispatched orchestrator subagent working in the isolated worktree .claude/worktrees/lib-mitosis-rename. Primary checkout never left main and finished clean, verified directly.

SHIPPED. Two commits on refactor/lib-mitosis-rename. 709ff7f moves lib/superpowers-parallel to lib/mitosis, renames resolve-superpowers.mjs to superpowers-prompts.mjs and parallel-plan-execution.js to mitosis-execute.js including its meta.name, changes all load-bearing strings in that same commit, and deletes the dead .gitignore line: 109 files, 47 insertions, 48 deletions. 693d54c merges .claude/docs/superpowers into .claude/docs specs and plans: 58 files, 56 renames at 100 percent similarity. PR #64 opened through the centralized pr-create tool and left unmerged.

THE CENSUS RESULT. 548 raw hits across 80 files resolved to 31 load-bearing lines across 18 files with zero unclassified - not the 17 lines across 12 files the prior audit recorded. All 17 cross-check sites were present and were fixed. The 14 additions include two that would have shipped broken: mitosis.js:21 ENGINE_PATH, the path mitosis.js actually executes, and block-inline-engine.mjs:5,13, where the direct-invocation guard matched the engine both by ENGINE_NAME and by a regex on the workflow filename. Renaming the workflow without that guard would have left it matching a name nothing uses - a silent, total loss of enforcement with every test still green. Three of its fixtures are case-variants invisible to a case-sensitive grep. This is the finding that justifies demoting the pinned list to a cross-check under 0344.

VERIFIED BY EXECUTION. npm test 2188 of 2188, equal to a pre-edit baseline measured on the same worktree, which is what proves the renamed glob resolves. Four import smoke tests OK. Phase-parity gate exit 0 with 13 phases and 0 dead. The guard exercised live on 6 payloads including uppercase and backslash forms. The drift-check hook run under a shimmed HOME in both directions. The agent's first guard run reported a false gap because it passed key path where the guard reads scriptPath; it read the source and re-ran rather than reporting either result.

DISCLOSED AS NOT RUN. CI on the branch, live LIB_DIR resolution, and an end-to-end engine run.

ONE REPORTING ERROR, CAUGHT ON REVIEW. The agent reported 693d54c as 0 insertions and 0 deletions and offered that as proof of a pure move. It is 2 and 2 - the cross-boundary pointer edits in rules/common/agent-roster.md and skills/report/SKILL.md that its own census had classified as load-bearing. The work is correct; the figure was not. Independently re-verified the commit list, the guard diff, the surviving references and the clean primary checkout rather than accepting the report as given.

LEFT DELIBERATELY. block-destructive-bash.test.mjs:280-281 keeps the superpowers-parallel string: it asserts a superseded path carries no gate exemption, and renaming it would convert a dead-path regression test into a claim about the live canonical directory. Phase 2 subdivision and the root-level docs/superpowers reconciliation stay undone per 0340.

FAILED OR INCOMPLETE. c5 does not close. Behaviour-neutrality is proven for everything provable inside the repository, but ~/.claude/lib/mitosis does not exist yet, so live LIB_DIR resolution is unproven until cutover. That is the same fact as 0345's deploy-ordering trap.