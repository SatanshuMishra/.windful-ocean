CORRECTION, discovered after the debrief was written: the promotion did NOT survive. The Stop hook's global config convergence reverted it within one turn. Its own output: "live is 59fc308..., main is 30f4b900...; live now resolves to 30f4b900... (was 59fc308...); applied the reconciled settings to /Users/satanshumishra/.claude/settings.json". Verified immediately after: ~/.claude/hooks/checkpoint-worktree.mjs and trash-rm.mjs are gone again and the live PreToolUse matchers are back to the pre-promotion set (Edit|Write -> block-env-edits, secret-scanner, protect-claude-config; Bash -> block-destructive-bash, pre-commit-scoped-verify).

STRUCTURAL CONSEQUENCE: convergence pins the live install to main on every turn. All of this thread's work lives on docs/max-autonomy-permission-spec, which has not reached main, so ANY promotion from this branch is undone automatically. Promotion is not a durable act from a feature branch. c16 is flipped back to not-done for that reason: the registration is merged and permanent, but the install half cannot hold. This also means the earlier three "promote did nothing" rounds may have been partly convergence undoing the work rather than promote no-opping - the --ref no-op was real and reproducible, but convergence is a second, independent cause of the same symptom and the two were never separated.

The convergence run also flagged two classification gaps worth reading: model (inert-repo-declaration) - the repo declares a live-owned key so promotion never applies its value; and sandbox (unclassified) - the manifest classifies no owner for this key, so live wins and it is held for classification. The sandbox one matters directly, because the sandbox block is what Layer 0 is made of and it is currently unowned by the manifest.