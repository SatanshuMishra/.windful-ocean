Wave 0 ran to completion and the SPEC was amended. Nothing was implemented; that was deliberate, because SPEC section 8 forbids implementing any step before its gating experiment runs.

SHIPPED: commit 582d8dc on docs/max-autonomy-permission-spec, pushed, PR #85 opened against main (https://github.com/SatanshuMishra/.windful-ocean/pull/85). SPEC grew 303 to 368 lines with 21 new measured facts; a 191-line companion findings doc records U1-U7 with method, raw observation, verdict and consequence per item.

EXPERIMENTS: U1 verified (a hook allow does suppress the auto-mode classifier). U2 refuted the SPEC's step 3 - a broad Bash star rule is silently discarded in auto mode and the command then falls through to the classifier and is blocked, while a narrow prefix both executes and skips the classifier; the rejection is visible only at debug level. U3 first refuted Layer 0 (the sandbox blocks loopback, unreachable by allowlist, and on defaults a denied command is silently re-run unsandboxed and reported as succeeded) then a follow-up rescued it with sandbox.network.allowLocalBinding. U4 verified (tmutil localsnapshot, 0.322s, no privilege prompt, but snapshots are purgeable). U5 verified (hook input rewriting turns rm into trash; the file was recovered intact). U6 verified. U7 answered: validate.mjs rejects on ten checks but is a syntax gate only and never executes hook logic. M17 was refuted on the argv path - no acceptance dialog appeared and nothing was refused.

D6 RE-MEASURED: the superrepo temp-index checkpoint captures nested repositories as empty gitlinks, so the eight worktrees holding the actual parallel agent work are captured not at all, at 795ms. The worktree-root variant captures 31.3 MiB of real content with zero gitlinks at 259ms. Plain add -A silently drops tracked-but-ignored files, reproduced on a controlled scratch repo, making --force a correctness requirement.

DISCOVERED, not in the SPEC: the settings-promotion ownership model, with two live self-destruct paths (a PR omitting the hooks key strips every hook from live including converge itself, and passes validation; permissions.deny has the same shape). Rollback undoes itself at the next converge tick. A merged PR does not reach live until a human advances local main, and nothing fetches. The --settings flag accepts inline JSON, making argv an ungoverned permission-injection channel at a precedence above project and local settings.

FAILED / NOT DONE: no implementation started. The unattended launcher and the plugin disable are deliberately held pending direct user confirmation after two harness security warnings fired on the permission-mode experiment agent; the user was away for all of Wave 0 and never answered the first. Experimentation in that class stopped entirely once all three probes returned verified.

LEFT BEHIND: 58 refs under refs/checkpoint-experiment/ in the repo from the benchmarks, about 9 MB. Deleting the refs is trivial but reclaiming the space needs gc --prune, which is guarded under D2, so it was not run. The amendment also flagged two contradictions it declined to fix as outside its brief - M7's "repo HEAD" wording, and whether R3's flag and section 5's flag are the same mechanism.