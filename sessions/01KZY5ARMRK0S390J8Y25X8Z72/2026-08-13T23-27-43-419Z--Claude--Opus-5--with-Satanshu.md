Session goal was "fix the release builder so config verify goes green". The builder turned out not to be broken, and a much larger live-config failure surfaced instead.

DISPROVED THE STANDING DIAGNOSIS. The previous brief asserted the release builder drops tracked top-level rules files. It does not. Three independent checks: (1) `git archive --format=tar 476ec59d .claude` extracted to a scratch dir contains `rules/context7.md`; (2) `buildRelease({configRoot: <scratch>, repoRoot, sha})` produces a release containing it; (3) `diff -rq <scratch build> <live release>` differs in EXACTLY the two verify findings and nothing else. There is no .gitattributes and export-ignore is unspecified. Of the five release dirs under ~/.claude/releases, only the live one lost the file. Conclusion: both findings are POST-BUILD mutations of the release tree, not build defects. Any future fix aimed at the archive step is aimed at nothing.

FINDING 2 ROOT-CAUSED AND FIXED. `hooks/agent-ledger/__pycache__/_ledger.cpython-314.pyc` is CPython's ordinary bytecode cache. Three hooks do `import _ledger as L` (roster-index-gen.py:9, agent-permission-capture.py:7, agent-fallback-capture.py:9). Hooks are registered as `$HOME/.claude/hooks/...`; that symlink resolves through `current/hooks` into `releases/<sha>/hooks`, so Python caches inside the release tree. `__pycache__/` is gitignored (.claude/.gitignore:39) so it can never be declared, which is why verify reports it as undeclared. Only imported modules are cached, never a script run as __main__ - hence exactly one .pyc. Fixed by adding PYTHONDONTWRITEBYTECODE=1 to the tracked settings env block; `env` is a declared manifest key (scripts/config/manifest.mjs:6) so it promotes normally. Receipt: with the var unset a __pycache__ dir appears, with it set none does. Verified already live in-session (PYTHONDONTWRITEBYTECODE=1 present in the shell env, sys.dont_write_bytecode True) because project settings env applies immediately. Committed as 8377436 on branch fix/release-tree-immutability, unsigned via --no-gpg-sign.

FINDING 1 NOT RESOLVED - and it exposed an active destructive process. `~/.claude` was missing six of the ten PROMOTED_ENTRIES: hooks, rules, skills, agents, workflows, CLAUDE.md. The four that survive (lib, docs, sounds, keybindings.json) still point at current/ and are dated Aug 10 15:55. Because every hook in live settings.json is registered under $HOME/.claude/hooks/, all gates were dead - protect-claude-config.sh, block-destructive-bash.sh, secret-scanner.sh, trash-rm.mjs, block-inline-engine.mjs. They fail OPEN silently for Bash (no error surfaced, calls proceeded); only the Agent tool hard-fails, which is how this was discovered. User restored the six symlinks at 17:12. Confirmed all ten present and gate scripts readable at ~17:13. Then two Agent dispatches were issued in one message: the first succeeded, the SECOND failed on the missing hook, and a re-check at 17:14:49 showed the same six gone again with the same four intact. So the destruction is reproducible and keyed to spawning a subagent, not historical damage.

HYPOTHESES TESTED AND KILLED. checkpoint-worktree.mjs only calls takeCheckpoint, and checkpoint.mjs:68 does `git add -A --force` against a mkdtemp temp index - it stages, it never touches a working tree. trash-rm.mjs only rewrites rm command strings and ~/.Trash is empty. The SessionStart shell hooks (superpowers-cmd-hook-heal.sh, superpowers-drift-check.sh, dep-audit.sh, graphify-provision.sh, ledger-staleness-scan.sh, plugin-update-check.sh) contain only temp-file cleanup, nothing touching ~/.claude entries. GNU Stow is confirmed present (.stow-local-ignore) and .claude/hooks/CLAUDE.md still documents the OLD topology - "~/.claude/hooks/block-destructive-bash.sh is a symlink into this repo file" - which contradicts the release model, but install_config.sh and update_config.sh are referenced only in docs and stale worktrees, so nothing invokes stow automatically. Two competing installation models remain a live lead for the six-vs-four split, unproven. Ruled out sandbox path-hiding: denied writes return EPERM while the missing entries return ENOENT, and the names are absent from the parent readdir, so they are genuinely deleted.

STILL RUNNING AT WRITE TIME. Subagent a6d29e1dbe3ccc8a5 (debugger, read-only) has the full live-reproduction evidence and is chasing the deleter. It is the critical path. It was told explicitly not to reproduce by spawning a subagent and not to write to ~/.claude. Resume it with SendMessage to a6d29e1dbe3ccc8a5; there is no shell to kill.

NOT DONE, AND WHY. The structural half of the fix - extracting the tree-vs-directory comparison so buildRelease stops trusting an existing release dir, plus closing the promote.mjs:264 `unchanged` short-circuit that makes a self-healing builder unreachable in exactly the state that needs it - was deliberately NOT attempted. Context was down to roughly ten percent, the Agent tool is destructive so it could not be delegated, and rushing a refactor across release.mjs/verify.mjs/integrity extraction at that budget is the precise pattern that produced the earlier fix-round regressions. Repairing the live release was also deliberately skipped: anything rebuilt is destroyed again on the next subagent spawn, so the repair would read as success and silently revert.

WHY THIS THREAD KEEPS REGRESSING (asked directly, recorded for the next session). Three structural causes. (1) The system under change is the system doing the checking - the hooks that guard the work live in the tree the work promotes, so a defect deletes the gate that would catch the next one. (2) Silent success is the default failure mode here - promote.mjs with no --ref exits 0 having done nothing, verify through a pipe reports tail's status, a missing hook fails open with no error; each converts a non-event into a completed event, so work is marked done on receipts never earned and the breakage surfaces rounds later misattributed. (3) Two installation topologies claim the same paths. Underneath all three: fix rounds have targeted findings rather than invariants, patching the observed path and leaving the defect class live everywhere unobserved.