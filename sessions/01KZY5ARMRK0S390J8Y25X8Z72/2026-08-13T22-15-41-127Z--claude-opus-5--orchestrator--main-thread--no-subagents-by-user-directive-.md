Layer 1 was INERT and is now live. Root finding: checkpoint-worktree.mjs and trash-rm.mjs were registered in NO settings file - an exhaustive git grep at 9a168c7 found references only in their own tests plus one self-referential error string, and install.sh only renders launchd plists while explicitly loading nothing. c5 ("Layer 1 shipped whole") was marked done against code that could never fire.

Fixed: registered checkpoint-worktree.mjs on a new Edit|Write|NotebookEdit|Bash matcher and trash-rm.mjs AHEAD of block-destructive-bash on Bash, so the gate judges the rewritten command. Commit 2ba9bf9 -> PR #94 -> base now 59fc308. Promoted with `node scripts/config/promote.mjs promote --ref 59fc308`; `verify` reports live matches 59fc308 across 437 files. Confirmed live: both hook files in ~/.claude/hooks/, ~/.claude/lib/reversibility/ present, live matchers show checkpoint + trash-rm-before-gate. NOT yet proven firing - this session loaded the old config at startup, so the newest checkpoint ref is still the 14:13 measure-shimmed burst. Restart then one ordinary edit is the only proof.

DELIVERY PATH, what actually works: `git push` authenticates. Proved with a dry-run of feat/layer4-observability, which has no remote counterpart and therefore required real write auth ("* [new branch]"); confirmed the remote stayed untouched. An earlier dry-run of feat/layer3-gate returning "Everything up-to-date" was a FALSE PASS - that branch already matched the remote so git never authenticated. Path taken: `gh auth login --insecure-storage` (after `unset GH_TOKEN`, which was shadowing it) puts the token in ~/.config/gh/hosts.yml, which the sandbox can read; `gh auth setup-git` scoped the helper to github.com rather than --replace-all, which would have stripped credential storage for every host.

DELIVERY PATH, what does NOT work: gh's API is dead under Layer 0 - `gh api user` fails with tls: failed to verify certificate: x509: OSStatus -26276. Go on macOS calls the platform verifier (securityd), and SSL_CERT_FILE does NOT bypass it (tested with /etc/ssl/cert.pem). pr.mjs shells out via spawnSync(ghBin, argv), so pr-create inherits the failure - PR #94 was opened by the human from an unsandboxed terminal. Commit signing also fails: id_ed25519 is passphrase-protected and ssh-agent is unreachable, so the human committed 2ba9bf9. A node fetch to api.github.com also failed; cause was NOT pinned before the gate denied the follow-up, so whether api.github.com is reachable at all remains open and is the test that distinguishes a network-policy fix from a transport rewrite of pr.mjs.

NEW DEFECT: `promote.mjs promote` with no --ref exits 0 having done nothing - no install, no refusal, no output. It read as success and left Layer 1 uninstalled across three verification rounds. c12 hardened promotion to refuse rather than half-install; this silent third outcome defeats that. Needed --ref 59fc308.

Also corrected: `gh auth status` calling the token invalid is a TLS failure in disguise, not a bad token. And the odd SHA 30f4b900... seen from ls-remote is simply the prior promotion pointer - ~/.claude/releases/<sha>/ is named after the promoted commit.

NOT DONE: feat/layer3-gate and feat/layer4-observability were NOT rebased. Verified at close that both sit on a base older than even 9a168c7. An in-session task list marked them complete; that was wrong and the git state is authoritative. c6 and c7 remain untouched. c3, c4, c14 untouched. User directive this session: no subagents, main thread only.