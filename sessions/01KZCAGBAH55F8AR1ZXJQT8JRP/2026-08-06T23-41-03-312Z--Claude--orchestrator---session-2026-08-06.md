c5 started and is BLOCKED on unsound glob data. The checker mechanism was built and passes its receipts; the registry data it would evaluate does not survive review.

SHIPPED (uncommitted, working tree on feat/invariant-inert-registry): the inert-basis evaluator in scripts/invariant-coverage-check.mjs plus 13 tests in .claude/lib/superpowers-parallel/tests/invariant-coverage-check.test.mjs. Implements 0273 as specified: closed-shape inert_when, hand-rolled glob matcher rejecting unsupported syntax, M3/M4/M5 structurally barred, NEGATIVE polarity (basis holds only if EVERY changed path matches), deletions counted via a separate --no-renames diff, checker-written check text, --write flag, scoped-only proof, loud failure when no base resolves. Receipts all executed: 13 red against main's checker then 31/31 green; polarity mutation (every->some) turns 2 named tests red; emptying the barred set turns 1 red; npm test 1949/1950. Saved as a patch outside the repo because the tree was switched out from under it mid-session.

COLLISION: a concurrent session (thread spec-a-implementation-config-staging-live-promotion) created feat/invariant-inert-registry, switched the shared checkout onto it, and committed the registry half at 369490f (pushed). It correctly refused to open a PR, reasoning that inert_when data on main whose checker never reads it is a dormant basis. Concur, and my earlier "the halves are independently landable" was wrong.

BLOCKED BY REVIEW: the glob audit returned BLOCK on BOTH tracks. G sets declare four executable test files inert while G quantifies over any agent-issued command. B sets fall two ways: node --test runs files in parallel (verified empirically, Node v26.4.0) while B oracles read their subjects from disk at run time, so an in-set test file that writes can corrupt a concurrent census; and block-destructive-bash.sh is declared inert for all six B ids yet runs on every Bash call with the working tree as the live gate. The auditor retracted its own earlier clearance of the B sets.

Two checker reviews (code-reviewer for correctness, security-reviewer for gate neutralization) were dispatched and were still in flight at hand-off. Their findings did not reach the ledger and must be re-run next session against the saved patch.

FAILED/NOT DONE: no commit of the checker, no coverage entry rewrite, no PR. Deliberate — shipping would have published a gate with a proven hole.

Durable artifacts written outside the repo, under ~/.claude/projects/-Users-satanshumishra-Documents-DevLabs--windful-ocean/artifacts/: 2026-08-06-invariant-threat-surface-map.md (per-invariant threat surfaces with citations), 2026-08-06-inert-glob-audit-BLOCK.md (the full audit, the fix, and the 8 coverage rows that go wrong), 2026-08-06-c5-checker-mechanism.patch (the evaluator, 740 lines).

Incidental: npm test on main is 1936/1 with one environmental failure - ~/.claude/settings.json is a real file, not a symlink into the repo, so protect-claude-config's guard is inert and a permanently red test will mask real regressions. Filed as a task chip, not fixed. Also: cutting a branch off main removed .claude/skills/context7-mcp from the live symlinked skill set; it exists only on chore/config-drift.