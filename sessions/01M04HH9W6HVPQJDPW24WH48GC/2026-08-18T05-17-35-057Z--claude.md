WAVES 4 AND 5 ARE LIVE. The thirteen-agent roster is on main and in force.

WHAT SHIPPED. Decision 0551 applied end to end: agent specs are JSON data read with readFileSync plus JSON.parse, the dynamic import is gone, and all thirteen specs converted. PR 204 went fully green including G14 after five surviving mutants were killed with per-mutant evidence. Waves 4 and 5 opened as PRs 205-208 stacked on the substrate.

THE STACKED-MERGE TRAP FIRED. All five PRs reported MERGED but only 204 reached main: the four children were merged 10 to 52 seconds after 204, while the base branch still existed, so GitHub never retargeted them and they merged into a branch that had just gone dead. A second fault sat underneath - 205 and 206 merged at stale heads because GitHub had not registered their final pushes, leaving the two pointer-scoping fixes only on their own branches. Recovered as decision 0560: one integration PR 210 carrying all four units plus both missing fixes. G11 then blocked 210 on 21 apparently-deleted test files, which was a stale base - PR 209 added them to main after the branch point, and the enforcer compares base tip to head tip. Merging main in cleared it.

VERIFIED FROM THE PRIMARY CHECKOUT, at c8c4cad0, in sync with origin/main. agent-generate --check exit 0 over 13 specs with every body matching source. determinism, phase-parity, dispatchable-agent-schema-capable and name-integrity all exit 0 - the first three had never been honestly runnable locally because from a worktree they census the primary checkout. Mitosis suite exit 0, 2289 tests, 0 fail, 1 skipped, 1 todo. Retirement census exit 41 with 19 name+line pairs across 18 distinct sites, reproducing the recorded pre-wave-6 baseline exactly. Roster is 22 files: the thirteen retained plus the nine still awaiting U7.1.

A MEASURED CONFIRMATION OF THE VACUITY WATCH-OUT. dispatchable-agent-schema-capable inspects only FIVE agents out of 22 definitions - code-reviewer, codebase-analyst, implementer, security-reviewer, test-engineer - because it only sees names appearing as engine string literals. It passed U4.1's four new agents by never looking at them, exactly as decision 0547 predicted. codebase-analyst is in that set and is U6.1's literal, so repointing it swaps investigator in.

C5 REWRITTEN, NOT EXTENDED. The user asked for the Lead-routing measurement to become a requirement of this thread without adding scope ahead of the work in flight. c5 already owned it - its first clause read "dispatch routes to the intended agents" - but that clause had no failing state and could never be graded. Decision 0561 rewrites c5 so the Lead share is measured against a threshold pinned before the window opens, and so the window opens only after U7.1 ships, because a log gathered while both rosters are live cannot distinguish a Lead not chosen from a retired agent still available. The criteria list stayed at seven. The prompting-side observation behind it: across roughly a dozen dispatches this session the main thread reached for implementer, test-engineer and codebase-analyst every time and never once for a Lead, even though the units were precisely delivery-lead's description.

STILL OPEN. U5.2's stated six-reference review is recorded in the U5.2 agent's return but was not re-confirmed by this session's verifier; it is named unverified-reasoned rather than claimed. Four local branches and their worktrees survived the remote deletion and are safe to remove now that content is confirmed on main.