0493 shipped and verified in force.

PR 153 (fix/capability-blocked-detector) merged to main as b306a44f. Content arrival was ASSERTED, not inferred from the MERGED label: `git merge-base --is-ancestor 490ba085 origin/main` returns true.

Activation confirmed at the live surface. ~/.claude/hooks resolves into the primary checkout's working tree; the live agent-run-analyzer.mjs is byte-identical to the origin/main blob (sha256 1f8eabd0...), and detectCapabilityBlocked at line 64 reads `d.last_assistant_message` with a fallback to `finalAssistantText(d.agent_transcript_path)`. The parent-transcript scan is absent.

Local main was 3 commits behind and still carried the PRE-FIX analyzer (sha256 fb01fde2...), so the fix was live only by virtue of the feature branch being checked out. The user fast-forwarded with `git fetch origin main:main`; that command printed nothing to stdout or stderr, so the ref was re-read directly rather than assumed. Local main is now b306a44f and its analyzer blob hashes to 1f8eabd0..., closing the regression path.

Branch cleanup: the remote branch had NOT been auto-deleted on merge and was still live at 490ba085 — the ref-survival trap. Checked for stacked children first (`gh pr list --base fix/capability-blocked-detector --state open` returned []), so nothing was orphaned. Primary checkout switched to main, a zero-file change since `git diff fix/capability-blocked-detector main` was empty and 490ba085 is an ancestor of main. Branch then deleted locally with `-d` (merged-safe, refuses an unmerged branch) and on origin. Three refs verified GONE: refs/heads local, a live `ls-remote` against origin, and the stale refs/remotes tracking ref. Worktree holds: 0. Untracked corrections.png and hero.png preserved; live analyzer still 1f8eabd0... after the switch.

No agent, rule, hook or skill file was edited this session, so the c3 no-mutation boundary holds.