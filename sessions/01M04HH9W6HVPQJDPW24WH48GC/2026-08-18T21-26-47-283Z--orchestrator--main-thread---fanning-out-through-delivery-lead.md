Session opened on a STALE brief: it said wave 7 was staged and uncommitted, owing npm test, commits, push and a pull request. None of that was owed. Wave 7 had already shipped as PR 212 before this session; confirmed by merge-base rather than by the MERGED label, with nine deletions gone and exactly 13 definitions on disk. npm test on main was green once duckdb was installed (brew gave v1.5.5, the version CI pins): 3059 pass, fail 0, and all 21 prior failures were traced to the absent binary rather than pattern-matched on message text.

c4 was evidenced complete: all 17 units U1.1 through U7.1 are ancestors of origin/main with content corroborated in the tree, the retirement census shows the real shape flip and a measured 22 to 13 drop, and name-integrity resolves 31 references with zero dangling.

c5 did NOT close, and that is the session's real result. Its instrument was broken in the exact place it measures. The audit classified dispatches by transcript presence, but transcript presence is an event-phase artifact - zero percent on start rows, 100 percent on stop rows - so all 295 real start rows were misclassified, dispatch_starts read 0, and all 13 roster agents read never-observed while a sibling metric reported 88.1 percent coverage over an empty population. The suite was green throughout, against fixtures in a shape the writer cannot emit.

Two pull requests fixed it. 217 (MERGED, content confirmed on origin/main) resolved population at dispatch grain. 218 (OPEN, awaiting a human merge) swapped the predicate to sidecar presence via depth, moved two row-wise questions to dispatch grain, and shipped with all three inertness mutations biting - predicate restored turns red, grain reverted turns red 3 !== 2, max reverted to min turns red 1 !== 2 - plus an observed census halt at exit 6 with zero bytes of stdout. Final suite 3107 tests, fail 0.

WHAT FAILED, and why it is the point: the Lead-share sensitivity test pinned in 0591 INVERTED. Included, 164 of 270 = 60.74 percent clears the 50 percent bar; excluded, 43 of 147 = 29.25 percent does not, both at n above 20, because 111 of the 123 removed start rows are delivery-lead. The rule was pinned before the numbers existed, so c5 stays open, neither reading is preferred, and agent-id reuse becomes its own unit. Attribution (100 percent at both grains) and the closed census with its observed halt ARE met and are recorded so the successor unit inherits them.

Two review rounds each found a real defect that would otherwise have shipped: an emitted population_note describing the predicate its own diff had just deleted, and a grain conversion with no test that could turn red. The second was an acceptance item declared in 0590, so it was tested rather than disclaimed - an acceptance item covered by a disclaimer is waived, not met.

Process deviation worth knowing: release-engineer re-ran all three mutations and the census halt before writing the Verified lines, unasked, on its own standing instruction. Every result matched, so no second error source appeared, but the PR honesty rule and delegation discipline point opposite ways here and the collision is real.