PR 154 merged and verified in force; skills-preload mechanism settled.

MERGE. PR 154 (fix/dead-agent-refs-and-tool-lists) merged as d2677929. Content arrival ASSERTED, not inferred: `git merge-base --is-ancestor ac266fe5 origin/main` true. Local main was 3 behind and was fast-forwarded BEFORE switching the checkout - switching first would have stripped the change out of the working tree, since the symlinked live surface follows the checkout. Verified content-identical before switching (`git diff HEAD origin/main` empty) so the three in-flight agents reading .claude/agents/ were not disturbed. Live surface confirmed after the switch: 15/15 agents carry StructuredOutput, 15/15 declare an explicit tools list, zero occurrences of build-error-resolver or e2e-runner in ~/.claude/rules/. Branch deleted local + remote + tracking, all three refs verified GONE, no stacked child PR, no worktree hold.

SHIPPED IN 154: dead refs repointed rather than deleted (build-error-resolver -> debugger, e2e-runner -> test-engineer); StructuredOutput appended to the 8 agents lacking it, no tools line removed. Receipt: check red on parent with 20 failures, green on fix, inertness mutation (injected ghost-agent reference) turned it red. Census halts on unclassified rather than counting to a pin.

SKILLS PRELOAD - record 0480 CONFIRMED, verdict (a). Binary trace of 2.1.233 found the single consumption site at the subagent spawn: an agent declaring `skills:` frontmatter gets each named skill's ENTIRE SKILL.md body inlined into its opening context as an isMeta message, byte-identical in shape to what the Skill tool delivers. No Skill tool is required for that path, and no tool-gate wraps the preload block.

THE INTERLOCK IS TIGHTER THAN 0480 ASSUMED. The skill INDEX is itself gated on the Skill tool: an agent whose tools line omits Skill receives NO skill content at all - not even the name-plus-description listing. So for the 9 non-Lead agents, `skills:` frontmatter is not a supplement to an index they already had; it is their ONLY channel to any skill content.

LIVE CONSEQUENCE, current config: zero of the 15 agents list the Skill tool AND zero declare a `skills:` frontmatter field. Therefore no specialized agent can currently reach ANY skill content whatsoever. Skills are entirely invisible to the custom roster today; only the built-ins with a full tool grant see them.

COST MODEL for the SPEC: the preloaded body is inlined on EVERY dispatch, unconditionally - a continuation skips only the invoked-state registration, not the message push. Combined with the R14 result (side files are never inlined, and the preload carries an absolute "Base directory for this skill:" path), the router-plus-side-files pattern is not merely viable, it is REQUIRED: a heavy SKILL.md is an eager per-dispatch tax, and every agent already holds Read, so side files are reachable by path.

TWO FAILURE MODES to design against: a typo or unknown skill name in `skills:` logs a warning and the agent spawns WITHOUT it - silent degradation, which makes the name-integrity check that 0470 and 0480 both recommend load-bearing rather than optional. And bare-name resolution falls back to suffix matching that takes the first registry hit, so an ambiguous bare name resolves arbitrarily; fully-qualified plugin:skill names avoid it.

HONESTY LADDER on the skills finding: `fixed` on the code fact, `unverified-reasoned` on execution. Static read of the shipping binary tracing the only consumption site and the renderer it calls; no live dispatch observed. An empirical confirmation needs a temporary agent file plus explicit permission, which was withheld.

OPEN: the in-flight branch fix/delete-unenforceable-confirmation-claim is based on b306a44f, now 3 behind origin/main - receipts G8 base-freshness applies to its PR. No file overlap with 154, so no conflict.