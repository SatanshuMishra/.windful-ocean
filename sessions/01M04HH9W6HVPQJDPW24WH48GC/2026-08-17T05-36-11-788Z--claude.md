SESSION CLOSE. The architecture was approved, c3 closed, and the SPEC written.

WHAT SHIPPED. Three pull requests, all merged and each verified in force rather than trusted from a MERGED label. PR 153 (opened earlier) fixed the capability-blocked detector. PR 154 repointed two dead agent references (build-error-resolver to debugger, e2e-runner to test-engineer) and granted StructuredOutput to the 8 agents lacking it, so all 15 now carry it. PR 155 deleted the unenforceable destructive-operation confirmation claim from four rule files plus three further sites its own census found that my brief had not named. Every merge was followed by an ancestry assertion, a fast-forward BEFORE any branch switch, a live-surface grep through the ~/.claude symlinks, and branch deletion with all three refs confirmed gone.

WHAT WAS ESTABLISHED, all by testing rather than assumption. R14 confirmed: skill side files are never inlined, proven by a binary trace and by a probe with seven pre-registered falsification markers and a trap string, which the probe correctly rejected. The `skills:` frontmatter mechanism confirmed: it inlines the entire SKILL.md body at spawn with no tool gate, and the skill INDEX is itself gated on the Skill tool - so all 15 current agents, holding neither, reach no skill content at all. model and effort are absent from the SubagentStop payload, confirmed from two independent extraction points. agent_id and agent_transcript_path are REQUIRED on every payload and the observer persists neither, which is the single root of the duplicate firings, the fourfold overcount and the mis-attribution of all 16,025 rows. The confirmation rule was owned by the bash gate, not the receipts gates, and its ASK verdict had been deleted in two dated commits.

THE SWEEP, and what it cost. Every decision record was checked against live state before the SPEC was written: 8 HOLDS, 4 not-fact-dependent, 5 MOVED, 1 UNTESTED. Two of my own records were among the defects - 0495 narrated a deletion in the past tense before PR 155 had landed and cited no PR, and the sweep was right to flag it. 0488 was stale 16 hours before it was written, because it restated a remembered pin instead of re-deriving against HEAD. Both are the same failure: an intended or remembered state recorded as an established one. 0500 carries the corrections.

DECISIONS RECORDED: 0495, 0496, 0500, 0501, 0503, 0505, 0506, 0507. 0501 freezes the approved architecture and is what the SPEC cites.

THE SPEC: .claude/docs/specs/2026-08-17-agent-roster-rebuild.md. 17 units in 8 waves. Roster reality is 6 MODIFY collisions, 7 ADD, 9 DELETE - not 13 new agents - and `researcher` gets its own unit because it is the one collision that inverts a caller's contract, gaining the dispatch tool that six routing instructions never expected.

WHAT FAILED OR IS BLOCKED. The supersession of thread 01KYERC4Y2XFVQ5GFQRRV0BKTS is impossible: two malformed decision refs make transition_thread AND archive_thread both refuse, so that thread cannot be closed by any tool. Waves 4 to 7 are blocked on the mitosis engine, itself on a thread that failed its own goal. "Four backup profiles" in c4 could not be identified from any surviving artifact and needs a definition or striking. "405 worktree copies" in c4 is stale - measured 18 worktrees, 270 copies.

RISKS RETIRED THIS SESSION, with reasons: archiving the events log and repointing the five skills are no longer risks but specified units, U0.1 and U6.2. Sharpening report section 11a is moot - the transferable lesson it named is now captured in 0500 and 0501. The report-validator risk moved to thread scope, dormant unless someone edits the report.

NOTHING IS STILL RUNNING. All six background agents completed; no shells, no worktrees left behind except worktree-agent-a7014b3f8b04d580d, which is unmerged, predates this session and was deliberately not deleted.