ADDENDUM - the substrate agent's report arrived during wind-down, after the first session log was written. It changes two things recorded there.

1. The determinism blocker is now fully characterised and RULED, as decision 0551, which supersedes 0550's recommendation. 0550 proposed a declared census exception; that was written before the precise violation was known. The implementer established that a non-literal import specifier is DELIBERATE policy - determinism-lint.test.mjs:366 pins it to halt, and no other engine module does a dynamic import - and that because every file in the engine root is censused there is no legal placement for one. It refused to choose between converting the specs and relaxing the census, on the grounds that the second legislates the verification standard. That refusal is correct. RULING: specs become JSON data files read with readFileSync and JSON.parse. The dynamic import disappears rather than being permitted, because a JSON file cannot contain Date.now - structurally impossible beats merely checked, the same argument that shaped the retirement census. Cost accepted: thirteen .spec.mjs files across four completed wave branches must be converted, mechanically.

2. Two open questions are RESOLVED, both by direct probe rather than inference. Bare project-skill names DO resolve: a throwaway project with skills: [probe-skill] resolved and reported its directory, while a bogus name ran with no error and no tool use, confirming platform fact 4. So U4.1's bare skills entries are correct, and SPEC section 3's "bare names are forbidden" is about PLUGIN skills, which need the prefix to disambiguate a marketplace. That distinction should be written into section 3 so the next reader does not "fix" it. The effort dial stays unverified-reasoned: zero of 16 agent definitions use it, every effort string in the binary is the session or model dial, and the --agents JSON surface accepts effort and a bogus key identically, so that probe proves nothing. Action is the same either way - a field that silently does nothing is worse than an honest gap.

Corrected numbers: the parent commit had 19 failures, not 18. Eighteen named agent-specs; the nineteenth was a SIGTERM timing flake that passed on re-run. After the fix: 2194 tests, 2191 pass, 2 fail, both the single dynamic-import finding. Gate verbs on the substrate branch: determinism 42 with one real violation (was halting), phase-parity 0 (was halting), dispatchable-agent-schema-capable 0 (was halting), retirement-census 41 at 19 sites (verb was absent), name-integrity 0.

The implementer also corrected its own method mid-report: its first binary search used timeout, which is not installed here, and the error was swallowed by a redirect so the search never ran. Only the re-run, bounded with grep -m, is cited. Same masking class as the piping trap.

Nothing is left running. All five worktrees clean, all branches pushed.