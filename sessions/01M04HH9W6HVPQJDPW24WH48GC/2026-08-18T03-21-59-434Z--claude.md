Landed waves 4 and 5 as five open, land-ready pull requests. Nothing was merged; merging is human-gated.

APPLIED DECISION 0551. The spec store moved off dynamic import to readFileSync plus JSON.parse, SPEC_SUFFIX became .spec.json, and all thirteen wave specs converted. The determinism census went green in CI, which is the honest verdict since CI checks the branch out as its primary checkout. The implementer drove the local census with explicit worktree-scoped roots and printed what the commondir path WOULD have resolved to, making the false-green trap visible in every run, plus a falsifiability probe.

THE CONVERSION RECIPE WAS NOT WHAT THE DISPATCH ASSUMED. The specs are not plain object literals: they build sections[].body with METHOD.join over module-level const arrays, so those multi-line strings exist in no literal in the file. A textual strip would have silently produced different agent bodies while every suffix check still passed. The recipe is evaluate-then-serialize with a lossy-round-trip guard. Every branch proved inertness by an empty .claude/agents/ diff against its pre-merge tip.

PRs OPENED: 205 (U4.1, four executing agents), 206 (U4.2, three Leads), 207 (U5.1, five collision agents), 208 (U5.2, researcher as Lead). All four based on feat/agent-generator-substrate, not main, so the topology forces the substrate to land first. U5.2's six-reference review came back complete: research.md lines 7, 9 and 12 amended on-branch, lines 13 and 17 plus .claude/CLAUDE.md:13 confirmed still correct, so nothing global is stranded on a feature branch.

FOUR FALSE-GREEN CLASSES SURFACED, all with honest local reports behind them. (1) Worktree-scoped gates censusing the primary checkout on main. (2) .spec.mjs files invisible to the JSON loader, making a store assertion pass for the wrong reason - it was a false green before it was a red. (3) A host-dependent plugin manifest: three PRs failed CI on a file that exists on this laptop and not on a runner. (4) The shared session scratchpad, where four concurrent agents collided and one agent could have reported a sibling's exit code as its own; it caught this itself and re-ran everything in a private namespace. Saved to memory.

G14 CLEARED ON PR 204. Five of twelve mutants had survived. All five killed with per-mutant evidence - hand-apply the mutation, watch the suite go red, restore byte-identically. The sharpest was EXIT_DIVERGED mutating 1 to 2, which made a diverged body indistinguishable from a halt with nothing asserting the difference.

HAND-OFF STATE AT CLOSE. PRs 204, 207 and 208 fully green in CI, 16 of 16. PRs 205 and 206 carry one stale failing check each: their final pointer-scoping commits (f4699710 and a0d112bd) are on the remote and confirmed by git ls-remote, but GitHub had not registered those pushes onto the PR records after roughly sixteen minutes of polling, so no pull_request synchronize event fired and CI never ran them. Both were verified locally red-then-green under a reproduced CI condition with HOME pointed at an empty directory. That is not CI and was not claimed as such.

Runbook written to artifacts/2026-08-18-waves-4-5-merge-runbook.md outside the repo.