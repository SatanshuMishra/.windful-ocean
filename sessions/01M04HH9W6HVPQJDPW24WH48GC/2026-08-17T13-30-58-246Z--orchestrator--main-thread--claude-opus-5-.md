Built all of SPEC wave 0 and wave 1, plus two units the SPEC did not contain, as one pull request per unit dispatched to a subagent working in its own worktree so the primary checkout never left main.

SHIPPED AND MERGED. PR 163 put the SPEC itself under version control, verbatim, because U1.2 must exclude it by path and U6.2 carves it out of the docs folder, both of which assume it is tracked. PR 166 delivered U1.1, the canonical roster resolver. Both were verified as having actually landed by comparing content on origin/main rather than by reading a MERGED label, and the checkout was pulled so the merged configuration is in force rather than merely merged.

SHIPPED, AWAITING MERGE. PR 164 (U0.1) archived 47 telemetry files and 9,237,759 bytes outside the repo with a committed manifest and verifier. PR 170 (U1.2a) repointed the continuity ledger rule. PR 171 (U1.3) added the body composition generator, drift check and plugin pointer resolver, +23 tests. PR 173 (U1.1b) canonicalised the engine source roots. U1.2, the name integrity census, was never dispatched because it needs PR 170 merged first.

WHAT FAILED, AND WHY. PR 164's receipts gate is BLOCKED and could not be cleared honestly. The unit legitimately downgraded one acceptance part to unverified-reasoned, but the enforcer reads ladder tags from the pull request BODY only, a label does not satisfy it, bodies are fixed at creation by rule, and pr-create has no flag for the tag. The agent's attempt to close and reopen was denied by the permission classifier and it did not work around it. It emitted CAPABILITY-BLOCKED and stopped, which is the convention working correctly.

TWO UNITS THE SPEC DID NOT CONTAIN. U1.1b closed the identical module-relative defect in engineSourceRoots, filed above U1.1's ceiling and inserted on an explicit user decision. U1.2a repaired the continuity ledger rule, which named two skills that do not exist; without it U1.2's census would have been red on arrival and U1.2 is forbidden from repairing anything it censuses.

FINDINGS THAT CHANGE LATER WORK. U3.2's stated premise is false: SubagentStart carries no parent field, established from the shipping binary's own schemas, so its acceptance criterion is unsatisfiable as written. Seven of the observer's 22 mandated fields are unreachable from a hook. The current observer computes its flagship metrics from the parent session's transcript rather than the subagent's, and a test pins that as expected. agent_type is empty on 84 percent of real rows. Hook registration lives in two settings files and only one is version controlled, so U3.4 has a component no pull request can deliver. The SPEC contradicts itself on wave 3, blocked in section 5a and clear to proceed in section 6; decision 0507 and the user's instruction resolve it as proceeding.

TRAPS MEASURED, NOT ASSUMED. The receipts G11 gate false-positives on any branch behind main, because it diffs the current main tip against the head and reads files main gained as deletions; merging origin/main before opening each pull request clears it. The hidden-directory trap belongs to glob and ripgrep discovery, not to find. The superpowers resolver hardcodes its own plugin key regardless of the argument passed, so the SPEC's instruction to reuse it unmodified would have returned a real but wrong path.

TWO BACKGROUND CHIPS FILED for a gitleaks false positive already on main, and for the pr-create ladder-tag gap that made PR 164's gate unclearable.