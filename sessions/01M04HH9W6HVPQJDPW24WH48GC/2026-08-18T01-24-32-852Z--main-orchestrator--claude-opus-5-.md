Session goal: rewrite U6.2's acceptance, then dispatch waves 4 to 7.

SHIPPED AND MERGED. PR 203, main now 016239bc. U6.2's acceptance was withdrawn and replaced by a standalone declaration at .claude/docs/specs/2026-08-17-u62-acceptance.md, following the U3.3 precedent. Measurement found the flagged defect was two defects: the U1.2 census resolves only reference-to-definition-on-disk, so with all nine retiring definitions still present the criterion was green before the unit and after it and had no failing state; and its grammar detects 5 of 19 real reference sites, 26 percent, which the census self-attests at name-integrity-census.mjs:12. The replacement is a grammar-free retirement census over nine known literals, shipped as the retirement-census gate verb. It runs on main at exit 41 with 19 sites, and five separate inertness mutations each turn its suite red. Two more units carried the same defect family and were repaired in the same change: U6.1's inertness was conditioned on U7.1 landing two waves later, and U7.1 named the same blind census. U6.2's deliverable list was corrected - it omitted five real sites, two of them in explain-my-config, a file it never named.

RULING MADE. A twentieth site, a three-line synthetic roster fixture, was ruled out of scope on the SPEC's own terms: U7.1's acceptance already carves out test fixtures. The exclusion is tested in both directions with identical file content inside and outside a fixtures directory.

OPEN, NOT MERGED. PR 204, branch feat/agent-generator-substrate at f813cdb5. A readiness audit found waves 4 and 5 could not run as specified: no per-agent spec store existed anywhere, no driver wrote a markdown file from a spec, the drift check could not run against the real roster, and zero of the six fragments decision 0481 requires existed. One substrate unit was inserted rather than letting four units each invent the same machinery. All six fragments shipped with sourced text, none invented.

FOUR WAVE UNITS COMPLETE, PUSHED, NO PRS. feat/u41-executing-agents f6f108fd, feat/u42-leads 439fcf70, feat/u51-collision-modifies 63d2086d, feat/u52-researcher-lead a0ed8dcf. Each authored one spec file per agent and generated its bodies through the driver. All four independently chose authority-boundary over delegation-boundary with the same reasoning reached separately. All four measured their parent commit in a disposable worktree and set-differenced failing test NAMES rather than reporting a raw total; each delta was one test, the same change-detector, now owned solely by U4.1 after U4.2 reverted its competing edit.

WHAT FAILED. The substrate created agent-specs inside the engine source root without teaching determinism-lint about it, and its closed census correctly halted, taking three gates down - 18 failures on its own parent commit. Classifying agent-specs as scanned engine source took that to 2. The residue is architectural and is decision 0550: the census refuses a non-literal import, the spec store must dynamically import files found by a directory scan, and neither requirement can yield. Recommended resolution is a declared exception carrying the U3.3 DuckDB precedent. NOT APPLIED - the substrate agent was still working at wind-down and its report never arrived, so only its commits are known, not its conclusions.

FINDINGS FILED, NOT FOLDED IN. U4.1's acceptance clause that its four new agents pass dispatchable-agent-schema-capable is vacuous - that gate only inspects agents appearing as engine string literals, so it passes them by never looking. Decision 0503's pointer channel does not exist for project-local skills over 4KB. SPEC section 5b's effort dial is not expressible at all. The local greens for determinism, phase-parity and the schema gate on every wave branch are FALSE, because from a worktree those gates censused main rather than the branch.

HAZARD THAT DID NOT FIRE. canonical-config-dir resolves through git commondir, so from a linked worktree a bare generator write lands in the PRIMARY checkout - the live global roster. All four units scoped every call explicitly. Verified untouched: 15 agents, clean status, mtimes still 2026-08-16.

PROCESS NOTE FOR THE USER. Four separate agents independently filed the session-level "while auto mode is active" directive as a prompt-injection attack. It is legitimate and appears in the main system prompt, but renders adjacent to the MCP instructions block and conflicts with subagent operating instructions. No work was affected; it costs attention on every dispatch and generates false security reports. Worth relocating or scoping.

NOTHING LEFT RUNNING. Five worktrees remain under .claude/worktrees: agent-generator-substrate, u41-executing, u42-leads, u51-collisions, u52-researcher. All clean, all pushed.