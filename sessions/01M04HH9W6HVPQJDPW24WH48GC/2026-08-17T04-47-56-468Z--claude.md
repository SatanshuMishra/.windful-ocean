PR 155 merged and verified in force; the four confirmation claims are gone from the live surface.

MERGE. PR 155 merged as cf4572d2. Ancestry asserted, not inferred. Unlike 154 this was NOT a content-neutral fast-forward - 8 files changed - but all four background audits had completed, so nothing was mid-read. Fast-forwarded d2677929 -> cf4572d2.

VERIFIED ON THE LIVE SURFACE, through the ~/.claude symlinks: zero occurrences of the confirmation claim in CLAUDE.md, git-workflow.md, commits.md and branching.md. The two sentences that had to SURVIVE a partial-line deletion are intact - "never waiting to be asked." and "easy to revert when problems arise." Gate and settings byte-identical across the merge (`git diff d2677929..HEAD -- .claude/hooks .claude/settings.json` empty), confirming the prose-only boundary held.

0495 IS NOW ACCURATE. The sweep correctly flagged it as narrating a deletion that had not happened; this merge makes its outcome text true. The methodological defect it exposed stands and is recorded in 0500 - an intended state was written as accomplished, citing no PR, unlike every other executed change in the set.

CLEANUP. The branch was held by a registered worktree, which is the case where a branch delete fails while the remote survives. Sequence: worktree removed (it reported not-locked despite an earlier listing showing locked - the lock cleared when the agent finished), pruned, then branch deleted local + remote, all three refs confirmed GONE, worktree directory gone. Merged stray branch worktree-agent-aafafe099db798303 deleted.

HELD BACK: worktree-agent-a7014b3f8b04d580d is NOT merged into main and was NOT deleted. It predates this session and may hold real work. Needs a disposition.

THIRD VARIANT OF THE STALENESS TRAP, worth carrying into the architecture. Today produced three distinct levels, each of which looks identical to success from the level above: (1) MERGED does not mean the content is on the trunk - assert ancestry; (2) on the trunk does not mean in force - the ~/.claude symlinks follow the checkout, so a pull and the right branch are required; (3) in force does not mean in THIS session's context. This session's system prompt still carries the pre-deletion CLAUDE.md listing the confirmation rule as a global invariant, because that snapshot was taken at session start. The distinction that matters for the rebuild: SUBAGENTS receive CLAUDE.md and the rules chain at DISPATCH, so a dispatched agent picks up config changes immediately; the MAIN THREAD does not until a new session. Agent bodies are likewise read at dispatch. So a config change is live for workers straight away and stale for the orchestrator until restart.

c3 IS NOT APPROVED. The finalized architecture was presented for approval and the user replied with a merge notification, which is not approval. c3 remains open.