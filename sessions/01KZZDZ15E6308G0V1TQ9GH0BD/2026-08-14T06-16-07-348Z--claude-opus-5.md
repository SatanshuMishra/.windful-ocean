c1 closed: the R3 bypassPermissions precondition is PROSE ONLY, and the precondition is additionally UNMET.

Method: static audit dispatched to codebase-analyst across the repo and live ~/.claude, then the two load-bearing claims re-verified directly by the orchestrator.

Enforcement (the c1 question): zero. A grep for the five doors named in ~/.claude/rules/common/hooks.md — --dangerously-skip-permissions, --permission-mode bypassPermissions, permissions.defaultMode, --settings inline JSON, --allow-dangerously-skip-permissions — returns no match anywhere in .claude/hooks, .claude/lib, .claude/workflows or .claude/settings.json (worktree copies excluded). The 30 permissions.deny entries cover hosted Supabase, gh pr create/merge, GitHub MCP PR-write tools and credential Read globs; none names a door. block-destructive-bash.sh classifies only Supabase-remote, gh pr merge/create/edit, dd/mkfs/sudo-rm, fork-bomb and credential-exfiltration shapes.

Precondition satisfaction (beyond what c1 asked, and the sharper finding): layer 0 has no config presence at all — no sandbox key in either the repo settings.json or live ~/.claude/settings.json. Layer 4's SessionStart heartbeat was never built; SessionStart registers seven hooks (dep-audit, superpowers-drift-check, graphify-provision, ledger-staleness-scan, superpowers-cmd-hook-heal, plugin-update-check, roster-index-gen) and none makes any layer 4 assertion. Its would-be predecessor session-config-drift-check.sh was deleted from main rather than wired in, resolving SPEC step 8 toward removal. Layer 1 exists only as a mitosis-run-scoped checkpoint ref (.claude/lib/mitosis/checkpoint.mjs); the rm-to-trash rewrite, the launchd APFS snapshot job and the reaper do not exist.

Consequence: SPEC:303 says bypassPermissions without the reversibility layer beneath it is not the design ratified there. Layers 0, 1 and 4 are absent, so the mode is outside the ratified design today — not merely ungated. c2's deny question is therefore load-bearing as the criterion predicted, but the route to measuring it now needs a ruling.

Scope note: two of the five doors are un-gateable in principle by in-repo machinery. A human typing the flag in their own terminal is parsed at CLI startup before any hook runs, so enforcement could only ever cover agent self-escalation.

Unrelated defect observed: mcp__ledger__reconcile fails with spawn git ENOENT. git is present at /opt/homebrew/bin/git (2.55.0); the ledger MCP server runs with a PATH that lacks /opt/homebrew/bin. Drift was not folded before this session's briefing.