SESSION OUTCOME: the thread's steps (1) and (2) evaporated mid-session — both salvaged fixes were landed by ANOTHER session as PR #11, merged 2026-07-28 20:57Z, while this thread was paused. Caught by re-fetching before opening a PR; was two steps from filing a duplicate. Net new work this session is the 0092 decision material and two corrections to the thread's own framing.

WHAT WAS DONE
1. Reproduced 0083's residue precisely before discovering PR #11. At the SAME commit 2c95405: main checkout 9/9 pass, clean worktree 7/9 (2 fail). Mechanism: the test derived its repo path from import.meta.url while the hook derives its second protected base by resolving ~/.claude/CLAUDE.md's symlink; those agree ONLY in the primary checkout. Clean-worktree full-suite baseline 1540/1538/2, matching the recorded receipt exactly. Failing names were the two REPO_CLAUDE assertions.
2. CORRECTION to the thread's framing: main was not merely red locally. CI on 2c95405 (run 30396831645, ubuntu-latest, no ~/.claude install at all) FAILED with those same two names, 1540/1538/2. Main's CI was red. CI is green at 2ef7bb7 and a229c9c on both test and security.
3. Applied the salvaged hermeticity fix in worktree .claude/worktrees/hermetic-guard-test and verified 9/9, invariant under HOME=/nonexistent and HOME=$(mktemp -d), full suite 1540/1540. Then discovered PR #11 and DISCARDED the work as superseded.
4. Assessed the LANDED test against the reviewer's findings. Confirmed protect-claude-config.sh is byte-identical between 2c95405 and a229c9c, so empirical probes of the hook remain valid. PR #11 is a strict superset of the salvaged patch and better: fileURLToPath over .pathname; describe-scoped fixture hooks; a runHook wrapper carrying payload context on failure; PROBE_FILES as a single source of truth; plus a second suite pinning the installed ~/.claude probes that skips cleanly when absent. Its ledger-lint regex is also tighter than the salvaged one — (?:^|[^.\w$])(?:globalThis\.)?process\.env admits only a globalThis prefix, where the salvaged lookbehind would have admitted any dot prefix, so foo.process.env.X would have wrongly counted as a real read.
5. Mapped 0092 to ground truth (codebase-analyst, read-only via git show against the object store because the main working tree is dirty). Independently corroborated the recorded diagnosis and added precision — see the decision record.

WHAT FAILED
- record_decision rejected three consecutive calls with ToolValidationError "(root) must have required property 'options'" despite options being supplied as a string array. Retried with progressively shorter payloads; all three failed identically. Suspected harness serialization of array-typed MCP params. The decision was written on the fourth attempt during wrap-up.
- git worktree add for a clean verification tree at a229c9c was DENIED by the sandbox classifier; git reset --hard on the superseded worktree was DENIED as destructive (correctly). Consequence: there is NO local full-suite run at a229c9c this session. The green claim rests on CI at a229c9c plus PR #11's self-reported 1544/1544 — not on an independent local run. Do not upgrade that to a measured local receipt without running it.

VERIFIED, NOT ASSERTED
Per the standing risk that a subagent committed while reporting otherwise, the implementer's "nothing committed, nothing pushed" was checked against the repository: zero commits on fix/hermetic-guard-test (tip still 2c95405), and no branch of mine on the remote. The remote fix/guard-hermeticity-and-lint-boundary at 00c8446 belongs to the OTHER session and became PR #11.

NEW FINDINGS NOT YET IN THE SPINE — three test-quality gaps that SURVIVE into the landed guard test. All pre-existing, none a regression from PR #11, all in .claude/hooks/tests/protect-claude-config.test.mjs:
(a) Layout drift. The new installed-topology suite pins only CLAUDE.md/settings.json/keybindings.json resolving out of the home tree. Nothing pins that workflows/mitosis.js or lib/superpowers-parallel/mitosis-git.mjs still sit at those relative paths. Rename .claude/workflows/ and every test stays green while the guard silently stops covering the real engine. That suite also skips in CI (no ~/.claude there), so it is a local-only check.
(b) The hook's per-candidate realpath branch, candidates.add(os.path.realpath(fp)), is asserted by nothing. The two "symlinked home path" tests match the home base LITERALLY and would pass against a plain directory, so five of the eight fixture symlinks gate no assertion. Distinguish this from base DISCOVERY via realpath, which IS covered and which PR #11's mutation check validates.
(c) The test named "a payload carrying no file path is ignored rather than guessed at" never reaches the branch it names: {"tool_input":{}} carries no .claude substring, so the bash prescreen at protect-claude-config.sh:3-6 exits before python3 starts.

ARTIFACTS LEFT (deletion unauthorized)
- Worktree .claude/worktrees/hermetic-guard-test on branch fix/hermetic-guard-test, NO commits, holding only superseded work. Safe to remove when authorized.
- Scratchpad backups: salvaged-backup.patch (sha256 d16a025b… matching the .git/salvaged original), superseded-my-hermeticity-work.diff, baseline-origin-main.txt.
Nothing is still running.