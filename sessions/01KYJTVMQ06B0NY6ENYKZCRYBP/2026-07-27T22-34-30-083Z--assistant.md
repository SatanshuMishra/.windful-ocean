Live GitHub state read for SatanshuMishra/.windful-ocean (read-only gh api, 2026-07-27), sharpening the Sections 2-5 human asks:

- Repo is PUBLIC and owned by a User (personal account), default branch `main`. Section 2's five-role org hierarchy therefore does NOT apply -- the runbook's personal-account carve-out does: any non-owner collaborator is effectively Write, and the non-admin requirement is met simply by the machine user not being the repo owner. No seat/billing concern (public, personal, Free).
- `gh api repos/:owner/:repo/rulesets` returns `[]` and `gh api repos/:owner/:repo/rules/branches/main` returns `[]`. There is NO ruleset and NO branch protection today. Section 4 is entirely unapplied, so the server-side merge boundary does not exist yet in any form.
- NEW RISK, not previously captured: `delete_branch_on_merge: false`. Runbook Section 5 (line 107) establishes that GitHub's auto-retarget fires on DELETION of the merged head branch, not on the merge itself. With this setting off, the retarget cascade never fires on this repo at all. The runbook itself calls this out as "a different, quieter failure mode worth checking for separately" -- now confirmed live. Section 5's merge-strategy decision must therefore be paired with a head-branch-deletion decision (repo setting, or the engine deleting each unit's integration branch after merge), or the stacked train silently leaves children pointed at a branch that still exists but receives no further commits.
- All three merge methods are enabled (`allow_merge_commit`, `allow_squash_merge`, `allow_rebase_merge` all true), so Section 5's choice is unconstrained by current repo config. Note the tension: observed practice on this repo is squash-merge, which the runbook's own table ranks worst for a stacked train (every child requires an explicit restack), while merge-commit carries zero restack cost.
- Semgrep pin state confirmed consistent with the standing risk despite origin/main's b2f45bb titled "re-vendor the p/default pin": `.semgrep/` holds only `canonicalize.py` and a 65-byte `p-default.sha256`. The pin remains HASH-ONLY; ruleset content was never vendored, so the next upstream drift still forces a from-scratch pragma re-adjudication with no diff to read.
- Residual gap on the Section 6 fix itself, not closeable in workflow YAML: if a child PR becomes conflicted after retarget, GitHub cannot build the merge ref and the `pull_request` run does not execute, leaving the stale green in place. Any automated merge gate must therefore check PR mergeability and check-run recency, not merely conclusion=success.

Shipped this session: PR #7, commit 9b3f28b on fix/ci-rerun-on-pr-retarget, applying Section 6 to security.yml and test.yml. Suite green, 1347 tests, 0 fail. Worktree at .claude/worktrees/ci-retarget-rerun. Test suite count is now 1347, not the 1217 recorded earlier in this thread.